# Hybrid-Rank Weight Changelog

Append-only record of every `HYBRID_WEIGHTS` + `MIN_COSINE_THRESHOLD` tuning event.

**Schema note (D-21):** columns below are the 14 D-21 fields; `sensitivity_metric` is always
`top1_flip_rate` (RESEARCH §A5 recommendation). `bge_reranker_active` flips `true` in Phase 3
EXP-06 when the bge-reranker model score replaces raw Vectorize cosine as the `rerank` input.

---

| date       | corpus_filename                    | corpus_size | corpus_split         | embedding_model               | weights (rerank/recency/type_match/scope_match) | threshold | F1_train | F1_validate | MRR_train | top1_train | sensitivity_pass_rate               | sensitivity_metric | bge_reranker_active | notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------- | ---------------------------------- | ----------- | -------------------- | ----------------------------- | ----------------------------------------------- | --------- | -------- | ----------- | --------- | ---------- | ----------------------------------- | ------------------ | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-08 | .planning/evals/recall-corpus.json | 100         | 70/30 train/validate | @cf/qwen/qwen3-embedding-0.6b | 0.6 / 0.05 / 0.10 / 0.05                        | 0.45      | 0.4476   | 0.4333      | 0.8481    | 0.7714     | 97.3% (top1_flip_rate=0.0268 < 30%) | top1_flip_rate     | false               | First v0.2 tune per Plan 02-03 (D-34 revised). Sweep recalibrated per D-34: RNK-06 gate changed from absolute 0.8254 (borrowed from production remember→recall pipeline, incompatible architecture) to "beat cosine-only baseline by ≥0.02 absolute F1". Cosine-only baseline F1=0.3381 (rerank=1.0, others=0, threshold=0.60); winner F1=0.4476; improvement_delta=0.1095 (gate passed). MIN_COSINE_THRESHOLD swept as 4th dimension [0.45,0.50,0.55,0.60]; winner threshold=0.45 — pre-fetch retains all topK=50 with no threshold during fetch (zero extra AI calls per D-34 budget rule). D-15 dual-corpus (27-entry real-corpus.json) skipped: budget 200 calls exhausted by main 100-entry sweep (same as prior run). 02-03a eval-design fix restored tunability: distinct F1=84 across 2500 configs (vs 7 before threshold sweep, 1 before 02-03a). Re-tune at v0.3 when corpus grows. |
