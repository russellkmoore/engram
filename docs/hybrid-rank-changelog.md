# Hybrid-Rank Weight Changelog

Append-only record of every `HYBRID_WEIGHTS` + `MIN_COSINE_THRESHOLD` tuning event.

**Schema note (D-21):** columns below are the 14 D-21 fields; `sensitivity_metric` is always
`top1_flip_rate` (RESEARCH §A5 recommendation). `bge_reranker_active` flips `true` in Phase 3
EXP-06 when the bge-reranker model score replaces raw Vectorize cosine as the `rerank` input.

---

| date       | corpus_filename                                                        | corpus_size                       | corpus_split         | embedding_model               | weights (rerank/recency/type_match/scope_match)                             | threshold                 | F1_train                      | F1_validate      | MRR_train        | top1_train       | sensitivity_pass_rate               | sensitivity_metric | bge_reranker_active | notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------- | ---------------------------------------------------------------------- | --------------------------------- | -------------------- | ----------------------------- | --------------------------------------------------------------------------- | ------------------------- | ----------------------------- | ---------------- | ---------------- | ---------------- | ----------------------------------- | ------------------ | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-08 | .planning/evals/recall-corpus.json                                     | 100                               | 70/30 train/validate | @cf/qwen/qwen3-embedding-0.6b | 0.6 / 0.05 / 0.10 / 0.05                                                    | 0.45                      | 0.4476                        | 0.4333           | 0.8481           | 0.7714           | 97.3% (top1_flip_rate=0.0268 < 30%) | top1_flip_rate     | false               | First v0.2 tune per Plan 02-03 (D-34 revised). Sweep recalibrated per D-34: RNK-06 gate changed from absolute 0.8254 (borrowed from production remember→recall pipeline, incompatible architecture) to "beat cosine-only baseline by ≥0.02 absolute F1". Cosine-only baseline F1=0.3381 (rerank=1.0, others=0, threshold=0.60); winner F1=0.4476; improvement_delta=0.1095 (gate passed). MIN_COSINE_THRESHOLD swept as 4th dimension [0.45,0.50,0.55,0.60]; winner threshold=0.45 — pre-fetch retains all topK=50 with no threshold during fetch (zero extra AI calls per D-34 budget rule). D-15 dual-corpus (27-entry real-corpus.json) skipped: budget 200 calls exhausted by main 100-entry sweep (same as prior run). 02-03a eval-design fix restored tunability: distinct F1=84 across 2500 configs (vs 7 before threshold sweep, 1 before 02-03a). Re-tune at v0.3 when corpus grows.                                                                                                                                                                                                                                       |
| 2026-06-08 | packages/mcp-server/src/**tests**/evals/fixtures/recall-corpus-v2.json | 60 (first 60 of 70 train entries) | train subset         | @cf/qwen/qwen3-embedding-0.6b | 0.6 / 0.05 / 0.10 / 0.05 (shipped, UNCHANGED)                               | 0.45 (shipped, UNCHANGED) | pending live run              | pending live run | pending live run | pending live run | N/A — ablation not yet run          | top1_flip_rate     | pending live run    | EXP-07 reranker weight ablation (Plan 03-04). D-EXP07 METRIC SUBSTITUTION: gate is precision@3/F1@3 (NOT precision@5) because the corpus labels expected_top_3_block_ids — exactly 3 gold ids per query; precision@5 caps at 3/5=0.6 against a 3-id gold set, making discrimination impossible. Gate threshold: RERANKER_IMPROVEMENT_MIN=0.03 (≥3% F1@3 delta, rerank-on vs rerank-off/raw-cosine). Budget: 60 queries × 3 calls (embed+Vectorize+reranker) = 180 calls ≤ MAX_AI_CALLS=200; full 70-query train split would require 210 calls (exceeds ceiling). Small-N caveat: 60/70 = 86% of train split. bge_reranker_active status: PENDING — ablation authored but deferred to nightly CI (CLOUDFLARE_API_TOKEN unavailable in authoring session). HYBRID_WEIGHTS.rerank left at shipped value 0.6 pending live run. Decision rule: if live run shows F1@3 delta ≥ 0.03 → keep HYBRID_WEIGHTS.rerank nonzero + set bge_reranker_active=true; if delta < 0.03 → set HYBRID_WEIGHTS.rerank=0.0 + bge_reranker_active=false. Eval file: packages/mcp-server/src/**tests**/evals/reranker-ablation.eval.test.ts (commit 044c797). |
| 2026-06-08 | packages/mcp-server/src/**tests**/evals/fixtures/recall-corpus-v2.json | 60 (first 60 of 70 train entries) | train subset         | @cf/qwen/qwen3-embedding-0.6b | 0.6 / 0.05 / 0.10 / 0.05 (UNCHANGED — reranker DISABLED, weight NOT zeroed) | 0.45 (UNCHANGED)          | 0.4556 (off/cosine = shipped) | N/A              | 0.8437 (off)     | 0.7667 (off)     | N/A                                 | top1_flip_rate     | false (DISABLED)    | EXP-07 ablation LIVE RUN (supersedes the pending row above). gate_passed=FALSE — bge-reranker decisively worse than raw cosine: F1@3(on)=0.2611 vs F1@3(off)=0.4556 (delta −0.1944 < +0.03 gate); MRR(on)=0.4824 vs (off)=0.8437; top1(on)=0.3500 vs (off)=0.7667; n=60 train queries. ACTION: added RERANKER_ENABLED=false in shared/ai-config/src/index.ts; gated the bge-reranker invocation off in tools.ts recall() — raw cosine fills the rerank slot via the `?? m.score` fallback at the tuned 0.6 weight (== ablation "off" arm). D-EXP07's literal "set HYBRID_WEIGHTS.rerank=0.0" was NOT followed: that weight multiplies the PRIMARY relevance signal (cosine when reranker inactive), so zeroing it would delete the cosine term and rank by recency/filters only. Disabling the invocation (not zeroing the weight) is the correct realization of the ablation outcome — confirmed with the user (AskUserQuestion 2026-06-08). EXP-06 wiring preserved behind the flag for a future larger-corpus re-tune. Bonus: removes one Workers AI round-trip per recall (EXP-11 latency).                                     |
