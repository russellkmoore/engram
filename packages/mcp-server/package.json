{
  "name": "@engram/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types:gen": "wrangler types",
    "test": "vitest run",
    "test:watch": "vitest",
    "pretest:eval": "node ../../scripts/sync-eval-corpus.mjs"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "0.7.0",
    "@engram/ai-config": "*",
    "@engram/schema": "*",
    "@engram/types": "*",
    "@engram/vectorize-utils": "*",
    "@engram/workspace-do": "*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.13.2",
    "gpt-tokenizer": "^3.4.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.9",
    "vitest": "^4.1.7"
  }
}
