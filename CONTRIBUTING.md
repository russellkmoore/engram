# Development Setup

This project uses GSD for Claude Code workflow management.

1. Install Claude Code
2. Install GSD plugin:
   /plugin marketplace add jnuyens/gsd-plugin
   /plugin install gsd@gsd-plugin
   /reload-plugins
3. Clone repo, open in VS Code, launch Claude Code
4. Run /gsd:map-codebase to get oriented
