---
phase: 04-core-tools-envelope
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts
  - packages/mcp-server/src/__tests__/token-budget.test.ts
autonomous: true
requirements: [TOL-07, MCP-08]
user_setup: []

must_haves:
  truths:
    - "`packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` body is LIVE (no longer RED scaffolding from Plan 04-01) and proves TOL-07 two-prong protection: Prong A (data-isolation via DO routing) + Prong B (active `assertOwnsWorkspace` regression lock per STO-07 / MT-1) — the BEHAVIORAL proof of T-04-WSF mitigation"
    - "Prong A exercises the LIVE Plan 04-03 handler bodies via `captureCallback(toolName, workspace_id)` — seeds `workspace_A` through the legitimate remember handler, then invokes recall with forged `props.workspace_id='workspace_B'` and asserts `result.memories === []` because `getAgentByName(env.WORKSPACE, 'workspace_B')` routes to an EMPTY DO (data isolation by routing — RESEARCH.md §Pattern 4)"
    - "Prong B mirrors the negative-path STO-07 test at `packages/workspace-do/src/__tests__/defense-in-depth.test.ts:180-228`: takes `stubA = env.WORKSPACE.get(env.WORKSPACE.idFromName('workspace_A'))`, calls `runInDurableObject(stubA, instance => instance.lexicalSearchBlocks({ workspace_id: 'workspace_B', query: ... }))`, asserts caught error is `instanceof McpError` with `code === ErrorCode.InvalidRequest` AND the message contains both `'workspace_A'` and `'workspace_B'` — the active-assertion regression lock for MT-1"
    - "`packages/mcp-server/src/__tests__/token-budget.test.ts` body is LIVE and proves MCP-08 via three test cases: (1) worst-case `recall()` envelope post-`trimToBudget` ≤ 7,500 cl100k_base tokens (D-09 + D-10); (2) each of 5 registered tool descriptions ≤ 1,500 UTF-8 bytes via `new TextEncoder().encode(desc).byteLength`; (3) negative-fixture sanity — the SAME worst-case envelope WITHOUT `trimToBudget` exceeds 8,000 tokens, proving the fixture is genuinely adversarial (not a sandbagged test)"
    - "Token-budget worst-case fixture matches the D-10 specification verbatim: 25 `LexicalSearchHit` rows × `content: 'x'.repeat(4000)` + `summary: 'y'.repeat(1000)` + fully populated metadata + `verbosity: 'synthesis'` envelope shape from `buildRecallResponse` (Plan 04-02)"
    - "Token-budget tool-description case uses the `captureRegistrations()` spy pattern from `packages/mcp-server/src/__tests__/tools.test.ts:67-84` — iterates ALL 5 registrations from `tools.ts`, asserts each `config.description` byte length ≤ 1,500 (per CLAUDE.md MCP Tool Surface description constraint + MCP-08)"
    - "Both test files use `cloudflare:test` / `cloudflare:workers` imports per the existing harness (pool=workers); no new vitest config / wrangler config / pool changes are required"
    - "No production code is modified in this plan — Plan 04-01 SCAFFOLDED these RED test files, Plan 04-02 shipped envelope.ts (incl. `trimToBudget`), Plan 04-03 shipped the live handler bodies. This plan turns the RED scaffolds GREEN by writing the assertion bodies that exercise the already-live production code"
    - "Full mcp-server vitest suite (`cd packages/mcp-server && npx vitest run`) exits 0 — TOL-07 + MCP-08 GREEN, no Wave 0/1/2 regression"
  artifacts:
    - path: "packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts"
      provides: "Live TOL-07 two-prong penetration test (data isolation by routing + active assertOwnsWorkspace regression lock)"
      contains: "runInDurableObject"
    - path: "packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts"
      provides: "Prong B mirror of defense-in-depth.test.ts:180-228 — assertOwnsWorkspace fires on forged workspace_id"
      contains: "assertOwnsWorkspace"
    - path: "packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts"
      provides: "Prong A exercises live Plan 04-03 handlers via captureCallback"
      contains: "captureCallback"
    - path: "packages/mcp-server/src/__tests__/token-budget.test.ts"
      provides: "MCP-08 worst-case recall envelope post-trim assertion via gpt-tokenizer cl100k_base"
      contains: "gpt-tokenizer/encoding/cl100k_base"
    - path: "packages/mcp-server/src/__tests__/token-budget.test.ts"
      provides: "MCP-08 ≤ 7,500 token cap assertion (D-10 safety margin under 8K)"
      contains: "<= 7500"
    - path: "packages/mcp-server/src/__tests__/token-budget.test.ts"
      provides: "MCP-08 tool-description ≤ 1,500 bytes assertion via TextEncoder (NOT Buffer.byteLength — per Anti-Patterns)"
      contains: "TextEncoder"
    - path: "packages/mcp-server/src/__tests__/token-budget.test.ts"
      provides: "Negative-fixture sanity: oversize envelope without trim exceeds 8,000 tokens (proves fixture is adversarial)"
      contains: "8000"
  key_links:
    - from: "packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts"
      to: "packages/mcp-server/src/tools.ts (Plan 04-03 live handlers)"
      via: "captureCallback(toolName, workspace_id) spy invokes the live remember/recall callbacks"
      pattern: "captureCallback"
    - from: "packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts"
      to: "packages/workspace-do/src/index.ts (assertOwnsWorkspace via lexicalSearchBlocks)"
      via: "runInDurableObject(stubA, instance => instance.lexicalSearchBlocks({ workspace_id: forged, ... }))"
      pattern: "runInDurableObject"
    - from: "packages/mcp-server/src/__tests__/token-budget.test.ts"
      to: "packages/mcp-server/src/envelope.ts (Plan 04-02 trimToBudget + buildRecallResponse)"
      via: "import { buildRecallResponse, trimToBudget } from '../envelope.js'"
      pattern: "from \"\\.\\./envelope"
    - from: "packages/mcp-server/src/__tests__/token-budget.test.ts"
      to: "gpt-tokenizer/encoding/cl100k_base"
      via: "encode(JSON.stringify(envelope)).length"
      pattern: "gpt-tokenizer/encoding/cl100k_base"
    - from: "packages/mcp-server/src/__tests__/token-budget.test.ts"
      to: "packages/mcp-server/src/tools.ts (description-size case iterates all 5 registrations)"
      via: "captureRegistrations() spy from tools.test.ts:67-84"
      pattern: "registerTool"
---

<objective>
Phase 4 Wave 3 — replace the Plan 04-01 RED scaffolds in `cross-workspace-pentest.test.ts` and `token-budget.test.ts` with LIVE assertion bodies that exercise the production code already shipped by Plans 04-02 (envelope.ts) and 04-03 (live tool handler bodies). This plan delivers the BEHAVIORAL proof of TOL-07 (cross-workspace isolation) and the resource-exhaustion safety verification for MCP-08 (8K-token + 1.5KB-description budgets).

Purpose: Plan 04-01 wrote these tests as RED scaffolds importing symbols that didn't exist yet — Plan 04-02 shipped `envelope.ts` with `buildRecallResponse` / `trimToBudget`, Plan 04-03 swapped the 5 `MethodNotFound` stubs in `tools.ts` for live handlers. This plan now writes the FULL test bodies that prove both threat-model mitigations actually work end-to-end: T-04-WSF (Workspace Forgery — Spoofing/EoP, mitigated by Plan 03's `assertOwnsWorkspace` + Plan 04-03's `props.workspace_id` derivation) and T-04-DOS (DoS via large response — mitigated by Plan 04-02's `trimToBudget` + Plan 04-01's schema-level `limit ≤ 25`).

Per RESEARCH.md §"Don't Hand-Roll": no inline envelope literals (use `buildRecallResponse`), no `Buffer.byteLength` (use `TextEncoder.encode().byteLength`), no barrel `gpt-tokenizer` import (use `gpt-tokenizer/encoding/cl100k_base`). Per RESEARCH.md §Pattern 4 / Pitfall 2: the two-pronged proof is INTENTIONAL — Prong A proves routing-by-DO-id isolation, Prong B proves the hard backstop fires.

Output: TWO modified test files (`cross-workspace-pentest.test.ts`, `token-budget.test.ts`). No production code edits. No new files. After this plan ships, only Plan 04-05 (TOL-08 local smoke + DEP-05 README amendment + Phase 5 hand-off note) remains before phase verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-tools-envelope/04-CONTEXT.md
@.planning/phases/04-core-tools-envelope/04-RESEARCH.md
@.planning/phases/04-core-tools-envelope/04-PATTERNS.md
@.planning/phases/04-core-tools-envelope/04-01-PLAN.md
@.planning/phases/04-core-tools-envelope/04-02-PLAN.md
@.planning/phases/04-core-tools-envelope/04-03-PLAN.md
@packages/mcp-server/src/tools.ts
@packages/mcp-server/src/envelope.ts
@packages/mcp-server/src/error-mapping.ts
@packages/mcp-server/src/schemas.ts
@packages/mcp-server/src/result-types.ts
@packages/mcp-server/src/__tests__/tools.test.ts
@packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts
@packages/mcp-server/src/__tests__/token-budget.test.ts
@packages/workspace-do/src/index.ts
@packages/workspace-do/src/types.ts
@packages/workspace-do/src/__tests__/defense-in-depth.test.ts
@packages/workspace-do/src/__tests__/helpers.test.ts
@shared/types/src/index.ts

<interfaces>
<!-- Plans 04-01..04-03 already shipped the upstream contracts. This plan -->
<!-- consumes them via tests; no new production interface lands here.   -->

From packages/mcp-server/src/envelope.ts (Plan 04-02 shipped — public surface):
```typescript
export function buildRecallResponse(
  input: { memories: LexicalSearchHit[]; verbosity: "synthesis" | "chunks" | "both" }
): EngramResponse<RecallResult>;

export function trimToBudget<T>(envelope: EngramResponse<T>): EngramResponse<T>;
// trimToBudget invariants (D-10):
//   1. Returns input UNCHANGED (referential identity) when countTokens(env) <= 7500.
//   2. Otherwise produces a NEW envelope (no input mutation).
//   3. Trim priority: drop result.memories[i].content → drop summary → drop trailing memories.
//   4. NEVER drops meta or context.conflicts. NEVER drops result.id on remember envelopes.

export const META_GAPS: { ... };
// Frozen strings per D-10 paragraph 3 (byte-determinism for MCP-08 fixtures).
```

From packages/mcp-server/src/tools.ts (Plan 04-03 shipped — live handlers, byte-stable registrations):
```typescript
export function registerTools(
  server: McpServer,
  getProps: () => EngramProps | undefined,
  env: Env
): void;
// Registers 5 tools: remember, recall, search, forget, ingest.
// Each callback derives workspace_id from getProps().workspace_id (NEVER from args).
// Each callback returns wrapMcpContent(trimToBudget(envelope)) on the happy path.
// Each callback throws via mapToMcpError(err) on failure (MCP-07).
```

From packages/workspace-do/src/index.ts (Phase 2 shipped — assertOwnsWorkspace per method):
```typescript
class WorkspaceDO {
  // assertOwnsWorkspace(args.workspace_id) is the FIRST line of every method.
  // Throws McpError(ErrorCode.InvalidRequest = -32600) on mismatch.
  // Message shape (defense-in-depth.test.ts:203-228 locks this):
  //   "Workspace mismatch: DO bound to 'X' but request claims 'Y'"
  insertBlock(args: { workspace_id: string; block: Memory }): void
  lexicalSearchBlocks(args: { workspace_id: string; query: string; limit?: number }): LexicalSearchHit[]
  deleteBlock(args: { workspace_id: string; id: string; cascade?: boolean }): { blocks_deleted: number; relations_deleted: number }
}
```

From packages/workspace-do/src/types.ts (Phase 2 shipped — LexicalSearchHit shape used by worst-case fixture):
```typescript
export interface LexicalSearchHit extends Memory {
  snippet: string | null;
  match_column: "content" | "summary" | null;
  score: number | null;
}
// Memory base fields (shared/types): id, type, content, summary, properties, embedding_id,
//                                    scope, project_id, source, confidence, created_at, updated_at
```

From packages/mcp-server/src/__tests__/tools.test.ts (Phase 3 / Plan 04-01 — captureRegistrations spy at lines 67-84):
```typescript
interface RegisteredToolCall {
  name: string;
  config: { description?: string; inputSchema?: unknown; ... };
  callback: (args: unknown, extra: unknown) => Promise<unknown>;
}
function captureRegistrations(): RegisteredToolCall[] {
  const spy = vi.spyOn(McpServer.prototype, "registerTool");
  try {
    const server = new McpServer({ name: "engram-mcp-server-test", version: "0.0.1" });
    registerTools(server, () => undefined, {} as unknown as Env);
    return spy.mock.calls.map((call) => { /* (name, config, callback) tuple */ });
  } finally { spy.mockRestore(); }
}
```

From Plan 04-01 04-PATTERNS.md §"tools-integration.test.ts" — the `captureCallback(toolName, workspace_id)` helper variant (returns ONE callback for invocation, NOT all 5 tuples):
```typescript
function captureCallback(toolName: string, workspace_id: string, user_id = "u-test") {
  const spy = vi.spyOn(McpServer.prototype, "registerTool");
  try {
    const server = new McpServer({ name: "test", version: "0.0.1" });
    registerTools(server, () => ({ workspace_id, user_id }), env as unknown as Env);
    const call = spy.mock.calls.find(([name]) => name === toolName);
    if (!call) throw new Error(`registration for '${toolName}' not captured`);
    return call[2] as (args: unknown, extra: unknown) => Promise<unknown>;
  } finally { spy.mockRestore(); }
}
```
Plan 04-01 placed `captureCallback` in `tools-integration.test.ts` as a top-of-file helper; Plan 04-04 may DUPLICATE this helper inline in `cross-workspace-pentest.test.ts` (test files do not share helpers across files in this repo by convention — each test file is self-contained). Do NOT extract to a shared `test-helpers.ts` module in this plan; that's a refactor outside Phase 4 scope.

From packages/workspace-do/src/__tests__/defense-in-depth.test.ts (Phase 2 shipped — Prong B analog at lines 180-228):
The negative-path STO-07 test pattern is:
```typescript
const id = env.WORKSPACE.idFromName("ws-actual");
await runInDurableObject(env.WORKSPACE.get(id), (instance) => {
  const ws = asWorkspaceDO(instance);   // type-coercion shim per the test file's helper
  let caught: unknown = undefined;
  try {
    ws.insertBlock({ workspace_id: "ws-claimed", block: ... });   // forged workspace_id arg
  } catch (err) {
    caught = err;
  }
  expect(caught).toBeInstanceOf(McpError);
  expect((caught as McpError).code).toBe(ErrorCode.InvalidRequest);
  const msg = (caught as McpError).message;
  expect(msg).toContain("Workspace mismatch");
  expect(msg).toContain("ws-actual");
  expect(msg).toContain("ws-claimed");
});
```
Prong B in this plan COPIES this shape — exchanging `insertBlock` for `lexicalSearchBlocks` (per RESEARCH.md §Pattern 4 example) and the workspace names for `workspace_A` / `workspace_B`.

From packages/mcp-server/src/result-types.ts (Plan 04-01 shipped):
- `RecallResult` = `{ memories: LexicalSearchHit[]; synthesis: string | null; chunks?: RecallChunk[] }`

The Plan 04-01 RED scaffold for `cross-workspace-pentest.test.ts` already declares both `it("TOL-07 Prong A: ...")` and `it("TOL-07 Prong B: ...")` `it()` blocks — this plan REPLACES their bodies with the live assertions. Confirm via Plan 04-01 acceptance criterion: "`grep -c 'Prong A\\|Prong B' cross-workspace-pentest.test.ts` returns `≥ 2`".

The Plan 04-01 RED scaffold for `token-budget.test.ts` already declares the three `it()` blocks per its acceptance criteria:
- `it("MCP-08: worst-case recall envelope (25 memories × 4KB content + 1KB summary) trims to ≤ 7,500 cl100k_base tokens")`
- `it("MCP-08: every registered tool description is ≤ 1,500 bytes (UTF-8)")`
- `it("MCP-08: trim drops content first, then summary, then trailing memories — meta and conflicts NEVER dropped")`

This plan REPLACES their bodies + ADDS one new negative-fixture-sanity case (the "adversarial proof" test that the worst-case envelope WITHOUT trim genuinely exceeds 8,000 tokens).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-01: Replace `cross-workspace-pentest.test.ts` Prong A body — TOL-07 data-isolation via DO routing through live handlers</name>
  <files>packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts</files>
  <read_first>
    - `packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` (full file — Plan 04-01 RED scaffold; this task REPLACES the Prong A body, preserving the `it("TOL-07 Prong A: ...")` outer frame, file JSDoc, and imports)
    - `packages/mcp-server/src/tools.ts` (Plan 04-03 live handlers — confirm `remember` and `recall` callbacks route via `await getAgentByName(env.WORKSPACE, props.workspace_id)` so forged props.workspace_id="workspace_B" lands the recall query on an EMPTY DO instance)
    - `packages/mcp-server/src/envelope.ts` (Plan 04-02 — `buildRecallResponse` shape; `result.memories` is the field Prong A asserts is `[]` after forgery)
    - `.planning/phases/04-core-tools-envelope/04-RESEARCH.md` §Pattern 4 lines 296-351 — canonical two-prong test shape. Prong A's mechanism: the handler routes by `getAgentByName(env.WORKSPACE, props.workspace_id)`, so forged props means it ADDRESSES A DIFFERENT DO; data isolation holds even if `assertOwnsWorkspace` didn't exist.
    - `.planning/phases/04-core-tools-envelope/04-PATTERNS.md` §"cross-workspace-pentest.test.ts" lines 528-545 — Prong A pattern + the `captureCallback` helper definition (which Plan 04-01 should have already placed at the top of this file as a top-of-file helper per Plan 04-01 Task 5 instructions)
    - `packages/mcp-server/src/__tests__/tools.test.ts` lines 67-84 — `captureRegistrations()` spy pattern (the model for `captureCallback`)
    - `packages/workspace-do/src/__tests__/helpers.test.ts` lines 21-30, 64-87 — `runInDurableObject` + `idFromName` round-trip pattern (Plan 04-04 Prong A may use these or may go through `captureCallback` only)
    - `packages/mcp-server/src/schemas.ts` — `RememberInputSchema` shape (the `content` field is required); `RecallInputSchema` shape (the `query` field is required; `verbosity` defaults to "both")
    - CONTEXT.md §"Claude's Discretion → Cross-workspace penetration test (TOL-07 / SC#9)" line 73 — two-workspace harness; STO-07's `assertOwnsWorkspace` guard MUST fire on direct DO call (Prong B); Prong A is the routing isolation surface
  </read_first>
  <action>
    Edit `packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts`. Plan 04-01 placed this file with a RED scaffold containing two `it()` blocks (Prong A + Prong B), file JSDoc, and imports. This task REPLACES the body of `it("TOL-07 Prong A: forged props.workspace_id routes to wrong DO — data isolation by routing")` with a live test. Do NOT modify the file JSDoc, the imports, or the Prong B `it()` block (Prong B is Task 04-02's scope).

    **Verify the `captureCallback` helper exists** at the top of the file. Per the Plan 04-01 task instructions (Task 5 step "**`packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts`** (NEW — TOL-07)" — the helper definition is documented as required for Prong A). If Plan 04-01 placed it inside the body of Prong A's RED `it()` block (not at file top), MOVE it to the top of the file (after imports, before the `describe` block). If it is missing entirely, ADD it now using the exact shape documented in the `<interfaces>` block above:

    ```text
    function captureCallback(toolName: string, workspace_id: string, user_id = "u-test"): (args: unknown, extra: unknown) => Promise<unknown> {
      // Spy on McpServer.prototype.registerTool, invoke registerTools with
      // a getProps callback returning { workspace_id, user_id }, and return
      // the captured callback for `toolName`.
    }
    ```
    The function MUST close over `env` (imported from `cloudflare:workers`) and pass it as the third arg to `registerTools(server, getProps, env as unknown as Env)`. This is the established Plan 04-01 pattern.

    **Replace the Prong A body** to perform the following sequence (per RESEARCH.md §Pattern 4 + PATTERNS.md §"cross-workspace-pentest.test.ts" Prong A lines 528-545):

    1. **Seed `workspace_A` via the legitimate handler path.** Obtain `rememberCb_A = captureCallback("remember", "workspace_A")`. Invoke `await rememberCb_A({ content: "secret in workspace A" }, {})`. Parse the returned content (the handler returns `{ content: [{ type: "text", text: JSON.stringify(envelope) }] }` per `wrapMcpContent`) — but for Prong A we only need the seed to succeed; assert the result has `.content` and the parsed envelope has a `result.id` (proves remember actually wrote a block via `stub.insertBlock` in the workspace_A DO).

    2. **Invoke `recall` with FORGED props.workspace_id = "workspace_B".** Obtain `recallCb_forged = captureCallback("recall", "workspace_B")`. Invoke `const result = await recallCb_forged({ query: "secret" }, {})`. Cast the result to `{ content: [{ type: "text"; text: string }] }` and parse the JSON.

    3. **Assert data isolation by routing.** The forged props caused the handler to call `await getAgentByName(env.WORKSPACE, "workspace_B")` — a DIFFERENT DO instance with no seeded data. Assert:
       - `envelope.result.memories` is an array.
       - `envelope.result.memories.length === 0` — the seeded "secret in workspace A" content is unreachable through the forged DO instance because the recall ran against the EMPTY workspace_B DO.
       - Add a comment above the assertion explaining: "Data isolation holds via getAgentByName(env.WORKSPACE, props.workspace_id) routing — even if assertOwnsWorkspace did not exist (Prong B), the data plane has already protected workspace_A's contents because the forged workspace_id pointed the handler at a different DO entirely."

    4. **Optional reinforcement assertion.** Also obtain `recallCb_legit = captureCallback("recall", "workspace_A")` and invoke `await recallCb_legit({ query: "secret" }, {})`. Parse the result. Assert `envelope.result.memories.length >= 1` — proves the seed actually landed in workspace_A (rules out the case where the test passes for the wrong reason because remember itself failed silently).

    **Important non-changes:**
    - Do NOT modify the file JSDoc, imports, or any `describe(...)` outer frame.
    - Do NOT touch the Prong B `it()` block (Task 04-02 handles it).
    - Do NOT add `runInDurableObject` to Prong A — Prong A's mechanism is routing-by-DO-id at the HANDLER layer, not direct DO method invocation. Mixing in `runInDurableObject` confuses the two prongs (the analog file at `packages/workspace-do/src/__tests__/helpers.test.ts:64-87` uses it for direct DO calls; Prong A here uses ONLY the live handler callbacks via `captureCallback`).
    - Do NOT use `args.workspace_id` anywhere — the production `tools.ts` defense-in-depth ban applies to ALL files; this test never passes a workspace_id through tool args.

    Reference: PATTERNS.md §"cross-workspace-pentest.test.ts" lines 528-545 shows the canonical Prong A skeleton. The two-pronged proof per RESEARCH.md §Pattern 4 last paragraph (lines 350-351): "(a) the handler's `getAgentByName(env.WORKSPACE, props.workspace_id)` ALONE prevents cross-workspace reads because it routes to a different DO instance (so data isolation holds even if `assertOwnsWorkspace` didn't exist) — that's the route-by-DO-id check. (b) The direct-DO-call test PROVES `assertOwnsWorkspace` fires for any caller that bypasses the handler and forges the workspace_id arg. Both must pass."
  </action>
  <verify>
    <automated>cd packages/mcp-server && npx vitest run src/__tests__/cross-workspace-pentest.test.ts -t "Prong A"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/cross-workspace-pentest.test.ts -t "Prong A"` exits 0 — Prong A passes GREEN.
    - The test file imports `cloudflare:workers` `env` (required for `captureCallback` to pass `env as unknown as Env` to `registerTools`) — `grep -c "from \"cloudflare:workers\"" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` returns `≥ 1`.
    - The test file contains the `captureCallback` helper at file scope (NOT inline inside the Prong A `it()` body) — `grep -c "function captureCallback" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` returns `1`. Use `awk` if needed to confirm the function declaration appears OUTSIDE any `describe(` block: `awk '/^function captureCallback/{print NR}' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` returns a line number that precedes the first `describe(` occurrence.
    - The Prong A body invokes the live `remember` handler — `awk '/it\(.*Prong A/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -c "captureCallback(\"remember\"" ` returns `≥ 1`.
    - The Prong A body invokes the live `recall` handler with forged props — `awk '/it\(.*Prong A/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -c "captureCallback(\"recall\"" ` returns `≥ 1` AND `awk '/it\(.*Prong A/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -c "workspace_B" ` returns `≥ 1`.
    - The Prong A body asserts `envelope.result.memories.length === 0` (data isolation) — `awk '/it\(.*Prong A/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -cE "(memories\\.length.*0|memories.*toEqual.*\\[\\])"` returns `≥ 1`.
    - The Prong A body does NOT use `runInDurableObject` (that's Prong B's mechanism) — `awk '/it\(.*Prong A/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -c "runInDurableObject"` returns `0`.
    - The Prong A body does NOT reference `args.workspace_id` (defense-in-depth invariant) — `awk '/it\(.*Prong A/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -v "^\\s*\\(//\\|\\*\\)" | grep -c "args.workspace_id"` returns `0`.
    - `npm run -ws --if-present typecheck` exits 0 — the test file typechecks (envelope JSON parsing typed correctly).
  </acceptance_criteria>
  <done>Prong A body live: seeds workspace_A via remember handler, invokes recall with forged props.workspace_id="workspace_B", asserts `result.memories === []` because the forged props routed the handler to an empty DO. T-04-WSF data-isolation mitigation behaviorally proven via routing.</done>
</task>

<task type="auto">
  <name>Task 04-02: Replace `cross-workspace-pentest.test.ts` Prong B body — TOL-07 active `assertOwnsWorkspace` regression lock mirroring defense-in-depth.test.ts:180-228</name>
  <files>packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts</files>
  <read_first>
    - `packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` (full file — post Task 04-01 state; Prong A is now GREEN, Prong B remains RED scaffold)
    - `packages/workspace-do/src/__tests__/defense-in-depth.test.ts` lines 1-100 (file JSDoc + helper functions including `asWorkspaceDO` type-coercion shim at lines 55-61)
    - `packages/workspace-do/src/__tests__/defense-in-depth.test.ts` lines 180-228 — the canonical NEGATIVE-path STO-07 assertion + message-shape lock-in pattern. Prong B in this plan MIRRORS this shape, exchanging `insertBlock`/`listMemoryTypes` for `lexicalSearchBlocks` (per RESEARCH.md §Pattern 4 example) and the workspace names for `workspace_A` / `workspace_B`.
    - `packages/workspace-do/src/index.ts` — confirm `lexicalSearchBlocks(args: { workspace_id, query, limit? })` runs `assertOwnsWorkspace(args.workspace_id)` as the first line (STO-07 invariant from Phase 2)
    - `.planning/phases/04-core-tools-envelope/04-RESEARCH.md` §Pattern 4 lines 336-351 — Prong B canonical shape using `runInDurableObject` + `expect.rejects.toMatchObject({ code: ErrorCode.InvalidRequest })` (note: per the defense-in-depth analog at lines 180-197, the SYNC throw pattern uses try/catch + caught assertion, NOT `.rejects.toThrow()` — see CRITICAL discrepancy note below)
    - `.planning/phases/04-core-tools-envelope/04-PATTERNS.md` §"cross-workspace-pentest.test.ts" lines 547-566 — Prong B pattern using try/catch + caught assertion (matches the defense-in-depth analog)
    - `packages/workspace-do/src/__tests__/defense-in-depth.test.ts` lines 36-41 — comment "Per D-01 the helpers (and therefore the DO methods) are synchronous. The `runInDurableObject` callback IS async, but `instance.method(...)` calls inside are NOT promises — we use a try/catch and `expect.fail()` on the no-throw path. Never use `.rejects.toThrow()` here." THIS IS A LOAD-BEARING DISCREPANCY VERSUS RESEARCH.md §Pattern 4 — the RESEARCH example uses `.rejects.toMatchObject(...)` which would NOT work because the DO methods are SYNC. Follow the defense-in-depth.test.ts shape (try/catch), NOT the RESEARCH.md `.rejects` shape.
    - `@modelcontextprotocol/sdk` `McpError` + `ErrorCode.InvalidRequest = -32600` — already imported in the RED scaffold per Plan 04-01
  </read_first>
  <action>
    Edit `packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts`. This task REPLACES the body of `it("TOL-07 Prong B: direct DO call with mismatched workspace_id throws McpError(InvalidRequest)")` with a live test mirroring `packages/workspace-do/src/__tests__/defense-in-depth.test.ts:180-228` (the negative-path STO-07 assertion + message-shape lock-in).

    **Type-coercion shim** — Prong B uses `runInDurableObject` with the `WorkspaceDO` class. The Plan 04-01 RED scaffold should already import `WorkspaceDO` as a type from `@engram/workspace-do`. Mirror the `asWorkspaceDO` shim from `defense-in-depth.test.ts:55-61` (a type-coercion helper that narrows `instance: unknown` to `WorkspaceDO`):

    ```text
    function asWorkspaceDO(instance: unknown): WorkspaceDO { return instance as WorkspaceDO; }
    ```

    Add this helper at file scope alongside `captureCallback` (after imports, before the `describe` block). If Plan 04-01 already placed it, leave it; if not, add it. The helper is documented in the defense-in-depth.test.ts file JSDoc as a "TS-level narrowing only" — runtime is unaffected.

    **CRITICAL — sync method invocation note** (per the defense-in-depth.test.ts file JSDoc at lines 36-41): the DO methods are SYNCHRONOUS. The `runInDurableObject` callback is `async`, but `instance.lexicalSearchBlocks(...)` is NOT a promise — it either returns a value synchronously or throws synchronously. Use try/catch inside the runInDurableObject callback; do NOT use `.rejects.toThrow()` or `await expect(...).rejects.toMatchObject(...)`. The RESEARCH.md §Pattern 4 example uses `.rejects` but that is WRONG for this codebase per the defense-in-depth analog — use the try/catch shape from `defense-in-depth.test.ts:180-228` instead.

    **Prong B body** — mirror `defense-in-depth.test.ts:180-228` (the dual negative tests: code assertion + message-shape lock-in). Combine into a single `it()` block per the Plan 04-01 RED scaffold. Sequence:

    1. `const id = env.WORKSPACE.idFromName("workspace_A");`
    2. `await runInDurableObject(env.WORKSPACE.get(id), (instance) => { ... });`
    3. Inside the callback:
       - `const ws = asWorkspaceDO(instance);`
       - `let caught: unknown = undefined;`
       - `try { ws.lexicalSearchBlocks({ workspace_id: "workspace_B", query: "anything" }); } catch (err) { caught = err; }` — the call is SYNC; the forged `workspace_id` triggers `assertOwnsWorkspace` to throw immediately.
       - `expect(caught).toBeInstanceOf(McpError);`
       - `expect((caught as McpError).code).toBe(ErrorCode.InvalidRequest);`
       - Message-shape lock-in assertions (mirror `defense-in-depth.test.ts:222-227`):
         - `const msg = (caught as McpError).message;`
         - `expect(msg).toContain("Workspace mismatch");`
         - `expect(msg).toContain("workspace_A");`
         - `expect(msg).toContain("workspace_B");`
    4. Add a comment above the try block: "Prong B (active assertOwnsWorkspace regression lock per MT-1 / STO-07): even if a caller bypasses the handler and forges the workspace_id arg on a DO method, assertOwnsWorkspace fires synchronously. Mirrors packages/workspace-do/src/__tests__/defense-in-depth.test.ts:180-228."

    **Important non-changes:**
    - Do NOT modify Task 04-01's Prong A body, the file JSDoc, the imports, the `captureCallback` helper, or the `describe(...)` outer frame.
    - Do NOT use `.rejects.toMatchObject(...)` — see the CRITICAL sync method invocation note above.
    - Do NOT use `lexicalSearchBlocks` with `await` — the method is SYNC.
    - Do NOT use the `instance` parameter directly as `WorkspaceDO` — use `asWorkspaceDO(instance)` for the type narrowing per the defense-in-depth analog pattern.
  </action>
  <verify>
    <automated>cd packages/mcp-server && npx vitest run src/__tests__/cross-workspace-pentest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/cross-workspace-pentest.test.ts` exits 0 — Prong A (from Task 04-01) AND Prong B (this task) both GREEN.
    - The Prong B body uses `runInDurableObject` — `awk '/it\(.*Prong B/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -c "runInDurableObject"` returns `≥ 1`.
    - The Prong B body uses `env.WORKSPACE.idFromName("workspace_A")` — `awk '/it\(.*Prong B/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -c "idFromName(\"workspace_A\")"` returns `≥ 1`.
    - The Prong B body calls `lexicalSearchBlocks` with a forged `workspace_id: "workspace_B"` — `awk '/it\(.*Prong B/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -c "workspace_id: \"workspace_B\""` returns `≥ 1`.
    - The Prong B body uses try/catch (NOT `.rejects.toThrow()` per the sync-method note) — `awk '/it\(.*Prong B/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -c "rejects.toThrow\\|rejects.toMatchObject"` returns `0` AND `awk '/it\(.*Prong B/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -c "catch (err)"` returns `≥ 1`.
    - The Prong B body asserts `instanceof McpError` AND `code === ErrorCode.InvalidRequest` AND the message contains both workspace ids — `awk '/it\(.*Prong B/,/^  }\);/' packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts | grep -c "toBeInstanceOf(McpError)"` returns `≥ 1` AND `grep -c "ErrorCode.InvalidRequest"` (file-wide) returns `≥ 1` AND the body contains `toContain("Workspace mismatch")`.
    - The `asWorkspaceDO` type-coercion shim is present at file scope — `grep -c "function asWorkspaceDO" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` returns `1`.
    - File-level invariants survive: `grep -c "function captureCallback" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` returns `1` (Task 04-01's helper still present); `grep -c "Prong A" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` returns `≥ 1` (Prong A test still present and named).
    - `npm run -ws --if-present typecheck` exits 0.
  </acceptance_criteria>
  <done>Prong B body live: takes `stubA = env.WORKSPACE.get(env.WORKSPACE.idFromName("workspace_A"))`, calls `lexicalSearchBlocks({ workspace_id: "workspace_B", query: ... })` via runInDurableObject, asserts McpError(InvalidRequest) thrown with message containing both workspace ids. T-04-WSF active-assertion regression lock for MT-1 / STO-07 behaviorally proven. TOL-07 fully GREEN.</done>
</task>

<task type="auto">
  <name>Task 04-03: Replace `token-budget.test.ts` worst-case `recall()` body — MCP-08 ≤ 7,500 cl100k_base token cap via Plan 04-02 trimToBudget</name>
  <files>packages/mcp-server/src/__tests__/token-budget.test.ts</files>
  <read_first>
    - `packages/mcp-server/src/__tests__/token-budget.test.ts` (full file — Plan 04-01 RED scaffold; this task REPLACES the worst-case `it()` body, preserving file JSDoc and imports)
    - `packages/mcp-server/src/envelope.ts` (Plan 04-02 live) — `buildRecallResponse({memories, verbosity})` signature; `trimToBudget<T>(envelope)` signature + behavior (returns input unchanged when under budget; otherwise produces a NEW envelope with memory fields dropped in priority order)
    - `.planning/phases/04-core-tools-envelope/04-CONTEXT.md` §D-10 lines 64-67 — exact trim priority order + 7,500 token cap + invariants (NEVER drop meta or context.conflicts)
    - `.planning/phases/04-core-tools-envelope/04-CONTEXT.md` §D-09 lines 60-62 — gpt-tokenizer rationale (cl100k_base over o200k_base for Claude-margin approximation)
    - `.planning/phases/04-core-tools-envelope/04-RESEARCH.md` §Pattern 3 lines 254-294 — `BUDGET = 7500`, the post-trim algorithm, worst-case sanity check (25 memories × 4KB content + 1KB summary)
    - `.planning/phases/04-core-tools-envelope/04-RESEARCH.md` §Pitfall 6 lines 430-438 — `gpt-tokenizer/encoding/cl100k_base` import path (NEVER the barrel default)
    - `.planning/phases/04-core-tools-envelope/04-PATTERNS.md` §"token-budget.test.ts" lines 592-628 — canonical MCP-08 test shape including the worst-case fixture builder + the description-size assertion (which lands in Task 04-04)
    - `packages/workspace-do/src/types.ts` — `LexicalSearchHit` fields (extends `Memory` + adds `snippet`, `match_column`, `score`)
    - `shared/types/src/index.ts` — `Memory` fields: `id, type, content, summary, properties, embedding_id, scope, project_id, source, confidence, created_at, updated_at`
  </read_first>
  <action>
    Edit `packages/mcp-server/src/__tests__/token-budget.test.ts`. Plan 04-01 placed this file with a RED scaffold containing the three `it()` blocks described in the Plan 04-01 acceptance criteria. This task REPLACES the body of the worst-case recall `it()` block, preserving file JSDoc, imports, and the other two `it()` blocks (Tasks 04-04 + 04-05 will handle them).

    **Verify imports** at the top of the file (Plan 04-01 should have placed these):
    - `import { encode } from "gpt-tokenizer/encoding/cl100k_base";` (NOT the barrel) — Pitfall 6.
    - `import { describe, it, expect, vi } from "vitest";`
    - `import { buildRecallResponse, trimToBudget } from "../envelope.js";`
    - `import type { LexicalSearchHit } from "@engram/workspace-do";` (type-only per `verbatimModuleSyntax`)
    - `import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";` (used by Task 04-04 description-size case — may already be imported by Plan 04-01)
    - `import { registerTools } from "../tools.js";` (used by Task 04-04)
    - `import { env } from "cloudflare:workers";` (used by Task 04-04 `registerTools(server, ..., env as unknown as Env)`)

    If any of the above are missing, add them now (this task touches the imports once for the whole file, simplifying Tasks 04-04 + 04-05).

    **Replace the body** of `it("MCP-08: worst-case recall envelope (25 memories × 4KB content + 1KB summary) trims to ≤ 7,500 cl100k_base tokens")` (or the equivalent test-name Plan 04-01 placed). Sequence:

    1. **Build the worst-case fixture** matching the D-10 + RESEARCH.md §Pattern 3 specification. Construct an array of 25 `LexicalSearchHit` rows. Each row MUST have every field populated to the worst-case size:
       - `id: \`blk-${i}\`` (`i` is the loop index, 0..24)
       - `type: "research_note"`
       - `content: "x".repeat(4000)` — 4KB content (the field `trimToBudget` Step 1 drops first)
       - `summary: "y".repeat(1000)` — 1KB summary (the field Step 2 drops next)
       - `properties: { foo: "bar".repeat(100) }` — non-null properties (envelope bloat; never trimmed per D-10)
       - `embedding_id: null`
       - `scope: "personal"` (literal, must match the Memory enum)
       - `project_id: \`proj-${i}\``
       - `source: "mcp:test"`
       - `confidence: 0.9`
       - `created_at: 1_700_000_000_000 + i` (deterministic timestamps; affects `meta.last_updated`)
       - `updated_at: 1_700_000_000_000 + i`
       - `snippet: "z".repeat(200)` (the LexicalSearchHit extension field — populated to make trim work harder)
       - `match_column: "content"` (literal — must match the union type)
       - `score: null` (D-07 — v0.1 unranked)

       Use `Array.from({length: 25}, (_, i) => ({ ... }))`. The `as const` assertion on `scope: "personal"` and `match_column: "content"` may be needed to satisfy the LexicalSearchHit type — let TypeScript guide.

    2. **Build the envelope** via the live builder: `const envelope = buildRecallResponse({ memories: worstCaseMemories, verbosity: "synthesis" });` — `verbosity: "synthesis"` is chosen because it OMITS the `chunks` field, giving the LEAST bloat. (The test asserts trim succeeds under the worst case; using `"both"` would add `chunks` and bloat further — adversarial fixture should use the minimum-bloat verbosity so the test isolates trim's effect on `memories[]` rather than chunks bloat. Future-proofs against verbosity-default flips.)

    3. **Sanity check the fixture is genuinely worst-case.** Before trim, compute `const rawTokens = encode(JSON.stringify(envelope)).length;` and assert `rawTokens > 7500` (the un-trimmed envelope MUST exceed the budget — otherwise the test would pass trivially). Add a comment: "Sanity check: the un-trimmed envelope MUST exceed the BUDGET ceiling — otherwise trimToBudget is not the load-bearing mitigation we are testing."

    4. **Trim and assert.** `const trimmed = trimToBudget(envelope);` then `const tokenCount = encode(JSON.stringify(trimmed)).length;` then `expect(tokenCount).toBeLessThanOrEqual(7500);` — the D-10 hard cap.

    5. **Assert D-10 invariants** on the trimmed envelope (NEVER drop meta or context.conflicts):
       - `expect(trimmed.meta).toBeDefined();`
       - `expect(trimmed.meta.gaps.length).toBeGreaterThan(0);` (META_GAPS.recall is non-empty per Plan 04-02)
       - `expect(trimmed.context.conflicts).toEqual([]);` (D-08 empty contract preserved through trim)
       - `expect(trimmed.context).toBeDefined();`

    **Important non-changes:**
    - Do NOT modify file JSDoc, imports (beyond the verification step above), or the other two `it()` blocks (Tasks 04-04 + 04-05).
    - Do NOT use the `gpt-tokenizer` barrel default import — Pitfall 6 requires the specific encoding path `gpt-tokenizer/encoding/cl100k_base`.
    - Do NOT use `Buffer.byteLength` or `Buffer.from(...).length` anywhere — Anti-Patterns + Pitfall 6 + repo convention.
    - Do NOT build the envelope as an inline literal — use `buildRecallResponse(...)` so the test exercises the LIVE Plan 04-02 builder; an inline literal would tautologically pass the trim assertion while not actually proving the production builder produces trim-safe envelopes.
  </action>
  <verify>
    <automated>cd packages/mcp-server && npx vitest run src/__tests__/token-budget.test.ts -t "worst-case"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/token-budget.test.ts -t "worst-case"` exits 0 — the worst-case recall envelope post-trim is ≤ 7,500 tokens.
    - `grep -c "gpt-tokenizer/encoding/cl100k_base" packages/mcp-server/src/__tests__/token-budget.test.ts` returns `≥ 1` — specific encoding import per Pitfall 6.
    - `grep -c "from \"gpt-tokenizer\"$" packages/mcp-server/src/__tests__/token-budget.test.ts` returns `0` — no barrel import.
    - `grep -c "Buffer.byteLength\\|Buffer.from" packages/mcp-server/src/__tests__/token-budget.test.ts` returns `0` — Anti-Patterns enforced.
    - The worst-case body invokes `buildRecallResponse` from `../envelope.js` — `awk '/it\(.*worst.case/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -c "buildRecallResponse(" ` returns `≥ 1`.
    - The worst-case body invokes `trimToBudget` from `../envelope.js` — `awk '/it\(.*worst.case/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -c "trimToBudget(" ` returns `≥ 1`.
    - The worst-case body asserts `<= 7500` — `awk '/it\(.*worst.case/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -cE "(toBeLessThanOrEqual\\(7500\\)|<= 7500)"` returns `≥ 1`.
    - The worst-case body constructs 25 memories — `awk '/it\(.*worst.case/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -cE "(length: 25|Array.from\\(.*25)"` returns `≥ 1`.
    - Each memory has `content: "x".repeat(4000)` (or equivalent 4KB content) — `awk '/it\(.*worst.case/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -cE "\"x\"\\.repeat\\(4000\\)|\"x\"\\.repeat\\(4_000\\)"` returns `≥ 1`.
    - Each memory has `summary: "y".repeat(1000)` (or equivalent 1KB summary) — `awk '/it\(.*worst.case/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -cE "\"y\"\\.repeat\\(1000\\)|\"y\"\\.repeat\\(1_000\\)"` returns `≥ 1`.
    - The body asserts `trimmed.meta` and `trimmed.context.conflicts` survive the trim — `awk '/it\(.*worst.case/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -cE "(trimmed\\.meta|trimmed\\.context\\.conflicts)"` returns `≥ 2`.
    - `npm run -ws --if-present typecheck` exits 0 — the LexicalSearchHit fixture rows typecheck.
  </acceptance_criteria>
  <done>Worst-case `recall()` post-trim test live: builds 25-memory × 4KB-content + 1KB-summary fixture via `buildRecallResponse`, asserts post-`trimToBudget` envelope ≤ 7,500 cl100k_base tokens, preserves meta + context.conflicts per D-10 invariants. T-04-DOS resource-exhaustion safety verification operational.</done>
</task>

<task type="auto">
  <name>Task 04-04: Add tool-description size assertion to `token-budget.test.ts` — MCP-08 ≤ 1,500 UTF-8 bytes per registration via TextEncoder</name>
  <files>packages/mcp-server/src/__tests__/token-budget.test.ts</files>
  <read_first>
    - `packages/mcp-server/src/__tests__/token-budget.test.ts` (post-Task-04-03 state — imports verified, worst-case body live; this task replaces the description-size `it()` body)
    - `packages/mcp-server/src/tools.ts` (Plan 04-03 live) — confirm `registerTools(server, getProps, env)` registers exactly 5 tools with byte-stable descriptions (Phase 3 D-05 invariant)
    - `packages/mcp-server/src/__tests__/tools.test.ts` lines 67-84 — `captureRegistrations()` spy pattern (the canonical model)
    - `.planning/REQUIREMENTS.md` MCP-08 — the description budget IS 1,500 bytes per registration
    - `.planning/phases/04-core-tools-envelope/04-RESEARCH.md` §"Common Pitfalls → Pitfall: Buffer.byteLength" lines 360-362 — `new TextEncoder().encode(description).byteLength` is the platform-native equivalent; `Buffer` is `node:` API; workerd has `nodejs_compat` but the repo posture is to use platform-native APIs
    - `.planning/phases/04-core-tools-envelope/04-PATTERNS.md` §"token-budget.test.ts" lines 616-628 — canonical description-size case
    - CLAUDE.md §"MCP Tool Surface" — descriptions are the CLI-visible surface MCP clients see; each must stay under the 1.5KB cap or fragments Claude's context window
  </read_first>
  <action>
    Edit `packages/mcp-server/src/__tests__/token-budget.test.ts`. This task REPLACES the body of `it("MCP-08: every registered tool description is ≤ 1,500 bytes (UTF-8)")` (or the equivalent test-name Plan 04-01 placed). The imports were verified in Task 04-03.

    **Inline `captureRegistrations` helper** OR call out to a shared one. Test files in this repo are self-contained (per the convention noted in Task 04-01); the `tools.test.ts` file already has its own `captureRegistrations`. For Plan 04-04 simplicity, INLINE a minimal version at file scope:

    ```text
    function captureToolRegistrations(): Array<{ name: string; description: string }> {
      const spy = vi.spyOn(McpServer.prototype, "registerTool");
      try {
        const server = new McpServer({ name: "test", version: "0.0.1" });
        registerTools(server, () => undefined, env as unknown as Env);
        return spy.mock.calls.map((call) => {
          const [name, config] = call as unknown as [string, { description?: string }];
          return { name, description: config.description ?? "" };
        });
      } finally {
        spy.mockRestore();
      }
    }
    ```

    (Naming it `captureToolRegistrations` distinguishes from the `tools.test.ts` `captureRegistrations` to avoid grep-collision in case both files are co-loaded by a future test-helper extract.)

    Add this helper at file scope alongside the other helpers (after imports, before the `describe` block).

    **Replace the body** of the description-size `it()` to:

    1. `const registrations = captureToolRegistrations();`
    2. `expect(registrations).toHaveLength(5);` — sanity check the spy captured exactly 5 tools.
    3. Iterate each registration and assert UTF-8 byte length ≤ 1,500:
       ```text
       for (const { name, description } of registrations) {
         const byteLength = new TextEncoder().encode(description).byteLength;
         expect(byteLength, `${name} description byte length`).toBeLessThanOrEqual(1500);
       }
       ```
       The error-message label (`${name} description byte length`) makes a failure surface WHICH tool's description blew the budget — important for diagnosis when CLAUDE.md descriptions get edited.

    4. Add a sanity assertion that each description is non-empty: `expect(description.length, ...).toBeGreaterThan(0);` — proves the test isn't passing because the descriptions are empty strings.

    **Important non-changes:**
    - Do NOT use `Buffer.byteLength(description, "utf8")` — Anti-Patterns + Pitfall: workerd-native posture requires `TextEncoder`.
    - Do NOT use `description.length` as the assertion target — that's a code-unit count (UTF-16), NOT a byte count. CLAUDE.md descriptions may contain em-dashes / non-ASCII chars; `description.length` would over-pass for ASCII-light strings AND under-fail for non-ASCII descriptions.
    - Do NOT touch the worst-case body (Task 04-03) or the file JSDoc / imports.
    - Do NOT touch the third `it()` block (Task 04-05 will handle the negative-fixture sanity case).
  </action>
  <verify>
    <automated>cd packages/mcp-server && npx vitest run src/__tests__/token-budget.test.ts -t "1,500 bytes\\|1500 bytes\\|tool description"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/token-budget.test.ts -t "tool description"` (or equivalent matcher) exits 0 — all 5 descriptions fit the 1.5KB budget.
    - The description-size body uses `TextEncoder` — `awk '/it\(.*description/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -c "TextEncoder"` returns `≥ 1`.
    - The description-size body does NOT use `Buffer.byteLength` or `Buffer.from(...).length` — `awk '/it\(.*description/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -c "Buffer\\."` returns `0`.
    - The description-size body asserts `<= 1500` — `awk '/it\(.*description/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -cE "(toBeLessThanOrEqual\\(1500\\)|toBeLessThanOrEqual\\(1_500\\)|<= 1500)"` returns `≥ 1`.
    - The description-size body iterates 5 registrations — `awk '/it\(.*description/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -cE "(toHaveLength\\(5\\)|registrations\\.length)"` returns `≥ 1`.
    - The `captureToolRegistrations` (or equivalent) helper exists at file scope — `grep -c "function captureToolRegistrations\\|function captureRegistrations" packages/mcp-server/src/__tests__/token-budget.test.ts` returns `≥ 1`.
    - The helper imports / uses `registerTools` from `../tools.js` — `grep -c "from \"../tools" packages/mcp-server/src/__tests__/token-budget.test.ts` returns `≥ 1`.
    - The helper passes `env as unknown as Env` to `registerTools` — `awk '/^function captureToolRegistrations/,/^}/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -c "env as unknown"` returns `≥ 1`.
    - `npm run -ws --if-present typecheck` exits 0.
  </acceptance_criteria>
  <done>Tool-description size assertion live: iterates all 5 registrations from `tools.ts`, asserts each `config.description` UTF-8 byte length ≤ 1,500 via `new TextEncoder().encode(desc).byteLength`. MCP-08 description budget verified.</done>
</task>

<task type="auto">
  <name>Task 04-05: Add negative-fixture sanity test to `token-budget.test.ts` — oversize envelope WITHOUT trimToBudget exceeds 8,000 tokens (proves fixture is adversarial)</name>
  <files>packages/mcp-server/src/__tests__/token-budget.test.ts</files>
  <read_first>
    - `packages/mcp-server/src/__tests__/token-budget.test.ts` (post-Task-04-04 state — both prior `it()` bodies live; this task EITHER replaces the third Plan-04-01 RED scaffold body OR adds a NEW `it()` block depending on which the scaffold provided)
    - `.planning/phases/04-core-tools-envelope/04-RESEARCH.md` §Pattern 3 lines 286-294 — worst-case sanity check: "25 memories × ~400 tokens/hit (4KB content + 1KB summary + metadata) ≈ 10K tokens raw"
    - `.planning/phases/04-core-tools-envelope/04-CONTEXT.md` §D-10 — the 8K-token MCP-08 ceiling; 7,500 is the trim target leaving margin
    - Plan 04-01 acceptance criterion: the third `it()` block in the RED scaffold tests "trim drops content first, then summary, then trailing memories — meta and conflicts NEVER dropped" — this task may EXTEND that block with the additional negative-fixture assertion OR add a fourth `it()` if the third has different content. Decision below.
  </read_first>
  <action>
    Edit `packages/mcp-server/src/__tests__/token-budget.test.ts`. Plan 04-01's third `it()` scaffold tests trim-priority invariants ("drops content first, then summary, then trailing memories — meta and conflicts NEVER dropped"). This task adds a NEW `it()` block — the "negative-fixture sanity proof" — and ALSO fills out the trim-priority `it()` body if it remains RED.

    **Decision tree** (inspect the current file before editing):

    **Branch A — the trim-priority `it()` already has a non-trivial body from Task 04-03 or some prior fill:** Leave it as-is. Just ADD the new negative-fixture sanity `it()` block.

    **Branch B — the trim-priority `it()` is still empty/RED:** Fill its body too. The trim-priority assertions are:
    - Construct a small in-budget envelope (e.g., 1 memory, short content); call `trimToBudget(env)`; assert `result === env` (referential identity short-circuit per Plan 04-02 Task 02-07 acceptance criterion).
    - Construct an over-budget envelope (reuse the Task 04-03 fixture builder if helpful, or build a smaller one — 10 memories × 4KB content); call `trimToBudget(env)`; assert `result !== env` (NEW envelope; no input mutation) AND assert `result.meta` is intact AND `result.context.conflicts` is intact AND `Array.isArray(result.result.memories)` is true AND `result.result.memories.length >= 1` (D-10 invariant: never drop all memories).

    **In BOTH branches, ADD a new `it()` block** (after the existing 2-3 blocks; in the same `describe(...)` if appropriate):

    ```text
    it("MCP-08: worst-case recall envelope WITHOUT trimToBudget exceeds 8,000 tokens — proves the fixture is adversarial", () => {
      // Build the SAME worst-case fixture as the "worst-case" test above
      // (25 memories × 4KB content + 1KB summary + populated metadata).
      // Assert the un-trimmed envelope EXCEEDS the MCP-08 8K-token ceiling.
      //
      // Purpose: this is the adversarial-proof test. If the fixture
      // does NOT exceed 8K tokens raw, then the "trims to ≤ 7,500"
      // assertion in the worst-case test passes trivially (trim never
      // had to do anything), and the entire MCP-08 guarantee is unverified.
      // This test locks the fixture's adversarial-ness as a regression check.
    });
    ```

    Inside the `it()`:
    1. Build the same `worstCaseMemories` array as in Task 04-03 (25 rows × `content: "x".repeat(4000)` + `summary: "y".repeat(1000)` + populated metadata). DUPLICATE the fixture inline (do NOT extract to a shared helper — keeps each `it()` block self-contained and grep-readable).
    2. `const envelope = buildRecallResponse({ memories: worstCaseMemories, verbosity: "synthesis" });` — same builder + verbosity as Task 04-03.
    3. `const rawTokens = encode(JSON.stringify(envelope)).length;`
    4. `expect(rawTokens).toBeGreaterThan(8000);` — proves the un-trimmed envelope exceeds the MCP-08 8K ceiling, confirming the fixture is genuinely adversarial.
    5. Add a comment: "If this assertion ever fails (rawTokens drops below 8K), the worst-case test above is no longer load-bearing — investigate whether `buildRecallResponse` output shape changed or whether the fixture sizes need scaling up."

    **Important non-changes:**
    - Do NOT delete or alter the Task 04-03 worst-case `it()` block or the Task 04-04 description-size `it()` block.
    - Do NOT extract the worst-case-fixture-builder to a top-of-file helper — duplicate it inline so each `it()` block's invariants are self-evident.
    - Do NOT modify file JSDoc or imports.
    - If Branch B applies (the trim-priority body is empty), use the algorithm in the "Decision tree → Branch B" section above. Per Plan 04-02 Task 02-07 acceptance criterion ("Worst-case fixture trims to ≤ 7,500 cl100k_base tokens; Plan 01 token-budget.test.ts MCP-08: trim drops content first... passes"), Plan 04-02's `trimToBudget` already implements the priority order; this test merely verifies it.
  </action>
  <verify>
    <automated>cd packages/mcp-server && npx vitest run src/__tests__/token-budget.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/token-budget.test.ts` exits 0 — all `it()` blocks GREEN (worst-case, description-size, trim-priority, negative-fixture-sanity).
    - The new negative-fixture `it()` block exists — `grep -c "adversarial\\|WITHOUT trimToBudget exceeds" packages/mcp-server/src/__tests__/token-budget.test.ts` returns `≥ 1`.
    - The new block asserts `> 8000` — `awk '/it\(.*WITHOUT trimToBudget\\|adversarial/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -cE "(toBeGreaterThan\\(8000\\)|toBeGreaterThan\\(8_000\\)|> 8000)"` returns `≥ 1`.
    - The new block constructs the worst-case fixture (same 25 × 4KB × 1KB shape as Task 04-03) — `awk '/it\(.*adversarial\\|WITHOUT trimToBudget/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -cE "(length: 25|Array.from\\(.*25)"` returns `≥ 1`.
    - The new block invokes `buildRecallResponse` from `../envelope.js` — `awk '/it\(.*adversarial\\|WITHOUT trimToBudget/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -c "buildRecallResponse(" ` returns `≥ 1`.
    - The new block does NOT invoke `trimToBudget` — `awk '/it\(.*adversarial\\|WITHOUT trimToBudget/,/^  }\);/' packages/mcp-server/src/__tests__/token-budget.test.ts | grep -c "trimToBudget"` returns `0`.
    - File still contains the Task 04-03 worst-case `it()` and Task 04-04 description-size `it()` (regression check): `grep -c "worst.case\\|tool description" packages/mcp-server/src/__tests__/token-budget.test.ts` returns `≥ 2`.
    - `npm run -ws --if-present typecheck` exits 0.
  </acceptance_criteria>
  <done>Negative-fixture sanity test live: builds the same 25 × 4KB × 1KB fixture, asserts the un-trimmed envelope > 8,000 cl100k_base tokens. Adversarial-fixture regression lock operational — the worst-case test (Task 04-03) is now verifiably load-bearing.</done>
</task>

<task type="auto">
  <name>Task 04-06: Run full mcp-server vitest suite — TOL-07 + MCP-08 GREEN; no Wave 0/1/2 regression</name>
  <files>packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts, packages/mcp-server/src/__tests__/token-budget.test.ts</files>
  <read_first>
    - `packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` (post-Tasks-04-01-and-04-02 state — both prongs live)
    - `packages/mcp-server/src/__tests__/token-budget.test.ts` (post-Tasks-04-03-04-04-04-05 state — all `it()` blocks live)
    - `packages/mcp-server/vitest.config.ts` — confirm pool=workers configured (no changes needed)
    - `.planning/phases/04-core-tools-envelope/04-VALIDATION.md` §"Per-Task Verification Map" (if present) — full-suite assertion for Phase 4 Wave 3 gate
    - Plan 04-01 + 04-02 + 04-03 SUMMARY files (when available) — confirm prior waves' tests are still expected to be GREEN
  </read_first>
  <action>
    Run the full `packages/mcp-server` vitest suite. This is a verification-only task — no test files are edited here. The acceptance criterion is "ALL mcp-server tests GREEN; ZERO regressions". If any test fails, investigate WHICH test and WHY before declaring this task done:

    1. `cd packages/mcp-server && npx vitest run` — runs the entire test directory under the workers pool.
    2. If exit code is 0, capture the test count and pass count from the vitest output for the Plan 04-04 SUMMARY.
    3. If any tests FAIL:
       - **TOL-07 prong failure** (`cross-workspace-pentest.test.ts` Prong A or B) → Tasks 04-01 / 04-02 incomplete. Re-read the failure message, fix the test body, re-run. Do NOT modify production code in this task; if production behavior is wrong, raise a checkpoint and stop.
       - **MCP-08 test failure** (`token-budget.test.ts` worst-case / description-size / negative-fixture / trim-priority) → Tasks 04-03 / 04-04 / 04-05 incomplete. Investigate; fix; re-run.
       - **Wave 0/1/2 regression** (any other test fails — `envelope.test.ts`, `tools-integration.test.ts`, `tools.test.ts`, `schemas.test.ts`, `error-mapping.test.ts`, `index.test.ts`, `oauth.test.ts`) → STOP. Plan 04-04 should NOT cause regressions because it only edits TWO test files. If a Wave 0/1/2 test fails, the most likely cause is the `cloudflare:test`/`cloudflare:workers` import surface being affected by something in Tasks 04-01..04-05 — e.g., a stale import, a test-helper collision, or a test isolation bug. Investigate and fix. If the regression cannot be explained by Plan 04-04 changes, raise it as a blocker for the user — DO NOT silently push through.
    4. Capture the final test count, pass count, and any skipped/todo tests for the SUMMARY artifact.

    Per CLAUDE.md `## Development Guidelines → Never Do This` + the Engram repo posture: a failing test is NEVER acceptable for "ship the contract Phase 5 inherits". A regression in a prior wave's test would mean the v0.1 contract is broken before Phase 5 ever sees it.

    If the suite is GREEN, also run a confidence check on the Phase 4 wave-0 production-code structural invariants (defense-in-depth + SENTINEL anchor + args.workspace_id ban):
    - `grep -c "SENTINEL-DD-RT-PHASE-03-TOOLS-TS" packages/mcp-server/src/tools.ts` returns `1`.
    - `grep -v '^\s*\(//\|\*\)' packages/mcp-server/src/tools.ts | grep -c "args.workspace_id"` returns `0`.
    - `grep -v '^\s*\(//\|\*\)' packages/mcp-server/src/schemas.ts | grep -c "workspace_id"` returns `0`.

    These should be GREEN from Plans 04-01 / 04-02 / 04-03; this task is just a final paranoia check before signing off Wave 3.
  </action>
  <verify>
    <automated>cd packages/mcp-server && npx vitest run && grep -c "SENTINEL-DD-RT-PHASE-03-TOOLS-TS" src/tools.ts | grep -q "^1$" && grep -v '^\s*\(//\|\*\)' src/tools.ts | grep -c "args.workspace_id" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/mcp-server && npx vitest run` exits 0 — entire mcp-server test suite GREEN.
    - vitest output shows at minimum the following test files passing (regression check + Wave 3 deliverables):
      - `src/__tests__/envelope.test.ts` (Wave 0/1 — TOL-06)
      - `src/__tests__/tools-integration.test.ts` (Wave 0/2 — TOL-01..05)
      - `src/__tests__/cross-workspace-pentest.test.ts` (Wave 0/3 — TOL-07) — BOTH Prong A and Prong B GREEN
      - `src/__tests__/token-budget.test.ts` (Wave 0/3 — MCP-08) — worst-case + description-size + negative-fixture-sanity + trim-priority all GREEN
      - `src/__tests__/tools.test.ts` (Wave 0/2 — happy-path + structural DD-RT)
      - `src/__tests__/schemas.test.ts` (Wave 0 — D-03 + D-10 schema diffs)
      - `src/__tests__/error-mapping.test.ts` (Wave 0 — MCP-07 + T-03-LEAK regression locks)
    - Total pass count from vitest output is ≥ the count Plan 04-03's SUMMARY recorded for the post-Plan-03 state (i.e., Plan 04-04 added 4-5 new passing assertions but no regressions).
    - `grep -c "SENTINEL-DD-RT-PHASE-03-TOOLS-TS" packages/mcp-server/src/tools.ts` returns `1` — Phase 3 DD-RT structural anchor survived all of Phase 4.
    - `grep -v '^\s*\(//\|\*\)' packages/mcp-server/src/tools.ts | grep -c "args.workspace_id"` returns `0` — defense-in-depth invariant survived.
    - `grep -v '^\s*\(//\|\*\)' packages/mcp-server/src/schemas.ts | grep -c "workspace_id"` returns `0` — schema-level invariant survived.
    - `npm run -ws --if-present typecheck` exits 0.
  </acceptance_criteria>
  <done>Full mcp-server vitest suite GREEN. TOL-07 + MCP-08 verified behaviorally. Wave 0/1/2 tests show zero regressions. Phase 4 production-code structural invariants (SENTINEL anchor + args.workspace_id ban + schema-level workspace_id ban) intact. Plan 04-04 ready for Plan 04-05 (smoke + docs).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → `EngramMcp` DO → `WorkspaceDO` | Forged `props.workspace_id` is the v0.1 primary cross-workspace attack surface. Two-layer defense: (1) handler derives from JWT props at the `EngramMcp` boundary (Plan 04-03); (2) `assertOwnsWorkspace` is the hard backstop inside every `WorkspaceDO` method (Phase 2 STO-07). This plan provides the BEHAVIORAL proof of both layers. |
| Tool response → MCP client → Claude context window | Oversize responses risk token-budget DoS / context-window flooding. Mitigation: schema-level `limit ≤ 25` (Plan 04-01) + post-`trimToBudget` (Plan 04-02). This plan VERIFIES the post-trim envelope stays under the MCP-08 7,500-token / 8K-token ceilings. |
| Tool description → MCP client tool catalog | A bloated description (> 1.5KB) consumes Claude's context disproportionately for low value. CLAUDE.md MCP Tool Surface caps descriptions at 1.5KB; this plan VERIFIES every Phase 4 description respects the cap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-WSF | Spoofing / Elevation of Privilege | Cross-workspace forgery via forged `props.workspace_id` | mitigate | Plan 04-04 Task 04-01 (Prong A — data-isolation by DO routing through live handlers, exercising Plan 04-03's `getAgentByName(env.WORKSPACE, props.workspace_id)` derivation) + Task 04-02 (Prong B — active `assertOwnsWorkspace` regression lock via direct DO call mirroring `defense-in-depth.test.ts:180-228`). The two-pronged proof is the BEHAVIORAL evidence that T-04-WSF cannot succeed at the v0.1 attack surface. |
| T-04-DOS | Denial of Service / Resource Exhaustion | Oversize tool response flooding MCP transport / Claude context | mitigate | Plan 04-04 Task 04-03 (worst-case `recall()` envelope post-`trimToBudget` ≤ 7,500 cl100k_base tokens — D-09 + D-10) + Task 04-04 (each of 5 tool descriptions ≤ 1,500 UTF-8 bytes) + Task 04-05 (adversarial-fixture sanity: un-trimmed envelope > 8,000 tokens, proves the worst-case test is load-bearing). The mitigation chain (schema `limit ≤ 25` from Plan 04-01 + `trimToBudget` from Plan 04-02) is BEHAVIORALLY proven by this plan. |
| T-04-WSF-MSG | Information Disclosure | `assertOwnsWorkspace` error message could leak workspace IDs to unauthorized callers | accept | Both workspace IDs MUST appear in the error message for debuggability (Phase 2 STO-07 message-shape contract — `defense-in-depth.test.ts:203-228` locks the wording verbatim). Acceptance rationale: the error fires only when a forged workspace_id is presented; the legitimate caller never sees this message; the forged caller already KNOWS both IDs (they crafted the forgery). No new information disclosure. Disposition matches Phase 2's accepted risk for `defense-in-depth.test.ts`. |
| T-04-RED-VS-LIVE | Tampering | Test files might be modified to silently skip the TOL-07 or MCP-08 invariants (e.g., changing `expect(...).toBeLessThanOrEqual(7500)` to a no-op) | mitigate | Plan 04-04 acceptance criteria assert `grep -c` counts on specific tokens (`gpt-tokenizer/encoding/cl100k_base`, `TextEncoder`, `runInDurableObject`, `<= 7500`, `> 8000`, `1500`) — a silent skip would fail the grep checks. Task 04-06 runs the full suite to guard against accidental test deletion. |
| T-04-DD-RT (carried forward) | Tampering / EoP | Production `tools.ts` defense-in-depth invariant (no `args.workspace_id` outside comments) | mitigate | Task 04-06 verifies `grep -v '^\s*\(//\|\*\)' packages/mcp-server/src/tools.ts \| grep -c "args.workspace_id"` returns `0` AND `grep -c "SENTINEL-DD-RT-PHASE-03-TOOLS-TS" packages/mcp-server/src/tools.ts` returns `1`. Both invariants survive Plan 04-04 unchanged (this plan touches no production code). |

</threat_model>

<verification>
After Task 04-06 completes:
- `cd packages/mcp-server && npx vitest run src/__tests__/cross-workspace-pentest.test.ts` exits 0 — TOL-07 Prong A + Prong B both GREEN.
- `cd packages/mcp-server && npx vitest run src/__tests__/token-budget.test.ts` exits 0 — MCP-08 worst-case + description-size + negative-fixture-sanity all GREEN.
- `cd packages/mcp-server && npx vitest run` (full suite) exits 0 — no Wave 0/1/2 regression.
- `grep -c "runInDurableObject" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` returns `≥ 1` — Prong B uses the correct DO-method invocation harness.
- `grep -c "captureCallback" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` returns `≥ 1` — Prong A uses the live-handler harness.
- `grep -c "gpt-tokenizer/encoding/cl100k_base" packages/mcp-server/src/__tests__/token-budget.test.ts` returns `≥ 1` — specific encoding import per Pitfall 6.
- `grep -c "TextEncoder" packages/mcp-server/src/__tests__/token-budget.test.ts` returns `≥ 1` — description-size uses workerd-native API.
- `grep -c "Buffer\." packages/mcp-server/src/__tests__/token-budget.test.ts` returns `0` — Anti-Patterns + Pitfall: no node:Buffer API.
- `grep -c "SENTINEL-DD-RT-PHASE-03-TOOLS-TS" packages/mcp-server/src/tools.ts` returns `1` — Phase 3 structural anchor survives.
- `grep -v '^\s*\(//\|\*\)' packages/mcp-server/src/tools.ts | grep -c "args.workspace_id"` returns `0` — defense-in-depth invariant survives.
- `npm run -ws --if-present typecheck` exits 0 across the workspace.
</verification>

<success_criteria>
- `packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` Prong A body invokes live `remember` + `recall` handlers via `captureCallback`, asserts forged `props.workspace_id="workspace_B"` produces `result.memories === []` (data isolation by routing — T-04-WSF Prong 1).
- `packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` Prong B body uses `runInDurableObject` to invoke `lexicalSearchBlocks({ workspace_id: "workspace_B", ... })` on the `workspace_A` DO instance; asserts caught error is `McpError(ErrorCode.InvalidRequest)` with message containing both `"workspace_A"` and `"workspace_B"` (mirroring `defense-in-depth.test.ts:180-228` — T-04-WSF Prong 2; STO-07 / MT-1 active-assertion regression lock).
- `packages/mcp-server/src/__tests__/token-budget.test.ts` worst-case test builds 25 × 4KB-content × 1KB-summary memories via `Array.from`, calls `buildRecallResponse({ memories, verbosity: "synthesis" })` then `trimToBudget`, asserts `encode(JSON.stringify(trimmed)).length <= 7500` and preserves `meta` + `context.conflicts`.
- `packages/mcp-server/src/__tests__/token-budget.test.ts` description-size test iterates ALL 5 registrations from `tools.ts` via `captureToolRegistrations` (or equivalent) spy, asserts each `config.description` UTF-8 byte length (via `new TextEncoder().encode(desc).byteLength`) ≤ 1,500.
- `packages/mcp-server/src/__tests__/token-budget.test.ts` negative-fixture-sanity test builds the same worst-case envelope, asserts the un-trimmed JSON-stringified bytes exceed 8,000 cl100k_base tokens — proves the worst-case fixture is genuinely adversarial.
- Full `cd packages/mcp-server && npx vitest run` exits 0 with no regressions in Wave 0/1/2 tests.
- Production-code structural invariants survive Plan 04-04 (this plan touched no production files, only test files): SENTINEL anchor present, `args.workspace_id` ban intact in both `tools.ts` and `schemas.ts`.
</success_criteria>

<output>
Create `.planning/phases/04-core-tools-envelope/04-04-SUMMARY.md` documenting:
- Prong A live: which `captureCallback` invocations exercise which handlers; the data-isolation assertion mechanism (routing-by-DO-id via `getAgentByName`).
- Prong B live: `runInDurableObject(stubA, instance => instance.lexicalSearchBlocks({ workspace_id: "workspace_B", ... }))` shape; mirror of `defense-in-depth.test.ts:180-228`; message-shape lock-in assertions.
- Worst-case token-budget test fixture: exact dimensions (25 × 4KB × 1KB), builder used (`buildRecallResponse` with `verbosity: "synthesis"`), trimmed token count observed, assertion threshold (`<= 7500`).
- Tool-description size assertion: 5 registrations iterated, `TextEncoder().encode(desc).byteLength` mechanism, max observed byte length for the largest description.
- Negative-fixture-sanity test: un-trimmed token count observed (should be > 8,000) — proves fixture is adversarial.
- Full-suite test count before and after Plan 04-04 (added test count should be 4-5: Prong A + Prong B + worst-case + description-size + negative-fixture; trim-priority may have been added in a prior task).
- Threat-model coverage: T-04-WSF behaviorally proven (both prongs); T-04-DOS behaviorally proven (token + description budgets); T-04-RED-VS-LIVE mitigated by grep-count acceptance criteria.
- Hand-off note to Plan 04-05: Wave 3 GREEN; Plan 04-05 is the final wave (TOL-08 local smoke + DEP-05 README amendment + Phase 5 hand-off note).
</output>
</content>
</invoke>