---
phase: 01-foundation
plan: 06
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05"]
files_modified:
  - .github/workflows/ci.yml
  - README.md
  - docs/architecture.svg
  - scripts/setup-dev.sh
autonomous: false
requirements: [FND-01, FND-03, FND-08]
requirements_addressed: [FND-01, FND-03, FND-08]
tags: [ci, github-actions, readme, portfolio, smoke-tests, setup-script]

must_haves:
  truths:
    - "GitHub Actions CI workflow runs on every push to main and every PR; lint + typecheck + format-check + lint:wrangler all gate the merge."
    - "README.md presents elevator pitch, architecture diagram (Mermaid + SVG), tech stack table, status, install/dev quickstart, and links to CLAUDE.md — portfolio quality."
    - "Top of README has three badges only: Apache-2.0 license, GitHub Actions CI status, package.json version."
    - "`scripts/setup-dev.sh` is retired (deleted) OR thinly shimmed to invoke `npm run setup`; no duplicated logic."
    - "Phase 1 closes with a human-verified portfolio review of the README on GitHub."
    - "D-12: CI provider is GitHub Actions — .github/workflows/ci.yml runs lint + typecheck + lint:wrangler + format:check on push and PR."
    - "D-16: CONTRIBUTING.md left at its current 10-line GSD-setup-only state — full contributor guidance deferred to v1.0; acceptance criteria asserts 'git diff --quiet CONTRIBUTING.md'."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "CI pipeline: checkout, setup-node, npm ci, types:gen, typecheck, lint, format:check, lint:wrangler"
      contains: "lint:wrangler"
    - path: README.md
      provides: "Portfolio-quality project README (D-13/D-14/D-15)"
      min_lines: 80
      contains: "Engram"
    - path: docs/architecture.svg
      provides: "Polished hero/social-share architecture diagram (D-14)"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/lint-wrangler.mjs
      via: "CI step `npm run lint:wrangler` invokes the script (durable gate against `new_classes` regressions per D-03)"
      pattern: 'lint:wrangler'
    - from: README.md
      to: CLAUDE.md
      via: "README links to CLAUDE.md for architectural depth (D-13 section 7)"
      pattern: 'CLAUDE\\.md'
    - from: README.md
      to: docs/architecture.svg
      via: "README references the SVG for the hero/social-share image (D-14)"
      pattern: 'docs/architecture\\.svg'
---

<objective>
Land the GitHub Actions CI workflow, the portfolio-quality README (with Mermaid diagram and three badges), the rendered SVG hero image, and decide the fate of the existing `scripts/setup-dev.sh` (recommended: fold into `npm run setup` and delete the standalone bash script). Close the phase with a single human-verify checkpoint for README portfolio quality (per `01-VALIDATION.md` §"Manual-Only Verifications").

Purpose: This is the wiring plan — every prior plan produced an artifact (configs, scripts, packages); this plan connects them into the durable CI gate (D-03), publishes the public-facing front door (D-13/14/15), and closes out the FND-01/FND-03/FND-08 acceptance loop by running the smoke + lint scripts inside CI for the first time. After this plan, the v0.1 README is what visitors see on GitHub before the v0.4 demo lands.

Output: 3 files created (CI workflow, README, SVG diagram) + 1 file removed or rewritten (`scripts/setup-dev.sh`). One blocking human-verify checkpoint for portfolio quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@CLAUDE.md
@CONTRIBUTING.md
@scripts/setup-dev.sh
@scripts/lint-wrangler.mjs
@scripts/smoke-install.sh
@scripts/smoke-wrangler-dev.sh
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create .github/workflows/ci.yml (Pattern 10 verbatim + smoke jobs)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §"Pattern 10" (verbatim CI workflow — lines 720-761)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Don't Hand-Roll" (actions/setup-node@v4 with .nvmrc + cache: npm)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Validation Architecture" §"Sampling Rate" (CI runs full suite green before /gsd:verify-work)
    - .planning/phases/01-foundation/01-PATTERNS.md §"Pattern Assignments → .github/workflows/ci.yml"
    - .planning/phases/01-foundation/01-VALIDATION.md §"Sampling Rate" + §"Wave 0 Requirements"
    - .planning/phases/01-foundation/01-CONTEXT.md §"Decisions" D-03 (CI is durable gate), D-12 (GitHub Actions chosen)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` matching RESEARCH §Pattern 10 verbatim, extended with three additional steps that wire in the smoke scripts from Plan 02. Required step order: `checkout` → `setup-node` → `npm ci` → `npm run types:gen` → `npm run typecheck` → `npm run lint` → `npm run format:check` → `npm run lint:wrangler` → `bash scripts/smoke-wrangler-dev.sh packages/mcp-server/wrangler.jsonc` → `bash scripts/smoke-wrangler-dev.sh packages/triage-worker/wrangler.jsonc` → `bash scripts/smoke-install.sh`.

    Workflow shape:
    - Name: `CI`
    - Triggers: `on.push.branches: [main]` and `on.pull_request.branches: [main]` (D-03 durable gate)
    - Single job `build` on `runs-on: ubuntu-latest`
    - `actions/checkout@v4`
    - `actions/setup-node@v4` with `node-version-file: .nvmrc` (so CI uses Node 22 from `.nvmrc` Plan 01 set) AND `cache: npm`

    LOCKED INVARIANTS:
    - `types:gen` step MUST precede `typecheck` (Pitfall 7: `worker-configuration.d.ts` must exist before `tsc` runs, otherwise typecheck fails on missing `Env`)
    - `smoke-install.sh` runs LAST because it `rm -rf node_modules && npm install` from scratch (~30s) and would invalidate the `npm ci` cache used by earlier steps
    - Both Worker smokes referenced explicitly: `packages/mcp-server/wrangler.jsonc` and `packages/triage-worker/wrangler.jsonc`
    - Step names should be descriptive (e.g., `Lint wrangler.jsonc (FND-08)`, `Smoke: wrangler dev (mcp-server)`)
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml &amp;&amp; grep -q 'lint:wrangler' .github/workflows/ci.yml &amp;&amp; grep -q 'smoke-wrangler-dev.sh' .github/workflows/ci.yml &amp;&amp; grep -q 'actions/setup-node@v4' .github/workflows/ci.yml &amp;&amp; grep -q 'node-version-file: .nvmrc' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - Workflow triggers on push to main AND pull_request to main: `grep -E 'branches:\\s*\\[main\\]' .github/workflows/ci.yml`
    - Uses `actions/checkout@v4` and `actions/setup-node@v4`: `grep -q 'actions/checkout@v4' .github/workflows/ci.yml && grep -q 'actions/setup-node@v4' .github/workflows/ci.yml`
    - Node version sourced from `.nvmrc`: `grep -q 'node-version-file: .nvmrc' .github/workflows/ci.yml`
    - npm cache enabled: `grep -q 'cache: npm' .github/workflows/ci.yml`
    - All required steps present: `for step in 'npm ci' 'types:gen' 'typecheck' 'npm run lint' 'format:check' 'lint:wrangler' 'smoke-wrangler-dev.sh' 'smoke-install.sh'; do grep -q "$step" .github/workflows/ci.yml || (echo "missing CI step: $step" && exit 1); done`
    - `types:gen` step appears BEFORE `typecheck` (Pitfall 7): `awk '/types:gen/{tg=NR} /run: npm run typecheck/{tc=NR} END{exit(tg<tc?0:1)}' .github/workflows/ci.yml`
    - `smoke-install.sh` referenced AFTER `lint:wrangler` to preserve install cache: `awk '/lint:wrangler/{lw=NR} /smoke-install.sh/{si=NR} END{exit(lw<si?0:1)}' .github/workflows/ci.yml`
    - Both Worker smokes referenced: `grep -q 'packages/mcp-server/wrangler.jsonc' .github/workflows/ci.yml && grep -q 'packages/triage-worker/wrangler.jsonc' .github/workflows/ci.yml`
  </acceptance_criteria>
  <done>CI workflow exists with all required steps in the correct order; references both smoke scripts and FND-08 lint; triggers on push to main and PRs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Resolve scripts/setup-dev.sh (delete or thin shim per RESEARCH Q1)</name>
  <files>scripts/setup-dev.sh, package.json</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §"Open Questions" Q1 (recommendation: fold; lines 922-925)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Assumptions Log" A4 (planner picks fold-or-shim)
    - .planning/phases/01-foundation/01-PATTERNS.md §"Pattern Assignments → scripts/setup-dev.sh (EXISTING — MODIFY OR DELETE)"
    - scripts/setup-dev.sh (existing 4-line file — only contains GSD-install echo lines)
    - CONTRIBUTING.md (DO NOT TOUCH per D-16 — already covers GSD install steps)
    - package.json (already has `"setup": "npm install && npm run types:gen"` from Plan 01)
  </read_first>
  <action>
    Apply RESEARCH §Open Questions Q1 recommendation: **retire (delete)** `scripts/setup-dev.sh`. Rationale per PATTERNS.md: its 4 lines of `echo` content already live in `CONTRIBUTING.md` (kept as-is per D-16) and the root `package.json` `setup` script already runs `npm install && npm run types:gen` (Pattern 5). Keeping it would duplicate the GSD-reminder logic, violating the "no duplicate logic" constraint in PATTERNS §setup-dev.sh.

    Steps:
    1. Run `git ls-files scripts/setup-dev.sh` to check if tracked. If tracked, use `git rm scripts/setup-dev.sh`; otherwise plain `rm scripts/setup-dev.sh`.
    2. Optional cosmetic enhancement to root `package.json` `setup` script: prepend a one-line stdout reminder so users see "Engram setup — see CONTRIBUTING.md for GSD plugin install steps." before `npm install` runs. Concretely: change `"setup": "npm install && npm run types:gen"` to `"setup": "node -e \"console.log('Engram setup: see CONTRIBUTING.md for GSD plugin install steps.')\" && npm install && npm run types:gen"`. CONTRIBUTING.md remains the source-of-truth for the actual install steps; we only echo a pointer.
    3. Verify: after the delete + optional script tweak, run `npm run setup` and confirm it prints the reminder line (if added), then runs `npm install` and `npm run types:gen` to completion.

    Fallback (if deletion would break an existing documented external link): keep `scripts/setup-dev.sh` as a thin shim — replace its content with exactly:
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    exec npm run setup
    ```
    Document the deviation in this plan's SUMMARY. The shim MUST NOT contain any logic that duplicates `npm run setup` (no GSD-reminder echoes — those would now live in two places).

    CONSTRAINTS:
    - DO NOT TOUCH `CONTRIBUTING.md` (D-16).
    - DO NOT create new dev-bootstrap scripts.
    - DO NOT add logic to the shim (if shim is used).
  </action>
  <verify>
    <automated>(test ! -f scripts/setup-dev.sh) || (head -3 scripts/setup-dev.sh | grep -q 'exec npm run setup' &amp;&amp; test $(grep -cvE '^[[:space:]]*#|^[[:space:]]*$' scripts/setup-dev.sh) -le 3)</automated>
  </verify>
  <acceptance_criteria>
    - Either `scripts/setup-dev.sh` is deleted (`test ! -f scripts/setup-dev.sh`) OR exists as a thin shim with body that does ONLY `exec npm run setup` (≤ 3 non-comment, non-blank lines): `head -5 scripts/setup-dev.sh | grep -q 'exec npm run setup'`
    - Root `package.json` still has a working `setup` script: `node -e "const p=require('./package.json'); process.exit(p.scripts.setup ? 0 : 1)"`
    - `CONTRIBUTING.md` is unchanged from pre-plan state: `git diff --quiet CONTRIBUTING.md` exits 0
    - No GSD-reminder logic duplicated outside CONTRIBUTING.md and the optional one-line echo in `package.json` `setup` script: if the file is a shim, `! grep -q 'plugin marketplace add\\|/plugin install\\|reload-plugins' scripts/setup-dev.sh`
    - `npm run setup` exits 0 (full setup loop works end-to-end): run after this task and confirm exit code
  </acceptance_criteria>
  <done>`scripts/setup-dev.sh` is either deleted or replaced with a minimal `exec npm run setup` shim; CONTRIBUTING.md untouched; `npm run setup` runs successfully.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create README.md (portfolio quality per D-13/14/15) + docs/architecture.svg</name>
  <files>README.md, docs/architecture.svg</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §"System Architecture Diagram" (Mermaid `flowchart LR` block — source-of-truth for README diagram + SVG render, lines 194-242)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Don't Hand-Roll" (Mermaid in README — GitHub renders natively, diffs in PRs)
    - .planning/phases/01-foundation/01-PATTERNS.md §"Pattern Assignments → README.md" + §"docs/architecture.svg"
    - .planning/phases/01-foundation/01-CONTEXT.md §"Decisions" D-13 (portfolio quality, 7 sections), D-14 (Mermaid in README + SVG in docs/), D-15 (3 badges only: license, CI, version)
    - CLAUDE.md §"What Is Engram" + §"Core Design Principle" + §"Tech Stack" + §"Milestones" (post-FND-07 amended state from Plan 03 — source material for README copy)
    - package.json (from Plan 01 — for version badge)
  </read_first>
  <action>
    Create two files:

    **`README.md`** — portfolio-quality README per D-13/14/15. Required 10-section structure (target ~100-150 lines total):

    1. **Top badges row** — exactly THREE badges per D-15:
       - Apache-2.0 license badge linking to `LICENSE`
       - GitHub Actions CI status badge — owner/repo extracted from `git remote get-url origin`. If repo not yet pushed, use placeholder owner `russellkmoore` (the user from CLAUDE.md context) with a `<!-- TODO: confirm owner after first push -->` HTML comment
       - Version badge — read version from `package.json` (`0.1.0`), display as `0.1.0-alpha`
       NO additional badges (no coverage, no npm-publish, no deploy badges — D-15 rationale: "broken badges look worse than missing badges on a portfolio repo")

    2. **Title + tagline** — `# Engram` heading and a single-sentence tagline. Derive from CLAUDE.md §"What Is Engram" (e.g., "Open-source, MCP-native second brain for AI assistants — persistent memory that the user owns and any MCP client can query.")

    3. **Elevator pitch** (1 paragraph) — Why Engram exists: the no-persistent-memory problem AI clients face, and the inversion ("Notion was built for humans to browse. Engram is built for AI to query.") from CLAUDE.md §"What Is Engram"

    4. **"Why Engram"** (1-2 paragraphs) — MCP-first design, user-owned data, layered memory model (personal → team → project → org). Pull from CLAUDE.md §"Core Design Principle": "return insights, not data; Claude reasons, Engram pre-processes" and the routing table that keeps cheap models off Claude's plate

    5. **Architecture diagram** — Mermaid fenced code block (D-14 source-of-truth): COPY VERBATIM the `flowchart LR` block from RESEARCH §"System Architecture Diagram" (lines 194-242). GitHub renders Mermaid natively. Below the block, link to the polished SVG: `> See [docs/architecture.svg](docs/architecture.svg) for the polished hero diagram.`

    6. **Tech stack** (markdown table) — From CLAUDE.md §"Tech Stack": Compute (Cloudflare Workers + TypeScript), Workspace storage (Durable Objects + SQLite), Semantic search (Vectorize), AI processing (Workers AI), Async pipeline (Queues), File storage (R2), Config (KV), Runtime (Wrangler), Package manager (npm workspaces).

    7. **Status** — Current milestone: `v0.1 — MCP Foundation (in progress)`. Link to `.planning/ROADMAP.md` and (optionally) the Linear milestone URL. One-line progress note: "Foundation scaffolding (Phase 1) complete. Working on WorkspaceDO + SQLite (Phase 2)."

    8. **Install / dev quickstart** — single fenced bash block (use `npm run dev:mcp` and `npm run dev:triage` aliases from Plan 01's `package.json` scripts):
       ```bash
       # Prerequisites: Node 22+, npm 10+
       gh repo clone russellkmoore/engram
       cd engram
       npm install
       npm run typecheck && npm run lint && npm run lint:wrangler

       # Boot a Worker locally
       npm run dev:mcp       # engram-mcp-server on http://localhost:8787
       npm run dev:triage    # engram-triage-worker
       ```

    9. **Architecture deep-dive link** — `For full architecture (SQLite schema, MCP tool surface, DO topology, build-order dependencies), see [CLAUDE.md](CLAUDE.md).`

    10. **License footer** — `Licensed under Apache License 2.0 — see [LICENSE](LICENSE). The v0.1 license selection is provisional pending v1.0 confirmation.`

    Tone: portfolio-quality (D-13 rationale: v0.1 ships to GitHub well before the v0.4 killer demo; the README is the first credibility signal).

    **`docs/architecture.svg`** — render the Mermaid source from README §Architecture to an SVG hero image.
    - Recommended tooling (D-disc): `@mermaid-js/mermaid-cli`. Concrete command: write the Mermaid block to a temp `.mmd` file, then run `npx -y @mermaid-js/mermaid-cli@latest mmdc -i /tmp/diagram.mmd -o docs/architecture.svg -t default -b transparent`. The `-t default` theme matches GitHub Mermaid default; `-b transparent` produces transparent background suitable for both light and dark README themes.
    - Create the `docs/` directory first: `mkdir -p docs`
    - If `npx` fails (no network in execution sandbox), document the fallback in this plan's SUMMARY and create a minimal placeholder SVG: a 400x100 SVG with a `<text>` element saying "Architecture diagram — see README.md Mermaid block; run `npx -y @mermaid-js/mermaid-cli mmdc -i <input> -o docs/architecture.svg` to regenerate." This preserves the file's existence (README link doesn't 404) without faking a polished diagram.
    - The README Mermaid block is the live source-of-truth (D-14); the SVG is the polished hero image. They are intentionally allowed to diverge slightly (theme/colors); the structural content must match.
  </action>
  <verify>
    <automated>test -f README.md &amp;&amp; test -f docs/architecture.svg &amp;&amp; grep -q '# Engram' README.md &amp;&amp; grep -q 'CLAUDE.md' README.md &amp;&amp; grep -q 'License' README.md &amp;&amp; grep -q 'shields.io' README.md &amp;&amp; grep -q '```mermaid' README.md &amp;&amp; grep -q 'npm run dev:mcp' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists with ≥ 80 lines (≥ 100 preferred): `test $(wc -l < README.md) -ge 80`
    - Title heading present: `head -20 README.md | grep -qE '^# Engram'`
    - Exactly THREE badges in the top section (D-15): `head -10 README.md | grep -c 'shields.io\\|actions/workflows' | awk '{exit($1>=3 && $1<=4)?0:1}'` (allow 3-4 to accommodate one being CI from `github.com/.../badge.svg` which may not contain `shields.io`)
    - Apache-2.0 license badge: `grep -q 'License: Apache' README.md || grep -q 'license/Apache' README.md`
    - GitHub Actions CI badge URL references the ci.yml workflow: `grep -q 'actions/workflows/ci.yml' README.md`
    - Version badge mentions `0.1.0`: `grep -q '0.1.0' README.md`
    - NO extra badges (no coverage/codecov, no npm version, no Vercel/Netlify): `! grep -qiE 'codecov\\|coverage\\|npm/v\\|vercel\\|netlify' README.md`
    - All 10 D-13 sections present (heading-anchored): `for h in 'Why Engram' 'Architecture' 'Tech [Ss]tack' 'Status' 'Install\\|Quickstart\\|Setup\\|Getting' 'License'; do grep -qE "$h" README.md || (echo "missing section: $h" && exit 1); done`
    - Mermaid fenced code block present (D-14 source-of-truth): `grep -q '\\`\\`\\`mermaid' README.md`
    - Mermaid block contains the architecture diagram from RESEARCH §System Architecture Diagram: `awk '/```mermaid/,/```/' README.md | grep -q 'flowchart'`
    - Tech stack table includes Cloudflare Workers, Durable Objects, Vectorize, Workers AI, Queues: `for t in 'Cloudflare Workers' 'Durable Objects' 'Vectorize' 'Workers AI' 'Queues'; do grep -q "$t" README.md || (echo "missing tech: $t" && exit 1); done`
    - Install quickstart uses the actual npm scripts from Plan 01: `grep -q 'npm install' README.md && grep -q 'npm run dev:mcp' README.md`
    - Architecture deep-dive link to CLAUDE.md present: `grep -q '\\[CLAUDE.md\\](CLAUDE.md)' README.md`
    - License footer references LICENSE file + v1.0 confirmation: `grep -q '\\[LICENSE\\](LICENSE)' README.md && grep -q 'provisional\\|v1.0' README.md`
    - `docs/architecture.svg` exists (real render OR documented placeholder): `test -f docs/architecture.svg && test $(wc -c < docs/architecture.svg) -gt 100`
    - SVG file starts with `<?xml` or `<svg`: `head -1 docs/architecture.svg | grep -qE '<\\?xml|<svg'`
  </acceptance_criteria>
  <done>README.md exists with all 10 D-13 sections, exactly 3 badges, Mermaid architecture block, tech stack table, install quickstart; docs/architecture.svg exists (rendered or documented placeholder).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify README portfolio quality (per VALIDATION.md Manual-Only check)</name>
  <files>README.md, docs/architecture.svg, LICENSE, .github/workflows/ci.yml, CLAUDE.md, CONTRIBUTING.md</files>
  <action>Pause for human verification. Claude has completed all automation. The human pushes the branch to GitHub and visually verifies README portfolio quality (badges live, Mermaid renders, links resolve), CI workflow runs green, LICENSE header present, CONTRIBUTING.md unchanged. See `<how-to-verify>` for the exact checklist.</action>
  <verify><human-check>Human runs the 5-step checklist in `<how-to-verify>` against the live GitHub UI and reports back.</human-check></verify>
  <done>Human types `approved` confirming all 5 visual checks passed AND the first CI run is green.</done>
  <what-built>
    The full Phase 1 deliverable is in place:
    - Root config (package.json, tsconfigs, ESLint, Prettier, Husky, dotfiles, LICENSE)
    - FND-08 wrangler lint script + fixtures + smoke scripts
    - CLAUDE.md amended per Pattern 11 (no `wrangler.toml`, no `ingest-worker` as v0.1, no `format?` on search)
    - Shared packages (`@engram/types`, `@engram/schema` with 7 system memory types)
    - Worker packages (`@engram/mcp-server` with two-DO topology, `@engram/triage-worker`, `@engram/workspace-do` library stub)
    - GitHub Actions CI workflow wiring lint + typecheck + format:check + FND-08 lint + smoke tests
    - Portfolio-quality README with Mermaid architecture diagram + three badges (license, CI, version)
    - Polished SVG hero diagram in `docs/architecture.svg`
    - `scripts/setup-dev.sh` retired (or thinly shimmed to `npm run setup`)

    All automated verifications have passed: `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run lint:wrangler`, both `wrangler dev` smokes, and (locally) the fresh-install smoke.
  </what-built>
  <how-to-verify>
    1. **Push the branch to GitHub** so the README renders in the GitHub UI (the local file preview does NOT render Mermaid; only github.com does).
       - `git status` should show: 13 new/modified root config files (from Plan 01), 5 lint/smoke/fixture files (from Plan 02), 1 modified CLAUDE.md (from Plan 03), 7 shared-package files (from Plan 04), 11 Worker-package files (from Plan 05), 4 wiring files (from this Plan 06).
       - Run `git add -A && git commit -m "Phase 1 foundation scaffolding complete" && git push -u origin <branch>` (branch name TBD — see GSD plan-phase Linear sync conventions).
    2. **Verify the CI workflow runs green** on the first push. Open GitHub Actions tab for the repo, watch the `CI` workflow on this branch. All steps should succeed: install → types:gen → typecheck → lint → format:check → lint:wrangler → smoke (mcp-server) → smoke (triage-worker) → smoke install. Total CI runtime expected ~90s.
    3. **Open the README on GitHub** (https://github.com/<owner>/engram on the branch). Verify:
       - All three badges render and are LIVE (not broken images): Apache-2.0 license (blue), CI status (green for the first successful run), version (`0.1.0-alpha`).
       - The Mermaid architecture diagram renders inline (GitHub's native renderer should display the flowchart, not just show the raw fenced code block).
       - The `docs/architecture.svg` link resolves (click it; should display the polished SVG).
       - The `CLAUDE.md` link resolves to the amended doc (Plan 03 edits applied).
       - The install quickstart commands are correct (`npm install`, `npm run dev:mcp`, etc. — match Plan 01's package.json scripts).
       - The "Status" section accurately reflects "Phase 1 complete, Phase 2 in progress" or similar.
       - The "Tech stack" table includes all 9 layers from CLAUDE.md §"Tech Stack".
       - Tone reads as portfolio-quality: elevator pitch is crisp, "Why Engram" makes the case, no broken/placeholder copy.
    4. **Open the LICENSE file on GitHub** — verify the v1.0 confirmation header is present in the first 5 lines (FND-06 acceptance).
    5. **Verify CONTRIBUTING.md is unchanged** — open it on GitHub, confirm it is byte-identical to the pre-Phase-1 12-line GSD setup file (D-16 compliance).
  </how-to-verify>
  <resume-signal>
    Type `approved` if README portfolio quality is acceptable AND the CI workflow ran green AND all 5 visual checks above passed.
    Type a description of any issues (e.g., "Mermaid diagram cut off, regenerate SVG with wider viewBox" or "badges 403'ing — fix owner placeholder") to send back for revision.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → repo merge gate | The CI workflow is the durable enforcement of D-03 (lint + typecheck + format + FND-08 + smoke). `--no-verify` on local commits is bypassable; CI on the PR is not. |
| README on GitHub → visitor first impression | The README is the public-facing front door — broken badges, missing Mermaid renders, or stale copy actively damage credibility per D-13 rationale. |
| `npx -y @mermaid-js/mermaid-cli` → SVG render | One-time SVG generation pulls an external package via npx. After the SVG exists, this is no longer in the trust path — but the SVG itself is committed and visible publicly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-23 | Tampering | CI workflow ordering bug — `types:gen` after `typecheck` causes false-positive failures (Pitfall 7) | mitigate | Acceptance criterion enforces step ordering via `awk` line-number check. |
| T-01-24 | Tampering | `--no-verify` bypasses pre-commit on local commits, ships unlinted code (RESEARCH §Security Domain) | mitigate | CI workflow is the durable gate per D-03. Acceptance criterion confirms CI workflow triggers on every PR — `--no-verify` is meaningless when the PR cannot merge red. |
| T-01-25 | Information Disclosure | Broken badges on README (e.g., wrong owner in CI badge URL) silently degrade credibility | mitigate | Human-verify checkpoint (Task 4) explicitly checks all 3 badges render live on GitHub. Plan SUMMARY records actual owner for the badge URL. |
| T-01-26 | Tampering | `npx -y @mermaid-js/mermaid-cli@latest` pulls an unpinned package version (supply-chain) | mitigate | The npx command runs ONCE during scaffolding to generate the static SVG. After the SVG is committed, the runtime trust path no longer touches mermaid-cli. If reproducibility is required, switch to pinned `@mermaid-js/mermaid-cli@11.x` (latest stable line as of 2026-05); document in SUMMARY. |
| T-01-27 | Tampering | `scripts/setup-dev.sh` retained alongside `npm run setup` with duplicated logic — drift makes one source of truth ambiguous | mitigate | Task 2 enforces deletion OR thin-shim-only (≤ 3 LOC, exec only); acceptance criterion `! grep -q 'plugin marketplace add'` blocks GSD-instructions duplication. |
</threat_model>

<verification>
After this plan: `.github/workflows/ci.yml` exists with all required steps in correct order. `README.md` has all 10 D-13 sections, exactly 3 badges, Mermaid block, tech stack table, install quickstart. `docs/architecture.svg` exists. `scripts/setup-dev.sh` is gone (or is a 3-line exec shim). `CONTRIBUTING.md` unchanged (D-16). Human checkpoint confirms the README renders correctly on GitHub and the first CI run is green.
</verification>

<success_criteria>
- CI workflow runs on push to main and PR to main; all 11 steps pass on the first push
- README renders correctly on GitHub with all 3 badges live, Mermaid diagram inline, all links resolving
- SVG hero diagram exists in `docs/`
- `scripts/setup-dev.sh` is retired or thinly shimmed (no duplicated logic)
- CONTRIBUTING.md is unchanged
- Human portfolio-quality verification approved
- Phase 1 closes with no FND-* requirements outstanding
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-06-SUMMARY.md` when done. Include: CI workflow first-run URL + result (link to GitHub Actions run), screenshots-or-description of README badges rendering on GitHub, list of which fallback (delete vs shim) was chosen for `setup-dev.sh` and why, mermaid-cli version used to render the SVG (or note if placeholder was used), and confirmation from the human-verify checkpoint that portfolio quality is approved.
</output>
</output>
