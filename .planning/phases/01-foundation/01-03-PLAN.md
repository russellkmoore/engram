---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [FND-07]
requirements_addressed: [FND-07]
tags: [documentation, claude-md, architecture-baseline]

must_haves:
  truths:
    - "CLAUDE.md no longer references `wrangler.toml` anywhere in v0.1-active sections."
    - "CLAUDE.md no longer references `ingest-worker` as a v0.1 package (only as a v0.4 future package)."
    - "CLAUDE.md `search` tool signature is `search(query, filters)` — no `format?` parameter."
    - "CLAUDE.md describes the two-DO topology (`EngramMcp` session DO + `WorkspaceDO` durable store) under one `migrations[0].new_sqlite_classes` entry."
    - "CLAUDE.md states that `agents/mcp` `McpAgent` is the MCP host (NOT raw `@modelcontextprotocol/sdk` HTTP transports)."
  artifacts:
    - path: CLAUDE.md
      provides: "Updated architecture baseline matching v0.1 corrections (JSONC, two-DO topology, McpAgent, search without format?, ingest-worker deferred)"
      contains: "EngramMcp"
  key_links:
    - from: CLAUDE.md
      to: "Phase 2-7 downstream agents"
      via: "Agents read CLAUDE.md as the architectural baseline; stale text propagates wrong patterns into future phases"
      pattern: "wrangler\\.jsonc"
---

<objective>
Apply RESEARCH §Pattern 11 verbatim edit map to `CLAUDE.md`: 6 line-anchored swaps + 3 additive paragraphs that bring the architecture baseline into alignment with the v0.1 corrections (JSONC config, two-DO topology, `agents/mcp` `McpAgent`, `search` without `format?`, `ingest-worker` deferred to v0.4).

Purpose: FND-07 is **NOT a cleanup task** — it is the canonical doc-drift mitigation. RESEARCH §Pitfall 8 + §Runtime State Inventory + §Security Domain (V14) all flag CLAUDE.md staleness as the doc-drift hazard that causes Phase 2–7 agents to re-introduce `wrangler.toml`, `ingest-worker`, and `format?`. Whichever version of these decisions Claude reads LAST wins, so the stale baseline must be removed BEFORE any other v0.1 phase starts reading it.

Output: 1 file modified (CLAUDE.md) with 6 surgical line-anchored edits and 3 additive content blocks. CONTRIBUTING.md is NOT touched (D-16).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify current CLAUDE.md line anchors are still accurate (re-grep before editing)</name>
  <files>CLAUDE.md (read-only check)</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §"Pattern 11" — CLAUDE.md Edit Map (lines 763-790, line numbers locked at 2026-05-25)
    - .planning/phases/01-foundation/01-PATTERNS.md §"Pattern Assignments → CLAUDE.md (MODIFY)" (6 line-anchored swaps + 3 additive paragraphs)
    - CLAUDE.md (read the full file to confirm line numbers; the file is 500 lines per pre-task check)
  </read_first>
  <action>
    Re-verify the line anchors from RESEARCH §Pattern 11 still match the current CLAUDE.md state (it was last edited 2026-05-25 by the researcher). Run these grep commands and confirm each returns the expected match:
    - `grep -n 'ingest-worker' CLAUDE.md` — expect a line containing `ingest-worker/        # Ingest pipeline Worker — fetch, chunk, embed, store` somewhere around line 57 in the Repository Structure block. NOTE: actual line may have shifted; the goal is to find the comment-text line, not require exact L57.
    - `grep -n 'wrangler.toml' CLAUDE.md` — expect MULTIPLE hits (root config line ~71, mcp-server ~401, workspace-do ~408, second root ~415). Record actual line numbers in this task's SUMMARY.
    - `grep -n 'search(query, filters, format?)' CLAUDE.md` — expect ~line 254 in the MCP tool surface block
    - `grep -n 'return memories' CLAUDE.md` or `grep -n 'export_url' CLAUDE.md` — expect the comment that needs updating with `search`

    If any anchor has shifted from RESEARCH §Pattern 11's documented line numbers (57, 71, 254, 401, 408, 415), proceed using the actual current line numbers (the edit operations are content-based, not position-based — `grep`/`sed` patterns match the text, not the line number). Record actual line numbers found in this task's SUMMARY so Task 2 has fresh anchors.

    This task is a read-only verification step; no edits to CLAUDE.md yet. Output a small report (printed to stdout or saved as a temp note) listing each anchor's actual current line number.
  </action>
  <verify>
    <automated>grep -q 'ingest-worker' CLAUDE.md &amp;&amp; grep -q 'wrangler\.toml' CLAUDE.md &amp;&amp; grep -q 'search(query, filters, format?)' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - All three stale-token anchors are present in CLAUDE.md before edits: `grep -q 'ingest-worker' CLAUDE.md`, `grep -q 'wrangler\\.toml' CLAUDE.md`, `grep -q 'search(query, filters, format?)' CLAUDE.md`
    - Multiple `wrangler.toml` occurrences exist (at least 3 — root + mcp-server + workspace-do): `test $(grep -c 'wrangler\\.toml' CLAUDE.md) -ge 3`
    - The Repository Structure block contains `ingest-worker/` as a directory entry: `grep -q 'ingest-worker/' CLAUDE.md`
  </acceptance_criteria>
  <done>All Pattern 11 anchor tokens are present in current CLAUDE.md; actual line numbers recorded for Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply Pattern 11 edits to CLAUDE.md (6 line-anchored swaps + 3 additive paragraphs)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §"Pattern 11" — exact replacement text for each edit (lines 763-790)
    - .planning/phases/01-foundation/01-PATTERNS.md §"Pattern Assignments → CLAUDE.md (MODIFY)" — edit map summary (line 213-228)
    - .planning/phases/01-foundation/01-CONTEXT.md §"Specifics" — rationale that FND-07 is hard requirement, not cleanup
    - Output of Task 1 (actual current line numbers)
  </read_first>
  <action>
    Apply the following edits to `CLAUDE.md` using the `Edit` tool. Each edit specifies the OLD text to find (use the full surrounding context to disambiguate; CLAUDE.md is 500 lines and `wrangler.toml` appears 4 times) and the NEW replacement text.

    **Edit 1 — Repository Structure block, ingest-worker line (currently around L57):**
    Find the line: `    ingest-worker/        # Ingest pipeline Worker — fetch, chunk, embed, store`
    DELETE this line entirely. Replace nothing in its place (the surrounding `triage-worker/` and `connector-*` entries remain). After the closing of the `packages/` block in the tree, ADD a one-line annotation outside the code fence (or as a tree-block comment if the structure permits):
    > *Note: `ingest-worker` was an earlier design — folded into `triage-worker` for v0.1; reintroduced in v0.4 if connector volume warrants it.*

    **Edit 2 — Root wrangler.toml line (currently around L71):**
    Find the line containing `wrangler.toml           # Root Cloudflare config` (root-level entry in the repo tree).
    REPLACE the entire line with the comment line: `  # No root wrangler config — each Worker package owns its own wrangler.jsonc.`
    (Preserve the surrounding indentation level of the tree block.)

    **Edit 3 — MCP tool surface `search` signature (currently around L254):**
    Find the line: `search(query, filters, format?)`
    REPLACE with: `search(query, filters)`
    Also find the adjacent comment `// Returns: memories[], count, export_url? (if format specified)` and REPLACE with: `// Returns: memories[], count`
    After the v0.1 `search` block, ADD this note as a new comment line: `  // Note: export(query, format, filters?) is a separate v0.3 tool — see Milestones.`

    **Edit 4 — v0.1 Scaffold Target, mcp-server wrangler reference (currently around L401):**
    Find the line: `      wrangler.toml` under `mcp-server/`
    REPLACE with: `      wrangler.jsonc`

    **Edit 5 — v0.1 Scaffold Target, workspace-do wrangler reference (currently around L408):**
    Find the line: `      wrangler.toml` under `workspace-do/`
    DELETE the entire line. Per D-10, `workspace-do` is library-only and has NO wrangler config.

    **Edit 6 — v0.1 Scaffold Target, second root wrangler reference (currently around L415):**
    Find the line: `  wrangler.toml           # root config, DO bindings`
    DELETE the entire line.

    **Additive Paragraph 1 — Two-DO topology subsection:**
    Locate the "Durable Object Per Workspace" section (around L41-44 — the block that explains `Every user/team gets their own WorkspaceDO`).
    AFTER that section, INSERT a new subsection titled `### Session DO vs Workspace DO` with body:
    > Each Worker that hosts an MCP endpoint actually owns **two DO classes** declared in the same `wrangler.jsonc`:
    > - **`EngramMcp`** — auto-managed by `agents/mcp` `McpAgent`; holds transient MCP session state (per active client connection). Lifecycle: one DO instance per session, garbage-collected when the session ends.
    > - **`WorkspaceDO`** — durable, per-workspace, reached via `getAgentByName(env.WORKSPACE, this.props.workspace_id)` after JWT validation. This is where the SQLite store lives.
    >
    > Both are declared together under `migrations[0].new_sqlite_classes: ["EngramMcp", "WorkspaceDO"]`. SQLite-backed (not KV-backed) is irreversible per Cloudflare's migration rules.

    **Additive Paragraph 2 — McpAgent annotation:**
    Locate the area where CLAUDE.md describes the MCP server (the "MCP Worker — primary interface for AI clients" comment near L51, or the section discussing MCP Tool Surface around L240-260).
    INSERT a sentence/short paragraph that states:
    > The Worker uses `import { McpAgent } from "agents/mcp"` and serves via `EngramMcp.serve("/mcp")`. Do NOT use raw `@modelcontextprotocol/sdk` HTTP transports — they depend on `node:http` and will not run on `workerd`.

    **Additive Paragraph 3 — ingest-worker deferred annotation in Milestones:**
    Locate the Milestones table (around L141-148 — the table listing v0.1 → v1.0).
    AFTER the table or in the v0.4 row's context, ADD:
    > `ingest-worker` is **not** part of v0.1. The triage-worker consumes the Queue directly. The `ingest-worker` package returns in v0.4 when external connectors (Slack, Drive) need a general ingest orchestration layer.

    CRITICAL: Do NOT touch any other section of CLAUDE.md. Do NOT touch CONTRIBUTING.md (D-16). Do NOT add/modify code blocks, table rows, or paragraphs beyond what is enumerated above. Use the `Edit` tool with unique surrounding context for each replacement to avoid ambiguity (since `wrangler.toml` appears in multiple lines).
  </action>
  <verify>
    <automated>! grep -nE 'wrangler\.toml|ingest-worker[^/]|search\(query, filters, format\?\)' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - Zero remaining `wrangler.toml` references in v0.1-active sections: `! grep -q 'wrangler\\.toml' CLAUDE.md` (or if any acceptable historical reference remains, it must be inside a milestone-context paragraph explicitly noting the v0.1 migration to JSONC — verify by reading match context)
    - `ingest-worker` only appears in v0.4-context (not as a v0.1 package directory): `! grep -E 'ingest-worker/\\s*#.*Ingest pipeline' CLAUDE.md` (the original repo-tree entry is gone)
    - `search` tool signature is updated: `grep -q 'search(query, filters)' CLAUDE.md` AND `! grep -q 'search(query, filters, format?)' CLAUDE.md`
    - `export_url?` comment is gone: `! grep -q 'export_url?' CLAUDE.md`
    - Two-DO topology subsection added: `grep -q 'Session DO vs Workspace DO' CLAUDE.md` AND `grep -q 'EngramMcp' CLAUDE.md` AND `grep -q 'getAgentByName' CLAUDE.md`
    - McpAgent annotation added: `grep -q 'from "agents/mcp"' CLAUDE.md` AND `grep -q 'workerd' CLAUDE.md`
    - ingest-worker v0.4 deferral noted: `grep -q 'ingest-worker.*v0.4\\|v0.4.*ingest-worker\\|not part of v0.1' CLAUDE.md`
    - Run the canonical staleness grep from RESEARCH §Validation Architecture FND-07 row: `! grep -nE 'wrangler\\.toml|ingest-worker[^/]|search\\(query, filters, format\\?\\)' CLAUDE.md` exits 0 (no stale tokens found)
    - CLAUDE.md still has its original structure (Repository Structure, Architecture, MCP Tool Surface, Milestones sections all intact): `grep -q '^## Tech Stack' CLAUDE.md && grep -q '^## Repository Structure' CLAUDE.md && grep -q '^## Milestones' CLAUDE.md && grep -q '^## MCP Tool Surface' CLAUDE.md`
    - CONTRIBUTING.md is unchanged: `git diff --quiet CONTRIBUTING.md` exits 0
  </acceptance_criteria>
  <done>All 6 Pattern 11 line-anchored edits applied; all 3 additive paragraphs inserted; staleness grep returns zero matches; CONTRIBUTING.md untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md → downstream agents (Phase 2-7) | Agents read CLAUDE.md as the architectural baseline for v0.1 work; stale references propagate wrong patterns into every later phase |
| Documentation → implementation | Documentation drift is a "tampering via stale source-of-truth" vector (RESEARCH §Security Domain V14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering (via documentation) | Stale CLAUDE.md re-introduces deprecated patterns (`wrangler.toml`, `ingest-worker`, `format?`) into Phase 2-7 work | mitigate | This plan applies the verbatim RESEARCH §Pattern 11 edit map. Acceptance criterion `! grep -nE 'wrangler\\.toml\|ingest-worker[^/]\|search\\(query, filters, format\\?\\)' CLAUDE.md` is the canonical staleness check. Phase verification (`/gsd:verify-work`) will re-run this grep as the FND-07 acceptance gate. |
| T-01-10 | Tampering | Edit accidentally touches CONTRIBUTING.md or unrelated CLAUDE.md sections (scope creep) | mitigate | Acceptance criterion `git diff --quiet CONTRIBUTING.md` confirms CONTRIBUTING.md is unchanged. Edit tool's `old_string`/`new_string` surgical replacement (with unique surrounding context per Edit 1-6) ensures no other CLAUDE.md sections are altered. |
| T-01-11 | Tampering | Two-DO topology mis-described (e.g., classes declared under separate migrations entries instead of one) | mitigate | Additive paragraph 1 explicitly states `migrations[0].new_sqlite_classes: ["EngramMcp", "WorkspaceDO"]` (single migration, array of two classes) — matches RESEARCH §Pattern 1 verbatim and is enforced by Plan 02's FND-08 lint when Plan 05 commits the actual mcp-server wrangler.jsonc. |
</threat_model>

<verification>
After this plan: `! grep -nE 'wrangler\.toml|ingest-worker[^/]|search\(query, filters, format\?\)' CLAUDE.md` exits 0 (no stale tokens in v0.1-active sections). `grep -q 'Session DO vs Workspace DO' CLAUDE.md` AND `grep -q 'from "agents/mcp"' CLAUDE.md` (both additive paragraphs landed). `git diff --quiet CONTRIBUTING.md` exits 0 (CONTRIBUTING.md untouched per D-16).
</verification>

<success_criteria>
- All 6 line-anchored swaps applied (wrangler.toml → wrangler.jsonc or deleted; ingest-worker tree entry removed; search signature corrected)
- All 3 additive paragraphs inserted (two-DO topology subsection; McpAgent annotation; ingest-worker v0.4 deferral note)
- Canonical staleness grep returns no matches
- CONTRIBUTING.md is byte-identical to pre-plan state
- CLAUDE.md structural sections (Tech Stack, Repository Structure, MCP Tool Surface, Milestones) all preserved
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done. Include: actual line numbers of each edit, before/after excerpts for each of the 6 line-anchored swaps, full text of the 3 additive paragraphs, and confirmation that CONTRIBUTING.md is unchanged.
</output>
