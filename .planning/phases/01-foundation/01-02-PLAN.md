---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lint-wrangler.mjs
  - scripts/smoke-install.sh
  - scripts/smoke-wrangler-dev.sh
  - tests/fixtures/good-wrangler.jsonc
  - tests/fixtures/bad-wrangler.jsonc
autonomous: true
requirements: [FND-08]
requirements_addressed: [FND-08]
tags: [lint, ci, scaffolding, smoke-tests, wrangler]

must_haves:
  truths:
    - "Running `node scripts/lint-wrangler.mjs` against the production glob (`packages/*/wrangler.jsonc`) exits 0 when only `new_sqlite_classes` is used."
    - "Running `node scripts/lint-wrangler.mjs` against `tests/fixtures/bad-wrangler.jsonc` in positional-arg mode exits 1 with a violation message mentioning `new_classes` (end-to-end FND-08 negative-path proof inside Wave 1)."
    - "Running `node scripts/lint-wrangler.mjs` against `tests/fixtures/good-wrangler.jsonc` in positional-arg mode exits 0 (end-to-end FND-08 positive-path proof inside Wave 1)."
    - "The lint script supports dual-mode invocation: no args → globs `packages/*/wrangler.jsonc` (production full-scan); with args → lints exactly the given files (lint-staged per-file mode + fixture invocations)."
    - "Smoke scripts (`smoke-install.sh`, `smoke-wrangler-dev.sh`) exist as executable shell scripts callable from CI."
  artifacts:
    - path: scripts/lint-wrangler.mjs
      provides: "FND-08 lint rule — rejects any wrangler.jsonc whose migrations declare a DO under `new_classes`"
      contains: "jsonc-parser"
      min_lines: 35
    - path: scripts/smoke-install.sh
      provides: "FND-01 fresh-clone install smoke (`rm -rf node_modules && npm install && ls -d node_modules/@engram/*`)"
    - path: scripts/smoke-wrangler-dev.sh
      provides: "FND-03 wrangler dev boot smoke for each Worker"
    - path: tests/fixtures/good-wrangler.jsonc
      provides: "Positive lint fixture (mirrors mcp-server two-DO good case)"
      contains: "new_sqlite_classes"
    - path: tests/fixtures/bad-wrangler.jsonc
      provides: "Negative lint fixture (uses `new_classes` to prove the lint catches it)"
      contains: "new_classes"
  key_links:
    - from: scripts/lint-wrangler.mjs
      to: tests/fixtures/bad-wrangler.jsonc
      via: "Negative-path test — running the script against this file MUST exit 1"
      pattern: 'new_classes'
    - from: scripts/lint-wrangler.mjs
      to: packages/*/wrangler.jsonc
      via: "Production glob — lint runs on every committed Worker config in CI and pre-commit"
      pattern: 'packages/\\*/wrangler\\.jsonc'
---

<objective>
Land the FND-08 wrangler.jsonc lint script (`scripts/lint-wrangler.mjs`) plus its positive/negative test fixtures, and the two smoke scripts (`smoke-install.sh`, `smoke-wrangler-dev.sh`) that verify FND-01 and FND-03 in CI.

Purpose: FND-08 is the architectural integrity control that prevents the irreversible `new_classes` regression (DO-1). Cloudflare workers-sdk issue #9909 documents: KV-backed DOs declared via `new_classes` CANNOT be retroactively converted to SQLite-backed. This lint script is the only durable defense once new Workers get scaffolded in future phases. It MUST land in Wave 1 so it is enforced from the very first wrangler.jsonc commit in Plan 05 (Wave 2).

The smoke scripts complete the validation scaffolding called out in `01-VALIDATION.md` §"Wave 0 Requirements". Plan 06 wires them into CI.

Output: 5 files (1 lint script + 2 smoke scripts + 2 test fixtures).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create scripts/lint-wrangler.mjs (FND-08 jsonc-parser-based lint rule)</name>
  <files>scripts/lint-wrangler.mjs</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §"Pattern 8" (verbatim ~40 LOC node script with jsonc-parser — lines 623-695)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Don't Hand-Roll" (rationale for jsonc-parser over jq/regex)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Anti-Patterns to Avoid" (`new_classes` for any DO; KV-backed cannot become SQLite-backed)
    - .planning/phases/01-foundation/01-PATTERNS.md §"Pattern Assignments → scripts/lint-wrangler.mjs" (DO-1 irreversibility, location decision, exit code conventions)
    - .planning/phases/01-foundation/01-CONTEXT.md §"Claude's Discretion" (FND-08 implementation language is open; researcher chose node + jsonc-parser; we honor that choice)
  </read_first>
  <action>
    Create `scripts/lint-wrangler.mjs` matching RESEARCH §Pattern 8 verbatim (~40 LOC). The script MUST:
    - Use Node 22 ESM imports: `import { readFileSync } from "node:fs"`, `import { glob } from "node:fs/promises"`, `import { parse, printParseErrorCode } from "jsonc-parser"`
    - Set constants `VIOLATION_KEY = "new_classes"` and `REQUIRED_KEY = "new_sqlite_classes"`
    - Operate in DUAL MODE: when `process.argv.length > 2`, lint exactly the files passed as positional args (used by lint-staged on a per-staged-file basis and by direct invocations like `node scripts/lint-wrangler.mjs tests/fixtures/bad-wrangler.jsonc`); when no positional args, glob `packages/*/wrangler.jsonc` (the production full-scan mode used by CI's `npm run lint:wrangler` step and by the pre-commit fallback). DO NOT glob `**/wrangler.jsonc` — `workspace-do` is library-only per D-10 and `tests/fixtures/` files must not be picked up by the production full-scan
    - Include a top-of-file header comment block documenting the dual-mode behavior verbatim: `// Usage: node scripts/lint-wrangler.mjs [file...]`, `//   - No args: globs packages/*/wrangler.jsonc (production full-scan mode used by CI).`, `//   - With args: lints exactly the given files (lint-staged per-file mode, fixture invocations).`, `// Exit codes: 0 clean | 1 violation | 2 no files matched (full-scan canary only)`.
    - The exit-2 canary applies ONLY to no-arg full-scan mode. When positional args are provided and any file is missing/unreadable, treat that as a violation (exit 1) — not as the empty-glob canary
    - Parse each file with `jsonc-parser` `parse(text, errors, { allowTrailingComma: true })`
    - On parse errors, print file path + each `printParseErrorCode(err.error)` line and increment violations
    - For each migrations entry, fail if `Array.isArray(mig?.[VIOLATION_KEY]) && mig[VIOLATION_KEY].length > 0` — emit message `[lint:wrangler] <file> migration[<i>] (tag: <tag>) declares new_classes=<json>. Engram requires SQLite-backed Durable Objects only — use new_sqlite_classes.`
    - Exit codes: `0` clean, `1` one-or-more violations, `2` no files found (canary against accidental `packages/` rename — RESEARCH §Pattern 8 line 651)
    - Print `[lint:wrangler] OK — checked <N> file(s).` on success

    NOTE: At Wave 1 the no-arg full-scan mode against `packages/*/wrangler.jsonc` will return zero files (Plan 05 commits them in Wave 2). The script will exit 2 if run with no args against the empty glob. This is the documented canary behavior — DO NOT add a "if no files, exit 0" workaround. The CI step `npm run lint:wrangler` will fail in Wave 1 builds; that is acceptable because the CI workflow itself does not land until Plan 06 (Wave 3). For Wave 1 verification of THIS task, invoke the script directly against the test fixtures via positional args (Task 3) — `node scripts/lint-wrangler.mjs tests/fixtures/good-wrangler.jsonc` (exit 0) and `node scripts/lint-wrangler.mjs tests/fixtures/bad-wrangler.jsonc` (exit 1). Positional-arg mode never returns exit 2.
  </action>
  <verify>
    <automated>node scripts/lint-wrangler.mjs; rc=$?; test $rc -eq 2 || test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lint-wrangler.mjs` exists with at least 35 non-comment lines: `grep -cv '^\\s*//\\|^\\s*$' scripts/lint-wrangler.mjs` returns ≥ 35
    - Imports `jsonc-parser`: `grep -q 'from "jsonc-parser"' scripts/lint-wrangler.mjs`
    - Defines violation key as `new_classes`: `grep -q 'new_classes' scripts/lint-wrangler.mjs`
    - Globs `packages/*/wrangler.jsonc`: `grep -q 'packages/\\*/wrangler\\.jsonc' scripts/lint-wrangler.mjs`
    - Uses Node ESM `node:fs/promises` glob (Node 22+): `grep -q 'node:fs/promises' scripts/lint-wrangler.mjs`
    - Exit code 2 when zero files found: `grep -q 'process.exit(2)' scripts/lint-wrangler.mjs`
    - Exit code 1 on violation: `grep -q 'process.exit(1)' scripts/lint-wrangler.mjs`
    - Node can parse the script (syntax check): `node --check scripts/lint-wrangler.mjs` exits 0
    - Header comment documents dual-mode behavior: `grep -q 'Usage: node scripts/lint-wrangler.mjs \[file\.\.\.\]' scripts/lint-wrangler.mjs` and `grep -q 'production full-scan mode' scripts/lint-wrangler.mjs` and `grep -q 'lint-staged per-file mode' scripts/lint-wrangler.mjs`
    - No-arg full-scan against empty Wave 1 glob behaves as canary: `node scripts/lint-wrangler.mjs; test $? -eq 2` (exit 2 because no packages/*/wrangler.jsonc exist yet in Wave 1)
    - Positional-arg mode against good fixture exits 0: `node scripts/lint-wrangler.mjs tests/fixtures/good-wrangler.jsonc; test $? -eq 0` (gated on Task 3 having created the fixture; run after Task 3)
    - Positional-arg mode against bad fixture exits 1: `node scripts/lint-wrangler.mjs tests/fixtures/bad-wrangler.jsonc; test $? -eq 1` (gated on Task 3; this is the FND-08 negative-path proof — DO-1 protection)
    - Positional-arg mode against a non-existent file exits 1 (not 2): `node scripts/lint-wrangler.mjs /tmp/does-not-exist-$.jsonc; rc=$?; test $rc -eq 1` (exit-2 canary applies only to no-arg full-scan mode)
  </acceptance_criteria>
  <done>Script exists, parses with `node --check`, uses jsonc-parser, supports dual-mode invocation (no-args → glob `packages/*/wrangler.jsonc`; with-args → lint exactly those files), exits 2 only when no-arg full-scan finds zero files (canary), exits 1 on violation (incl. missing files in positional-arg mode), exits 0 on clean files.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create smoke scripts (smoke-install.sh + smoke-wrangler-dev.sh)</name>
  <files>scripts/smoke-install.sh, scripts/smoke-wrangler-dev.sh</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §"Validation Architecture" → "Phase Requirements → Test Map" (FND-01 row for smoke-install; FND-03 row for smoke-wrangler-dev)
    - .planning/phases/01-foundation/01-VALIDATION.md §"Wave 0 Requirements" (exact shell shapes)
    - .planning/phases/01-foundation/01-PATTERNS.md §"Pattern Assignments → scripts/smoke-install.sh" + §"scripts/smoke-wrangler-dev.sh"
  </read_first>
  <action>
    Create both smoke scripts. Use `#!/usr/bin/env bash` shebang and `set -euo pipefail` at the top. Mark both executable (`chmod +x`).

    **`scripts/smoke-install.sh`** — FND-01 acceptance per VALIDATION.md:
    - Body: `rm -rf node_modules` then `npm install` then assert `ls -d node_modules/@engram/* >/dev/null 2>&1` (workspace symlinks present). If the assertion fails because Wave 1/2 hasn't created shared packages yet, emit a clear message; exit 0 only when both `npm install` succeeds AND at least one `node_modules/@engram/*` symlink exists.
    - In Wave 1 (before Plan 04 lands shared packages), this script will exit non-zero. That is expected; CI does not invoke it until Plan 06 wires it after Wave 2 completes.

    **`scripts/smoke-wrangler-dev.sh`** — FND-03 acceptance per VALIDATION.md:
    - Accepts a single positional arg: the path to a `wrangler.jsonc` (default: `packages/mcp-server/wrangler.jsonc`)
    - Boots wrangler dev in the background: `timeout 15 npx wrangler dev --config "$CONFIG" --port 8787 &`
    - Sleeps 8 seconds
    - Curls `http://localhost:8787` expecting HTTP 200 (`curl -sf http://localhost:8787`)
    - Sends SIGTERM to the background wrangler process (`kill %1 2>/dev/null || true`)
    - Exits 0 on successful curl, non-zero otherwise

    Use absolute-path-friendly quoting; do not assume `pwd` is repo root (CI may run from a different cwd). Use `${BASH_SOURCE[0]}` resolution if needed.
  </action>
  <verify>
    <automated>test -x scripts/smoke-install.sh &amp;&amp; test -x scripts/smoke-wrangler-dev.sh &amp;&amp; bash -n scripts/smoke-install.sh &amp;&amp; bash -n scripts/smoke-wrangler-dev.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke-install.sh` exists, executable (`test -x`), syntactically valid (`bash -n` exits 0)
    - `scripts/smoke-wrangler-dev.sh` exists, executable, syntactically valid
    - Both files start with `#!/usr/bin/env bash`: `head -1 scripts/smoke-install.sh | grep -q '#!/usr/bin/env bash'`
    - Both files use strict mode: `grep -q 'set -euo pipefail' scripts/smoke-install.sh` AND `grep -q 'set -euo pipefail' scripts/smoke-wrangler-dev.sh`
    - smoke-install.sh runs `rm -rf node_modules` and `npm install` and checks for `@engram/`: `grep -q 'rm -rf node_modules' scripts/smoke-install.sh && grep -q 'npm install' scripts/smoke-install.sh && grep -q '@engram' scripts/smoke-install.sh`
    - smoke-wrangler-dev.sh references wrangler dev with timeout + port 8787 + curl: `grep -q 'wrangler dev' scripts/smoke-wrangler-dev.sh && grep -q '8787' scripts/smoke-wrangler-dev.sh && grep -q 'curl' scripts/smoke-wrangler-dev.sh && grep -q 'timeout' scripts/smoke-wrangler-dev.sh`
    - smoke-wrangler-dev.sh defaults to mcp-server config when no arg: `grep -q 'packages/mcp-server/wrangler.jsonc' scripts/smoke-wrangler-dev.sh`
  </acceptance_criteria>
  <done>Both smoke scripts exist as executable bash scripts with strict mode, correct entry shapes, and pass `bash -n` syntax check.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create lint test fixtures (good-wrangler.jsonc + bad-wrangler.jsonc) and verify lint script behavior</name>
  <files>tests/fixtures/good-wrangler.jsonc, tests/fixtures/bad-wrangler.jsonc</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §"Pattern 1" (verbatim mcp-server wrangler.jsonc — mirror for good fixture, lines 321-347)
    - .planning/phases/01-foundation/01-VALIDATION.md §"Wave 0 Requirements" (bad fixture: `new_classes: ["WorkspaceDO"]`)
    - .planning/phases/01-foundation/01-PATTERNS.md §"Pattern Assignments → tests/fixtures/good-wrangler.jsonc" + §"bad-wrangler.jsonc" (must NOT be picked up by the production glob; fixture lives outside `packages/`)
    - scripts/lint-wrangler.mjs (from Task 1 of this plan — needed to verify behavior)
  </read_first>
  <action>
    Create both fixtures under `tests/fixtures/` (note: `tests/` is OUTSIDE the production glob `packages/*/wrangler.jsonc`, so the production lint will not pick them up — by design per PATTERNS §bad-wrangler.jsonc risk note).

    **`tests/fixtures/good-wrangler.jsonc`** — copy RESEARCH §Pattern 1 verbatim (mcp-server two-DO good case). Includes:
    - `"$schema": "../../node_modules/wrangler/config-schema.json"` (relative from `tests/fixtures/`, this WILL be a broken path inside the fixture — that's fine; jsonc-parser ignores `$schema`)
    - `"name": "engram-mcp-server-good-fixture"` (rename so it's clearly a fixture, not confused with real mcp-server)
    - `"main": "src/index.ts"`, `"compatibility_date": "2026-05-22"`, `"compatibility_flags": ["nodejs_compat"]`
    - `"durable_objects.bindings"` with `EngramMcp` + `WorkspaceDO`
    - `"migrations": [ { "tag": "v1", "new_sqlite_classes": ["EngramMcp", "WorkspaceDO"] } ]`
    - Include a `//` comment line to exercise jsonc-parser's comment handling

    **`tests/fixtures/bad-wrangler.jsonc`** — same as good fixture, but with the migration declaring `new_classes: ["WorkspaceDO"]` instead of `new_sqlite_classes` (the exact regression FND-08 prevents). Rename to `engram-mcp-server-bad-fixture`.

    After creating both fixtures, verify the lint script behaves correctly against each by running explicit invocations:
    - `node -e "import('jsonc-parser').then(m => { const t=require('fs').readFileSync('tests/fixtures/good-wrangler.jsonc','utf8'); const e=[]; const c=m.parse(t,e,{allowTrailingComma:true}); const mig=c.migrations[0]; process.exit((Array.isArray(mig.new_classes) && mig.new_classes.length>0)?1:0); })"` → expect exit 0 (good fixture has no `new_classes`)
    - Same for bad fixture → expect exit 1 (bad fixture has `new_classes`)

    These ad-hoc node checks validate the fixture content shape. Then run the END-TO-END FND-08 proof using the actual lint script in positional-arg mode (which is the dual-mode behavior Task 1 establishes):
    - `node scripts/lint-wrangler.mjs tests/fixtures/good-wrangler.jsonc` — expect exit code 0 and a clean OK message (positive-path proof)
    - `node scripts/lint-wrangler.mjs tests/fixtures/bad-wrangler.jsonc` — expect exit code 1 and a violation message mentioning `new_classes` (negative-path proof — DO-1 protection)

    These two invocations together exercise the script's complete exit-code matrix (0 clean, 1 violation) end-to-end inside Wave 1 against real files. The no-arg full-scan exit-2 canary is verified separately in Task 1 acceptance. Plan 05 reuses these same positional-arg invocations against the bad fixture, and additionally runs no-arg full-scan against the real `packages/*/wrangler.jsonc` files committed in Wave 2.
  </action>
  <verify>
    <automated>node -e "const p=require('jsonc-parser'); const fs=require('fs'); const good=p.parse(fs.readFileSync('tests/fixtures/good-wrangler.jsonc','utf8'),[],{allowTrailingComma:true}); const bad=p.parse(fs.readFileSync('tests/fixtures/bad-wrangler.jsonc','utf8'),[],{allowTrailingComma:true}); const goodOk=Array.isArray(good.migrations[0].new_sqlite_classes) &amp;&amp; !good.migrations[0].new_classes; const badOk=Array.isArray(bad.migrations[0].new_classes) &amp;&amp; bad.migrations[0].new_classes.length>0; process.exit((goodOk &amp;&amp; badOk)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/good-wrangler.jsonc` exists; `grep -q '"new_sqlite_classes"' tests/fixtures/good-wrangler.jsonc` AND `! grep -q '"new_classes"' tests/fixtures/good-wrangler.jsonc`
    - `tests/fixtures/bad-wrangler.jsonc` exists; `grep -q '"new_classes"' tests/fixtures/bad-wrangler.jsonc` (the regression we're preventing)
    - good-wrangler.jsonc contains a JSONC comment (`//`): `grep -q '^\\s*//' tests/fixtures/good-wrangler.jsonc` (exercises jsonc-parser comment handling)
    - Both fixtures parse with jsonc-parser without errors (validated by the automated verify command above)
    - good fixture has BOTH `EngramMcp` and `WorkspaceDO` in `new_sqlite_classes`: `grep -q 'EngramMcp' tests/fixtures/good-wrangler.jsonc && grep -q 'WorkspaceDO' tests/fixtures/good-wrangler.jsonc`
    - bad fixture's `new_classes` array contains at least one DO class name (non-empty), satisfying the lint trigger condition (verified by automated command above checking `length>0`)
    - Fixtures are NOT inside `packages/` so the production no-arg full-scan glob (`packages/*/wrangler.jsonc`) does not pick them up: `ls tests/fixtures/` shows them outside `packages/`
    - **FND-08 end-to-end negative-path proof (closes W-02):** invoke the lint script in positional-arg mode against the bad fixture and assert exit code 1: `node scripts/lint-wrangler.mjs tests/fixtures/bad-wrangler.jsonc; rc=$?; test $rc -eq 1`. Capture the script's stderr/stdout message and confirm it mentions `new_classes` (the violation text)
    - **FND-08 end-to-end positive-path proof:** invoke the lint script in positional-arg mode against the good fixture and assert exit code 0: `node scripts/lint-wrangler.mjs tests/fixtures/good-wrangler.jsonc; rc=$?; test $rc -eq 0`. Together with the negative-path check above, this exercises BOTH lint exit paths against real files inside Wave 1 (no dependency on Wave 2 `packages/` content)
  </acceptance_criteria>
  <done>Both fixtures exist; good fixture passes the FND-08 rule (no `new_classes`), bad fixture fails it (`new_classes` present with non-empty array); jsonc-parser parses both successfully; end-to-end positional-arg invocation of `scripts/lint-wrangler.mjs` against the good fixture exits 0 AND against the bad fixture exits 1 (FND-08 exit-1 path proven end-to-end inside Wave 1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo on disk | Lint script is the gate that blocks `new_classes` regressions from merging |
| Developer machine → wrangler.jsonc edits | Pre-commit hook (Plan 01) routes `**/wrangler.jsonc` changes through `scripts/lint-wrangler.mjs` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | `new_classes` regression introduced in a future Worker's wrangler.jsonc (irreversible DO-1; KV-backed DO cannot convert to SQLite-backed per Cloudflare workers-sdk issue #9909) | mitigate | This plan delivers the FND-08 lint script + fixtures. Plan 06 (Wave 3) wires it into CI as `npm run lint:wrangler` so every PR is gated. The script exits 1 on any non-empty `new_classes` array in any `packages/*/wrangler.jsonc`. |
| T-01-07 | Tampering | Lint script silently passes when no wrangler.jsonc files exist (accidental rename of `packages/` directory, broken glob) | mitigate | Script exits with code 2 when zero files match, distinct from "exit 0 clean" — CI step will fail visibly. Acceptance criterion verifies this canary directly. |
| T-01-08 | Tampering | Test fixtures accidentally picked up by production lint (would cause spurious CI failure or hide a real regression) | mitigate | Fixtures live in `tests/fixtures/` (outside `packages/` glob). Pattern 8 glob `packages/*/wrangler.jsonc` is explicit and tested against an empty `packages/` state in this plan's verify step (exits 2, not 0). |
</threat_model>

<verification>
After this plan: `scripts/lint-wrangler.mjs` exists and parses (`node --check`). No-arg full-scan exits 2 against the empty Wave 1 `packages/*/wrangler.jsonc` glob (canary, not a failure). Positional-arg mode exits 0 against `tests/fixtures/good-wrangler.jsonc` AND exits 1 against `tests/fixtures/bad-wrangler.jsonc` (end-to-end FND-08 proof of BOTH exit paths inside Wave 1 — closes W-02). The two fixtures exist; the good fixture has no `new_classes`, the bad fixture does. The jsonc-parser ad-hoc check on both fixtures passes. Smoke scripts are executable bash with strict mode. Full FND-08 end-to-end behavior against real Worker packages (`npm run lint:wrangler` no-arg full-scan exits 0 on Wave 2 production files) is exercised in Plan 05; CI wiring lands in Plan 06.
</verification>

<success_criteria>
- `scripts/lint-wrangler.mjs` exists, ≥35 LOC, uses jsonc-parser, supports dual-mode invocation (no-args → glob `packages/*/wrangler.jsonc`; with-args → lint given files), exit codes {0: clean, 1: violation (incl. missing files in positional-arg mode), 2: no-arg full-scan found zero files}
- End-to-end FND-08 proof inside Wave 1: positional-arg invocation against `tests/fixtures/bad-wrangler.jsonc` exits 1; against `tests/fixtures/good-wrangler.jsonc` exits 0
- `tests/fixtures/good-wrangler.jsonc` mirrors RESEARCH §Pattern 1 with `new_sqlite_classes`
- `tests/fixtures/bad-wrangler.jsonc` declares `new_classes` (the regression we're preventing)
- Smoke scripts (`smoke-install.sh`, `smoke-wrangler-dev.sh`) exist as executable bash scripts with strict mode
- Ad-hoc jsonc-parser checks confirm fixtures have the expected migration shapes
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done. Include: line counts for the lint script, ad-hoc jsonc-parser check output proving both fixtures match expectations, and confirmation that `node scripts/lint-wrangler.mjs` exits 2 against the empty `packages/` state.
</output>
