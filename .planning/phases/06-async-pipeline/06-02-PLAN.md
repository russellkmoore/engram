---
phase: 06-async-pipeline
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - scripts/setup-queue.sh
  - package.json
  - packages/mcp-server/wrangler.jsonc
  - packages/triage-worker/wrangler.jsonc
  - packages/mcp-server/src/tools.ts
  - packages/mcp-server/src/index.ts
autonomous: true
requirements: [PIP-01, PIP-02]
must_haves:
  truths:
    - "A `wrangler queues create engram-ingest`-shaped setup script exists at scripts/setup-queue.sh and is idempotent (re-runs are no-op)"
    - "mcp-server/wrangler.jsonc declares `queues.producers[]` with binding INGEST_QUEUE → engram-ingest"
    - "triage-worker/wrangler.jsonc declares `queues.consumers[]` with queue engram-ingest, max_batch_size 10, max_batch_timeout 5, max_retries 3, and NO dead_letter_queue field"
    - "registerTools() accepts a 4th `getCtx: () => DurableObjectState` parameter so handler bodies can call ctx.waitUntil(...)"
    - "EngramMcp.init() passes `() => this.ctx` as the new 4th arg to registerTools()"
    - "No existing handler behavior changed by the registerTools signature widening (existing tests stay GREEN)"
  artifacts:
    - path: "scripts/setup-queue.sh"
      provides: "Idempotent CLI provisioning of the engram-ingest Cloudflare Queue (mirrors setup-vectorize.sh)"
      min_lines: 30
    - path: "packages/mcp-server/wrangler.jsonc"
      provides: "queues.producers[] block binding INGEST_QUEUE → engram-ingest"
      contains: "INGEST_QUEUE"
    - path: "packages/triage-worker/wrangler.jsonc"
      provides: "queues.consumers[] block for engram-ingest with the D-03 retry config"
      contains: "engram-ingest"
    - path: "packages/mcp-server/src/tools.ts"
      provides: "registerTools widened to accept getCtx closure as 4th param (signature only — handler bodies still unchanged)"
      contains: "getCtx"
    - path: "packages/mcp-server/src/index.ts"
      provides: "init() calls registerTools with `() => this.ctx` as 4th arg"
      contains: "() => this.ctx"
  key_links:
    - from: "packages/mcp-server/src/index.ts EngramMcp.init()"
      to: "packages/mcp-server/src/tools.ts registerTools"
      via: "function call with 4 args (server, getProps, env, getCtx)"
      pattern: "registerTools\\(this\\.server, \\(\\) => this\\.props, this\\.env, \\(\\) => this\\.ctx\\)"
    - from: "packages/mcp-server/wrangler.jsonc"
      to: "Cloudflare Queue engram-ingest"
      via: "queues.producers[] binding declaration"
      pattern: "\"binding\":\\s*\"INGEST_QUEUE\""
    - from: "packages/triage-worker/wrangler.jsonc"
      to: "Cloudflare Queue engram-ingest"
      via: "queues.consumers[] binding declaration"
      pattern: "\"queue\":\\s*\"engram-ingest\""
---

<objective>
Wave 2 (parallel with 06-03): provision the Queue infrastructure end-to-end and thread the Worker `ctx` (DurableObjectState) into the tools callback closure so Plan 06-04 can call `ctx.waitUntil(env.INGEST_QUEUE.send(...))` from inside `remember()` without further plumbing.

Purpose: Phase 6's split — "tools.ts handler body" (06-04) versus "Worker config + accessor wiring" (this plan) — keeps each plan's diff focused. By the time 06-04 runs, both `env.INGEST_QUEUE` (producer binding) and a `getCtx()` closure (waitUntil accessor) MUST exist; otherwise the one-line ctx.waitUntil() call has nowhere to land.

Output: idempotent `scripts/setup-queue.sh` (NEW); npm script `setup:queue` (root package.json amendment); `queues.producers[]` block on mcp-server/wrangler.jsonc; `queues.consumers[]` block on triage-worker/wrangler.jsonc per CONTEXT.md Claude's Discretion §"Cloudflare Queues consumer config"; widened `registerTools` signature in tools.ts (4th param: `getCtx: () => DurableObjectState`); EngramMcp.init() passes `() => this.ctx` as the 4th arg. No handler bodies change in this plan — that is 06-04 territory.

Parallel-execution note: this plan does NOT modify `packages/workspace-do/*` (06-03 owns that). It DOES modify `packages/mcp-server/src/tools.ts` — 06-04 also modifies tools.ts BUT in a different region (handler body of `remember()`); the wave ordering (06-02 → 06-04) guarantees no merge conflict.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-async-pipeline/06-CONTEXT.md
@.planning/phases/06-async-pipeline/06-PATTERNS.md
@packages/mcp-server/wrangler.jsonc
@packages/triage-worker/wrangler.jsonc
@packages/mcp-server/src/tools.ts
@packages/mcp-server/src/index.ts
@scripts/setup-vectorize.sh

<interfaces>
<!-- Cloudflare Queues + agents/mcp + DurableObjectState contracts the executor needs. -->

From Cloudflare Queues (per CONTEXT.md canonical refs §"Cloudflare official docs"):
- `wrangler queues create <name>` is NOT natively idempotent (second run errors "queue already exists"). Precheck with `wrangler queues info <name>` — exits 0 if found, non-zero otherwise. If `info` is not in `wrangler@4` (verify at execute time), fall back to `wrangler queues list 2>/dev/null | grep -q "${QUEUE_NAME}"`.
- Producer binding (consumed by `mcp-server/wrangler.jsonc`):
  ```jsonc
  "queues": { "producers": [{ "binding": "INGEST_QUEUE", "queue": "engram-ingest" }] }
  ```
- Consumer binding (consumed by `triage-worker/wrangler.jsonc`):
  ```jsonc
  "queues": {
    "consumers": [{
      "queue": "engram-ingest",
      "max_batch_size": 10,
      "max_batch_timeout": 5,
      "max_retries": 3
    }]
  }
  ```
  No `dead_letter_queue` field — per D-03 the failure surface is `blocks.ingest_status = 'failed'` + Workers Analytics Engine + `console.error`.

From @cloudflare/workers-types index.d.ts (line 641):
```typescript
interface DurableObjectState<Props = unknown> {
  waitUntil(promise: Promise<any>): void;
  // ... id, storage, blockConcurrencyWhile, etc.
}
```
`DurableObjectState.waitUntil` exists natively — no ExecutionContext threading needed.

From agents/mcp (node_modules/agents/dist/mcp/index.js:1314):
- `McpAgent extends Agent extends Server extends DurableObject` — so `this.ctx` on an `EngramMcp` instance IS a `DurableObjectState` and exposes `waitUntil`.
- `EngramMcp.init()` (existing in packages/mcp-server/src/index.ts lines 85–96) is the canonical wire-up site. It already passes `this.server, () => this.props, this.env` to `registerTools`.

From packages/mcp-server/src/tools.ts (existing line 183):
```typescript
export function registerTools(
  server: McpServer,
  getProps: () => EngramProps | undefined,
  env: Env,
): void { /* ... */ }
```
This plan widens the signature to:
```typescript
export function registerTools(
  server: McpServer,
  getProps: () => EngramProps | undefined,
  env: Env,
  getCtx: () => DurableObjectState,    // NEW 4th param
): void { /* body unchanged — Plan 06-04 wires the first ctx.waitUntil(...) call */ }
```
The closure form (vs. passing `ctx` directly) matches the existing `() => this.props` pattern — survives any future agents/mcp rebinding of `this.ctx` across token refresh (Pitfall 6 in 03-RESEARCH.md).

**Per checker B2 fix: the `captureCallback` test helper in this plan ALSO accepts an optional `ctxOverride` parameter** so that Plan 06-05 Task 2's PIP-02 latency test can inject a tracking `waitUntil` implementation. The default override is a drop-the-promise stub. See Task 3 below for the signature.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/setup-queue.sh + root npm script (PIP-01)</name>
  <files>scripts/setup-queue.sh, package.json</files>
  <read_first>
    - scripts/setup-vectorize.sh (lines 1–65 — the canonical idempotent-CLI-provisioning template Phase 6 mirrors; copy `set -euo pipefail`, the precheck-then-create pattern, the WARNING comment about not running `delete` in production)
    - package.json (root — the existing `scripts` block — find where `setup:vectorize` is registered, if any; sibling `setup:queue` lands next to it)
    - .planning/phases/06-async-pipeline/06-CONTEXT.md §"Decisions" D-03 (queue name `engram-ingest`); §"Canonical References" §"Cloudflare official docs" (links to the Queue create/info subcommands)
    - .planning/phases/06-async-pipeline/06-PATTERNS.md §11 (full setup-queue.sh template — includes precheck-fallback note for `wrangler queues info` vs `wrangler queues list`)
  </read_first>
  <action>
    Two coordinated edits:

    1. **Create `scripts/setup-queue.sh`** mirroring `scripts/setup-vectorize.sh` exactly. Required shape:
       - Shebang `#!/usr/bin/env bash` + `set -euo pipefail`.
       - Header doc-block citing PIP-01, the producer/consumer pair, CONTEXT.md §"Claude's Discretion → Cloudflare Queues consumer config", the WARNING about never deleting the queue in production (deleting destroys in-flight MemoryEvents — the at-least-once guarantee assumes the queue exists; A11/IP-1 mitigation reference), and the dual usage line (`bash scripts/setup-queue.sh` direct + `npm run setup:queue` via the npm script).
       - Constant `QUEUE_NAME="engram-ingest"`.
       - Idempotency precheck via `npx wrangler queues info "${QUEUE_NAME}" >/dev/null 2>&1`. If `info` is not a valid subcommand in the wrangler version available, the script falls back to `npx wrangler queues list 2>/dev/null | grep -q "${QUEUE_NAME}"`. Choose ONE precheck at script-write time by running `npx wrangler queues --help 2>&1 | grep -E "(info|list)"` once during execution and pinning the choice into the script.
       - Skip branch (queue exists): print `[skip] Queue '${QUEUE_NAME}' already exists — no-op.` and exit 0.
       - Create branch (queue absent): print `[create] Creating Cloudflare Queue '${QUEUE_NAME}'...`, run `npx wrangler queues create "${QUEUE_NAME}"`, print `[ok] Queue created.`
       - Trailing `echo "=== Queue setup complete ==="`.
       - File must be executable: after creating, run `chmod +x scripts/setup-queue.sh`.

    2. **Amend root `package.json`** — add `"setup:queue": "bash scripts/setup-queue.sh"` to the `scripts` block (preserve alphabetical or sequential ordering — if `setup:vectorize` exists, add `setup:queue` adjacent to it). Do NOT touch `dependencies` / `devDependencies` / any other field. Do NOT touch workspace member declarations.

    No code-side changes in tools.ts or wrangler configs in this task — those land in Tasks 2 + 3.
  </action>
  <verify>
    <automated>test -x scripts/setup-queue.sh \
      && bash -n scripts/setup-queue.sh \
      && grep -q "QUEUE_NAME=\"engram-ingest\"" scripts/setup-queue.sh \
      && grep -q "set -euo pipefail" scripts/setup-queue.sh \
      && grep -qE "queues (info|list)" scripts/setup-queue.sh \
      && grep -q "wrangler queues create" scripts/setup-queue.sh \
      && grep -q "setup:queue" package.json \
      && grep -q "PIP-01" scripts/setup-queue.sh</automated>
  </verify>
  <done>
    `scripts/setup-queue.sh` exists, is executable, passes bash syntax check (`bash -n`), declares `QUEUE_NAME=engram-ingest`, contains the precheck-then-create idempotency pattern, and cites PIP-01 in its header. Root `package.json` has the `setup:queue` script entry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wrangler bindings — INGEST_QUEUE producer (mcp-server) + engram-ingest consumer (triage-worker)</name>
  <files>packages/mcp-server/wrangler.jsonc, packages/triage-worker/wrangler.jsonc</files>
  <read_first>
    - packages/mcp-server/wrangler.jsonc (existing top-level keys lines 13–32; the producer block lands between `analytics_engine_datasets` (line 15) and `durable_objects` (line 16); mirror the existing JSONC trailing-comma + array-of-object style)
    - packages/triage-worker/wrangler.jsonc (existing top-level keys lines 14–27; the consumer block REPLACES the placeholder comment at lines 28–30 which currently reads "Queue consumer block lands in Phase 6 PIP-01"; insert the new block in that exact location)
    - .planning/phases/06-async-pipeline/06-CONTEXT.md §"Claude's Discretion → Cloudflare Queues consumer config" — the literal D-03 values: `max_batch_size: 10`, `max_batch_timeout: 5`, `max_retries: 3`, NO `dead_letter_queue` field
    - .planning/phases/06-async-pipeline/06-PATTERNS.md §5 (consumer block exact JSONC) + §8 (producer block exact JSONC); §"Shared Pattern A" notes on the sibling-binding shape; §"No dead_letter_queue" callout
    - CLAUDE.md §"Tech Stack" — confirms Cloudflare Queues as the async-pipeline mechanism for Engram
  </read_first>
  <action>
    Two coordinated edits to per-package wrangler.jsonc files:

    1. **`packages/mcp-server/wrangler.jsonc`** — append a new top-level `queues` key. Place it between `analytics_engine_datasets` (line 15) and `durable_objects` (line 16). Exact shape (preserve JSONC trailing-comma convention used by sibling keys):
       ```jsonc
       "queues": {
         "producers": [{ "binding": "INGEST_QUEUE", "queue": "engram-ingest" }],
       },
       ```
       Do NOT modify `migrations`, `kv_namespaces`, `durable_objects`, `ai`, `vectorize`, `analytics_engine_datasets`, `observability`, `compatibility_date`, `compatibility_flags`, `main`, `name`, `$schema`, or the file-header comment. Strict-additive change.

    2. **`packages/triage-worker/wrangler.jsonc`** — REPLACE the placeholder comment block at lines 28–30 (`// Queue consumer block lands in Phase 6 PIP-01.` plus the two follow-up explanatory lines) with the live consumer block. Insert AT THE SAME LOCATION (top-level, before the closing brace). Exact shape:
       ```jsonc
       "queues": {
         "consumers": [
           {
             "queue": "engram-ingest",
             "max_batch_size": 10,
             "max_batch_timeout": 5,
             "max_retries": 3,
           },
         ],
       },
       ```
       Keep one trailing inline comment ABOVE the consumer block citing the source: `// Phase 6 PIP-01 / CONTEXT.md D-03 + §"Claude's Discretion → Cloudflare Queues consumer config".` AND a second comment ABOVE that line: `// No dead_letter_queue per D-03: failure surface is blocks.ingest_status = 'failed' + Workers Analytics Engine + console.error.`
       Do NOT add a `dead_letter_queue` field. Do NOT modify `durable_objects`, `ai`, `vectorize`, `analytics_engine_datasets`, `observability`, `dev`, `compatibility_date`, `compatibility_flags`, `main`, `name`, `$schema`, or the file-header comment.

    Validation: after both edits, `npx wrangler types --config packages/mcp-server/wrangler.jsonc` must not throw a config-parse error; same for `--config packages/triage-worker/wrangler.jsonc`. (Type regeneration is not committed here — it lands implicitly when 06-04 imports `MemoryEvent` and references `env.INGEST_QUEUE`.)

    Lint gate: `npm run lint:wrangler` (FND-08 invariant — the `[[migrations]]` `new_classes` check) must still pass on both wrangler files. The `lint:wrangler` script tolerates additive top-level keys; only the `migrations` block is policed.
  </action>
  <verify>
    <automated>grep -q "\"binding\":\\s*\"INGEST_QUEUE\"" packages/mcp-server/wrangler.jsonc \
      && grep -q "\"queue\":\\s*\"engram-ingest\"" packages/mcp-server/wrangler.jsonc \
      && grep -q "\"queue\":\\s*\"engram-ingest\"" packages/triage-worker/wrangler.jsonc \
      && grep -q "\"max_batch_size\":\\s*10" packages/triage-worker/wrangler.jsonc \
      && grep -q "\"max_batch_timeout\":\\s*5" packages/triage-worker/wrangler.jsonc \
      && grep -q "\"max_retries\":\\s*3" packages/triage-worker/wrangler.jsonc \
      && ! grep -q "dead_letter_queue" packages/triage-worker/wrangler.jsonc \
      && ! grep -q "dead_letter_queue" packages/mcp-server/wrangler.jsonc \
      && npm run lint:wrangler</automated>
  </verify>
  <done>
    Both wrangler.jsonc files declare their Queue bindings per D-03. No `dead_letter_queue` field anywhere. `npm run lint:wrangler` exits 0. Sibling bindings (ai, vectorize, analytics_engine_datasets, durable_objects, kv_namespaces, migrations) are untouched.

    **Note (per checker W2 fix):** The previous draft included a `node -e 'const c=require("./packages/mcp-server/wrangler.jsonc"); ...'` positive check. Node's `require()` does NOT parse JSONC (trailing commas + comments fail), so that branch was dead code; only the grep fallback ever executed. The `<verify>` block above now relies solely on grep-based assertions (positive checks for the binding declarations + negative checks for `dead_letter_queue`) plus `npm run lint:wrangler` for the structural sanity. This is grep-only, JSONC-safe, and runs without any Node-side parse attempt.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: registerTools 4th param `getCtx` + EngramMcp.init() wiring + captureCallback ctxOverride (PIP-02 plumbing)</name>
  <files>packages/mcp-server/src/tools.ts, packages/mcp-server/src/index.ts</files>
  <read_first>
    - packages/mcp-server/src/tools.ts (existing `registerTools` signature at lines 183–187; the doc-block above it lines 169–182 describing the 3 existing params — extend with a 4th param doc entry; the handler bodies between lines 257–652 are NOT touched in this plan)
    - packages/mcp-server/src/index.ts (EngramMcp.init() lines 85–96 — the canonical wire-up site; `this.ctx` is available because `McpAgent extends Agent extends Server extends DurableObject`; `this.ctx` is typed `DurableObjectState`)
    - node_modules/@cloudflare/workers-types/index.d.ts line 641 (DurableObjectState interface — `waitUntil(promise: Promise<any>): void` is a native method, no ExecutionContext threading required)
    - packages/mcp-server/src/__tests__/tools-integration.test.ts (the `captureCallback` helper at lines 184–210 — passes positional args to `registerTools`; this plan must keep that helper green by adding a 4th arg there too AND adding an optional `ctxOverride` parameter per checker B2 fix)
    - packages/mcp-server/src/__tests__/tools.test.ts (calls `registerTools` directly per task 4 of this plan; needs the same 4th-arg amendment)
    - .planning/phases/06-async-pipeline/06-CONTEXT.md §"Claude's Discretion → Producer wiring" — confirms the closure pattern (vs. passing ctx directly) and the placement of the eventual `ctx.waitUntil(...)` call in 06-04
  </read_first>
  <behavior>
    - Test 1: existing `tools-integration.test.ts` describes (TOL-01 through TOL-05) continue to pass — the signature widening must be backward-compatible. The 4th param `getCtx` is consumed by NO handler body in this plan, so a drop-the-promise stub `() => ({ waitUntil: (p: Promise<unknown>) => { void p; } })` in test callers is acceptable. The production call site (EngramMcp.init) MUST pass the real `() => this.ctx`.
    - Test 2: a new compile-time check — TypeScript MUST error if a caller passes 3 args (the old signature). Verified implicitly by `npm run typecheck` failing if EngramMcp.init() is not updated.
    - Test 3 (no behavior change): the existing `tools.test.ts` structural assertions (e.g., the T-03-DD-RT sentinel check, the no-`args.workspace_id` rule) stay green — the new param does not touch any handler body.
    - Test 4 (forward-compatibility for Plan 06-05): the extended `captureCallback(toolName, workspace_id, ctxOverride?)` signature accepts an optional override; default is the drop-the-promise stub; when provided, the override's `waitUntil` is invoked by handler bodies that call `getCtx().waitUntil(...)`. This is the B2 fix landing site.
  </behavior>
  <action>
    Three coordinated edits — pure plumbing, no handler-body changes.

    1. **`packages/mcp-server/src/tools.ts`** — widen the `registerTools` signature to accept a 4th param `getCtx`. Three sub-changes within this file:
       - Import the `DurableObjectState` type at the top of the file (alongside the existing `DurableObjectNamespace` import). Use a type-only import: `import type { DurableObjectState } from "@cloudflare/workers-types"` (or amend an existing `import type { ... } from "@cloudflare/workers-types"` if one exists). NOTE: `DurableObjectState` is a global ambient type — if it resolves without the import, omit the import; verify by running `tsc --noEmit` after.
       - Amend the function signature at line 183 from `(server: McpServer, getProps: () => EngramProps | undefined, env: Env): void` to `(server: McpServer, getProps: () => EngramProps | undefined, env: Env, getCtx: () => DurableObjectState): void`. The new parameter MUST be the 4th positional arg (do not insert it earlier — backward source compatibility for the test files is preserved by appending).
       - Amend the doc-block above lines 169–182 to add a `@param getCtx` entry. Doc text: "lazy closure returning the live `DurableObjectState` of the owning `EngramMcp` session DO. Used by Phase 6 Plan 06-04's `remember()` handler to invoke `getCtx().waitUntil(...)` for fire-and-forget Queue enqueue. Lazy pattern matches `getProps` — survives any future agents/mcp rebinding of `this.ctx` across token refresh (Pitfall 6)."
       - Inside the function body, BEFORE the existing `// --- Phase-4-ready handler shape ---` comment block (around line 188), add a single-line use of `getCtx` so the lint rule for unused params does not flag it. Use the form: `void getCtx; // wired in Plan 06-04 — getCtx().waitUntil(env.INGEST_QUEUE.send(memoryEvent)) lands in remember() handler body`.
       - Do NOT modify any of the 5 `server.registerTool(...)` calls. Do NOT modify any handler bodies. Do NOT modify the `workspaceNs` declaration at line 250.

    2. **`packages/mcp-server/src/index.ts`** — amend the single call to `registerTools` at line 94 from `registerTools(this.server, () => this.props, this.env);` to `registerTools(this.server, () => this.props, this.env, () => this.ctx);`. Preserve the surrounding eslint-disable / eslint-enable block. Touch nothing else in the file.

    3. **Test-file amendments** (required for typecheck to pass after the signature change):
       - **`packages/mcp-server/src/__tests__/tools-integration.test.ts`** — TWO changes per checker B2 fix:
         (i) Extend the `captureCallback` helper (lines 184–210) signature to accept an optional third parameter `ctxOverride?: { waitUntil: (p: Promise<unknown>) => void }`. Default to a drop-the-promise stub `{ waitUntil: (p: Promise<unknown>) => { void p; } }` when not provided. This default is SAFE — existing TOL-* tests do not exercise the `getCtx().waitUntil(...)` call path (that lands in Plan 06-04 + Plan 06-05).
         (ii) In the `registerTools(...)` invocation inside `captureCallback`, pass the resolved override as the 4th arg: `registerTools(server, () => ({ workspace_id, user_id }), env, () => (ctxOverride ?? defaultCtxStub) as unknown as DurableObjectState);` where `defaultCtxStub` is the drop-the-promise stub described above. Use `as unknown as DurableObjectState` to satisfy the strict-TS type without requiring a full DurableObjectState mock (the test-time `waitUntil` is the only method actually invoked by Phase 6 handler code).
         Plan 06-05 Task 2 will pass a tracking-promises override to verify the PIP-02 latency invariant.
       - **`packages/mcp-server/src/__tests__/tools.test.ts`** (any direct `registerTools(...)` call sites): add the same 4th-arg stub `() => ({ waitUntil: (p: Promise<unknown>) => { void p; } }) as unknown as DurableObjectState`. Grep the file first; multiple call sites may exist.

    RED → GREEN cycle (TDD framing): step (a) widen the signature in tools.ts FIRST so TypeScript breaks every caller; step (b) run `cd packages/mcp-server && npx tsc --noEmit` and observe the call-site errors in index.ts + test files; step (c) fix each call site by appending the 4th arg and extending `captureCallback`; step (d) re-run typecheck and the existing integration tests — both must GREEN with no behavior change.

    No new tests in this plan — the behavior is "signature widened, no runtime effect". Plan 06-04 introduces the first actual `getCtx().waitUntil(...)` invocation and Plan 06-05 Task 2 introduces the PIP-02 behavioral test that uses the `ctxOverride` parameter.
  </action>
  <verify>
    <automated>cd packages/mcp-server && npx tsc --noEmit 2>&1 | tee /tmp/06-02-tsc.log; \
      [[ ! -s /tmp/06-02-tsc.log ]] || ! grep -E "error TS" /tmp/06-02-tsc.log; \
      grep -q "getCtx: () => DurableObjectState" packages/mcp-server/src/tools.ts \
      && grep -q "() => this\\.ctx" packages/mcp-server/src/index.ts \
      && grep -q "ctxOverride" packages/mcp-server/src/__tests__/tools-integration.test.ts \
      && cd packages/mcp-server && npx vitest run src/__tests__/tools-integration.test.ts src/__tests__/tools.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    `registerTools` accepts a 4th `getCtx: () => DurableObjectState` param; EngramMcp.init() passes `() => this.ctx`; `captureCallback` accepts an optional `ctxOverride` parameter (B2 fix forward-compatibility for Plan 06-05); all existing mcp-server tests stay green; `tsc --noEmit` reports zero errors in the mcp-server package; no handler-body diff in tools.ts (a `git diff` shows only the import, signature, doc-block, single `void getCtx;` line, and zero edits inside the registerTool callbacks).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mcp-server` → Cloudflare Queue (producer) | `env.INGEST_QUEUE.send(...)` payload (`MemoryEvent`) leaves the Worker and enters the Queue's at-least-once delivery layer. The binding declaration lands here; the actual `.send(...)` call is 06-04. |
| Cloudflare Queue → `triage-worker` (consumer) | `MessageBatch<MemoryEvent>` arrives at the triage-worker `queue()` handler. The binding declaration + retry config land here; the consumer body extension is 06-04. |
| `scripts/setup-queue.sh` → wrangler CLI | The script invokes `npx wrangler queues create engram-ingest` against the developer's Cloudflare account. Trust boundary is the developer's local shell + Cloudflare auth state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-QPOISON | Tampering / Spoofing | `engram-ingest` Queue payload | mitigate (deferred to 06-04) | The Queue producer binding (this plan) does NOT validate payload — that is the producer's responsibility. 06-04 produces `MemoryEvent` only from server-side state (`block.id` UUID minted by `crypto.randomUUID()`, `workspace_id` from `props.workspace_id`, `content` from validated tool args). No client-controlled JSON enters the Queue. |
| T-06-02-SCRIPT-INJECT | Tampering | `scripts/setup-queue.sh` | mitigate | The script takes no positional args and substitutes no environment values into `wrangler queues create`. `QUEUE_NAME` is a hard-coded constant. No `eval`, no `bash -c "${user_input}"`. |
| T-06-02-DLQ-MISSING | Repudiation | Consumer retry-exhaustion silent drop | mitigate (deferred to 06-04) | Per D-03, no DLQ queue in v0.1. The Triage Worker's `markIngestFailed` pre-emption at `message.attempts >= 2` (lands in 06-04) prevents silent ack-on-retry-exhaustion. This plan ships the `max_retries: 3` config that makes the pre-emption math correct. Without `markIngestFailed` (06-03 + 06-04), the consumer would silently drop on retry exhaustion — accepted risk window is the gap between this plan committing and 06-04 committing; v0.1 single-user volume means no live traffic exposed. |
| T-06-02-CTX-LEAK | Information Disclosure | `getCtx` closure | accept | `DurableObjectState` is exposed only to the `registerTools` callback closure (which already has full access to `env` and `props`). `getCtx().waitUntil` does not surface SQLite contents or session secrets; it only schedules a Promise. The closure form prevents cross-handler state sharing (each callback re-invokes `getCtx()` to get the current ctx). |
| T-06-02-CONFIG-DRIFT | Tampering | wrangler.jsonc bindings | mitigate | `npm run lint:wrangler` (FND-08) policies the `migrations` block for `new_classes` regressions; this plan does not modify `migrations`. Producer/consumer additions are strict-additive; reviewers verify the `dead_letter_queue` field is ABSENT (negative-assertion grep in `<verify>`). |
| T-06-02-AUTH-PASSTHROUGH | Spoofing | Queue producer → consumer auth chain | mitigate | The `workspace_id` field on the Queue message body (populated by 06-04) is sourced from `props.workspace_id` (JWT-derived). The consumer's `STO-07 assertOwnsWorkspace` (Phase 2) gates DO RPC writes. This plan does not change the auth chain — 06-04 honors it. |
| T-06-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — wrangler is already a dev-dependency from Phase 1 (FND-02). Slopcheck N/A. |

</threat_model>

<verification>
After all 3 tasks complete:

1. `bash -n scripts/setup-queue.sh` exits 0 (script syntax valid).
2. `test -x scripts/setup-queue.sh` succeeds (executable bit set).
3. `npm run lint:wrangler` exits 0 (no `new_classes` regression).
4. `cd packages/mcp-server && npx tsc --noEmit` exits 0 (signature widening propagated cleanly).
5. `cd packages/mcp-server && npx vitest run src/__tests__/tools-integration.test.ts src/__tests__/tools.test.ts` exits 0 (no behavior regression).
6. `grep -c "INGEST_QUEUE\|engram-ingest" packages/mcp-server/wrangler.jsonc packages/triage-worker/wrangler.jsonc` returns ≥ 2 lines (1+ matches per file).
7. `grep -L "dead_letter_queue" packages/mcp-server/wrangler.jsonc packages/triage-worker/wrangler.jsonc` lists BOTH files (negative assertion — neither file contains the field).
8. `grep -q "ctxOverride" packages/mcp-server/src/__tests__/tools-integration.test.ts` succeeds (B2 fix — captureCallback accepts the override).
9. (Optional sanity) `scripts/setup-queue.sh` dry-run: run it once and observe either `[skip]` (queue already exists from prior dev work) or `[create]` (followed by `[ok]`). A second run MUST print `[skip]`. (Skip if developer is not authenticated to Cloudflare locally.)

If any check fails, the plan is incomplete. Common failure modes:
- TypeScript error on `getCtx` use: ambient `DurableObjectState` not resolving — add explicit `import type { DurableObjectState } from "@cloudflare/workers-types"` in tools.ts.
- `tools-integration.test.ts` failing on 3-arg `registerTools` call: forgot to amend the `captureCallback` helper.
- `lint:wrangler` failing: accidentally widened the `migrations` array instead of adding a top-level `queues` key.
- W2 regression: do NOT reintroduce a `node -e 'const c = require("./packages/.../wrangler.jsonc")'` check; Node's `require()` does not parse JSONC (trailing commas + comments fail) and the branch was dead code. Use grep-only verification.
</verification>

<success_criteria>
- `engram-ingest` Queue is provisionable end-to-end via `npm run setup:queue` (idempotent).
- Both Worker wrangler configs carry the producer/consumer bindings per D-03.
- The `getCtx` plumbing exists so 06-04 can wire `ctx.waitUntil(env.INGEST_QUEUE.send(...))` as a single-line addition inside the existing `remember()` handler.
- `captureCallback` accepts an optional `ctxOverride` (B2 fix) so Plan 06-05 Task 2 can inject a tracking-promises override for the PIP-02 latency test.
- No existing test fails; no handler body changed; the diff in tools.ts is restricted to the signature + doc-block + single `void getCtx;` line.
</success_criteria>

<output>
Create `.planning/phases/06-async-pipeline/06-02-SUMMARY.md` when done. Summary should record:
- Which wrangler-queues precheck subcommand was pinned (info vs list) and why.
- Whether `DurableObjectState` resolved as an ambient global or required an explicit import (affects downstream cf-code-assist routing in 06-04).
- Confirmation that no handler bodies changed (`git diff packages/mcp-server/src/tools.ts` shows only signature + doc + single-line use).
- Confirmation that `captureCallback`'s `ctxOverride` parameter is present and defaults to a drop-the-promise stub (B2 fix).
- `setup-queue.sh` dry-run output if executed locally (skip + create branches observed).
- Open carry-forward: 06-04 must implement the actual `getCtx().waitUntil(...)` call in `remember()` AND the `MemoryEvent` assembly above it (with the B3 fix — lazy INGEST_QUEUE dereference inside the handler body, NOT closure-captured at registerTools entry).
</output>
</content>
</invoke>