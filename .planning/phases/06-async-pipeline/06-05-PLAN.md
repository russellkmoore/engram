---
phase: 06-async-pipeline
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified:
  - packages/triage-worker/src/__tests__/queue-integration.test.ts
  - packages/mcp-server/src/__tests__/tools-integration.test.ts
autonomous: true
requirements: [PIP-01, PIP-02, PIP-03, PIP-04, PIP-05, PIP-06]
must_haves:
  truths:
    - "Replaying the same MemoryEvent through the queue handler twice produces exactly 1 inbox row, 1 block row, and 1 Vectorize entry (PIP-03 / IP-1)"
    - "After a memorability > 0.8 happy-path message, the block row transitions ingest_status pending → enriched (PIP-06)"
    - "After a memorability 0.4–0.8 message, the block transitions ingest_status pending → enriched AND an inbox row exists for it (PIP-06)"
    - "After a memorability < 0.4 message, the block has cold_storage=1 AND ingest_status='enriched' simultaneously (D-03 orthogonality)"
    - "After Zod parse fails on attempts >= 2, the block transitions ingest_status pending → failed AND the message is acked (PIP-05)"
    - "After DO RPC fails on attempts >= 2, the block transitions ingest_status pending → failed AND the message is acked (no silent drop — PIP-05)"
    - "remember() resolves in less time than the queue.send mock takes (PIP-02 latency invariant — ctx.waitUntil decouples response from enqueue)"
    - "No block row in any test ever has cold_storage=1 AND ingest_status='failed' simultaneously (negative assertion — D-03 orthogonality)"
  artifacts:
    - path: "packages/triage-worker/src/__tests__/queue-integration.test.ts"
      provides: "PIP-01..06 queue handler integration tests covering replay-twice idempotency, ingest_status lifecycle (3 happy paths + 2 failure paths), and orthogonality"
      min_lines: 200
    - path: "packages/mcp-server/src/__tests__/tools-integration.test.ts"
      provides: "PIP-02 latency describe extension — remember() resolves before INGEST_QUEUE.send awaits (ctx.waitUntil decoupling proof)"
      contains: "PIP-02"
  key_links:
    - from: "queue-integration.test.ts"
      to: "WorkspaceDO.markIngestFailed RPC (Plan 06-03) + triage-worker queue handler (Plan 06-04)"
      via: "import handler from '../index.js'; call handler.queue({ messages: [...] }, env)"
      pattern: "handler.queue"
    - from: "queue-integration.test.ts"
      to: "blocks.ingest_status column (Plan 06-01)"
      via: "runInDurableObject + raw SQL SELECT ingest_status FROM blocks WHERE id = ?"
      pattern: "ingest_status"
    - from: "tools-integration.test.ts PIP-02 describe"
      to: "remember() handler ctx.waitUntil(INGEST_QUEUE.send(...)) (Plan 06-04)"
      via: "patched env.INGEST_QUEUE.send with delay; assert remember() resolves before delay elapses"
      pattern: "queueSendInvokedAt"
---

<objective>
Wave 4: behavioral verification of the entire Phase 6 async pipeline. After 06-01 through 06-04 have shipped the plumbing, this plan proves that the plumbing actually works end-to-end via integration tests that exercise real DO state (`runInDurableObject` against the workerd test pool) and real consumer-handler invocation (calling the default-exported `handler.queue(batch, env)` directly with mock `MessageBatch` objects).

Purpose: Phase 6's success criteria are predominantly behavioral — "replaying the same event through the consumer twice does not duplicate blocks" (PIP-03), "remember() returns before the queue message is consumed" (PIP-02), "blocks.ingest_status tracks per-block enrichment state" (PIP-06). These cannot be verified by static lint or single-helper unit tests; they require end-to-end exercise of the producer → queue → consumer → DO-RPC → SQLite chain. This plan is where Phase 6's "done" status earns its claim.

Output: 1 new file `packages/triage-worker/src/__tests__/queue-integration.test.ts` (~200–300 lines, 6 it() blocks covering the 5 must_haves.truths above for the consumer side); extension of `packages/mcp-server/src/__tests__/tools-integration.test.ts` with 1 new describe + 1 it() for the PIP-02 latency invariant; both files GREEN under `@cloudflare/vitest-pool-workers`.

No production-code changes in this plan — purely test-shipping. If the tests surface a regression, the fix lands as a follow-up amendment to 06-04 (or as a gap-closure Wave 5 plan if the bug is significant). This plan is the verification gate that Phase 6 is ready for `/gsd:verify-work 6`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-async-pipeline/06-CONTEXT.md
@.planning/phases/06-async-pipeline/06-PATTERNS.md
@packages/triage-worker/src/index.ts
@packages/triage-worker/src/extract.ts
@packages/triage-worker/src/__tests__/extract.test.ts
@packages/mcp-server/src/__tests__/tools-integration.test.ts
@packages/workspace-do/src/__tests__/helpers.test.ts

<interfaces>
<!-- The two test-harness patterns this plan combines. -->

From packages/triage-worker/src/__tests__/extract.test.ts (lines 41–131) — the vi.fn message mock shape:
- A `message` object with `retry: vi.fn()`, `ack: vi.fn()`, `attempts: <number>`, `body: <MemoryEvent>` — cast `as unknown as Parameters<typeof extractAndScore>[2]` for the older extract path; for the new queue-integration harness, build `MessageBatch<MemoryEvent>` objects: `{ messages: [{ ack, retry, attempts, body }] } as unknown as MessageBatch<MemoryEvent>`.
- A `mockEnv` literal with `AI: { run: async () => ({...}) }` for happy-path; in queue-integration we add real `env.WORKSPACE` (cloudflare:workers binding) and patched `env.ANALYTICS` (or undefined — analytics writes tolerate absent binding).

From packages/mcp-server/src/__tests__/tools-integration.test.ts (lines 39–210) — real-DO harness:
- `import { runInDurableObject } from "cloudflare:test"` and `import { env } from "cloudflare:workers"` give real WorkspaceDO instances under workerd.
- `env.WORKSPACE.idFromName("ws-<test-name>")` + `env.WORKSPACE.get(id)` resolve a real DO stub backed by SQLite.
- `runInDurableObject(stub, (_inst, state) => { state.storage.sql.exec("SELECT ...").toArray()[0] })` is the pattern for raw SQL state assertions inside the test process.
- `captureCallback("remember", workspace_id)` builds an isolated McpServer + binds the registerTools closure to a deterministic workspace; returns the captured callback for direct invocation.
- `patchEnvBindings()` (lines 93–151) installs vi.fn stubs for env.AI and env.VECTORIZE so the AI-03 / AI-04 paths don't hit remote Cloudflare bindings.

For the queue-integration tests we BOTH need: (a) the real `env.WORKSPACE` binding (so the queue handler's DO RPCs hit real SQLite state), and (b) the same `env.AI` patching strategy from tools-integration.test.ts (so `extractAndScore` returns a deterministic `TriageOutput` and the `routeByMemorability` branch is selectable).

Default export from packages/triage-worker/src/index.ts (after Plan 06-04):
```typescript
export default {
  async queue(batch: MessageBatch<MemoryEvent>, env: Env): Promise<void> { /* ... */ }
};
```
Invoke as: `await handler.queue(mockBatch, env)`. Because the handler is sequential `for...of`, multi-message batches process in order.

Routing predicate (existing — packages/triage-worker/src/memorability.ts):
- memorability > 0.8 → "store-normal" → updateBlockEnrichment
- 0.4 <= memorability <= 0.8 → "inbox" → moveToInbox
- memorability < 0.4 → "cold-storage" → moveToColdStorage

To select the routing branch in a test, control the `aiResp.response.memorability` field in the mocked `env.AI.run()` return value.

Cloudflare Queue mock shape (from extract.test.ts existing pattern):
```typescript
const message = { retry: vi.fn(), ack: vi.fn(), attempts: 0, body: memoryEvent };
const batch = { messages: [message] };
await handler.queue(batch as unknown as MessageBatch<MemoryEvent>, env as Env);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: queue-integration.test.ts — replay-twice idempotency + ingest_status lifecycle + orthogonality (PIP-01..06)</name>
  <files>packages/triage-worker/src/__tests__/queue-integration.test.ts</files>
  <read_first>
    - packages/triage-worker/src/__tests__/extract.test.ts (the vi.fn message mock pattern at lines 41–80; the mockEnv literal pattern at lines 50–56; the test naming convention; the import style)
    - packages/mcp-server/src/__tests__/tools-integration.test.ts (the real-DO harness at lines 39–210 — `runInDurableObject` import, `env.WORKSPACE.idFromName`, `patchEnvBindings` pattern; the `captureCallback` helper for invoking remember() to seed blocks)
    - packages/workspace-do/src/__tests__/helpers.test.ts (the per-helper test shape — how blocks are seeded via insertBlock + how raw SQL assertions are written inside `runInDurableObject`)
    - packages/triage-worker/src/index.ts (the queue handler default export after Plan 06-04 — confirm the signature `async queue(batch: MessageBatch<MemoryEvent>, env: Env): Promise<void>` and the import path `import handler from "../index.js"`)
    - packages/triage-worker/src/memorability.ts (the routing predicate so the test can compute the right `memorability` value to select each branch)
    - packages/triage-worker/src/schemas.ts (the `TriageOutput` shape so the mocked `env.AI.run` return value parses cleanly through `TRIAGE_JSON_SCHEMA`)
    - .planning/phases/06-async-pipeline/06-CONTEXT.md §"Specifics" §"Russell's job-search agent as the acceptance proof" — confirms that PIP-06 success means the block transitions to `ingest_status='enriched'` before Phase 7's recall-in-conversation-B test runs
    - .planning/phases/06-async-pipeline/06-PATTERNS.md §13 (the full queue-integration test skeleton with 6 it() blocks + the harness-combination notes + the test-infrastructure caveat about wrangler local Queue emulator being thin → use in-process mock instead)
  </read_first>
  <action>
    Create a NEW file `packages/triage-worker/src/__tests__/queue-integration.test.ts` covering Phase 6's behavioral invariants end-to-end via the in-process queue-handler mock pattern (NOT wrangler's local Queue emulator — per CONTEXT.md §"Test infrastructure", coverage is thin in wrangler@4; in-process is the established analog).

    File structure (follow extract.test.ts header conventions exactly):

    1. **File header doc-block** — cite PIP-01..06; describe the harness combination (vi.fn message mock from extract.test.ts + real-DO state from tools-integration.test.ts); cite the in-process-mock rationale; `@module @engram/triage-worker/__tests__/queue-integration` tag.

    2. **Imports** —
       - `import { runInDurableObject } from "cloudflare:test";`
       - `import { env } from "cloudflare:workers";`
       - `import { describe, it, expect, vi, beforeEach } from "vitest";`
       - `import type { MessageBatch } from "@cloudflare/workers-types";`
       - `import type { MemoryEvent } from "@engram/types";`
       - `import handler from "../index.js";` (default export from index.ts)

    3. **Helpers** —
       - `buildMessage(body: MemoryEvent, attempts = 0)` returns `{ ack: vi.fn(), retry: vi.fn(), attempts, body }` cast as the Cloudflare Message shape.
       - `buildBatch(messages)` returns `{ messages } as unknown as MessageBatch<MemoryEvent>`.
       - `buildEvent(overrides: Partial<MemoryEvent>)` returns a MemoryEvent with defaults: `id` = `crypto.randomUUID()`, `source: "mcp:claude"`, `content: "test content"`, `workspace_id: "ws-pip06-default"`, `timestamp: Date.now()`.
       - `seedBlockInDO(workspace_id, blockId, content)` — runs `insertBlock` on a real `env.WORKSPACE.get(...)` stub so the block exists with `ingest_status='pending'` before the queue handler fires. Use the same `insertBlock` RPC the production code uses (via the stub cast). The default `ingest_status='pending'` is set by V3_SQL.
       - `getIngestStatus(workspace_id, blockId)` — runs `runInDurableObject` + raw SQL to return the current `ingest_status` value.
       - `getInboxRows(workspace_id, blockId)` — same pattern, returns `SELECT * FROM inbox WHERE id = ?`.
       - `getBlockRow(workspace_id, blockId)` — `SELECT cold_storage, ingest_status, properties FROM blocks WHERE id = ?`.
       - `patchAI(memorability: number, extras?)` — mutates `env.AI` similar to tools-integration.test.ts pattern, but the response is a TRIAGE_JSON_SCHEMA-valid output: `{ response: { classified_type: "research_note", extracted_fields: {}, summary: "test", confidence: 0.9, memorability } }`. Vary `memorability` per test to select the routing branch.
       - `patchAIThrow(err)` — mutates `env.AI.run` to throw (for the non-429 throw branch test).
       - `patchAIInvalidJson()` — returns a Triage-shaped object missing a required field, so Zod fails to parse — drives the Zod-permanent-fail path.

    4. **`beforeEach`** — reset `vi` mock state, clear `env.AI` patches between tests.

    5. **Six describes / it blocks** —

       **(a) `it("PIP-03: replay-twice with same MemoryEvent.id produces exactly 1 inbox row")`**
       - workspace = `ws-pip03-replay`, memorability = 0.5 (inbox branch).
       - Seed a block (so the sync write that would have happened in `remember()` exists).
       - Build a single MemoryEvent. Wrap it in a batch. Call `handler.queue(batch, env)` TWICE with the same event id.
       - Assert: exactly 1 row in inbox table (`expect(getInboxRows(...).length).toBe(1)`).
       - Assert: exactly 1 block row (already true from seed — UNIQUE PK on block.id prevents duplicates).
       - Assert: `getIngestStatus` returns `'enriched'` after both runs (UPDATE idempotent).

       **(b) `it("PIP-06: memorability > 0.8 transitions ingest_status from pending → enriched")`**
       - workspace = `ws-pip06-storenormal`, memorability = 0.9.
       - Seed block. Confirm initial status is `'pending'` (`expect(getIngestStatus(...)).toBe("pending")`).
       - Run the queue handler. Assert status now `'enriched'`.
       - Assert: no inbox row created.
       - Assert: block.cold_storage === 0 (not cold).

       **(c) `it("PIP-06: memorability 0.4–0.8 transitions ingest_status pending → enriched AND writes inbox row")`**
       - workspace = `ws-pip06-inbox`, memorability = 0.6.
       - Seed block. Run handler. Assert status `'enriched'`. Assert inbox row exists with `memorability_score === 0.6`. Assert `inbox.id === block.id` (per moveToInbox composition).

       **(d) `it("PIP-06 / D-03: memorability < 0.4 transitions ingest_status pending → enriched AND sets cold_storage=1 (orthogonality)")`**
       - workspace = `ws-pip06-cold`, memorability = 0.2.
       - Seed block. Run handler. Assert status `'enriched'` AND `cold_storage === 1`. Negative assertion: status is NOT `'failed'`.

       **(e) `it("PIP-05: Zod parse failure on attempts >= 2 transitions ingest_status pending → failed AND acks")`**
       - workspace = `ws-pip05-zod`, AI returns malformed JSON (drives `parsed.success === false`).
       - Seed block. Build message with `attempts: 2` (last allowed attempt). Run handler.
       - Assert status now `'failed'`.
       - Assert: `properties` column parses as `{error: <string starting with "zod-parse-fail:">, failed_at: <number>}`.
       - Assert: `message.ack` was called; `message.retry` was NOT called.

       **(f) `it("PIP-05: DO RPC failure on attempts >= 2 calls markIngestFailed + acks (no silent drop)")`**
       - workspace = `ws-pip05-dorpc`, memorability = 0.9 (store-normal path).
       - Seed block. Construct a scenario where the DO RPC throws — easiest is to NOT seed the block (so `updateBlockEnrichment` throws `NotFoundError("block", id)`). Pass message with `attempts: 2`.
       - HOWEVER: if the block is not seeded, `markIngestFailed` ALSO throws NotFoundError. Per the Plan 06-04 secondary-try/catch pattern, the test expects the outer ack still fires and a `triage:mark-failed-also-threw` console.error is logged. Use `vi.spyOn(console, "error")` to assert.
       - Alternative scenario (cleaner): seed the block but stub the WorkspaceDO RPC method via `vi.spyOn` to throw a synthetic error on `updateBlockEnrichment`, then verify the catch path calls `markIngestFailed` successfully and acks. This is harder to set up with cloudflare:workers env — pick the simpler scenario at execute time.
       - Assert: `message.ack` was called; `message.retry` was NOT called.

    6. **Optional `(g)` integration sanity test** — `it("end-to-end: remember-style sync write + queue handler produces ingest_status='enriched'")` — exercises the full Phase 6 happy path without going through the producer-side `remember()` (because that requires the full mcp-server harness). Seed a block, build a corresponding MemoryEvent (id == block.id), run handler with memorability 0.9, assert status flips to 'enriched'. This duplicates (b) but acts as a smoke test for the full pipeline shape.

    Pattern from PATTERNS.md §13 — the test-infrastructure caveat: do NOT use wrangler's Queue local emulator. The default-exported handler can be invoked directly with mock MessageBatch objects; this avoids wrangler@4's thin Queue emulation coverage and stays aligned with the existing extract.test.ts harness style.

    Final file should be 200–400 lines (header + helpers + 6+ it blocks). Keep the helpers tight; the variation between tests is in `patchAI(...)` and the assertion phase, not in the harness boilerplate.
  </action>
  <verify>
    <automated>cd packages/triage-worker && npx tsc --noEmit 2>/dev/null; TSC_EXIT=$?; \
      test -f packages/triage-worker/src/__tests__/queue-integration.test.ts \
      && grep -q "PIP-03" packages/triage-worker/src/__tests__/queue-integration.test.ts \
      && grep -q "PIP-05" packages/triage-worker/src/__tests__/queue-integration.test.ts \
      && grep -q "PIP-06" packages/triage-worker/src/__tests__/queue-integration.test.ts \
      && grep -q "replay-twice" packages/triage-worker/src/__tests__/queue-integration.test.ts \
      && grep -q "ingest_status" packages/triage-worker/src/__tests__/queue-integration.test.ts \
      && grep -q "cold_storage" packages/triage-worker/src/__tests__/queue-integration.test.ts \
      && grep -q "markIngestFailed\\|ingest-failed" packages/triage-worker/src/__tests__/queue-integration.test.ts \
      && [ "$TSC_EXIT" = "0" ] \
      && cd packages/triage-worker && npx vitest run src/__tests__/queue-integration.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    `queue-integration.test.ts` exists with 6+ it blocks covering replay-twice idempotency, the 3 happy-path memorability branches (store-normal / inbox / cold-storage), and the 2 failure paths (Zod-permanent and DO-RPC-catch). All tests GREEN under @cloudflare/vitest-pool-workers. `tsc --noEmit` zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: tools-integration.test.ts — PIP-02 latency describe (remember() returns before queue send awaits)</name>
  <files>packages/mcp-server/src/__tests__/tools-integration.test.ts</files>
  <read_first>
    - packages/mcp-server/src/__tests__/tools-integration.test.ts (the file's existing structure — TOL-01..05 describes, `patchEnvBindings` helper at lines 93–151, `captureCallback` at lines 184–210, `parseEnvelope` at line 213); particularly the analog `it.skip("p50 latency under 400ms for default-verbosity (chunks) recall")` at lines 482–509 — same `Date.now()` / `latencies.sort()` / `Math.floor(...)` pattern that the PIP-02 test mirrors
    - packages/mcp-server/src/tools.ts (the remember() handler's new Phase 6 block — the `getCtx().waitUntil(...)` call site that this test verifies decouples response latency from queue.send latency)
    - .planning/phases/06-async-pipeline/06-CONTEXT.md §"Decisions" D-03 + §"Specifics" §"Russell's job-search agent as the acceptance proof" — Phase 6 success means remember() stays interactive-latency while async enrichment happens off-path
    - .planning/phases/06-async-pipeline/06-PATTERNS.md §14 (the full PIP-02 latency describe block with the `Object.defineProperty`-based env.INGEST_QUEUE.send patching, the `queueSendInvokedAt` tracking variable, the timing assertion, and the post-await sanity check that the waitUntil promise eventually fires)
  </read_first>
  <action>
    Extend `tools-integration.test.ts` with a NEW describe `PIP-02: remember() returns before async enrichment completes` placed AFTER the existing TOL-04 / TOL-05 describes (do NOT splice into the middle of an existing describe). The new describe contains ONE `it()` block.

    Required test shape (per PATTERNS.md §14):

    1. Establish a workspace name (`ws-pip02-latency`).
    2. Use `captureCallback("remember", workspace_id)` to get the live remember() handler with real env wiring.
    3. **Patch `env.INGEST_QUEUE.send` with a deliberate delay**:
       - `let queueSendInvokedAt: number | null = null;`
       - Install on `(env as any).INGEST_QUEUE = { send: async (_evt) => { await new Promise(r => setTimeout(r, 200)); queueSendInvokedAt = Date.now(); } };`
       - The 200ms delay simulates a slow queue.send; the test asserts that `remember()` resolves in well under 200ms (proving decoupling).
    4. Record `rememberStart = Date.now()`. Call `await rememberCb({ content: "PIP-02 latency probe" }, {});`. Record `rememberDuration = Date.now() - rememberStart;`.
    5. **Critical assertion** — `expect(rememberDuration).toBeLessThan(150);` (remember() resolves in < 150ms; the 50ms buffer below the 200ms queue.send delay is the proof that ctx.waitUntil decouples).
    6. **Optional secondary assertion** — `queueSendInvokedAt may still be null at this point` (the inner queue.send promise may not have resolved yet; that's the proof). Then `await new Promise(r => setTimeout(r, 500));` and assert `expect(queueSendInvokedAt).not.toBeNull();` (the waitUntil promise eventually fires).

    Sanity reads to honor:
    - `captureCallback` returns a closure; calling it does NOT actually await env.INGEST_QUEUE.send synchronously — the waitUntil pattern hands the promise to the runtime. In the test env (vitest), there is no "runtime" — the IIFE inside waitUntil is a free-floating Promise. The test must let microtasks run (the `await new Promise(r => setTimeout(r, 500))` does this) before asserting `queueSendInvokedAt` is set.
    - Cleanup: after the test, remove or reset `env.INGEST_QUEUE` so subsequent tests don't see the patched binding. Use the existing `afterEach` block at lines 157–171 — extend it to also clear `(env as any).INGEST_QUEUE`.

    Sanity caveat: the existing `patchEnvBindings()` does NOT patch INGEST_QUEUE. The new test installs its own patch (Object.defineProperty or direct assignment) and the afterEach cleanup ensures isolation from TOL-01..05 / TOL-07 tests that run after.

    Note: if vitest's `setTimeout` resolution is too noisy to assert `<150ms` reliably on slow CI machines, raise the assertion threshold to `<180ms` (still well below the 200ms inner-send delay). The proof is the gap between `rememberDuration` and the inner-send latency, not an absolute number.
  </action>
  <verify>
    <automated>grep -q "PIP-02" packages/mcp-server/src/__tests__/tools-integration.test.ts \
      && grep -q "queueSendInvokedAt" packages/mcp-server/src/__tests__/tools-integration.test.ts \
      && grep -q "INGEST_QUEUE" packages/mcp-server/src/__tests__/tools-integration.test.ts \
      && cd packages/mcp-server && npx tsc --noEmit 2>/dev/null; TSC_EXIT=$?; \
      [ "$TSC_EXIT" = "0" ] \
      && cd packages/mcp-server && npx vitest run src/__tests__/tools-integration.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    `tools-integration.test.ts` has a new PIP-02 describe with the latency-decoupling test. The test passes (remember() resolves in <150ms despite a 200ms queue.send delay) under workerd. No regression to TOL-01..05 / TOL-07 / AI-08 describes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → real WorkspaceDO under workerd | `runInDurableObject` invokes real SQL against the test pool's isolated SQLite state. No cross-test contamination because each test uses a unique `idFromName(...)`. |
| `vi.fn()` message mock → queue handler | The mock controls `attempts`, `ack`, `retry` deterministically. No external Queue runtime is involved — the handler is invoked in-process. |
| Patched `env.AI` / `env.INGEST_QUEUE` | Both bindings are mutated on the `env` proxy via Object.defineProperty or direct assignment. The afterEach hook ensures isolation across tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-TEST-LEAK | Information Disclosure | env binding patches across tests | mitigate | Both queue-integration.test.ts and the PIP-02 describe extension use `beforeEach` / `afterEach` to reset env patches. Test isolation is verified by running the full suite multiple times in sequence — no test order dependence. |
| T-06-05-FLAKY-LATENCY | Repudiation | PIP-02 latency assertion under slow CI | mitigate | The 200ms inner delay + 150ms outer threshold gives a 50ms safety buffer. If CI surfaces flakiness, raise the inner delay to 500ms and the threshold to 300ms; the proof (gap > 0) is what matters. |
| T-06-05-MOCK-DRIFT | Tampering | TRIAGE_JSON_SCHEMA shape mocked in `patchAI` | mitigate | The mock builder returns a shape matching the current `TriageOutput` zod schema. If the schema changes in a future plan, the mock builder must be updated in lockstep — flagged as a known carry-forward in the SUMMARY. |
| T-06-05-FALSE-GREEN | Repudiation | DO-RPC failure test (Test 1f) reaching the markIngestFailed RPC | mitigate | The test scenario (un-seeded block → updateBlockEnrichment throws NotFoundError → markIngestFailed also throws NotFoundError → fall-through to ack) is the worst-case but valid. The test asserts `message.ack` was called AND a `triage:mark-failed-also-threw` console.error fired — these prove the secondary try/catch wrapper works as designed, even though no SQLite row carries `ingest_status='failed'` (because the block doesn't exist). An alternative cleaner test (seed block + vi.spyOn the RPC method) is harder to set up but produces a clearer green signal; executor picks. |
| T-06-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. Slopcheck N/A. |

</threat_model>

<verification>
After both tasks complete:

1. `cd packages/triage-worker && npx vitest run --reporter=verbose` exits 0 (extract.test.ts + queue-integration.test.ts + evals/ all GREEN).
2. `cd packages/mcp-server && npx vitest run --reporter=verbose` exits 0 (TOL-* + new PIP-02 describe GREEN, no regressions).
3. `cd packages/workspace-do && npx vitest run --reporter=verbose` exits 0 (no regression from 06-01 / 06-03).
4. `npm run lint` (root) exits 0.
5. `npm run typecheck` (or per-package `tsc --noEmit`) exits 0 across all packages.
6. `npm run lint:wrangler` exits 0 (Phase 1 / 06-02 invariant preserved).
7. `npm run lint:blockconcurrency` exits 0 (Phase 2 STO-10 invariant preserved — no new env.* / fetch( / await this.ai inside the WorkspaceDO constructor; this plan touches no DO constructor code).
8. End-to-end sanity: run the full `npm test` from the root once to confirm cross-package state. Note the run time delta vs. pre-Phase-6 — if the queue-integration.test.ts adds > 30s, the test isolation may be too aggressive (consider reducing the number of `runInDurableObject` invocations per test).

Common failure modes:
- queue-integration tests fail because handler.queue's MessageBatch type cast is wrong: ensure `as unknown as MessageBatch<MemoryEvent>` (not `as MessageBatch<MemoryEvent>` directly — the vi.fn shape is structurally different).
- PIP-02 test flaky under slow CI: raise the inner-delay-vs-threshold gap (200/150 → 500/300).
- Triage Worker tests fail with "no WORKSPACE binding": ensure `wrangler.test.jsonc` for triage-worker carries the `WORKSPACE` binding (declared in 06-02 / Phase 5; verify via grep).
- Test (1f) reports markIngestFailed never called: the test scenario picked a non-seeded block, but the catch path checks `attempts >= 2` BEFORE calling markIngestFailed — confirm the mock's `attempts: 2` is properly threaded through.
</verification>

<success_criteria>
- All Phase 6 behavioral invariants (PIP-01..06 + D-03 orthogonality + IP-1 idempotency) are verified by automated tests.
- `npm test` (root, across all packages) exits 0.
- The Phase 6 plumbing demonstrably works under workerd: a `remember()` call returns fast (PIP-02), a Triage Worker consumer happy-path enriches the block (PIP-04/06), and the permanent-failure paths flip ingest_status to 'failed' (PIP-05) without silent drops.
- Phase 6 is ready for `/gsd:verify-work 6` and (after that passes) for Phase 7 (Deploy + Acceptance) where the end-to-end acceptance test (remember in conversation A → recall in conversation B 1+ hour later) becomes the final proof that the async pipeline works in production.
</success_criteria>

<output>
Create `.planning/phases/06-async-pipeline/06-05-SUMMARY.md` when done. Summary should record:
- Per-test result (which it() blocks GREEN, any flakiness observed and the mitigation).
- The chosen DO-RPC failure test scenario for Test (1f) — un-seeded block vs vi.spyOn'd RPC — and why.
- The PIP-02 timing delta (`rememberDuration` vs the 200ms inner-send delay) observed in the test run, as evidence the ctx.waitUntil decoupling works.
- Open items for Phase 7 hand-off:
  - The v0.2 stuck-pending sweep Cron Worker (D-03 known limitation — ctx.waitUntil fire-and-forget means a Cloudflare Queues outage leaves blocks at ingest_status='pending' indefinitely; v0.2 owns the sweep).
  - The v0.2 inbox UI for partial-failure visibility (`ingest_status='failed'` rows are the v0.2 starting point).
  - The v0.2 DLQ queue + replay tool (deferred from v0.1 per D-03; v0.2 can add `engram-ingest-dlq` if real traffic shows the need).
  - The v0.2 conflict-detection-with-measured-precision (D-01 deferral; the pending todo file carries the validation harness).
- Confirmation that the Phase 6 SUMMARY (this file + 06-04-SUMMARY.md + 06-03-SUMMARY.md + 06-02-SUMMARY.md + 06-01-SUMMARY.md) covers all 6 success criteria from ROADMAP.md §"Phase 6: Async Pipeline" (after the PIP-04 amendment from Plan 06-01 lands).
- Ready signal: "Phase 6 ready for /gsd:verify-work 6."
</output>
</content>
