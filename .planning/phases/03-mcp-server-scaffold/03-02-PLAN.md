---
phase: 03-mcp-server-scaffold
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - packages/mcp-server/src/schemas.ts
  - packages/mcp-server/src/error-mapping.ts
  - packages/mcp-server/src/__tests__/schemas.test.ts
autonomous: true
requirements:
  - MCP-06
tags:
  - mcp-server
  - zod-schemas
  - defense-in-depth
  - error-mapping

must_haves:
  truths:
    - "All 5 zod input schemas exist as exported constants — RememberInputSchema, RecallInputSchema, SearchInputSchema, ForgetInputSchema, IngestInputSchema"
    - "All 5 zod input schemas live in packages/mcp-server/src/schemas.ts as the single source of truth (no build-step type generation, no inline schema definitions elsewhere) (D-06)"
    - "NONE of the 5 schemas declares a workspace_id field (defense-in-depth structural contract T-03-DD-IN)"
    - "Each schema has a corresponding `z.infer<typeof X>` exported type alias"
    - "mapToMcpError(err) returns McpError unchanged for McpError input, McpError(InvalidParams) for NotFoundError input, McpError(InternalError) with sanitized message otherwise"
    - "sanitize() strips /Users/... paths and 32+ char hex strings from leaked error messages"
    - "schemas.test.ts is GREEN — the 5 structural assertions + defense-in-depth check pass"
  artifacts:
    - path: "packages/mcp-server/src/schemas.ts"
      provides: "Single source of truth for the 5 v0.1 tool input schemas"
      min_lines: 40
      exports: ["RememberInputSchema", "RecallInputSchema", "SearchInputSchema", "ForgetInputSchema", "IngestInputSchema", "RememberInput", "RecallInput", "SearchInput", "ForgetInput", "IngestInput"]
    - path: "packages/mcp-server/src/error-mapping.ts"
      provides: "mapToMcpError function — exception → McpError converter"
      min_lines: 20
      exports: ["mapToMcpError"]
  key_links:
    - from: "packages/mcp-server/src/schemas.ts"
      to: "zod"
      via: "import { z } from \"zod\""
      pattern: "from \"zod\""
    - from: "packages/mcp-server/src/error-mapping.ts"
      to: "@engram/workspace-do (NotFoundError)"
      via: "import { NotFoundError } from \"@engram/workspace-do\""
      pattern: "NotFoundError"
    - from: "packages/mcp-server/src/error-mapping.ts"
      to: "@modelcontextprotocol/sdk (McpError, ErrorCode)"
      via: "import { McpError, ErrorCode } from \"@modelcontextprotocol/sdk/types.js\""
      pattern: "ErrorCode"
---

<objective>
Land the two pure-data/pure-function modules that have no inter-dependency: `schemas.ts` (5 zod schemas with the defense-in-depth structural invariant baked in) and `error-mapping.ts` (the `mapToMcpError` helper Phase 4 tool handlers will import). Turn `schemas.test.ts` from RED (pending) to GREEN.

Purpose: These two files have ZERO dependency on each other and zero dependency on Wave 2 (tools.ts, oauth.ts) — they can land in parallel within Wave 1. Both encode load-bearing cross-phase contracts: schemas.ts is the structural enforcement of MCP-05/STO-07 (no workspace_id in tool input), error-mapping.ts is the Phase 4 error-translation convention.

Output: 2 new source files (`schemas.ts`, `error-mapping.ts`); 1 test file updated (`schemas.test.ts`) from RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-mcp-server-scaffold/03-CONTEXT.md
@.planning/phases/03-mcp-server-scaffold/03-RESEARCH.md
@.planning/phases/03-mcp-server-scaffold/03-PATTERNS.md
@.planning/phases/03-mcp-server-scaffold/03-01-SUMMARY.md
@packages/workspace-do/src/errors.ts
@packages/workspace-do/src/index.ts
@shared/schema/src/system-types.ts
@packages/mcp-server/src/__tests__/schemas.test.ts

<interfaces>
<!-- Key contracts extracted from RESEARCH.md and codebase. -->

From @modelcontextprotocol/sdk/types.js (installed):
```typescript
export class McpError extends Error {
  code: ErrorCode;
  data?: unknown;
  constructor(code: ErrorCode, message: string, data?: unknown);
}
export enum ErrorCode {
  InvalidRequest = -32600,
  MethodNotFound = -32601,
  InvalidParams = -32602,
  InternalError = -32603,
  // ...
}
```

From packages/workspace-do/src/errors.ts (Phase 2):
```typescript
export class NotFoundError extends Error {
  resource: string;
  id: string;
  // ...
}
```

From @engram/types (for cross-checking shape parity — schemas may diverge where MCP tool input differs from canonical Memory shape):
```typescript
type Memory = { id, type, content, summary, properties, embedding_id, scope, project_id, source, confidence, created_at, updated_at };
type MemoryEvent = { id, source, content, hint?, context?, workspace_id, timestamp };
```

Phase 3 zod schema target shapes (verbatim from RESEARCH.md Example 1):
- RememberInputSchema: { content: string-min-1, type?, project?, tags?: string[], source?, expires?: datetime }
- RecallInputSchema: { query: string-min-1, types?: string[], project?, scope?: "personal"|"project"|"org", limit?: int 1-100, since?: datetime, until?: datetime }
- SearchInputSchema: { query: string-min-1, filters?: Record<string, unknown> }
- ForgetInputSchema: { id: string-min-1, cascade?: boolean }
- IngestInputSchema: { source: string-min-1, type?, project?, priority?: "fast"|"deep", threshold?: number 0-1 }

CRITICAL DEFENSE-IN-DEPTH CONTRACT: NONE of these schemas declares workspace_id. The workspace is sourced from JWT props at the handler layer (Wave 2).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tool input | Tool input is fully untrusted; zod schemas are the first validation layer (auto-invoked by registerTool before handler). |
| WorkspaceDO exception → MCP client | Exceptions thrown inside WorkspaceDO methods (NotFoundError, McpError from assertOwnsWorkspace, unexpected runtime errors) cross the trust boundary back to the client; error-mapping.ts sanitizes anything that isn't already an McpError. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-DD-IN | Tampering / Elevation of Privilege | packages/mcp-server/src/schemas.ts | mitigate | NONE of the 5 schemas declares a workspace_id field. Verified structurally by schemas.test.ts (`Object.keys(SchemaShape).indexOf('workspace_id') === -1` for each schema). Inherits Phase 2 MT-1/STO-07 invariant — workspace MUST come from JWT props, not tool input. |
| T-03-DD-RT | Tampering | (deferred to Plan 03 tools.ts where the runtime routing happens) | accept-here | This plan's schemas.ts file is the STRUCTURAL guard; the runtime guard lives in tools.ts (Plan 03). Listed in this threat model as inherited from the phase, not mitigated here. |
| T-03-LEAK | Information Disclosure | packages/mcp-server/src/error-mapping.ts (sanitize function) | mitigate | sanitize() strips `/Users/[^\s]+` filesystem paths and 32+ char hex strings (potential token/secret leak). 500-char truncation cap. Verified by unit test asserting sanitize("Error at /Users/r/secret.ts:43") returns no "/Users/" substring. |
| T-03-SC | Tampering | npm hoisted node_modules/zod, node_modules/@modelcontextprotocol/sdk | mitigate | Both [OK] per slopcheck (RESEARCH.md §Package Legitimacy Audit). Already installed Plan 01. No additional gate. |
| T-03-JWT | Spoofing | (deferred to Plan 04 oauth.ts) | accept-here | Listed for phase-level threat-model continuity; Plan 04 mitigates. |
| T-03-PROPS | Spoofing | (deferred to Plan 04 oauth.ts) | accept-here | Listed for phase-level threat-model continuity; Plan 04 mitigates. |
| T-03-KV-LEAK | Information Disclosure | (deferred to Plan 04 oauth.ts) | accept-here | Listed for phase-level threat-model continuity; Plan 04 mitigates. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create packages/mcp-server/src/schemas.ts (5 zod schemas + 5 z.infer aliases)</name>
  <files>packages/mcp-server/src/schemas.ts</files>
  <read_first>
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Code Examples → Example 1 (lines 731-794) — verbatim schema definitions (target body)
    - .planning/phases/03-mcp-server-scaffold/03-PATTERNS.md §1 (lines 60-87) — mandatory file header comment block and analog choice
    - .planning/phases/03-mcp-server-scaffold/03-CONTEXT.md §D-06 — single source of truth decision
    - shared/schema/src/system-types.ts (sibling style — data-as-constants module with JSDoc and named exports)
    - CLAUDE.md §"Universal Response Envelope" — confirms tool input contracts vs response envelope (this plan does only inputs)
  </read_first>
  <behavior>
    - Test 1 (`schemas.test.ts` defense-in-depth): for each of the 5 schemas, `Object.keys(Schema.shape).indexOf('workspace_id') === -1` (no schema accepts workspace_id)
    - Test 2 (`schemas.test.ts` happy-path per schema): RememberInputSchema.parse({content: "hello"}) succeeds; RecallInputSchema.parse({query: "x"}) succeeds; SearchInputSchema.parse({query: "x"}) succeeds; ForgetInputSchema.parse({id: "abc"}) succeeds; IngestInputSchema.parse({source: "https://example.com"}) succeeds
    - Test 3 (`schemas.test.ts` rejection paths): empty `content` rejected by RememberInputSchema; non-positive limit rejected by RecallInputSchema; threshold out of [0,1] rejected by IngestInputSchema; non-datetime `expires` rejected by RememberInputSchema; non-enum scope rejected by RecallInputSchema
    - Test 4 (type-only): `RememberInput` exported type alias resolves to `z.infer<typeof RememberInputSchema>` (same for the other four)
  </behavior>
  <action>
    Create packages/mcp-server/src/schemas.ts. Copy the mandatory header comment from 03-PATTERNS.md §1 lines 70-82 VERBATIM (the "CRITICAL DEFENSE-IN-DEPTH CONTRACT" block — it is load-bearing for MCP-05/STO-07/T-03-DD-IN). Add a leading JSDoc block (mirror packages/workspace-do/src/errors.ts header style: one-line summary → cross-phase contract notes → design notes → `@module` tag): the module name is `@engram/mcp-server/schemas`. Implement the 5 schemas exactly as defined in 03-RESEARCH.md §Example 1 lines 749-793: RememberInputSchema (content min-1, type/project/source string?, tags string[]?, expires datetime?), RecallInputSchema (query min-1, types string[]?, project string?, scope enum personal|project|org, limit int positive max 100, since/until datetime?), SearchInputSchema (query min-1, filters Record<string, unknown>?), ForgetInputSchema (id min-1, cascade boolean?), IngestInputSchema (source min-1, type/project string?, priority enum fast|deep, threshold number 0-1?). After EACH `export const XxxInputSchema = z.object({...})`, add the corresponding `export type XxxInput = z.infer<typeof XxxInputSchema>;`. Use `import { z } from "zod"` at the top. **CRITICAL: do not declare a workspace_id field on ANY schema** — this is the defense-in-depth structural invariant the schemas.test.ts contract enforces. The schema field order should mirror the canonical @engram/types shapes where possible for downstream readability (content first on Remember; query first on Recall/Search; id first on Forget; source first on Ingest).
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; test -f packages/mcp-server/src/schemas.ts &amp;&amp; grep -q "export const RememberInputSchema" packages/mcp-server/src/schemas.ts &amp;&amp; grep -q "export const RecallInputSchema" packages/mcp-server/src/schemas.ts &amp;&amp; grep -q "export const SearchInputSchema" packages/mcp-server/src/schemas.ts &amp;&amp; grep -q "export const ForgetInputSchema" packages/mcp-server/src/schemas.ts &amp;&amp; grep -q "export const IngestInputSchema" packages/mcp-server/src/schemas.ts &amp;&amp; (! grep -v '^[[:space:]]*//' packages/mcp-server/src/schemas.ts | grep -q "workspace_id") &amp;&amp; npx tsc -p packages/mcp-server/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/mcp-server/src/schemas.ts` exists
    - File contains `import { z } from "zod"`
    - File contains all 5 named exports: `export const RememberInputSchema`, `export const RecallInputSchema`, `export const SearchInputSchema`, `export const ForgetInputSchema`, `export const IngestInputSchema`
    - File contains all 5 type aliases: `export type RememberInput`, `export type RecallInput`, `export type SearchInput`, `export type ForgetInput`, `export type IngestInput`
    - File contains the mandatory header comment string `CRITICAL DEFENSE-IN-DEPTH CONTRACT` (verbatim — load-bearing for future contributors)
    - Stripping JS comment lines (`grep -v '^[[:space:]]*//'`) and searching for the literal token `workspace_id` returns ZERO matches (T-03-DD-IN structural check — the contract phrase appears only in the comment header, never in code)
    - `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` exits 0
    - Each `z.object({...})` literal contains at least one `.min(1)` or analogous constraint on the primary required field (content/query/id/source)
  </acceptance_criteria>
  <done>
    schemas.ts exists with 5 zod schemas + 5 type aliases. Defense-in-depth structural contract is encoded (no `workspace_id` field appears in non-comment code). Header comment explicitly documents the contract for future contributors. TypeScript compiles cleanly. Wave 2 tools.ts can import all 5 schemas.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create packages/mcp-server/src/error-mapping.ts (mapToMcpError + sanitize)</name>
  <files>packages/mcp-server/src/error-mapping.ts</files>
  <read_first>
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Code Examples → Example 2 (lines 796-832) — verbatim body
    - .planning/phases/03-mcp-server-scaffold/03-PATTERNS.md §4 (lines 244-296) — JSDoc style + mandatory header
    - .planning/phases/03-mcp-server-scaffold/03-CONTEXT.md §Claude's Discretion → "Error mapping convention"
    - packages/workspace-do/src/errors.ts (sibling — small single-purpose module style for the JSDoc header)
    - packages/workspace-do/src/index.ts lines 139-146 (the McpError shape that mapToMcpError MUST pass through unchanged)
  </read_first>
  <behavior>
    - Test 1: `mapToMcpError(new McpError(ErrorCode.InvalidRequest, "x"))` returns the SAME McpError instance unchanged (referential equality)
    - Test 2: `mapToMcpError(new NotFoundError("block", "abc"))` returns an McpError with `.code === ErrorCode.InvalidParams (-32602)` and message containing "abc"
    - Test 3: `mapToMcpError(new Error("oops"))` returns an McpError with `.code === ErrorCode.InternalError (-32603)` and sanitized message
    - Test 4: `mapToMcpError("string error")` returns an McpError with `.code === ErrorCode.InternalError` and message "Internal error"
    - Test 5: sanitize("Error at /Users/r/secret.ts:43") returns no "/Users/" substring (replaced by "<path>")
    - Test 6: sanitize("token abc1234567890abcdef1234567890abcdef") returns no 32+ char hex sequence (replaced by "<hex>")
    - Test 7: sanitize(string longer than 500 chars) returns at most 500 chars
  </behavior>
  <action>
    Create packages/mcp-server/src/error-mapping.ts. Top-of-file JSDoc per 03-PATTERNS.md §4 lines 277-294 (one-line summary → cross-phase contract → design notes including "pass-through preserves Phase 2 STO-07 InvalidRequest", "NotFoundError → InvalidParams (-32602)", "anything else → InternalError (-32603) sanitized" → `@module @engram/mcp-server/error-mapping`). Imports: `import { McpError, ErrorCode } from "@modelcontextprotocol/sdk/types.js"` and `import { NotFoundError } from "@engram/workspace-do"`. Exports: ONE function `mapToMcpError(err: unknown): McpError` and ONE internal `function sanitize(message: string): string`. Body per RESEARCH.md §Example 2 verbatim: if `err instanceof McpError` → return unchanged; if `err instanceof NotFoundError` → return `new McpError(ErrorCode.InvalidParams, err.message)`; otherwise extract `err instanceof Error ? err.message : "Internal error"` and return `new McpError(ErrorCode.InternalError, sanitize(message))`. The sanitize function: `message.replace(/\/Users\/[^\s]+/g, "<path>").replace(/[a-f0-9]{32,}/g, "<hex>").slice(0, 500)`. No default export. Make `sanitize` non-exported (file-local) — it's an internal detail; only mapToMcpError is the public surface. `mapToMcpError` is exported as a named export. Update `schemas.test.ts` is NOT this task's job — there is no error-mapping.test.ts in Wave 0 scope; the assertions for this module live in schemas.test.ts as supplementary "error-mapping.ts behavior" describe block, OR a new error-mapping.test.ts file MAY be created here if the planner prefers — pick whichever is cleaner; if creating a new test file, mirror schemas.test.ts structure and update vitest config include glob is unnecessary since it already globs `src/__tests__/**/*.test.ts`. Recommendation: ADD a new `packages/mcp-server/src/__tests__/error-mapping.test.ts` with the 7 behavior assertions above — keep the test files single-concern, parallel to the Phase 2 convention. The vitest pool will discover it automatically.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; test -f packages/mcp-server/src/error-mapping.ts &amp;&amp; grep -q "export function mapToMcpError" packages/mcp-server/src/error-mapping.ts &amp;&amp; grep -q "function sanitize" packages/mcp-server/src/error-mapping.ts &amp;&amp; grep -q "NotFoundError" packages/mcp-server/src/error-mapping.ts &amp;&amp; grep -q "ErrorCode.InvalidParams" packages/mcp-server/src/error-mapping.ts &amp;&amp; grep -q "ErrorCode.InternalError" packages/mcp-server/src/error-mapping.ts &amp;&amp; grep -q "\\.replace(/.*Users.*/" packages/mcp-server/src/error-mapping.ts &amp;&amp; grep -q "slice(0, 500)" packages/mcp-server/src/error-mapping.ts &amp;&amp; npx tsc -p packages/mcp-server/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/mcp-server/src/error-mapping.ts` exists
    - File contains `export function mapToMcpError(err: unknown): McpError` (or equivalent named-export form)
    - File contains a `sanitize` function (internal, non-exported — `function sanitize(message: string): string`)
    - File imports `McpError` and `ErrorCode` from `"@modelcontextprotocol/sdk/types.js"`
    - File imports `NotFoundError` from `"@engram/workspace-do"`
    - File contains the literal substring `ErrorCode.InvalidParams` (used for NotFoundError mapping)
    - File contains the literal substring `ErrorCode.InternalError` (used for unknown errors)
    - File contains the literal `slice(0, 500)` (sanitize truncation cap)
    - File contains a `/Users/[^\s]+/g` or equivalent regex literal (sanitize filesystem-path scrubber — T-03-LEAK mitigation)
    - File contains a `[a-f0-9]{32,}/g` or equivalent regex literal (sanitize hex-string scrubber — T-03-LEAK mitigation)
    - `mapToMcpError` is NOT default-exported (named export only — matches established repo convention)
    - `sanitize` is NOT exported (file-local helper)
    - `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` exits 0
  </acceptance_criteria>
  <done>
    error-mapping.ts exists with mapToMcpError + sanitize. Pass-through semantics preserve Phase 2 STO-07 McpErrors. NotFoundError maps to InvalidParams. Unknown errors map to InternalError with sanitized message (filesystem paths and hex strings scrubbed). Phase 4 tool handlers can import and use `mapToMcpError(err)` uniformly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Turn schemas.test.ts (+ error-mapping.test.ts if created) from RED to GREEN</name>
  <files>packages/mcp-server/src/__tests__/schemas.test.ts, packages/mcp-server/src/__tests__/error-mapping.test.ts</files>
  <read_first>
    - packages/mcp-server/src/__tests__/schemas.test.ts (Wave 0 RED stub — replace it.skip with it() bodies)
    - packages/mcp-server/src/schemas.ts (just-created — Task 1)
    - packages/mcp-server/src/error-mapping.ts (just-created — Task 2)
    - packages/workspace-do/src/__tests__/defense-in-depth.test.ts (lines 95-200) — canonical `it(...)` shape with try/catch + McpError assertions
    - packages/workspace-do/src/__tests__/seeding.test.ts (lines 1-50) — pure-unit test shape (no `runInDurableObject` shell needed for these schema/error tests)
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Validation Architecture (lines 971-1016)
  </read_first>
  <behavior>
    - Test 1 (`npm test -- schemas`): exits 0; output reports ≥ 11 passing tests (5 schema-shape + 5 happy-path + 1 defense-in-depth aggregate, depending on grouping)
    - Test 2 (`npm test -- error-mapping`): exits 0; output reports ≥ 7 passing tests (pass-through, NotFoundError mapping, unknown-error mapping, string-error mapping, sanitize-path, sanitize-hex, sanitize-truncation)
    - Test 3: NO `it.skip` or `it.todo` markers remain in either test file (every test executes)
    - Test 4: Test file imports work — `import { RememberInputSchema } from "../schemas.js"` resolves and the import is consumed in the body
  </behavior>
  <action>
    Replace the it.skip/it.todo stubs in `packages/mcp-server/src/__tests__/schemas.test.ts` with real `it(...)` bodies. Imports: `import { describe, it, expect } from "vitest"` + `import { RememberInputSchema, RecallInputSchema, SearchInputSchema, ForgetInputSchema, IngestInputSchema } from "../schemas.js"`. Tests to land (group with `describe(...)` blocks): (1) `describe('defense-in-depth (T-03-DD-IN)')` containing `it.each([...5 schemas...])('schema $name does not accept workspace_id field', ({ schema }) => { expect(Object.keys(schema.shape)).not.toContain('workspace_id'); })`. (2) `describe('happy paths')` containing `it('RememberInputSchema parses valid input', () => { expect(RememberInputSchema.safeParse({ content: 'hello' }).success).toBe(true); })` and analogous for the other 4. (3) `describe('rejection paths')` with one rejection per schema: empty content rejected by Remember; non-datetime expires rejected by Remember; non-enum scope rejected by Recall; non-positive limit rejected by Recall; threshold > 1 rejected by Ingest. Use `safeParse` (returns `{ success: boolean }`) instead of `parse` to avoid try/catch boilerplate. Create `packages/mcp-server/src/__tests__/error-mapping.test.ts` from scratch (if Task 2 chose to colocate the assertions in schemas.test.ts, this file may be skipped — but recommended is a dedicated file): imports `import { describe, it, expect } from "vitest"`, `import { McpError, ErrorCode } from "@modelcontextprotocol/sdk/types.js"`, `import { NotFoundError } from "@engram/workspace-do"`, `import { mapToMcpError } from "../error-mapping.js"`. Tests: pass-through (referential equality with `expect(mapToMcpError(input)).toBe(input)` for an McpError input), NotFoundError → InvalidParams (with `.code === -32602`), Error → InternalError (with `.code === -32603`), string/unknown → InternalError with message "Internal error", sanitize strips `/Users/...` (assert no "/Users/" in message), sanitize strips 32+ char hex (assert no `[a-f0-9]{32}` regex match in message), sanitize truncates to ≤ 500 chars. Each `it(...)` invocation is concrete (no it.skip / it.todo remain). After updates, run `npm test --workspace=@engram/mcp-server -- --run` from repo root and confirm ≥ 11 + 7 passing tests with 0 failures and 0 skipped/pending.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; (! grep -q "it\\.skip\\|it\\.todo" packages/mcp-server/src/__tests__/schemas.test.ts) &amp;&amp; (! test -f packages/mcp-server/src/__tests__/error-mapping.test.ts || ! grep -q "it\\.skip\\|it\\.todo" packages/mcp-server/src/__tests__/error-mapping.test.ts) &amp;&amp; npm test --workspace=@engram/mcp-server -- --run 2>&amp;1 | tee /tmp/test-out.txt &amp;&amp; grep -qE "(Tests +([1-9]\\d* )+passed|✓.*schemas)" /tmp/test-out.txt</automated>
  </verify>
  <acceptance_criteria>
    - `packages/mcp-server/src/__tests__/schemas.test.ts` contains NO `it.skip(` and NO `it.todo(` (regex search returns 0)
    - `packages/mcp-server/src/__tests__/schemas.test.ts` contains AT LEAST one `expect(` invocation per top-level `it(`
    - `packages/mcp-server/src/__tests__/schemas.test.ts` references all 5 schema names: RememberInputSchema, RecallInputSchema, SearchInputSchema, ForgetInputSchema, IngestInputSchema
    - `packages/mcp-server/src/__tests__/schemas.test.ts` contains at least one structural assertion: `not.toContain('workspace_id')` or `indexOf('workspace_id')` (covers T-03-DD-IN)
    - If `error-mapping.test.ts` exists: it contains `mapToMcpError`, `NotFoundError`, `ErrorCode.InvalidParams`, `ErrorCode.InternalError`, and assertions for sanitize behavior (paths + hex)
    - `npm test --workspace=@engram/mcp-server -- --run` exits 0
    - The vitest output contains "passed" with a number ≥ 11 (or shows individual checkmarks for ≥ 11 it() calls)
    - The vitest output contains 0 failed tests
  </acceptance_criteria>
  <done>
    schemas.test.ts (and optional error-mapping.test.ts) are GREEN. Every it.skip from Wave 0 is replaced with a real assertion. The defense-in-depth structural invariant (T-03-DD-IN) is now CI-enforced — any future change adding workspace_id to any schema fails the suite immediately.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks land:

1. **Full mcp-server test suite green:**
   ```bash
   cd /Users/rmoore/Workspaces/engram
   npm test --workspace=@engram/mcp-server -- --run
   ```
   Exit 0. Output shows ≥ 11 schema tests + ≥ 7 error-mapping tests passing. Zero failed. Zero skipped.

2. **TypeScript compiles cleanly:**
   ```bash
   npx tsc -p packages/mcp-server/tsconfig.json --noEmit
   ```
   Exit 0.

3. **Structural defense-in-depth check:**
   ```bash
   grep -v '^[[:space:]]*//' packages/mcp-server/src/schemas.ts | grep -c "workspace_id"
   ```
   Returns 0 (only the comment header references the contract phrase).

4. **Phase 2 tests still pass (no cross-package regression):**
   ```bash
   npm test --workspaces -- --run
   ```
   Exit 0; workspace-do's 9+ defense-in-depth tests still GREEN.
</verification>

<success_criteria>
- `packages/mcp-server/src/schemas.ts` exists with 5 zod schemas + 5 z.infer aliases; none declares workspace_id (T-03-DD-IN structural invariant)
- `packages/mcp-server/src/error-mapping.ts` exists with mapToMcpError + internal sanitize; sanitize strips paths + hex strings (T-03-LEAK)
- `schemas.test.ts` GREEN with ≥ 11 passing assertions (5 schema-shape + 5 happy-path + ≥ 1 defense-in-depth aggregate)
- `error-mapping.test.ts` GREEN with ≥ 7 passing assertions (pass-through + NotFoundError + unknown + string + sanitize-path + sanitize-hex + sanitize-truncation)
- `npm test --workspace=@engram/mcp-server -- --run` exits 0 with no skipped/pending counts
- `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` exits 0
</success_criteria>

<output>
Create `.planning/phases/03-mcp-server-scaffold/03-02-SUMMARY.md` when done. Required sections: Outcome, Files Changed, Verification, Cross-Phase Contracts (highlight: T-03-DD-IN structurally enforced; mapToMcpError ready for Phase 4 imports), Deviations, Next Plans (03 — tools.ts + 04 — oauth.ts; can run in parallel within Wave 2).
</output>
</output>
