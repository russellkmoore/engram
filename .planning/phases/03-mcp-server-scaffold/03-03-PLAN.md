---
phase: 03-mcp-server-scaffold
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - packages/mcp-server/src/tools.ts
  - packages/mcp-server/src/__tests__/tools.test.ts
autonomous: true
requirements:
  - MCP-05
  - MCP-06
tags:
  - mcp-server
  - tool-registration
  - mcp-sdk
  - defense-in-depth

must_haves:
  truths:
    - "registerTools(server, getProps, env) registers exactly 5 tools by name: remember, recall, search, forget, ingest"
    - "Every stub handler throws McpError(ErrorCode.MethodNotFound) with a message containing 'Phase 3' AND 'Phase 4 (TOL-0N)'"
    - "Each tool registration declares a description string"
    - "Each tool registration declares the corresponding zod inputSchema from schemas.ts via the `.shape` property"
    - "tools.ts source contains a Phase-4-ready comment block documenting the canonical handler shape (await getAgentByName + props.workspace_id contract)"
    - "tools.test.ts is GREEN — 5 MethodNotFound assertions + 1 tool-count assertion + 1 defense-in-depth-routing structural assertion pass"
  artifacts:
    - path: "packages/mcp-server/src/tools.ts"
      provides: "registerTools function — registers all 5 v0.1 MCP tools as stubs throwing MethodNotFound"
      min_lines: 80
      exports: ["registerTools"]
  key_links:
    - from: "packages/mcp-server/src/tools.ts"
      to: "packages/mcp-server/src/schemas.ts"
      via: 'import { RememberInputSchema, RecallInputSchema, SearchInputSchema, ForgetInputSchema, IngestInputSchema } from "./schemas.js"'
      pattern: "from \"./schemas\\.js\""
    - from: "packages/mcp-server/src/tools.ts"
      to: "@modelcontextprotocol/sdk types (McpError, ErrorCode)"
      via: 'import { McpError, ErrorCode } from "@modelcontextprotocol/sdk/types.js"'
      pattern: "ErrorCode\\.MethodNotFound"
    - from: "packages/mcp-server/src/tools.ts"
      to: "packages/mcp-server/src/index.ts (EngramProps type)"
      via: 'import type { EngramProps } from "./index.js"'
      pattern: "EngramProps"
---

<objective>
Land `packages/mcp-server/src/tools.ts` — the function that registers all 5 v0.1 MCP tools (`remember`, `recall`, `search`, `forget`, `ingest`) on the McpServer instance. Each tool registration uses `server.registerTool(name, { description, inputSchema }, callback)` from `@modelcontextprotocol/sdk`. Each callback body is exactly one line: `throw new McpError(ErrorCode.MethodNotFound, "<tool> not implemented in Phase 3 — ships in Phase 4 (TOL-0N)")`. Above the stubs, a Phase-4-ready comment block documents the canonical handler shape Phase 4 plans will diff against (await `getAgentByName(env.WORKSPACE, props.workspace_id)` + `args.workspace_id: props.workspace_id` defense-in-depth contract).

Purpose: MCP-06 ("All five v0.1 tools registered with zod input schemas") and MCP-05 ("Tool handlers route to WorkspaceDO via getAgentByName(env.WORKSPACE, this.props.workspace_id)"). Phase 3 ships the registration + stub; Phase 4 swaps each callback body. Runs in parallel with Plan 04 (oauth.ts) within Wave 2 — both touch independent files.

Output: 1 new source file (`tools.ts`); 1 test file updated (`tools.test.ts`) from RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-mcp-server-scaffold/03-CONTEXT.md
@.planning/phases/03-mcp-server-scaffold/03-RESEARCH.md
@.planning/phases/03-mcp-server-scaffold/03-PATTERNS.md
@.planning/phases/03-mcp-server-scaffold/03-01-SUMMARY.md
@.planning/phases/03-mcp-server-scaffold/03-02-SUMMARY.md
@packages/mcp-server/src/schemas.ts
@packages/mcp-server/src/error-mapping.ts
@packages/mcp-server/src/index.ts
@packages/mcp-server/src/__tests__/tools.test.ts
@packages/workspace-do/src/index.ts
@packages/workspace-do/src/__tests__/defense-in-depth.test.ts

<interfaces>
<!-- Key contracts the executor needs in-hand. -->

From @modelcontextprotocol/sdk/server/mcp.js (verified against node_modules):
The McpServer class exposes `registerTool(name, config, callback)`. `config.inputSchema` accepts a ZodRawShape (i.e., the `.shape` property of a `z.object({...})` instance, NOT the schema itself). The SDK auto-runs validation BEFORE invoking the callback — handlers receive PRE-VALIDATED args via ShapeOutput<Args> typing.

From @modelcontextprotocol/sdk/types.js:
- McpError class with `.code: ErrorCode`, constructor(code, message, data?)
- ErrorCode.MethodNotFound = -32601
- ErrorCode.InvalidParams = -32602
- ErrorCode.InvalidRequest = -32600
- ErrorCode.InternalError = -32603

From packages/mcp-server/src/schemas.ts (Plan 02):
RememberInputSchema, RecallInputSchema, SearchInputSchema, ForgetInputSchema, IngestInputSchema — each is a `z.object({...})` exposing `.shape` for the SDK's inputSchema parameter.

From packages/mcp-server/src/index.ts (Plan 01 emitted):
`export interface EngramProps extends Record<string, unknown> { workspace_id: string; user_id: string; }`

From packages/workspace-do/src/index.ts (Phase 2 — what Phase 4 handlers will route to):
- insertBlock(args: { workspace_id: string; block: Memory }): void
- getBlock(args: { workspace_id: string; id: string }): Memory
- lexicalSearchBlocks(args: { workspace_id: string; query: string; limit?: number }): LexicalSearchHit[]
- deleteBlock(args: { workspace_id: string; id: string; cascade?: boolean }): { blocks_deleted: number; relations_deleted: number }
- listMemoryTypes(args: { workspace_id: string }): MemoryType[]
- createInboxEntry(args: { workspace_id: string; entry: InboxEntry }): void
- listConflicts(args: { workspace_id: string; resolved?: boolean; limit?: number }): Conflict[]

From `agents` (verified against node_modules/agents/dist/agent-tool-types-Dn9n-3SI.d.ts):
`getAgentByName(namespace, name)` returns `Promise<DurableObjectStub<T>>` — MUST be awaited (per RESEARCH.md Pitfall 1).

Tool → TOL → method mapping (Phase-4-ready comment must reflect this):
- remember → TOL-01 → insertBlock
- recall → TOL-02 → lexicalSearchBlocks (P5 swaps to semantic via Vectorize)
- search → TOL-03 → lexicalSearchBlocks (with filters)
- forget → TOL-04 → deleteBlock
- ingest → TOL-05 → (no direct WorkspaceDO method — Phase 6 wires Queue producer)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tool callback args | Tool callback receives `args` already validated by the SDK's auto-parse (using inputSchema from schemas.ts). Stub callbacks throw before consuming args — no further validation needed. |
| Tool callback → WorkspaceDO RPC | Deferred boundary. Phase 4 will route via `await getAgentByName(env.WORKSPACE, props.workspace_id).<method>({ workspace_id: props.workspace_id, ... })`. Phase 3 stubs throw before this call — the Phase-4-ready comment block in tools.ts source IS the contract documentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-DD-RT | Tampering / Elevation of Privilege | packages/mcp-server/src/tools.ts | mitigate | (1) Stubs do NOT consume args.workspace_id (they throw immediately). (2) Phase-4-ready comment block documents the canonical handler shape: `await getAgentByName(env.WORKSPACE, props.workspace_id); stub.<method>({ workspace_id: props.workspace_id, ... })` — NEVER `args.workspace_id`. (3) tools.test.ts asserts the comment block contains the literal phrase "this.props.workspace_id" or "props.workspace_id" AND the production code (excluding comment lines) does NOT contain `args.workspace_id` anywhere. |
| T-03-DD-IN | Tampering | (mitigated by Plan 02 schemas.ts) | accept-here | The schema-layer guard against workspace_id input is already in place (Plan 02). This plan benefits structurally — tool callbacks cannot receive workspace_id because the schema doesn't accept it. |
| T-03-DOS | Denial of Service | tools.ts callbacks | accept | Phase 3 stubs throw immediately and consume no resources; rate-limiting + budget checks are MCP-08 (Phase 4). |
| T-03-JWT | Spoofing | (deferred to Plan 04 + Plan 05) | accept-here | tools.ts callbacks ONLY execute after the OAuthProvider validates the JWT — that defense lives in Plan 04 + Plan 05 default-export wiring. |
| T-03-PROPS | Spoofing | (deferred to Plan 04) | accept-here | Plan 04 mitigates. |
| T-03-MSG | Information Disclosure | McpError message strings in tool stubs | mitigate | MethodNotFound messages MUST mention only "Phase 3" and "Phase 4 (TOL-0N)" — no DB internals, no env values, no stack traces (per CONTEXT.md §Claude's Discretion → "Error mapping convention"). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create packages/mcp-server/src/tools.ts (registerTools + 5 stubs + Phase-4-ready comment)</name>
  <files>packages/mcp-server/src/tools.ts</files>
  <read_first>
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Pattern 4 (lines 412-521) — verbatim registerTools shape with all 5 server.registerTool() calls
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Pattern 5 (lines 523-552) — Phase-4-ready comment block (copy verbatim above the stubs)
    - .planning/phases/03-mcp-server-scaffold/03-PATTERNS.md §2 (lines 90-170) — analog choice and WorkspaceDO method signature catalog for the comment block
    - .planning/phases/03-mcp-server-scaffold/03-CONTEXT.md §D-05 + §D-06 — stub error message format and schemas-source decision
    - packages/mcp-server/src/schemas.ts (Plan 02 — 5 schema exports to import)
    - packages/mcp-server/src/index.ts (EngramProps type to import via `import type`)
    - packages/workspace-do/src/index.ts lines 163-206 (7 WorkspaceDO method signatures — comment block accuracy)
    - CLAUDE.md §"MCP Tool Surface" (canonical tool descriptions)
  </read_first>
  <behavior>
    - Test 1: file exports `registerTools(server: McpServer, getProps: () => EngramProps | undefined, env: Env): void` (signature per Pattern 4 line 436)
    - Test 2: registerTools registers exactly 5 tools by name — remember, recall, search, forget, ingest
    - Test 3: Each tool registration passes `{ description: <non-empty string>, inputSchema: <Schema>.shape }` as config
    - Test 4: Each tool callback throws `McpError(ErrorCode.MethodNotFound, "<tool> not implemented in Phase 3 — ships in Phase 4 (TOL-0N)")` with correct TOL number per tool
    - Test 5: The file source contains the Phase-4-ready comment block including the phrase "props.workspace_id" AND a phrase like "NEVER from args" or "NEVER from tool input" (documents T-03-DD-RT)
    - Test 6: The file source does NOT reference `args.workspace_id` outside comment lines — `grep -E "args\.workspace_id"` filtered to non-comment lines returns 0 matches
  </behavior>
  <action>
    Create packages/mcp-server/src/tools.ts. Top-of-file JSDoc mirroring 03-PATTERNS.md §B style (one-line summary → cross-phase contract → design notes → Plan boundaries → `@module @engram/mcp-server/tools`). Module role: "registers all 5 v0.1 MCP tools as stubs throwing McpError(MethodNotFound); Phase 4 swaps each callback body while preserving the registration." Imports: `import { McpError, ErrorCode } from "@modelcontextprotocol/sdk/types.js"`, `import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"`, `import { RememberInputSchema, RecallInputSchema, SearchInputSchema, ForgetInputSchema, IngestInputSchema } from "./schemas.js"`, `import type { EngramProps } from "./index.js"`. For the `Env` type reference: use the wrangler-generated worker-configuration.d.ts global if available; otherwise declare a temporary local `type Env = unknown` — Plan 05's wrangler-types regeneration will produce the real one. Export ONE function: `export function registerTools(server: McpServer, getProps: () => EngramProps | undefined, _env: Env): void`. Function body: insert the Phase-4-ready comment block FIRST (before any registerTool call) — verbatim from 03-RESEARCH.md §Pattern 5 lines 530-552. The comment block MUST include: (a) sourcing claim mentioning the verified `getAgentByName` signature; (b) the phrase "props.workspace_id"; (c) the phrase "NEVER from args" or "NEVER from tool input"; (d) the canonical handler skeleton showing `const stub = await getAgentByName(env.WORKSPACE, props.workspace_id);` and `stub.<method>({ workspace_id: props.workspace_id, ... });`; (e) reference to `mapToMcpError` from `./error-mapping.js`. Then 5 `server.registerTool(name, config, callback)` calls in this order — remember, recall, search, forget, ingest — using verbatim shape from Pattern 4 lines 442-515. Config object per tool: `description` is a one-sentence string sourced from CLAUDE.md "MCP Tool Surface" (verbatim text in RESEARCH.md Pattern 4 lines 446, 461, 475, 489, 503). `inputSchema` is `<Schema>.shape` (note: `.shape` — the SDK expects ZodRawShape). Callback signature: `async (_args, _extra) => { throw new McpError(ErrorCode.MethodNotFound, "<tool> not implemented in Phase 3 — ships in Phase 4 (TOL-0N)"); }` substituting TOL number per tool: remember→TOL-01, recall→TOL-02, search→TOL-03, forget→TOL-04, ingest→TOL-05. Function tail: `void getProps;` to suppress the unused-variable lint until Phase 4 fills handlers (Pattern 4 line 519). Keep the Phase-4-ready comment block structure verbatim from Pattern 5 — tools.test.ts will grep for specific phrases.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; test -f packages/mcp-server/src/tools.ts &amp;&amp; grep -q "export function registerTools" packages/mcp-server/src/tools.ts &amp;&amp; grep -q 'server.registerTool(\s*"remember"' packages/mcp-server/src/tools.ts &amp;&amp; grep -q 'server.registerTool(\s*"recall"' packages/mcp-server/src/tools.ts &amp;&amp; grep -q 'server.registerTool(\s*"search"' packages/mcp-server/src/tools.ts &amp;&amp; grep -q 'server.registerTool(\s*"forget"' packages/mcp-server/src/tools.ts &amp;&amp; grep -q 'server.registerTool(\s*"ingest"' packages/mcp-server/src/tools.ts &amp;&amp; grep -q "TOL-01" packages/mcp-server/src/tools.ts &amp;&amp; grep -q "TOL-02" packages/mcp-server/src/tools.ts &amp;&amp; grep -q "TOL-03" packages/mcp-server/src/tools.ts &amp;&amp; grep -q "TOL-04" packages/mcp-server/src/tools.ts &amp;&amp; grep -q "TOL-05" packages/mcp-server/src/tools.ts &amp;&amp; grep -q "MethodNotFound" packages/mcp-server/src/tools.ts &amp;&amp; grep -q "props.workspace_id" packages/mcp-server/src/tools.ts &amp;&amp; grep -q "RememberInputSchema" packages/mcp-server/src/tools.ts &amp;&amp; ( ! grep -v -E "^[[:space:]]*(//|\*)" packages/mcp-server/src/tools.ts | grep -q "args\.workspace_id" ) &amp;&amp; npx tsc -p packages/mcp-server/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/mcp-server/src/tools.ts` exists
    - File contains `export function registerTools`
    - File contains 5 `server.registerTool(` invocations, one per tool name (remember, recall, search, forget, ingest) — verified by grep
    - File references all 5 schema imports from `./schemas.js`: RememberInputSchema, RecallInputSchema, SearchInputSchema, ForgetInputSchema, IngestInputSchema
    - File contains the literal string `MethodNotFound`
    - File contains exactly the 5 TOL pointers: `TOL-01`, `TOL-02`, `TOL-03`, `TOL-04`, `TOL-05`
    - Each TOL pointer appears within a `MethodNotFound` error message string (per-tool pairing — verified by manual scan of source)
    - File contains the phrase `props.workspace_id` (Phase-4-ready comment block)
    - File contains a phrase like `NEVER from args`, `NEVER from tool input`, or `NEVER from input` (the explicit anti-pattern)
    - Stripping comment lines (`grep -v -E "^[[:space:]]*(//|\*)"`) and searching for `args.workspace_id` returns 0 matches (T-03-DD-RT structural check — phrase only appears in the comment block)
    - File contains import line `import type { EngramProps } from "./index.js"`
    - `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` exits 0
  </acceptance_criteria>
  <done>
    tools.ts exists with the registerTools function, 5 tool registrations, MethodNotFound stubs with phase-pinned messages, and the Phase-4-ready comment block. Defense-in-depth (T-03-DD-RT) is structurally enforced — `args.workspace_id` is absent from non-comment code. The file compiles cleanly. Plan 05 will wire `EngramMcp.init()` to call `registerTools(this.server, () => this.props, this.env)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn tools.test.ts from RED to GREEN (5 MethodNotFound + tool-count + DD-RT structural)</name>
  <files>packages/mcp-server/src/__tests__/tools.test.ts</files>
  <read_first>
    - packages/mcp-server/src/__tests__/tools.test.ts (Wave 0 RED stub — replace it.skip with it() bodies)
    - packages/mcp-server/src/tools.ts (just-created Task 1)
    - packages/workspace-do/src/__tests__/defense-in-depth.test.ts (lines 95-200) — canonical `runInDurableObject` + try/catch + McpError assertion shape; this test file does NOT need runInDurableObject because registerTools can be exercised on a bare McpServer instance, but the McpError assertion triplet (instanceof + .code + .message.contains) is mirrored
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Validation Architecture (lines 971-1016) — required test commands
  </read_first>
  <behavior>
    - Test 1: `describe('registerTools')` block exists with describes for tool-count, MethodNotFound per tool, DD-RT structural
    - Test 2: instantiate a bare McpServer, call registerTools(server, () => undefined, {} as any), then assert server registered exactly 5 tools (use `server.listTools()` or whatever MCP SDK exposes; if unavailable, exercise via spy on registerTool)
    - Test 3: For each of 5 tools, invoke the registered callback (via server-internal dispatch OR by spying on registerTool during registration) and assert it throws McpError with `.code === ErrorCode.MethodNotFound (-32601)` and `.message` contains "Phase 3" AND "Phase 4 (TOL-0N)" (correct N per tool)
    - Test 4 (T-03-DD-RT structural): use `node:fs` to readFileSync packages/mcp-server/src/tools.ts and assert: (a) the source contains the phrase "props.workspace_id"; (b) stripping comment lines, the source does NOT contain "args.workspace_id" (the defense-in-depth structural enforcement)
    - Test 5: `npm test --workspace=@engram/mcp-server -- tools` exits 0 with all assertions passing, zero skipped
  </behavior>
  <action>
    Replace it.skip/it.todo stubs in packages/mcp-server/src/__tests__/tools.test.ts. Imports: `import { describe, it, expect, vi } from "vitest"`, `import { readFileSync } from "node:fs"`, `import { McpError, ErrorCode } from "@modelcontextprotocol/sdk/types.js"`, `import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"`, `import { registerTools } from "../tools.js"`. Strategy for callback invocation: the McpServer's `registerTool` API stores the callback internally; the SDK exposes a way to invoke it via the JSON-RPC dispatcher, but for unit-test simplicity, **spy on `McpServer.prototype.registerTool`** to capture the (name, config, callback) tuples during registration, then invoke each captured callback directly with empty args. Pattern: `const spy = vi.spyOn(McpServer.prototype, "registerTool"); const server = new McpServer({ name: "test", version: "0.0.1" }); registerTools(server, () => undefined, {} as any); expect(spy).toHaveBeenCalledTimes(5);` then iterate `spy.mock.calls` and assert each call's first arg (name) and invoke the third arg (callback). Tests to land: (1) `describe('registerTools registration')`: assert spy called exactly 5 times; assert tool names are `["remember", "recall", "search", "forget", "ingest"]` (sort to compare); assert every call's config has `inputSchema` defined AND `description` is a non-empty string. (2) `describe('MethodNotFound stubs')`: `it.each([["remember", "TOL-01"], ["recall", "TOL-02"], ["search", "TOL-03"], ["forget", "TOL-04"], ["ingest", "TOL-05"]])('throws McpError(MethodNotFound) with phase 3 + %s pointer', async ([toolName, tolId]) => { ... })` — for each, find the matching spy call, invoke its callback, assert `caught instanceof McpError && caught.code === ErrorCode.MethodNotFound && caught.message.includes("Phase 3") && caught.message.includes("Phase 4") && caught.message.includes(tolId)`. (3) `describe('defense-in-depth structural T-03-DD-RT')`: `it('source documents props.workspace_id contract', () => { const src = readFileSync("packages/mcp-server/src/tools.ts", "utf8"); expect(src).toMatch(/props\.workspace_id/); })`. `it('source does not reference args.workspace_id outside comments', () => { const src = readFileSync("packages/mcp-server/src/tools.ts", "utf8"); const nonComment = src.split("\n").filter(line => !line.trim().startsWith("//") && !line.trim().startsWith("*")).join("\n"); expect(nonComment).not.toMatch(/args\.workspace_id/); })`. Note: this test file does NOT need `runInDurableObject` — registerTools operates on a bare McpServer, no DO required. The vitest config from Plan 01 still routes through the workerd pool (per Plan 01's vitest.config.ts), so workerd-side file reads via `node:fs` should be available; if `node:fs` isn't supported under workerd, fall back to bundling the source into the test via `import.meta.glob` or use vitest's `--pool=forks` for this specific file — but try `node:fs` first since the wrangler.test.jsonc declares `nodejs_compat` implicitly through the workerd test pool's defaults. The Plan 01 vitest config can be amended if `node:fs` proves unavailable — that amendment is acceptable scope creep here.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; ( ! grep -E "it\.(skip|todo)\(" packages/mcp-server/src/__tests__/tools.test.ts ) &amp;&amp; grep -q "registerTools" packages/mcp-server/src/__tests__/tools.test.ts &amp;&amp; grep -q "MethodNotFound" packages/mcp-server/src/__tests__/tools.test.ts &amp;&amp; grep -q "props.workspace_id" packages/mcp-server/src/__tests__/tools.test.ts &amp;&amp; npm test --workspace=@engram/mcp-server -- --run tools 2>&amp;1 | tee /tmp/tools-test-out.txt &amp;&amp; ( ! grep -qE "FAIL|failed" /tmp/tools-test-out.txt )</automated>
  </verify>
  <acceptance_criteria>
    - `packages/mcp-server/src/__tests__/tools.test.ts` contains NO `it.skip(` or `it.todo(` (regex `it\.(skip|todo)\(` returns 0 matches)
    - File references `registerTools` (imported from `../tools.js`)
    - File contains the literal `MethodNotFound`
    - File contains the literal `props.workspace_id` (structural assertion of the contract phrase in the source-under-test)
    - File contains the literal `args.workspace_id` (structural assertion that the test checks NON-presence in code)
    - File contains exactly one `vi.spyOn` or equivalent registerTool capture pattern
    - File references all 5 TOL pointers: TOL-01, TOL-02, TOL-03, TOL-04, TOL-05
    - `npm test --workspace=@engram/mcp-server -- --run tools` exits 0
    - The vitest output for this file contains 0 failed tests
    - The vitest output for this file contains ≥ 7 passing tests (5 MethodNotFound + 1 tool-count + 1 DD-RT structural — actual count depends on it.each grouping)
  </acceptance_criteria>
  <done>
    tools.test.ts is GREEN. Every it.skip from Wave 0 is replaced. The structural defense-in-depth check (T-03-DD-RT) now fires on regression — any future tool callback that reads args.workspace_id breaks CI. The 5 MethodNotFound + tool-count assertions validate MCP-06 (5 tools registered with zod schemas) at the unit level.
  </done>
</task>

</tasks>

<verification>
After both tasks land:

1. **Full mcp-server test suite green (tools.test.ts now GREEN, schemas.test.ts + error-mapping.test.ts still GREEN from Plan 02):**
   ```bash
   cd /Users/rmoore/Workspaces/engram
   npm test --workspace=@engram/mcp-server -- --run
   ```
   Exit 0. Output shows ≥ 11 schema tests + ≥ 7 error-mapping tests + ≥ 7 tools tests passing. Zero failed. Zero skipped (oauth.test.ts + index.test.ts still RED from Wave 0; that's Plan 04 + Plan 05 territory).

2. **TypeScript compiles cleanly:**
   ```bash
   npx tsc -p packages/mcp-server/tsconfig.json --noEmit
   ```
   Exit 0.

3. **Structural defense-in-depth check (T-03-DD-RT):**
   ```bash
   grep -v -E "^[[:space:]]*(//|\*)" packages/mcp-server/src/tools.ts | grep -c "args\.workspace_id"
   ```
   Returns 0 (only the comment block references the anti-pattern phrase).

4. **Phase 2 cross-package non-regression:**
   ```bash
   npm test --workspaces -- --run
   ```
   Exit 0; all workspace-do tests still green.
</verification>

<success_criteria>
- `packages/mcp-server/src/tools.ts` exists with `registerTools` exported, 5 stub registrations, MethodNotFound errors with phase-pinned messages, and the Phase-4-ready comment block
- T-03-DD-RT structurally enforced: no production code reads `args.workspace_id`
- `tools.test.ts` GREEN with ≥ 7 passing assertions; tool-count = 5; tool names match; all 5 callbacks throw MethodNotFound with correct TOL pointer
- `npm test --workspace=@engram/mcp-server -- --run tools` exits 0
- `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` exits 0
- Plan does NOT modify packages/mcp-server/src/index.ts — Plan 05 wires `EngramMcp.init()` to call registerTools
</success_criteria>

<output>
Create `.planning/phases/03-mcp-server-scaffold/03-03-SUMMARY.md` when done. Required sections: Outcome, Files Changed, Verification, Cross-Phase Contracts (highlight: T-03-DD-RT now structurally enforced; tools.ts ready for Plan 05 wiring), Deviations, Next Plan (05 — index.ts + wrangler.jsonc v2 migration, blocked on Plan 04 oauth.ts completion).
</output>
