---
phase: 03-mcp-server-scaffold
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - packages/mcp-server/src/index.ts
  - packages/mcp-server/src/oauth.ts
  - packages/mcp-server/wrangler.jsonc
  - packages/mcp-server/worker-configuration.d.ts
  - packages/mcp-server/src/__tests__/index.test.ts
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
tags:
  - mcp-server
  - wrangler-migration
  - default-export
  - oauth-provider

must_haves:
  truths:
    - "packages/mcp-server/src/index.ts default export is `new OAuthProvider({...})` instance — NOT a plain {fetch} object"
    - "EngramMcp.init() body calls `registerTools(this.server, () => this.props, this.env)` (wires Plan 03's tools.ts)"
    - "OAuthProvider constructor receives apiRoute: '/mcp', apiHandler: EngramMcp.serve('/mcp', { binding: 'MCP_OBJECT' }), defaultHandler (from Plan 04 oauth.ts), authorizeEndpoint: '/authorize', tokenEndpoint: '/token', clientRegistrationEndpoint: '/register'"
    - "packages/mcp-server/wrangler.jsonc migrations[] contains v1 (WorkspaceDO) AND v2 (EngramMcp) entries using new_sqlite_classes"
    - "packages/mcp-server/wrangler.jsonc kv_namespaces declares OAUTH_KV and ENGRAM_IDENTITIES bindings"
    - "Phase 1 'deferred to v2' JSDoc comment is removed from wrangler.jsonc (per D-09 acceptance)"
    - "`npm run lint:wrangler` exits 0 after the edits"
    - "`npx wrangler deploy --dry-run` exits 0 (validates the v2 migration is well-formed and EngramMcp is declared as a class)"
    - "index.test.ts is GREEN — default export is OAuthProvider, EngramMcp registers 5 tools, wrangler.jsonc v2 migration shape correct"
    - "Phase 1 type-witness `Phase1Pong` interface and `export default { fetch() }` block are removed (replaced by OAuthProvider default export)"
    - "After Task 3 regenerates worker-configuration.d.ts, ALL temporary local `interface Env { ... }` declarations are removed from BOTH packages/mcp-server/src/oauth.ts AND packages/mcp-server/src/index.ts — the canonical Env now comes exclusively from the regenerated global d.ts (prevents silent shape drift between local and global Env types)"
  artifacts:
    - path: "packages/mcp-server/src/index.ts"
      provides: "Worker entry point — OAuthProvider default export wraps EngramMcp + defaultHandler"
      exports: ["EngramMcp", "EngramProps", "WorkspaceDO (re-export)"]
    - path: "packages/mcp-server/wrangler.jsonc"
      provides: "Production wrangler config with v1 + v2 migrations and KV bindings"
      contains: "EngramMcp"
    - path: "packages/mcp-server/worker-configuration.d.ts"
      provides: "Generated TypeScript Env interface for the Worker"
      contains: "ENGRAM_IDENTITIES"
  key_links:
    - from: "packages/mcp-server/src/index.ts (EngramMcp.init)"
      to: "packages/mcp-server/src/tools.ts (registerTools)"
      via: "registerTools(this.server, () => this.props, this.env)"
      pattern: "registerTools\\(this\\.server"
    - from: "packages/mcp-server/src/index.ts (OAuthProvider constructor)"
      to: "packages/mcp-server/src/oauth.ts (defaultHandler)"
      via: "defaultHandler import + constructor option"
      pattern: "defaultHandler"
    - from: "packages/mcp-server/src/index.ts (OAuthProvider apiHandler)"
      to: "EngramMcp.serve('/mcp')"
      via: "apiHandler: EngramMcp.serve('/mcp', { binding: 'MCP_OBJECT' })"
      pattern: "EngramMcp\\.serve"
    - from: "packages/mcp-server/wrangler.jsonc migrations[1]"
      to: "EngramMcp class"
      via: 'migrations[1].new_sqlite_classes contains "EngramMcp"'
      pattern: "\"EngramMcp\""
---

<objective>
Replace the Phase 1 stub default export in `packages/mcp-server/src/index.ts` with the real `OAuthProvider`-wrapping default export. Wire `EngramMcp.init()` to call Plan 03's `registerTools`. Mount Plan 04's `defaultHandler` as the OAuthProvider's `defaultHandler`. Add the v2 migration entry to `packages/mcp-server/wrangler.jsonc` declaring `EngramMcp` as a SQLite-backed Durable Object (resolves Phase 2 D-07 forward-note + Phase 3 D-09). Add the `OAUTH_KV` and `ENGRAM_IDENTITIES` KV bindings to wrangler.jsonc. Regenerate `worker-configuration.d.ts` so TypeScript resolves the new bindings. Turn `index.test.ts` from RED to GREEN.

Purpose: MCP-01 (`agents/mcp` McpAgent host), MCP-02 (`McpAgent.serve("/mcp")`), MCP-03 (BOTH DO classes in `new_sqlite_classes`). This plan is the Wave 3 unifier — it consumes all four prior plans' outputs (schemas, error-mapping, tools, oauth) and produces the deployable Worker. After this plan lands, `wrangler deploy` would succeed if Russell pushed it to Cloudflare.

Output: 1 file modified (`src/index.ts` — full body swap, preserving the `EngramProps` interface + `WorkspaceDO` re-export from Plan 01); 1 file modified (`src/oauth.ts` — local `interface Env` removed after global d.ts regen, per checker WARNING 1); 1 file modified (`wrangler.jsonc` — additive migration + KV bindings + JSDoc cleanup); 1 file regenerated (`worker-configuration.d.ts`); 1 test file updated (`index.test.ts`) from RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-mcp-server-scaffold/03-CONTEXT.md
@.planning/phases/03-mcp-server-scaffold/03-RESEARCH.md
@.planning/phases/03-mcp-server-scaffold/03-PATTERNS.md
@.planning/phases/03-mcp-server-scaffold/03-01-SUMMARY.md
@.planning/phases/03-mcp-server-scaffold/03-02-SUMMARY.md
@.planning/phases/03-mcp-server-scaffold/03-03-SUMMARY.md
@.planning/phases/03-mcp-server-scaffold/03-04-SUMMARY.md
@packages/mcp-server/src/index.ts
@packages/mcp-server/src/tools.ts
@packages/mcp-server/src/oauth.ts
@packages/mcp-server/src/schemas.ts
@packages/mcp-server/src/error-mapping.ts
@packages/mcp-server/wrangler.jsonc
@packages/mcp-server/src/__tests__/index.test.ts
@scripts/lint-wrangler.mjs

<interfaces>
<!-- Key contracts the executor needs in-hand. -->

From @cloudflare/workers-oauth-provider (verified against RESEARCH.md):
```typescript
new OAuthProvider({
  apiRoute: "/mcp",                                  // exact-match path (RESEARCH Open Question 1 unresolved — assume exact)
  apiHandler: <ExportedHandler-like>,                // EngramMcp.serve("/mcp", { binding: "MCP_OBJECT" })
  defaultHandler: <ExportedHandler>,                 // Plan 04's defaultHandler
  authorizeEndpoint: "/authorize",
  tokenEndpoint: "/token",
  clientRegistrationEndpoint: "/register",
  // library emits /.well-known/oauth-protected-resource, /.well-known/oauth-authorization-server, /jwks automatically
  // Defaults: accessTokenTTL=3600s, refreshTokenTTL=2592000s
})
```

From agents/mcp (verified):
```typescript
class McpAgent<Env, State, Props extends Record<string, unknown>> {
  abstract server: McpServer | Server | Promise<...>;
  abstract init(): Promise<void> | void;
  readonly props: Props;          // populated by OAuthProvider after JWT validation
  static serve(path: string, opts?: { binding: string }): ExportedHandler;  // mounts the MCP HTTP transport
}
```

Final index.ts shape (per 03-PATTERNS.md §10 lines 514-547):
```typescript
import { McpAgent } from "agents/mcp";
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { OAuthProvider } from "@cloudflare/workers-oauth-provider";
import { registerTools } from "./tools.js";
import { defaultHandler } from "./oauth.js";

export interface EngramProps extends Record<string, unknown> {
  workspace_id: string;
  user_id: string;
}

export class EngramMcp extends McpAgent<Env, unknown, EngramProps> {
  server = new McpServer({ name: "engram-mcp-server", version: "0.1.0" });
  async init(): Promise<void> {
    registerTools(this.server, () => this.props, this.env);
  }
}

export { WorkspaceDO } from "@engram/workspace-do";

export default new OAuthProvider({
  apiRoute: "/mcp",
  apiHandler: EngramMcp.serve("/mcp", { binding: "MCP_OBJECT" }),
  defaultHandler,
  authorizeEndpoint: "/authorize",
  tokenEndpoint: "/token",
  clientRegistrationEndpoint: "/register",
});
```

Final wrangler.jsonc shape (per 03-RESEARCH.md §Pattern 6 lines 560-591):
```jsonc
{
  "$schema": "../../node_modules/wrangler/config-schema.json",
  "name": "engram-mcp-server",
  "main": "src/index.ts",
  "compatibility_date": "2026-05-22",
  "compatibility_flags": ["nodejs_compat"],
  "observability": { "enabled": true },
  "durable_objects": {
    "bindings": [
      { "name": "MCP_OBJECT", "class_name": "EngramMcp" },
      { "name": "WORKSPACE", "class_name": "WorkspaceDO" }
    ]
  },
  "kv_namespaces": [
    { "binding": "OAUTH_KV", "id": "<id-from-wrangler-kv-namespace-create>" },
    { "binding": "ENGRAM_IDENTITIES", "id": "<id-from-wrangler-kv-namespace-create>" }
  ],
  "migrations": [
    { "tag": "v1", "new_sqlite_classes": ["WorkspaceDO"] },
    { "tag": "v2", "new_sqlite_classes": ["EngramMcp"] }
  ]
}
```

FND-08 lint contract: `scripts/lint-wrangler.mjs` (Phase 1) rejects any `new_classes` array on a migration. The v2 entry uses `new_sqlite_classes` (passes by construction). NOTE: `COOKIE_ENCRYPTION_KEY` is NOT declared in wrangler.jsonc — it's set via `wrangler secret put` (Plan 06 README documents this).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare → Worker bindings | KV namespaces (OAUTH_KV, ENGRAM_IDENTITIES), DO namespaces (MCP_OBJECT → EngramMcp, WORKSPACE → WorkspaceDO), and the COOKIE_ENCRYPTION_KEY secret cross into the runtime via env. wrangler.jsonc declares the binding shape; Cloudflare provisions the actual values at deploy time. |
| HTTP client → / endpoint | OAuthProvider matches `apiRoute: "/mcp"` first; everything else falls through to defaultHandler. RESEARCH Pitfall 4 + Open Question 1: assume exact-match on /mcp (not prefix); verification command in Plan 06's manual smoke checks `/mcp/leaked` does NOT bypass auth. |
| EngramMcp.init → tools.ts | `this.props` is the authenticated identity. `this.env` is the wrangler-injected bindings. registerTools receives both as closure inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-JWT | Spoofing | OAuthProvider library (mounted as default export) | mitigate | OAuthProvider validates the bearer JWT before dispatching to apiHandler. Unauthenticated requests to /mcp receive a 401 + WWW-Authenticate response from the library — no custom code in this plan. index.test.ts asserts that a fetch against /mcp without a bearer token returns 401 (structural verification of the library integration). |
| T-03-PROPS | Spoofing | EngramMcp.init() → registerTools | mitigate | registerTools receives `() => this.props` as a getter — the closure captures the live `this.props` so token refresh (per RESEARCH Pitfall 6) is reflected on the next tool call. Plan 03 tools.ts already structurally enforces `props.workspace_id` over `args.workspace_id`. Inherited. |
| T-03-MIGR | Tampering | wrangler.jsonc migrations[] | mitigate | The v2 entry adds EngramMcp using `new_sqlite_classes` (NOT `new_classes`) per FND-08 lint. EngramMcp SQLite-backed is irreversible (Cloudflare workers-sdk #9909) — chosen per RESEARCH Open Question 5 (declare SQLite-backed regardless, future-proofing for McpAgent.sql usage). `npm run lint:wrangler` is a gate in this plan's verify. |
| T-03-DRYDEPLOY | Tampering / Configuration | wrangler.jsonc → deploy | mitigate | `npx wrangler deploy --dry-run` is the canary — if EngramMcp is declared as a binding but not present in any migration (or vice versa), the dry-run fails. RESEARCH Pitfall 3 documents the failure mode. This plan's verify includes the dry-run command. |
| T-03-KV-PLACEHOLDER | Information Disclosure / Configuration | wrangler.jsonc kv_namespaces.id | accept (post-deploy task) | The KV namespace IDs in wrangler.jsonc are placeholders (`<id-from-wrangler-kv-namespace-create>` literal strings) until Russell runs `wrangler kv namespace create` and pastes the real IDs. The placeholder is safe to commit (not a secret). Plan 06 README documents the swap procedure. KV namespace IDs are NOT secrets per RESEARCH §Security Domain. |
| T-03-COOKIE | Information Disclosure | COOKIE_ENCRYPTION_KEY | mitigate | NOT declared in wrangler.jsonc (would be a leak vector); set via `wrangler secret put COOKIE_ENCRYPTION_KEY`. Plan 06 README documents the one-line setup step. This plan's wrangler.jsonc MUST NOT contain any literal `COOKIE_ENCRYPTION_KEY` value. |
| T-03-ENV-DRIFT | Tampering / Configuration | local `interface Env` declarations in oauth.ts + index.ts | mitigate | After Task 3 regenerates `worker-configuration.d.ts` (providing the canonical global Env), any remaining inline `interface Env { ... }` in `oauth.ts` (declared by Plan 04 Task 1 as a temporary bridge) or `index.ts` (if Plan 01/Task 1 left a temporary block) MUST be removed. Two declarations with potentially-drifting shapes can cause TypeScript to silently resolve to the wrong type. Task 3's acceptance criteria include explicit `grep -c "^interface Env"` assertions against BOTH files returning 0 (checker feedback WARNING 1). |
| T-03-DD-IN, T-03-DD-RT, T-03-PROPS, T-03-KV-LEAK, T-03-PARSE | (inherited from Plans 02, 03, 04) | inherited | accept-here | Mitigations live in earlier plans. This plan inherits them by wiring oauth.ts + tools.ts into the default export. |
| T-03-SC | Tampering | npm hoisted node_modules | mitigate | Plans 01 + 02 already verified [OK] slopcheck disposition for all 4 new deps. No additional gate. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap packages/mcp-server/src/index.ts default export to OAuthProvider, wire EngramMcp.init → registerTools</name>
  <files>packages/mcp-server/src/index.ts</files>
  <read_first>
    - packages/mcp-server/src/index.ts (Plan 01 emitted EngramProps; Phase 1 stub fetch handler still present — must be removed)
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Pattern 1 (lines 261-296) + §Pattern 2 (lines 298-322) — verbatim final shape
    - .planning/phases/03-mcp-server-scaffold/03-PATTERNS.md §10 (lines 496-549) — REPLACE vs PRESERVE elements
    - packages/mcp-server/src/tools.ts (Plan 03) — registerTools signature to call
    - packages/mcp-server/src/oauth.ts (Plan 04) — defaultHandler import
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Anti-Patterns line 626 — never use raw `@modelcontextprotocol/sdk/server/streamableHttp`
  </read_first>
  <behavior>
    - Test 1 (typecheck): `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` exits 0
    - Test 2: file imports `OAuthProvider` from `@cloudflare/workers-oauth-provider`
    - Test 3: file imports `registerTools` from `./tools.js` and `defaultHandler` from `./oauth.js`
    - Test 4: `EngramMcp.init()` body calls `registerTools(this.server, () => this.props, this.env)` (NOT `() => undefined` or `this.props` as a value)
    - Test 5: file's default export is `new OAuthProvider({...})` — NOT a plain `{ fetch }` object
    - Test 6: the OAuthProvider constructor receives apiRoute "/mcp", apiHandler `EngramMcp.serve("/mcp", { binding: "MCP_OBJECT" })`, defaultHandler, authorizeEndpoint "/authorize", tokenEndpoint "/token", clientRegistrationEndpoint "/register"
    - Test 7: `export interface EngramProps` (Plan 01) is PRESERVED
    - Test 8: `export { WorkspaceDO } from "@engram/workspace-do"` (Phase 1) is PRESERVED
    - Test 9: the Phase 1 `Phase1Pong` interface AND `export default { fetch() }` block are REMOVED
    - Test 10: the file does NOT import from `@modelcontextprotocol/sdk/server/streamableHttp` (anti-pattern check — raw transport breaks workerd)
    - Test 11: any temporary local `interface Env { ... }` declared by Plan 01 may still be present here (Task 1 may need it because `worker-configuration.d.ts` is not yet regenerated); Task 3 enforces its removal AFTER the regen lands. If Plan 01 did NOT leave one, do NOT add one — prefer letting Task 3's regen surface a TS error and then resolve via global d.ts.
  </behavior>
  <action>
    Modify packages/mcp-server/src/index.ts. The file should end up matching 03-PATTERNS.md §10 lines 514-547 verbatim, with EngramProps preserved from Plan 01. Top-of-file JSDoc replaces the Plan 01 placeholder block with a description of the Phase 3 final state (`@module @engram/mcp-server` etc.). REMOVE: (a) Phase 1's `Phase1Pong` interface; (b) the FND-04 type-witness import block (`import type { MemoryEvent, Memory, Entity, EngramResponse, Conflict } from "@engram/types"` — no longer needed since the witnesses move to tools.ts/schemas.ts); (c) the FND-05 `import { SYSTEM_TYPES } from "@engram/schema"` line; (d) the `export default { fetch() { ... } }` Phase 1 block at the bottom. Plan 01's temporary local `interface Env` declaration (if present) MAY remain here during Task 1 — Task 3 enforces removal AFTER `worker-configuration.d.ts` regen. ADD: `import { OAuthProvider } from "@cloudflare/workers-oauth-provider"`, `import { registerTools } from "./tools.js"`, `import { defaultHandler } from "./oauth.js"`. PRESERVE: `import { McpAgent } from "agents/mcp"`, `import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"`, the `EngramProps` interface, `export class EngramMcp extends McpAgent<Env, unknown, EngramProps>`, `server = new McpServer({ name: "engram-mcp-server", version: "0.1.0" })`, and `export { WorkspaceDO } from "@engram/workspace-do"`. MODIFY the `init()` body: replace the no-op with `registerTools(this.server, () => this.props, this.env);` — note the `() => this.props` arrow function (the lazy props getter — RESEARCH Pitfall 6 / Open Question 4 about token-refresh props staleness). At the bottom, add `export default new OAuthProvider({ apiRoute: "/mcp", apiHandler: EngramMcp.serve("/mcp", { binding: "MCP_OBJECT" }), defaultHandler, authorizeEndpoint: "/authorize", tokenEndpoint: "/token", clientRegistrationEndpoint: "/register" });` — verbatim from RESEARCH Pattern 2. The `Env` reference in `McpAgent<Env, unknown, EngramProps>` will resolve through the regenerated worker-configuration.d.ts (Task 3 of this plan). If the regenerated d.ts isn't available at Task 1 time, leave a temporary local Env declaration above the class; Task 3 removes it. Run `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` after edits to confirm no errors. Anti-pattern enforcement: ensure NO `import` line references `@modelcontextprotocol/sdk/server/streamableHttp` (RESEARCH §Anti-Patterns line 626 — raw transport breaks workerd).
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; grep -q "import { OAuthProvider } from \"@cloudflare/workers-oauth-provider\"" packages/mcp-server/src/index.ts &amp;&amp; grep -q "import { registerTools }" packages/mcp-server/src/index.ts &amp;&amp; grep -q "import { defaultHandler }" packages/mcp-server/src/index.ts &amp;&amp; grep -q "export default new OAuthProvider" packages/mcp-server/src/index.ts &amp;&amp; grep -q "EngramMcp.serve(\"/mcp\"" packages/mcp-server/src/index.ts &amp;&amp; grep -q "binding: \"MCP_OBJECT\"" packages/mcp-server/src/index.ts &amp;&amp; grep -q "registerTools(this.server" packages/mcp-server/src/index.ts &amp;&amp; grep -q "export interface EngramProps" packages/mcp-server/src/index.ts &amp;&amp; grep -q "export { WorkspaceDO }" packages/mcp-server/src/index.ts &amp;&amp; ( ! grep -q "Phase1Pong" packages/mcp-server/src/index.ts ) &amp;&amp; ( ! grep -q "streamableHttp" packages/mcp-server/src/index.ts ) &amp;&amp; ( ! grep -q "export default {" packages/mcp-server/src/index.ts ) &amp;&amp; npx tsc -p packages/mcp-server/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File contains import `import { OAuthProvider } from "@cloudflare/workers-oauth-provider"`
    - File contains import `import { registerTools } from "./tools.js"` (or `"./tools"`)
    - File contains import `import { defaultHandler } from "./oauth.js"` (or `"./oauth"`)
    - File contains `export default new OAuthProvider(`
    - File contains `apiRoute: "/mcp"` AND `apiHandler: EngramMcp.serve("/mcp"` AND `binding: "MCP_OBJECT"`
    - File contains `authorizeEndpoint: "/authorize"`, `tokenEndpoint: "/token"`, `clientRegistrationEndpoint: "/register"`
    - File contains `registerTools(this.server, () => this.props, this.env)` (or with semantically equivalent arrow-function getter)
    - File preserves `export interface EngramProps`
    - File preserves `export { WorkspaceDO } from "@engram/workspace-do"`
    - File does NOT contain `Phase1Pong` (removed)
    - File does NOT contain `streamableHttp` (anti-pattern check — RESEARCH §Anti-Patterns)
    - File does NOT contain `export default {` followed by an inline-object literal (the Phase 1 fetch handler is gone)
    - `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` exits 0
    - NOTE: any temporary local `interface Env { ... }` declaration here is NOT removed in Task 1 — Task 3's acceptance criteria enforce removal AFTER the global d.ts is regenerated. This task does not regress on the local Env decision.
  </acceptance_criteria>
  <done>
    index.ts now wraps EngramMcp + defaultHandler in an OAuthProvider default export. EngramMcp.init() wires registerTools with the lazy props getter. Phase 1 placeholder content removed. TypeScript compiles cleanly. Local `interface Env` cleanup is deferred to Task 3 (after worker-configuration.d.ts is regenerated).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add v2 migration + KV bindings to packages/mcp-server/wrangler.jsonc, remove deferred JSDoc</name>
  <files>packages/mcp-server/wrangler.jsonc</files>
  <read_first>
    - packages/mcp-server/wrangler.jsonc (current Phase 1 state — what to PRESERVE vs ADD vs REMOVE)
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Pattern 6 (lines 560-591) — verbatim final shape
    - .planning/phases/03-mcp-server-scaffold/03-PATTERNS.md §11 (lines 553-580) — three required changes (remove JSDoc, add v2, add kv_namespaces)
    - .planning/phases/03-mcp-server-scaffold/03-CONTEXT.md §D-09 — acceptance criteria for this edit
    - scripts/lint-wrangler.mjs (FND-08 lint that runs against this file — must continue to pass)
    - packages/workspace-do/wrangler.test.jsonc (sibling test config showing the two-migration shape)
  </read_first>
  <behavior>
    - Test 1: file is valid JSONC (jsonc-parser parses without errors)
    - Test 2: `migrations` array has exactly 2 entries with tags "v1" and "v2"
    - Test 3: migrations[0].new_sqlite_classes === ["WorkspaceDO"] (preserved from Phase 1)
    - Test 4: migrations[1].new_sqlite_classes === ["EngramMcp"] (new, per D-09)
    - Test 5: NEITHER migration entry contains a `new_classes` array (FND-08 lint)
    - Test 6: `kv_namespaces` array exists with two entries: { binding: "OAUTH_KV", id: ... } AND { binding: "ENGRAM_IDENTITIES", id: ... }
    - Test 7: durable_objects.bindings still contains BOTH `{ name: "MCP_OBJECT", class_name: "EngramMcp" }` AND `{ name: "WORKSPACE", class_name: "WorkspaceDO" }` (preserved from Phase 1)
    - Test 8: Phase 1's "deferred to v2" JSDoc comment block (lines 22-26 of the current file) is REMOVED — D-09 acceptance
    - Test 9: file does NOT contain any literal value for `COOKIE_ENCRYPTION_KEY` (T-03-COOKIE — secrets are set via `wrangler secret put`, never committed)
    - Test 10: `npm run lint:wrangler` exits 0
  </behavior>
  <action>
    Modify packages/mcp-server/wrangler.jsonc per 03-RESEARCH.md §Pattern 6. Three changes: (1) REMOVE the Phase 1 "deferred to v2" JSDoc comment block (current lines 22-26: the block starting with `// WorkspaceDO is re-exported from @engram/workspace-do — see src/index.ts:30.` through `// McpAgent persistence semantics are real (see REVIEW-FIX WR-06).`). Replace with a single-line trailing comment on the v2 entry like `// v2 added EngramMcp (Phase 3)`. (2) ADD a second migration entry after the v1 entry: `{ "tag": "v2", "new_sqlite_classes": ["EngramMcp"] }`. (3) ADD a `kv_namespaces` block (place it between `durable_objects` and `migrations` for readability — order is not semantically significant). The kv_namespaces entries use PLACEHOLDER IDs since the real ones come from `wrangler kv namespace create` (Plan 06 README documents Russell's setup step). Use a literal placeholder string that the lint won't flag — `"<id-from-wrangler-kv-namespace-create>"`. The placeholder makes the file syntactically valid for `wrangler deploy --dry-run` even though deploying against this would fail (which is what we want — production deploy requires Russell to swap real IDs in). Final JSONC shape matches 03-RESEARCH.md Pattern 6 lines 560-591 verbatim. PRESERVE the existing `$schema`, `name`, `main`, `compatibility_date`, `compatibility_flags`, `observability`, and `durable_objects` blocks unchanged. Do NOT add a `COOKIE_ENCRYPTION_KEY` line anywhere in the file — that secret is set via `wrangler secret put` and is never declared in wrangler.jsonc (T-03-COOKIE). After editing, run `npm run lint:wrangler` to confirm the lint still exits 0 (the v2 entry uses `new_sqlite_classes`, not `new_classes`).
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; node -e "const fs=require('fs'); const jp=require('jsonc-parser'); const text=fs.readFileSync('packages/mcp-server/wrangler.jsonc','utf8'); const errs=[]; const c=jp.parse(text,errs,{allowTrailingComma:true}); if(errs.length)throw new Error('JSONC parse: '+JSON.stringify(errs)); if(!Array.isArray(c.migrations))throw new Error('no migrations'); if(c.migrations.length!==2)throw new Error('expected 2 migrations, got '+c.migrations.length); if(c.migrations[0].tag!=='v1')throw new Error('m[0].tag!=v1'); if(c.migrations[1].tag!=='v2')throw new Error('m[1].tag!=v2'); if(!c.migrations[0].new_sqlite_classes||!c.migrations[0].new_sqlite_classes.includes('WorkspaceDO'))throw new Error('v1 missing WorkspaceDO'); if(!c.migrations[1].new_sqlite_classes||!c.migrations[1].new_sqlite_classes.includes('EngramMcp'))throw new Error('v2 missing EngramMcp'); if(c.migrations[0].new_classes)throw new Error('v1 has new_classes (FND-08 violation)'); if(c.migrations[1].new_classes)throw new Error('v2 has new_classes (FND-08 violation)'); if(!Array.isArray(c.kv_namespaces))throw new Error('no kv_namespaces'); const kvBindings=c.kv_namespaces.map(k=>k.binding); if(!kvBindings.includes('OAUTH_KV'))throw new Error('OAUTH_KV missing'); if(!kvBindings.includes('ENGRAM_IDENTITIES'))throw new Error('ENGRAM_IDENTITIES missing'); if(text.includes('COOKIE_ENCRYPTION_KEY'))throw new Error('COOKIE_ENCRYPTION_KEY leaked in JSONC (T-03-COOKIE violation)'); if(text.includes('deferred to v2')||text.includes('REVIEW-FIX WR-06'))throw new Error('Phase 1 deferred-to-v2 JSDoc not removed'); console.log('OK');" &amp;&amp; npm run lint:wrangler</automated>
  </verify>
  <acceptance_criteria>
    - `packages/mcp-server/wrangler.jsonc` is valid JSONC (jsonc-parser succeeds)
    - `migrations` array has exactly 2 entries
    - `migrations[0].tag === "v1"` AND `migrations[0].new_sqlite_classes` contains `"WorkspaceDO"`
    - `migrations[1].tag === "v2"` AND `migrations[1].new_sqlite_classes` contains `"EngramMcp"`
    - NO migration entry has a `new_classes` array (FND-08)
    - `kv_namespaces` array contains TWO entries with bindings `"OAUTH_KV"` and `"ENGRAM_IDENTITIES"`
    - `durable_objects.bindings` still contains BOTH `MCP_OBJECT → EngramMcp` and `WORKSPACE → WorkspaceDO` (preserved)
    - File does NOT contain the literal string `"deferred to v2"` or `"REVIEW-FIX WR-06"` (Phase 1 JSDoc removed, D-09 acceptance)
    - File does NOT contain the literal string `COOKIE_ENCRYPTION_KEY` (T-03-COOKIE — secrets never in JSONC)
    - `npm run lint:wrangler` exits 0
  </acceptance_criteria>
  <done>
    wrangler.jsonc has v1 + v2 migrations declaring BOTH DO classes under `new_sqlite_classes`. KV bindings for OAUTH_KV + ENGRAM_IDENTITIES declared with placeholder IDs. Phase 1 deferred JSDoc removed per D-09. FND-08 lint passes. COOKIE_ENCRYPTION_KEY is correctly absent (set via wrangler secret put at deploy time).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regenerate worker-configuration.d.ts + remove local `interface Env` from oauth.ts AND index.ts + run wrangler deploy --dry-run gate</name>
  <files>packages/mcp-server/worker-configuration.d.ts, packages/mcp-server/src/oauth.ts, packages/mcp-server/src/index.ts</files>
  <read_first>
    - packages/mcp-server/package.json (the `types:gen` script: `wrangler types`)
    - packages/mcp-server/wrangler.jsonc (just-updated Task 2 — new bindings to regenerate types for)
    - packages/mcp-server/src/oauth.ts (Plan 04 Task 1 declared an inline `interface Env { ... }` here as a temporary bridge — this task removes it)
    - packages/mcp-server/src/index.ts (Task 1 of this plan may have left a temporary local Env declaration here — this task removes it)
    - .planning/phases/03-mcp-server-scaffold/03-CONTEXT.md §Claude's Discretion → "Worker Env interface shape"
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Pitfall 3 (lines 673-678) — `wrangler deploy --dry-run` is the canary for migration/class declaration mismatch
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Assumptions A5 (line 917) — types regen reliability
  </read_first>
  <behavior>
    - Test 1: `npm run types:gen --workspace=@engram/mcp-server` (which runs `wrangler types`) exits 0 and (re)generates `packages/mcp-server/worker-configuration.d.ts`
    - Test 2: the regenerated d.ts contains type declarations for: WORKSPACE (DurableObjectNamespace), MCP_OBJECT (DurableObjectNamespace), ENGRAM_IDENTITIES (KVNamespace), OAUTH_KV (KVNamespace)
    - Test 3: ALL inline `interface Env { ... }` declarations are removed from BOTH `packages/mcp-server/src/oauth.ts` AND `packages/mcp-server/src/index.ts` — `grep -c "^interface Env"` against EACH file returns 0 (T-03-ENV-DRIFT — checker WARNING 1)
    - Test 4: `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` exits 0 (no Env-related compile errors after both the regen AND the local declaration removals — confirms the global d.ts now provides the canonical Env for both files)
    - Test 5: `npx wrangler deploy --dry-run --config packages/mcp-server/wrangler.jsonc` exits 0 (RESEARCH Pitfall 3 canary — validates EngramMcp class is declared in some migration AND the binding resolves)
    - Test 6: index.ts and oauth.ts both compile cleanly with ONLY the global Env from worker-configuration.d.ts in scope (no fall-through to a local declaration)
  </behavior>
  <action>
    Run `npm run types:gen --workspace=@engram/mcp-server` from repo root (this invokes `wrangler types` per packages/mcp-server/package.json:9). Confirm it regenerates `packages/mcp-server/worker-configuration.d.ts` — the regenerated file should contain a global `Cloudflare.Env` interface (or equivalent — wrangler types convention) with the four new bindings: WORKSPACE, MCP_OBJECT, ENGRAM_IDENTITIES, OAUTH_KV. If the regenerated file does NOT include OAUTH_KV or ENGRAM_IDENTITIES, inspect wrangler.jsonc — likely a typo in the kv_namespaces block.

    After regen, perform the **local Env cleanup** in BOTH source files (T-03-ENV-DRIFT — checker WARNING 1 fix):

    1. **packages/mcp-server/src/oauth.ts** — Plan 04 Task 1 declared an inline `interface Env { WORKSPACE: DurableObjectNamespace; MCP_OBJECT: DurableObjectNamespace; ENGRAM_IDENTITIES: KVNamespace; OAUTH_KV: KVNamespace; OAUTH_PROVIDER: any; COOKIE_ENCRYPTION_KEY: string; }` (or similar shape) as a temporary bridge because `worker-configuration.d.ts` was not yet regenerated at Plan 04 time. REMOVE this entire declaration block now. The `defaultHandler: ExportedHandler<Env>` reference will resolve through the regenerated global d.ts.

    2. **packages/mcp-server/src/index.ts** — if Task 1 (or Plan 01's residue) left a temporary local `interface Env` declaration here, REMOVE it now. The `McpAgent<Env, unknown, EngramProps>` reference will resolve through the regenerated global d.ts.

    After both removals, run `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` to confirm the cleanup compiles. If TS errors surface (e.g., missing field on Env), the global d.ts is incomplete — inspect wrangler.jsonc for missing bindings and re-regen rather than re-adding a local declaration. The acceptance criteria's `grep -c "^interface Env"` assertions against BOTH files MUST return 0.

    Then run `npx wrangler deploy --dry-run --config packages/mcp-server/wrangler.jsonc` — this validates the wrangler config is well-formed AND that both DO classes (EngramMcp + WorkspaceDO) are declared in migrations. If the dry-run fails with "class not declared in any migration" — that's RESEARCH Pitfall 3 firing, fix wrangler.jsonc; if it fails with KV namespace ID errors — that's the placeholder IDs (acceptable for dry-run, fails differently — dry-run usually tolerates placeholders since it doesn't validate KV existence remotely). If dry-run fails specifically because of placeholder KV IDs, document the failure mode in the SUMMARY and accept the test condition as "dry-run validates migration shape, KV placeholder validation deferred to real deploy in Phase 7" — adjust the acceptance criteria accordingly. NOTE: if `wrangler deploy --dry-run` requires authentication that isn't present in the dev environment, the verify command may need adjustment (e.g., `wrangler deploy --dry-run --outdir /tmp/dry-run-out`); document and adjust. Goal: capture the deploy-dry-run signal NOW so deploy regressions don't slip to Phase 7.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; npm run types:gen --workspace=@engram/mcp-server &amp;&amp; test -f packages/mcp-server/worker-configuration.d.ts &amp;&amp; grep -q "ENGRAM_IDENTITIES" packages/mcp-server/worker-configuration.d.ts &amp;&amp; grep -q "OAUTH_KV" packages/mcp-server/worker-configuration.d.ts &amp;&amp; grep -q "WORKSPACE" packages/mcp-server/worker-configuration.d.ts &amp;&amp; grep -q "MCP_OBJECT" packages/mcp-server/worker-configuration.d.ts &amp;&amp; [ "$(grep -c '^interface Env' packages/mcp-server/src/oauth.ts)" = "0" ] &amp;&amp; [ "$(grep -c '^interface Env' packages/mcp-server/src/index.ts)" = "0" ] &amp;&amp; npx tsc -p packages/mcp-server/tsconfig.json --noEmit &amp;&amp; (npx wrangler deploy --dry-run --config packages/mcp-server/wrangler.jsonc 2>&amp;1 | tee /tmp/wrangler-dry.txt; grep -qE "Total Upload|Compiled|deploy" /tmp/wrangler-dry.txt &amp;&amp; ! grep -qE "not declared in any migration|new_classes" /tmp/wrangler-dry.txt)</automated>
  </verify>
  <acceptance_criteria>
    - `packages/mcp-server/worker-configuration.d.ts` exists after the regen command
    - File contains all 4 binding identifiers: `ENGRAM_IDENTITIES`, `OAUTH_KV`, `WORKSPACE`, `MCP_OBJECT`
    - `grep -c "^interface Env" packages/mcp-server/src/oauth.ts` returns `0` (the inline declaration Plan 04 Task 1 introduced has been removed — T-03-ENV-DRIFT, checker WARNING 1)
    - `grep -c "^interface Env" packages/mcp-server/src/index.ts` returns `0` (any temporary local declaration from Task 1 or Plan 01 residue has been removed — T-03-ENV-DRIFT, checker WARNING 1)
    - `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` exits 0 after the regen AND after both local Env declarations are removed (proves the global d.ts is sufficient as the sole Env source)
    - `npx wrangler deploy --dry-run --config packages/mcp-server/wrangler.jsonc` either (a) exits 0 — preferred — or (b) fails ONLY due to placeholder KV namespace IDs (which is acceptable; document the failure mode in SUMMARY). In either case, the output MUST NOT contain "class not declared in any migration" or "new_classes" (Pitfall 3 + Pitfall 8 canaries)
  </acceptance_criteria>
  <done>
    worker-configuration.d.ts regenerated with all new bindings (WORKSPACE, MCP_OBJECT, ENGRAM_IDENTITIES, OAUTH_KV). ALL inline `interface Env` declarations removed from BOTH `oauth.ts` and `index.ts` — the canonical Env now flows exclusively from the regenerated global d.ts, eliminating the T-03-ENV-DRIFT silent-shape-mismatch risk surfaced by the checker (WARNING 1). TypeScript compiles cleanly against the regenerated types. `wrangler deploy --dry-run` validates the migration + class declaration are well-formed (Pitfall 3 canary).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Turn index.test.ts from RED to GREEN (default export is OAuthProvider; EngramMcp registers 5 tools; v1+v2 migration shape)</name>
  <files>packages/mcp-server/src/__tests__/index.test.ts</files>
  <read_first>
    - packages/mcp-server/src/__tests__/index.test.ts (Wave 0 RED stub — replace it.skip with it() bodies)
    - packages/mcp-server/src/index.ts (just-updated Task 1)
    - packages/mcp-server/wrangler.jsonc (just-updated Task 2)
    - packages/workspace-do/src/__tests__/defense-in-depth.test.ts (canonical test shape using runInDurableObject if needed)
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Validation Architecture lines 985-988 (MCP-01/02/03 test commands)
  </read_first>
  <behavior>
    - Test 1: `npm test --workspace=@engram/mcp-server -- --run index` exits 0
    - Test 2 (default export shape): `const mod = await import("../index.js"); expect(mod.default).toBeDefined(); expect(typeof mod.default.fetch).toBe("function")` (OAuthProvider exposes a fetch handler)
    - Test 3 (named exports): EngramMcp, EngramProps (type — checked structurally via at-least-one consumer line), and WorkspaceDO are all exported
    - Test 4 (EngramMcp registers 5 tools): spy on McpServer.prototype.registerTool BEFORE constructing an EngramMcp instance (via `runInDurableObject`), then call `instance.init()` and assert spy was called 5 times with names ["remember","recall","search","forget","ingest"]
    - Test 5 (wrangler.jsonc v2 migration shape): readFileSync wrangler.jsonc, JSONC-parse, assert migrations[0] has tag v1 + WorkspaceDO in new_sqlite_classes, assert migrations[1] has tag v2 + EngramMcp in new_sqlite_classes
    - Test 6 (no streamableHttp anti-pattern): readFileSync index.ts source, assert no occurrence of `streamableHttp` (anti-pattern from RESEARCH)
    - Test 7 (defaultHandler wired): the OAuthProvider construction arg contains defaultHandler — verified by importing index.js and using runtime inspection (e.g., check that calling `mod.default.fetch(new Request("https://x.dev/health"))` returns 200 — exercises the defaultHandler indirectly) OR a structural source-grep
  </behavior>
  <action>
    Replace it.skip/it.todo stubs in packages/mcp-server/src/__tests__/index.test.ts. Imports: `import { describe, it, expect, vi, afterEach } from "vitest"`, `import { readFileSync } from "node:fs"`, `import { parse as parseJsonc } from "jsonc-parser"`, `import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"`, dynamic `import("../index.js")` inside test bodies as needed. Test groups: (1) `describe('module shape (MCP-01/02)')`: `it('default export is fetchable (OAuthProvider exposes fetch)', async () => { const mod = await import("../index.js"); expect(typeof mod.default.fetch).toBe("function"); })`. `it('exports EngramMcp class extending McpAgent', async () => { const mod = await import("../index.js"); expect(typeof mod.EngramMcp).toBe("function"); })`. `it('re-exports WorkspaceDO from @engram/workspace-do', async () => { const mod = await import("../index.js"); expect(typeof mod.WorkspaceDO).toBe("function"); })`. (2) `describe('EngramMcp.init registers 5 tools (MCP-06 integration)')`: spy on `McpServer.prototype.registerTool`; construct an EngramMcp under `runInDurableObject` (use env.MCP_OBJECT from wrangler.test.jsonc); call `instance.init()`; assert spy called 5 times with the 5 expected names. NOTE: if runInDurableObject + McpAgent integration is non-trivial within the test pool (McpAgent extends DurableObject but has init() as a real method, not the DO constructor), an alternative is to import `EngramMcp` directly, instantiate via `new EngramMcp(mockCtx, mockEnv)` with a minimal ctx mock — McpAgent likely accepts (ctx, env) like any DurableObject. Pick whichever is simpler; document the choice. (3) `describe('wrangler.jsonc v2 migration shape (MCP-03)')`: readFileSync wrangler.jsonc, parseJsonc, assert two-entry migrations array with v1.new_sqlite_classes containing WorkspaceDO and v2.new_sqlite_classes containing EngramMcp. (4) `describe('anti-patterns')`: readFileSync index.ts source, assert NOT match `/streamableHttp/` (RESEARCH §Anti-Patterns) and NOT match `args\.workspace_id` in non-comment lines (defense-in-depth structural inheritance from Plan 03). `afterEach(() => { vi.restoreAllMocks(); })`. After updates, run `npm test --workspace=@engram/mcp-server -- --run index` and confirm zero failures, zero skipped.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; ( ! grep -E "it\.(skip|todo)\(" packages/mcp-server/src/__tests__/index.test.ts ) &amp;&amp; grep -q "OAuthProvider\\|default.fetch\\|mod.default" packages/mcp-server/src/__tests__/index.test.ts &amp;&amp; grep -q "EngramMcp" packages/mcp-server/src/__tests__/index.test.ts &amp;&amp; grep -q "new_sqlite_classes" packages/mcp-server/src/__tests__/index.test.ts &amp;&amp; grep -q "streamableHttp" packages/mcp-server/src/__tests__/index.test.ts &amp;&amp; npm test --workspace=@engram/mcp-server -- --run 2>&amp;1 | tee /tmp/full-test-out.txt &amp;&amp; ( ! grep -qE "FAIL|failed" /tmp/full-test-out.txt )</automated>
  </verify>
  <acceptance_criteria>
    - `packages/mcp-server/src/__tests__/index.test.ts` contains NO `it.skip(` or `it.todo(`
    - File contains assertion on default-export `fetch` function (verifies OAuthProvider wraps the export)
    - File contains assertion on named export `EngramMcp`
    - File contains assertion that walks `wrangler.jsonc` migrations and verifies both v1 (WorkspaceDO) and v2 (EngramMcp) entries
    - File contains a `streamableHttp` reference inside a NEGATIVE assertion (`not.toMatch(/streamableHttp/)` or `not.toContain("streamableHttp")`) — anti-pattern enforcement
    - Test file references `registerTool` (verifying the 5-tool count via spy)
    - `npm test --workspace=@engram/mcp-server -- --run` exits 0 (FULL mcp-server suite — all test files now GREEN)
    - The vitest output contains 0 failed tests, 0 skipped
    - The vitest output contains ≥ 4 passing tests for index.test.ts
  </acceptance_criteria>
  <done>
    index.test.ts is GREEN. MCP-01/02/03 are unit-asserted: default export is OAuthProvider-shaped (fetch handler), EngramMcp.init registers 5 tools, wrangler.jsonc declares both DO classes in new_sqlite_classes. Anti-pattern check (streamableHttp) enforced structurally. The full mcp-server test suite is now GREEN — Wave 0 RED stubs are all replaced.
  </done>
</task>

</tasks>

<verification>
After all 4 tasks land:

1. **Full mcp-server test suite GREEN (all 4 prior test files + index.test.ts):**
   ```bash
   cd /Users/rmoore/Workspaces/engram
   npm test --workspace=@engram/mcp-server -- --run
   ```
   Exit 0. Output shows schemas + error-mapping + tools + oauth + index tests all passing. Zero failed. Zero skipped.

2. **Cross-workspace non-regression:**
   ```bash
   npm test --workspaces -- --run
   ```
   Exit 0. workspace-do's 9+ tests still green.

3. **TypeScript compiles cleanly across the full workspace:**
   ```bash
   npx tsc -p packages/mcp-server/tsconfig.json --noEmit
   npx tsc -p packages/workspace-do/tsconfig.json --noEmit
   ```
   Both exit 0.

4. **FND-08 wrangler lint passes:**
   ```bash
   npm run lint:wrangler
   ```
   Exit 0.

5. **STO-10 blockconcurrency lint passes (no regression):**
   ```bash
   npm run lint:blockconcurrency
   ```
   Exit 0.

6. **Wrangler deploy dry-run validates migration + class declaration:**
   ```bash
   npx wrangler deploy --dry-run --config packages/mcp-server/wrangler.jsonc
   ```
   Exit 0 (preferred) OR fails ONLY on placeholder KV namespace IDs (acceptable — Russell swaps real IDs at deploy time per Plan 06 README). MUST NOT fail with "class not declared in any migration" or "new_classes" warnings.

7. **T-03-ENV-DRIFT structural check (checker WARNING 1 — no local Env declarations after Task 3):**
   ```bash
   grep -c "^interface Env" packages/mcp-server/src/oauth.ts
   grep -c "^interface Env" packages/mcp-server/src/index.ts
   ```
   BOTH return `0`. The canonical Env now comes exclusively from the regenerated `worker-configuration.d.ts`.

8. **All 7 phase requirement IDs covered:**
   - MCP-01: index.ts uses `agents/mcp` McpAgent (no streamableHttp anti-pattern) ✓
   - MCP-02: `EngramMcp.serve("/mcp", { binding: "MCP_OBJECT" })` ✓
   - MCP-03: wrangler.jsonc migrations[] has BOTH WorkspaceDO (v1) and EngramMcp (v2) under new_sqlite_classes ✓
   - MCP-04: oauth.ts /authorize populates props from ENGRAM_IDENTITIES KV; OAuthProvider validates JWT before apiHandler dispatch (Plan 04) ✓
   - MCP-05: tools.ts Phase-4-ready comment block + structural absence of args.workspace_id (Plan 03) ✓
   - MCP-06: 5 tools registered in EngramMcp.init via registerTools, each with zod inputSchema (Plans 02, 03, this plan) ✓
   - MCP-09: deferred to Plan 06 manual smoke ☐ (Plan 06)
</verification>

<success_criteria>
- `packages/mcp-server/src/index.ts` default export is `new OAuthProvider({...})` wiring EngramMcp + defaultHandler
- `EngramMcp.init()` calls `registerTools(this.server, () => this.props, this.env)`
- Phase 1 `Phase1Pong` interface and `export default { fetch() }` are removed
- `packages/mcp-server/wrangler.jsonc` migrations[] has v1 (WorkspaceDO) + v2 (EngramMcp) using `new_sqlite_classes`
- `packages/mcp-server/wrangler.jsonc` kv_namespaces declares OAUTH_KV + ENGRAM_IDENTITIES bindings
- Phase 1 "deferred to v2" JSDoc removed (D-09)
- No `COOKIE_ENCRYPTION_KEY` literal anywhere in wrangler.jsonc (T-03-COOKIE)
- `packages/mcp-server/worker-configuration.d.ts` regenerated with all 4 new bindings
- ALL inline `interface Env { ... }` declarations removed from BOTH `packages/mcp-server/src/oauth.ts` AND `packages/mcp-server/src/index.ts` (T-03-ENV-DRIFT — checker WARNING 1; `grep -c "^interface Env"` on each file returns 0)
- `npm run lint:wrangler` exits 0
- `npx wrangler deploy --dry-run` exits 0 OR fails only on placeholder KV namespace IDs (acceptable, documented)
- `npm test --workspace=@engram/mcp-server -- --run` exits 0 with zero failed, zero skipped
- `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` exits 0
- The full mcp-server test suite is GREEN; Phase 3 is functionally complete except for Plan 06's README + MCP Inspector manual smoke
</success_criteria>

<output>
Create `.planning/phases/03-mcp-server-scaffold/03-05-SUMMARY.md` when done. Required sections: Outcome, Files Changed, Verification (include the wrangler deploy --dry-run output — paste verbatim), Cross-Phase Contracts (highlight: full defense-in-depth chain — T-03-DD-IN structural at schemas, T-03-DD-RT structural at tools, T-03-PROPS structural at oauth, T-03-JWT mitigated by library wiring, T-03-ENV-DRIFT eliminated via Task 3 local Env cleanup per checker WARNING 1), Deviations (any divergence from Pattern 6 wrangler.jsonc shape), Next Plan (06 — DEP-05 README + MCP-09 manual smoke).
</output>
</output>
