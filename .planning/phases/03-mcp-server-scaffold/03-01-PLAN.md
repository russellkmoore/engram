---
phase: 03-mcp-server-scaffold
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/mcp-server/package.json
  - packages/mcp-server/vitest.config.ts
  - packages/mcp-server/wrangler.test.jsonc
  - packages/mcp-server/src/__tests__/schemas.test.ts
  - packages/mcp-server/src/__tests__/tools.test.ts
  - packages/mcp-server/src/__tests__/oauth.test.ts
  - packages/mcp-server/src/__tests__/index.test.ts
  - packages/mcp-server/src/index.ts
  - scripts/kv-bootstrap.mjs
  - package.json
autonomous: true
requirements:
  - MCP-01
  - MCP-06
tags:
  - mcp-server
  - test-infra
  - oauth
  - cloudflare-workers
user_setup:
  - service: cloudflare-kv
    why: "Two KV namespaces (OAUTH_KV, ENGRAM_IDENTITIES) must be created against Russell's Cloudflare account before deploy or `wrangler dev --remote` can resolve KV bindings. Documented in DEP-05 README (Plan 06), executed by Russell at deploy time. Wave 0 only adds the bootstrap script + script registration — actual KV creation is a human action against Cloudflare."
    env_vars: []
    dashboard_config:
      - task: "Create OAUTH_KV namespace: `wrangler kv namespace create OAUTH_KV` — paste returned ID into wrangler.jsonc (Plan 05)"
        location: "Cloudflare dashboard or CLI"
      - task: "Create ENGRAM_IDENTITIES namespace: `wrangler kv namespace create ENGRAM_IDENTITIES` — paste returned ID into wrangler.jsonc (Plan 05)"
        location: "Cloudflare dashboard or CLI"
      - task: "Set encryption secret: `wrangler secret put COOKIE_ENCRYPTION_KEY` — random 32+ character string"
        location: "Cloudflare CLI"

must_haves:
  truths:
    - "vitest can resolve @engram/mcp-server's test config and discover the 4 RED test files"
    - "All 4 RED test files reference real (not yet implemented) module paths and are skipped/pending — they must FAIL when implementation lands wrong"
    - "package.json declares @cloudflare/workers-oauth-provider@0.7.0 (exact-pin), zod@^4, and vitest/test scripts"
    - "EngramProps interface is exported from packages/mcp-server/src/index.ts so Wave 1+ can import it without circular dependency"
    - "scripts/kv-bootstrap.mjs exists and exits cleanly when --help is passed"
  artifacts:
    - path: "packages/mcp-server/vitest.config.ts"
      provides: "vitest pool config pointing at wrangler.test.jsonc (single workerd project, no node-pool)"
      contains: "@cloudflare/vitest-pool-workers"
    - path: "packages/mcp-server/wrangler.test.jsonc"
      provides: "Test-only wrangler config with MCP_OBJECT + WORKSPACE bindings + v1/v2 migrations"
      contains: "EngramMcp"
    - path: "packages/mcp-server/src/__tests__/schemas.test.ts"
      provides: "RED stub: 5 it.skip for schema-shape + 1 it.skip for defense-in-depth (no workspace_id field)"
      contains: "RememberInputSchema"
    - path: "packages/mcp-server/src/__tests__/tools.test.ts"
      provides: "RED stub: 5 it.skip for stub MethodNotFound + 1 it.skip for tool count assertion"
      contains: "MethodNotFound"
    - path: "packages/mcp-server/src/__tests__/oauth.test.ts"
      provides: "RED stub: KV-lookup contract + 403 on missing KV entry + /health route"
      contains: "ENGRAM_IDENTITIES"
    - path: "packages/mcp-server/src/__tests__/index.test.ts"
      provides: "RED stub: default export is OAuthProvider; EngramMcp registers 5 tools"
      contains: "OAuthProvider"
    - path: "scripts/kv-bootstrap.mjs"
      provides: "CLI script for seeding ENGRAM_IDENTITIES KV — wraps wrangler kv key put"
      contains: "ENGRAM_IDENTITIES"
    - path: "packages/mcp-server/package.json"
      provides: "Phase 3 deps declared; test scripts wired"
      contains: "@cloudflare/workers-oauth-provider"
  key_links:
    - from: "packages/mcp-server/vitest.config.ts"
      to: "packages/mcp-server/wrangler.test.jsonc"
      via: "cloudflareTest({ wrangler: { configPath: './wrangler.test.jsonc' } })"
      pattern: "configPath.*wrangler\\.test\\.jsonc"
    - from: "packages/mcp-server/src/__tests__/*.test.ts"
      to: "packages/mcp-server/src/index.ts"
      via: "import type { EngramProps } from \"../index.js\""
      pattern: "EngramProps"
    - from: "package.json (root) scripts"
      to: "scripts/kv-bootstrap.mjs"
      via: "kv:bootstrap npm script"
      pattern: "kv:bootstrap"
---

<objective>
Establish all test-infrastructure scaffolding, dependency declarations, and RED test stubs that downstream Wave 1-3 plans will turn GREEN. Also publish the `EngramProps` interface from `index.ts` (without swapping the default export yet) so Wave 1+ files can import it without circular-dep workarounds.

Purpose: Wave 0 produces the failure surface — every test file imports modules that don't yet exist or asserts behaviors that aren't implemented. Wave 1-3 implementations make these tests GREEN one by one, providing the sampling rate documented in 03-VALIDATION.md.

Output: vitest config + test pool config + 4 RED test files + package.json deps + KV bootstrap script + EngramProps interface exported from index.ts. No production behavior changes yet (Phase 1 stub fetch handler stays intact until Plan 05 swaps it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mcp-server-scaffold/03-CONTEXT.md
@.planning/phases/03-mcp-server-scaffold/03-RESEARCH.md
@.planning/phases/03-mcp-server-scaffold/03-PATTERNS.md
@.planning/phases/03-mcp-server-scaffold/03-VALIDATION.md
@packages/mcp-server/src/index.ts
@packages/mcp-server/wrangler.jsonc
@packages/mcp-server/package.json
@packages/workspace-do/wrangler.test.jsonc
@packages/workspace-do/vitest.config.ts
@packages/workspace-do/package.json
@packages/workspace-do/src/__tests__/defense-in-depth.test.ts
@packages/workspace-do/src/index.ts
@scripts/lint-wrangler.mjs

<interfaces>
<!-- Key contracts the executor needs in-hand. Extracted from codebase + RESEARCH.md. -->

From packages/workspace-do/src/index.ts (Phase 2 — what tool handlers will eventually route to):
```typescript
export class WorkspaceDO extends DurableObject<unknown> {
  insertBlock(args: { workspace_id: string; block: Memory }): void
  getBlock(args: { workspace_id: string; id: string }): Memory
  lexicalSearchBlocks(args: { workspace_id: string; query: string; limit?: number }): LexicalSearchHit[]
  deleteBlock(args: { workspace_id: string; id: string; cascade?: boolean }): { blocks_deleted: number; relations_deleted: number }
  listMemoryTypes(args: { workspace_id: string }): MemoryType[]
  createInboxEntry(args: { workspace_id: string; entry: InboxEntry }): void
  listConflicts(args: { workspace_id: string; resolved?: boolean; limit?: number }): Conflict[]
}
export { NotFoundError } from "./errors.js";
```

EngramProps shape (Phase 3 publishes; Wave 0 emits the interface from index.ts):
```typescript
export interface EngramProps extends Record<string, unknown> {
  workspace_id: string;
  user_id: string;
}
```

Defense-in-depth contract (inherited from Phase 2 STO-07):
- this.ctx.id.name in WorkspaceDO is set to workspace_id at idFromName() time
- assertOwnsWorkspace throws McpError(InvalidRequest = -32600) when ctx.id.name !== args.workspace_id
- Phase 3 zod schemas MUST NOT include a workspace_id field — workspace is only ever sourced from JWT props
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test pool → workerd | Vitest test runner spawns workerd isolate via @cloudflare/vitest-pool-workers; tests run with real DO bindings declared in wrangler.test.jsonc. RED stubs do not execute privileged paths. |
| RED test imports → source modules | Test files import from modules that don't exist yet (schemas.ts, tools.ts, oauth.ts) — these imports MUST fail at the resolver during Wave 0, NOT silently succeed via accidental aliasing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-DD-IN | Tampering | packages/mcp-server/src/__tests__/schemas.test.ts | mitigate | RED stub asserts Object.keys(SchemaShape).indexOf('workspace_id') === -1 for all 5 schemas; turns GREEN in Wave 1 when schemas.ts lands; failure = a future contributor added workspace_id to a schema, breaking MT-1/STO-07 defense-in-depth. |
| T-03-DD-RT | Tampering | packages/mcp-server/src/__tests__/tools.test.ts | mitigate | RED stub asserts grep of registered tool callbacks contains 'this.props.workspace_id' but NOT 'args.workspace_id' (Wave 2 makes GREEN when tools.ts lands). Inherits Phase 2 STO-07 contract. |
| T-03-JWT | Spoofing / Elevation of Privilege | OAuth provider routing in packages/mcp-server/src/__tests__/oauth.test.ts | mitigate | RED stub asserts unauthenticated /mcp requests return 401; OAuthProvider is the JWT validation primitive (library-provided, RFC 6749/9728 compliant); turns GREEN in Plan 05 when default export = OAuthProvider. |
| T-03-PROPS | Spoofing | packages/mcp-server/src/__tests__/oauth.test.ts | mitigate | RED stub asserts ENGRAM_IDENTITIES KV lookup is the ONLY source of props.{workspace_id, user_id} — no value derived from request body or query params; turns GREEN in Wave 2 (oauth.ts). |
| T-03-KV-LEAK | Information Disclosure | scripts/kv-bootstrap.mjs | mitigate | Script logs MUST NOT print KV value content to stdout; only the sub key is echoed for confirmation. Sanitization gate in the script source enforced via grep assertion in this plan's verify. |
| T-03-SC | Tampering | npm install of @cloudflare/workers-oauth-provider@0.7.0 + zod@^4 | mitigate | RESEARCH.md §Package Legitimacy Audit ran slopcheck on both: [OK] verdict for both packages. Cloudflare-published + Colin Hacks-maintained, both high-download mature packages. Not [ASSUMED]/[SUS]. No blocking-human checkpoint required. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 3 deps + test scripts to packages/mcp-server/package.json</name>
  <files>packages/mcp-server/package.json</files>
  <read_first>
    - packages/mcp-server/package.json (Phase 1 deps that must be preserved: agents@^0.13.2, @modelcontextprotocol/sdk@^1.29.0, @engram/types, @engram/schema, @engram/workspace-do)
    - packages/workspace-do/package.json (sibling — exact-template for test scripts + devDeps blocks)
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Standard Stack (lines 86-117) and §Common Pitfalls 6+7 (lines 696-708)
    - .planning/phases/03-mcp-server-scaffold/03-PATTERNS.md §12 (lines 584-618)
  </read_first>
  <behavior>
    - Test 1 (verified via npm install): all declared deps resolve from registry without error
    - Test 2 (verified via grep): exact-pin "@cloudflare/workers-oauth-provider": "0.7.0" present (NOT ^0.7.0 — pre-1.0 lib per Pitfall 6 and Assumption A6)
    - Test 3 (verified via grep): "zod": "^4.0.0" (or "^4") present in dependencies
    - Test 4 (verified via grep): devDependencies contain @cloudflare/vitest-pool-workers and vitest
    - Test 5 (verified via grep): scripts contain "test": "vitest run" and "test:watch": "vitest"
    - Test 6: Phase 1 deps (agents@^0.13.2, @modelcontextprotocol/sdk@^1.29.0, @engram/*) are PRESERVED unchanged
  </behavior>
  <action>
    Modify packages/mcp-server/package.json. Preserve the existing Phase 1 dependencies block exactly. Add `"@cloudflare/workers-oauth-provider": "0.7.0"` (EXACT-PIN, NOT caret-pin, per RESEARCH.md Pitfall 6 + Assumption A6 — this is a pre-1.0 Cloudflare-maintained library and minor versions ship breaking changes) and `"zod": "^4.0.0"` to the dependencies block. Add a devDependencies block mirroring packages/workspace-do/package.json: `"@cloudflare/vitest-pool-workers": "^0.16.9"` and `"vitest": "^4.1.7"`. Add scripts: `"test": "vitest run"` and `"test:watch": "vitest"` alongside existing `dev`, `deploy`, `types:gen` scripts. Run `npm install` from the repo root after editing so npm hoists the new deps into the root node_modules (workspaces project — installs happen at root). Confirm the install completes without errors before declaring the task done. Do NOT add an `exports` field (the @engram/mcp-server package is a Worker, not a library — it has no public TS-source exports surface; only @engram/workspace-do, @engram/types, @engram/schema do).
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; npm install &amp;&amp; node -e "const p=require('./packages/mcp-server/package.json'); if(p.dependencies['@cloudflare/workers-oauth-provider']!=='0.7.0')throw new Error('oauth-provider not exact-pinned to 0.7.0'); if(!/^\^?4/.test(p.dependencies.zod||''))throw new Error('zod not ^4'); if(!p.devDependencies['@cloudflare/vitest-pool-workers'])throw new Error('missing vitest-pool-workers'); if(p.scripts.test!=='vitest run')throw new Error('missing test script'); if(!p.dependencies.agents)throw new Error('agents dep lost'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/mcp-server/package.json` dependencies has the literal key-value `"@cloudflare/workers-oauth-provider": "0.7.0"` (exact-pin, no caret)
    - `packages/mcp-server/package.json` dependencies has key `zod` matching `^4` (regex `^\^?4`)
    - `packages/mcp-server/package.json` devDependencies has keys `@cloudflare/vitest-pool-workers` AND `vitest`
    - `packages/mcp-server/package.json` scripts has `"test": "vitest run"` AND `"test:watch": "vitest"`
    - `packages/mcp-server/package.json` dependencies preserves `agents` (^0.13.2), `@modelcontextprotocol/sdk` (^1.29.0), `@engram/types`, `@engram/schema`, `@engram/workspace-do`
    - `npm install` from repo root exits 0
    - `node_modules/@cloudflare/workers-oauth-provider/` directory exists with `package.json` inside
    - `node_modules/zod/` directory exists with `package.json` inside whose version starts with `4.`
  </acceptance_criteria>
  <done>
    package.json declares all Phase 3 production + dev deps with correct version pinning (oauth-provider exact-pinned, zod caret-4); npm install resolves cleanly; agents/MCP SDK deps preserved unchanged. The new modules are physically present under node_modules and will be importable from Wave 1+ source files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add EngramProps interface to packages/mcp-server/src/index.ts (interface-first, no default-export swap)</name>
  <files>packages/mcp-server/src/index.ts</files>
  <read_first>
    - packages/mcp-server/src/index.ts (current Phase 1 stub — what to PRESERVE vs ADD)
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Pattern 1 (lines 261-296) — the EngramProps shape
    - .planning/phases/03-mcp-server-scaffold/03-CONTEXT.md §D-04 + §Claude's Discretion → "Worker Env interface shape"
    - .planning/phases/03-mcp-server-scaffold/03-PATTERNS.md §10 (lines 496-549) — full final index.ts shape (Plan 05 lands this)
  </read_first>
  <behavior>
    - Test 1 (typecheck): `EngramProps` interface is exported and has exact-shape `{ workspace_id: string; user_id: string }` plus `Record<string, unknown>` extension
    - Test 2 (typecheck): the McpAgent type-arg substitution `extends McpAgent<Env, unknown, EngramProps>` compiles WITHOUT swapping the Phase 1 fetch handler (Plan 05 swaps it)
    - Test 3 (Phase 1 behavior preserved): `export default { fetch() }` still exists and still returns Phase1Pong shape — Plan 05 will replace it
    - Test 4 (Phase 1 contract preserved): `export { WorkspaceDO } from "@engram/workspace-do"` re-export still present
  </behavior>
  <action>
    Modify packages/mcp-server/src/index.ts to add (NOT replace yet): the `EngramProps` interface export — exact shape from RESEARCH.md §Pattern 1: `export interface EngramProps extends Record<string, unknown> { workspace_id: string; user_id: string; }`. Place it AFTER the imports and BEFORE the EngramMcp class declaration. Update the EngramMcp class generic parameters from `extends McpAgent` to `extends McpAgent<Env, unknown, EngramProps>` so the type position is correct (the abstract `server` and `init()` Phase 1 stubs remain unchanged). PRESERVE everything else: the existing Phase 1 imports (McpAgent, McpServer, @engram/types type imports, SYSTEM_TYPES from @engram/schema), the `Phase1Pong` interface, the `export default { fetch() }` block, and the `export { WorkspaceDO } from "@engram/workspace-do"` line. Add a top-of-file JSDoc block (mirror packages/workspace-do/src/index.ts:1-64 style) documenting that Plan 05 will swap the default export to `new OAuthProvider(...)` and that EngramProps is the cross-plan contract Wave 1+ consumes. The current Phase 1 type-witness comment block should be updated with a note pointing at the Phase 3 evolution. The `Env` type reference in `McpAgent<Env, unknown, EngramProps>` will resolve to the wrangler-generated `worker-configuration.d.ts` Env interface — if that file doesn't yet contain the new KV bindings (it won't until Plan 05 runs `wrangler types`), TS will still accept `Env` because the d.ts declares a placeholder. If a compile error surfaces here, write a temporary local `interface Env { WORKSPACE: DurableObjectNamespace; MCP_OBJECT: DurableObjectNamespace; }` ABOVE the EngramMcp class — Plan 05 removes this temporary in favor of the regenerated worker-configuration.d.ts.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; grep -q "export interface EngramProps" packages/mcp-server/src/index.ts &amp;&amp; grep -q "workspace_id: string" packages/mcp-server/src/index.ts &amp;&amp; grep -q "user_id: string" packages/mcp-server/src/index.ts &amp;&amp; grep -q "export default" packages/mcp-server/src/index.ts &amp;&amp; grep -q "export { WorkspaceDO }" packages/mcp-server/src/index.ts &amp;&amp; npx tsc -p packages/mcp-server/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File contains the literal string `export interface EngramProps` (regex `export interface EngramProps`)
    - File contains the literal string `workspace_id: string` within the EngramProps interface body
    - File contains the literal string `user_id: string` within the EngramProps interface body
    - File contains `extends Record<string, unknown>` on the EngramProps interface (per Pattern 1)
    - File preserves the Phase 1 line `export { WorkspaceDO } from "@engram/workspace-do"`
    - File still has an `export default` block (Plan 05 swaps it, Plan 01 does not)
    - `npx tsc -p packages/mcp-server/tsconfig.json --noEmit` exits 0
  </acceptance_criteria>
  <done>
    EngramProps interface is exported from index.ts. Wave 1+ files can `import type { EngramProps } from "../index.js"` without circular-dep concerns. Phase 1 default export remains operational — Plan 05 will swap it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create vitest config, wrangler.test.jsonc, and 4 RED test files</name>
  <files>packages/mcp-server/vitest.config.ts, packages/mcp-server/wrangler.test.jsonc, packages/mcp-server/src/__tests__/schemas.test.ts, packages/mcp-server/src/__tests__/tools.test.ts, packages/mcp-server/src/__tests__/oauth.test.ts, packages/mcp-server/src/__tests__/index.test.ts</files>
  <read_first>
    - packages/workspace-do/vitest.config.ts (sibling — exact-template, but Phase 3 simplifies to single workerd project, no node-pool subprocess tests)
    - packages/workspace-do/wrangler.test.jsonc (sibling — exact-template with binding swaps for two DO classes + two migrations)
    - packages/workspace-do/src/__tests__/defense-in-depth.test.ts (lines 44-100 — canonical "describe + runInDurableObject + try/catch McpError" shape to mirror in tools.test.ts and index.test.ts)
    - packages/workspace-do/src/__tests__/seeding.test.ts (sibling — pure-unit test shape without runInDurableObject — mirror for schemas.test.ts)
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Validation Architecture (lines 971-1016) — required test commands per requirement
    - .planning/phases/03-mcp-server-scaffold/03-PATTERNS.md §5-7 (lines 299-432) — file-by-file analog mapping for test files
    - .planning/phases/03-mcp-server-scaffold/03-VALIDATION.md §Wave 0 Requirements (lines 62-71) — the four RED stub responsibilities
  </read_first>
  <behavior>
    - Test 1: `npm test --workspace=@engram/mcp-server -- --run` resolves the config and discovers all 4 test files (counts ≥4 collected, not 0)
    - Test 2 (RED state — schemas.test.ts): contains describe('schemas') with it.skip or it.todo for each of the 5 schemas + 1 it.skip asserting defense-in-depth contract (no workspace_id key)
    - Test 3 (RED state — tools.test.ts): contains describe('tools') with it.skip for 5 MethodNotFound assertions + 1 it.skip for "tools list has exactly 5 names" + 1 it.skip for "no handler reads args.workspace_id"
    - Test 4 (RED state — oauth.test.ts): contains describe('oauth') with it.skip for KV lookup contract, 403 on missing KV entry, /health no-auth, / no-auth
    - Test 5 (RED state — index.test.ts): contains describe('index') with it.skip for "default export is OAuthProvider", "EngramMcp registers 5 tools", "wrangler.jsonc v2 entry contains EngramMcp"
    - Test 6: `npm test --workspace=@engram/mcp-server -- --run` exits 0 (it.skip/it.todo do not fail; they pend)
  </behavior>
  <action>
    Create `packages/mcp-server/vitest.config.ts` mirroring `packages/workspace-do/vitest.config.ts` but SIMPLIFIED to a single workerd project (no node-pool — Phase 3 has no STO-10-style subprocess lint self-test). Final shape per 03-PATTERNS.md §6: `defineConfig({ test: { plugins: [cloudflareTest({ wrangler: { configPath: "./wrangler.test.jsonc" } })], include: ["src/__tests__/**/*.test.ts"] } })`. Include a top-of-file JSDoc explaining the simplification vs Phase 2. Create `packages/mcp-server/wrangler.test.jsonc` mirroring `packages/workspace-do/wrangler.test.jsonc` byte-for-byte with these specific differences: (1) name = "engram-mcp-server-test", (2) durable_objects.bindings = TWO entries — `{ "name": "MCP_OBJECT", "class_name": "EngramMcp" }` AND `{ "name": "WORKSPACE", "class_name": "WorkspaceDO" }`, (3) migrations = TWO entries — `{ "tag": "v1", "new_sqlite_classes": ["WorkspaceDO"] }` AND `{ "tag": "v2", "new_sqlite_classes": ["EngramMcp"] }`. Copy the workspace-do file's header comment block adapting it for two DO classes. NOTE: do NOT add kv_namespaces here yet — oauth.test.ts will use mocks/unsafe-bindings if it needs KV (Wave 2's oauth.test.ts implementation can extend wrangler.test.jsonc if absolutely needed; Wave 0's RED stub is structural-only). Create the four RED test files under `packages/mcp-server/src/__tests__/`. Each test file uses `it.skip(...)` (preferred — it shows up in vitest output but does not fail) or `it.todo(...)`. Each file imports from real-but-unimplemented paths (e.g., `import { RememberInputSchema } from "../schemas.js"`) — these imports SHOULD fail TS compilation when Wave 1+ runs typecheck before the source files exist, which is the RED signal we want. WORKAROUND for vitest config validation: use type-only imports (`import type { RememberInputSchema } from "../schemas.js"`) wrapped in a try/catch dynamic-import inside the test body — this lets vitest LOAD the test file (it.skip doesn't actually invoke the body), while still preserving the test-shape contract. For schemas.test.ts: 5 it.skip — one per schema — each describing "Object.keys(SchemaShape).indexOf('workspace_id') === -1 AND parse(validInput).success === true". Plus 1 it.skip for "barrel re-export shape (5 schemas + 5 z.infer types)". For tools.test.ts: 5 it.skip — one per tool name — each describing "throws McpError(MethodNotFound) with message containing 'Phase 3' AND 'Phase 4 (TOL-0N)'". Plus 1 it.skip for "registerTools registers exactly 5 tools by name (remember, recall, search, forget, ingest)". Plus 1 it.skip for "no tool callback reads args.workspace_id (defense-in-depth T-03-DD-RT)". For oauth.test.ts: 4 it.skip — "/health returns {status:'ok'}", "/ returns project info JSON", "/authorize with valid KV entry calls completeAuthorization with props.{workspace_id, user_id}", "/authorize with missing KV entry returns 403 with 'Unknown OAuth subject' in body". For index.test.ts: 3 it.skip — "default export is OAuthProvider instance", "EngramMcp.init() calls server.registerTool 5 times", "wrangler.jsonc migrations[] has v1 with WorkspaceDO and v2 with EngramMcp". Each test file gets a top-of-file JSDoc explaining the RED state and what Wave 1/2/3 plan turns it GREEN. The test files MUST NOT import any module that doesn't yet exist at file load time — wrap with `it.skip` so Vitest collects them without executing. Verify by running `npm test --workspace=@engram/mcp-server -- --run` from repo root; output should show "X skipped" lines and exit 0.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; test -f packages/mcp-server/vitest.config.ts &amp;&amp; test -f packages/mcp-server/wrangler.test.jsonc &amp;&amp; test -f packages/mcp-server/src/__tests__/schemas.test.ts &amp;&amp; test -f packages/mcp-server/src/__tests__/tools.test.ts &amp;&amp; test -f packages/mcp-server/src/__tests__/oauth.test.ts &amp;&amp; test -f packages/mcp-server/src/__tests__/index.test.ts &amp;&amp; grep -q "cloudflareTest" packages/mcp-server/vitest.config.ts &amp;&amp; grep -q "wrangler.test.jsonc" packages/mcp-server/vitest.config.ts &amp;&amp; grep -q "EngramMcp" packages/mcp-server/wrangler.test.jsonc &amp;&amp; grep -q "WorkspaceDO" packages/mcp-server/wrangler.test.jsonc &amp;&amp; grep -q '"tag": "v2"' packages/mcp-server/wrangler.test.jsonc &amp;&amp; node scripts/lint-wrangler.mjs &amp;&amp; npm test --workspace=@engram/mcp-server -- --run 2>&amp;1 | grep -qE "(skipped|todo)"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist at the listed paths
    - `packages/mcp-server/vitest.config.ts` contains string `cloudflareTest` AND `configPath: "./wrangler.test.jsonc"` (or single-quoted)
    - `packages/mcp-server/wrangler.test.jsonc` contains BOTH `"EngramMcp"` and `"WorkspaceDO"` as class_name values
    - `packages/mcp-server/wrangler.test.jsonc` migrations array contains entries with `"tag": "v1"` AND `"tag": "v2"`
    - `packages/mcp-server/wrangler.test.jsonc` is excluded from FND-08 lint glob (filename ends `.test.jsonc`, not `.jsonc`) — verified by `node scripts/lint-wrangler.mjs` exiting 0
    - Each test file (`schemas.test.ts`, `tools.test.ts`, `oauth.test.ts`, `index.test.ts`) contains at least one `it.skip(` or `it.todo(` invocation
    - Each test file contains `describe(` at least once
    - `tools.test.ts` body contains the literal substring `MethodNotFound` (case-sensitive)
    - `schemas.test.ts` body contains the literal substring `workspace_id` (used in the structural assertion description — pattern: "NO `workspace_id` field")
    - `oauth.test.ts` body contains the literal substring `ENGRAM_IDENTITIES`
    - `index.test.ts` body contains the literal substring `OAuthProvider`
    - `npm test --workspace=@engram/mcp-server -- --run` (executed from repo root) exits 0 AND output contains the word `skipped` (or `todo` — vitest 4.x emits "X tests pending" for it.todo)
  </acceptance_criteria>
  <done>
    vitest configuration is wired; the test pool can resolve wrangler.test.jsonc; all 4 RED test files exist with describe/it.skip/it.todo shape; `npm test --workspace=@engram/mcp-server` exits 0 with skipped/pending tests; `npm run lint:wrangler` exits 0 (the .test.jsonc suffix excludes it from the FND-08 glob).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create scripts/kv-bootstrap.mjs + register kv:bootstrap script in root package.json</name>
  <files>scripts/kv-bootstrap.mjs, package.json</files>
  <read_first>
    - scripts/lint-wrangler.mjs (sibling — canonical in-repo .mjs script style: shebang-less ESM imports, process.argv.slice(2), exit-code matrix 0/1/2, [tag:name] log prefix)
    - .planning/phases/03-mcp-server-scaffold/03-RESEARCH.md §Code Examples → Example 3 (lines 834-860) — body content
    - .planning/phases/03-mcp-server-scaffold/03-PATTERNS.md §9 (lines 464-493) — analog mapping
    - .planning/phases/03-mcp-server-scaffold/03-CONTEXT.md §D-04 — KV-backed identities decision
    - package.json (root) — existing scripts block to mirror (kv:bootstrap takes the lint:wrangler / lint:blockconcurrency slot pattern)
  </read_first>
  <behavior>
    - Test 1: `node scripts/kv-bootstrap.mjs --help` (or no args) prints usage to stderr and exits with code 1 (or 0 if --help is explicit, 1 if missing required args — pick one and document)
    - Test 2: `node scripts/kv-bootstrap.mjs --sub test-sub-001 --workspace-id rmoore-personal --user-id rmoore --dry-run` echoes the planned wrangler command to stdout WITHOUT executing it
    - Test 3: `npm run kv:bootstrap -- --help` (from repo root) successfully proxies to the script
    - Test 4: Script source must NOT print the parsed identity value to stdout in non-dry-run mode (T-03-KV-LEAK mitigation); only the sub key + a success/failure indication
  </behavior>
  <action>
    Create `scripts/kv-bootstrap.mjs` mirroring scripts/lint-wrangler.mjs style: shebang-less, ESM `import` statements, `process.argv.slice(2)` argument parsing, exit-code matrix documented at the top, `[kv:bootstrap]` log prefix on all stderr writes. The script: (1) Parses CLI args `--sub <oauth-subject>` (REQUIRED), `--workspace-id <id>` (optional, default "rmoore-personal"), `--user-id <id>` (optional, default "rmoore"), `--dry-run` flag (optional). (2) Validates `--sub` is present; if missing, prints usage block to stderr and `process.exit(1)`. (3) Constructs the wrangler invocation: `npx wrangler kv key put --binding ENGRAM_IDENTITIES --remote "$SUB" "$(JSON.stringify({workspace_id, user_id}))"`. (4) In dry-run mode: prints the planned command to stdout (DO NOT print the value content — only the command shape with placeholders, per T-03-KV-LEAK threat: `[kv:bootstrap] DRY RUN: would call: npx wrangler kv key put --binding ENGRAM_IDENTITIES --remote <sub> <identity-json-redacted>`). (5) Non-dry-run mode: uses `node:child_process.spawnSync` to invoke wrangler; reports the wrangler exit code; the script exits with 0 on success, 2 on wrangler failure. (6) Exit code 1 for missing required args; exit code 2 for wrangler subprocess failure; exit code 0 on success or successful dry-run. Top-of-file JSDoc mirrors lint-wrangler.mjs lines 1-13: cite source decision (D-04), explain purpose, list exit codes, list flags. Mandatory header comment includes a one-line note that this script is read-only by design and never reads KV values back (T-03-KV-LEAK mitigation). After creating the script, modify root `package.json` to add `"kv:bootstrap": "node scripts/kv-bootstrap.mjs"` to the scripts block (mirror the existing `lint:wrangler` and `lint:blockconcurrency` entries). Verify by running `npm run kv:bootstrap` (no args) — script should exit non-zero with usage printed; then `npm run kv:bootstrap -- --sub test-001 --dry-run` should exit 0 with the planned command echoed.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; test -f scripts/kv-bootstrap.mjs &amp;&amp; grep -q "ENGRAM_IDENTITIES" scripts/kv-bootstrap.mjs &amp;&amp; grep -q "kv:bootstrap" package.json &amp;&amp; (npm run kv:bootstrap -- --sub test-001 --dry-run 2>&amp;1 | grep -q "DRY RUN") &amp;&amp; (npm run kv:bootstrap 2>&amp;1; test $? -ne 0) &amp;&amp; (! grep -E "console\.log\\(.*identity" scripts/kv-bootstrap.mjs)</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/kv-bootstrap.mjs` exists
    - File contains literal string `ENGRAM_IDENTITIES`
    - File contains literal string `--sub` (CLI flag handling)
    - File contains literal string `[kv:bootstrap]` (log tag — mirror lint-wrangler.mjs `[lint:wrangler]`)
    - File contains literal string `--dry-run`
    - Root `package.json` scripts block contains `"kv:bootstrap"` key
    - `npm run kv:bootstrap` (no args, from repo root) exits with non-zero code (missing --sub)
    - `npm run kv:bootstrap -- --sub test-001 --dry-run` exits with code 0 AND stdout contains `DRY RUN`
    - `grep -E "console\.log\(.*identity" scripts/kv-bootstrap.mjs` returns no matches (T-03-KV-LEAK structural check — never log the parsed identity object in production paths)
  </acceptance_criteria>
  <done>
    `scripts/kv-bootstrap.mjs` exists with correct CLI surface; `npm run kv:bootstrap` is discoverable from repo root; dry-run mode echoes the planned wrangler command without leaking identity values; missing-args mode exits non-zero with usage; non-dry-run mode is wired to spawn `wrangler kv key put` (Russell will exercise it once at deploy time per Plan 06's README).
  </done>
</task>

</tasks>

<verification>
After all 4 tasks land:

1. **All RED test files discoverable but pending:**
   ```bash
   cd /Users/rmoore/Workspaces/engram
   npm test --workspace=@engram/mcp-server -- --run
   ```
   Must exit 0, output must reference `skipped` or `pending` test count.

2. **Wrangler lint still passes (.test.jsonc excluded):**
   ```bash
   npm run lint:wrangler
   ```
   Exit 0.

3. **Typecheck still passes for mcp-server:**
   ```bash
   npx tsc -p packages/mcp-server/tsconfig.json --noEmit
   ```
   Exit 0.

4. **KV bootstrap script behaves:**
   ```bash
   npm run kv:bootstrap                                    # exits 1
   npm run kv:bootstrap -- --sub abc --dry-run             # exits 0
   ```

5. **Defense-in-depth scaffolding is in place:**
   - `schemas.test.ts` references the contract phrase "workspace_id" (the test that will fire on regression)
   - `tools.test.ts` references both `MethodNotFound` and "this.props.workspace_id" (or the negative — "no args.workspace_id")
</verification>

<success_criteria>
- vitest can discover and skip-pend 4 test files in `packages/mcp-server/src/__tests__/`
- `npm test --workspace=@engram/mcp-server` exits 0 with skipped/pending output
- `@cloudflare/workers-oauth-provider@0.7.0` and `zod@^4` resolvable via `node_modules/`
- `EngramProps` interface exported from `packages/mcp-server/src/index.ts` (no default-export swap yet)
- `scripts/kv-bootstrap.mjs` exists with --sub / --dry-run / --workspace-id / --user-id args; `npm run kv:bootstrap` is registered at root
- `npm run lint:wrangler` continues to exit 0 (the new wrangler.test.jsonc is intentionally excluded by glob)
</success_criteria>

<output>
Create `.planning/phases/03-mcp-server-scaffold/03-01-SUMMARY.md` when done. Required sections per templates/summary.md: Outcome, Files Changed, Verification, Cross-Phase Contracts (note EngramProps published for Wave 1+), Deviations (if any), Next Plan (02 — schemas + error-mapping).
</output>
