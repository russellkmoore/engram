---
phase: "05-integration-kitchen-sink"
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - packages/mcp-server/src/__tests__/envelope.test.ts
  - packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts
autonomous: true
requirements:
  - INT-02
  - INT-03

must_haves:
  truths:
    - "envelope.test.ts passes unchanged PLUS one new non-breaking context.conflicts discipline assertion"
    - "cross-workspace-pentest.test.ts has 3 new Prong-A cases for v0.2 paths (expanded-query fan-out, reranker, synthesis)"
    - "Each Prong-A case includes a positive-control assertion (workspace_A returns ≥ 1 memory) to prevent vacuous isolation proof"
    - "Each Prong-A case has a corresponding it.skip Prong-C stub for nightly real-creds run"
    - "D-09 is respected: Prong B forge-arg is NOT duplicated per new path"
    - "D-11 is satisfied: expanded-query Prong A asserts that a workspace_B recall returns zero workspace_A memories (all fan-out variants query workspace_B namespace only)"
  artifacts:
    - path: "packages/mcp-server/src/__tests__/envelope.test.ts"
      provides: "backward-compat proof — existing tests pass + new context.conflicts discipline assertion"
    - path: "packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts"
      provides: "3 new Prong-A cases + 3 it.skip Prong-C stubs for v0.2 isolation proof"
  key_links:
    - from: "cross-workspace-pentest.test.ts"
      to: "packages/mcp-server/src/tools.ts recall() expanded-query path"
      via: "captureCallback('recall', 'workspace_B') with top1 < 0.65 score override"
      pattern: "captureCallback.*recall.*workspace_B"
    - from: "cross-workspace-pentest.test.ts"
      to: "packages/mcp-server/src/tools.ts recall() synthesis path"
      via: "captureCallback('recall', 'workspace_B') with verbosity='synthesis'"
      pattern: "verbosity.*synthesis"
---

<objective>
Two in-place test file extensions that close INT-02 and INT-03 (mcp-server paths):

1. `envelope.test.ts` extension (INT-02): Add one non-breaking `context.conflicts` discipline assertion.
   The existing file already passes against the v0.2 envelope shape — INT-02 is primarily satisfied by
   running the file and seeing GREEN. The one genuinely-missing assertion is the CON-05 D-08 field-omit
   discipline at the builder level.

2. `cross-workspace-pentest.test.ts` extension (INT-03 mcp-server): Add 3 Prong-A cases covering the
   v0.2 code paths (expanded-query fan-out per D-11, reranker path, synthesis path) plus 3 corresponding
   `it.skip` Prong-C stubs. Per D-09, the generic Prong B forge-arg backstop is NOT duplicated.

Purpose: INT-02 proves the v0.1 envelope contract is backward-compatible with v0.2's new optional fields.
INT-03 mcp-server proves workspace isolation holds across the entire expanded attack surface — every new
v0.2 code path that touches Vectorize or synthesizes content must be covered by a forged-workspace test.

Output: Two modified test files, both passing in the mcp-server workerd pool.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-integration-kitchen-sink/05-CONTEXT.md
@.planning/phases/05-integration-kitchen-sink/05-RESEARCH.md
@.planning/phases/05-integration-kitchen-sink/05-PATTERNS.md
@.planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md

<interfaces>
<!-- Key patterns extracted from RESEARCH.md + PATTERNS.md for both test files. -->

From packages/mcp-server/src/__tests__/envelope.test.ts (from RESEARCH.md §INT-02):
- Existing snapshot: META_GAPS byte-determinism ONLY — covers byte-frozen strings like "queryExpansionUnavailable", "recallChunksOmittedSynthesis", etc. Snapshot MUST NOT change.
- Existing assertion at line 79: expect(envelope.context.conflicts).toEqual([]) — builder always returns empty array
- Existing describe block lines 229–264 ("D-01 verbosity shapes"): already covers result.synthesis population for verbosity="synthesis"
- INT-02 verdict from RESEARCH.md §INT-02: "The existing D-01 describe block already covers synthesis population. The one genuinely missing assertion is context.conflicts field-omit discipline."
- New assertion to add (PATTERNS.md §envelope.test.ts):
  it("buildRecallResponse omits context.conflicts when no conflicts provided (CON-05 D-08 field-omit discipline)", () => {
    const envelope = buildRecallResponse({ memories: [], verbosity: "chunks" });
    expect(envelope.context.conflicts).toEqual([]);
    // field-OMIT behavior at tools.ts handler level is already covered by recall-conflicts.test.ts Test 3
  })

From packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts (from RESEARCH.md §INT-03 + PATTERNS.md):
- captureCallback(toolName, workspace_id, user_id?) at lines 125–161
- parseEnvelope(result) at lines 164–167
- Existing Prong A structure at lines 183–207: seed workspace_A → call tool from workspace_B → assert memories=[] → positive-control assert workspace_A returns ≥ 1 memory
- it.skip Prong C pattern at lines 266–294: `it.skip("AI-02 Prong C ...", async () => { /* SKIPPED: requires real Cloudflare Vectorize */ }, 15_000)`
- penTestVectorizeStore: Map<string, Set<string>> — existing stateful mock (same namespace tracking pattern)
- Per D-09: DO NOT add new Prong B cases (generic assertOwnsWorkspace forge-arg already covers the backstop)
- Per D-11: expanded-query Prong A must assert all fan-out variants resolve workspace_B namespace only (no workspace_A bleed)
- RERANKER_ENABLED = false: reranker Prong A proves isolation in the disabled-reranker context — filteredMatches is workspace-scoped, so reranker contexts contain zero workspace_A content
- SYN-07 guard: synthesis Prong A — workspace_B has 0 ranked memories → synthesis is null (guard fires because ranked.length < 2); assert result.synthesis is null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend envelope.test.ts with context.conflicts discipline assertion (INT-02)</name>
  <files>packages/mcp-server/src/__tests__/envelope.test.ts</files>

  <read_first>
    - `packages/mcp-server/src/__tests__/envelope.test.ts` — full file (existing describe blocks, snapshot usage, existing context.conflicts assertion at line 79, D-01 verbosity describe block at lines 229–264)
    - `packages/mcp-server/src/__tests__/__snapshots__/envelope.test.ts.snap` — existing snapshot (META_GAPS only; must not change)
    - `.planning/phases/05-integration-kitchen-sink/05-RESEARCH.md` §INT-02 — INT-02 implementation verdict and the exact new assertion to add
  </read_first>

  <behavior>
    Test: "buildRecallResponse omits context.conflicts when no conflicts provided (CON-05 D-08 field-omit discipline)"
    - Input: buildRecallResponse({ memories: [], verbosity: "chunks" }) — no conflicts parameter
    - Assert: envelope.context.conflicts equals [] (builder returns empty array when no conflicts, per CON-05 backward-compat)
    - This test documents the builder-level contract (empty = []); the field-omit at tools.ts handler level is already covered by recall-conflicts.test.ts Test 3
    - The snapshot must NOT change
  </behavior>

  <action>
[route] 05-03-T1 envelope.test.ts addition (~5 LOC, single assertion) | claude | N/N/Y | too small for cf-code-assist (<15 LOC rule), context-prep overhead exceeds savings

Add one `it()` block inside the existing "envelope builders (TOL-06...)" or nearest describe block that already contains the `context.conflicts` line 79 assertion.

The new test mirrors the PATTERNS.md §envelope.test.ts excerpt. Do NOT modify any existing tests or the snapshot. The snapshot file is off-limits.

After the addition, run the envelope test suite to confirm GREEN: `npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=envelope`

Append a routing row to `.planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md` for this task.
  </action>

  <verify>
    <automated>npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=envelope 2>&1 | tail -5</automated>
  </verify>

  <done>
    - `npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=envelope` exits 0
    - New `it()` block present with "CON-05 D-08" in description
    - Snapshot file UNCHANGED (no new snapshot assertions added)
    - 05-CF-CODE-ASSIST-USAGE.md routing row appended
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend cross-workspace-pentest.test.ts with 3 Prong-A + 3 Prong-C cases (INT-03)</name>
  <files>packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts</files>

  <read_first>
    - `packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts` — FULL file (understand: existing Prong A at lines 183–207 to mirror, existing it.skip Prong C at lines 266–294 to mirror, the penTestVectorizeStore mock, the captureCallback and parseEnvelope helpers)
    - `.planning/phases/05-integration-kitchen-sink/05-RESEARCH.md` §INT-03 — exact call sites in tools.ts for the 3 new paths (expanded-query lines 795–809, reranker lines 890–909, synthesis lines 1006–1031); positive-control pattern; D-09 do NOT duplicate Prong B
    - `.planning/phases/05-integration-kitchen-sink/05-PATTERNS.md` §cross-workspace-pentest.test.ts — the 3 Prong-A case code excerpts with exact test descriptions
    - `packages/mcp-server/src/tools.ts` (lines 780–815) — expanded-query fan-out: workspace_id is always props.workspace_id, never an arg; confirms D-11 isolation proof
  </read_first>

  <behavior>
    3 new it() blocks (Prong A) + 3 new it.skip() blocks (Prong C):

    Prong A Case 1: "TOL-07 Prong A v0.2-EXP: expanded-query fan-out variants query only the forged workspace namespace (D-11)"
    - Seed workspace_A_exp via remember
    - Override Vectorize mock top1 score to < 0.65 to trigger adaptive fan-out (fan-out variants will query workspace_B_exp namespace)
    - Call recall from workspace_B_exp (forged workspace)
    - Assert memories = [] (fan-out queries workspace_B_exp namespace, finds nothing — workspace_A_exp data inaccessible)
    - Positive control: call recall from workspace_A_exp; assert memories.length ≥ 1 (seeding worked, not vacuous)

    Prong A Case 2: "TOL-07 Prong A v0.2-RNK: reranker contexts contain only workspace-scoped blocks (RERANKER_ENABLED=false → raw cosine path)"
    - Seed workspace_A_rnk via remember
    - Call recall from workspace_B_rnk (RERANKER_ENABLED=false; filteredMatches already workspace-scoped by Vectorize)
    - Assert memories = [] (no workspace_A_rnk data reaches workspace_B_rnk caller, even through the reranker code path)
    - Positive control: workspace_A_rnk recall returns ≥ 1 memory

    Prong A Case 3: "TOL-07 Prong A v0.2-SYN: synthesis called from workspace_B returns null synthesis (SYN-07 fires — no workspace_A data)"
    - Seed workspace_A_syn via remember
    - Call recall from workspace_B_syn with verbosity: "synthesis"
    - Assert result.synthesis is null (SYN-07 guard: ranked.length < 2 for workspace_B_syn → synthesis skipped)
    - Assert memories = [] (no workspace_A_syn bleed)

    3 Prong C it.skip stubs:
    - "AI-02 Prong C v0.2-EXP: expanded-query variants upserted under workspace_A namespace NOT returned by fan-out query in workspace_B"
    - "AI-02 Prong C v0.2-RNK: reranker contexts for workspace_B call contain zero blocks from workspace_A namespace"
    - "AI-02 Prong C v0.2-SYN: synthesis for workspace_B call is null — workspace_A vectors not in workspace_B namespace"
    Each with: /* SKIPPED: requires real Cloudflare Vectorize binding (remote: true). Run nightly with CLOUDFLARE_ACCOUNT_ID set + wrangler login. */ and 15_000ms timeout

    Do NOT add any Prong B cases (D-09: generic assertOwnsWorkspace forge-arg covers the backstop).
  </behavior>

  <action>
[route] 05-03-T2 cross-workspace-pentest.test.ts additions (3 Prong-A + 3 Prong-C blocks, ~80 LOC, well-defined pattern from PATTERNS.md) | scaffoldTests | N/Y/Y | single-file additions with stable Prong-A template from existing file

Add the 6 new test cases inside the existing `describe("TOL-07 / AI-02: ...")` block in `cross-workspace-pentest.test.ts`. Insert after the existing Prong-A tests and before the existing Prong-C skip. The new Prong-C `it.skip` stubs go alongside the existing Prong-C stubs.

Key behavioral constraints:
- Each Prong-A case must include a positive-control assertion (workspace_A recalls ≥ 1 memory after workspace_B recall returns 0) per PATTERNS.md §positive-control pattern and RESEARCH.md Pitfall 5.
- For Case 1 (EXP fan-out D-11): the Vectorize mock must return top1 score < 0.65 to trigger adaptive gate. Override in the test setup by temporarily setting the mock's first-returned score to 0.60.
- For Case 3 (SYN): assert result.synthesis is null — SYN-07 fires because workspace_B has no ranked memories.
- The penTestVectorizeStore (already in the file) correctly tracks namespace = workspace_id; the new cases leverage this without modification.
- Do NOT call runInDurableObject or duplicate assertOwnsWorkspace logic (D-09).

After extending the file, run the full pentest suite: `npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=cross-workspace-pentest`

Append a routing row to `.planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md` for this task.
  </action>

  <verify>
    <automated>npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=cross-workspace-pentest 2>&1 | tail -10</automated>
  </verify>

  <done>
    - `npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=cross-workspace-pentest` exits 0
    - 3 new Prong-A it() blocks present with "v0.2-EXP", "v0.2-RNK", "v0.2-SYN" in descriptions
    - 3 new it.skip Prong-C stubs present
    - No Prong B cases added (D-09 respected)
    - Each Prong-A case has a positive-control assertion
    - 05-CF-CODE-ASSIST-USAGE.md routing row appended
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forged workspace_id → tools.ts recall() | The pentest's threat model: an attacker supplies a crafted workspace_id to access another workspace's data |
| props.workspace_id → Vectorize namespace | The isolation invariant: Vectorize namespace = workspace_id from JWT-validated props, never a request arg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-SC | Tampering | npm installs | accept | No new packages; test files only |
| T-05-03-01 | Information Disclosure | Cross-workspace data via expanded-query fan-out | mitigate | D-11: Prong-A Case 1 asserts all fan-out variants query workspace_B namespace (namespace = props.workspace_id, never fan-out arg); zero workspace_A data in workspace_B response |
| T-05-03-02 | Information Disclosure | Cross-workspace data via synthesis path | mitigate | Prong-A Case 3 asserts synthesis is null when workspace_B has no ranked memories (SYN-07 guard + Vectorize namespace isolation prevents workspace_A data from entering synthesis input) |
| T-05-03-03 | Information Disclosure | Cross-workspace data via reranker path | mitigate | Prong-A Case 2: RERANKER_ENABLED=false; filteredMatches is workspace-scoped by Vectorize before reranker path; reranker contexts contain zero workspace_A content when querying workspace_B |
| T-05-03-04 | Tampering | Vacuous Prong-A isolation (no seeding) | mitigate | Positive-control pattern: after workspace_B returns 0 memories, assert workspace_A returns ≥ 1 memory (proves seeding worked — test not vacuously true) |
</threat_model>

<verification>
After plan completes:

```bash
# Envelope test GREEN (backward-compat proof)
npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=envelope

# Pentest suite GREEN (3 new Prong-A cases pass)
npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=cross-workspace-pentest

# Verify Prong-A cases present
grep -c "Prong A v0.2" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts

# Verify Prong-C skips present
grep -c "Prong C v0.2" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts

# Verify no Prong B duplication (D-09 — count should be 1, the original)
grep -c "Prong B" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts

# Verify positive-control pattern
grep -c "workspace_A.*recalls\|positive-control\|toBeGreaterThanOrEqual(1)" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts
```
</verification>

<success_criteria>
- `npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=envelope` exits 0
- `npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=cross-workspace-pentest` exits 0
- 3 new Prong-A it() blocks in cross-workspace-pentest.test.ts (v0.2-EXP, v0.2-RNK, v0.2-SYN)
- 3 new it.skip Prong-C stubs in cross-workspace-pentest.test.ts
- D-09 respected: no new Prong-B cases added
- D-11 satisfied: EXP fan-out test asserts workspace_B isolation (memories = [])
- envelope.test.ts snapshot unchanged
- CF-CODE-ASSIST-USAGE.md routing rows appended for both tasks
</success_criteria>

<output>
Create `.planning/phases/05-integration-kitchen-sink/05-03-SUMMARY.md` when done.
</output>
