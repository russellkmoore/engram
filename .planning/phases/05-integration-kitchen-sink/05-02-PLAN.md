---
phase: "05-integration-kitchen-sink"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts
autonomous: true
requirements:
  - INT-01
  - INT-04

must_haves:
  truths:
    - "v02-kitchen-sink.test.ts exists and passes in the mcp-server workerd pool"
    - "Post-trim envelope for a 10-conflict + 50-entity + verbosity=synthesis fixture is ≤ 7,500 cl100k tokens AND synthesis string survives trim AND at least one high-severity conflict survives trim"
    - "Pre-trim token count is > 7,500 (adversarial-proof: trimToBudget actually trims)"
    - "All 6 matrix rows are covered by this test file (kitchen-sink as the umbrella: rows 1-6 each have a describe block exercising the specific composition pair)"
    - "RNK×CON: conflicts survive hybridRank when conflict-linked memories have lower scores"
    - "RNK×EXP: unmocked hybridRank receives a real RRF-merged candidate set (top1 < 0.65 triggers fan-out)"
    - "EXP×SYN: fan-out fires (top1 < 0.65) AND synthesis is non-null in the envelope"
    - "CON×SYN: verbosity=synthesis + conflict rows present → both context.conflicts[] and result.synthesis populated"
    - "adaptive-routing×cosine-edge: top1 near 0.64 + RateLimitError from expandQuery → single-query fallback returns well-ranked results"
  artifacts:
    - path: "packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts"
      provides: "INT-01 worst-case envelope test + all 6 matrix row describe blocks"
      min_lines: 200
  key_links:
    - from: "v02-kitchen-sink.test.ts"
      to: "packages/mcp-server/src/tools.ts recall()"
      via: "captureCallback('recall', workspace_id) + real hybridRank (not mocked)"
      pattern: "captureCallback.*recall"
    - from: "v02-kitchen-sink.test.ts"
      to: "packages/mcp-server/src/envelope.ts trimToBudget"
      via: "trimToBudget(buildRecallResponse({...}))"
      pattern: "trimToBudget"
    - from: "v02-kitchen-sink.test.ts"
      to: "gpt-tokenizer/encoding/cl100k_base encode()"
      via: "encode(JSON.stringify(trimmed)).length"
      pattern: "cl100k_base"
---

<objective>
Author `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts` — the INT-01 worst-case
envelope test and the umbrella for all 6 v0.2-INTEGRATION-MATRIX rows. This is the highest-value deliverable
of Phase 5: it proves the composed `recall()` pipeline (expand → RRF → reranker path → hybridRank →
conflict-surfacing → synthesis → trimToBudget) honors the v0.1 EngramResponse<T> contract under worst-case
adversarial load.

Purpose: Catches the cross-feature regressions that isolated unit tests by definition cannot catch. Every
matrix row has a dedicated `describe` block exercising the specific composition pair with an adversarial
fixture designed to trip the real failure mode. The full kitchen-sink test (INT-01) additionally proves
content preservation under trim: synthesis + high-severity conflicts survive the budget cut, not just raw
chunk volume.

Output: `v02-kitchen-sink.test.ts` (new file) passing in the mcp-server workerd pool.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-integration-kitchen-sink/05-CONTEXT.md
@.planning/phases/05-integration-kitchen-sink/05-RESEARCH.md
@.planning/phases/05-integration-kitchen-sink/05-PATTERNS.md
@.planning/phases/05-integration-kitchen-sink/05-VALIDATION.md
@.planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from PATTERNS.md and RESEARCH.md source audit. -->

From packages/mcp-server/src/__tests__/integration/recall-conflicts.test.ts (primary analog):
- Stateful Vectorize mock: `const store = new Map<string, Set<string>>()` — namespace tracks which block IDs are upserted per workspace
- captureCallback(toolName, workspace_id, user_id?): captures the registered MCP tool callback for direct invocation
- parseEnvelope(result): parses the MCP CallToolResult text as JSON → Record<string, unknown>
- stub.insertBlock({...}) — seeds block into DO SQLite via RPC
- stub.insertConflictAsInbox({...}) — seeds conflict row into DO inbox table via RPC
- patchEnvBindings() in beforeAll — sets env.AI + env.VECTORIZE to mocks; afterEach clears + restores

From packages/mcp-server/src/__tests__/token-budget.test.ts (INT-01 harness analog):
- `import { encode } from "gpt-tokenizer/encoding/cl100k_base"` — EXACT import path (Pitfall 4: not the barrel)
- `import { buildRecallResponse, trimToBudget } from "../envelope.js"`
- buildWorstCaseMemories() returns Array of LexicalSearchHit shape (25 entries, 4KB content + 1KB summary each)
- Pattern: encode(JSON.stringify(envelope)).length for token counting
- Adversarial-proof pattern: expect(beforeTokens).toBeGreaterThan(7_500) BEFORE trim assertion

From packages/mcp-server/src/tools.ts (integration surface — key constants):
- ADAPTIVE_TOP1_THRESHOLD = 0.65 (line 781) — top1 < 0.65 triggers fan-out
- MIN_COSINE_THRESHOLD = 0.6 (from @engram/ai-config) — below this is filtered
- RERANKER_ENABLED = false (line 888 comment, live ablation 2026-06-08) — reranker disabled; raw cosine fills slot
- recall() composition: embed → vectorizeQuery → EXP-03 adaptive gate → RRF → filteredMatches → hybridRank → listInboxConflictsForMemoryIds → generateSynthesis → buildRecallResponse → trimToBudget

AI mock dispatch (from tools-integration.test.ts line 102):
- Embed calls return: { data: [MOCK_VECTOR], shape: [1, EMBEDDING_DIMS] }
- Synthesis calls return: { response: "Mock synthesis." }
- Expansion calls return: { response: JSON.stringify(["variant1", "variant2"]) } (zod-gated expandQuery)
- Single mock handles all by checking which key is present in the expected output

Key fixture constraints (from RESEARCH.md pitfall guards):
- Pitfall 2 (SYN-07 guard): seed ≥ 2 blocks so ranked.length ≥ 2, synthesis not skipped
- Pitfall 3 (fan-out not firing): set first Vectorize score to 0.60 (< ADAPTIVE_TOP1_THRESHOLD) when testing EXP paths
- Pitfall 6 (severity): use created_at: Date.now() for high-severity conflicts (diffDays=0 → "high")
- Pitfall 1 (trivial fixture): assert beforeTokens > 7_500 to prove trimToBudget actually trims
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create v02-kitchen-sink.test.ts with all 6 matrix row describe blocks + INT-01 worst-case test</name>
  <files>packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts</files>

  <read_first>
    - `packages/mcp-server/src/__tests__/integration/recall-conflicts.test.ts` — primary analog: full file structure (stateful Vectorize mock, patchEnvBindings, captureCallback, parseEnvelope, seedTwoMemoriesWithTimestamps, insertConflictRow helpers, afterEach cleanup)
    - `packages/mcp-server/src/__tests__/token-budget.test.ts` — secondary analog: cl100k_base import, buildWorstCaseMemories shape (LexicalSearchHit), adversarial-proof pattern (beforeTokens > 7_500), trimToBudget assertion
    - `packages/mcp-server/src/__tests__/tools-integration.test.ts` (lines 95–120) — AI mock dispatch pattern (embed vs synthesis vs expansion response shapes distinguished by key)
    - `packages/mcp-server/src/__tests__/recall.test.ts` (lines 1–100) — fan-out test structure: how to override Vectorize mock score to 0.40 to trigger adaptive gate
    - `packages/mcp-server/src/tools.ts` (lines 780–800, 1040–1055) — recall() adaptive gate + buildRecallResponse call site confirming `conflicts` parameter
    - `.planning/phases/05-integration-kitchen-sink/05-PATTERNS.md` — all code excerpt patterns (stateful mock, captureCallback, parseEnvelope, buildKitchenSinkMemories, D-10 assertion shape, positive-control pattern)
  </read_first>

  <behavior>
    File: packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts

    Describe block structure (6 blocks + 1 INT-01 block):

    1. "RNK × CON: conflicts survive hybridRank when conflict-linked memories rank lower"
       - Seed 3 blocks in workspace_A: blockA (low score 0.62), blockB (high score 0.85), blockC (medium score 0.75)
       - Insert a conflict row linking blockA and blockB via stub.insertConflictAsInbox
       - Call recall({ query: "...", verbosity: "chunks" }) with a Vectorize mock that returns scores in order [0.62, 0.85, 0.75]
       - Assert: envelope.context.conflicts is an array with length ≥ 1 (conflict not sorted away despite blockA having lowest score)
       - Assert: envelope.result.memories is non-empty (real hybridRank ran — not mocked)

    2. "RNK × EXP: real hybridRank receives RRF-merged candidate set when fan-out fires"
       - Set Vectorize mock top1 score to 0.60 (< ADAPTIVE_TOP1_THRESHOLD=0.65) to trigger adaptive fan-out
       - Mock expandQuery to return ["original", "variant1", "variant2"] — mock AI returns expansion JSON
       - Seed ≥ 2 blocks in workspace
       - Assert: AI.run was called multiple times (embed + expansion calls)
       - Assert: envelope.result.memories is non-empty (real hybridRank ran on merged candidates — NOT mocked)
       - Assert: no TypeErrors (the RRF-merged LexicalSearchHit list is valid hybridRank input)

    3. "EXP × SYN: synthesis is non-null when fan-out fires and ≥ 2 memories are returned"
       - Set Vectorize top1 score to 0.60 (triggers fan-out)
       - Seed ≥ 2 blocks (satisfies SYN-07 guard: ranked.length ≥ 2)
       - Call recall({ query: "...", verbosity: "synthesis" })
       - Mock AI.run: embed calls → { data: [...], shape: [...] }; synthesis call → { response: "Mock synthesis." }
       - Assert: result.synthesis is not null ("Mock synthesis." present)
       - Assert: envelope.result.memories.length ≥ 2 (fan-out expanded the candidate set)

    4. "CON × SYN: both context.conflicts[] and result.synthesis populated simultaneously"
       - Seed ≥ 2 blocks (created_at: Date.now() for high-severity per Pitfall 6)
       - Insert conflict row linking two of the seeded blocks
       - Call recall({ query: "...", verbosity: "synthesis" })
       - Assert: result.synthesis is not null (synthesis ran)
       - Assert: context.conflicts is an array with length ≥ 1 (conflicts present alongside synthesis)
       - Assert: the conflict in context.conflicts has severity "high" (same-day created_at)

    5. "adaptive-routing × cosine-edge: RateLimitError from expandQuery → single-query fallback with well-ranked results"
       - Set Vectorize top1 score to 0.64 (just below ADAPTIVE_TOP1_THRESHOLD=0.65 — triggers fan-out attempt)
       - Mock expandQuery to throw RateLimitError (or mock AI.run for expansion call to throw RateLimitError)
       - Seed ≥ 1 block
       - Call recall({ query: "...", verbosity: "chunks" })
       - Assert: result.memories is non-empty (single-query fallback returned results)
       - Assert: meta.gaps contains "queryExpansionUnavailable" string (EXP-10 meta.gaps note)
       - Assert: no unhandled exception (graceful fallback)

    6. "INT-01 kitchen-sink: worst-case envelope ≤ 7,500 tokens post-trim AND synthesis + high-severity conflicts survive trim"
       - Build worst-case fixture: 25 memories (4KB content, 1KB summary each) via buildKitchenSinkMemories(25)
       - Build 10 conflict objects with severity: "high" (use created_at: Date.now() pattern)
       - Build 50 entity-like strings (pad properties JSON with entity arrays)
       - Call buildRecallResponse({ memories: kitchenSinkMemories, verbosity: "synthesis", synthesis: "Synthesis text.", conflicts: conflictArray })
       - Step 1 (adversarial-proof): const beforeTokens = encode(JSON.stringify(envelope)).length; expect(beforeTokens).toBeGreaterThan(7_500)
       - Step 2: const trimmed = trimToBudget(envelope)
       - Step 3 (budget): const afterTokens = encode(JSON.stringify(trimmed)).length; expect(afterTokens).toBeLessThanOrEqual(7_500)
       - Step 4 (D-05 content-preservation — the "teeth"):
         expect(trimmed.result.synthesis).not.toBeNull()
         const survivingConflicts = trimmed.context.conflicts ?? []
         const highSeverity = survivingConflicts.filter(c => c.severity === "high")
         expect(highSeverity.length).toBeGreaterThan(0)

    Infrastructure:
    - Imports: vitest (describe/it/expect/vi/beforeAll/afterEach), cloudflare:workers (env), McpServer, registerTools, EMBEDDING_DIMS, WorkspaceDO type, InboxConflictRow type, InboxConflictProperties type, encode from "gpt-tokenizer/encoding/cl100k_base", buildRecallResponse + trimToBudget from "../envelope.js"
    - kitchenSinkVectorizeStore: Map<string, Set<string>>
    - MOCK_VECTOR: Array(EMBEDDING_DIMS).fill(0.1)
    - patchEnvBindings(opts?: { top1Score?: number }) in beforeAll — patches env.AI and env.VECTORIZE per recall-conflicts.test.ts pattern; AI.run returns embed shape by default, synthesis shape when called with synthesis model, expansion shape when called with expansion model
    - afterEach: clear store + clear mock call counts + vi.restoreAllMocks()
    - captureCallback and parseEnvelope helpers: copy from recall-conflicts.test.ts (localized as kitchenSinkCaptureCallback / kitchenSinkParseEnvelope per PATTERNS.md §Shared Patterns)
    - buildKitchenSinkMemories(count): returns Array<LexicalSearchHit> with 4KB content + 1KB summary (extend buildWorstCaseMemories shape from token-budget.test.ts)
    - Do NOT mock hybridRank in tests 1–5 (RESEARCH anti-pattern: mocking hybridRank defeats the matrix's purpose)
    - Do NOT mock generateSynthesis in tests 3–4 (let the real path run with mocked AI.run)
    - Do NOT mock expandQuery; instead mock env.AI.run to return expansion-shaped response for expansion model calls
  </behavior>

  <action>
[route] 05-02-T1 v02-kitchen-sink.test.ts (~200+ LOC, known assertion spec from PATTERNS.md) | scaffoldTests | N/Y/Y | single-file mechanical scaffold; all patterns from recall-conflicts.test.ts analog; stable assertion spec in PATTERNS.md

Create `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts` following the behavior spec above.

File location: `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts` (alongside the existing `recall-conflicts.test.ts`).

Critical constraints (per RESEARCH.md anti-patterns and pitfalls):
- Use `import { encode } from "gpt-tokenizer/encoding/cl100k_base"` — NOT the barrel (Pitfall 4).
- Assert pre-trim tokens > 7,500 BEFORE the post-trim ≤ 7,500 assertion (Pitfall 1: adversarial-proof).
- Use created_at: Date.now() for high-severity conflict fixtures (Pitfall 6: diffDays=0 → severity "high").
- Do NOT mock hybridRank anywhere in this file (RESEARCH anti-pattern §Architecture Patterns).
- Seed ≥ 2 blocks before synthesis recall to satisfy SYN-07 guard (Pitfall 2).
- Set Vectorize top1 score to 0.60 for describe blocks 2, 3 to trigger EXP-03 adaptive gate (Pitfall 3).
- The stateful Vectorize mock must track namespace = workspace_id (per recall-conflicts.test.ts lines 46–105).

INT-01 fixture detail: use buildKitchenSinkMemories(25) (25 memories × ~5KB each → well over 7,500 tokens pre-trim). Build 10 conflict objects as InboxConflictRow shape with severity: "high". The 50-entity requirement from INT-01 spec is satisfied by including rich `properties` JSON on each memory (entity arrays in properties field). The `buildRecallResponse` call receives the conflicts array directly (per tools.ts line 1046 confirmation from RESEARCH.md §INT-01).

After writing the file, append a routing row to `.planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md` documenting the actual route taken for this task (update the estimate row seeded in Plan 05-01, or append a new row if different from the estimate).
  </action>

  <verify>
    <automated>npm test --workspace=packages/mcp-server -- --project=workerd --reporter=verbose 2>&1 | grep -E "v02-kitchen-sink|PASS|FAIL" | head -20</automated>
  </verify>

  <done>
    - `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts` exists with ≥ 6 describe blocks
    - `npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=v02-kitchen-sink` exits 0 (all tests pass)
    - Test file contains `expect(beforeTokens).toBeGreaterThan(7_500)` (adversarial-proof assertion present)
    - Test file contains `expect(trimmed.result.synthesis).not.toBeNull()` (D-05 content-preservation present)
    - Test file imports from `gpt-tokenizer/encoding/cl100k_base` (not the barrel)
    - 05-CF-CODE-ASSIST-USAGE.md routing row updated/appended for this task
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → recall() → WorkspaceDO | Test-seeded workspace IDs are workspace-scoped by construction; the kitchen-sink tests operate within a single workspace (isolation is INT-03's concern) |
| env.AI mock → tools.ts synthesis path | AI mock returns controlled strings; synthesis content never escapes the test assertion boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — gpt-tokenizer already in mcp-server deps (confirmed by existing token-budget.test.ts) |
| T-05-02-01 | Information Disclosure | Cross-workspace data via fan-out | mitigate | This plan tests single-workspace composition only; cross-workspace isolation is INT-03 (Plan 05-03). Kitchen-sink tests use a single workspace_id throughout |
| T-05-02-02 | Tampering | trimToBudget dropping security-relevant fields | mitigate | D-05 content-preservation assertion: high-severity conflicts survive trim; synthesis survives trim. This is the assertion that proves trimToBudget's NEVER-drop invariant for high-value fields |
| T-05-02-03 | Tampering | Vacuously-true token budget assertion | mitigate | Adversarial-proof: expect(beforeTokens).toBeGreaterThan(7_500) before post-trim assertion (Pitfall 1 guard). If this assertion fails, the fixture is too small and the test is self-invalidating |
</threat_model>

<verification>
After plan completes:

```bash
# Kitchen-sink test passes
npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=v02-kitchen-sink

# Adversarial-proof assertion present
grep -c "toBeGreaterThan(7_500)" packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts

# D-05 content-preservation assertion present
grep -c "result.synthesis.*not.*toBeNull\|not.*toBeNull.*result.synthesis" packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts

# cl100k_base import (not the barrel)
grep -c "gpt-tokenizer/encoding/cl100k_base" packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts

# All 6 matrix rows covered by describe blocks
grep -c "describe\|RNK.*CON\|RNK.*EXP\|EXP.*SYN\|CON.*SYN\|kitchen-sink\|adaptive-routing" packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts
```
</verification>

<success_criteria>
- `npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=v02-kitchen-sink` exits 0
- All 6 describe blocks present (RNK×CON, RNK×EXP, EXP×SYN, CON×SYN, adaptive-routing×cosine-edge, INT-01 kitchen-sink)
- Adversarial-proof assertion present (`beforeTokens > 7_500`)
- D-05 content-preservation assertions present (synthesis + high-severity conflicts survive trim)
- cl100k_base encoding used (not barrel import)
- hybridRank is NOT mocked anywhere in the file
- CF-CODE-ASSIST-USAGE.md routing row appended for this task
</success_criteria>

<output>
Create `.planning/phases/05-integration-kitchen-sink/05-02-SUMMARY.md` when done.
</output>
