---
phase: "05-integration-kitchen-sink"
plan: "05"
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
  - "05-04"
files_modified:
  - .planning/research/v0.2-INTEGRATION-MATRIX.md
autonomous: false
requirements:
  - INT-04
  - INT-05

user_setup:
  - service: wrangler
    why: "INT-05 manual staging ritual requires a deployed Worker environment"
    dashboard_config:
      - task: "Deploy both Workers to production or a named preview before running manual smoke"
        location: "wrangler deploy in packages/mcp-server and packages/triage-worker"

must_haves:
  truths:
    - "v0.2-INTEGRATION-MATRIX.md has zero `pending` rows"
    - "Every `tested` row has a non-empty Test File column pointing to an existing file on disk"
    - "INT-05a automated smoke runs: wrangler dev local boot of both Workers + scripted remember → recall(synthesis) → conflict-surfacing sequence exits 0"
    - "INT-05b manual staging ritual is documented as a verify-work checklist item (not a PR-blocking gate)"
    - "Full test suite is GREEN: npm test --workspaces --if-present exits 0"
  artifacts:
    - path: ".planning/research/v0.2-INTEGRATION-MATRIX.md"
      provides: "Zero-pending integration matrix — INT-04 closure gate satisfied"
      contains: "tested"
  key_links:
    - from: "v0.2-INTEGRATION-MATRIX.md"
      to: "packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts"
      via: "tested rows pointing to this file (all 6 rows)"
      pattern: "v02-kitchen-sink.test.ts"
    - from: "v0.2-INTEGRATION-MATRIX.md"
      to: "packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts"
      via: "NOT directly — isolation test is INT-03 not INT-04; but confirmed on disk before matrix flip"
      pattern: "tested"
---

<objective>
Wave 3 close: flip all 6 matrix rows from `pending` to `tested`, verify the INT-04 grep gate passes,
run the INT-05a automated smoke, document the INT-05b manual staging ritual, and confirm the full test
suite is GREEN.

This plan is the milestone-close gate for v0.2. It is intentionally Wave 3 (depends on 05-02, 05-03,
05-04) because the matrix can only show zero `pending` rows AFTER the covering tests exist on disk. The
D-03 Closure Rule requires each `tested` row to point to a file that resolves via `test -f`. The grep
gate is the INT-04 acceptance condition and must pass before `/gsd:verify-work 5` can declare success.

Purpose: Close the INT-04 matrix gate and verify end-to-end smoke so v0.2 milestone close is unambiguously
provable from CI output.

Output: v0.2-INTEGRATION-MATRIX.md (all rows `tested`), INT-05a smoke passing, INT-05b checklist documented.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-integration-kitchen-sink/05-CONTEXT.md
@.planning/phases/05-integration-kitchen-sink/05-RESEARCH.md
@.planning/phases/05-integration-kitchen-sink/05-VALIDATION.md
@.planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md
@.planning/phases/05-integration-kitchen-sink/05-01-SUMMARY.md
@.planning/phases/05-integration-kitchen-sink/05-02-SUMMARY.md
@.planning/phases/05-integration-kitchen-sink/05-03-SUMMARY.md
@.planning/phases/05-integration-kitchen-sink/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip all 6 matrix rows from `pending` to `tested` and verify INT-04 grep gate</name>
  <files>.planning/research/v0.2-INTEGRATION-MATRIX.md</files>

  <read_first>
    - `.planning/research/v0.2-INTEGRATION-MATRIX.md` — current state (all 6 rows `pending` after Plan 05-01 Notes update; Test File column = `—`)
    - `.planning/phases/05-integration-kitchen-sink/05-CONTEXT.md` §D-03 — status vocabulary is fixed; Test File cell must be non-empty and resolve to a real file
    - `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts` — confirm file exists on disk before flipping rows that point to it
    - `packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts` — confirm exists (INT-03 D-10, not INT-04 — but double-check the matrix does not reference this file unless the audit in Plan 05-01 determined it was a Test File for any row)
  </read_first>

  <action>
Before editing the matrix:

Step 1 — Confirm all covering test files exist on disk:
- test -f packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts
- test -f packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts
- test -f packages/mcp-server/src/__tests__/envelope.test.ts
- test -f packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts

If any file is missing, STOP and raise a blocker — do not flip the matrix until the covering test exists.

Step 2 — Update v0.2-INTEGRATION-MATRIX.md rows:

Per RESEARCH.md §Open Questions 1 resolution (all matrix rows fold into v02-kitchen-sink.test.ts as describe blocks), set all 6 rows as follows:

Row 1 (RNK × CON): Status → `tested`, Test File → `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts`
Row 2 (RNK × EXP): Status → `tested`, Test File → `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts`
Row 3 (EXP × SYN): Status → `tested`, Test File → `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts`
Row 4 (CON × SYN): Status → `tested`, Test File → `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts`
Row 5 (kitchen-sink): Status → `tested`, Test File → `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts`
Row 6 (adaptive-routing × cosine-edge): Status → `tested`, Test File → `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts`

Status vocabulary must match EXACTLY: `tested` (all lowercase). Do NOT write `Tested` or `TESTED`.

Step 3 — Run the INT-04 grep gate:
grep -c "pending" .planning/research/v0.2-INTEGRATION-MATRIX.md

Expected: 0 (the Closure Rule: zero pending rows)

Step 4 — Verify each `tested` row's Test File resolves on disk:
test -f packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts

Expected: exits 0
  </action>

  <verify>
    <automated>! grep -q "pending" /Users/rmoore/Workspaces/engram/.planning/research/v0.2-INTEGRATION-MATRIX.md && test -f /Users/rmoore/Workspaces/engram/packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts && echo "INT-04 GATE PASS"</automated>
  </verify>

  <done>
    - `grep -c "pending" .planning/research/v0.2-INTEGRATION-MATRIX.md` returns 0
    - All 6 rows show `tested` status with Test File = `packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts`
    - `test -f packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: INT-05a automated smoke — local wrangler dev boot + scripted recall sequence</name>
  <files>scripts/smoke-wrangler-dev.sh</files>

  <read_first>
    - `scripts/smoke-wrangler-dev.sh` — existing smoke script (binding-mode=local/remote, mode=http/boot, existing invocation patterns)
    - `.planning/phases/05-integration-kitchen-sink/05-RESEARCH.md` §INT-05 — exact split: (a) automated local wrangler dev CI-runnable; (b) documented manual ritual at milestone close
    - `.planning/phases/05-integration-kitchen-sink/05-CONTEXT.md` §INT-05 discretion — "automated programmatic smoke — local wrangler dev boot of both Workers + scripted remember → recall(verbosity='synthesis') → conflict-surfacing-in-recall, CI-runnable"
  </read_first>

  <action>
INT-05a is the automated smoke. The existing `smoke-wrangler-dev.sh` already supports `binding-mode=local` (stubs remote bindings, CI-safe) and `binding-mode=remote` (real Cloudflare bindings via env vars).

Run the existing smoke script in `binding-mode=local` mode for both Workers to verify local boot:

```
bash scripts/smoke-wrangler-dev.sh packages/mcp-server/wrangler.jsonc 8787 http local
bash scripts/smoke-wrangler-dev.sh packages/triage-worker/wrangler.jsonc 8788 boot local
```

If the smoke script does NOT already have a sequence that exercises `remember → recall(verbosity="synthesis") → conflict-surfacing-in-recall` programmatically, add a minimal smoke sequence to the script or as a companion script `scripts/smoke-kitchen-sink.sh`:
- POST remember({ content: "kitchen-sink smoke test memory" }) to the local MCP server
- POST recall({ query: "kitchen-sink", verbosity: "synthesis" }) and assert the response has a `result` field
- Assert the response JSON parses without error (no 500, no malformed envelope)
- The smoke sequence runs with `binding-mode=local` so no real Cloudflare bindings needed at PR-time

The smoke exits 0 = INT-05a PASS. It does NOT need to assert synthesis quality — just that the pipeline runs without throwing, returns a valid JSON envelope, and the verbosity=synthesis path is exercised.

If the smoke script already covers this sequence, document that finding in the task execution summary without modifying the file. Append a note in the SUMMARY.md.

This task does NOT create a new test file (the smoke script is not a vitest file — it's a bash script). No vitest.config.ts changes needed.
  </action>

  <verify>
    <automated>bash scripts/smoke-wrangler-dev.sh packages/mcp-server/wrangler.jsonc 8787 http local 2>&1 | tail -5 ; echo "smoke exit: $?"</automated>
  </verify>

  <done>
    - Smoke script runs without error in `binding-mode=local` mode
    - The `remember → recall(verbosity="synthesis")` sequence is exercised (either by existing script or new companion script)
    - Smoke exits 0 (INT-05a PASS)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 5 test deliverables are complete:
- v02-kitchen-sink.test.ts: INT-01 worst-case envelope + all 6 matrix rows (Plan 05-02)
- envelope.test.ts extended: INT-02 backward-compat (Plan 05-03)
- cross-workspace-pentest.test.ts extended: INT-03 mcp-server Prong A/C (Plan 05-03)
- conflict-pipeline-isolation.test.ts: INT-03 triage-worker D-10 (Plan 05-04)
- v0.2-INTEGRATION-MATRIX.md: all 6 rows `tested` (Task 1 above)
- INT-05a smoke: automated local wrangler dev boot (Task 2 above)

Full test suite run:
  </what-built>

  <how-to-verify>
Run the full test suite and verify all gates pass:

1. Full workspaces test suite:
   ```
   npm test --workspaces --if-present
   ```
   Expected: all projects GREEN, no failures.

2. INT-04 grep gate:
   ```
   grep -c "pending" .planning/research/v0.2-INTEGRATION-MATRIX.md
   ```
   Expected: 0

3. Spot-check kitchen-sink test:
   ```
   npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=v02-kitchen-sink --reporter=verbose
   ```
   Expected: all 6 describe blocks GREEN.

4. Spot-check pentest:
   ```
   npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=cross-workspace-pentest --reporter=verbose
   ```
   Expected: existing + 3 new Prong-A cases GREEN; Prong-C cases SKIPPED (expected, not failures).

5. INT-05b staging ritual (not a gate — document for milestone close):
   - No staging environment is configured (both wrangler configs are production-only per RESEARCH.md §INT-05)
   - The manual ritual at milestone close: deploy both workers to production (or preview), run the MCP smoke sequence manually in a Claude Desktop session: `remember(...)` → `recall(verbosity="synthesis")` → verify synthesis field is non-null in the response
   - This is a milestone-close checklist item, NOT a PR-blocking gate

Confirm all automated gates pass, then type "approved" to complete Phase 5.
  </how-to-verify>

  <resume-signal>Type "approved" when all automated gates pass. If any test is RED, describe the failure so it can be investigated.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matrix status cells → /gsd:verify-work 5 grep gate | The grep gate reads the exact literal `pending`; any status drift breaks the gate |
| INT-05a smoke → local Workers | Smoke uses binding-mode=local; no real Cloudflare bindings needed at PR-time; no secrets transmitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-SC | Tampering | npm installs | accept | No new packages in this plan |
| T-05-05-01 | Tampering | Matrix status vocabulary drift | mitigate | D-03: only `tested`/`pending`/`out-of-scope` valid; verify `grep -c "pending" == 0` AND `grep -oE "tested|pending|out-of-scope" | sort | uniq` shows only valid tokens |
| T-05-05-02 | Tampering | `tested` row with missing Test File (vacuous closure) | mitigate | Closure Rule step 2: every `tested` row has non-empty Test File; `/gsd:verify-work 5` asserts `test -f <Test File>` for each row |
| T-05-05-03 | Spoofing | INT-05b deployed-staging smoke bypassed without documentation | accept | INT-05b is a documented manual ritual at milestone close, NOT a PR gate. The absence of a staging environment is confirmed (RESEARCH.md §INT-05). Documented checklist is the accepted control. |
</threat_model>

<verification>
After plan completes:

```bash
# INT-04 gate: zero pending rows
grep -c "pending" .planning/research/v0.2-INTEGRATION-MATRIX.md

# All tested rows point to existing files
test -f packages/mcp-server/src/__tests__/integration/v02-kitchen-sink.test.ts && echo "Test File FOUND"

# Full suite GREEN
npm test --workspaces --if-present

# Kitchen-sink
npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=v02-kitchen-sink

# Triage-worker isolation
npm test --workspace=packages/triage-worker -- --project=workerd --testPathPattern=conflict-pipeline-isolation

# Pentest (Prong C skips are expected, not failures)
npm test --workspace=packages/mcp-server -- --project=workerd --testPathPattern=cross-workspace-pentest

# INT-05b staging ritual is a verify-work checklist item only
echo "INT-05b: manual ritual at milestone close — deploy both Workers + run remember/recall smoke manually"
```
</verification>

<success_criteria>
- `grep -c "pending" .planning/research/v0.2-INTEGRATION-MATRIX.md` returns 0
- All 6 matrix rows show `tested` with Test File = v02-kitchen-sink.test.ts
- `npm test --workspaces --if-present` exits 0 (full suite GREEN)
- INT-05a automated smoke passes in `binding-mode=local`
- INT-05b manual staging ritual documented in verify-work checklist
- Human checkpoint approved by Russell
</success_criteria>

<output>
Create `.planning/phases/05-integration-kitchen-sink/05-05-SUMMARY.md` when done.
</output>
