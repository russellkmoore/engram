---
phase: "05-integration-kitchen-sink"
plan: "04"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts
autonomous: true
requirements:
  - INT-03

must_haves:
  truths:
    - "conflict-pipeline-isolation.test.ts exists and passes in the triage-worker workerd pool"
    - "D-10 assertion: WORKSPACE.idFromName is called with newBlock.workspace_id, not a forgeable arg"
    - "D-10 assertion: insertConflictAsInbox is called with workspace_id matching newBlock.workspace_id"
    - "A positive-control assertion proves the seeding worked (conflict detection did run)"
  artifacts:
    - path: "packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts"
      provides: "INT-03 D-10 triage-worker workspace routing isolation proof"
      min_lines: 50
  key_links:
    - from: "conflict-pipeline-isolation.test.ts"
      to: "packages/triage-worker/src/conflict-pipeline.ts conflictPipeline()"
      via: "WORKSPACE.idFromName spy captures workspace_id arg"
      pattern: "idFromName.*workspace_id\|workspace_id.*idFromName"
    - from: "conflict-pipeline-isolation.test.ts"
      to: "packages/triage-worker/src/__tests__/conflict-pipeline.test.ts"
      via: "makeWorkspaceStub pattern (copy from analog)"
      pattern: "makeWorkspaceStub"
---

<objective>
Create `packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts` — the INT-03 D-10
triage-worker isolation test. This file proves that `conflictPipeline()` in the triage-worker routes its
inbox write to the correct workspace DO (identified by `newBlock.workspace_id`) and not to a hardcoded
or forgeable DO identifier.

Purpose: The cross-workspace pentest in Plan 05-03 covers mcp-server isolation. The conflict pipeline
lives in a DIFFERENT Worker (triage-worker) and is called via `ctx.waitUntil(conflictPipeline(...))` in
the async fire-and-forget path. Without an explicit isolation test in the triage-worker package, a bug
that hardcodes the DO lookup or uses a forgeable request arg would be invisible to mcp-server tests.
D-10 closes this gap.

Output: `conflict-pipeline-isolation.test.ts` (new file) passing in the triage-worker workerd pool.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-integration-kitchen-sink/05-CONTEXT.md
@.planning/phases/05-integration-kitchen-sink/05-RESEARCH.md
@.planning/phases/05-integration-kitchen-sink/05-PATTERNS.md
@.planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md

<interfaces>
<!-- Key patterns extracted from PATTERNS.md and RESEARCH.md for the triage-worker test. -->

From packages/triage-worker/src/__tests__/conflict-pipeline.test.ts (primary analog):
- Import pattern (lines 24–59):
  import { describe, it, expect, vi, beforeEach } from "vitest"
  import { env } from "cloudflare:workers"
  import { conflictPipeline } from "../conflict-pipeline.js"
  import { EMBEDDING_MODEL } from "@engram/ai-config"
  vi.mock("@engram/vectorize-utils", ...) — vectorizeNeighbors mock
  vi.mock("../conflict-detection.js", ...) — detectConflict mock
  vi.mock("../analytics.js", ...) — writeAnalytics + workspaceTag mocks

- makeWorkspaceStub(overrides) at lines 91–109: creates { WORKSPACE, mockGetBlocks, mockInsertConflict }
  WORKSPACE = { idFromName: vi.fn().mockReturnValue({ toString: () => "fake-do-id" }), get: vi.fn().mockReturnValue(stub) }

- baseNewBlock at lines 66–73: { id, workspace_id, type, scope, content, created_at }
- fakeEmbedResp: { data: [new Array<number>(1024).fill(0.1)] }
- neighborBelowCeiling: { id: "blk-neighbor-1", score: 0.75, values: [] }
- neighborBlock: { id: "blk-neighbor-1", content: "A is B", created_at: Date.now() - 1000 }
- beforeEach(() => vi.clearAllMocks()) — inter-test reset

From packages/triage-worker/src/conflict-pipeline.ts line 152 (from RESEARCH.md §INT-03):
- env.WORKSPACE.get(env.WORKSPACE.idFromName(newBlock.workspace_id)) — the routing line under test
- insertConflictAsInbox({ workspace_id: newBlock.workspace_id, ... }) — the write target

D-10 isolation assertion shape (PATTERNS.md §conflict-pipeline-isolation.test.ts):
- spy on WORKSPACE.idFromName to capture which workspace_id is passed
- expect(WORKSPACE.idFromName).toHaveBeenCalledWith(targetWorkspaceId)
- expect(mockInsertConflict).toHaveBeenCalledWith(expect.objectContaining({ workspace_id: targetWorkspaceId }))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create conflict-pipeline-isolation.test.ts (INT-03 D-10)</name>
  <files>packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts</files>

  <read_first>
    - `packages/triage-worker/src/__tests__/conflict-pipeline.test.ts` — primary analog: full file (makeWorkspaceStub pattern, baseNewBlock, fakeEmbedResp, neighborBelowCeiling, neighborBlock, vi.mock declarations, beforeEach vi.clearAllMocks)
    - `packages/triage-worker/src/conflict-pipeline.ts` (lines 145–165) — the DO routing call site: `env.WORKSPACE.get(env.WORKSPACE.idFromName(newBlock.workspace_id))` and `insertConflictAsInbox` call
    - `.planning/phases/05-integration-kitchen-sink/05-RESEARCH.md` §INT-03 "Triage-worker conflict-pipeline write path (D-10)" — exact isolation assertion shape and what the current conflict-pipeline.test.ts does NOT assert
    - `.planning/phases/05-integration-kitchen-sink/05-PATTERNS.md` §conflict-pipeline-isolation.test.ts — the D-10 isolation assertion code excerpt
  </read_first>

  <behavior>
    File: packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts

    describe("D-10 conflict-pipeline workspace routing isolation"):

    Test 1: "D-10: conflictPipeline routes WORKSPACE.idFromName lookup by newBlock.workspace_id, not a hardcoded or forgeable arg"
    - targetWorkspaceId = "ws-isolation-target-123"
    - Create mockEnv extending env with:
      - AI: { run: vi.fn().mockResolvedValue(fakeEmbedResp) }
      - WORKSPACE: from makeWorkspaceStub(getBlocksByIds: returns [neighborBlock])
      (use vi.spyOn or direct spy on WORKSPACE.idFromName to capture the argument)
    - Mock vectorizeNeighbors to return [neighborBelowCeiling] (score 0.75 — below the 0.92 dupe ceiling, above 0.7 prefilter threshold)
    - Mock detectConflict to return { is_conflict: true, category: "contradiction", confidence: 0.88, reason: "test" }
    - Create block = { ...baseNewBlock, workspace_id: targetWorkspaceId }
    - await conflictPipeline(mockEnv, block)
    - Assert 1 (D-10 routing): expect(WORKSPACE.idFromName).toHaveBeenCalledWith(targetWorkspaceId)
    - Assert 2 (write target): expect(mockInsertConflict).toHaveBeenCalledWith(expect.objectContaining({ workspace_id: targetWorkspaceId }))

    Test 2: "D-10 positive control: conflictPipeline does NOT write for a different workspace_id"
    - Run conflictPipeline with workspace_id = "ws-A"
    - Assert idFromName was NOT called with "ws-B" (it was called with "ws-A")
    - This is the anti-vacuous control: if idFromName is called with a hardcoded string, it would fail here
    - expect(WORKSPACE.idFromName).not.toHaveBeenCalledWith("ws-B")

    Infrastructure (mirror conflict-pipeline.test.ts):
    - Same vi.mock declarations for @engram/vectorize-utils, ../conflict-detection.js, ../analytics.js
    - Same baseNewBlock, fakeEmbedResp, neighborBelowCeiling, neighborBlock fixtures (copy verbatim)
    - makeWorkspaceStub copied from conflict-pipeline.test.ts (or imported — but copy per PATTERNS.md §shared patterns rule: copy, not abstract)
    - beforeEach(() => vi.clearAllMocks())
  </behavior>

  <action>
[route] 05-04-T1 conflict-pipeline-isolation.test.ts (~80 LOC, makeWorkspaceStub pattern + D-10 assertion spec) | scaffoldTests | N/Y/Y | single-file new test; stable template from conflict-pipeline.test.ts analog + D-10 assertion spec in PATTERNS.md

Create `packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts` following the behavior spec above.

Copy the following verbatim from conflict-pipeline.test.ts (do NOT reinvent):
- All vi.mock() declarations at the top (vectorizeNeighbors, detectConflict, analytics)
- baseNewBlock, fakeEmbedResp, neighborBelowCeiling, neighborBlock fixtures
- makeWorkspaceStub() helper function
- The vi.mocked imports after the vi.mock blocks

The critical difference from conflict-pipeline.test.ts: extend makeWorkspaceStub so the WORKSPACE stub's idFromName is a real spy that records its argument, not just `vi.fn().mockReturnValue(...)`. The existing mock returns a fake DO ID stub — that is correct for DO stub resolution, but now we also need to assert what workspace_id was passed to idFromName. This can be done by wrapping: `WORKSPACE.idFromName = vi.fn().mockImplementation((id) => ({ toString: () => \`fake-do-id-\${id}\` }))` and then using `expect(WORKSPACE.idFromName).toHaveBeenCalledWith(targetWorkspaceId)`.

After writing the file, run: `npm test --workspace=packages/triage-worker -- --project=workerd --testPathPattern=conflict-pipeline-isolation`

Append a routing row to `.planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md` for this task.
  </action>

  <verify>
    <automated>npm test --workspace=packages/triage-worker -- --project=workerd --testPathPattern=conflict-pipeline-isolation 2>&1 | tail -10</automated>
  </verify>

  <done>
    - `packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts` exists with ≥ 2 test cases
    - `npm test --workspace=packages/triage-worker -- --project=workerd --testPathPattern=conflict-pipeline-isolation` exits 0
    - Test file contains `toHaveBeenCalledWith(targetWorkspaceId)` assertion for WORKSPACE.idFromName
    - Test file contains `expect.objectContaining({ workspace_id: targetWorkspaceId })` assertion for insertConflictAsInbox
    - Positive-control test (anti-vacuous) present
    - 05-CF-CODE-ASSIST-USAGE.md routing row appended
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| triage-worker Queue message → conflictPipeline | The MemoryEvent carries workspace_id; the pipeline must route its DO writes by that workspace_id only |
| conflictPipeline → WorkspaceDO inbox write | The routing invariant: WORKSPACE.idFromName(newBlock.workspace_id) — not a request arg, not a hardcoded string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-SC | Tampering | npm installs | accept | No new packages; test file only |
| T-05-04-01 | Tampering | conflict-pipeline write targets wrong workspace DO | mitigate | D-10 assertion: WORKSPACE.idFromName spy captures the argument; assert it equals newBlock.workspace_id. If a developer ever changes the routing line to use a hardcoded string or a forgeable request arg, this test fails. |
| T-05-04-02 | Information Disclosure | Conflict data written to wrong workspace inbox | mitigate | insertConflictAsInbox called with expect.objectContaining({ workspace_id: targetWorkspaceId }) — asserts the write payload includes the correct workspace scope |
| T-05-04-03 | Tampering | Vacuously passing isolation test | mitigate | Positive-control test: assert idFromName was NOT called with a different workspace_id ("ws-B"), proving the spy actually records the argument correctly |
</threat_model>

<verification>
After plan completes:

```bash
# Triage-worker isolation test GREEN
npm test --workspace=packages/triage-worker -- --project=workerd --testPathPattern=conflict-pipeline-isolation

# D-10 assertion present (idFromName routing proof)
grep -c "toHaveBeenCalledWith(targetWorkspaceId)" packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts

# insertConflictAsInbox workspace_id assertion present
grep -c "workspace_id.*targetWorkspaceId\|targetWorkspaceId.*workspace_id" packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts

# Positive-control assertion present
grep -c "not.*toHaveBeenCalledWith\|ws-B" packages/triage-worker/src/__tests__/conflict-pipeline-isolation.test.ts
```
</verification>

<success_criteria>
- `npm test --workspace=packages/triage-worker -- --project=workerd --testPathPattern=conflict-pipeline-isolation` exits 0
- WORKSPACE.idFromName routing assertion present
- insertConflictAsInbox workspace_id assertion present
- Positive-control (anti-vacuous) test present
- CF-CODE-ASSIST-USAGE.md routing row appended
</success_criteria>

<output>
Create `.planning/phases/05-integration-kitchen-sink/05-04-SUMMARY.md` when done.
</output>
