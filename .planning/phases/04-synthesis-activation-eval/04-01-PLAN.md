---
phase: 04-synthesis-activation-eval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/ai-config/src/index.ts
  - packages/mcp-server/src/__tests__/synthesis-postprocess.test.ts
  - packages/mcp-server/src/__tests__/synthesis-preflight.test.ts
  - .planning/phases/04-synthesis-activation-eval/04-CF-CODE-ASSIST-USAGE.md
autonomous: true
requirements:
  - SYN-03
  - SYN-05
  - SYN-06
  - SYN-07
  - SYN-10

must_haves:
  truths:
    - "JUDGE_MODEL constant exists in shared/ai-config/src/index.ts with eval-only docblock — a larger CF model than the Scout generator, never Scout-judging-Scout (D-04)"
    - "Unit tests for post-processor helpers (mapPositionsToCitationIds, dropUncitedSentences, applyHedgePrefix) exist and pass in the workerd pool"
    - "Unit tests for trimRankedForSynthesis preflight throw behavior exist and pass"
    - "Intl.Segmenter availability is probed in the workerd pool; regex fallback is defined if absent"
    - "04-CF-CODE-ASSIST-USAGE.md is scaffolded with 3-question-checklist columns"
  artifacts:
    - path: shared/ai-config/src/index.ts
      provides: "JUDGE_MODEL = '@cf/meta/llama-3.3-70b-instruct-fp8-fast' as const (eval-only)"
      exports: ["JUDGE_MODEL"]
    - path: packages/mcp-server/src/__tests__/synthesis-postprocess.test.ts
      provides: "Unit tests for D-02/D-03/D-09/SYN-06 post-processing behavior (no AI calls)"
    - path: packages/mcp-server/src/__tests__/synthesis-preflight.test.ts
      provides: "Unit tests for SYN-05 preflight throw behavior"
    - path: .planning/phases/04-synthesis-activation-eval/04-CF-CODE-ASSIST-USAGE.md
      provides: "D-10 cf-code-assist routing tracker"
  key_links:
    - from: packages/mcp-server/src/__tests__/synthesis-postprocess.test.ts
      to: packages/mcp-server/src/tools.ts
      via: "imports mapPositionsToCitationIds, dropUncitedSentences, applyHedgePrefix (or the file that exports them after 04-03)"
      pattern: "import.*tools\\.js"
    - from: packages/mcp-server/src/__tests__/synthesis-fidelity.eval.test.ts
      to: shared/ai-config/src/index.ts
      via: "import { JUDGE_MODEL } from '@engram/ai-config'"
      pattern: "JUDGE_MODEL"
---

<objective>
Wave 0 infrastructure: add the JUDGE_MODEL constant, scaffold the cf-code-assist routing tracker, and create unit test stubs for all post-processing behavior. These are the blocking dependencies for the production hardening (Plan 04-03) and the eval gate (Plan 04-04).

Purpose: Establish the test contracts before the production code is written, and wire the eval-only constant so the eval test file can import it. The Intl.Segmenter probe must run first to determine whether the primary sentence-segmenter or the regex fallback is used in the post-processor.

Output: JUDGE_MODEL constant, 04-CF-CODE-ASSIST-USAGE.md, synthesis-postprocess.test.ts (failing — helpers not yet exported), synthesis-preflight.test.ts (failing — throw not yet implemented).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-synthesis-activation-eval/04-CONTEXT.md
@.planning/phases/04-synthesis-activation-eval/04-RESEARCH.md
@.planning/phases/04-synthesis-activation-eval/04-PATTERNS.md
@.planning/phases/04-synthesis-activation-eval/04-VALIDATION.md

<interfaces>
From shared/ai-config/src/index.ts (lines 99–110 — the eval-only constant pattern to mirror):

```typescript
/**
 * A/B challenger model for EXP-08 query-expansion recall comparison.
 *
 * EVAL-ONLY CONSTANT — do NOT use in production code. Scout (`QUERY_EXPANSION_MODEL`)
 * stays the default for query expansion. This constant is imported only by
 * `query-expansion-recall.eval.test.ts` [...]
 */
export const EXPANSION_CHALLENGER_MODEL = "@cf/meta/llama-3.2-3b-instruct" as const;
```

From packages/mcp-server/src/__tests__/query-expansion.test.ts — vi.mock hoisting pattern:

```typescript
// vi.mock declarations MUST be hoisted BEFORE any imports.
vi.mock("../ai-helper.js", () => ({
  safeRun: vi.fn(),
}));
vi.mock("../analytics.js", () => ({
  writeAnalytics: vi.fn(),
  workspaceTag: vi.fn().mockResolvedValue("test-workspace-tag"),
}));

import { describe, it, expect, vi, beforeEach } from "vitest";
const mockEnv = { AI: {} as Ai };

beforeEach(() => { vi.clearAllMocks(); });
```

From packages/mcp-server/src/tools.ts — existing file-local helper pattern (lines 139–166):

```typescript
function trimRankedForSynthesis(memories: LexicalSearchHit[], maxTokens: number): LexicalSearchHit[] {
  const charBudget = maxTokens * 4;
  let used = 0;
  const out: LexicalSearchHit[] = [];
  for (const m of memories) {
    const cost = (m.summary?.length ?? 0) + (m.content?.length ?? 0);
    if (used + cost > charBudget) break;
    out.push(m);
    used += cost;
  }
  return out;
}
```

Post-processor helpers (to be exported from tools.ts in Plan 04-03, but tests import against
the function contracts defined here):
- mapPositionsToCitationIds(synthesis: string, trimmedForSynth: LexicalSearchHit[]): string
- dropUncitedSentences(synthesis: string, trimmedForSynth: LexicalSearchHit[], opts: { lowConfidenceHedge?: boolean }): string
- applyHedgePrefix(synthesis: string, lowConfidence: boolean): string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JUDGE_MODEL constant + scaffold 04-CF-CODE-ASSIST-USAGE.md</name>
  <files>shared/ai-config/src/index.ts, .planning/phases/04-synthesis-activation-eval/04-CF-CODE-ASSIST-USAGE.md</files>

  <read_first>
  1. shared/ai-config/src/index.ts lines 99–115 — the EXPANSION_CHALLENGER_MODEL pattern to mirror exactly (eval-only JSDoc tone, `as const`, insert AFTER line 110 BEFORE the EMBEDDING_DIMS block).
  2. .planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md lines 1–45 — the full header, instructions, 3-question checklist, and table template to copy for the Phase 4 tracker.
  3. .planning/phases/04-synthesis-activation-eval/04-CONTEXT.md D-04 (judge model) and D-10 (tracker).
  4. .planning/phases/04-synthesis-activation-eval/04-PATTERNS.md "shared/ai-config/src/index.ts modification" section — exact constant text including the "EVAL-ONLY CONSTANT — do NOT call in production" warning.
  </read_first>

  <action>
  Step A — JUDGE_MODEL constant in shared/ai-config/src/index.ts:

  Insert the following block after the EXPANSION_CHALLENGER_MODEL block (after line 110, before the EMBEDDING_DIMS block). Use the exact PATTERNS.md text:

  Export name: JUDGE_MODEL
  Value: "@cf/meta/llama-3.3-70b-instruct-fp8-fast" (as const)
  JSDoc must include:
  - "LLM judge model for `synthesis-fidelity.eval.test.ts` faithfulness gate (SYN-02)."
  - "EVAL-ONLY CONSTANT — do NOT call in the production recall() path."
  - "Using a larger model than SYNTHESIS_MODEL (Scout) is required — Scout-judging-Scout is self-lenient (D-04)."
  - "Model confirmed active on CF catalog 2026-06-09. Do not substitute `@cf/meta/llama-3.1-70b-instruct` — deprecated 2026-05-30."

  Do NOT use @cf/meta/llama-3.3-70b-instruct (without -fp8-fast) — that model does not exist.

  Step B — 04-CF-CODE-ASSIST-USAGE.md:

  Copy the Phase 2 tracker structure verbatim from 02-CF-CODE-ASSIST-USAGE.md lines 1–45, substituting "Phase 4 (Synthesis Activation Eval)" for "Phase 2 (Recall Quality Baseline)" and "04-01 through 04-04" for "02-01 through 02-09".

  The Phase 4 candidate task shapes section (per D-10) must name:
  - synthesis-fidelity.eval.test.ts judge-call loop scaffold (scaffoldTests — N/Y/Y candidate)
  - position→[memory_id] post-processor helpers in tools.ts (generateCode — N/Y/Y candidate)
  - generate-synthesis-captions.mjs script body (generateCode — N/Y/Y candidate)
  - byte-frozen-prompt-adjacent wiring, SYN-02 judge-prompt rubric authoring, SYN-09 analytics blob changes: stays with Claude (Y or cross-file consistency)

  Leave the routing table empty (no rows) — rows are appended during execution.

  [route] 04-01-T1 JUDGE_MODEL constant + 04-CF-CODE-ASSIST-USAGE.md | claude | Y/N/N | <10 lines constant + doc scaffold, under 50-line threshold; no stable template for the doc section
  </action>

  <verify>
    <automated>grep -n "JUDGE_MODEL" /Users/rmoore/Workspaces/engram/shared/ai-config/src/index.ts | grep "fp8-fast"</automated>
  </verify>

  <acceptance_criteria>
  - shared/ai-config/src/index.ts exports JUDGE_MODEL = "@cf/meta/llama-3.3-70b-instruct-fp8-fast" as const with EVAL-ONLY docblock.
  - grep confirms: `grep -c "llama-3.3-70b-instruct-fp8-fast" shared/ai-config/src/index.ts` returns 1.
  - grep confirms the model is NOT the deprecated -3.1-70b variant: `grep "llama-3.1-70b" shared/ai-config/src/index.ts` returns nothing (or only the JSDoc deprecation note).
  - 04-CF-CODE-ASSIST-USAGE.md exists with the 3-question checklist and an empty routing table.
  - cd shared/ai-config && npx tsc --noEmit exits 0.
  </acceptance_criteria>

  <done>JUDGE_MODEL constant is exported and type-checks; tracker file is scaffolded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test stubs — synthesis-postprocess.test.ts + synthesis-preflight.test.ts + Intl.Segmenter probe</name>
  <files>
    packages/mcp-server/src/__tests__/synthesis-postprocess.test.ts,
    packages/mcp-server/src/__tests__/synthesis-preflight.test.ts
  </files>

  <read_first>
  1. packages/mcp-server/src/__tests__/query-expansion.test.ts — the full vi.mock hoisting pattern, minimal mockEnv, beforeEach clearAllMocks, and describe/it structure.
  2. packages/mcp-server/src/__tests__/recall.test.ts lines 70–74 — the analytics mock required for any test importing from tools.ts.
  3. .planning/phases/04-synthesis-activation-eval/04-PATTERNS.md sections "synthesis-postprocess.test.ts" and "synthesis-preflight.test.ts" for the exact describe/it structure, Intl.Segmenter fallback pattern, and preflight throw test.
  4. .planning/phases/04-synthesis-activation-eval/04-RESEARCH.md Pattern 3 (D-02 position mapping), Pattern 4 (D-09 drop), Q4 (Intl.Segmenter probe), and the SYN-05/SYN-07/SYN-06 code examples.
  5. packages/mcp-server/src/tools.ts lines 139–166 — trimRankedForSynthesis implementation being tested (preflight test target).
  </read_first>

  <behavior>
  synthesis-postprocess.test.ts — all tests import mapPositionsToCitationIds, dropUncitedSentences, applyHedgePrefix from "../tools.js" (these will be exported in Plan 04-03; until then tests fail at import, which is expected RED state):

  - Test 1 (Intl.Segmenter probe): new Intl.Segmenter("en", {granularity:"sentence"}) does not throw in workerd pool; if it does throw, confirm the regex fallback /(?<=[.!?])\s+(?=[A-Z])/ is used.
  - Test 2 (D-02 mapPositionsToCitationIds): given synthesis "memory 1 did X and memory 2 confirmed it" and trimmedForSynth = [{id:"blk-001"}, {id:"blk-002"}], result = "[blk-001] did X and [blk-002] confirmed it".
  - Test 3 (D-02 case-insensitive): "Memory 1 is first" → "[blk-001] is first".
  - Test 4 (D-03 out-of-range guard is applied by dropUncitedSentences after mapping): a sentence referencing a mapped ID passes; a sentence with no citation is dropped (the out-of-range guard — a positional "memory 5" with only 2 supplied — is consumed by the mapping (no match) and the resulting uncited sentence is dropped by dropUncitedSentences).
  - Test 5 (D-09 dropUncitedSentences): "[blk-001] told us the answer. This sentence has no citation." → "[blk-001] told us the answer."
  - Test 6 (D-09 hedge exception): first sentence is hedge + lowConfidenceHedge=true → hedge sentence kept even without citation.
  - Test 7 (D-09 gap-ack exception): sentence containing "no information" → kept even without citation.
  - Test 8 (SYN-06 applyHedgePrefix lowConfidence=true): returns "Note: the following is based on loosely-matched memories and may be incomplete. " + original synthesis.
  - Test 9 (SYN-06 applyHedgePrefix lowConfidence=false): returns synthesis unchanged.

  synthesis-preflight.test.ts — imports trimRankedForSynthesis from "../tools.js" (already exported-adjacent; see note below):

  - Test 1 (SYN-05 throw): single memory with content.length = 6001*4+1 chars → trimRankedForSynthesis([oversized], 6000) throws Error matching /synthesis-preflight: all memories exceed 6K token budget/.
  - Test 2 (SYN-05 partial truncation): two memories where only the first fits budget → returns [first] only; no throw.
  - Test 3 (SYN-05 meta.gaps note): when truncation occurs (second memory dropped), caller must surface a meta.gaps note; this behavior is tested at the tools.ts integration level in 04-03, not here — this test documents the boundary.

  Note on imports: trimRankedForSynthesis is currently a file-local (non-exported) function in tools.ts. For the preflight test, Plan 04-03 must make it exported or extract it. Mark this test's imports as expected-failing until 04-03 lands. The test file itself must be valid TypeScript and pass `tsc --noEmit`.
  </behavior>

  <action>
  Create synthesis-postprocess.test.ts first:

  1. Open with vi.mock("../ai-helper.js") and vi.mock("../analytics.js") hoisted BEFORE imports (mandatory — tools.ts imports both).
  2. Import { describe, it, expect, vi, beforeEach } from "vitest".
  3. Import mapPositionsToCitationIds, dropUncitedSentences, applyHedgePrefix from "../tools.js" (these will be exported in 04-03; place the import so it compiles without error — use `// @ts-expect-error TODO 04-03: export pending` comment if tsc complains).
  4. Write the Intl.Segmenter probe as describe("Intl.Segmenter availability probe (workerd)", () => { it("Intl.Segmenter is available or regex fallback is defined", ...) }). The probe calls `new Intl.Segmenter("en", { granularity: "sentence" })` in a try/catch. If it throws, the test logs a warning and verifies the regex fallback string `'(?<=[.!?])\\s+(?=[A-Z])'` can be constructed as a RegExp without error. The test PASSES in both cases — it documents which path is active, not fails on the missing API.
  5. Write the mapPositionsToCitationIds, dropUncitedSentences, and applyHedgePrefix test cases per the behavior block. Use a helper `makeLexicalSearchHit(id: string, content: string): LexicalSearchHit` that constructs the minimal shape (`{ id, content, score: 0.8 }`).
  6. Use `beforeEach(() => { vi.clearAllMocks(); })`.

  Create synthesis-preflight.test.ts:

  1. Same vi.mock hoisting (ai-helper.js and analytics.js) and import pattern.
  2. Import { trimRankedForSynthesis } from "../tools.js" — with `// @ts-expect-error TODO 04-03: export pending` if needed.
  3. Write the three test cases from the behavior block.

  No fenced code blocks in this action — the patterns are provided in PATTERNS.md and RESEARCH.md.

  [route] 04-01-T2 synthesis-postprocess.test.ts + synthesis-preflight.test.ts | claude | N/N/Y | <50 LOC each (two small unit test files); context-prep overhead exceeds savings at this size; but stable spec available — borderline. Claude handles to preserve hoisting rules and import contracts across two related files.
  </action>

  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram/packages/mcp-server && npx vitest run --project workerd synthesis-postprocess synthesis-preflight 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
  - Both test files pass TypeScript compilation: cd packages/mcp-server && npx tsc --noEmit exits 0.
  - npx vitest run --project workerd synthesis-postprocess exits (tests may fail at import if helpers not yet exported from tools.ts — that is acceptable RED state; the test files themselves must be syntactically valid).
  - The Intl.Segmenter probe test passes and logs which path (Segmenter or regex) is active.
  - synthesis-postprocess.test.ts contains at least 9 describe/it blocks covering D-02, D-03, D-09, and SYN-06 behaviors.
  - synthesis-preflight.test.ts contains the SYN-05 throw test (Test 1) and at least one partial-truncation test.
  </acceptance_criteria>

  <done>Both test files exist, type-check, and the Intl.Segmenter probe executes. Tests are in RED state until Plan 04-03 exports the helpers — that is correct TDD posture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → Workers AI (judge) | Untrusted judge response crosses into verdict consumption; must be Zod-gated |
| test → tools.ts imports | Import surface must not expose production security controls to tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | 04-CF-CODE-ASSIST-USAGE.md | accept | Tracker contains no credentials or secrets; task IDs only |
| T-04-01-02 | Tampering | shared/ai-config/src/index.ts JUDGE_MODEL | mitigate | Constant is `as const` — immutable at runtime; wrong model ID would cause silent eval failure, not security breach; JSDoc deprecation warning for llama-3.1-70b prevents accidental substitution |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — all dependencies are existing project infrastructure |
</threat_model>

<verification>
1. `grep "llama-3.3-70b-instruct-fp8-fast" /Users/rmoore/Workspaces/engram/shared/ai-config/src/index.ts` — confirms the correct model ID (with -fp8-fast suffix).
2. `grep "EVAL-ONLY" /Users/rmoore/Workspaces/engram/shared/ai-config/src/index.ts` — confirms at least one EVAL-ONLY docblock exists.
3. `cd /Users/rmoore/Workspaces/engram/shared/ai-config && npx tsc --noEmit` — exits 0.
4. `ls /Users/rmoore/Workspaces/engram/.planning/phases/04-synthesis-activation-eval/04-CF-CODE-ASSIST-USAGE.md` — file exists.
5. `ls /Users/rmoore/Workspaces/engram/packages/mcp-server/src/__tests__/synthesis-postprocess.test.ts /Users/rmoore/Workspaces/engram/packages/mcp-server/src/__tests__/synthesis-preflight.test.ts` — both files exist.
6. `cd /Users/rmoore/Workspaces/engram/packages/mcp-server && npx tsc --noEmit` — exits 0 (both test files type-check).
</verification>

<success_criteria>
- JUDGE_MODEL = "@cf/meta/llama-3.3-70b-instruct-fp8-fast" exported from @engram/ai-config with eval-only docblock.
- 04-CF-CODE-ASSIST-USAGE.md exists with 3-question checklist and empty routing table.
- synthesis-postprocess.test.ts and synthesis-preflight.test.ts exist, type-check, and contain the behavior contracts (RED state until 04-03 exports the helpers is acceptable).
- Intl.Segmenter probe runs and documents which segmentation path is active in this workerd build.
- No new npm packages introduced.
</success_criteria>

<output>
Create `.planning/phases/04-synthesis-activation-eval/04-01-SUMMARY.md` when done.
</output>
