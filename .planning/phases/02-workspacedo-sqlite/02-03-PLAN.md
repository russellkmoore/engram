---
phase: 02-workspacedo-sqlite
plan: 03
type: execute
wave: 1
depends_on: [02-00, 02-01, 02-02]
files_modified:
  - packages/workspace-do/src/seeding.ts
autonomous: true
requirements: [STO-05]
must_haves:
  truths:
    - "seedSystemTypes(sql) is a SYNCHRONOUS function (matches the migration runner — runs inside blockConcurrencyWhile)"
    - "Uses INSERT OR IGNORE on the memory_types table — idempotent on PK conflict (id)"
    - "Iterates SYSTEM_TYPES from @engram/schema — never re-derives the 7 type definitions locally"
    - "fields column gets JSON.stringify(t.fields) — TEXT/JSON column convention per D-03"
    - "workspace_id is hard-coded NULL and source is hard-coded 'system' (discriminants per CLAUDE.md SQLite schema spec)"
    - "Running the loop 100 times still produces exactly 7 rows (idempotency invariant — covered by seeding.test.ts in Plan 04)"
  artifacts:
    - path: "packages/workspace-do/src/seeding.ts"
      provides: "System-type seed loop: seedSystemTypes(sql) → void"
      exports: ["seedSystemTypes"]
  key_links:
    - from: "packages/workspace-do/src/seeding.ts"
      to: "shared/schema/src/system-types.ts (via @engram/schema barrel)"
      via: "import { SYSTEM_TYPES } from '@engram/schema'"
      pattern: "import.*SYSTEM_TYPES.*from.*@engram/schema"
---

<objective>
Implement the system-type seed loop for STO-05: a synchronous `seedSystemTypes(sql)` function that iterates the canonical `SYSTEM_TYPES` array from `@engram/schema` and writes each as an `INSERT OR IGNORE` into the `memory_types` table. Idempotent on PK collision — re-running is safe (which is the STO-05 + STO-09 invariant).

Purpose: STO-05 mandates that the 7 system memory types are present after first init AND that double-init doesn't duplicate. The `INSERT OR IGNORE` semantic is the implementation. Called immediately after `runMigrations` inside the same `blockConcurrencyWhile` block (wiring lands in Plan 04).

Output:
- `packages/workspace-do/src/seeding.ts` exporting `seedSystemTypes(sql: SqlStorage): void`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspacedo-sqlite/02-CONTEXT.md
@.planning/phases/02-workspacedo-sqlite/02-RESEARCH.md
@.planning/phases/02-workspacedo-sqlite/02-PATTERNS.md
@shared/schema/src/system-types.ts
@packages/workspace-do/src/schema.ts
@packages/workspace-do/src/migrations.ts
@packages/workspace-do/src/__tests__/seeding.test.ts

<interfaces>
<!-- From shared/schema/src/system-types.ts: -->

```typescript
export interface SystemMemoryType {
  id: string;
  name: string;
  fields: readonly FieldDefinition[];
}
export const SYSTEM_TYPES: readonly SystemMemoryType[]; // 7 entries: job_application, contact, company, project, research_note, decision_log, meeting_note
```

<!-- Target memory_types row (per schema.ts V1_SQL): -->

memory_types: id PK, name NOT NULL, fields TEXT-as-JSON NOT NULL, workspace_id (NULL for system), source NOT NULL

<!-- Loop pattern (verbatim from PATTERNS.md §5 / RESEARCH.md §2): -->

```typescript
import { SYSTEM_TYPES } from "@engram/schema";
import type { SqlStorage } from "@cloudflare/workers-types";

export function seedSystemTypes(sql: SqlStorage): void {
  for (const t of SYSTEM_TYPES) {
    sql.exec(
      "INSERT OR IGNORE INTO memory_types (id, name, fields, workspace_id, source) VALUES (?, ?, ?, NULL, 'system')",
      t.id, t.name, JSON.stringify(t.fields),
    );
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create seeding.ts (seedSystemTypes loop)</name>
  <files>packages/workspace-do/src/seeding.ts</files>
  <behavior>
    - First call on a clean memory_types table inserts exactly 7 rows (one per SYSTEM_TYPES entry)
    - Second call inserts 0 additional rows (INSERT OR IGNORE no-op on PK conflict)
    - `SELECT COUNT(*) FROM memory_types` returns 7 after both first and second call
    - `SELECT id FROM memory_types ORDER BY id` returns the 7 system type IDs in alphabetical order (or any order — assertion is that all 7 present)
    - Every row has `workspace_id IS NULL` and `source = 'system'`
    - `JSON.parse(fields)` on a row returns the corresponding `SystemMemoryType.fields` array structure (round-trip preserved)
  </behavior>
  <read_first>
    - .planning/phases/02-workspacedo-sqlite/02-RESEARCH.md §2 (seeding pattern — verbatim INSERT OR IGNORE shape)
    - .planning/phases/02-workspacedo-sqlite/02-PATTERNS.md §5 (seeding.ts pattern + drift risk + convention to honor)
    - .planning/phases/02-workspacedo-sqlite/02-CONTEXT.md (D-03 — JSON.stringify on write)
    - shared/schema/src/system-types.ts (the source-of-truth array — read to understand the SystemMemoryType shape + the 7 entries)
    - packages/workspace-do/src/schema.ts (the memory_types DDL — confirm column order matches the INSERT statement)
    - packages/workspace-do/src/__tests__/seeding.test.ts (RED stub — see what Plan 04 will assert against)
  </read_first>
  <action>
    Create `packages/workspace-do/src/seeding.ts`. File contains JSDoc header with `@module @engram/workspace-do/seeding` and design notes citing STO-05 + CLAUDE.md §"Memory Types (Schema-as-Data)" + D-03 (JSON column write convention). Imports: `import { SYSTEM_TYPES } from "@engram/schema";` and `import type { SqlStorage } from "@cloudflare/workers-types";`. Export `seedSystemTypes(sql: SqlStorage): void` — synchronous (matches `runMigrations` from Plan 01). Function body iterates `SYSTEM_TYPES` with `for (const t of SYSTEM_TYPES)` and calls `sql.exec("INSERT OR IGNORE INTO memory_types (id, name, fields, workspace_id, source) VALUES (?, ?, ?, NULL, 'system')", t.id, t.name, JSON.stringify(t.fields))` — positional `?` bindings only (Key APIs §B), single-statement so multi-statement binding gotcha (Pitfall 8) does not apply. `workspace_id` is hard-coded `NULL` in the SQL (NOT a binding — `NULL` literal) and `source` is hard-coded `'system'` in the SQL (NOT a binding — string literal); these are the discriminants distinguishing system seeds from user-defined types (CLAUDE.md §"SQLite Schema" memory_types comment). Function MUST be synchronous: no `async`, no `await`, no Promise return — Plan 04 calls it inside `blockConcurrencyWhile` immediately after `runMigrations`. Add an inline comment at the top of the function body: `// Sync function — called inside blockConcurrencyWhile from WorkspaceDO constructor. INSERT OR IGNORE provides idempotency on PK collision (id), so this loop is safe to re-run on hibernation replay (STO-09).` Do NOT add seed entries for non-system memory types here (PATTERNS.md §5 drift risk — user-defined types are Phase 3+). Do NOT add zod validation of `t.fields` here (D-03 defers validation to Phase 4 MCP tool boundary).
  </action>
  <verify>
    <automated>grep -q "INSERT OR IGNORE" packages/workspace-do/src/seeding.ts && grep -q "SYSTEM_TYPES" packages/workspace-do/src/seeding.ts && grep -q "JSON.stringify(t.fields)" packages/workspace-do/src/seeding.ts && grep -v '^\s*//\|^\s*\*' packages/workspace-do/src/seeding.ts | grep -cE "\basync\b|\bawait\b" | grep -q "^0$" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/workspace-do/src/seeding.ts` exists
    - File contains `export function seedSystemTypes`
    - File imports `SYSTEM_TYPES` from `@engram/schema` (not from a relative path — uses the workspace symlink)
    - File contains the literal string `INSERT OR IGNORE INTO memory_types` (exact case, exact phrasing — the test in Plan 04 grep-asserts on this)
    - File contains `JSON.stringify(t.fields)` (D-03 convention — write JSON columns by stringify on the helper boundary)
    - File contains the hard-coded `NULL` literal in the SQL for workspace_id and the hard-coded `'system'` literal for source (NOT bindings — discriminant constants)
    - `grep -v '^\s*//\|^\s*\*' packages/workspace-do/src/seeding.ts | grep -cE "\basync\b|\bawait\b"` returns 0 (synchronous only — passes STO-10 lint when wired in Plan 04)
    - File header JSDoc cites STO-05 and D-03 (so future contributors can grep for the rationale)
    - `npm run typecheck` exits 0
    - File contains NO additional seed inserts for non-system types (drift risk per PATTERNS.md §5)
    - File contains NO `env.`, `fetch(`, or `await this.ai` tokens (forward-compatible with the STO-10 lint script from Plan 07)
  </acceptance_criteria>
  <done>Seeding helper exists with the correct synchronous shape, ready for Plan 04 to call it inside the constructor's blockConcurrencyWhile block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeding.ts → memory_types table | Writes 7 system rows on first init; replays idempotently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | `memory_types` system rows | mitigate | `INSERT OR IGNORE` guarantees PK conflict is a no-op, not a throw. Hibernation replay test in Plan 04 verifies count stays at 7 after double-init. |
| T-02-03-02 | Spoofing | User adds a row with `source = 'system'` post-seed | mitigate | This file only writes the canonical 7 from `SYSTEM_TYPES`. Phase 3+ user-defined types use `source = 'user'`. Cross-source-discriminant validation lands at the Phase 4 tool boundary (per D-03 deferral). |
| T-02-03-03 | Tampering | `t.fields` JSON serialization corruption | accept | `JSON.stringify` on the read side is the canonical inverse (D-03). If `t.fields` ever contains a `undefined` value (Pitfall 4 — would silently drop), `SYSTEM_TYPES` is `as const satisfies readonly SystemMemoryType[]` — TS rejects undefined at definition time. |
</threat_model>

<verification>
After this plan:
- `grep -c "INSERT OR IGNORE" packages/workspace-do/src/seeding.ts` returns at least 1
- `grep -c "SYSTEM_TYPES" packages/workspace-do/src/seeding.ts` returns at least 1 (the import)
- `npm run typecheck` exits 0
- The seeding test stub (Plan 00 created it) still shows skipped/RED — Plan 04 wires the constructor call that exercises the loop
</verification>

<success_criteria>
- seedSystemTypes(sql) exists and is exported
- Synchronous (no async/await)
- Uses INSERT OR IGNORE for idempotency
- Reads SYSTEM_TYPES from @engram/schema (no local re-definition)
- JSON.stringify on the fields column (D-03)
- workspace_id NULL and source 'system' hard-coded
</success_criteria>

<output>
Create `.planning/phases/02-workspacedo-sqlite/02-03-SUMMARY.md` documenting the seed loop contract + the INSERT OR IGNORE idempotency choice + the rationale for hard-coded NULL/'system' literals.
</output>
