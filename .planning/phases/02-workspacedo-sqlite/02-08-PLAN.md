---
phase: 02-workspacedo-sqlite
plan: 08
type: execute
wave: 5
depends_on: [02-04, 02-05, 02-06, 02-07]
files_modified:
  - .github/workflows/ci.yml
  - .lintstagedrc.json
autonomous: true
requirements: [STO-08, STO-10]
must_haves:
  truths:
    - "CI workflow runs `Lint blockConcurrencyWhile I/O (STO-10)` step AFTER `Lint (ESLint)` and BEFORE `Lint wrangler.jsonc (FND-08)` (D-11 ordering)"
    - "CI workflow includes the 3-step lint pattern from FND-08: main + negative-fixture invert + positive-fixture pass"
    - "CI workflow runs `Test (Vitest)` step AFTER smoke tests (so wrangler-dev failures surface before test failures)"
    - ".lintstagedrc.json adds a glob `packages/workspace-do/src/**/*.ts` that runs `node scripts/lint-blockconcurrency.mjs` (additive — does NOT replace the eslint/prettier chain on .ts files; A5)"
    - "CI fails when lint:blockconcurrency exits non-zero (proves the gate is wired)"
    - "CI fails when any test fails (vitest exit code propagated)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with STO-10 lint block + Vitest step"
      contains: "lint:blockconcurrency\\|npm test"
    - path: ".lintstagedrc.json"
      provides: "Pre-commit hook running lint:blockconcurrency on staged workspace-do .ts files"
      contains: "packages/workspace-do/src/\\*\\*/\\*\\.ts"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/lint-blockconcurrency.mjs"
      via: "npm run lint:blockconcurrency"
      pattern: "lint:blockconcurrency"
    - from: ".lintstagedrc.json"
      to: "scripts/lint-blockconcurrency.mjs"
      via: "node scripts/... invocation on the workspace-do glob"
      pattern: "lint-blockconcurrency.*packages/workspace-do/src"
---

<objective>
Wire the Phase 2 lint script + vitest suite into the project's automation: insert a three-step CI block for `lint:blockconcurrency` (mirroring FND-08's pattern) between the existing `lint` and `lint:wrangler` steps in `.github/workflows/ci.yml`, add a Vitest step after the smoke tests, and add a lint-staged rule that runs the lint script on staged `packages/workspace-do/src/**/*.ts` files.

Purpose: STO-08 mandates that the Phase 2 vitest suite is green in CI. STO-10 mandates that the lint script "fails CI on regression." Both are wired here. Plan 07 created the script; Plan 04/06 made the test suite green; this plan turns those into permanent CI/pre-commit gates.

Output:
- `.github/workflows/ci.yml`: 3 new lint steps (main + negative-fixture + positive-fixture) inserted between `lint` and `lint:wrangler`; 1 new test step after smoke tests
- `.lintstagedrc.json`: 1 new glob rule for `packages/workspace-do/src/**/*.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspacedo-sqlite/02-CONTEXT.md
@.planning/phases/02-workspacedo-sqlite/02-RESEARCH.md
@.planning/phases/02-workspacedo-sqlite/02-PATTERNS.md
@.github/workflows/ci.yml
@.lintstagedrc.json
@scripts/lint-blockconcurrency.mjs
@scripts/lint-wrangler.mjs
@package.json

<interfaces>
<!-- CI workflow shape (mirror FND-08's 3-step block at .github/workflows/ci.yml:36-52): -->

The new block lands BETWEEN `Lint (ESLint)` (line 30-31) and `Lint wrangler.jsonc (FND-08)` (line 36) — i.e., D-11 ordering.

```yaml
- name: Lint blockConcurrencyWhile I/O (STO-10)
  run: npm run lint:blockconcurrency

# STO-10 negative-fixture assertion: the bad fixture MUST trigger a
# lint failure. If this step succeeds (lint exits 0 on bad fixture),
# the linter has regressed.
- name: Lint blockConcurrencyWhile — negative fixture must fail (STO-10)
  run: |
    if node scripts/lint-blockconcurrency.mjs packages/workspace-do/__fixtures__/bad-blockconcurrency.ts; then
      echo "STO-10 regression: bad fixture did not trigger lint failure"
      exit 1
    fi
    echo "STO-10 negative fixture correctly failed."

# STO-10 positive-fixture assertion:
- name: Lint blockConcurrencyWhile — positive fixture must pass (STO-10)
  run: node scripts/lint-blockconcurrency.mjs packages/workspace-do/__fixtures__/good-blockconcurrency.ts
```

The Vitest step lands AFTER the existing smoke steps (so wrangler-dev boot failures surface before test failures):

```yaml
- name: Test (Vitest)
  run: npm test
```

<!-- lint-staged rule (mirror existing `**/wrangler.jsonc` rule): -->

Current `.lintstagedrc.json`:
```json
{
  "*.{ts,mts,cts,js,mjs,cjs}": ["eslint --fix", "prettier --write"],
  "*.{json,jsonc,md,yaml,yml}": ["prettier --write"],
  "**/wrangler.jsonc": ["node scripts/lint-wrangler.mjs"]
}
```

After Plan 08:
```json
{
  "*.{ts,mts,cts,js,mjs,cjs}": ["eslint --fix", "prettier --write"],
  "*.{json,jsonc,md,yaml,yml}": ["prettier --write"],
  "**/wrangler.jsonc": ["node scripts/lint-wrangler.mjs"],
  "packages/workspace-do/src/**/*.ts": ["node scripts/lint-blockconcurrency.mjs"]
}
```

<!-- A5 (PATTERNS.md §17): the new glob is ADDITIVE — the general .ts rule still runs eslint/prettier. lint-staged runs both globs' matchers (in parallel when files match both). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch .github/workflows/ci.yml (3 lint steps + Vitest step)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current state — full file; identifies exact insertion points)
    - .planning/phases/02-workspacedo-sqlite/02-PATTERNS.md §16 (CI patch shape — verbatim YAML to insert; mirrors FND-08 3-step block)
    - .planning/phases/02-workspacedo-sqlite/02-CONTEXT.md (D-11 — CI step ordering: lint:blockconcurrency AFTER lint, BEFORE lint:wrangler)
    - scripts/lint-blockconcurrency.mjs (verify the path the CI steps invoke is correct)
  </read_first>
  <action>
    Edit `.github/workflows/ci.yml`. Insert three new steps BETWEEN the existing `- name: Lint (ESLint)` step (currently ending at line 31) and the existing `- name: Lint wrangler.jsonc (FND-08)` step (currently starting at line 36), preserving the exact YAML indentation (6 spaces per level — verify against existing steps before writing).

    The three new steps:

    1. **Main lint step**:
       ```yaml
             - name: Lint blockConcurrencyWhile I/O (STO-10)
               run: npm run lint:blockconcurrency
       ```

    2. **Negative-fixture assertion** (mirrors FND-08 lines 39-48):
       ```yaml

             # STO-10 negative-fixture assertion: the bad fixture MUST trigger a
             # lint failure. If this step succeeds (lint exits 0 on bad fixture),
             # the linter has regressed and is no longer catching forbidden tokens
             # inside blockConcurrencyWhile blocks.
             - name: Lint blockConcurrencyWhile — negative fixture must fail (STO-10)
               run: |
                 if node scripts/lint-blockconcurrency.mjs packages/workspace-do/__fixtures__/bad-blockconcurrency.ts; then
                   echo "STO-10 regression: bad fixture did not trigger lint failure"
                   exit 1
                 fi
                 echo "STO-10 negative fixture correctly failed."
       ```

    3. **Positive-fixture assertion** (mirrors FND-08 lines 50-52):
       ```yaml

             # STO-10 positive-fixture assertion: the good fixture must pass cleanly.
             - name: Lint blockConcurrencyWhile — positive fixture must pass (STO-10)
               run: node scripts/lint-blockconcurrency.mjs packages/workspace-do/__fixtures__/good-blockconcurrency.ts
       ```

    Then APPEND a Vitest step AFTER the last existing `- name: Smoke - fresh install` step (currently the file's last step at line 60-61):

    ```yaml

          - name: Test (Vitest)
            run: npm test
    ```

    Critical constraints:
    - Step ordering is `Lint (ESLint)` → `Format check` → **`Lint blockConcurrencyWhile I/O (STO-10)`** → **negative-fixture** → **positive-fixture** → `Lint wrangler.jsonc (FND-08)` → ... → `Smoke - fresh install` → **`Test (Vitest)`**. D-11 places blockconcurrency between lint and lint:wrangler; the Vitest step is appended after smoke tests so test failures don't mask wrangler-dev boot failures.
    - Use the literal fixture paths `packages/workspace-do/__fixtures__/bad-blockconcurrency.ts` and `packages/workspace-do/__fixtures__/good-blockconcurrency.ts` (NOT `src/__tests__/fixtures/...` — Plan 00 placed them under `__fixtures__/` per PATTERNS.md §17 drift mitigation).
    - Preserve YAML indentation exactly — `actions/setup-node@v4` style requires consistent 6-space indents per level. Verify by running `yamllint .github/workflows/ci.yml` if available, OR by ensuring the file `git diff` shows clean insertion without any indentation churn outside the new lines.
    - Do NOT alter any existing step (FND-08 block, smoke tests, install, typecheck, etc.) — Phase 2 is additive only.
  </action>
  <verify>
    <automated>grep -q "Lint blockConcurrencyWhile I/O (STO-10)" .github/workflows/ci.yml && grep -q "negative fixture must fail (STO-10)" .github/workflows/ci.yml && grep -q "positive fixture must pass (STO-10)" .github/workflows/ci.yml && grep -q "Test (Vitest)" .github/workflows/ci.yml && grep -q "npm test" .github/workflows/ci.yml && grep -q "__fixtures__/bad-blockconcurrency.ts" .github/workflows/ci.yml && grep -q "__fixtures__/good-blockconcurrency.ts" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains the 3 new lint step names (literal substrings: `"Lint blockConcurrencyWhile I/O (STO-10)"`, `"negative fixture must fail (STO-10)"`, `"positive fixture must pass (STO-10)"`)
    - `.github/workflows/ci.yml` contains the new Vitest step name `"Test (Vitest)"` with `run: npm test`
    - Step order is verified: `awk '/- name:/' .github/workflows/ci.yml` lists `Lint (ESLint)` BEFORE `Lint blockConcurrencyWhile I/O (STO-10)` BEFORE `Lint wrangler.jsonc (FND-08)`
    - Step order: `awk '/- name:/' .github/workflows/ci.yml | grep "Test (Vitest)"` appears AFTER `Smoke - fresh install` in the same awk output
    - Both fixture path references use `__fixtures__/` (NOT `src/__tests__/fixtures/`) — verified by `grep -c "src/__tests__/fixtures" .github/workflows/ci.yml` returning 0
    - YAML still parses validly (smoke check: `node -e "const yaml = require('js-yaml'); yaml.load(require('fs').readFileSync('.github/workflows/ci.yml', 'utf8'))"` — note: js-yaml may not be installed; if so, fall back to `cat .github/workflows/ci.yml` and visually verify indentation OR rely on `git diff` cleanliness)
    - Indentation matches existing steps (6 spaces for step-level keys, 8 spaces for `run:` values) — verified by `awk '/^      - name:/' .github/workflows/ci.yml | wc -l` counting all step entries and matching the expected count post-insertion
  </acceptance_criteria>
  <done>CI workflow now runs the STO-10 lint (with fixture canaries) and the Phase 2 vitest suite on every push/PR.</done>
</task>

<task type="auto">
  <name>Task 2: Patch .lintstagedrc.json (workspace-do .ts glob)</name>
  <files>.lintstagedrc.json</files>
  <read_first>
    - .lintstagedrc.json (current state — 3 globs)
    - .planning/phases/02-workspacedo-sqlite/02-PATTERNS.md §17 (lint-staged rule shape — mirrors existing wrangler.jsonc entry)
    - .planning/phases/02-workspacedo-sqlite/02-CONTEXT.md (Assumption A5 — ADDITIVE: new glob does NOT replace the general .ts eslint/prettier chain)
    - .planning/phases/02-workspacedo-sqlite/02-RESEARCH.md (A5 in Assumptions Log — lint-staged runs both globs' matchers in parallel when a file matches multiple)
  </read_first>
  <action>
    Edit `.lintstagedrc.json`. Add a new top-level key after the existing `**/wrangler.jsonc` entry:

    Before:
    ```json
    {
      "*.{ts,mts,cts,js,mjs,cjs}": ["eslint --fix", "prettier --write"],
      "*.{json,jsonc,md,yaml,yml}": ["prettier --write"],
      "**/wrangler.jsonc": ["node scripts/lint-wrangler.mjs"]
    }
    ```

    After:
    ```json
    {
      "*.{ts,mts,cts,js,mjs,cjs}": ["eslint --fix", "prettier --write"],
      "*.{json,jsonc,md,yaml,yml}": ["prettier --write"],
      "**/wrangler.jsonc": ["node scripts/lint-wrangler.mjs"],
      "packages/workspace-do/src/**/*.ts": ["node scripts/lint-blockconcurrency.mjs"]
    }
    ```

    Critical points:
    - Glob is `packages/workspace-do/src/**/*.ts` — narrower than the production full-scan glob; lint-staged only sees staged files in that dir.
    - Fixtures (in `packages/workspace-do/__fixtures__/`) are OUTSIDE this glob — so staging a fixture file does NOT invoke the lint on itself.
    - The rule is ADDITIVE per A5: a staged `packages/workspace-do/src/index.ts` matches BOTH the general `*.{ts,mts,...}` rule (gets eslint/prettier) AND this new rule (gets lint:blockconcurrency). lint-staged runs both chains.
    - JSON must remain valid (trailing comma forbidden in standard JSON; `.lintstagedrc.json` is plain JSON, not JSONC). Verify by `node -e "JSON.parse(require('fs').readFileSync('.lintstagedrc.json', 'utf8'))"`.

    Optional: verify the lint-staged rule by staging a fixture change in a side branch and observing both linters run — DEFER this manual verification to a developer or document it in the SUMMARY; do not block this task on a manual workflow test.
  </action>
  <verify>
    <automated>node -e "const c = JSON.parse(require('fs').readFileSync('.lintstagedrc.json', 'utf8')); if (!c['packages/workspace-do/src/**/*.ts']) process.exit(1); if (!c['packages/workspace-do/src/**/*.ts'].includes('node scripts/lint-blockconcurrency.mjs')) process.exit(2); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.lintstagedrc.json` is valid JSON (parses without error)
    - `.lintstagedrc.json` contains the new top-level key `"packages/workspace-do/src/**/*.ts"` with value `["node scripts/lint-blockconcurrency.mjs"]`
    - All 3 pre-existing rules (`*.{ts,mts,cts,js,mjs,cjs}`, `*.{json,jsonc,md,yaml,yml}`, `**/wrangler.jsonc`) are preserved unchanged
    - Total key count is 4 (verified by `node -e "console.log(Object.keys(JSON.parse(require('fs').readFileSync('.lintstagedrc.json', 'utf8'))).length)"` returning 4)
    - No trailing commas (standard JSON, not JSONC)
  </acceptance_criteria>
  <done>Pre-commit hook now runs the STO-10 lint on staged workspace-do source files, mirroring the FND-08 pre-commit defense.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer commit → lint-staged pre-commit | Forbidden tokens caught before they reach the remote |
| GitHub Actions CI → main branch | All Phase 2 invariants verified on every push/PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Tampering | Developer disables pre-commit hooks locally (husky --no-verify) | accept | The CI workflow is the final gate. Pre-commit is a developer-experience nicety; CI catches anything that bypasses husky. |
| T-02-08-02 | Tampering | CI step ordering swap (e.g., lint:blockconcurrency runs after smoke tests) | mitigate | D-11 ordering documented in CONTEXT.md + asserted by acceptance criteria via awk ordering check. Reviewer can spot any reorder in PR diff. |
| T-02-08-03 | Tampering | Negative-fixture step succeeds (lint regresses) | mitigate | The CI block's `if node ... ; then exit 1; fi` pattern (mirroring FND-08 lines 42-48) inverts the exit code — when the bad fixture stops failing, the step explicitly fails the workflow. Open Question O4 confirmed this is the correct pattern. |
| T-02-08-04 | Tampering | Fixture path drift between CI and the actual fixture location | mitigate | Acceptance criteria asserts the CI references `__fixtures__/` and NOT `src/__tests__/fixtures/` — this is the PATTERNS.md §17 drift mitigation locked in. |
| T-02-08-05 | Tampering | Vitest fails silently due to missing test script in a workspace | accept | `--workspaces --if-present` skips workspaces without a `test` script. Plan 00 added the script to @engram/workspace-do; if a future workspace needs tests, it adds its own `"test"` script. No silent regression of Phase 2's coverage. |
</threat_model>

<verification>
After both tasks:
1. `grep -c "STO-10" .github/workflows/ci.yml` returns at least 3 (one per new lint step)
2. `grep -c "Test (Vitest)" .github/workflows/ci.yml` returns 1
3. `awk '/- name:/' .github/workflows/ci.yml` shows the correct ordering: lint(eslint) → blockconcurrency main → blockconcurrency negative → blockconcurrency positive → wrangler(FND-08) → ... → smoke → Test (Vitest)
4. `.lintstagedrc.json` parses cleanly via `JSON.parse` and contains the new glob rule
5. Locally simulate CI behavior: `npm run lint && npm run lint:blockconcurrency && npm run lint:wrangler && npm test` from repo root all exit 0
6. Negative-fixture path is `packages/workspace-do/__fixtures__/bad-blockconcurrency.ts` (verified by `grep "__fixtures__/bad-blockconcurrency" .github/workflows/ci.yml | wc -l` returning 1)
</verification>

<success_criteria>
- CI workflow runs lint:blockconcurrency with the 3-step pattern (main + negative + positive fixtures)
- CI step ordering matches D-11 (between lint and lint:wrangler)
- CI runs Vitest after smoke tests
- lint-staged rule covers workspace-do/src/**/*.ts (additive to eslint/prettier chain)
- All Phase 2 lint scripts + tests gated in CI
- STO-08 + STO-10 invariants enforced
</success_criteria>

<output>
Create `.planning/phases/02-workspacedo-sqlite/02-08-SUMMARY.md` documenting the CI patch + lint-staged rule + the explicit invariant (D-11 ordering, fixture paths under __fixtures__/, additive lint-staged behavior). Note the Phase 2 phase-completion gates: `npm run lint && npm run lint:wrangler && npm run lint:blockconcurrency && npm test` all green.
</output>
