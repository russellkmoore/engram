---
phase: 07-deploy-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/kv-bootstrap.mjs
autonomous: true
requirements: [DEP-01, DEP-05]
tags: [deploy, npm-scripts, setup-automation, wrangler]

must_haves:
  truths:
    - "Operator can run `npm run setup` and it chains install → types:gen → setup:vectorize → setup:queue → final echo (D-07)"
    - "Operator can run `npm run deploy` and it triggers predeploy eval gate, then deploys mcp-server BEFORE triage-worker (D-08 + cross-worker DO order invariant)"
    - "Operator can run `npm run deploy:mcp` or `npm run deploy:triage` for surgical re-deploys that skip the eval gate (D-08 npm lifecycle semantics)"
    - "`npm run kv:bootstrap -- --help` mentions README §Getting Started Step 4 for discoverability (CONTEXT.md integration points)"
  artifacts:
    - path: "package.json"
      provides: "Root npm scripts including deploy wrapper, per-package deploys, extended setup"
      contains: '"deploy:mcp": "npm run deploy --workspace=@engram/mcp-server"'
    - path: "package.json"
      provides: "Deploy wrapper chains mcp-server BEFORE triage-worker"
      contains: '"deploy": "npm run deploy:mcp && npm run deploy:triage"'
    - path: "package.json"
      provides: "Extended setup chain with setup:queue + completion echo"
      contains: "&& npm run setup:queue &&"
    - path: "scripts/kv-bootstrap.mjs"
      provides: "Discoverability hint pointing at README Step 4"
      contains: "Getting Started"
  key_links:
    - from: "package.json scripts.deploy"
      to: "package.json scripts.deploy:mcp"
      via: "npm script chain"
      pattern: "deploy:mcp && npm run deploy:triage"
    - from: "package.json scripts.predeploy"
      to: "package.json scripts.deploy"
      via: "npm lifecycle hook (fires before literal `deploy` script only — NOT before deploy:mcp / deploy:triage per A4)"
      pattern: '"predeploy": "npm run evals:ci"'
    - from: "package.json scripts.setup"
      to: "package.json scripts.setup:queue"
      via: "chained && invocation appended after setup:vectorize"
      pattern: "setup:vectorize && npm run setup:queue"
---

<objective>
Wire the npm script surface that turns Phase 7's deploy + setup story into one-command UX. Extend `npm run setup` to include `setup:queue` and a completion echo (D-07). Add `npm run deploy` wrapper + per-package `deploy:mcp` / `deploy:triage` for surgical re-deploys (D-08), with the wrapper enforcing the cross-worker DO deploy-order invariant (mcp-server BEFORE triage-worker). Add a one-line discoverability hint to `scripts/kv-bootstrap.mjs --help` pointing at README §Getting Started Step 4 (CONTEXT.md integration points: "may add `--help` banner referenced by the README, but does NOT change the script's argument shape").

Purpose: Phase 7 ships zero new code; this plan establishes the npm-script vocabulary that Plan 02 (README) documents and Plan 04 (deploy execution) invokes. Without this plan, README Step 2 has no `npm run deploy` to reference.

Output: Updated `package.json` (3 new script entries + 1 edited setup entry) and a one-line additive edit to the existing `usage()` banner in `scripts/kv-bootstrap.mjs`. No behavior changes to the kv-bootstrap script itself; argument shape, exit codes, redaction behavior all preserved per PATTERNS §scripts/kv-bootstrap.mjs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deploy-acceptance/07-CONTEXT.md
@.planning/phases/07-deploy-acceptance/07-RESEARCH.md
@.planning/phases/07-deploy-acceptance/07-PATTERNS.md
@package.json
@scripts/kv-bootstrap.mjs
@packages/mcp-server/package.json
@packages/triage-worker/package.json
@packages/triage-worker/wrangler.jsonc

<interfaces>
<!-- Per-package deploy scripts already exist — verified by RESEARCH §"npm scripts already use workspace delegation" and PATTERNS §package.json. -->

packages/mcp-server/package.json:
  scripts.deploy = "wrangler deploy"
  package name = "@engram/mcp-server"

packages/triage-worker/package.json:
  scripts.deploy = "wrangler deploy"
  package name = "@engram/triage-worker"

packages/triage-worker/wrangler.jsonc:20-22:
  "durable_objects.bindings": [
    { "name": "WORKSPACE", "class_name": "WorkspaceDO", "script_name": "engram-mcp-server" }
  ]
  // CROSS-WORKER DO BINDING INVARIANT (RESEARCH §"Cross-Worker DO Deploy Order"):
  // Reverse deploy order = wrangler error.

package.json:33 (existing — DO NOT change):
  "predeploy": "npm run evals:ci"
  // npm fires `pre<X>` ONLY for literal script `X` (RESEARCH Assumption A4 — verified
  // against docs.npmjs.com/cli/v11/using-npm/scripts). So `predeploy` fires before
  // `npm run deploy` but NOT before `npm run deploy:mcp` / `npm run deploy:triage`.
  // This IS the desired D-08 semantic.

scripts/kv-bootstrap.mjs:46-59 (existing usage banner):
  const TAG = "[kv:bootstrap]";
  function usage(stream) { ... }
  // ALREADY handles --help / -h (lines 90-93 + 101-104). Phase 7 adds ONE line for
  // discoverability — does NOT change arg shape / exit codes / redaction behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend root package.json with deploy wrapper + per-package deploys + setup chain (D-07 + D-08)</name>
  <files>package.json</files>
  <read_first>
    - package.json (the whole file — only 52 lines; you need to see existing scripts.setup line 29, scripts.predeploy line 33, scripts.setup:queue line 27, scripts.evals:ci line 32 before editing)
    - .planning/phases/07-deploy-acceptance/07-CONTEXT.md (lines 36-50 — D-07 + D-08 + Claude's Discretion on script invocation shape and final-echo wording)
    - .planning/phases/07-deploy-acceptance/07-PATTERNS.md (lines 133-196 — package.json analog section with current scripts table + Pattern excerpt for Phase 7 additions + "deploy order in wrapper is load-bearing" note)
    - .planning/phases/07-deploy-acceptance/07-RESEARCH.md (lines 230-265 — Pattern 1 + Pattern 2: deploy script chain + cross-worker DO deploy order invariant)
    - packages/mcp-server/package.json (verify the `deploy` script + package name `@engram/mcp-server` match the workspace-delegation form planned below)
    - packages/triage-worker/package.json (verify the `deploy` script + package name `@engram/triage-worker`)
    - packages/triage-worker/wrangler.jsonc (lines 20-22 — confirm `script_name: "engram-mcp-server"` cross-worker DO binding is the source of the deploy-order invariant)
  </read_first>
  <behavior>
    - `npm run setup` chains: install → types:gen → setup:vectorize → setup:queue → echo "Setup complete. Next: npm run deploy ..."
    - `npm run deploy:mcp` invokes `npm run deploy --workspace=@engram/mcp-server` (workspace-delegation form per PATTERNS §"Pattern 1 — wrangler deploy via per-package workspace script")
    - `npm run deploy:triage` invokes `npm run deploy --workspace=@engram/triage-worker` (same form)
    - `npm run deploy` invokes `npm run deploy:mcp && npm run deploy:triage` (load-bearing order per RESEARCH §"Pattern 2 — Cross-worker DO deploy order")
    - Existing `predeploy` hook (line 33) is UNTOUCHED — it fires automatically before `npm run deploy` but not before per-package commands (npm lifecycle semantics RESEARCH A4)
    - All existing scripts (prepare, lint, lint:wrangler, lint:blockconcurrency, kv:bootstrap, format, format:check, typecheck, types:gen, test, dev:mcp, dev:triage, setup:queue, setup:vectorize, evals:vitest, evals:promptfoo, evals:ci, predeploy) preserved verbatim
    - File remains valid JSON (no trailing commas, proper escaping in echo strings)
  </behavior>
  <action>
    Edit `package.json` to:

    1. Replace the existing `setup` script (currently on line 29):
       FROM: `"setup": "node -e \"console.log('Engram setup: see CONTRIBUTING.md for GSD plugin install steps.')\" && npm install && npm run types:gen && npm run setup:vectorize"`
       TO:   `"setup": "node -e \"console.log('Engram setup: see CONTRIBUTING.md for GSD plugin install steps.')\" && npm install && npm run types:gen && npm run setup:vectorize && npm run setup:queue && node -e \"console.log('\\n[OK] Setup complete.\\n  Next:  npm run deploy        # ships both Workers (runs eval gate first)\\n         see README Step 4      # OAuth bootstrap for Claude Desktop\\n')\""`

       (D-07 — adds `&& npm run setup:queue && node -e \"...\"` to the end of the chain. The opening `node -e` about CONTRIBUTING.md stays as-is per PATTERNS §"setup opening node -e snippet should stay as-is".)

    2. Add THREE new script entries after the existing `predeploy` line (currently line 33). Order matters for readability — put per-package commands first, wrapper last:
       ```
       "deploy:mcp": "npm run deploy --workspace=@engram/mcp-server",
       "deploy:triage": "npm run deploy --workspace=@engram/triage-worker",
       "deploy": "npm run deploy:mcp && npm run deploy:triage"
       ```

       (D-08. Workspace-delegation form per RESEARCH §"Primary recommendation". Deploy order in wrapper is load-bearing — mcp-server BEFORE triage-worker per cross-worker DO `script_name` binding in `packages/triage-worker/wrangler.jsonc:20-22`. Reversed order = wrangler error per RESEARCH §"Pitfall 1".)

    3. Do NOT touch any of: `prepare`, `lint`, `lint:wrangler`, `lint:blockconcurrency`, `kv:bootstrap`, `format`, `format:check`, `typecheck`, `types:gen`, `test`, `dev:mcp`, `dev:triage`, `setup:queue`, `setup:vectorize`, `evals:vitest`, `evals:promptfoo`, `evals:ci`, `predeploy`, `devDependencies`, `dependencies`, `workspaces`, `engines`, `name`, `version`, `private`, `type`.

    Use ASCII `[OK]` marker in the completion echo (NOT a unicode checkmark or emoji) per CLAUDE.md global directive "Only use emojis if the user explicitly requests it" and RESEARCH Open Question 2 cross-platform safety.
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); const s = p.scripts; const errors = []; if (s['deploy'] !== 'npm run deploy:mcp && npm run deploy:triage') errors.push('deploy script wrong: ' + s['deploy']); if (s['deploy:mcp'] !== 'npm run deploy --workspace=@engram/mcp-server') errors.push('deploy:mcp wrong: ' + s['deploy:mcp']); if (s['deploy:triage'] !== 'npm run deploy --workspace=@engram/triage-worker') errors.push('deploy:triage wrong: ' + s['deploy:triage']); if (!s['setup'].includes('npm run setup:queue')) errors.push('setup missing setup:queue chain'); if (!s['setup'].includes('[OK] Setup complete')) errors.push('setup missing completion echo'); if (s['predeploy'] !== 'npm run evals:ci') errors.push('predeploy regressed: ' + s['predeploy']); if (errors.length) { console.error(errors.join('\\n')); process.exit(1); } console.log('OK'); "</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` parses as valid JSON (`node -e "require('./package.json')"` exits 0)
    - `scripts.deploy` equals literally `"npm run deploy:mcp && npm run deploy:triage"` (deploy-order invariant — mcp BEFORE triage)
    - `scripts.deploy:mcp` equals literally `"npm run deploy --workspace=@engram/mcp-server"`
    - `scripts.deploy:triage` equals literally `"npm run deploy --workspace=@engram/triage-worker"`
    - `scripts.setup` includes the substring `npm run setup:queue` AFTER `npm run setup:vectorize`
    - `scripts.setup` includes the substring `[OK] Setup complete`
    - `scripts.predeploy` is unchanged (`"npm run evals:ci"`)
    - All other pre-existing scripts (prepare, lint, lint:wrangler, lint:blockconcurrency, kv:bootstrap, format, format:check, typecheck, types:gen, test, dev:mcp, dev:triage, setup:queue, setup:vectorize, evals:vitest, evals:promptfoo) are byte-identical to before
    - `npm run deploy --help` (or `npm run deploy:mcp --dry-run` if supported) shows the wrapper would chain in the documented order
    - No new top-level package.json keys added; only `scripts` entries edited
  </acceptance_criteria>
  <done>The `package.json` ships the D-07 setup extension and the D-08 deploy trio. Plan 02 can reference these script names in the README; Plan 04 can invoke them during deploy execution. Cross-worker DO deploy order is encoded in the `deploy` wrapper.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add discoverability hint to scripts/kv-bootstrap.mjs --help banner</name>
  <files>scripts/kv-bootstrap.mjs</files>
  <read_first>
    - scripts/kv-bootstrap.mjs (the whole file — only ~170 lines; you need to see existing `usage()` function at lines 48-59 AND the --help branch at lines 90-93 + 101-104 AND the redaction logic at lines 121-152 so you understand what NOT to touch)
    - .planning/phases/07-deploy-acceptance/07-CONTEXT.md (lines 113 — Integration Points: "scripts/kv-bootstrap.mjs (POSSIBLE EDIT) — add `--help` banner if needed to make README references discoverable; do NOT change arg shape")
    - .planning/phases/07-deploy-acceptance/07-PATTERNS.md (lines 199-238 — kv-bootstrap.mjs analog section including the explicit ONLY allowed edit and 4 DO NOT rules)
  </read_first>
  <behavior>
    - Running `node scripts/kv-bootstrap.mjs --help` still exits 1 (existing convention — `0 success | 1 missing arg / --help | 2 wrangler subprocess failed`)
    - The usage banner output includes a new `Discoverability:` line referencing README §Getting Started Step 4
    - Existing usage lines (--sub / --workspace-id / --user-id / --local / --dry-run / --help / Exit codes) all preserved with `${TAG}` prefix
    - All other behavior preserved: arg parsing (lines 72-99), required-arg checks (lines 106-119), redaction (lines 121-152), wrangler invocation
  </behavior>
  <action>
    Edit `scripts/kv-bootstrap.mjs` `usage()` function (lines 48-59) to add ONE additional line to the existing banner. The new line follows the same `${TAG}`-prefixed greppable format and points at the root README's Getting Started Step 4 walkthrough:

    Insert the new line BETWEEN the existing `--help:` line (line 56) and the `Exit codes:` line (line 57). The new line:

    ```
    `${TAG} Discoverability: See README.md "Getting Started -> Step 4: First tool call" for the end-to-end bootstrap walkthrough.\n` +
    ```

    Do NOT change:
    - The argument parser (lines 72-99)
    - The required-arg checks for --sub / --workspace-id / --user-id (lines 106-119)
    - The `--local` flag semantics (line 84-86, 131)
    - The `--dry-run` flag semantics (line 87-89)
    - The exit codes (existing `0 success | 1 missing arg / --help | 2 wrangler subprocess failed`)
    - The T-03-KV-LEAK redaction behavior (lines 121-152)
    - The `TAG = "[kv:bootstrap]"` constant
    - The `stream` parameter on `usage(stream)` — preserve the existing single-parameter shape

    Use plain ASCII arrows (`->`) NOT unicode arrows in the discoverability line for cross-platform terminal safety.
  </action>
  <verify>
    <automated>node scripts/kv-bootstrap.mjs --help 2>&1 | grep -c "Discoverability" | grep -q "^1$" && node scripts/kv-bootstrap.mjs --help 2>&1 | grep -q "Getting Started" && node scripts/kv-bootstrap.mjs --help; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/kv-bootstrap.mjs --help` exits with status 1 (preserves existing convention)
    - The help output contains the literal string `Discoverability:` (exactly one occurrence)
    - The help output contains the literal substring `Getting Started`
    - The help output contains the literal substring `Step 4` (so users grepping for the README section name find this script)
    - The help output STILL contains all pre-existing lines: `--sub`, `--workspace-id`, `--user-id`, `--local`, `--dry-run`, `--help`, `Exit codes:`
    - The help output STILL begins with `[kv:bootstrap] usage:` (TAG-prefix pattern preserved)
    - `node scripts/kv-bootstrap.mjs` (no args) still exits 1 with the `ERROR: --sub is required` line (required-arg behavior preserved)
    - `git diff scripts/kv-bootstrap.mjs` shows ONLY an addition inside `usage()` — no deletions, no other-function changes
  </acceptance_criteria>
  <done>The kv-bootstrap CLI's `--help` banner now points users at the README's Step 4 walkthrough. Argument shape, exit codes, and redaction behavior are byte-identical to the pre-Phase-7 script per PATTERNS §"DO NOT change" rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 7 Plan 01 adds NO new trust boundaries. The boundaries already in place (locked by Phases 3-6) are:

| Boundary | Description |
|----------|-------------|
| Cloudflare control plane → Worker code | `wrangler deploy` ships code to edge — auth via `wrangler login` / Cloudflare API token (operator-supplied, not in repo). Locked Phase 3. |
| Operator shell → npm scripts | npm scripts run with operator's shell privileges. No script in this plan elevates or escapes. |
| Operator shell → `scripts/kv-bootstrap.mjs` | Already-vetted CLI (Phase 3 + Phase 3 CR-01 follow-up). T-03-KV-LEAK redaction (lines 121-152) is the relevant defense; this plan does NOT touch that surface. |

## What's NOT Being Added in This Plan

Per Phase 7 security posture (planning_context Security gate): Phase 7's new attack surface is essentially zero. This plan specifically:

- Adds NO new endpoints
- Adds NO new auth surfaces
- Adds NO new data flows
- Adds NO new env vars or secrets
- Adds NO new external service integrations
- Adds NO new package installs (zero new deps — verified RESEARCH §"Standard Stack")
- Touches NO Worker code (only root `package.json` scripts + one CLI banner line)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-DO | Denial of Service | `npm run deploy` wrapper | accept | If predeploy eval gate (`evals:ci`) infinite-loops or hangs, operator Ctrl+C stops the chain before any `wrangler deploy` fires. Phase 5's eval suite has a finite timeout enforced by vitest config. Risk is operator-time, not infrastructure. |
| T-07-01-OR | Tampering | `npm run deploy` order (mcp BEFORE triage) | mitigate | The wrapper hardcodes `deploy:mcp && npm run deploy:triage` (Task 1 acceptance criterion #2). Reverse order = wrangler error (RESEARCH §Pattern 2 + Pitfall 1). The README in Plan 02 also documents the precondition for surgical `deploy:triage` use. |
| T-07-01-LEAK | Information Disclosure | `setup` completion echo | accept | Echo is plain ASCII text with no env-var interpolation, no secrets, no workspace identifiers. Renders identically on every operator's terminal. |
| T-07-01-SC | Tampering | npm/wrangler install chain | accept | Phase 7 installs zero new packages (RESEARCH §"Package Legitimacy Audit"). All packages referenced (`wrangler`, `mcp-remote`, `@modelcontextprotocol/inspector`) were vetted Phase 1/3 + re-verified via slopcheck 2026-05-29 (RESEARCH §"Package Legitimacy Audit" table). No package-legitimacy checkpoint required in this plan. |
| T-07-01-KV | Information Disclosure | kv-bootstrap discoverability line | accept | The new `Discoverability:` line is a static string referencing a README section name. No KV value, no `sub`, no workspace_id is interpolated. T-03-KV-LEAK redaction logic (lines 121-152) is preserved unchanged. |
</threat_model>

<verification>
## Plan-Level Verification

After both tasks complete:

1. `node -e "JSON.parse(require('fs').readFileSync('package.json'))"` exits 0 (file is valid JSON)
2. `npm run` lists `deploy`, `deploy:mcp`, `deploy:triage` among available scripts (verifies they're parsed correctly)
3. `node scripts/kv-bootstrap.mjs --help 2>&1 | grep -E "(Discoverability|Step 4|Getting Started)" | wc -l` returns ≥3 (all three discoverability anchors present)
4. `git diff --stat package.json scripts/kv-bootstrap.mjs` shows changes to exactly these 2 files (no scope creep)
5. `npm run typecheck` passes (sanity — no accidental syntax break)
6. `npm run lint` passes (sanity — eslint doesn't complain about package.json shape)

NOTE: Do NOT run `npm run deploy` or `npm run setup` as part of plan verification — those execute live infrastructure changes (Plan 04 territory). This plan's verification is static analysis only.
</verification>

<success_criteria>
1. `package.json` contains exactly the 3 new deploy script entries with the documented values
2. `package.json` `setup` script chain includes `setup:queue` and the `[OK] Setup complete` echo
3. `scripts/kv-bootstrap.mjs --help` output contains a `Discoverability:` line referencing README Step 4
4. All pre-existing npm scripts, kv-bootstrap arg parsing, and kv-bootstrap redaction logic preserved byte-identically
5. No new dependencies added
6. Deploy order invariant (mcp-server BEFORE triage-worker) is encoded in the `deploy` wrapper script
</success_criteria>

<output>
Create `.planning/phases/07-deploy-acceptance/07-01-SUMMARY.md` when done. The summary should:
- Confirm the exact final values of `scripts.deploy`, `scripts.deploy:mcp`, `scripts.deploy:triage`, `scripts.setup`
- Note that `predeploy` was intentionally untouched (npm lifecycle semantics give D-08 eval-gate-then-deploy posture for free)
- Note that kv-bootstrap arg shape / exit codes / redaction are preserved
- Reference Plan 02 as the consumer of the new script names
- Include the inline cf-code-assist routing log entry for each task (expected: both tasks Q2=N small wrapper-script edits, route = Claude per `~/.claude/CLAUDE.md` 3-question checklist)
</output>
