---
phase: 07-deploy-acceptance
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - README.md
autonomous: true
requirements: [DEP-02, DEP-05]
tags: [readme, docs, deploy, oauth-bootstrap, troubleshooting]

must_haves:
  truths:
    - "A first-time deployer (Russell now, Devon eventually) can follow root README from clean clone to working Claude Desktop integration without consulting any other artifact (D-02)"
    - "Root README §Getting Started has explicit numbered steps 1-4 (Install → Deploy → Configure Claude Desktop → First tool call OAuth bootstrap), per PATTERNS Q1 answer"
    - "Root README §Deploy section documents `npm run deploy` / `deploy:mcp` / `deploy:triage` with the deploy-order precondition for surgical `deploy:triage` use (D-08 + cross-worker DO invariant)"
    - "Root README §Getting Started Step 4 quotes the literal 403 body `Unknown OAuth subject: ${sub}. Bootstrap via npm run kv:bootstrap.` verbatim so users grepping the error find the walkthrough (PATTERNS §'Literal-string preservation')"
    - "Root README §Troubleshooting covers OTHER errors (KV ID placeholders, stale mcp-auth JWT, close-vs-quit Claude Desktop, eval-gate flake) but NOT the bootstrap-403 walkthrough — that lives INLINE in Step 4 per D-02"
    - "Claude Desktop config snippet includes the dated `// tested with mcp-remote@0.1.38 on Claude Desktop <ver> 2026-05-29` comment as the rollback marker per CONTEXT.md Claude's Discretion + RESEARCH §'mcp-remote pinning'"
  artifacts:
    - path: "README.md"
      provides: "User-facing onboarding from clone to working Claude Desktop integration"
      contains: "### 4. First tool call"
      min_lines: 540
    - path: "README.md"
      provides: "Deploy section with three commands documented"
      contains: "npm run deploy"
    - path: "README.md"
      provides: "Literal OAuth 403 body verbatim quote"
      contains: "Unknown OAuth subject"
    - path: "README.md"
      provides: "Deploy order precondition for surgical deploy:triage"
      contains: "engram-mcp-server"
    - path: "README.md"
      provides: "mcp-remote version rollback marker"
      contains: "mcp-remote@0.1.38"
  key_links:
    - from: "README.md §Getting Started Step 4"
      to: "packages/mcp-server/src/oauth.ts:201 (literal 403 body)"
      via: "verbatim string quote"
      pattern: "Unknown OAuth subject"
    - from: "README.md §Getting Started Step 4"
      to: "scripts/kv-bootstrap.mjs (existing CLI)"
      via: "npm run kv:bootstrap -- --sub ... --workspace-id ... --user-id ..."
      pattern: "npm run kv:bootstrap"
    - from: "README.md §Deploy"
      to: "package.json scripts.deploy (Plan 01 output)"
      via: "command references"
      pattern: "npm run deploy"
    - from: "README.md §Reference"
      to: "packages/mcp-server/README.md §First-Time Setup"
      via: "down-link for full KV namespace creation procedure"
      pattern: "packages/mcp-server/README.md"
---

<objective>
Hoist the user-facing 80% of `packages/mcp-server/README.md`'s Phase 3 setup content into the root README.md so a first-time deployer (Russell now, Devon eventually) can follow ONE document from clean clone to working Claude Desktop integration. Per CONTEXT.md D-02, the OAuth bootstrap flow lives INLINE in Getting Started as Step 4 — NOT in Troubleshooting. Per D-08 + PATTERNS Q1 answer, restructure Getting Started to use explicit numbered H3s (1. Install → 2. Deploy → 3. Configure Claude Desktop → 4. First tool call) AND add a new H2 `## Deploy` section between Getting Started and Tool Surface (v0.1) for the full deploy reference. Refresh Troubleshooting with P1-P6 observed errors (planner-triaged subset per PATTERNS Q3).

Purpose: Phase 3 over-delivered `packages/mcp-server/README.md` (448 lines). Phase 7 work is hoist + reorder + cross-link, NOT from-scratch authoring. The README delta is ~100-150 lines per CONTEXT.md Claude's Discretion — scoped realistically, not budgeting for a full README rewrite.

Output: Updated root `README.md` (~540-590 lines from the current 443). New sections:
1. `### 1. Install` (renamed from `### Install and run`)
2. `### 2. Deploy` (short H3 narrative pointing at H2 reference)
3. `### 3. Configure Claude Desktop` (new — hoist from packages/mcp-server/README.md:173-217)
4. `### 4. First tool call (the OAuth bootstrap)` (new — hoist from packages/mcp-server/README.md:110-169 with tone shift)
5. `## Deploy` (new H2 between Getting Started and Tool Surface — full deploy reference per D-08)
6. `## Troubleshooting` (new — P1-P6 observed errors per PATTERNS Q3 triage; explicitly EXCLUDES the bootstrap-403 entry which lives inline in Step 4)
7. `## Reference` (new — down-links to per-package READMEs + sideways link to CLAUDE.md per PATTERNS §"README cross-linking depth")

The existing `## Tool Surface (v0.1)` section through `## Architecture Deep Dive` and `## License` remain BYTE-IDENTICAL — Phase 7 does not touch them.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deploy-acceptance/07-CONTEXT.md
@.planning/phases/07-deploy-acceptance/07-RESEARCH.md
@.planning/phases/07-deploy-acceptance/07-PATTERNS.md
@.planning/phases/07-01-SUMMARY.md
@README.md
@packages/mcp-server/README.md
@packages/mcp-server/src/oauth.ts
@packages/triage-worker/wrangler.jsonc
@CLAUDE.md

<interfaces>
<!-- Phase 3 packages/mcp-server/README.md sections to HOIST (per PATTERNS §"Phase 7 root README delta plan"). -->

packages/mcp-server/README.md:110-169 → root README §"### 4. First tool call":
  Source: "### Bootstrap the identity record" — 2-step flow walkthrough
  Adaptations: tone shift ("you just installed, here's what you're about to see") vs source's
  developer-iteration tone; preserve the literal `Unknown OAuth subject: ${sub}. Bootstrap via
  npm run kv:bootstrap.` quote (verbatim from packages/mcp-server/src/oauth.ts:201).

packages/mcp-server/README.md:173-217 → root README §"### 3. Configure Claude Desktop":
  Source: "## Claude Desktop Configuration" — production + local snippets + path locations.
  Adaptations: Reuse Production snippet (line 184-193). Add dated rollback-marker comment
  `// tested with mcp-remote@0.1.38 on Claude Desktop <ver> 2026-05-29`. Drop Local development
  snippet (per-package README still owns it; root README is user-onboarding-focused).

packages/mcp-server/README.md:341-422 → root README §"## Troubleshooting" (NEW H2):
  Source: 6 existing per-package Troubleshooting entries.
  Triage per PATTERNS Q2 + Q3:
    KEEP (general operator concerns):
      - `wrangler deploy` fails with "class not declared in any migration" (FND-08 lint rule reminder)
      - `npm install` fails with engine constraint complaints (lint-staged@17 engines)
    DROP (Inspector-internal, per-package README owns them):
      - MCP Inspector OAuth resource mismatch
      - Inspector "Stream closed" mid-session
    PROMOTE (PATTERNS Q2 candidate set — sourced from RESEARCH §"Common Pitfalls"):
      - KV namespace IDs are still placeholders (Pitfall 2)
      - Cached ~/.mcp-auth/ JWT (Pitfall 3)
      - Closed-vs-quit Claude Desktop (Pitfall 4)
      - Eval gate flaked on deploy (Pitfall 6)
    EXCLUDE explicitly:
      - 403 "Unknown OAuth subject" (per D-02 lives INLINE in Step 4, NOT here)

packages/mcp-server/src/oauth.ts:201 (verified verbatim — DO NOT change this string in oauth.ts):
  return new Response(`Unknown OAuth subject: ${sub}. Bootstrap via npm run kv:bootstrap.`, { status: 403 });

packages/triage-worker/wrangler.jsonc:20-22 (the deploy-order invariant source):
  "durable_objects.bindings": [
    { "name": "WORKSPACE", "class_name": "WorkspaceDO", "script_name": "engram-mcp-server" }
  ]

Existing root README structure (verified — file is 443 lines per PATTERNS §README table):
  Line 13:  ## Why Engram         (KEEP)
  Line 25:  ## Architecture       (KEEP — mermaid diagram)
  Line 79:  ## Tech Stack         (KEEP)
  Line 95:  ## Status             (UPDATE — "Foundation scaffolding (Phase 1) complete. Working on
                                   WorkspaceDO + SQLite schema (Phase 2)." is stale; bump to current)
  Line 105: ## Getting Started    (RESTRUCTURE)
  Line 107: ### Prerequisites     (KEEP as preamble before "### 1. Install")
  Line 112: ### Install and run   (RENAME to "### 1. Install" — content adapted, deploy refs removed)
  Line 132: ## Tool Surface (v0.1) (KEEP — NEW H2 "## Deploy" + "## Troubleshooting" + "## Reference"
                                    insert BEFORE this)
  Line 132+: ... through line 443 ... (KEEP BYTE-IDENTICAL — Tool Surface, Token budget, Error
                                       semantics, Source of truth, Architecture Deep Dive, License)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restructure root README §Getting Started into numbered Steps 1-4 (D-02 + PATTERNS Q1)</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 95-132 — the Status + Getting Started region you're restructuring)
    - packages/mcp-server/README.md (lines 110-217 — the source content you're hoisting for Steps 3 + 4)
    - packages/mcp-server/src/oauth.ts (lines 190-210 — verify the EXACT 403 body string is still `Unknown OAuth subject: ${sub}. Bootstrap via npm run kv:bootstrap.`)
    - .planning/phases/07-deploy-acceptance/07-CONTEXT.md (lines 19-21 — D-01 + D-02 OAuth bootstrap UX, and lines 38-39 — D-07 + D-08 setup/deploy automation)
    - .planning/phases/07-deploy-acceptance/07-PATTERNS.md (lines 68-129 — Pattern 1 hoist Step 4 + Pattern 2 insert Deploy section + Pattern 3 Troubleshooting; AND lines 434-468 — Q1 + Q2 explicit answers on Getting Started numbering + Troubleshooting structure mirror)
    - .planning/phases/07-deploy-acceptance/07-RESEARCH.md (lines 267-289 — Pattern 3 claude_desktop_config.json snippet with dated rollback marker)
    - .planning/phases/07-01-SUMMARY.md (verify the npm script names match what you're about to reference: `npm run deploy`, `npm run deploy:mcp`, `npm run deploy:triage`, `npm run setup`, `npm run kv:bootstrap`)
  </read_first>
  <behavior>
    - `## Status` section bumped from "Foundation (Phase 1) complete, working on Phase 2" to reflect current v0.1 state (Phases 1-6 complete, Phase 7 in progress)
    - `## Getting Started` retains `### Prerequisites` unchanged as preamble
    - `### Install and run` (line 112 current) renamed to `### 1. Install` with deploy lines removed (deploy goes to its own Step 2)
    - NEW `### 2. Deploy` short H3 — 3-4 lines pointing readers to the `## Deploy` H2 below for full reference; this H3 is the linear walkthrough placeholder per PATTERNS Q1 reconciliation
    - NEW `### 3. Configure Claude Desktop` — Production snippet from packages/mcp-server/README.md:182-200 PLUS dated rollback-marker comment per RESEARCH §"mcp-remote pinning"; mentions config file location per OS (Mac/Linux/Windows) per packages/mcp-server/README.md:178-180; explicit "fully quit Claude Desktop (Cmd+Q on Mac), not just close the window" instruction per RESEARCH §Pitfall 4
    - NEW `### 4. First tool call (the OAuth bootstrap)` — frames the 403 as a normal first-run step per D-02; quotes literal 403 body verbatim (`Unknown OAuth subject: <some-long-string>. Bootstrap via npm run kv:bootstrap.`); shows `npm run kv:bootstrap -- --sub <some-long-string> --workspace-id <pick-an-identifier> --user-id <pick-an-identifier>`; reminds restart-not-close per Pitfall 4
    - All snippets use ASCII placeholders like `<your-subdomain>`, `<some-long-string>`, `<pick-an-identifier>` (NOT real values) per CONTEXT.md Security gate + RESEARCH §"Known Threats Pattern 3"
    - NO emojis used (per CLAUDE.md global directive); ASCII markers only
  </behavior>
  <action>
    Restructure the root `README.md` Getting Started region (currently lines 95-132). Do this as a single coherent edit so the section boundaries align cleanly.

    **Step A — Update `## Status` (currently lines 95-101):**

    Replace the stale Phase 2 reference with current state. New content:
    ```markdown
    ## Status

    **Current milestone:** v0.1 — MCP Foundation (in progress, target 2026-06-07)

    Phases 1-6 complete (Foundation → WorkspaceDO + SQLite → MCP Server Scaffold → Core Tools + Envelope → AI Integration → Async Pipeline). Phase 7 (Deploy + Acceptance) is the active phase.

    See [.planning/ROADMAP.md](.planning/ROADMAP.md) for the full milestone arc.
    ```

    **Step B — Restructure `## Getting Started`:**

    Keep `### Prerequisites` (currently line 107-110) BYTE-IDENTICAL as an unnumbered preamble. Then rename `### Install and run` (line 112) to `### 1. Install` and STRIP the `npm run dev:mcp` / `npm run dev:triage` lines (those are dev-loop concerns; dev is documented in per-package README — root README is deploy-focused). The new `### 1. Install` body:

    ```markdown
    ### 1. Install

    ```bash
    # Clone the repo
    gh repo clone russellkmoore/engram
    cd engram

    # One-command bootstrap: installs deps, generates types, provisions
    # Vectorize index + Queue (idempotent — safe to re-run).
    npm run setup

    # Verify the monorepo is healthy
    npm run typecheck && npm run lint && npm run lint:wrangler
    ```

    `npm run setup` chains `npm install` + `npm run types:gen` + `npm run setup:vectorize` + `npm run setup:queue` + a completion echo. The setup scripts skip if their target already exists, so re-running on an established environment is a no-op.
    ```

    Insert AFTER `### 1. Install`:

    **Step C — Add `### 2. Deploy` (short H3 — points at H2 below):**

    ```markdown
    ### 2. Deploy

    ```bash
    npm run deploy
    ```

    Ships both Workers to your Cloudflare account. The `predeploy` hook runs the eval gate (`npm run evals:ci`) first; on green, mcp-server deploys, then triage-worker. See [Deploy](#deploy) below for the full reference (surgical re-deploys, eval-gate failure handling, deploy-order invariant).
    ```

    **Step D — Add `### 3. Configure Claude Desktop`:**

    Hoist from `packages/mcp-server/README.md` lines 173-200 (Production snippet only — drop the Local development snippet; root README is user-onboarding-focused, not dev-loop). Add the dated rollback-marker comment per RESEARCH lines 282 and CONTEXT.md Claude's Discretion. New content:

    ```markdown
    ### 3. Configure Claude Desktop

    Claude Desktop reads `claude_desktop_config.json` to learn about MCP servers. The file lives at:

    - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
    - **Linux:** `~/.config/Claude/claude_desktop_config.json`
    - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`

    Add Engram to the `mcpServers` map:

    ```json
    {
      "mcpServers": {
        "engram": {
          "command": "npx",
          "args": ["-y", "mcp-remote", "https://engram-mcp-server.<your-subdomain>.workers.dev/mcp"]
        }
      }
    }
    ```

    <!-- tested with mcp-remote@0.1.38 on Claude Desktop 2026-05-29 -->

    Replace `<your-subdomain>` with your Cloudflare account's `workers.dev` subdomain (visible in the Cloudflare dashboard under "Workers & Pages"). The `-y` flag auto-accepts the `npx` install prompt that Claude Desktop's subprocess context cannot answer.

    **Fully quit Claude Desktop** (Cmd+Q on macOS, right-click tray icon → Exit on Windows) and re-launch — Claude Desktop only reads `claude_desktop_config.json` at process start, and closing the window is not enough on macOS.
    ```

    **Step E — Add `### 4. First tool call (the OAuth bootstrap)`:**

    Hoist + adapt from `packages/mcp-server/README.md` lines 110-169. Tone shift to "you just installed, here's what you're about to see" per D-02 framing. The literal 403 body MUST appear verbatim per PATTERNS §"Literal-string preservation":

    ```markdown
    ### 4. First tool call (the OAuth bootstrap)

    The first time you ask Claude to use any Engram tool (e.g. "Engram, remember that I'm preparing for an interview"), `mcp-remote` opens a browser tab to your Worker's `/authorize` endpoint. Because `ENGRAM_IDENTITIES` (the KV that maps your OAuth subject to a workspace) is empty on a fresh install, you will see a 403 response with this body:

    ```text
    Unknown OAuth subject: <some-long-string>. Bootstrap via npm run kv:bootstrap.
    ```

    **This is expected — it's the bootstrap signal, not a bug.** Copy the `<some-long-string>` value (it's the OAuth `sub` claim from `mcp-remote`'s dynamic registration; safe to log — it is not a secret). Then from the engram repo root:

    ```bash
    npm run kv:bootstrap -- \
      --sub <some-long-string> \
      --workspace-id <pick-an-identifier-for-your-workspace> \
      --user-id <pick-an-identifier-for-yourself>
    ```

    `--workspace-id` and `--user-id` are arbitrary — pick whatever identifiers you want; they become the `workspace_id` and `user_id` claims in every subsequent JWT. The script writes the identity to the production KV namespace via `wrangler kv key put`.

    **Fully quit Claude Desktop and re-launch.** On the next tool call, the OAuth flow succeeds, `mcp-remote` caches the resulting JWT in `~/.mcp-auth/`, and every subsequent tool call is one authenticated JSON-RPC request to `/mcp`.

    For the full bootstrap rationale (why manual, why not auto), see [`packages/mcp-server/README.md` §"Bootstrap the identity record"](./packages/mcp-server/README.md#bootstrap-the-identity-record).
    ```

    **Step F — Verify NO other content is touched:**

    The `## Tool Surface (v0.1)` H2 (currently line 132) and everything below it through `## License` (line 441-443) remain BYTE-IDENTICAL. Do not edit, reorder, or normalize.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const md = fs.readFileSync('README.md', 'utf8'); const checks = []; if (!md.includes('### 1. Install')) checks.push('missing Step 1 heading'); if (!md.includes('### 2. Deploy')) checks.push('missing Step 2 heading'); if (!md.includes('### 3. Configure Claude Desktop')) checks.push('missing Step 3 heading'); if (!md.includes('### 4. First tool call')) checks.push('missing Step 4 heading'); if (!md.includes('Unknown OAuth subject: <some-long-string>. Bootstrap via npm run kv:bootstrap.')) checks.push('Step 4 missing literal 403 body verbatim'); if (!md.includes('mcp-remote@0.1.38')) checks.push('Step 3 missing dated rollback-marker comment'); if (!md.includes('Fully quit Claude Desktop')) checks.push('Pitfall 4 instruction missing'); if (!md.includes('npm run setup')) checks.push('Step 1 missing npm run setup reference'); if (!md.includes('Phases 1-6 complete')) checks.push('Status section not updated'); if (!md.includes('## Tool Surface (v0.1)')) checks.push('Tool Surface H2 missing — DID YOU ACCIDENTALLY DELETE IT?'); if (!md.includes('## License')) checks.push('License section missing — DID YOU ACCIDENTALLY DELETE IT?'); if (checks.length) { console.error(checks.join('\\n')); process.exit(1); } console.log('OK'); "</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains exactly these four H3 step headings in this order, each on its own line: `### 1. Install`, `### 2. Deploy`, `### 3. Configure Claude Desktop`, `### 4. First tool call (the OAuth bootstrap)`
    - Step 4 contains the literal string `Unknown OAuth subject: <some-long-string>. Bootstrap via npm run kv:bootstrap.` exactly once (verbatim from oauth.ts:201 with the runtime `${sub}` interpolation replaced by `<some-long-string>` placeholder)
    - Step 3 contains the literal string `mcp-remote@0.1.38` (the dated rollback marker)
    - Step 3 contains the literal string `Fully quit Claude Desktop` (RESEARCH §Pitfall 4 mitigation)
    - Step 4 contains the literal string `Fully quit Claude Desktop` (repeated for the restart-after-bootstrap step)
    - Step 1 references `npm run setup` (the chained Plan 01 script) NOT raw `npm install`
    - `## Status` section no longer references "Foundation scaffolding (Phase 1) complete. Working on Phase 2"
    - `## Tool Surface (v0.1)` H2 still exists at its original location with byte-identical content (NOT accidentally deleted during restructure)
    - `## License` H2 still exists at its original location with byte-identical content
    - No emojis anywhere in the new sections (per CLAUDE.md global directive)
    - All snippets use ASCII placeholders (`<your-subdomain>`, `<some-long-string>`, `<pick-an-identifier-for-your-workspace>`, `<pick-an-identifier-for-yourself>`) — NO real OAuth `sub` values, NO real workspace IDs, NO real Cloudflare subdomain
    - Step 2's H3 narrative is short (3-5 lines) and cross-links to `[Deploy](#deploy)` H2 below (Task 2)
  </acceptance_criteria>
  <done>Root README Getting Started is the linear walkthrough Russell + Devon follow from clone to working Claude Desktop integration. The literal 403 body is grep-discoverable. The mcp-remote rollback marker is in place. All non-Getting-Started sections preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add new H2 sections — ## Deploy, ## Troubleshooting, ## Reference — between Getting Started and Tool Surface</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 125-135 — the boundary where new H2 sections insert, AFTER Step 4 finishes and BEFORE `## Tool Surface (v0.1)`)
    - packages/mcp-server/README.md (lines 341-422 — Troubleshooting source content to triage)
    - packages/mcp-server/README.md (lines 424-447 — Architecture Reference section showing the down-link pattern adapted from per-package to root)
    - packages/triage-worker/wrangler.jsonc (lines 20-22 — the source of the deploy-order invariant you'll document under `npm run deploy:triage` precondition)
    - .planning/phases/07-deploy-acceptance/07-CONTEXT.md (lines 39 — D-08 + Claude's Discretion on eval-gate failure UX)
    - .planning/phases/07-deploy-acceptance/07-RESEARCH.md (lines 333-380 — Common Pitfalls 1-8 with the user-facing subset for Troubleshooting)
    - .planning/phases/07-deploy-acceptance/07-PATTERNS.md (lines 86-130 — Pattern 2 Deploy section content + Pattern 3 Troubleshooting structure + lines 382-405 — README cross-linking depth Reference section)
    - .planning/phases/07-01-SUMMARY.md (verify the npm script names you reference exist as documented)
  </read_first>
  <behavior>
    - NEW `## Deploy` H2 inserted between the end of `## Getting Started` (after Step 4) and `## Tool Surface (v0.1)` (currently line 132)
    - `## Deploy` documents all three scripts: `npm run deploy` (full eval-gate-then-deploy chain), `npm run deploy:mcp` (per-package, skips eval gate), `npm run deploy:triage` (per-package, skips eval gate, REQUIRES mcp-server to have been deployed at least once)
    - Cross-worker DO binding precondition is explicit under `deploy:triage`
    - Eval-gate failure UX documented per CONTEXT.md Claude's Discretion ("re-run once; if it fails twice, investigate via `npm run evals:ci` directly")
    - NEW `## Troubleshooting` H2 inserted after `## Deploy` and before `## Tool Surface (v0.1)`
    - Troubleshooting includes exactly 6 entries: (a) `wrangler deploy` "class not declared in any migration" (FND-08 reminder, KEEP from per-package README:343-359), (b) `npm install` engine constraint complaints (KEEP from per-package README:403-413), (c) KV namespace IDs are placeholders (PROMOTE from RESEARCH §Pitfall 2), (d) Cached ~/.mcp-auth/ JWT (PROMOTE from RESEARCH §Pitfall 3), (e) Closed-vs-quit Claude Desktop (PROMOTE from RESEARCH §Pitfall 4), (f) Eval gate flaked on deploy (PROMOTE from RESEARCH §Pitfall 6)
    - Troubleshooting explicitly EXCLUDES the 403 "Unknown OAuth subject" entry (lives INLINE in Step 4 per D-02 — duplicating here would split source-of-truth)
    - NEW `## Reference` H2 inserted after `## Troubleshooting` and before `## Tool Surface (v0.1)`
    - Reference links DOWN to `packages/mcp-server/README.md` sections (First-Time Setup, Smoke Test) and SIDEWAYS to `CLAUDE.md` (architecture rationale); does NOT cross-link into `.planning/` (internal GSD surface) per PATTERNS §"README cross-linking depth"
  </behavior>
  <action>
    Insert three new H2 sections into `README.md` between the end of `## Getting Started` Step 4 (added in Task 1) and `## Tool Surface (v0.1)` (currently line 132 — will shift to ~line 220+ after all insertions). Insert them in this order with `---` horizontal-rule separators matching the existing README convention (verified — the existing README uses `---` between top-level sections, e.g. line 11, 23, 77, 93, 103, 130).

    **Insertion order:**
    1. `---` separator
    2. `## Deploy`
    3. `---` separator
    4. `## Troubleshooting`
    5. `---` separator
    6. `## Reference`
    7. `---` separator (which precedes the existing `## Tool Surface (v0.1)` — verify the existing `---` separator above Tool Surface is still in place; if not, add it)

    **Section A — `## Deploy`:**

    ```markdown
    ## Deploy

    Engram ships two Workers — `engram-mcp-server` (MCP host + OAuth + WorkspaceDO) and `engram-triage-worker` (Queue consumer for async enrichment). Three commands cover the day-1 happy path and day-N surgical re-deploys.

    ### `npm run deploy` (the full chain)

    ```bash
    npm run deploy
    ```

    Runs in this order:

    1. **`predeploy` hook fires `npm run evals:ci`** — vitest evals + promptfoo against Workers AI. If any assertion fails, the deploy aborts before any `wrangler deploy` runs.
    2. **`npm run deploy:mcp`** — `wrangler deploy` for `packages/mcp-server`.
    3. **`npm run deploy:triage`** — `wrangler deploy` for `packages/triage-worker`.

    On green, both Workers are live at `https://engram-mcp-server.<your-subdomain>.workers.dev` and `https://engram-triage-worker.<your-subdomain>.workers.dev`.

    ### `npm run deploy:mcp` / `npm run deploy:triage` (per-package, surgical)

    ```bash
    npm run deploy:mcp      # rebuild + ship just the MCP server
    npm run deploy:triage   # rebuild + ship just the triage worker
    ```

    These commands **skip the eval gate** — they exist for day-N "I know exactly what changed, evals passed last deploy, just push the fix" workflows. Use them after small surgical fixes; use the full `npm run deploy` whenever you want confidence the change hasn't regressed evals.

    **Important precondition for `deploy:triage`:** `engram-mcp-server` must have been deployed at least once. The triage Worker's `wrangler.jsonc` binds `WORKSPACE` to `WorkspaceDO` via `script_name: "engram-mcp-server"` (a cross-Worker Durable Object binding). If you deploy `triage-worker` first on a fresh Cloudflare account, `wrangler` rejects the binding with `Could not find a Worker with the name "engram-mcp-server"`. The `npm run deploy` wrapper enforces the correct order automatically — only worry about this if you're invoking `deploy:triage` directly.

    ### Eval-gate failure handling

    If `npm run deploy` aborts at the `predeploy` step, the failure surface is the `evals:ci` output (vitest assertions + promptfoo pass-rate). LLM evals against Workers AI have inherent variance, so:

    1. **Re-run once** — many failures are transient.
    2. If it fails twice, run `npm run evals:ci` directly to see which assertion failed, and fix the regression before re-running `npm run deploy`.
    3. For a surgical re-deploy after a code fix that you know is unrelated to the eval-gate failure (e.g. a typo in a comment), use `npm run deploy:mcp` or `npm run deploy:triage` to skip the gate. Don't make this a habit — the gate exists to catch real regressions.
    ```

    **Section B — `## Troubleshooting`:**

    Mirror the per-package README's Troubleshooting structure (H3 per error, body explaining cause, optional code block for fix). Six entries:

    ```markdown
    ## Troubleshooting

    ### `wrangler deploy` fails with "class not declared in any migration"

    A `wrangler.jsonc` `migrations` entry was likely renamed or deleted. Both Worker `wrangler.jsonc` files must declare their Durable Object classes under `new_sqlite_classes` (NOT `new_classes` — SQLite-backed DOs are irreversible). Run `npm run lint:wrangler` from the repo root; it fails loudly if any `wrangler.jsonc` regresses to `new_classes`. See [`packages/mcp-server/README.md` §Troubleshooting](./packages/mcp-server/README.md#troubleshooting) for the full migration entry shape.

    ### `npm install` fails with engine constraint complaints

    `lint-staged@17` declares `node >=22.22.1` while the repo allows 22+. Use:

    ```bash
    npm install --engine-strict=false
    ```

    Pre-existing condition tracked outside this README; does not affect the Worker runtime.

    ### KV namespace IDs in `wrangler.jsonc` point at someone else's account

    `packages/mcp-server/wrangler.jsonc` commits real KV namespace IDs for `OAUTH_KV` and `ENGRAM_IDENTITIES`. These are not secrets, but they ARE account-specific — a fresh Cloudflare account has different IDs. `wrangler deploy` does NOT fail on bad KV IDs at deploy time; the first `/authorize` request returns a 500 instead of the expected 403. See [`packages/mcp-server/README.md` §Create KV namespaces](./packages/mcp-server/README.md#create-kv-namespaces) for the procedure: `npx wrangler kv namespace create OAUTH_KV` (and `ENGRAM_IDENTITIES`), then paste the new IDs into `wrangler.jsonc`.

    ### Claude Desktop ignores changes to `claude_desktop_config.json`

    Closing the Claude Desktop window does NOT reload the config on macOS — the app stays running in the background. **Fully quit** (Cmd+Q on macOS, right-click tray icon → Exit on Windows) and re-launch. Claude Desktop only reads `claude_desktop_config.json` at process start.

    ### Tool calls fail with stale workspace_id after re-bootstrap

    `mcp-remote` caches JWTs in `~/.mcp-auth/`. If you re-ran `npm run kv:bootstrap` with a new `--workspace-id`, the cached JWT still encodes the old workspace. Clear the cache:

    ```bash
    rm -rf ~/.mcp-auth/
    ```

    Then fully quit + re-launch Claude Desktop to trigger a fresh OAuth flow.

    ### `npm run deploy` aborts at the eval gate

    The `predeploy` hook runs `npm run evals:ci` (vitest + promptfoo). LLM evals have inherent variance; re-run `npm run deploy` once. If it fails twice, run `npm run evals:ci` directly to see the specific assertion failure, and fix the regression before re-deploying. For a surgical re-deploy after an unrelated code fix, use `npm run deploy:mcp` or `npm run deploy:triage` to skip the gate.
    ```

    **Section C — `## Reference`:**

    Down-links to per-package READMEs + sideways to CLAUDE.md per PATTERNS §"README cross-linking depth":

    ```markdown
    ## Reference

    - [`packages/mcp-server/README.md`](./packages/mcp-server/README.md) §"First-Time Setup" — full KV namespace creation procedure for a fresh Cloudflare account.
    - [`packages/mcp-server/README.md`](./packages/mcp-server/README.md) §"Smoke Test: MCP Inspector" — pre-OAuth Worker liveness check using `npx @modelcontextprotocol/inspector`.
    - [`packages/mcp-server/README.md`](./packages/mcp-server/README.md) §"OAuth Flow (under the hood)" — sequence diagram of the OAuth dance, including which library owns which endpoint.
    - [`CLAUDE.md`](./CLAUDE.md) §"MCP Tool Surface" — design rationale for the 5-tool v0.1 surface.
    - [`CLAUDE.md`](./CLAUDE.md) §"Architecture" — Durable Object topology and per-workspace SQLite isolation.
    - [`CLAUDE.md`](./CLAUDE.md) §"Tech Stack" — Cloudflare bindings the deployed Workers consume (KV, Vectorize, Queues, AI, Analytics Engine).
    ```

    Do NOT link from root README into `.planning/` — internal GSD surface per PATTERNS §"README cross-linking depth".
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const md = fs.readFileSync('README.md', 'utf8'); const checks = []; if (!md.includes('## Deploy')) checks.push('## Deploy H2 missing'); if (!md.includes('## Troubleshooting')) checks.push('## Troubleshooting H2 missing'); if (!md.includes('## Reference')) checks.push('## Reference H2 missing'); if (!md.includes('engram-mcp-server')) checks.push('Deploy section missing deploy-order precondition reference to engram-mcp-server'); if (!md.includes('Could not find a Worker')) checks.push('Deploy section missing wrangler error message text'); if (!md.includes('rm -rf ~/.mcp-auth/')) checks.push('Troubleshooting missing JWT cache clear command'); if (!md.includes('--engine-strict=false')) checks.push('Troubleshooting missing engine-strict fallback'); if (!md.includes('lint:wrangler')) checks.push('Troubleshooting missing FND-08 lint reference'); if (md.split('## Deploy').length !== 2) checks.push('## Deploy heading appears more than once or zero times'); if (md.split('## Troubleshooting').length !== 2) checks.push('## Troubleshooting heading appears more than once'); if (md.split('## Reference').length !== 2) checks.push('## Reference heading appears more than once'); if (md.indexOf('## Deploy') > md.indexOf('## Tool Surface')) checks.push('## Deploy appears AFTER ## Tool Surface — section order wrong'); if (md.indexOf('## Reference') > md.indexOf('## Tool Surface')) checks.push('## Reference appears AFTER ## Tool Surface — section order wrong'); if (md.indexOf('## Deploy') < md.indexOf('### 4. First tool call')) checks.push('## Deploy appears BEFORE Step 4 — section order wrong'); if (md.match(/Unknown OAuth subject/g).length !== 1) checks.push('403 body should appear EXACTLY ONCE in README (inline in Step 4, NOT duplicated in Troubleshooting per D-02)'); if (checks.length) { console.error(checks.join('\\n')); process.exit(1); } console.log('OK'); "</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains exactly one `## Deploy` heading, one `## Troubleshooting` heading, and one `## Reference` heading
    - All three new H2 headings appear AFTER `### 4. First tool call` and BEFORE `## Tool Surface (v0.1)` (file position order: Step 4 → Deploy → Troubleshooting → Reference → Tool Surface)
    - `## Deploy` references all three npm scripts by name (`npm run deploy`, `npm run deploy:mcp`, `npm run deploy:triage`)
    - `## Deploy` contains the deploy-order precondition: "engram-mcp-server" must have been deployed BEFORE `deploy:triage` (cross-worker DO binding invariant per PATTERNS §"Pattern 2")
    - `## Deploy` quotes the literal wrangler error message `Could not find a Worker with the name "engram-mcp-server"` (so users grepping the failure find this section)
    - `## Troubleshooting` contains 6 H3 entries matching the planned triage list
    - `## Troubleshooting` does NOT contain a `Unknown OAuth subject` entry (it lives inline in Step 4 per D-02; duplicating violates source-of-truth)
    - The literal string `Unknown OAuth subject` appears EXACTLY ONCE in the entire README (only in Step 4)
    - `## Troubleshooting` includes the `rm -rf ~/.mcp-auth/` JWT cache clear command (RESEARCH §Pitfall 3)
    - `## Troubleshooting` includes the `--engine-strict=false` fallback (per-package README §Troubleshooting)
    - `## Reference` has down-links to per-package README sections + sideways to CLAUDE.md sections — no `.planning/` cross-links
    - `## Tool Surface (v0.1)` H2 and all content below it remain BYTE-IDENTICAL to the pre-Plan-02 state (no accidental edits during section insertion)
    - No emojis (per CLAUDE.md global directive)
    - File still parses cleanly as Markdown (no broken fenced-code-block tags, no orphaned `---` separators)
  </acceptance_criteria>
  <done>Root README has the full Deploy / Troubleshooting / Reference reference surface a first-time deployer needs. Cross-worker DO deploy-order invariant is encoded in the README (matching the wrapper script in Plan 01 — defense in depth across script + docs). Per-package README is correctly cross-referenced (DOWN-link) rather than duplicated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 7 Plan 02 is docs-only and adds NO new trust boundaries. Documenting the system does not change the system. The boundaries already in place:

| Boundary | Description |
|----------|-------------|
| Operator's keyboard → README instructions | Operator follows instructions to configure their own machine. The README does not execute code on the operator's machine. |
| README → packages/mcp-server/README.md (cross-link) | Down-link to a sibling README already vetted in Phase 3. |

## What's NOT Being Added in This Plan

- NO new endpoints
- NO new auth surfaces
- NO new data flows
- NO new env vars or secrets
- NO new external service integrations
- NO new package installs
- NO changes to Worker code or runtime behavior
- NO changes to `oauth.ts` (the literal 403 string is QUOTED from oauth.ts:201, not re-implemented or re-defined there)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-LEAK | Information Disclosure | README OAuth bootstrap walkthrough | mitigate | All snippets use ASCII placeholders (`<some-long-string>`, `<your-subdomain>`, `<pick-an-identifier>`) per CONTEXT.md Security gate explicit requirement. No real OAuth `sub`, no real workspace_id, no real Cloudflare subdomain interpolated into example commands. (Task 1 acceptance criterion. PATTERNS §"Security note" reinforces this.) |
| T-07-02-SC | Tampering | `mcp-remote` supply-chain risk via README directive | mitigate | The dated rollback-marker comment `// tested with mcp-remote@0.1.38 on Claude Desktop 2026-05-29` is in Step 3's config snippet (Task 1 acceptance criterion). If a future `mcp-remote` release breaks the bootstrap flow, the dated comment anchors the working version. v0.4+ owns the swap to native Streamable HTTP per CONTEXT.md Deferred Ideas. slopcheck [OK] verified Phase 7 RESEARCH §"Package Legitimacy Audit". |
| T-07-02-DUP | Repudiation / Source-of-truth drift | Multiple copies of 403 body string in README | mitigate | The literal `Unknown OAuth subject: ${sub}. Bootstrap via npm run kv:bootstrap.` appears EXACTLY ONCE in the README (only in Step 4) per Task 2 acceptance criterion. Troubleshooting section explicitly EXCLUDES this entry per D-02 — no duplication = no drift risk when the source string changes. |
| T-07-02-WALKTHROUGH | Information Disclosure | Step 4 walkthrough on bootstrap process | accept | The walkthrough discloses HOW the bootstrap works (KV-backed identity lookup). This is by design — the bootstrap is intentionally transparent per D-01 ("the user understands what just happened"). The KV value is keyed on the OAuth `sub` (already known to the requester per T-03-PROPS); no secret material is disclosed by documenting the lookup. |
| T-07-02-BOOTSTRAP-DEFER | Spoofing | Deferred `kv-bootstrap-interactive` script | accept | The README documents the v0.4 deferred script in Step 4's "For the full bootstrap rationale" cross-link (which points at packages/mcp-server/README.md). The deferred v0.4 work is captured in CONTEXT.md §Deferred Ideas. No spoofing risk from documenting the future feature today. |
</threat_model>

<verification>
## Plan-Level Verification

After both tasks complete:

1. README structural integrity:
   - `cat README.md | grep -c "^## "` returns the expected number of H2 headings (original 8 + 3 new = 11)
   - `cat README.md | grep -c "^### "` includes the 4 new numbered step H3s
   - File parses as valid Markdown (no orphaned fenced-code-block tags — verify by viewing in a Markdown previewer)
2. Source-of-truth invariants:
   - `grep -c "Unknown OAuth subject" README.md` returns exactly 1 (only inline in Step 4)
   - `grep -c "mcp-remote@0.1.38" README.md` returns at least 1 (rollback marker present)
   - `grep -c "engram-mcp-server" README.md` returns at least 2 (Step 3 config snippet + Deploy section precondition note)
3. Cross-link integrity:
   - All `./packages/mcp-server/README.md` links resolve (file exists at that path — verify by `ls`)
   - All `./CLAUDE.md` links resolve (file exists at repo root)
   - No links into `.planning/` (verify `grep -c "(.planning/" README.md` returns 1 — the existing `.planning/ROADMAP.md` link in Status section; no NEW `.planning/` cross-links introduced by Plan 02)
4. Preservation:
   - The `## Tool Surface (v0.1)` H2 and everything below it (through `## License`) is byte-identical to the pre-plan state — verify with `git diff README.md` and visually confirm the diff is entirely above line 132 (current Tool Surface location)
</verification>

<success_criteria>
1. README Getting Started restructured into numbered steps 1-4 (D-02 + PATTERNS Q1)
2. Step 4 quotes the literal 403 body verbatim from `packages/mcp-server/src/oauth.ts:201`
3. Step 3 includes the dated `mcp-remote@0.1.38` rollback marker comment
4. New `## Deploy` H2 documents all three scripts with deploy-order precondition
5. New `## Troubleshooting` H2 with 6 triaged entries; EXCLUDES the bootstrap-403 (lives inline in Step 4)
6. New `## Reference` H2 with down-links to per-package READMEs + sideways to CLAUDE.md
7. `## Tool Surface (v0.1)` and below sections preserved byte-identically
8. README delta is ~150-180 lines (within CONTEXT.md Claude's Discretion estimate of ~100-150)
9. NO emojis used anywhere in the new content
10. All example snippets use ASCII placeholders (no real sub / workspace / subdomain values)
</success_criteria>

<output>
Create `.planning/phases/07-deploy-acceptance/07-02-SUMMARY.md` when done. The summary should:
- Confirm the new H2 + H3 heading counts and their byte-anchors in the file (line numbers post-edit)
- Confirm the literal 403 body appears exactly once and is sourced from `packages/mcp-server/src/oauth.ts:201`
- Confirm the dated mcp-remote rollback marker is in place
- Confirm Tool Surface and below sections were not touched (cite git diff stats)
- Reference Plan 03 (HUMAN-UAT skeleton) as the next plan in the wave chain
- Include the inline cf-code-assist routing log entry. Per `~/.claude/CLAUDE.md` 3-question checklist + Phase 7 CONTEXT.md tracker projection: this is the most likely `generateDocs` candidate in the phase. Apply the 3 questions: Q1 (is synthesis cross-file?) = N (single file, README only); Q2 (>50 lines mechanical generation?) = Y (~150-180 lines); Q3 (stable template/spec to anchor on?) = Y (PATTERNS.md hoist instructions + source content in packages/mcp-server/README.md). Answer: N/Y/Y → route candidate. However, the README is high-touch user-facing content that benefits from Claude's tone calibration and cross-link verification — executor's call whether to route or hand-author. Either is defensible; log the choice and rationale.
</output>
