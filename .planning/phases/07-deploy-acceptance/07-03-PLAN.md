---
phase: 07-deploy-acceptance
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - .planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md
autonomous: true
requirements: [DEP-03, DEP-04]
tags: [human-uat, acceptance-test, evidence-template]

must_haves:
  truths:
    - "An EMPTY but well-structured `07-HUMAN-UAT.md` exists in the phase directory, ready for Russell to populate during the 1-2 day acceptance window (D-04)"
    - "The skeleton has three test stubs: DEP-03 Run 1, DEP-03 Run 2 (different posting per D-03), DEP-04 rewire smoke (per D-06)"
    - "Each stub includes the expected/result/notes shape from PATTERNS §HUMAN-UAT.md analog merge (Phase 1 frontmatter + Phase 3 smoke-record per-test richness)"
    - "Skeleton includes operator-facing redaction guidance: do NOT paste raw OAuth `sub` values in conv excerpts (CONTEXT.md Security gate + RESEARCH §'Known Threats Pattern 3')"
    - "Skeleton `## Summary` block matches GSD `/gsd:audit-uat` consumption format (total / passed / pending / fail / skipped / blocked counts) per PATTERNS §'HUMAN-UAT.md gap-closure flow'"
  artifacts:
    - path: ".planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md"
      provides: "Empty HUMAN-UAT evidence skeleton for DEP-03 + DEP-04 acceptance"
      contains: "status: in_progress"
    - path: ".planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md"
      provides: "DEP-03 Run 1 + Run 2 test stubs"
      contains: "### Run 1:"
    - path: ".planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md"
      provides: "DEP-04 rewire smoke stub"
      contains: "### Rewire smoke:"
    - path: ".planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md"
      provides: "Summary block consumed by /gsd:audit-uat"
      contains: "total: 3"
  key_links:
    - from: ".planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md frontmatter `source:`"
      to: ".planning/REQUIREMENTS.md §Deploy + Acceptance (DEP) DEP-03 + DEP-04"
      via: "explicit requirement ID citation (no separate VERIFICATION.md exists for Phase 7)"
      pattern: "source: \\[REQUIREMENTS.md#DEP-03"
    - from: ".planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md tests"
      to: "Russell's manual execution during 1-2 day acceptance window"
      via: "human-gated UAT (post-execute-phase automation completion)"
      pattern: "result: \\[pending\\]"
---

<objective>
Create the EMPTY `07-HUMAN-UAT.md` skeleton that will hold the DEP-03 (cross-conversation recall, 2 runs) and DEP-04 (job-search agent rewire smoke) evidence during the 1-2 day acceptance window. Per CONTEXT.md D-04 + planning_context Key Finding #5, this skeleton is created at plan-execute time but the actual pass/fail/notes entries are populated by Russell DURING the acceptance window — which happens AFTER the rest of Phase 7's automated work completes. The skeleton must match the standard GSD HUMAN-UAT.md shape so `/gsd:audit-uat` and `/gsd:verify-work` consume it correctly.

Purpose: Phase 7 is unusual — the acceptance test IS the v0.1 done-state, and it runs on real wall-clock time (1+ hour waits, 1-2 day window). Without a pre-built skeleton, Russell would write the file from scratch under acceptance-time pressure and risk frontmatter shape drift (RESEARCH §Pitfall 8). The skeleton de-risks the recording step so Russell focuses on executing the test, not on getting the file format right.

Output: Single file `.planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md` (~70-90 lines). Frontmatter merges Phase 1 HUMAN-UAT shape (status / phase / source / started / updated) with Phase 7-specific source citations (REQUIREMENTS.md DEP-03 + DEP-04 — no separate VERIFICATION.md). Body has three test stubs (DEP-03 Run 1, DEP-03 Run 2, DEP-04 rewire smoke) each with the richer per-test shape from `03-MCP-INSPECTOR-SMOKE.md` Smoke Run subsection. Summary block matches Phase 1's count-format so `/gsd:audit-uat` parses it.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-deploy-acceptance/07-CONTEXT.md
@.planning/phases/07-deploy-acceptance/07-RESEARCH.md
@.planning/phases/07-deploy-acceptance/07-PATTERNS.md
@.planning/phases/07-02-SUMMARY.md
@.planning/phases/01-foundation/01-HUMAN-UAT.md
@.planning/phases/03-mcp-server-scaffold/03-MCP-INSPECTOR-SMOKE.md

<interfaces>
<!-- Phase 1 HUMAN-UAT.md frontmatter shape (verified — 01-HUMAN-UAT.md:1-7): -->
---
status: partial          # one of: partial | resolved | passed | in_progress
phase: 01-foundation     # phase slug (NOT bare number)
source: [01-VERIFICATION.md]  # source doc(s) enumerating tests
started: 2026-05-25T22:00:00Z
updated: 2026-05-25T22:00:00Z
---

<!-- Phase 1 HUMAN-UAT.md body shape (verified — 01-HUMAN-UAT.md:9-43): -->
## Current Test

[awaiting human testing]

## Tests

### N. <test title>
expected: <one-sentence expected outcome>
result: [pending]   # or [pass] / [fail]

## Summary
total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

<!-- Phase 3 smoke per-test richer shape (verified — 03-MCP-INSPECTOR-SMOKE.md:93-107): -->
- **Date:** YYYY-MM-DD
- **Mode:** <details>
- **Observed <key>:** <value>
- **Result:** <pass | fail | pending>
- **Notes:** <body>
- **Deviations:** <numbered list>

<!-- Phase 7 merged shape per PATTERNS §"07-HUMAN-UAT.md target shape" -->
<!-- Frontmatter: Phase 1 shape (slug `phase: 07-deploy-acceptance`) -->
<!-- Tests: Phase 1 outer structure + Phase 3 per-test richness -->

<!-- REQUIREMENTS.md citations (verified — REQUIREMENTS.md:82-86): -->
DEP-03: Acceptance test: Russell remember()s a job posting in conv A; 1+ hour later
        in conv B (no shared chat history), `recall` returns the correct posting with
        extracted fields. Passes on at least two consecutive runs.
DEP-04: Russell's job-search agent reconfigured to use Engram as its memory backend;
        agent's existing job-storage flow continues to work end-to-end (no regression).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 07-HUMAN-UAT.md skeleton with frontmatter + DEP-03 Run 1/Run 2 + DEP-04 rewire smoke stubs + Summary block</name>
  <files>.planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-HUMAN-UAT.md (the full 45-line file — your frontmatter + Summary outer shape source)
    - .planning/phases/03-mcp-server-scaffold/03-MCP-INSPECTOR-SMOKE.md (lines 90-107 — the per-test richer evidence shape with Date/Mode/Observed/Result/Notes/Deviations)
    - .planning/REQUIREMENTS.md (lines 82-86 — DEP-03 + DEP-04 verbatim acceptance criteria; the `source:` frontmatter cites these)
    - .planning/phases/07-deploy-acceptance/07-CONTEXT.md (lines 25-33 — D-03 + D-04 acceptance protocol + D-05 + D-06 rewire scope; lines 121-127 — `<specifics>` on two-day window + cardinal rules)
    - .planning/phases/07-deploy-acceptance/07-PATTERNS.md (lines 241-350 — full HUMAN-UAT.md analog merge with frontmatter format choice, test stub template, security redaction note)
    - .planning/phases/07-deploy-acceptance/07-RESEARCH.md (lines 450-489 — DEP-03/DEP-04 HUMAN-UAT.md code example showing the literal text shape)
  </read_first>
  <behavior>
    - File `.planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md` exists with valid YAML frontmatter
    - Frontmatter `status` = `in_progress` (NOT `passed` — the file is a skeleton; Russell flips to `passed` only when all three tests pass per PATTERNS §"HUMAN-UAT.md gap-closure flow")
    - Frontmatter `phase` = `07-deploy-acceptance` (slug form, matching Phase 1 precedent)
    - Frontmatter `source` cites the two requirement IDs (`REQUIREMENTS.md#DEP-03`, `REQUIREMENTS.md#DEP-04`) since Phase 7 has no separate `07-VERIFICATION.md`
    - Frontmatter `started` + `updated` set to today (the date the skeleton was created)
    - Body contains exactly three test entries:
      - `### Run 1: <date>` (DEP-03 first run — first job posting)
      - `### Run 2: <date+1>` (DEP-03 second run — DIFFERENT job posting per D-03 to exercise Vectorize semantic recall vs memoization)
      - `### Rewire smoke: <date>` (DEP-04 — single end-to-end smoke per D-06)
    - Each test entry has the `Date`/`Expected`/`Posting`/`Wait duration`/`Result`/`Conv A excerpt`/`Conv B excerpt`/`Notes` shape (merged Phase 1 + Phase 3 richness)
    - Each `Result:` field defaults to `[pending]` (matches Phase 1 convention)
    - Operator-facing security note in the body directs Russell to redact OAuth `sub` values if visible in conv excerpts (CONTEXT.md Security gate + RESEARCH §"Known Threats Pattern 3")
    - `## Summary` block with `total: 3, passed: 0, pending: 3, issues: 0, skipped: 0, blocked: 0` (matches Phase 1 count format for `/gsd:audit-uat` parsing)
    - `## Gaps` section exists (empty placeholder — populated if gap-closure cycle needed)
  </behavior>
  <action>
    Create the file at `.planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md` with the following EXACT content (substitute the timestamp values with today's actual ISO 8601 timestamps when writing). The file is a skeleton — placeholders like `<TBD: ...>` are intentional; Russell fills them in during the acceptance window.

    Use the standard YAML frontmatter shape from Phase 1 (`status` / `phase` slug / `source` list / `started` / `updated`). The `source:` list cites REQUIREMENTS.md anchors directly since Phase 7 has no separate `07-VERIFICATION.md` per CONTEXT.md (Phase 7's verification IS the HUMAN-UAT itself).

    File body structure (literal Markdown to write):

    ```markdown
    ---
    status: in_progress
    phase: 07-deploy-acceptance
    source: [REQUIREMENTS.md#DEP-03, REQUIREMENTS.md#DEP-04]
    captured_by: russell
    started: <today ISO 8601 timestamp, e.g. 2026-05-29T12:00:00Z>
    updated: <today ISO 8601 timestamp>
    ---

    # Phase 7 HUMAN-UAT — Deploy + Acceptance

    > Created as an empty skeleton at plan-execute time per CONTEXT.md D-04 + planning_context Key Finding #5.
    > Russell populates each test entry's `Result:` + body fields DURING the 1-2 day acceptance window.
    > Phase 7 does not close until all three tests are `[pass]` per PATTERNS §"HUMAN-UAT.md gap-closure flow" — there is no "phase-passes-with-known-issues" escape hatch for DEP-03 (CONTEXT.md `<specifics>`).

    ## Current Test

    [awaiting human testing]

    ## Operator Notes (read before running)

    - **Redact OAuth `sub` values** if they appear in any conv excerpt you paste below. The `sub` is dynamic per `mcp-remote` registration (not a long-term secret), but exposing it in version control reveals identity-mapping shape. Use `<sub-redacted>` as the replacement marker.
    - **Two real wall-clock runs over 1-2 days** per D-03. Run 1 morning of Day N → recall afternoon of Day N (1+ hour gap). Run 2 morning of Day N+1 → recall afternoon of Day N+1, using a DIFFERENT job posting (different role/company/URL) to exercise Vectorize semantic recall vs memoization of the exact same content.
    - **Verify the conv-A-side write succeeded BEFORE relying on the 1+ hour wait** per RESEARCH §Pitfall 7. In conv A, immediately after `remember()`, call `recall()` with the same query and confirm the just-stored block returns. If it does → conv-A-side write is good. If conv B fails after the wait, the issue is workspace_id mismatch or Vectorize namespace handling, NOT a missed write.
    - **DEP-04 rewire smoke is exclusively forward-looking** per D-05 — no migration of pre-Engram job files. Pre-Engram local files (markdown, JSON, XLS) stay untouched on disk as historical record. The rewire is a capture-path swap on the agent side; this UAT entry verifies the new path works end-to-end.

    ## Tests

    ### Run 1: <TBD: YYYY-MM-DD>

    - **DEP ID:** DEP-03
    - **Expected:** In a fresh Claude Desktop chat session (conv B, no shared history with the conv that performed the `remember`), asking "what job posting did I save earlier?" causes Claude to call `recall` against Engram and return the posting with extracted fields (URL + company + role) intact.
    - **Conv A action:** `remember()` a fresh job posting (URL + role + company) via Claude Desktop.
    - **Job posting used (conv A):** <TBD: e.g. "Anthropic Solutions Engineer SF — https://...">
    - **Wait duration:** <TBD: actual wall-clock minutes between conv A `remember()` and conv B `recall()` — must be ≥60 minutes per DEP-03>
    - **Conv B action:** New Claude Desktop chat → "what job posting did I save earlier?"
    - **Result:** [pending]
    - **Conv A excerpt (redact `<sub>`):** <TBD>
    - **Conv B excerpt (redact `<sub>`):** <TBD>
    - **Notes:** <TBD: any deviations, env state, observed timing, anomalies>

    ### Run 2: <TBD: YYYY-MM-DD (next calendar day per D-03)>

    - **DEP ID:** DEP-03
    - **Expected:** Same shape as Run 1 but with a **different** job posting (different role + company + URL) to exercise Vectorize semantic recall, not memoization of Run 1's content.
    - **Conv A action:** `remember()` a DIFFERENT fresh job posting.
    - **Job posting used (conv A):** <TBD: must be different role + company + URL from Run 1>
    - **Wait duration:** <TBD: ≥60 minutes>
    - **Conv B action:** New Claude Desktop chat → "what job posting did I save earlier?"
    - **Result:** [pending]
    - **Conv A excerpt (redact `<sub>`):** <TBD>
    - **Conv B excerpt (redact `<sub>`):** <TBD>
    - **Notes:** <TBD>

    ### Rewire smoke: <TBD: YYYY-MM-DD>

    - **DEP ID:** DEP-04
    - **Expected:** With Russell's existing job-search agent reconfigured to use Engram as its memory backend (capture path swap; NO migration of pre-Engram local files per D-05), the following end-to-end smoke passes:
      1. Paste a fresh job posting into the agent → agent calls Engram `remember()` → verify the block landed by calling Engram `recall()` in the same conversation.
      2. Next day, open a fresh Claude conversation → ask the same agent "what jobs have I saved?" → verify the recent capture is returned with extracted fields.
    - **Agent rewire location:** <TBD: claude_desktop_config.json change | agent prompt template change | other — Russell will know at execution time per RESEARCH Open Question 3>
    - **Job posting used:** <TBD>
    - **Same-day `recall()` result:** <TBD: pass | fail>
    - **Next-day fresh-conv `recall()` result:** <TBD: pass | fail>
    - **Result:** [pending]
    - **Agent capture excerpt:** <TBD>
    - **Next-day recall excerpt:** <TBD>
    - **Notes:** <TBD: any regression in the agent's pre-Engram capture flow, env state, observed semantics>

    ## Summary

    total: 3
    passed: 0
    issues: 0
    pending: 3
    skipped: 0
    blocked: 0

    ## Gaps

    <!-- Populate ONLY if a test fails. Each gap becomes a gap-closure planning input. -->
    ```

    Replace `<today ISO 8601 timestamp>` in the frontmatter `started` + `updated` fields with the actual creation timestamp at execute time (use `new Date().toISOString()` or the current ISO 8601 UTC timestamp). All other `<TBD: ...>` placeholders are intentional — Russell fills them in during the acceptance window.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const path = '.planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md'; if (!fs.existsSync(path)) { console.error('File missing'); process.exit(1); } const md = fs.readFileSync(path, 'utf8'); const checks = []; if (!md.startsWith('---\\n')) checks.push('frontmatter must open file'); if (!md.includes('status: in_progress')) checks.push('status not in_progress'); if (!md.includes('phase: 07-deploy-acceptance')) checks.push('phase slug missing or wrong'); if (!md.includes('REQUIREMENTS.md#DEP-03')) checks.push('source missing DEP-03'); if (!md.includes('REQUIREMENTS.md#DEP-04')) checks.push('source missing DEP-04'); if (!md.includes('### Run 1:')) checks.push('Run 1 stub missing'); if (!md.includes('### Run 2:')) checks.push('Run 2 stub missing'); if (!md.includes('### Rewire smoke:')) checks.push('Rewire smoke stub missing'); if (!md.includes('total: 3')) checks.push('Summary total: 3 missing'); if (!md.includes('pending: 3')) checks.push('Summary pending: 3 missing'); if (!md.includes('passed: 0')) checks.push('Summary passed: 0 missing'); if (!md.includes('Redact OAuth')) checks.push('sub redaction operator note missing'); if (!md.includes('## Gaps')) checks.push('Gaps section missing'); const resultCount = (md.match(/result: \\[pending\\]|Result:\\*\\* \\[pending\\]|Result: \\[pending\\]/g) || []).length; if (resultCount < 3) checks.push('expected 3 result: [pending] markers, found ' + resultCount); if (checks.length) { console.error(checks.join('\\n')); process.exit(1); } console.log('OK'); "</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md`
    - File opens with `---\n` (valid YAML frontmatter delimiter)
    - Frontmatter `status:` equals `in_progress` (NOT `passed`, NOT `partial`)
    - Frontmatter `phase:` equals `07-deploy-acceptance` (slug form matching Phase 1 precedent per PATTERNS §"target shape")
    - Frontmatter `source:` is a list containing both `REQUIREMENTS.md#DEP-03` and `REQUIREMENTS.md#DEP-04`
    - Frontmatter `started:` and `updated:` are valid ISO 8601 UTC timestamps (e.g. `2026-05-29T12:00:00Z`)
    - Body contains exactly three H3 test headings: `### Run 1: <TBD: ...>`, `### Run 2: <TBD: ...>`, `### Rewire smoke: <TBD: ...>`
    - Each test stub references its DEP ID (`DEP-03` for Run 1 + Run 2; `DEP-04` for Rewire smoke)
    - Each test stub's `Result:` (or `**Result:**`) defaults to `[pending]`
    - Body includes an "Operator Notes" section with the literal string `Redact OAuth` (the `sub` redaction directive per CONTEXT.md Security gate)
    - Body includes guidance directing Russell to verify the conv-A-side write succeeded before relying on the 1+ hour wait (RESEARCH §Pitfall 7 mitigation)
    - Body includes the literal string `different role + company + URL` (or similar) in Run 2 to encode D-03's "exercises Vectorize semantic recall, not memoization" rule
    - `## Summary` block contains literal `total: 3`, `passed: 0`, `pending: 3`, `issues: 0`, `skipped: 0`, `blocked: 0` (matches Phase 1 count format for /gsd:audit-uat parsing)
    - `## Gaps` heading exists at end of file
    - File is well-formed Markdown (no fenced-code-block tag mismatches, no orphaned brackets)
    - NO real OAuth `sub` values, NO real Cloudflare workspace IDs, NO real Russell-specific account identifiers leak into the skeleton (all such fields are `<TBD: ...>` placeholders)
  </acceptance_criteria>
  <done>The HUMAN-UAT.md skeleton exists, validated by `/gsd:audit-uat` parsing format. Russell can populate Run 1 by editing only the `<TBD: ...>` placeholders for that section — no frontmatter regeneration or structural rebuild required. Plan 04 invokes the deploy + references this skeleton as the human-gated acceptance evidence surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 7 Plan 03 creates a documentation artifact in version-controlled storage. Boundaries already in place:

| Boundary | Description |
|----------|-------------|
| File creation → git history | The empty skeleton becomes part of the repo; future-Russell + Devon read it from git. No code execution. |
| Skeleton placeholder values → operator | The operator reads placeholders like `<TBD: ...>` and fills them in. No interpretation of injected values. |

## What's NOT Being Added in This Plan

- NO new endpoints
- NO new auth surfaces
- NO new data flows
- NO new env vars or secrets
- NO new external service integrations
- NO new package installs
- NO changes to Worker code, Worker config, or runtime behavior
- NO new GSD slash command registrations or workflow changes

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-LEAK | Information Disclosure | Skeleton placeholders | mitigate | All placeholder values are `<TBD: ...>` markers — NO real OAuth `sub`, NO real workspace_id, NO real user_id, NO real Russell-specific account identifiers in the skeleton. Task 1 acceptance criterion enforces this. (PATTERNS §"Security note" + RESEARCH §"Known Threats Pattern 3" — the Phase 3 smoke recorded a real `sub` in `03-MCP-INSPECTOR-SMOKE.md:96` which is informational rather than blocking, but the Phase 7 template's redaction guidance is precautionary and reduces the risk of accidental sub-leak in production-time acceptance evidence.) |
| T-07-03-SUB-PASTE | Information Disclosure | Operator-pasted conv excerpts during acceptance | mitigate | The skeleton's `## Operator Notes` section explicitly directs Russell to use `<sub-redacted>` as the replacement marker when pasting conv excerpts that might contain a `sub`. Task 1 acceptance criterion verifies the literal "Redact OAuth" string is present. The guidance is operator-action; cannot enforce in CI but the documented rule shifts risk from default-disclose to default-redact. |
| T-07-03-VERIFY-SHAPE | Repudiation | `/gsd:audit-uat` format compatibility | mitigate | Summary block uses the literal count format (`total:`, `passed:`, `pending:`, `issues:`, `skipped:`, `blocked:`) verified against Phase 1's HUMAN-UAT.md and confirmed parseable by `/gsd:audit-uat` per PATTERNS §"HUMAN-UAT.md gap-closure flow". Frontmatter shape merged from Phase 1 (proven analog). Repudiation risk is low because the format IS the contract. |
| T-07-03-PREMATURE | Spoofing | `status: in_progress` vs `status: passed` | mitigate | Skeleton sets `status: in_progress` per Task 1 acceptance criterion. The phase MUST NOT close until all three tests are `[pass]` (CONTEXT.md `<specifics>` cardinal rule). Russell cannot accidentally flip `status: passed` at skeleton creation; flipping requires a deliberate edit after the three tests populate. |
</threat_model>

<verification>
## Plan-Level Verification

After Task 1 completes:

1. File existence + structure:
   - `test -f .planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md` → success
   - `head -10 .planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md` shows frontmatter with `status: in_progress`, `phase: 07-deploy-acceptance`
   - `grep -c "^### " .planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md` returns 3 (Run 1, Run 2, Rewire smoke)
   - `grep -c "\[pending\]" .planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md` returns ≥3 (one per test)
2. Frontmatter parseability:
   - The file's frontmatter is valid YAML (parse with a YAML lib in the verify command)
3. /gsd:audit-uat compatibility (manual — done by /gsd:audit-uat once Russell starts populating):
   - The Summary block counts must agree with the actual `[pending]` / `[pass]` / `[fail]` markers across all three tests; Russell maintains this invariant when populating
4. No leaked identifiers:
   - `grep -E "(sk-|pk-|workers\.dev|sub-[a-z0-9]{8,})" .planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md` returns nothing (no API keys, no real subdomain, no real sub values)
</verification>

<success_criteria>
1. `.planning/phases/07-deploy-acceptance/07-HUMAN-UAT.md` exists with valid YAML frontmatter
2. Frontmatter: `status: in_progress`, `phase: 07-deploy-acceptance`, `source` cites both DEP-03 + DEP-04
3. Body has 3 test stubs (DEP-03 Run 1, DEP-03 Run 2 with different posting, DEP-04 rewire smoke) each with the merged Phase 1 + Phase 3 evidence shape
4. Operator notes include sub-redaction guidance and the Pitfall 7 "verify conv-A-side write succeeded first" mitigation
5. `## Summary` block matches Phase 1 count format (`total: 3, passed: 0, pending: 3, ...`)
6. `## Gaps` section exists (empty placeholder)
7. NO real identifiers leak — all variable fields are `<TBD: ...>` placeholders
</success_criteria>

<output>
Create `.planning/phases/07-deploy-acceptance/07-03-SUMMARY.md` when done. The summary should:
- Confirm the file exists at the expected path with the expected frontmatter
- Explicitly note that this is a SKELETON — pass/fail body content is populated by Russell DURING the 1-2 day acceptance window (after Plan 04 executes the deploy)
- Reference Plan 04 as the consumer of this skeleton (Plan 04 invokes `npm run deploy` and then enters the human-gated acceptance window)
- Note that `/gsd:verify-work 7` will read this file as the DEP-03 + DEP-04 evidence source; phase verification depends on Summary block showing `passed: 3, pending: 0, fail: 0` (per PATTERNS §"HUMAN-UAT.md gap-closure flow")
- Include the inline cf-code-assist routing log entry. Per `~/.claude/CLAUDE.md` 3-question checklist: Q1 (synthesis cross-file?) = N (single new file); Q2 (>50 lines mechanical?) = N (~90 lines but structured-template rather than mechanical-code); Q3 (stable template?) = Y (PATTERNS §"target shape"). Answer: N/N/Y — diff under threshold; route to Claude (text-only template work, not code generation).
</output>
