---
phase: 02-recall-quality-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md
  - scripts/sync-eval-corpus.mjs
  - shared/vectorize-utils/package.json
  - shared/vectorize-utils/tsconfig.json
  - shared/vectorize-utils/src/index.ts
  - packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json
  - packages/mcp-server/package.json
  - packages/triage-worker/package.json
autonomous: true
requirements:
  - RNK-01
tags:
  - rnk
  - scaffolding
  - shared-package
linear_subissue: rnk
must_haves:
  truths:
    - "Sweep test can import `vectorizeQuery` and `vectorizeNeighbors` from `@engram/vectorize-utils`."
    - "Vendored 100-entry corpus fixture exists at the path the sweep test will import."
    - "`pretest:eval` script re-syncs the corpus before any eval-tier run."
    - "cf-code-assist routing tracker exists and contains the 3-question checklist; downstream tasks can append rows."
  artifacts:
    - path: "shared/vectorize-utils/package.json"
      provides: "Workspace package manifest for vectorize-utils"
      contains: "@engram/vectorize-utils"
    - path: "shared/vectorize-utils/tsconfig.json"
      provides: "TS config extending tsconfig.base.json"
      contains: "tsconfig.base.json"
    - path: "shared/vectorize-utils/src/index.ts"
      provides: "vectorizeQuery + vectorizeNeighbors exports"
      contains: "vectorizeNeighbors"
    - path: "scripts/sync-eval-corpus.mjs"
      provides: "Authoritative-corpus → vendored-fixture sync"
      contains: "sync-eval-corpus"
    - path: "packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json"
      provides: "Vendored 100-entry corpus the sweep test imports"
      contains: "corpus_version"
    - path: ".planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md"
      provides: "Phase 2 routing tracker mirroring Phase 1 pattern"
      contains: "3-Question Checklist"
  key_links:
    - from: "packages/mcp-server/package.json"
      to: "scripts/sync-eval-corpus.mjs"
      via: "pretest:eval npm script"
      pattern: "pretest:eval"
    - from: "packages/mcp-server/package.json"
      to: "shared/vectorize-utils/"
      via: "dependencies: @engram/vectorize-utils"
      pattern: "@engram/vectorize-utils"
    - from: "packages/triage-worker/package.json"
      to: "shared/vectorize-utils/"
      via: "dependencies: @engram/vectorize-utils"
      pattern: "@engram/vectorize-utils"
---

<objective>
RNK Wave 0 scaffolding. Stand up everything downstream Phase 2 plans need before any sweep code is written: the new `shared/vectorize-utils/` workspace package (extracting `vectorizeQuery` verbatim and adding `vectorizeNeighbors`), the `scripts/sync-eval-corpus.mjs` corpus syncer, the vendored 100-entry fixture, the Phase 2 cf-code-assist routing tracker, and the consumer-side `package.json` wiring for both `mcp-server` and `triage-worker`.

Purpose: Wave 0 unblocks RNK-01 (sweep test needs `vectorizeQuery` import + the vendored fixture) and CON-02 (conflict-pipeline needs `vectorizeNeighbors`). Per CONTEXT.md D-19 the routing tracker is also a Wave 0 deliverable — every code-producing task in Phase 2 appends a row to it.

Output:
- `shared/vectorize-utils/` (new workspace package: `package.json`, `tsconfig.json`, `src/index.ts`)
- `scripts/sync-eval-corpus.mjs` (cross-tree file copier, `--check` mode for CI drift detection)
- `packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json` (initial sync)
- `packages/mcp-server/package.json` + `packages/triage-worker/package.json` (add `@engram/vectorize-utils` dep + `pretest:eval` script on mcp-server)
- `.planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md` (routing tracker scaffold)

Linear sub-issue to be created at execution start (gsd-executor responsibility) per D-17: one sub-issue under the Phase 2 ENG issue for the RNK workstream.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-recall-quality-baseline/02-CONTEXT.md
@.planning/phases/02-recall-quality-baseline/02-RESEARCH.md
@.planning/phases/02-recall-quality-baseline/02-PATTERNS.md
@.planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md
@CLAUDE.md

<interfaces>
<!-- Extracted from existing codebase — executor should not re-explore -->

From `packages/mcp-server/src/vectorize-helper.ts` (the source for the `vectorizeQuery` extraction — copy verbatim, do NOT alter behavior):
```typescript
const NAMESPACE_MAX_BYTES = 64;
const VECTORIZE_TOPK_DEFAULT = 25;
function assertNamespace(workspaceId: string): void { /* throws if empty or >64 bytes */ }
export function vectorizeQuery(
  env: { VECTORIZE: VectorizeIndex },
  workspaceId: string,
  vector: number[],
  opts: {
    topK?: number;
    filter?: Record<string, unknown>;
    returnMetadata?: "none" | "indexed" | "all";
  },
): Promise<VectorizeMatches>;
```

From `shared/ai-config/src/index.ts` (the constant `vectorizeNeighbors` will use for over-fetch):
```typescript
export const VECTORIZE_OVERFETCH_FACTOR = 2;
```

From `shared/ai-config/package.json` (verbatim shape to mirror for `shared/vectorize-utils/package.json` — change only the `name` field):
```json
{
  "name": "@engram/ai-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": { ".": { "types": "./src/index.ts", "default": "./src/index.ts" } },
  "files": ["src"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared/vectorize-utils/ workspace package (D-08, D-10)</name>
  <files>shared/vectorize-utils/package.json, shared/vectorize-utils/tsconfig.json, shared/vectorize-utils/src/index.ts</files>
  <read_first>
    - shared/ai-config/package.json (CANONICAL TEMPLATE — mirror verbatim; only `name` field changes from `@engram/ai-config` to `@engram/vectorize-utils`)
    - shared/ai-config/tsconfig.json (4-line file; mirror verbatim)
    - packages/mcp-server/src/vectorize-helper.ts lines 1-99 (source for `vectorizeQuery` extraction — preserve `assertNamespace` 64-byte guard, `VECTORIZE_TOPK_DEFAULT = 25`, `returnMetadata: "all"` default, conditional `filter` spread)
    - .planning/phases/02-recall-quality-baseline/02-PATTERNS.md §"shared/vectorize-utils/src/index.ts" (lines 256-330 — full file contracts for both exports including the `vectorizeNeighbors` over-fetch + threshold + slice pattern)
    - .planning/phases/02-recall-quality-baseline/02-RESEARCH.md §"Pattern 4: shared/vectorize-utils/ Package Shape" (lines 454-568 — Vectorize filter operator semantics + Context7 verification facts)
  </read_first>
  <action>
    Create the new workspace package at `shared/vectorize-utils/` mirroring the `shared/ai-config/` shape per D-08.

    1. `shared/vectorize-utils/package.json` — mirror `shared/ai-config/package.json` verbatim with `name: "@engram/vectorize-utils"`. Keep `version: "0.1.0"`, `private: true`, `type: "module"`, `exports`, `files: ["src"]`.

    2. `shared/vectorize-utils/tsconfig.json` — copy `shared/ai-config/tsconfig.json` verbatim (4 lines: `extends: "../../tsconfig.base.json"`, `include: ["src/**/*.ts"]`).

    3. `shared/vectorize-utils/src/index.ts` — export BOTH:
       - `vectorizeQuery` extracted VERBATIM from `packages/mcp-server/src/vectorize-helper.ts` lines 39-99. Preserve every line of `NAMESPACE_MAX_BYTES = 64`, `VECTORIZE_TOPK_DEFAULT = 25`, `assertNamespace()`, and the `vectorizeQuery` body. Pre-async synchronous throw discipline must remain identical.
       - `vectorizeNeighbors` — NEW. Signature: `export function vectorizeNeighbors(env: { VECTORIZE: VectorizeIndex }, workspaceId: string, vector: number[], opts: VectorizeNeighborsOpts): Promise<VectorizeMatches["matches"]>` where `VectorizeNeighborsOpts = { topK: number; type?: string; scope?: string; threshold: number }`. Implementation: build a `filter` object as `{ type: { $in: [opts.type] } }` if `opts.type` is defined, AND with `{ scope: { $eq: opts.scope } }` if `opts.scope` is defined (these stack as implicit AND per Cloudflare Vectorize docs). Call `vectorizeQuery(env, workspaceId, vector, { topK: opts.topK * VECTORIZE_OVERFETCH_FACTOR, returnMetadata: "all", ...(Object.keys(filter).length > 0 ? { filter } : {}) })`. Filter result matches by `m.score >= opts.threshold` (client-side — Vectorize has NO native score floor per Context7 verification in RESEARCH §Pattern 4). Slice to `opts.topK`.
       - Import `VECTORIZE_OVERFETCH_FACTOR` from `@engram/ai-config`.
       - File-header comment must cite D-08 + D-10, name `vectorize-helper.ts` as the extraction source, and call out the AI-02 cross-workspace isolation lock that motivates the mandatory positional `workspaceId` argument (do not relegate it to a filter-object key — RESEARCH §Pitfall 4).
       - Do NOT include `vectorizeUpsert` or `vectorizeDelete` — those stay in `packages/mcp-server/src/vectorize-helper.ts` (not needed by triage-worker per RESEARCH §"Pattern 4 → ADAPT").

    DO NOT yet update `packages/mcp-server/src/tools.ts` to import from the new package — that is Plan 02-02's job (per D-09 ordering). DO NOT delete `vectorizeQuery` from `packages/mcp-server/src/vectorize-helper.ts` in this plan — Plan 02-02 owns the import swap and the source removal happens after the swap lands cleanly.

    Append a routing-tracker row to `02-CF-CODE-ASSIST-USAGE.md` for this task per D-19 (3-question checklist, route decision recorded BEFORE this task's commit).
  </action>
  <verify>
    <automated>cd shared/vectorize-utils && test -f package.json && test -f tsconfig.json && test -f src/index.ts && grep -q '"@engram/vectorize-utils"' package.json && grep -q 'export function vectorizeQuery' src/index.ts && grep -q 'export function vectorizeNeighbors' src/index.ts && grep -q 'assertNamespace' src/index.ts && grep -q 'VECTORIZE_OVERFETCH_FACTOR' src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `shared/vectorize-utils/package.json` exists and `npm install` (when run) auto-links via workspaces. JSON parses cleanly.
    - File `shared/vectorize-utils/src/index.ts` exports `vectorizeQuery` (identical-behavior extraction) AND `vectorizeNeighbors` (new).
    - `vectorizeQuery` body in the new file is BYTE-FOR-BYTE identical to the original except for module-scope imports — diff vs `packages/mcp-server/src/vectorize-helper.ts:39-99` shows zero behavior changes.
    - `vectorizeNeighbors` signature exactly matches: `(env, workspaceId, vector, opts: { topK, type?, scope?, threshold })`. `workspaceId` is non-optional positional (compile-time defense per RESEARCH §Pitfall 4).
    - `vectorizeNeighbors` calls `vectorizeQuery` internally with `topK: opts.topK * VECTORIZE_OVERFETCH_FACTOR` (over-fetch pattern from `tools.ts:556`).
    - Client-side filter applied via `result.matches.filter(m => m.score >= opts.threshold).slice(0, opts.topK)` — never relies on a Vectorize-side score floor.
    - `tsconfig.json` extends `../../tsconfig.base.json` and includes `src/**/*.ts`.
    - `tsc --noEmit` from monorepo root succeeds (no new TS errors introduced).
    - `02-CF-CODE-ASSIST-USAGE.md` has a routing-tracker row for this task with Q1/Q2/Q3 answered (the package.json + tsconfig.json portion is `cf-code-assist:generateCode`-eligible per D-19, the `vectorizeNeighbors` body is small enough to fall in the gray zone — executor records the actual decision).
  </acceptance_criteria>
  <done>Both helpers exported from `@engram/vectorize-utils`, `assertNamespace` 64-byte guard preserved, client-side threshold filter applied, AI-02 namespace discipline preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wire consumers + create sync script + vendor corpus (D-13, D-12)</name>
  <files>scripts/sync-eval-corpus.mjs, packages/mcp-server/package.json, packages/triage-worker/package.json, packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json</files>
  <read_first>
    - scripts/eval-budget-summary.mjs lines 1-60 (CLI header / arg-parse / TAG / exit-code convention to mirror)
    - .planning/evals/apply-split.mjs lines 1-30 (`import.meta.url` → `__dirname` path-resolution pattern + JSON read/write)
    - packages/mcp-server/package.json (current shape; add `pretest:eval` script + `@engram/vectorize-utils` dep)
    - packages/triage-worker/package.json (current shape; add `@engram/vectorize-utils` dep — no `pretest:eval` here)
    - .planning/evals/recall-corpus.json (authoritative source per D-11 — the file `sync-eval-corpus.mjs` copies FROM; verify it exists and has `corpus_version: 2` + 100 entries)
    - .planning/phases/02-recall-quality-baseline/02-PATTERNS.md §"scripts/sync-eval-corpus.mjs" (lines 162-211 — full header + arg-parse skeleton + `--check` mode discipline)
    - packages/mcp-server/src/__tests__/evals/fixtures/real-corpus.json (existing 27-entry fixture; sibling for the vendored copy — KEEP this in place per D-14, do not modify)
  </read_first>
  <action>
    Build the corpus-sync plumbing per D-12 + D-13 and wire both consumer packages to `@engram/vectorize-utils`.

    1. Create `scripts/sync-eval-corpus.mjs` (Node ESM script, no Cloudflare creds needed):
       - Header comment cites D-13 + D-11 + D-12. Usage line: `node scripts/sync-eval-corpus.mjs [--check] [--help]`.
       - Source: `.planning/evals/recall-corpus.json` (resolved via `import.meta.url → __dirname → ../../.planning/evals/recall-corpus.json`).
       - Target: `packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json` (resolved relative to repo root).
       - Default mode: read source JSON, attach `_auto_synced_from: ".planning/evals/recall-corpus.json"` as a top-level sentinel field at the wrapper (JSON does not support comments — sentinel field is the convention per PATTERNS.md §"recall-corpus-v2.json"), write target.
       - `--check` mode: read both files, compare contents (after normalizing the sentinel field on the target side), exit 1 if they differ instead of overwriting. Used by CI to detect drift.
       - Exit codes (mirror `eval-budget-summary.mjs`): 0 success, 1 drift in `--check` mode, 2 source missing.
       - Use `TAG = "[sync-eval-corpus]"` for log lines per the established CLI convention.

    2. Run `node scripts/sync-eval-corpus.mjs` once to produce the initial `packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json`. Verify it has `corpus_version: 2`, `embedding_model: "@cf/qwen/qwen3-embedding-0.6b"`, 100 entries, and 70/30 train/validate split — these are the D-15 + RNK-01 inputs.

    3. Edit `packages/mcp-server/package.json`:
       - Add to `dependencies` (or wherever `@engram/ai-config` lives — same section): `"@engram/vectorize-utils": "*"`.
       - Add `"pretest:eval": "node ../../scripts/sync-eval-corpus.mjs"` to `scripts` (npm runs `pretest:<name>` automatically before `test:<name>` per the Phase 1 PRE-02 tier convention).

    4. Edit `packages/triage-worker/package.json`:
       - Add `"@engram/vectorize-utils": "*"` to dependencies. No `pretest:eval` here — conflict-precision eval runs against the existing fixture, not the recall corpus.

    5. Run `npm install` at repo root to relink workspaces. Verify no errors.

    DO NOT modify `recall-f1.eval.test.ts` or remove existing 27-entry `real-corpus.json` / 20-entry `reference-corpus.json` (D-14 keeps them as the RNK-06 baseline regression check).

    Append routing-tracker rows: `sync-eval-corpus.mjs` is on D-19's `generateCode` candidate list (Q1=N, Q2=N — under 50 LOC, but Q3=Y stable template); package.json edits are sub-15-line edits.
  </action>
  <verify>
    <automated>test -f scripts/sync-eval-corpus.mjs && node scripts/sync-eval-corpus.mjs --check && test -f packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json && grep -q 'corpus_version' packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json && grep -q '"@engram/vectorize-utils"' packages/mcp-server/package.json && grep -q '"@engram/vectorize-utils"' packages/triage-worker/package.json && grep -q '"pretest:eval"' packages/mcp-server/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/sync-eval-corpus.mjs` exists, is executable as `node scripts/sync-eval-corpus.mjs`, supports `--check` and `--help` flags, exits 0 on identical files, 1 on drift, 2 on missing source.
    - Running the script (default mode) produces `packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json` matching `.planning/evals/recall-corpus.json` byte-for-byte except for the `_auto_synced_from` sentinel field.
    - Running again with `--check` after a fresh sync exits 0 (no drift).
    - `jq '.corpus_version' packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json` returns `2`.
    - `jq '.entries | length' packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json` returns at least `100`.
    - `jq '.embedding_model' packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json` returns `"@cf/qwen/qwen3-embedding-0.6b"` (matches `EMBEDDING_MODEL` constant exactly — fail-fast safety per the existing `recall-f1.eval.test.ts` header).
    - `packages/mcp-server/package.json` declares `@engram/vectorize-utils: "*"` and has a `pretest:eval` script invoking the sync.
    - `packages/triage-worker/package.json` declares `@engram/vectorize-utils: "*"`.
    - Existing fixtures `real-corpus.json` (27 entries) and `reference-corpus.json` (20 entries) are UNTOUCHED.
    - `npm install` at repo root completes without errors; `npm ls @engram/vectorize-utils` shows both packages linking to `shared/vectorize-utils`.
    - `02-CF-CODE-ASSIST-USAGE.md` has tracker rows for the script + package.json edits.
  </acceptance_criteria>
  <done>Corpus syncer in place + ran successfully, vendored 100-entry corpus committed, both Workers declare the new shared dep, `pretest:eval` hook wires the sync into eval runs.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold 02-CF-CODE-ASSIST-USAGE.md routing tracker (D-19)</name>
  <files>.planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md</files>
  <read_first>
    - .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md (CANONICAL TEMPLATE — copy verbatim with phase-number swap)
    - CLAUDE.md §"AI Model Routing" + §"Phase character heuristic" (Phase 2 is mixed character: contract-integration on CON + content-generation on RNK)
    - .planning/phases/02-recall-quality-baseline/02-CONTEXT.md §D-19 (lists 5 candidate task shapes that default to cf-code-assist + 3 task shapes that stay with Claude)
    - .planning/phases/02-recall-quality-baseline/02-RESEARCH.md §"cf-code-assist Routing — Additional Candidate Task Shapes" (lines 1159-1177 — 3 additional candidates beyond D-19's list)
  </read_first>
  <action>
    Create `.planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md` by copying `.planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md` and applying these adaptations per PATTERNS.md §"02-CF-CODE-ASSIST-USAGE.md":

    - Header: "# Phase 2 — cf-code-assist Routing Tracker (v0.2 milestone)".
    - Scope statement: change "Phase 1" → "Phase 2 (Recall Quality Baseline — RNK + CON workstreams)". Stop-trigger reference: `/gsd:verify-work 2`.
    - Phase character paragraph: change "<10% foundation" expectation to "15–30% mixed character — higher than Phase 1's <10%, lower than Phase 5's 40–60% AI-integration". Cite CLAUDE.md's phase-character heuristic explicitly.
    - "Instructions for the executor" section: keep verbatim (Phase 1 lines 11-24).
    - "3-Question Checklist" section: keep verbatim (Phase 1 lines 27-37).
    - "Routing Log" table header: keep verbatim.
    - Add a "Phase 2 candidate task shapes" subsection BEFORE the seed row, copying the 5 candidates from CONTEXT.md D-19 (sync-eval-corpus.mjs, sweep test scaffold, shared/vectorize-utils extraction, hybrid-rank-changelog.md initial row, insertConflictAsInbox helper) + the 3 additional candidates from RESEARCH §"cf-code-assist Routing" (Pareto-front + metric helpers, this routing-tracker scaffold itself, vectorize-utils package.json+tsconfig.json) — as planning guidance, not pre-filled log rows.
    - Seed row: `| _seed_ | _(no rows yet — first executor task appends below this line)_ | _n/a_ | _n/a_ | _Tracking starts at execute-phase kickoff_ | _n/a_ |`.
    - Trailing "End-of-Phase Summary" placeholder section: `_TBD — populated at /gsd:verify-work 2 close._`.

    The file is documentation, so the routing decision for this scaffold-creation task itself is `claude` (Q1=N, Q2=N, Q3=Y — the canonical template is the anchor — but Q2 says under 50 lines mechanical, so context-prep beats savings; the doc-on-doc routing for the tracker itself was `claude` in Phase 1 row `01-04-T1` per the analog).

    Append a routing-tracker row at the bottom of the file (after the seed row) for THIS task, mirroring Phase 1's `01-04-T1` row format (`claude | N/N/N | Doc creation, not code generation`).
  </action>
  <verify>
    <automated>test -f .planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md && grep -q '# Phase 2 — cf-code-assist Routing Tracker' .planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md && grep -q '3-Question Checklist' .planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md && grep -q '/gsd:verify-work 2' .planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md && grep -q 'Routing Log' .planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md`.
    - Contains "# Phase 2 — cf-code-assist Routing Tracker (v0.2 milestone)" exactly as first heading.
    - Contains the 3-question checklist section verbatim from Phase 1 (text identity preserved — `grep -F` against Phase 1 file finds the checklist text intact).
    - Contains a "Routing Log" markdown table with the standard 6-column header (Task | Artifact | Route | Checklist (Q1/Q2/Q3) | Reason | Approx tokens saved).
    - Contains the seed `| _seed_ |` row.
    - Contains the candidate-task-shapes guidance subsection enumerating the 5 D-19 candidates + 3 RESEARCH-additional candidates.
    - Scope statement references `/gsd:verify-work 2` (not 1 or any other phase number).
    - Trailing "End-of-Phase Summary" placeholder exists.
    - File contains a tracker row for this very task (`02-01-T3` or equivalent) with route=claude per the doc-on-doc analog.
  </acceptance_criteria>
  <done>Tracker scaffold ready; every subsequent code-producing task in Phase 2 has a place to append a row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `vectorize-utils` API → `env.VECTORIZE` | Worker → Cloudflare Vectorize binding crossing into CF internal mTLS plane |
| `sync-eval-corpus.mjs` filesystem operations | Dev/CI host filesystem; only reads from `.planning/evals/` + writes to `packages/mcp-server/src/__tests__/evals/fixtures/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | `vectorizeNeighbors` cross-workspace leak via missing namespace | mitigate | `workspaceId` is non-optional positional arg (compile-time defense per RESEARCH §Pitfall 4); `assertNamespace` throws SYNCHRONOUSLY before any binding call; extraction preserves this discipline verbatim |
| T-02-01-02 | Information Disclosure | Namespace > 64 bytes silently truncated by Vectorize (Pitfall 9) | mitigate | `assertNamespace` 64-byte UTF-8 guard preserved from `vectorize-helper.ts:50-60` extraction |
| T-02-01-03 | Tampering | Sentinel `_auto_synced_from` field could be tampered to silently desync the vendored fixture | mitigate | `--check` mode in `sync-eval-corpus.mjs` detects drift; CI runs it as a guard; sentinel field is documented |
| T-02-01-SC | Tampering | npm install of new workspace package | accept | No new external npm packages — `@engram/vectorize-utils` is a workspace-local package linked via `npm workspaces`. Slopcheck N/A per RESEARCH §"Package Legitimacy Audit" |
</threat_model>

<verification>
- Both helpers exported from `@engram/vectorize-utils` and TypeScript compiles cleanly with `npm run typecheck` (or equivalent) at repo root.
- `node scripts/sync-eval-corpus.mjs --check` exits 0 immediately after a fresh sync.
- Routing-tracker file exists and is structurally identical to Phase 1's pattern (sections present, columns intact).
- Both consumer packages have the new dep declaration AND `npm ls @engram/vectorize-utils` resolves to the workspace package.
- All v0.1 + Phase 1 tests still pass: `cd packages/mcp-server && npm test -- --project=workerd` returns green (no consumer changes yet, just dep addition — should be a no-op).
</verification>

<success_criteria>
- `shared/vectorize-utils/{package.json,tsconfig.json,src/index.ts}` committed
- `scripts/sync-eval-corpus.mjs` committed AND run once (vendored fixture committed)
- `packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json` committed, contains 100 entries with 70/30 split
- Both consumer `package.json` files declare `@engram/vectorize-utils: "*"` AND `mcp-server` has `pretest:eval`
- `.planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md` scaffolded; first 3 rows appended for the Wave 0 tasks
- No production code path consumes the new helpers YET (that's Plan 02-02's job per D-09) — this plan is pure scaffolding
- Wave 0 unblocks Plan 02-02 (parameterization + import swap) and Plan 02-06 (conflict-pipeline)
</success_criteria>

<output>
Create `.planning/phases/02-recall-quality-baseline/02-01-SUMMARY.md` when done. Summary must list: package files created, sync-script behavior, the vendored corpus entry count + embedding-model field value, both consumer package.json edits, and confirm the routing tracker is ready for downstream rows.
</output>
