---
phase: 02-recall-quality-baseline
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - shared/ai-config/src/index.ts
  - packages/mcp-server/src/hybrid-rank.ts
  - packages/mcp-server/src/__tests__/hybrid-rank.test.ts
  - packages/mcp-server/src/tools.ts
  - packages/mcp-server/src/vectorize-helper.ts
  - packages/mcp-server/src/__tests__/lint-no-direct-vectorize.test.ts
  - .planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md
autonomous: true
requirements:
  - RNK-05
tags:
  - rnk
  - refactor
  - shared-config
linear_subissue: rnk
must_haves:
  truths:
    - "`HYBRID_WEIGHTS` lives in `@engram/ai-config` with the `rerank` key (was `cosine`)."
    - "`hybridRank` accepts an optional `weights` parameter defaulting to `HYBRID_WEIGHTS` — no behavior change to existing production callers."
    - "Audit comment placeholder exists in `shared/ai-config/src/index.ts` near `HYBRID_WEIGHTS` (final values filled in by Plan 02-03 after sweep runs)."
    - "`tools.ts` `recall()` handler imports `vectorizeQuery` from `@engram/vectorize-utils` (not `./vectorize-helper.js`)."
    - "All existing tests still pass: `npm test -- --project=workerd` and `--project=lint-node` both green."
  artifacts:
    - path: "shared/ai-config/src/index.ts"
      provides: "HYBRID_WEIGHTS literal moved here with renamed key + audit-comment placeholder"
      contains: "HYBRID_WEIGHTS"
    - path: "packages/mcp-server/src/hybrid-rank.ts"
      provides: "hybridRank parameterized on weights; imports HYBRID_WEIGHTS from @engram/ai-config"
      contains: "weights: HybridWeights = HYBRID_WEIGHTS"
    - path: "packages/mcp-server/src/tools.ts"
      provides: "recall() imports vectorizeQuery from @engram/vectorize-utils"
      contains: "@engram/vectorize-utils"
  key_links:
    - from: "packages/mcp-server/src/hybrid-rank.ts"
      to: "shared/ai-config/src/index.ts"
      via: "import { HYBRID_WEIGHTS } from '@engram/ai-config'"
      pattern: "@engram/ai-config"
    - from: "packages/mcp-server/src/tools.ts"
      to: "shared/vectorize-utils/src/index.ts"
      via: "import { vectorizeQuery } from '@engram/vectorize-utils'"
      pattern: "@engram/vectorize-utils"
---

<objective>
Restructure the hybrid-rank surface so the 625-config sweep in Plan 02-03 can drive `hybridRank()` with per-config weights without duplicating the formula. Move the `HYBRID_WEIGHTS` literal from `hybrid-rank.ts` into `shared/ai-config/src/index.ts` (single source of truth per ENG-25 convention), rename `HYBRID_WEIGHTS.cosine` → `HYBRID_WEIGHTS.rerank` per D-05, install a D-06 audit-comment PLACEHOLDER (final values land in Plan 02-03 after the sweep), parameterize `hybridRank()` to accept `weights: HybridWeights = HYBRID_WEIGHTS`, rename the local `cosine` variable to `rerank` per D-07, and swap the `tools.ts` recall handler's `vectorizeQuery` import from `./vectorize-helper.js` to `@engram/vectorize-utils` per D-09.

Purpose: D-05 + D-07 + D-09 unblock the sweep (Plan 02-03) and align Phase 2 with Phase 3's EXP-06 score-source swap. The audit comment is the cross-phase contract bridge — Phase 3 readers depend on the comment text to know which score source feeds the rerank weight. This plan installs the COMMENT STRUCTURE with placeholder values; Plan 02-03 fills in the actual F1/MRR/top-1 numbers after the sweep.

Output:
- `shared/ai-config/src/index.ts` — gains `HYBRID_WEIGHTS` export (`rerank` key, NOT `cosine`); D-06 audit comment with placeholder sweep date + scores
- `packages/mcp-server/src/hybrid-rank.ts` — imports `HYBRID_WEIGHTS` from `@engram/ai-config`; local `cosine` variable renamed to `rerank`; `hybridRank()` gains 5th param `weights = HYBRID_WEIGHTS`
- `packages/mcp-server/src/__tests__/hybrid-rank.test.ts` — updated to import `HYBRID_WEIGHTS` from new location; assertions reference `weights.rerank`
- `packages/mcp-server/src/tools.ts` — `vectorizeQuery` import swap to `@engram/vectorize-utils`
- `packages/mcp-server/src/vectorize-helper.ts` — `vectorizeQuery` REMOVED (now lives in shared); `vectorizeUpsert` + `vectorizeDelete` stay
- `packages/mcp-server/src/__tests__/lint-no-direct-vectorize.test.ts` — update HELPER_FILE allowlist to include `@engram/vectorize-utils` import sites if needed

This plan changes ZERO production behavior — all existing tests must still pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-recall-quality-baseline/02-CONTEXT.md
@.planning/phases/02-recall-quality-baseline/02-RESEARCH.md
@.planning/phases/02-recall-quality-baseline/02-PATTERNS.md
@.planning/phases/02-recall-quality-baseline/02-01-SUMMARY.md
@shared/ai-config/src/index.ts
@packages/mcp-server/src/hybrid-rank.ts

<interfaces>
<!-- Current production shape — required reading for the executor before any edit -->

From `packages/mcp-server/src/hybrid-rank.ts` (current):
```typescript
export const HYBRID_WEIGHTS = {
  cosine: 1.0,
  recency: 0.15,
  type_match: 0.2,
  scope_match: 0.15,
} as const;

export function hybridRank(
  matches: VectorizeMatches["matches"],
  blocks: LexicalSearchHit[],
  args: Partial<RecallInput>,
  now: number = Date.now(),
): LexicalSearchHit[];
```

The formula inside (line 120-124):
```
const _score =
  HYBRID_WEIGHTS.cosine * cosine +
  HYBRID_WEIGHTS.recency * recency +
  HYBRID_WEIGHTS.type_match * type_match +
  HYBRID_WEIGHTS.scope_match * scope_match;
```

From `shared/ai-config/src/index.ts` audit-comment convention (existing example near `MIN_COSINE_THRESHOLD`):
- JSDoc block explaining purpose + tuning provenance
- Tuned-date + corpus reference inline
- "If you swap X you MUST retune" follow-up note pattern

From `packages/mcp-server/src/tools.ts` (recall handler — the import to swap is at the top of file):
```typescript
import { vectorizeQuery } from "./vectorize-helper.js";
// ... ~line 562:
const result = await vectorizeQuery(env, props.workspace_id, queryVector, { ... });
```

D-06 audit-comment VERBATIM TEXT (this is a byte-frozen contract — single-character drift breaks Phase 3 EXP-06 reading-comprehension):
```
// v0.2 Phase 2: `rerank` weight values tuned against RAW COSINE (`match.score` from Vectorize).
// bge-reranker invocation lands in Phase 3 (EXP-06). Until then, `HYBRID_WEIGHTS.rerank * match.score`
// means "raw-cosine weighted by the tuned rerank weight." Do NOT read `HYBRID_WEIGHTS.rerank` as
// "reranker active" in v0.2.
// Corpus: .planning/evals/recall-corpus.json (100 entries, qwen3-embedding-0.6b, sweep date YYYY-MM-DD)
// Scores: F1=X.XX, MRR=X.XX, top1=X.XX
// Re-tune at v0.3 when corpus grows.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move HYBRID_WEIGHTS to @engram/ai-config + install D-06 audit comment placeholder (D-05, D-06)</name>
  <files>shared/ai-config/src/index.ts, packages/mcp-server/src/hybrid-rank.ts, packages/mcp-server/src/__tests__/hybrid-rank.test.ts</files>
  <read_first>
    - shared/ai-config/src/index.ts (current file end-to-end; identify the section near `MIN_COSINE_THRESHOLD` lines 96-113 where the new export will live — the existing audit-comment convention is the model)
    - packages/mcp-server/src/hybrid-rank.ts lines 35-49 (current `HYBRID_WEIGHTS` literal to relocate) + lines 117-125 (current formula `HYBRID_WEIGHTS.cosine * cosine`)
    - packages/mcp-server/src/__tests__/hybrid-rank.test.ts (whole file; identify every assertion that references `HYBRID_WEIGHTS.cosine` — must update to `.rerank`)
    - .planning/phases/02-recall-quality-baseline/02-CONTEXT.md §D-06 (VERBATIM AUDIT COMMENT — byte-frozen contract; reproduce exactly, with placeholder `YYYY-MM-DD` for sweep date and `X.XX` placeholders for scores — Plan 02-03 fills in the real numbers)
    - .planning/phases/02-recall-quality-baseline/02-PATTERNS.md §"shared/ai-config/src/index.ts" lines 333-401 (full audit-comment shape including the JSDoc wrapper)
  </read_first>
  <action>
    Relocate `HYBRID_WEIGHTS` to `shared/ai-config/src/index.ts` and apply the D-05 key rename + D-06 audit comment.

    1. In `shared/ai-config/src/index.ts`:
       - Add a new export `HYBRID_WEIGHTS` AFTER the `VECTORIZE_OVERFETCH_FACTOR` declaration (line 121 area) — keep the file's existing "Role-named aliases" section after it intact.
       - Wrap the literal in a JSDoc block matching the existing `MIN_COSINE_THRESHOLD` audit-comment convention (the JSDoc-style block at lines 96-113). The JSDoc block contains:
         * One-line summary: "Hybrid-rank component weights — single source of truth for both Workers."
         * Then the VERBATIM D-06 multi-line audit comment as documented in `<interfaces>` above (use the `//`-prefixed lines or convert to JSDoc body — but the EXACT TEXT of each of the 7 marker lines must be preserved byte-for-byte: starting with "v0.2 Phase 2: `rerank` weight values tuned against RAW COSINE" and ending with "Re-tune at v0.3 when corpus grows.").
         * For this plan, use placeholders: sweep date = `YYYY-MM-DD`, F1/MRR/top1 = `X.XX`. Plan 02-03 will edit these in-place with real numbers from the sweep run.
       - The literal itself:
         ```typescript
         export const HYBRID_WEIGHTS = {
           rerank: 1.0,        // D-05 rename from `cosine`; placeholder pre-tuning value (Plan 02-03 commits the tuned value)
           recency: 0.15,
           type_match: 0.2,
           scope_match: 0.15,
         } as const;
         ```
       - The CURRENT default values (1.0 / 0.15 / 0.2 / 0.15) are the v0.1 production weights — using them as the Plan 02-02 placeholder ensures zero behavior change until Plan 02-03 commits tuned values.
       - Also add `export type HybridWeights = typeof HYBRID_WEIGHTS;` immediately below the literal so consumers can type-check the parameterization in Task 2.

    2. In `packages/mcp-server/src/hybrid-rank.ts`:
       - DELETE the local `HYBRID_WEIGHTS` declaration (lines ~35-49 including the JSDoc).
       - Add `import { HYBRID_WEIGHTS, type HybridWeights } from "@engram/ai-config";` at the top of the file.
       - Re-export for backwards compatibility with any test that imports from this path: `export { HYBRID_WEIGHTS } from "@engram/ai-config";` (one-liner). This preserves the test's existing `import { HYBRID_WEIGHTS } from "../hybrid-rank.js"` while moving the source of truth.
       - In the formula at line ~120-124, change `HYBRID_WEIGHTS.cosine` to `HYBRID_WEIGHTS.rerank` and rename the local variable `const cosine = match.score;` to `const rerank = match.score;` per D-07. Update the inline comment `// ---- Component: cosine (already normalized to [0..1] by Vectorize) ----` to `// ---- Component: rerank (raw cosine in v0.2; bge-reranker score in Phase 3 — see HYBRID_WEIGHTS audit comment) ----`.
       - The formula MUST stay structurally identical per D-07: `weights.rerank * rerank + weights.recency * recency + weights.type_match * type_match + weights.scope_match * scope_match`. Same operators, same component order, same coefficient pattern.
       - Update the file-header JSDoc lines 35-43 — remove the "starting weights from AI-SPEC.md §4" note and replace with "Weights live in `@engram/ai-config` (single source of truth per ENG-25). v0.2 Phase 2 renamed `cosine` → `rerank` per D-05; bge-reranker invocation lands Phase 3."

    3. In `packages/mcp-server/src/__tests__/hybrid-rank.test.ts`:
       - Find every reference to `HYBRID_WEIGHTS.cosine` and rename to `HYBRID_WEIGHTS.rerank`.
       - Find every reference to the local `cosine` variable in test assertions (if any in the per-component math checks) — these are expected/calculated values, not function exports, so they may or may not need renaming depending on the test's existing wording. Update for clarity.
       - The test's import line stays as `import { HYBRID_WEIGHTS } from "../hybrid-rank.js"` because of the re-export added in step 2.
       - Run the test — must still pass with the renamed key (the formula's numerical behavior is identical when `rerank` has the same value as the old `cosine`).

    Authorial discipline (D-19): the audit comment text is BYTE-FROZEN. cf-code-assist routing for this task is `claude` (Q1=Y — cross-file synthesis touching ai-config + hybrid-rank + test). Append a tracker row.
  </action>
  <verify>
    <automated>grep -q 'HYBRID_WEIGHTS' shared/ai-config/src/index.ts && grep -q 'rerank' shared/ai-config/src/index.ts && grep -q 'v0.2 Phase 2:' shared/ai-config/src/index.ts && grep -q 'export type HybridWeights' shared/ai-config/src/index.ts && ! grep 'cosine: 1.0' shared/ai-config/src/index.ts && grep -q '@engram/ai-config' packages/mcp-server/src/hybrid-rank.ts && grep -q 'HYBRID_WEIGHTS.rerank' packages/mcp-server/src/hybrid-rank.ts && ! grep 'HYBRID_WEIGHTS.cosine' packages/mcp-server/src/hybrid-rank.ts && cd packages/mcp-server && npm test -- --project=workerd hybrid-rank.test.ts --run 2>&1 | tail -5 | grep -E '(PASS|passed)'</automated>
  </verify>
  <acceptance_criteria>
    - `shared/ai-config/src/index.ts` exports `HYBRID_WEIGHTS` AND `HybridWeights` type.
    - `HYBRID_WEIGHTS` has keys `{ rerank, recency, type_match, scope_match }` in that exact order. NO `cosine` key.
    - Numeric values in `HYBRID_WEIGHTS`: `rerank: 1.0, recency: 0.15, type_match: 0.2, scope_match: 0.15` (identical to pre-Phase-2 production weights — Plan 02-03 commits tuned values later).
    - JSDoc / audit comment block above `HYBRID_WEIGHTS` contains the 7 D-06 marker lines verbatim (with `YYYY-MM-DD` and `X.XX` placeholders for sweep results — placeholders are STAGE 1 of a 2-stage commit; Plan 02-03 fills them in).
    - `packages/mcp-server/src/hybrid-rank.ts` no longer declares the constant locally; it imports from `@engram/ai-config` and re-exports for backwards compat.
    - Formula at line ~120 reads `HYBRID_WEIGHTS.rerank * rerank + HYBRID_WEIGHTS.recency * recency + ...` (or equivalent referencing `weights.rerank` if Task 2 has landed first — but in a single-task PR for Task 1, the formula uses `HYBRID_WEIGHTS.rerank * rerank`).
    - Existing test `packages/mcp-server/src/__tests__/hybrid-rank.test.ts` PASSES with `npm test -- hybrid-rank.test.ts` (rename-only change; identical numerical behavior because `HYBRID_WEIGHTS.rerank` = old `HYBRID_WEIGHTS.cosine` = 1.0).
    - `grep -F 'HYBRID_WEIGHTS.cosine' packages/mcp-server/ shared/` returns ZERO matches across all .ts files.
    - `tsc --noEmit` at repo root: zero new TS errors.
    - `02-CF-CODE-ASSIST-USAGE.md` has a tracker row for this task: route=claude, Q1=Y/Q2=N/Q3=N, reason "cross-file synthesis + byte-frozen audit comment".
  </acceptance_criteria>
  <done>HYBRID_WEIGHTS migrated to shared/ai-config with D-06 audit-comment placeholder; hybrid-rank.ts uses the renamed key; existing hybrid-rank tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Parameterize hybridRank() to accept optional weights (RESEARCH §Pattern 1, Pitfall 1)</name>
  <files>packages/mcp-server/src/hybrid-rank.ts, packages/mcp-server/src/__tests__/hybrid-rank.test.ts</files>
  <read_first>
    - packages/mcp-server/src/hybrid-rank.ts (post-Task-1 state — needs the 5th parameter added; current 4-arg signature at line 77-82)
    - packages/mcp-server/src/__tests__/hybrid-rank.test.ts (full file; identify the test cases that already exercise the default-weights path; add ONE NEW TEST that exercises a custom-weights argument)
    - .planning/phases/02-recall-quality-baseline/02-RESEARCH.md §"Open Question 1" lines 1019-1024 (researcher recommendation: parameterize on weights with `HYBRID_WEIGHTS` as default; zero behavior change to production callers — this is the recommended approach the planner adopted)
    - .planning/phases/02-recall-quality-baseline/02-RESEARCH.md §"Pitfall 1" lines 703-714 (`HYBRID_WEIGHTS as const` blocks the sweep — this parameterization is the documented mitigation)
  </read_first>
  <action>
    Extend the `hybridRank()` signature with an optional `weights: HybridWeights = HYBRID_WEIGHTS` 5th parameter so Plan 02-03's sweep can drive it with per-config weights without touching the formula. Production callers pass nothing and get the v0.1 behavior unchanged.

    1. In `packages/mcp-server/src/hybrid-rank.ts`:
       - Update the function signature to:
         ```typescript
         export function hybridRank(
           matches: VectorizeMatches["matches"],
           blocks: LexicalSearchHit[],
           args: Partial<RecallInput>,
           now: number = Date.now(),
           weights: HybridWeights = HYBRID_WEIGHTS,
         ): LexicalSearchHit[]
         ```
       - In the formula, replace `HYBRID_WEIGHTS.rerank` with `weights.rerank` (and `HYBRID_WEIGHTS.recency`, `.type_match`, `.scope_match` with their `weights.*` equivalents). The local `rerank`/`recency`/etc. variable names stay unchanged.
       - Update the function-level JSDoc to document the new parameter: "@param weights - Hybrid-rank component weights. Defaults to `HYBRID_WEIGHTS` from `@engram/ai-config`. Plan 02-03's 625-config sweep passes per-config weights to drive grid evaluation without duplicating the formula. Zero behavior change to production callers."
       - The `HybridWeights` type was already exported from `@engram/ai-config` in Task 1 and imported at the top of this file.

    2. In `packages/mcp-server/src/__tests__/hybrid-rank.test.ts`:
       - Add ONE new test case: "uses caller-supplied weights when the 5th argument is provided" — call `hybridRank(matches, blocks, args, now, { rerank: 0.5, recency: 0.5, type_match: 0.0, scope_match: 0.0 })` against a small fixture, assert that the computed `_score` matches the expected value with those weights (math: `0.5 * cosine + 0.5 * recency + 0 + 0`). This proves the parameterization works without yet running the full sweep.
       - All EXISTING tests must still pass unchanged — they don't supply the 5th arg, so they get `HYBRID_WEIGHTS` from `@engram/ai-config` by default.

    3. Run `npm test -- --project=workerd hybrid-rank.test.ts` and confirm green.

    Routing per D-19: this is a public-API contract change to a shipped function; researchers flagged it as "Stays with Claude" (cross-file impact reasoning). Append tracker row.
  </action>
  <verify>
    <automated>cd packages/mcp-server && grep -q 'weights: HybridWeights = HYBRID_WEIGHTS' src/hybrid-rank.ts && grep -q 'weights.rerank' src/hybrid-rank.ts && grep -q 'weights.recency' src/hybrid-rank.ts && grep -q 'weights.type_match' src/hybrid-rank.ts && grep -q 'weights.scope_match' src/hybrid-rank.ts && npm test -- --project=workerd hybrid-rank.test.ts --run 2>&1 | tail -5 | grep -E '(PASS|passed)'</automated>
  </verify>
  <acceptance_criteria>
    - `hybridRank` signature has 5 parameters; 5th is `weights: HybridWeights = HYBRID_WEIGHTS`.
    - Formula inside the loop uses `weights.rerank`, `weights.recency`, `weights.type_match`, `weights.scope_match` (NOT `HYBRID_WEIGHTS.*` directly — that's the parameterization point).
    - Default-arg behavior is preserved: callers passing only the existing 4 args produce identical output to pre-Task-2.
    - New test case calling `hybridRank(..., { rerank: 0.5, recency: 0.5, type_match: 0, scope_match: 0 })` produces scores computed with those weights (verifiable arithmetically against a fixed fixture).
    - All pre-existing tests in `hybrid-rank.test.ts` pass.
    - `tools.ts` recall handler does NOT need to update its call site — it still passes 4 args and gets default weights.
    - `02-CF-CODE-ASSIST-USAGE.md` has a tracker row: route=claude, Q1=Y (public API contract change reasoning), Q2=N (~5 LOC), Q3=Y.
  </acceptance_criteria>
  <done>hybridRank accepts optional weights; production behavior unchanged; new test confirms parameterization; sweep test in Plan 02-03 can now drive it.</done>
</task>

<task type="auto">
  <name>Task 3: Swap tools.ts vectorizeQuery import to @engram/vectorize-utils + clean up vectorize-helper.ts (D-09)</name>
  <files>packages/mcp-server/src/tools.ts, packages/mcp-server/src/vectorize-helper.ts, packages/mcp-server/src/__tests__/vectorize-helper.test.ts, packages/mcp-server/src/__tests__/lint-no-direct-vectorize.test.ts</files>
  <read_first>
    - packages/mcp-server/src/tools.ts lines 1-50 (current imports — find the `vectorizeQuery` import and the `vectorizeUpsert` import; they may share a single line or be separate)
    - packages/mcp-server/src/vectorize-helper.ts (current full file; `vectorizeQuery` at lines 78-99 will be REMOVED after Plan 02-01 extracted it; `vectorizeUpsert` lines 119-135 and `vectorizeDelete` lines 149-159 STAY)
    - packages/mcp-server/src/__tests__/vectorize-helper.test.ts (existing tests; identify which assertions cover `vectorizeQuery` vs `vectorizeUpsert/Delete` — move the `vectorizeQuery` test coverage to the new package's test file OR keep here but update the import)
    - packages/mcp-server/src/__tests__/lint-no-direct-vectorize.test.ts lines 30-60 (HELPER_FILE allowlist — verify whether the lint grep needs `@engram/vectorize-utils` import sites added to the allowlist; if the lint targets literal `env.VECTORIZE.{query|upsert|deleteByIds}` strings only, no allowlist change needed)
    - shared/vectorize-utils/src/index.ts (post-Plan-02-01 — confirms `vectorizeQuery` is exported with identical behavior; Task 3 just swaps the consumer)
  </read_first>
  <action>
    Complete the D-09 import swap and remove the redundant `vectorizeQuery` from the mcp-server-local helper.

    1. In `packages/mcp-server/src/tools.ts`:
       - Find the import line(s) for `vectorizeQuery`/`vectorizeUpsert`/`vectorizeDelete` from `./vectorize-helper.js`. If a single import statement: split it. Change `vectorizeQuery` to import from `@engram/vectorize-utils`; keep `vectorizeUpsert` and `vectorizeDelete` imported from `./vectorize-helper.js`.
       - Resulting imports:
         ```typescript
         import { vectorizeQuery } from "@engram/vectorize-utils";
         import { vectorizeUpsert, vectorizeDelete } from "./vectorize-helper.js";
         ```
       - NO OTHER CHANGES to tools.ts in this plan. Recall handler call site stays exactly the same (`vectorizeQuery(env, props.workspace_id, queryVector, { ... })`) — same signature, same behavior.

    2. In `packages/mcp-server/src/vectorize-helper.ts`:
       - REMOVE the `vectorizeQuery` function (lines 62-99) AND its supporting constants `VECTORIZE_TOPK_DEFAULT` (line 44) and the `assertNamespace` function (lines 50-60) ONLY IF they are not used by `vectorizeUpsert`/`vectorizeDelete`. Re-read the post-removal file: `vectorizeUpsert` DOES call `assertNamespace` (line 124) and `vectorizeDelete` DOES call `assertNamespace` (line 154). So `assertNamespace` + `NAMESPACE_MAX_BYTES` MUST STAY. The `VECTORIZE_TOPK_DEFAULT` constant is only used by `vectorizeQuery` — DELETE it.
       - Update the file-header JSDoc: change "`vectorizeQuery` / `vectorizeUpsert` / `vectorizeDelete`" to "`vectorizeUpsert` / `vectorizeDelete`". Add a one-line note pointing to the new location: "`vectorizeQuery` moved to `@engram/vectorize-utils` in Phase 2 D-09; this module retains the upsert/delete writers."

    3. In `packages/mcp-server/src/__tests__/vectorize-helper.test.ts`:
       - Identify tests that exercise `vectorizeQuery` (likely covers namespace validation, topK default, returnMetadata default). These tests SHOULD MOVE to a new test file for the shared package: create `shared/vectorize-utils/src/index.test.ts` with the migrated test coverage (preserve all assertions; only the import path changes from `./vectorize-helper.js` to `./index.js`).
       - Alternative if creating a test in the shared package is structurally awkward (no existing test-runner config for `shared/*`): KEEP the `vectorizeQuery` tests in `packages/mcp-server/src/__tests__/vectorize-helper.test.ts` but change the import to `import { vectorizeQuery } from "@engram/vectorize-utils"`. The test still covers the same code, just imported from the new package — this is the simpler path. Choose this option.
       - Either way: ALL pre-existing test assertions must still pass.

    4. In `packages/mcp-server/src/__tests__/lint-no-direct-vectorize.test.ts`:
       - Re-read the lint logic. If it greps for literal strings `env.VECTORIZE.query`, `env.VECTORIZE.upsert`, `env.VECTORIZE.deleteByIds` and asserts they only appear inside the HELPER_FILE list — confirm the new `shared/vectorize-utils/src/index.ts` is added to the HELPER_FILE allowlist (because the extracted `vectorizeQuery` calls `env.VECTORIZE.query`).
       - If HELPER_FILE is hardcoded to a single path: update to an array including both `packages/mcp-server/src/vectorize-helper.ts` AND `shared/vectorize-utils/src/index.ts`.
       - Run the lint test — must pass.

    5. Run the full mcp-server workerd + lint-node test suites: `cd packages/mcp-server && npm test -- --project=workerd && npm test -- --project=lint-node`. All green.

    Routing per D-19: file-edit + ~10 lines of imports/JSDoc rewording = `claude` (Q1=Y mixed cross-file concern + test migration). Append tracker row.
  </action>
  <verify>
    <automated>cd packages/mcp-server && grep -q 'from "@engram/vectorize-utils"' src/tools.ts && ! grep 'export function vectorizeQuery' src/vectorize-helper.ts && grep -q 'export function vectorizeUpsert' src/vectorize-helper.ts && npm test -- --project=workerd --run 2>&1 | tail -10 | grep -iE '(pass|✓)'</automated>
  </verify>
  <acceptance_criteria>
    - `packages/mcp-server/src/tools.ts` imports `vectorizeQuery` from `@engram/vectorize-utils` (not `./vectorize-helper.js`).
    - `packages/mcp-server/src/tools.ts` still imports `vectorizeUpsert` and `vectorizeDelete` from `./vectorize-helper.js`.
    - `packages/mcp-server/src/vectorize-helper.ts` no longer exports `vectorizeQuery` (function definition removed).
    - `packages/mcp-server/src/vectorize-helper.ts` still exports `vectorizeUpsert` and `vectorizeDelete`; `assertNamespace` + `NAMESPACE_MAX_BYTES` retained because they are called from the surviving exports.
    - `packages/mcp-server/src/__tests__/vectorize-helper.test.ts` updated: `vectorizeQuery` tests now import from `@engram/vectorize-utils` and pass; `vectorizeUpsert`/`vectorizeDelete` tests unchanged.
    - `packages/mcp-server/src/__tests__/lint-no-direct-vectorize.test.ts` passes — the new shared module is on the HELPER_FILE allowlist if the lint required updating.
    - Full mcp-server `workerd` + `lint-node` test suites pass with no regressions.
    - `tsc --noEmit` at repo root succeeds.
    - `02-CF-CODE-ASSIST-USAGE.md` has a tracker row for this task.
  </acceptance_criteria>
  <done>Recall handler reads through `@engram/vectorize-utils`; `vectorize-helper.ts` slimmed to just upsert/delete; existing test coverage migrated cleanly; CI grep gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `recall()` handler → `vectorizeQuery` (now via shared package) | Unchanged — same workspace_id namespace discipline crossed; the boundary is preserved across the import swap |
| `hybridRank` `weights` parameter | Pure-math input from in-process eval test (Plan 02-03); no untrusted-user data flows through this parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | Audit comment text drift (single-character changes break Phase 3 EXP-06 reading) | mitigate | Comment text is BYTE-FROZEN per D-06; routing locked to `claude` for authoring; manual `/gsd:verify-work 2` review cross-references against CONTEXT.md D-06 verbatim |
| T-02-02-02 | Information Disclosure | Import-swap regresses cross-workspace isolation (vectorizeQuery loses `workspaceId` discipline) | mitigate | Shared-package `vectorizeQuery` was extracted byte-for-byte from the original (Plan 02-01 acceptance); `assertNamespace` 64-byte guard preserved; `workspaceId` remains non-optional positional argument; `lint-no-direct-vectorize` test asserts the only paths to `env.VECTORIZE.query` are through the helpers |
| T-02-02-03 | Tampering | `weights` parameter could be misused at the call site to silently change production ranking | accept | All in-tree production callers pass 4 args (default-arg fall-through); sweep test is the only consumer of the 5th arg; future call-site changes are caught by the existing `hybrid-rank.test.ts` weight assertions |
</threat_model>

<verification>
- `npm run typecheck` (or `tsc --noEmit`) at repo root: zero new errors.
- `cd packages/mcp-server && npm test -- --project=workerd --run`: all green.
- `cd packages/mcp-server && npm test -- --project=lint-node --run`: all green.
- `cd shared/ai-config && grep -A 1 'HYBRID_WEIGHTS' src/index.ts | head`: shows the literal with `rerank` key + audit-comment placeholder block above.
- `cd packages/mcp-server && grep 'HYBRID_WEIGHTS.cosine' src/`: returns NO matches (rename complete).
- `cd packages/mcp-server && grep 'from "./vectorize-helper' src/tools.ts`: still references `vectorizeUpsert`/`vectorizeDelete` only (not `vectorizeQuery`).
- All v0.1 recall behavior preserved: a recall call against a test fixture returns identical ranked results pre/post-Plan 02-02 (manual smoke optional — automated coverage via `hybrid-rank.test.ts` is the gate).
</verification>

<success_criteria>
- `HYBRID_WEIGHTS` lives in `@engram/ai-config` with `rerank` key and D-06 audit-comment placeholder (final scores TBD by Plan 02-03)
- `hybridRank()` is parameterized on optional `weights` arg defaulting to `HYBRID_WEIGHTS`
- `tools.ts` recall handler imports `vectorizeQuery` from `@engram/vectorize-utils`
- `vectorize-helper.ts` retains `vectorizeUpsert` + `vectorizeDelete` only
- All pre-existing tests pass; one new test added for the parameterized weights path
- `02-CF-CODE-ASSIST-USAGE.md` has rows for all three tasks in this plan
- Plan 02-03 is unblocked: it can write a sweep test that calls `hybridRank(matches, blocks, args, now, configWeights)` for each of 625 configs
</success_criteria>

<output>
Create `.planning/phases/02-recall-quality-baseline/02-02-SUMMARY.md` when done. Summary must list: HYBRID_WEIGHTS new location + key rename + audit-comment placeholder text, hybridRank parameterization signature, the tools.ts import swap, the vectorize-helper.ts slimming, and confirm all pre-existing tests pass.
</output>
