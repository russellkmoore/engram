---
phase: 02-recall-quality-baseline
plan: 07
type: execute
wave: 7
depends_on: ["02-06"]
files_modified:
  - packages/triage-worker/src/index.ts
  - packages/triage-worker/src/extract.ts
  - packages/triage-worker/src/__tests__/queue-integration.test.ts
  - .planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md
autonomous: true
requirements:
  - CON-03
tags:
  - con
  - triage-worker
  - waitUntil
  - integration
linear_subissue: con
must_haves:
  truths:
    - "`ctx.waitUntil(conflictPipeline(env, ...))` is invoked from the `store-normal` case in `packages/triage-worker/src/index.ts` AFTER `updateBlockEnrichment`."
    - "The conflict scan never blocks the ingest-response path: `ctx.waitUntil` is fire-and-forget."
    - "The embedding vector reaches `conflictPipeline` without recomputation — re-use the embedding already produced by the extract+score stage to stay under MAX_AI_CALLS budget."
    - "Integration test confirms: when a 'store-normal' MemoryEvent flows through the queue, the conflict-pipeline path is invoked exactly once with the expected newBlock shape (verified by spying on conflictPipeline)."
    - "Pre-existing queue-consumer behavior is unchanged: no regression on extract.test.ts or queue-integration.test.ts existing cases."
  artifacts:
    - path: "packages/triage-worker/src/index.ts"
      provides: "Queue consumer with conflict-pipeline ctx.waitUntil insertion"
      contains: "conflictPipeline"
    - path: "packages/triage-worker/src/__tests__/queue-integration.test.ts"
      provides: "Integration test asserting CON-03 fire-and-forget wiring"
      contains: "conflictPipeline"
  key_links:
    - from: "packages/triage-worker/src/index.ts"
      to: "packages/triage-worker/src/conflict-pipeline.ts"
      via: "ctx.waitUntil(conflictPipeline(env, { ... }))"
      pattern: "ctx.waitUntil.*conflictPipeline"
---

<objective>
Wire `conflictPipeline` (from Plan 02-06) into the triage-worker queue consumer via `ctx.waitUntil(...)` per CON-03. The insertion is SURGICAL: one fire-and-forget call after `updateBlockEnrichment` in the `store-normal` switch case. Surrounding code unchanged.

The non-obvious risk is the `embedding` field. `conflictPipeline` needs the qwen3-embedding-0.6b vector for the new block. The triage-worker's `store-normal` branch already has access to embeddings somewhere upstream (the embed step is part of memorability + extract+score earlier in the queue path). Per CONTEXT.md `<code_context>` lines 186-188, the planner's options:
- (a) recompute the embedding here — REJECTED (doubles the AI budget)
- (b) extract from a stage that already computed it — the extract+score flow already calls `env.AI.run(EMBEDDING_MODEL, { text: [event.content] })` somewhere; pipe the resulting vector forward into the `store-normal` scope
- (c) re-Vectorize-query the just-written block by id — REJECTED (cosine math on a self-vector is undefined and Vectorize requires the vector input, not an id lookup, to query against)

The clean fix is (b): ensure the embed step's output is carried forward as part of the `parsed` extracted shape (or as a sibling const) into the `store-normal` case. Inspect the existing extract pipeline to find where the embedding is computed and ADD a return-side property if it isn't already exposed.

Integration test: confirm that for a `store-normal` MemoryEvent, `conflictPipeline` is called exactly once with the right shape. The test uses `vi.spyOn` against the imported orchestrator (the actual orchestrator's logic is unit-tested in Plan 02-06 — this test only validates the WIRING).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-recall-quality-baseline/02-CONTEXT.md
@.planning/phases/02-recall-quality-baseline/02-RESEARCH.md
@.planning/phases/02-recall-quality-baseline/02-PATTERNS.md
@.planning/phases/02-recall-quality-baseline/02-06-SUMMARY.md
@packages/triage-worker/src/index.ts
@packages/triage-worker/src/extract.ts
@packages/triage-worker/src/conflict-pipeline.ts

<interfaces>
From `packages/triage-worker/src/index.ts` lines 214-242 (the `store-normal` branch — current shape):
```typescript
case "store-normal":
  await (stub as unknown as {
    updateBlockEnrichment: (args: { ... }) => Promise<void>;
  }).updateBlockEnrichment({
    workspace_id: event.workspace_id,
    block_id: event.id,
    properties: parsed.extracted_fields,
    summary: parsed.summary,
    confidence: parsed.confidence,
    type: parsed.classified_type,
  });
  // ← CON-03 INSERTION POINT
  break;
```

From `packages/triage-worker/src/conflict-pipeline.ts` (post-Plan-02-06):
```typescript
export async function conflictPipeline(
  env: { AI; VECTORIZE; WORKSPACE; ANALYTICS? },
  newBlock: { id; workspace_id; type; scope; content; embedding: number[]; created_at },
): Promise<void>;
```

Queue handler signature (Cloudflare Worker `queue` export):
```typescript
async queue(batch: MessageBatch<MemoryEvent>, env: Env, ctx: ExecutionContext): Promise<void>
```
`ctx` is available in scope — it's the third arg of the `queue(batch, env, ctx)` Worker entry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate + expose the embedding vector from the extract stage</name>
  <files>packages/triage-worker/src/extract.ts, packages/triage-worker/src/index.ts</files>
  <read_first>
    - packages/triage-worker/src/index.ts FULL FILE (find the queue consumer; find where `parsed` is assigned; find where `event.scope` is referenced; identify what `parsed` shape currently contains)
    - packages/triage-worker/src/extract.ts FULL FILE (find the `extractAndScore` or equivalent function returning the `parsed` shape; identify whether `env.AI.run(EMBEDDING_MODEL, ...)` is called inside it, and whether the resulting vector is returned)
    - packages/triage-worker/src/memorability.ts (if extract delegates to memorability, the embed call might live there instead)
    - shared/ai-config/src/index.ts (find `EMBEDDING_MODEL` to confirm what model the embed call uses)
    - .planning/phases/02-recall-quality-baseline/02-PATTERNS.md §"packages/triage-worker/src/index.ts" lines 723-755 (single-line insertion pattern + embedding-source resolution note)
  </read_first>
  <action>
    Inspect the extract pipeline and EXPOSE the embedding vector so the `store-normal` case can pass it to `conflictPipeline` without a second AI call.

    **Step 1: Reconnaissance.** Find where the qwen3 embedding is computed during the queue-consumer path. Likely candidates:
    - `extractAndScore(env, event)` in `extract.ts` calls `env.AI.run(EMBEDDING_MODEL, { text: [event.content] })` and uses the vector for either Vectorize upsert OR memorability scoring upstream.
    - The `parsed` shape returned to `index.ts:queue(...)` may or may not include the vector currently.

    Run:
    ```bash
    grep -n 'EMBEDDING_MODEL\|env.AI.run\|embedding' packages/triage-worker/src/*.ts
    ```
    Read the lines surrounding each match. Identify:
    - Which function/file does the embed call.
    - Whether the embedding is currently returned in the shape that flows to `index.ts:queue`.

    **Step 2: Expose the embedding if necessary.**
    - If the embed result is currently returned by `extractAndScore` (or whatever the upstream function is named): no work needed in `extract.ts`. Confirm the field name (likely `embedding: number[]`) and move to Step 3.
    - If the embed call's output is discarded after upserting to Vectorize: ADD `embedding` to the return shape. Smallest delta:
      1. Find the line where `env.AI.run(EMBEDDING_MODEL, { text: [event.content] })` is awaited and the resulting vector is bound to a local variable.
      2. Find the return statement of the extract function. Add `embedding: <localVarName>` to the returned object.
      3. Update the return-type interface declaration to include `embedding: number[];`.
    - If the embedding is computed INSIDE the WorkspaceDO via `vectorizeUpsert` (i.e., the embed happens after the DO RPC, not in the Worker): this is a structural problem — the embedding would not be available in the Worker's `parsed`. Re-architect: the embed MUST happen in the Worker BEFORE the upsert call, and the vector must be passed alongside the upsert payload. If this is the current state, the planner's instruction is to move the `env.AI.run(EMBEDDING_MODEL, ...)` call into the extract/score stage and forward the vector through `parsed.embedding`. Document this in the routing-tracker as architectural rework, not just wiring.

    **Step 3: Document the embedding source in a top-of-file comment in `extract.ts`** (or wherever the embed call lives):
    ```typescript
    // CON-03 / Plan 02-07: the qwen3-embedding-0.6b vector is exposed on the
    // extract-result's `embedding` field so the conflict-pipeline can re-use it
    // in the store-normal branch without a second AI call (MAX_AI_CALLS budget
    // discipline).
    ```

    **Step 4: Do NOT modify the AI call signature or any other extract behavior.** This task is purely about exposing an existing computation. No new AI calls.

    Routing per D-19: reconnaissance + ~3-line type-and-return-statement edit (Q1=Y if both extract.ts AND index.ts are touched, but the synthesis is mechanical and consistency-only) → `claude` because the reconnaissance step requires reading files Claude must read anyway. Append a tracker row.
  </action>
  <verify>
    <automated>cd packages/triage-worker && grep -q 'embedding' src/extract.ts && grep -q 'EMBEDDING_MODEL' src/extract.ts && grep -q 'CON-03' src/extract.ts && npx tsc --noEmit && npm test -- --project=workerd extract.test.ts --run 2>&1 | tail -5 | grep -iE '(pass|✓)'</automated>
  </verify>
  <acceptance_criteria>
    - The function that produces `parsed` (likely `extractAndScore` in `extract.ts`) returns an object that includes `embedding: number[]` (the qwen3 vector for `event.content`).
    - The return-type interface declares `embedding: number[];`.
    - The "CON-03 / Plan 02-07" comment is present in `extract.ts` (or the appropriate file) explaining the source.
    - Zero new `env.AI.run(EMBEDDING_MODEL, ...)` call sites compared to pre-plan baseline (re-use, don't recompute).
    - `tsc --noEmit` clean.
    - Existing `extract.test.ts` passes unchanged.
    - 02-CF-CODE-ASSIST-USAGE.md tracker row appended.
  </acceptance_criteria>
  <done>Embedding vector exposed; Plan 02-07 Task 2 can read `parsed.embedding` in the store-normal case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert ctx.waitUntil(conflictPipeline(...)) in store-normal branch (CON-03)</name>
  <files>packages/triage-worker/src/index.ts, packages/triage-worker/src/__tests__/queue-integration.test.ts</files>
  <read_first>
    - packages/triage-worker/src/index.ts lines 200-250 (full `store-normal` case context; identify `ctx` variable scope — it's the third arg of the `queue(batch, env, ctx)` Worker entry)
    - packages/triage-worker/src/index.ts top of file (existing imports)
    - packages/triage-worker/src/conflict-pipeline.ts (Plan 02-06 — signature confirmed)
    - packages/mcp-server/src/tools.ts lines 448-474 (existing `ctx.waitUntil` precedent — pattern to mirror; the v0.1 pattern uses `ctx.waitUntil(env.QUEUE.send(...))` for fire-and-forget post-write side effects)
    - packages/triage-worker/src/__tests__/queue-integration.test.ts (existing tests; understand the harness — workerd-pool, real Worker invocation; extend with a new case OR add a sibling test)
  </read_first>
  <behavior>
    - When a `store-normal` MemoryEvent flows through the queue consumer, `conflictPipeline` is invoked exactly ONCE with `newBlock = { id: event.id, workspace_id: event.workspace_id, type: parsed.classified_type, scope: event.scope ?? "personal", content: event.content, embedding: parsed.embedding, created_at: <appropriate timestamp> }`.
    - The call is wrapped in `ctx.waitUntil(...)` — non-blocking.
    - The call happens AFTER `await updateBlockEnrichment(...)` resolves — the new block exists in SQLite before the conflict scan runs (no race window).
    - When a `store-inbox` MemoryEvent flows through (low-confidence content destined for human review), `conflictPipeline` is NOT invoked — only the `store-normal` branch wires the scan.
    - When a `cold-storage` MemoryEvent flows through, `conflictPipeline` is NOT invoked.
  </behavior>
  <action>
    1. In `packages/triage-worker/src/index.ts`:
       - Add at the top: `import { conflictPipeline } from "./conflict-pipeline.js";`
       - In the `store-normal` case, immediately after the `await updateBlockEnrichment(...)` line and before the `break`, insert:
         ```typescript
         ctx.waitUntil(
           conflictPipeline(env, {
             id: event.id,
             workspace_id: event.workspace_id,
             type: parsed.classified_type,
             scope: event.scope ?? "personal",
             content: event.content,
             embedding: parsed.embedding,
             created_at: Date.now(),
           }),
         );
         ```
       - Verify `ctx` is in scope. The queue handler is the Cloudflare Worker entry point `async queue(batch, env, ctx)`. The case statement runs inside an inner loop over `batch.messages`. If `ctx` is not currently propagated into the loop, propagate it (smallest delta — pass as a parameter or capture in closure). Read the file structure to confirm scope.
       - Do NOT modify any other case (`store-inbox`, `cold-storage`) — only `store-normal` wires the scan per CON-03.
       - Do NOT add `await` before `ctx.waitUntil(...)` — the call returns void, and the whole point is fire-and-forget.

    2. In `packages/triage-worker/src/__tests__/queue-integration.test.ts`:
       - Add a new test case: "CON-03: store-normal MemoryEvent fires conflictPipeline via ctx.waitUntil exactly once"
       - Use `vi.mock("../conflict-pipeline.js", () => ({ conflictPipeline: vi.fn().mockResolvedValue(undefined) }))` to replace the orchestrator with a spy.
       - Simulate a store-normal queue message (use the existing test harness's MemoryEvent fixture — re-use the shape used in `queue-integration.test.ts`).
       - Assert `conflictPipeline` was called exactly once.
       - Assert the first arg matches the env binding shape.
       - Assert the second arg (newBlock) has the expected shape: `id`, `workspace_id`, `type`, `scope`, `content`, `embedding: expect.any(Array)`, `created_at: expect.any(Number)`.
       - Add a second test: "CON-03: store-inbox / cold-storage events do NOT trigger conflictPipeline" — assert the spy was called zero times for non-store-normal verdicts.

    Routing per D-19: index.ts insertion is ~10 lines surgical (Q1=N single file, Q2=N small diff, Q3=Y pattern from mcp-server tools.ts:448). The test scaffold is moderate (~50 LOC) — `claude` for both. Append tracker rows.
  </action>
  <verify>
    <automated>cd packages/triage-worker && grep -q 'import { conflictPipeline }' src/index.ts && grep -q 'ctx.waitUntil(' src/index.ts && grep -q 'conflictPipeline(env, {' src/index.ts && grep -q 'embedding: parsed.embedding' src/index.ts && npx tsc --noEmit && npm test -- --project=workerd queue-integration.test.ts --run 2>&1 | tail -10 | grep -iE '(pass|✓|Tests)'</automated>
  </verify>
  <acceptance_criteria>
    - `packages/triage-worker/src/index.ts` imports `conflictPipeline` from `./conflict-pipeline.js`.
    - `ctx.waitUntil(conflictPipeline(env, { ... }))` exists in the `store-normal` case, immediately after the `await updateBlockEnrichment(...)` line and before the `break`.
    - The newBlock object passed to `conflictPipeline` has the 7-field shape per the `<behavior>` block.
    - `ctx` is in scope at the insertion point (propagated from the queue entry point if necessary).
    - Other queue cases (`store-inbox`, `cold-storage`) are UNCHANGED — no `conflictPipeline` calls in them.
    - The `await updateBlockEnrichment(...)` is NOT removed — the scan runs AFTER the block is written.
    - `ctx.waitUntil(...)` is NOT awaited.
    - New integration tests in `queue-integration.test.ts` pass.
    - All pre-existing triage-worker tests pass.
    - `tsc --noEmit` clean.
    - 02-CF-CODE-ASSIST-USAGE.md tracker rows appended.
  </acceptance_criteria>
  <done>CON-03 wiring complete; the conflict scan fires asynchronously after every store-normal write without blocking the ingest-response path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ctx.waitUntil` lifetime | Cloudflare guarantees waitUntil through the Worker invocation lifecycle; throws inside MUST be caught locally (verified in Plan 02-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | `conflictPipeline` re-throws inside `ctx.waitUntil`, marking the queue consumer as failed → message retried → duplicate inbox writes | mitigate | Plan 02-06 `try/catch/finally` discipline guarantees no re-throw; integration test in this plan asserts the wrapper resolves |
| T-02-07-02 | Tampering | Race condition: `conflictPipeline` runs against a block that's not yet committed to SQLite | mitigate | The insertion point is AFTER `await updateBlockEnrichment(...)` — the row exists before the scan starts |
| T-02-07-03 | Denial of Service | Embedding recomputed twice (once in extract, once in conflict-pipeline) exceeds MAX_AI_CALLS budget | mitigate | Task 1 exposes the existing embedding through `parsed.embedding`; Task 2 reuses it; no second AI.run call |
| T-02-07-04 | Information Disclosure | `conflictPipeline` invoked for store-inbox or cold-storage events that should not trigger conflict scans | mitigate | Only `case "store-normal":` wires the call; other cases unchanged; integration test asserts zero calls for non-store-normal |
</threat_model>

<verification>
- `cd packages/triage-worker && npm test -- --project=workerd --run`: full triage-worker workerd suite green
- `cd packages/triage-worker && npx tsc --noEmit`: zero errors
- Existing `extract.test.ts` passes
- Existing `queue-integration.test.ts` cases pass + new CON-03 wiring test passes
- `grep 'ctx.waitUntil(conflictPipeline' packages/triage-worker/src/index.ts`: exactly one match
- `grep -A 1 'case "store-inbox":' packages/triage-worker/src/index.ts | grep conflictPipeline`: zero matches (other cases NOT wired)
</verification>

<success_criteria>
- Embedding vector exposed from extract → store-normal scope
- `ctx.waitUntil(conflictPipeline(env, ...))` lands in the single insertion point
- Integration test validates the wiring + the negative cases (store-inbox, cold-storage do NOT trigger the scan)
- No regression on existing tests
- 02-CF-CODE-ASSIST-USAGE.md has rows
- Plan 02-08 (recall envelope SQL-join) is unblocked
</success_criteria>

<output>
Create `.planning/phases/02-recall-quality-baseline/02-07-SUMMARY.md` when done. Summary must list: the file/function where the embed call lives, the `parsed.embedding` exposure delta, the exact ctx.waitUntil insertion site (file + line range), the integration test cases added, and confirm zero new AI calls in the queue path.
</output>
