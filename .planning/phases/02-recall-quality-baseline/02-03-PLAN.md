---
phase: 02-recall-quality-baseline
plan: 03
type: execute
wave: 4
depends_on: ["02-03a"]
files_modified:
  - packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts
  - shared/ai-config/src/index.ts
  - docs/hybrid-rank-changelog.md
  - .planning/phases/02-recall-quality-baseline/02-CF-CODE-ASSIST-USAGE.md
autonomous: true
requirements:
  - RNK-01
  - RNK-02
  - RNK-03
  - RNK-04
  - RNK-06
  - RNK-07
tags:
  - rnk
  - eval
  - sweep
  - changelog
linear_subissue: rnk
must_haves:
  truths:
    - "Eval is demonstrably TUNABLE (anti-reward-hack, closes the HR-2 blocker): F1 varies across the 625 configs (non-degenerate spread; top1_flip_rate > 0 across the sweep), proving the recency + scope + type variance introduced by Plan 02-03a actually moves the score. A flatline (all configs identical F1, flip_rate 0.0000) is a FAIL and re-opens the blocker."
    - "625-config sweep runs against the 100-entry corpus and stays within MAX_AI_CALLS=200 (D-19 cache discipline), as its OWN eval session (not bundled with 02-03a's reseed/reachability sessions or 02-04's conflict eval)."
    - "Sweep uses the verbatim D-01 grid (cosine in {0.6, 0.8, 1.0, 1.2, 1.5}, recency in {0.05, 0.10, 0.15, 0.20, 0.30}, type_match in {0.10, 0.15, 0.20, 0.25, 0.35}, scope_match in {0.05, 0.10, 0.15, 0.20, 0.30}) -- symmetric around defaults at index 2."
    - "Sweep passes expected_args into hybridRank() when the corpus entry has one (D-26 call-site change at the args={} sites), and omits it (args={}) for unlabeled open queries -- so type_match and scope_match actually contribute."
    - "Sweep reads created_at + scope from Vectorize metadata (D-24; the read path at recall-ranking.eval.test.ts lines 434-449 resolves to the real per-block values seeded by 02-03a, not the 1-day fallback) -- so recency varies per block."
    - "Sensitivity analysis reuses sweep output -- adjacent grid neighbors ARE the +/-0.05 perturbations (implements D-02 -- no separate sensitivity sweep needed)."
    - "Pareto front over (F1, MRR, top-1) on top-3-by-F1 selects the winning weights per D-03."
    - "Train->validate F1 gap < 10 percentage points STRICT per D-04 (boundary rejected)."
    - "Sensitivity: +/-0.05 perturbations flip top-1 in <30% of queries per RNK-04."
    - "Winner F1 on 100-entry corpus >= 0.8254 v0.1 baseline AND winner re-scored on the existing 27-entry real-corpus.json >= 0.8254 (D-15 dual-corpus gate; D-14 baseline-comparison check retained because the 27+20-entry fixtures stay in place; meaningful again because 02-03a's reachability relabel restored the F1 headroom)."
    - "Tuned weights committed to `shared/ai-config/src/index.ts` `HYBRID_WEIGHTS` with D-06 audit comment filled in (sweep date + F1/MRR/top1 numbers)."
    - "`docs/hybrid-rank-changelog.md` first row populated per D-21 schema."
    - "RNK plans landed first per D-16 merge-order discipline (this is the last RNK plan before the CON workstream begins)."
  artifacts:
    - path: "packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts"
      provides: "625-config sweep + Pareto + sensitivity + train/validate split test, now passing expected_args + reading metadata-backed created_at/scope"
      contains: "expected_args"
      min_lines: 200
    - path: "docs/hybrid-rank-changelog.md"
      provides: "Append-only changelog with the v0.2 first row"
      contains: "qwen3-embedding-0.6b"
    - path: "shared/ai-config/src/index.ts"
      provides: "Tuned HYBRID_WEIGHTS with completed D-06 audit comment (placeholders replaced)"
      contains: "HYBRID_WEIGHTS"
  key_links:
    - from: "packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts"
      to: "shared/vectorize-utils/src/index.ts"
      via: "import { vectorizeQuery } from '@engram/vectorize-utils'"
      pattern: "@engram/vectorize-utils"
    - from: "packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts"
      to: "packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json"
      via: "import corpusJson with { type: 'json' }; reads entry.expected_args"
      pattern: "expected_args"
    - from: "packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts"
      to: "Vectorize metadata (created_at + scope seeded by 02-03a)"
      via: "pre-resolution reads m.metadata.created_at + m.metadata.scope (no fallback collapse)"
      pattern: "created_at"
    - from: "shared/ai-config/src/index.ts"
      to: "packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts"
      via: "audit comment cites the sweep date + scores produced by this test"
      pattern: "Corpus: .planning/evals/recall-corpus.json"
---

<objective>
Build the 625-config recall-ranking sweep test, run it against the 100-entry corpus, select the winning hybrid-rank weights per D-03 (top-3 by F1 -> Pareto over F1/MRR/top-1), verify the D-04 train->validate gap < 10pp, run the RNK-04 sensitivity check, run the D-15 dual-corpus regression gate, then COMMIT the tuned values to `shared/ai-config/src/index.ts` `HYBRID_WEIGHTS` (replacing the Plan 02-02 placeholders) and seed `docs/hybrid-rank-changelog.md` with the v0.2 first row per D-21.

**The eval is now fixed by Plan 02-03a (dependency).** Before this plan runs, Plan 02-03a has: (1) seeded deterministic per-block `created_at` (exp-decay 0-90d) + `scope` in {personal, project} into Vectorize metadata, (2) labeled 40-60 corpus queries with `expected_args`, and (3) relabeled the 34 qwen3-unreachable corpus entries (+ pre-checked the 27-entry real-corpus). The three previously-constant hybridRank() components (recency, type_match, scope_match) now have real variance, so the 625-config grid can actually tune. This plan's first must_have asserts that tunability (F1 varies across configs), closing the HR-2 reward-hacking blocker that paused the original 02-03.

Purpose: closes RNK-01..04 + RNK-06 + RNK-07. The sweep is pure-math reranking over pre-cached embeddings + Vectorize matches (RESEARCH section Pattern 1) so the entire 625 x 100 evaluation fits inside MAX_AI_CALLS=200 (about 100 embedding calls + 100 Vectorize calls during pre-resolution; zero AI calls in the 62,500-iteration inner loop).

Output:
- `packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts` -- the eval-tier test (runs only when `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` are present via Phase 1's `hasEvalCreds()` gate); now passes `expected_args` into hybridRank() and reads metadata-backed `created_at`/`scope`
- `shared/ai-config/src/index.ts` -- `HYBRID_WEIGHTS` tuned values replace the Plan 02-02 placeholders; D-06 audit comment placeholders (`YYYY-MM-DD`, `X.XX`) replaced with real values from the sweep
- `docs/hybrid-rank-changelog.md` -- new file, first row populated per D-21 schema with 14 columns (date, corpus_filename, corpus_size, corpus_split, embedding_model, weights, F1_train, F1_validate, MRR_train, top1_train, sensitivity_pass_rate, sensitivity_metric, bge_reranker_active=false, notes)

**Critical eval-session discipline (RESEARCH section Pitfall 3):** Plan 02-03's sweep runs as a STANDALONE eval session -- NOT bundled with Plan 02-03a's reseed or reachability sessions, NOT bundled with CON-01's conflict-precision eval (Plan 02-04). Each combined budget would breach the 200-call cap. The `pretest:eval` hook from Plan 02-01 re-syncs the corpus before the sweep runs. Plan 02-03a's Vectorize upsert ran in its OWN earlier session, so the index already holds the varied metadata by the time this sweep starts.

**Sensitivity metric choice (RESEARCH A5, Open Question 2):** Use `top1_flip_rate` -- simpler and more interpretable than Kendall tau over top-3. Document the choice in both the audit comment and the changelog `sensitivity_metric` column.

**STOP procedure if D-15 dual-corpus gate fails:** If sweep-winner F1 on 100-entry corpus >= 0.8254 BUT re-scoring against 27-entry `real-corpus.json` regresses below 0.8254, STOP and surface as a decision point (do NOT auto-merge). CONTEXT.md D-15 says "the planner must surface this as a decision point" -- translate to: executor logs a `gsd-add-blocker`-style entry on the Linear RNK sub-issue and pauses for human decision.

**STOP procedure if the eval flatlines again:** If the sweep produces a degenerate spread (all 625 configs share one F1 / sweep-wide top1_flip_rate == 0), the 02-03a eval-design fix did NOT take effect (index not reseeded, or expected_args/metadata not wired). STOP, do NOT commit weights, log a blocker citing the flatline, and re-verify 02-03a ran (reseed session + relabel) before retrying.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-recall-quality-baseline/02-CONTEXT.md
@.planning/phases/02-recall-quality-baseline/02-RESEARCH.md
@.planning/phases/02-recall-quality-baseline/02-PATTERNS.md
@.planning/phases/02-recall-quality-baseline/02-01-SUMMARY.md
@.planning/phases/02-recall-quality-baseline/02-02-SUMMARY.md
@.planning/phases/02-recall-quality-baseline/02-03a-PLAN.md
@packages/mcp-server/src/hybrid-rank.ts
@packages/mcp-server/src/__tests__/evals/recall-f1.eval.test.ts
@packages/mcp-server/src/__tests__/evals/seed-eval-fixtures.eval.test.ts
@packages/mcp-server/src/__tests__/evals/eval-budget.setup.ts
@shared/ai-config/src/index.ts

<interfaces>
<!-- Post-Plan-02-02 shape: hybridRank is parameterized -->

```typescript
import { HYBRID_WEIGHTS, type HybridWeights } from "@engram/ai-config";

export function hybridRank(
  matches: VectorizeMatches["matches"],
  blocks: LexicalSearchHit[],
  args: Partial<RecallInput>,
  now: number = Date.now(),
  weights: HybridWeights = HYBRID_WEIGHTS,
): LexicalSearchHit[];
```

D-01 grid (VERBATIM -- copy into the test exactly):
```typescript
const COSINE_GRID = [0.6, 0.8, 1.0, 1.2, 1.5];          // 5 values; v0.1 default 1.0 at index 2
const RECENCY_GRID = [0.05, 0.10, 0.15, 0.20, 0.30];    // 5 values; v0.1 default 0.15 at index 2
const TYPE_MATCH_GRID = [0.10, 0.15, 0.20, 0.25, 0.35]; // 5 values; v0.1 default 0.20 at index 2
const SCOPE_MATCH_GRID = [0.05, 0.10, 0.15, 0.20, 0.30];// 5 values; v0.1 default 0.15 at index 2
// 5^4 = 625 configs
```

Post-02-03a eval surfaces (the variance sources this sweep consumes):
- Vectorize metadata per ef-* block now carries real `created_at` (exp-decay 0-90d) + `scope` in {personal, project} -- seeded by 02-03a Task 1. The pre-resolution block-hydration at lines 434-449 reads these directly; the 1-day fallback now only fires for genuinely missing metadata (should be none post-reseed).
- Corpus entries now carry optional `expected_args: { types?: string[]; scope?: string }` -- 02-03a Task 2. Pass it into hybridRank() when present; pass `{}` when absent.
- 34 corpus entries have relabeled `expected_top_3_block_ids` (qwen3-reachable) + audit fields -- 02-03a Task 3. The sweep scores against the relabeled (reachable) ids, restoring D-15 headroom.

From `packages/mcp-server/src/__tests__/evals/recall-f1.eval.test.ts` (existing analog -- KEEP VERBATIM):
- Import discipline: `import corpusJson from "./fixtures/recall-corpus-v2.json" with { type: "json" }` (Vite build-time JSON import -- `fs.readFileSync` does NOT work in workerd-pool)
- EMBEDDING_MODEL fail-fast at top: assert `corpus.embedding_model === EMBEDDING_MODEL` (string equality)
- `hasEvalCreds()` gate from `eval-budget.setup.ts`
- The `captureCallback` pattern at lines 94-124 if any inline F1 sanity check on `recall()` is included
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author recall-ranking.eval.test.ts sweep + Pareto + sensitivity + dual-corpus gate, now consuming 02-03a variance (RNK-01..04, RNK-06)</name>
  <files>packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts</files>
  <read_first>
    - packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts (the EXISTING paused sweep test -- this task revises its call sites; lines 216/246/296/299 pass args={} and must pass entry.expected_args when present; lines 434-449 are the metadata read path that now resolves to real created_at/scope post-02-03a)
    - packages/mcp-server/src/__tests__/evals/recall-f1.eval.test.ts (FULL FILE -- direct analog; same eval-tier shape, same `hasEvalCreds()` gate, same corpus-loader Vite build-time JSON import, same EMBEDDING_MODEL fail-fast at top)
    - packages/mcp-server/src/__tests__/evals/eval-budget.setup.ts (MAX_AI_CALLS=200 enforcement; understand how `aiCallCount` increments and how to stay UNDER the cap)
    - packages/mcp-server/src/__tests__/evals/seed-eval-fixtures.eval.test.ts (POST-02-03a; confirms the metadata now seeded: created_at via createdAtForEntry, scope via scopeForEntry -- so the sweep can rely on real per-block values)
    - packages/mcp-server/src/hybrid-rank.ts (post-Plan-02-02 parameterized signature; lines 101-110 show type_match needs args.types, scope_match needs args.scope -- this is why expected_args must be passed)
    - shared/ai-config/src/index.ts (HYBRID_WEIGHTS default; v0.1 baseline F1 = 0.8254 documented in MIN_COSINE_THRESHOLD comment context)
    - packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json (POST-02-03a: confirm `corpus_version: 2`, 100 entries, 70/30 split, `embedding_model: "@cf/qwen/qwen3-embedding-0.6b"`, expected_args on 40-60 entries, relabel audit fields on ~34 entries)
    - packages/mcp-server/src/__tests__/evals/fixtures/real-corpus.json (existing 27-entry baseline -- needed for D-15 dual-corpus gate; the test re-uses the same fixture loader pattern)
    - .planning/phases/02-recall-quality-baseline/02-RESEARCH.md section "Code Examples -> Embedding cache + sweep skeleton" (lines 767-835 -- the canonical sweep skeleton including Pareto + sensitivity + D-15 dual gate)
    - .planning/phases/02-recall-quality-baseline/02-RESEARCH.md section "Pattern 2: Pareto-Front Computation" (lines 393-431 -- hand-rolled 15-line filter)
    - .planning/phases/02-recall-quality-baseline/02-RESEARCH.md section "Pattern 3: Sensitivity Analysis" (lines 432-453 -- top1_flip_rate recommendation)
    - .planning/phases/02-recall-quality-baseline/02-RESEARCH.md section "Pitfall 2: Eval-budget counter accidentally consumed during sweep" (lines 715-723 -- every `env.AI.run` and `env.VECTORIZE.query` counts; cache them once during pre-resolution; the inner loop is pure math)
    - .planning/phases/02-recall-quality-baseline/02-RESEARCH.md section "Pitfall 3: CON-01 unskip without first verifying the budget" (lines 725-733 -- eval sessions must be run SEPARATELY)
  </read_first>
  <behavior>
    Test cases (declare expectations BEFORE writing implementation):

    - **Test: Loaded corpus integrity** -- Reading `recall-corpus-v2.json` produces an object with `corpus_version === 2`, `embedding_model === EMBEDDING_MODEL` (string equality), `entries.length >= 100`, and an exactly-non-zero `train` set + non-zero `validate` set when filtering `entries` by `split` field. Additionally assert that between 40 and 60 entries have a non-empty `expected_args` (the 02-03a labeling landed). Fails fast if any of these is wrong.

    - **Test: Variance present (anti-flatline precondition)** -- After pre-resolution, the set of distinct `block.created_at` values across all hydrated blocks is > 1 (recency variance survived the seed) AND the set of distinct `block.scope` values is > 1 (scope variance survived). If either collapses to a single value, the 02-03a reseed did not take effect -- fail with a clear message before running the sweep.

    - **Test: Sweep budget discipline** -- After running pre-resolution against the corpus, `aiCallCount` (from `eval-budget.setup.ts`) is <= 130 (allowing slack for retries on top of ~100 corpus + ~27 real-corpus embed calls). The inner 625-config loop runs WITHOUT any further `env.AI.run` or `env.VECTORIZE.query` calls.

    - **Test: Sweep produces 625 results** -- `sweepResults.length === 625`. Each result has `cfg.rerank`, `cfg.recency`, `cfg.type_match`, `cfg.scope_match` populated from the exact D-01 grid values + per-split metrics object `{ f1, mrr, top1 }` for both train and validate splits.

    - **Test: Eval is TUNABLE (closes HR-2)** -- Across the 625 SweepResults, the distinct count of `train.f1` values is > 1 (the F1 surface is non-degenerate; the prior blocker had exactly 1). Equivalently, `max(train.f1) - min(train.f1) > 0`. This is the core anti-reward-hack assertion: the eval can actually distinguish weight configs.

    - **Test: D-04 strict train->validate gap gate** -- `winner.train.f1 - winner.validate.f1 < 0.10` (strict). The test FAILS if the gap is exactly 0.10 (CONTEXT.md D-04 explicitly rejects the boundary).

    - **Test: D-03 selection rule** -- Winner is identified by (a) sort all 625 by `train.f1` descending, take top-3; (b) compute Pareto front over (`f1`, `mrr`, `top1`) on those top-3; (c) if single Pareto winner -> it's the winner; (d) if multiple -> tiebreak by `mrr` descending. Deterministic: the same input always picks the same config.

    - **Test: RNK-04 sensitivity (top1_flip_rate < 30%)** -- For each of 8 +/-0.05 perturbations of the winner config (4 axes x 2 directions), score every TRAIN query under the perturbed weights and count how many queries had a different top-1 result compared to the winner. Average flip rate across the 8 perturbations must be < 0.30 AND > 0 (a zero flip rate would again signal a flat surface).

    - **Test: RNK-06 baseline regression on the sweep corpus** -- `winner.train.f1 >= 0.8254`. The test FAILS if regression.

    - **Test: D-15 dual-corpus regression on the 27-entry corpus** -- Load `real-corpus.json` (27 entries) via the same Vite JSON import pattern; pre-resolve all 27 queries (~27 more AI calls -- still under budget); re-rank with the winning weights; assert F1 >= 0.8254. The 02-03a reachability pre-check ensures these 27 are reachable, so this is a meaningful gate. On regression: emit a console warning and fail the assertion (STOP procedure).

    - **Test: D-15 STOP procedure** -- When the 27-entry F1 < 0.8254, the test BOTH (a) fails the assertion AND (b) writes the winner weights + both F1 scores to `console.warn("RNK-06-D15-REGRESSION", { ... })` so the executor can capture this in a Linear blocker comment.
  </behavior>
  <action>
    Revise the EXISTING `packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts` (the paused sweep) to consume the 02-03a variance and add the tunability assertion. Preserve all existing sweep mechanics, the D-01 grid constants (kept byte-stable by the 223fabb prettier-ignore), the Pareto/sensitivity/dual-corpus gates, and the budget-cache pattern. Apply these changes:

    **Change 1 -- pass expected_args into hybridRank() (D-26):** At the call sites that currently pass `args={}` (the blocker notes lines 216, 246, 296, 299), pass the corpus entry's `expected_args` when present, else `{}`. Concretely, replace the literal third argument `{}` with `(entry.expected_args ?? {})` in the scoring loops (the train/validate scoreSplit loops) AND in the sensitivity `top1FlipRate` loop (use the same per-entry expected_args inside the perturbation scoring so type_match/scope_match contribute consistently). For the sensitivity helper which currently hardcodes `{}`, thread the per-entry expected_args through (iterate the train entries and read `entry.expected_args ?? {}` for each). Add a `CorpusEntry` type field `expected_args?: { types?: string[]; scope?: string }`.

    **Change 2 -- rely on metadata-backed created_at/scope (D-24):** The block-hydration at lines 434-449 already reads `m.metadata.created_at` + `m.metadata.scope`. Keep that read path. Add the "Variance present" precondition assertion (distinct created_at > 1 AND distinct scope > 1) right after pre-resolution so a missing reseed fails loudly instead of silently flatlining. Do NOT remove the fallback (it is a safety net for genuinely-missing metadata) but the assertion guarantees the common case is real data.

    **Change 3 -- add the tunability assertion (anti-reward-hack):** After building `sweepResults`, assert `new Set(sweepResults.map(r => r.train.f1)).size > 1` (the F1 surface is non-degenerate). Log the F1 spread `[RNK] f1 spread: min=X.XXXX max=X.XXXX distinct=N` so the SUMMARY can record it. This is the direct closure of the HR-2 flatline blocker.

    **Preserve VERBATIM (do not regress what already exists):**
    - The D-01 grids (COSINE_GRID / RECENCY_GRID / TYPE_MATCH_GRID / SCOPE_MATCH_GRID) exactly as written.
    - `BASELINE_F1 = 0.8254`, `TRAIN_VALIDATE_GAP_LIMIT = 0.10`, `SENSITIVITY_FLIP_LIMIT = 0.30`, `TOP_N_BY_F1 = 3`.
    - The Vite JSON imports for both `recall-corpus-v2.json` and `real-corpus.json` with `with { type: "json" }`.
    - `import { vectorizeQuery } from "@engram/vectorize-utils";`
    - The pre-resolution function (single env.AI.run + vectorizeQuery + block hydration per query; inner loop pure math).
    - `paretoFront`, `top1FlipRate`, `computeF1`, `computeMRR`, `computeTop1` pure helpers.
    - `hasEvalCreds()` gate at the `describe` block.
    - The `[RNK-WINNER]` log line format so Task 2 can grep it.
    - The `console.warn("RNK-06-D15-REGRESSION", ...)` branch for the D-15 STOP procedure.
    - The 60_000 timeout per `it()`.

    The `[RNK-WINNER]` log line stays exactly (Task 2 greps it):
    `[RNK-WINNER] cfg={"rerank":X.X,"recency":X.X,"type_match":X.X,"scope_match":X.X} f1_train=X.XXXX f1_validate=X.XXXX mrr_train=X.XXXX top1_train=X.XXXX sensitivity_top1_flip_rate=X.XXXX real_corpus_f1=X.XXXX`

    DO NOT run the test yet -- Task 2 is the run + commit step.

    Routing per D-19: the call-site rewrite to thread expected_args + the tunability assertion are surgical edits to an existing test (claude -- runtime/cross-file awareness). The pure helpers already exist; if any need regeneration they could route to cf-code-assist:generateCode. Append tracker rows.
  </action>
  <verify>
    <automated>test -f packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && grep -q 'COSINE_GRID = \[0.6, 0.8, 1.0, 1.2, 1.5\]' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && grep -q 'RECENCY_GRID = \[0.05, 0.10, 0.15, 0.20, 0.30\]' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && grep -q 'TYPE_MATCH_GRID = \[0.10, 0.15, 0.20, 0.25, 0.35\]' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && grep -q 'SCOPE_MATCH_GRID = \[0.05, 0.10, 0.15, 0.20, 0.30\]' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && grep -q 'paretoFront' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && grep -q 'top1FlipRate' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && grep -q 'BASELINE_F1 = 0.8254' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && grep -q 'TRAIN_VALIDATE_GAP_LIMIT = 0.10' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && grep -q 'expected_args' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && ! grep -E 'hybridRank\([^,]+,[^,]+,\s*\{\},\s*Date.now\(\),' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && grep -q 'recall-corpus-v2.json' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && grep -q 'real-corpus.json' packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts && cd packages/mcp-server && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists; TypeScript compiles cleanly (`tsc --noEmit` zero errors).
    - D-01 grids appear VERBATIM (same arrays, same number formatting like `0.10` not `0.1`).
    - Vite JSON imports use `with { type: "json" }` for both `recall-corpus-v2.json` AND `real-corpus.json`.
    - Test imports `hybridRank` and passes per-config weights as 5th arg AND passes `(entry.expected_args ?? {})` as the 3rd arg at every scoring call site (the bare `{}` literal third-arg pattern is GONE).
    - Test imports `vectorizeQuery` from `@engram/vectorize-utils`.
    - Pre-resolution calls `env.AI.run` + `vectorizeQuery` + block hydration exactly ONCE per query; the 625-config inner loop never touches `env.AI` or `env.VECTORIZE`.
    - "Variance present" precondition asserts distinct created_at > 1 AND distinct scope > 1 after pre-resolution.
    - Tunability assertion present: distinct `train.f1` across 625 results > 1 (the anti-flatline / HR-2 closure check), with an `[RNK] f1 spread` log.
    - `paretoFront`, `top1FlipRate`, `computeF1`, `computeMRR`, `computeTop1` are defined as pure functions; `top1FlipRate` threads per-entry expected_args.
    - `hasEvalCreds()` gate present at the `describe` block.
    - D-04 (gap < 0.10 STRICT), RNK-04 (flip rate < 0.30 and > 0), RNK-06 (winner F1 >= 0.8254), D-15 dual-corpus assertions all present.
    - `console.warn("RNK-06-D15-REGRESSION", ...)` branch exists for the D-15 STOP procedure.
    - `[RNK-WINNER]` log line uses the exact format so Task 2 can grep it.
    - Test does NOT modify `HYBRID_WEIGHTS` (Task 2 owns the commit).
    - 02-CF-CODE-ASSIST-USAGE.md tracker rows logged; decisions captured BEFORE commit.
  </acceptance_criteria>
  <done>Sweep test consumes 02-03a's recency/scope/type variance, threads expected_args, asserts non-degenerate F1 (HR-2 closed), with D-04/RNK-04/RNK-06/D-15 gates intact; runs only when eval creds are present.</done>
</task>

<task type="auto">
  <name>Task 2: Run sweep + commit tuned weights + author audit comment + seed changelog (RNK-05, RNK-07)</name>
  <files>shared/ai-config/src/index.ts, docs/hybrid-rank-changelog.md</files>
  <read_first>
    - packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts (the test from Task 1; the executor reads its `[RNK-WINNER]` console log to extract the tuned values + scores + sensitivity rate, and the `[RNK] f1 spread` line to confirm tunability)
    - shared/ai-config/src/index.ts (current state post-Plan-02-02: HYBRID_WEIGHTS has `rerank: 1.0, recency: 0.15, type_match: 0.2, scope_match: 0.15` with D-06 audit-comment block containing `YYYY-MM-DD` and `X.XX` placeholders -- Task 2 replaces ONLY the numeric placeholders + the literal values)
    - .planning/phases/02-recall-quality-baseline/02-CONTEXT.md section D-06 (audit-comment verbatim -- the 7-marker-line text must STAY identical; only the corpus-line date + scores-line numbers change)
    - .planning/phases/02-recall-quality-baseline/02-CONTEXT.md section D-21 (changelog row schema -- exact 14 columns)
    - .planning/phases/02-recall-quality-baseline/02-PATTERNS.md section "docs/hybrid-rank-changelog.md" lines 543-567 (file structure + first-row template)
    - .planning/phases/02-recall-quality-baseline/02-RESEARCH.md section "Pitfall 3" (run the sweep as a STANDALONE eval session -- do not combine with conflict-precision or with 02-03a's reseed/reachability; the `pretest:eval` script from Plan 02-01 re-syncs the corpus before this run)
  </read_first>
  <action>
    Run the sweep, extract the tuned values, commit them to `HYBRID_WEIGHTS`, fill in the audit comment, and seed the changelog.

    **Pre-run discipline:**
    - Confirm Plan 02-03a has fully run: the Vectorize index is reseeded with varied metadata (02-03a Task 1 session) AND the corpus carries expected_args + relabeled reachable ids (02-03a Tasks 2-3). If not, STOP -- this sweep depends on 02-03a.
    - Ensure NO other eval test is running in the same session (the budget counter is shared; conflict-precision in Plan 02-04 consumes ~30 calls separately; 02-03a's sessions already completed).
    - Confirm Cloudflare creds are available (`CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`).

    **Step 1: Run the sweep.**
    From repo root:
    `cd packages/mcp-server && npm test -- --project=eval recall-ranking.eval.test.ts --run 2>&1 | tee /tmp/recall-ranking-sweep.log`
    The `pretest:eval` hook re-syncs the corpus before the test.

    **Step 2: Verify tunability + extract sweep results.**
    - Confirm the `[RNK] f1 spread` line shows `distinct > 1` (the eval tuned -- HR-2 closed). If `distinct == 1`, STOP: the eval flatlined again, meaning 02-03a's variance did not reach Vectorize. Log a blocker and re-verify 02-03a's reseed.
    - From the `[RNK-WINNER]` line capture: `winner_rerank`, `winner_recency`, `winner_type_match`, `winner_scope_match` (numeric from `cfg`); `F1_train`, `F1_validate`, `MRR_train`, `top1_train` (4 decimals); `sensitivity_top1_flip_rate` (4 decimals); `real_corpus_f1` (D-15 27-entry score).

    **Step 3: Gate check before committing.**
    - If the test passed (all gates green, including the tunability + D-15) -> proceed to Step 4.
    - If it failed at the D-15 dual-corpus assertion (RNK-06-D15-REGRESSION warning) -> STOP. Do NOT commit weights. Log a Linear blocker on the RNK sub-issue: "RNK-06 D-15 dual-corpus regression: sweep-winner F1 on 100-entry corpus = X.XX (passes baseline) BUT re-scoring against 27-entry real-corpus.json = X.XX (regresses below 0.8254). Per CONTEXT.md D-15, surfacing as decision point. Need human input on whether to (a) accept and proceed, (b) re-tune with corpus weighting, or (c) defer to v0.3 re-tune." Pause.
    - If it failed at the tunability assertion (flatline) -> STOP, log a blocker citing the flatline + re-verify 02-03a ran. Do NOT commit.
    - If it failed at any other gate (D-04 gap >= 0.10, RNK-04 flip rate >= 0.30 or == 0, RNK-06 sweep F1 < 0.8254) -> STOP, raise a blocker citing the specific gate. Do NOT commit. Re-plan via `/gsd:plan-phase 2 --replan-section weight-sweep` as needed.

    **Step 4: Commit tuned weights to `shared/ai-config/src/index.ts`.**
    Edit the `HYBRID_WEIGHTS` literal -- replace the placeholder values with the captured winner values (rerank, recency, type_match, scope_match).

    **Step 5: Fill in the D-06 audit comment.**
    In the same file, edit the audit comment block above `HYBRID_WEIGHTS`:
    - Replace `YYYY-MM-DD` with today's date in ISO `YYYY-MM-DD` on the "Corpus:" line.
    - Replace `F1=X.XX, MRR=X.XX, top1=X.XX` with the rounded F1_train, MRR_train, top1_train from the sweep.
    - Add a NEW line before "Re-tune at v0.3 when corpus grows.": `Sensitivity (top1_flip_rate over +/-0.05 perturbations of 4 axes x all train queries): XX.X% (RNK-04 gate < 30%)`
    - Add a follow-up line: `D-15 dual-corpus check: 27-entry real-corpus.json F1 = X.XX (gate >= 0.8254 passed).`
    The other 7 D-06 marker lines must remain BYTE-IDENTICAL. Do NOT alter the cross-phase footgun warning text.

    **Step 6: Seed `docs/hybrid-rank-changelog.md`.**
    Create the new file with the header + the 14-column table per D-21 (date, corpus_filename, corpus_size, corpus_split, embedding_model, weights, F1_train, F1_validate, MRR_train, top1_train, sensitivity_pass_rate, sensitivity_metric=top1_flip_rate, bge_reranker_active=false, notes). Populate one row with the real values. Notes column records: first v0.2 tune, D-15 dual-corpus gate passed (27-entry F1=real_corpus_f1), RNK-04 sensitivity passed, D-04 train->validate gap in pp, and that the eval-design fix (02-03a) restored tunability (F1 spread distinct > 1). Trailing comment: schema fixed per D-21; bge_reranker_active flips true in Phase 3 EXP-06.

    **Step 7: Verify the hybrid-rank unit test still passes** with the committed values:
    `cd packages/mcp-server && npm test -- --project=workerd hybrid-rank.test.ts --run`

    Routing per D-19: BOTH actions (audit-comment fill-in + changelog row) authored by claude (Q1=Y byte-frozen contract; Q2=N small edits; Q3=Y template exists). Append tracker rows.
  </action>
  <verify>
    <automated>test -f docs/hybrid-rank-changelog.md && grep -q 'recall-corpus.json' docs/hybrid-rank-changelog.md && grep -q '@cf/qwen/qwen3-embedding-0.6b' docs/hybrid-rank-changelog.md && grep -q 'top1_flip_rate' docs/hybrid-rank-changelog.md && grep -q 'bge_reranker_active' docs/hybrid-rank-changelog.md && grep -q 'false' docs/hybrid-rank-changelog.md && ! grep -q 'YYYY-MM-DD' shared/ai-config/src/index.ts && ! grep -q 'F1=X.XX' shared/ai-config/src/index.ts && grep -q 'D-15 dual-corpus' shared/ai-config/src/index.ts && grep -q 'top1_flip_rate' shared/ai-config/src/index.ts && cd packages/mcp-server && npm test -- --project=workerd hybrid-rank.test.ts --run 2>&1 | tail -5 | grep -iE '(pass|ok)'</automated>
  </verify>
  <acceptance_criteria>
    - Sweep eval ran and printed `[RNK-WINNER]` plus an `[RNK] f1 spread` line showing distinct > 1 (tunability confirmed; HR-2 closed); all gates passed (D-04 gap < 0.10, RNK-04 flip rate in (0, 0.30), RNK-06 sweep F1 >= 0.8254, D-15 27-entry F1 >= 0.8254).
    - `shared/ai-config/src/index.ts` `HYBRID_WEIGHTS` literal has the WINNER values (numeric placeholders replaced).
    - `shared/ai-config/src/index.ts` audit comment has: real ISO date; real F1/MRR/top1 numbers (no `X.XX` remain); NEW sensitivity line citing top1_flip_rate; NEW D-15 dual-corpus line citing 27-entry F1; BYTE-IDENTICAL 7-marker D-06 text (cross-phase footgun warning preserved exactly).
    - `docs/hybrid-rank-changelog.md` exists with header + 14-column table + one row of real values; `bge_reranker_active = false`; `sensitivity_metric = top1_flip_rate`; notes mention the 02-03a eval-design fix restored tunability.
    - `cd packages/mcp-server && npm test -- --project=workerd hybrid-rank.test.ts --run`: passes (unit test compares against tuned values).
    - `cd packages/mcp-server && npm test -- --project=workerd --run`: full workerd suite green (no regression).
    - If the sweep test FAILED at any gate (flatline, D-15, D-04, RNK-04, RNK-06), execution STOPPED (no commit, blocker logged on Linear RNK sub-issue, paused for human decision).
    - 02-CF-CODE-ASSIST-USAGE.md has tracker rows for the audit-comment fill + changelog seed (both route=claude).
  </acceptance_criteria>
  <done>Tuned weights committed with audit comment fully filled in; changelog seeded; tunability confirmed (F1 spread > 1) and all gates passed -- or STOPPED with a Linear blocker if any failed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sweep test -> Workers AI (embedding model) | Eval-tier test runs against REAL Cloudflare bindings (workerd-pool); ~100 embedding calls + ~127 with D-15 dual-corpus check |
| Sweep test -> Vectorize | Real index queries (~100 calls during pre-resolution); reads only -- no upserts; the index already holds 02-03a's varied metadata |
| Audit-comment text | Byte-frozen contract -- Phase 3 EXP-06 readers depend on it for understanding which score source feeds the rerank weight |
| Eval tunability | The sweep can only tune because 02-03a introduced variance; a silent flatline would re-commit overfit/garbage weights |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Denial of Service | MAX_AI_CALLS=200 budget breach via per-config AI calls (PITFALLS HR-2 misexecution) | mitigate | Cache embeddings + Vectorize matches during pre-resolution (Task 1); inner 625-config loop is pure math; budget assertion is the gate |
| T-02-03-02 | Tampering | Sweep-result drift via running RNK + CON (or 02-03a) evals in the same session (budget cap shared) | mitigate | Plan 02-03 documents a standalone eval session; 02-03a's reseed/reachability and CON-01 (Plan 02-04) are SEPARATE sessions; instruction repeated in changelog notes |
| T-02-03-03 | Tampering | D-06 audit-comment text drift (single character changes break Phase 3 reading) | mitigate | Task 2 instructions call out BYTE-IDENTICAL preservation of the 7 marker lines; only date + scores update; routing locked to claude; `/gsd:verify-work 2` cross-references against CONTEXT.md D-06 |
| T-02-03-04 | Information Disclosure | Sweep test queries against a shared eval-workspace namespace that should not contain production data | mitigate | Existing recall-f1.eval.test.ts establishes the eval-fixtures namespace isolation; new test reuses it |
| T-02-03-05 | Tampering | Overfit win committed as "passing" via boundary D-04 gap of exactly 0.10 | mitigate | D-04 boundary rule is STRICT (`< 0.10`, not `<= 0.10`); test asserts strict less-than |
| T-02-03-06 | Repudiation | Eval flatlines again (02-03a not actually in effect) and garbage weights get committed silently | mitigate | Tunability assertion (distinct train.f1 > 1) + "variance present" precondition fail loudly before commit; Task 2 Step 2 STOP-on-flatline check |
</threat_model>

<verification>
- `cd packages/mcp-server && npm test -- --project=workerd --run`: green
- `cd packages/mcp-server && npm test -- --project=lint-node --run`: green
- `tsc --noEmit` at repo root: zero errors
- `grep -F 'YYYY-MM-DD' shared/ai-config/src/index.ts`: zero matches (placeholders replaced)
- `grep -F 'F1=X.XX' shared/ai-config/src/index.ts`: zero matches
- Sweep log shows `[RNK] f1 spread` with distinct > 1 (tunability / HR-2 closure)
- `docs/hybrid-rank-changelog.md` exists with exactly one data row, all 14 columns populated
- `02-CF-CODE-ASSIST-USAGE.md` has rows for Task 1 (sweep-test revision) and Task 2 (audit-comment fill + changelog seed)
- Linear sync: RNK sub-issue marked with sweep results comment (winner cfg + scores + tunability confirmation)
</verification>

<success_criteria>
- recall-ranking.eval.test.ts ran end-to-end with all gates green, including the tunability (non-degenerate F1) and D-15 dual-corpus gates
- HYBRID_WEIGHTS tuned values committed with D-06 audit comment fully filled in
- docs/hybrid-rank-changelog.md seeded with v0.2 first row
- D-15 dual-corpus gate enforced (or STOP procedure invoked on regression)
- HR-2 reward-hacking blocker closed (eval demonstrably tunable on the 02-03a-fixed eval)
- RNK-01..04 + RNK-06 + RNK-07 closed
- Linear RNK sub-issue updated with sweep summary
- Plan 02-04 (CON workstream entry) can now run as a SEPARATE eval session; merge-to-main of RNK changes lands BEFORE any CON changes per D-16
</success_criteria>

<output>
Create `.planning/phases/02-recall-quality-baseline/02-03-SUMMARY.md` when done. Summary must list: sweep winner cfg + all metric scores (train + validate + sensitivity + 27-entry dual-corpus), the F1 spread (min/max/distinct) proving tunability, the date the audit comment cites, changelog row contents, any gate failures + how resolved, and confirm the sweep ran as a separate eval session from 02-03a's reseed/reachability and from 02-04's conflict eval.
</output>
