---
phase: 05-ai-integration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md
  - .planning/phases/05-ai-integration/05-AI-SPEC.md
  - .planning/phases/05-ai-integration/05-VALIDATION.md
  - CLAUDE.md
  - .claude/skills/spike-findings-engram/SKILL.md
  - packages/mcp-server/wrangler.jsonc
  - packages/mcp-server/wrangler.test.jsonc
  - packages/triage-worker/wrangler.jsonc
  - packages/triage-worker/wrangler.test.jsonc
  - packages/triage-worker/package.json
  - packages/triage-worker/vitest.config.ts
  - packages/triage-worker/tsconfig.json
  - packages/mcp-server/src/schemas.ts
  - packages/workspace-do/src/schema.ts
  - packages/workspace-do/src/migrations.ts
  - packages/workspace-do/src/queries.ts
  - packages/workspace-do/src/index.ts
  - packages/workspace-do/src/types.ts
  - packages/mcp-server/src/__tests__/vectorize-helper.test.ts
  - packages/mcp-server/src/__tests__/ai-helper.test.ts
  - packages/mcp-server/src/__tests__/hybrid-rank.test.ts
  - packages/mcp-server/src/__tests__/envelope.test.ts
  - packages/mcp-server/src/__tests__/tools-integration.test.ts
  - packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts
  - packages/triage-worker/src/__tests__/extract.test.ts
autonomous: false
requirements:
  - AI-02
  - AI-03
  - AI-04
  - AI-05
  - AI-06
  - AI-07
  - AI-08

must_haves:
  truths:
    - "The cf-code-assist routing tracker exists with the 3-question-checklist header and a seed row; subsequent code-producing tasks in Plans 05-02..05-07 can append rows BEFORE committing route decisions (CLAUDE.md mandate)."
    - "Every package that hits Workers AI or Vectorize at runtime declares the `AI` + `VECTORIZE` bindings in both production and test wrangler configs; lint:wrangler still passes for production configs."
    - "The triage-worker package has a vitest harness that can run unit tests under @cloudflare/vitest-pool-workers with AI + VECTORIZE + WORKSPACE bindings present, and the package declares the dependencies it needs to host Phase 5 code (zod, vitest, vitest-pool-workers)."
    - "Schema migration v2 (`blocks.cold_storage INTEGER NOT NULL DEFAULT 0`) lands behind the `_schema_migrations` runner and runs idempotently across DO restarts (per D-07 cold-storage routing)."
    - "Every WorkspaceDO RPC method that Phase 5 handlers and the Triage Worker will call (`stampEmbedding`, `getBlocksByIds`, `updateBlockEnrichment`, `moveToInbox`, `moveToColdStorage`) exists, calls `assertOwnsWorkspace` as the first executable line (STO-07 / S3), and `getBlocksByIds` excludes cold-storage rows by default."
    - "RecallInputSchema.verbosity default flips from `\"both\"` to `\"chunks\"` per D-01; enum shape unchanged."
    - "RED test stubs exist for every new Phase 5 helper / boundary (vectorize-helper, ai-helper, hybrid-rank, extract) and the three existing test files have new it() scaffolds for D-03 verbosity parameterization, AI-08 5-second-sleep round-trip, and AI-02 Vectorize Prong C — all currently failing because the production code does not exist yet (Wave 1+ flips them GREEN)."
    - "AI-SPEC.md §4 diagram, CLAUDE.md Ingest Pipeline, and spike-findings-engram SKILL.md verbosity-default line are amended per D-04 / D-07 / D-05 so downstream readers see Phase 5 canon."
  artifacts:
    - path: ".planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md"
      provides: "Phase 5 cf-code-assist routing tracker with 3-question checklist header + seed row"
      contains: "Phase 5 cf-code-assist Routing Tracker"
    - path: "packages/mcp-server/wrangler.jsonc"
      provides: "Production AI + VECTORIZE bindings on mcp-server"
      contains: "\"binding\": \"VECTORIZE\""
    - path: "packages/mcp-server/wrangler.test.jsonc"
      provides: "Test-time AI + VECTORIZE bindings (mirrors production for vitest-pool-workers)"
      contains: "\"binding\": \"VECTORIZE\""
    - path: "packages/triage-worker/wrangler.jsonc"
      provides: "Triage Worker production wrangler with AI + VECTORIZE + cross-Worker WORKSPACE DO binding (script_name=engram-mcp-server)"
      contains: "\"script_name\": \"engram-mcp-server\""
    - path: "packages/triage-worker/wrangler.test.jsonc"
      provides: "Triage Worker test wrangler with AI + VECTORIZE + WORKSPACE bindings"
      contains: "\"binding\": \"WORKSPACE\""
    - path: "packages/triage-worker/vitest.config.ts"
      provides: "Vitest pool-workers harness mirroring mcp-server"
    - path: "packages/triage-worker/package.json"
      provides: "Triage Worker dependencies — vitest, @cloudflare/vitest-pool-workers, zod, plus test scripts"
    - path: "packages/workspace-do/src/schema.ts"
      provides: "V1_SQL (unchanged) + new V2_SQL constant for blocks.cold_storage column"
      contains: "V2_SQL"
    - path: "packages/workspace-do/src/migrations.ts"
      provides: "MIGRATIONS array with v2 entry; runner skips already-applied versions"
      contains: "v2_cold_storage"
    - path: "packages/workspace-do/src/queries.ts"
      provides: "5 new typed query helpers: stampEmbedding, getBlocksByIds, updateBlockEnrichment, moveToInbox, moveToColdStorage"
    - path: "packages/workspace-do/src/index.ts"
      provides: "5 new DO RPC methods wrapping the helpers with assertOwnsWorkspace as first executable line"
    - path: "packages/mcp-server/src/schemas.ts"
      provides: "RecallInputSchema.verbosity default = \"chunks\" (D-01)"
      contains: ".default(\"chunks\")"
  key_links:
    - from: "packages/workspace-do/src/index.ts"
      to: "packages/workspace-do/src/queries.ts"
      via: "import { stampEmbedding as stampEmbeddingQuery, getBlocksByIds as getBlocksByIdsQuery, updateBlockEnrichment as updateBlockEnrichmentQuery, moveToInbox as moveToInboxQuery, moveToColdStorage as moveToColdStorageQuery } from \"./queries.js\""
      pattern: "as stampEmbeddingQuery"
    - from: "packages/workspace-do/src/migrations.ts"
      to: "packages/workspace-do/src/schema.ts"
      via: "import { V1_SQL, V2_SQL } from \"./schema.js\""
      pattern: "V1_SQL, V2_SQL"
    - from: "packages/triage-worker/wrangler.jsonc"
      to: "packages/mcp-server (DO host)"
      via: "durable_objects.bindings[].script_name = \"engram-mcp-server\""
      pattern: "script_name"
---

<objective>
Wave 0: close every test-infrastructure, schema-migration, doc-touch-up, and BLOCKING tracker gap so Waves 1–5 can land without surfacing infrastructure work mid-implementation. This plan owns the prerequisites cited by `05-VALIDATION.md` §"Wave 0 Requirements" and by `05-RESEARCH.md` §"Wave 0 Gaps".

Purpose:
1. Stand up the cf-code-assist routing tracker (per project CLAUDE.md) as a BLOCKING task; every subsequent code-producing task in Plans 05-02..05-07 must append a row.
2. Add the `AI` + `VECTORIZE` bindings (and the cross-Worker `WORKSPACE` DO binding for triage-worker) so every later handler/test can import them.
3. Build the triage-worker test infra (vitest.config.ts, wrangler.test.jsonc, package.json deps) so Plans 05-04/05-06 don't have to fight infrastructure.
4. Land schema migration v2 (`blocks.cold_storage INTEGER NOT NULL DEFAULT 0`) and the 5 new WorkspaceDO RPC methods (stampEmbedding, getBlocksByIds, updateBlockEnrichment, moveToInbox, moveToColdStorage) — every subsequent plan consumes these.
5. Flip the `RecallInputSchema.verbosity` default to `"chunks"` (D-01) and create RED test stubs for every new helper/boundary so the test surface is in place before Wave 1 helpers ship.
6. Touch up AI-SPEC.md §4 diagram (D-04), CLAUDE.md `## Ingest Pipeline` (D-07), and spike-findings-engram SKILL.md (D-05) so doc canon matches Phase 5 reality.

Output: BLOCKING routing tracker, two wrangler.jsonc + two wrangler.test.jsonc updates, triage-worker package scaffolding, schema v2 migration, 5 DO RPC methods, RecallInputSchema diff, 4 RED test stubs + 3 existing-test extensions, 3 doc touch-ups. `wave_0_complete: true` flipped in `05-VALIDATION.md` frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.claude/skills/spike-findings-engram/SKILL.md
@.planning/phases/05-ai-integration/05-CONTEXT.md
@.planning/phases/05-ai-integration/05-AI-SPEC.md
@.planning/phases/05-ai-integration/05-RESEARCH.md
@.planning/phases/05-ai-integration/05-VALIDATION.md
@.planning/phases/05-ai-integration/05-PATTERNS.md
@.planning/phases/04-core-tools-envelope/04-PHASE-5-HANDOFF.md
@.planning/phases/04-core-tools-envelope/04-CF-CODE-ASSIST-USAGE.md

<interfaces>
<!-- Existing surface the executor must NOT regress -->

From packages/workspace-do/src/queries.ts (insertBlock at lines 314–332 is the canonical typed-helper shape):
```typescript
// Sync helpers; positional `?` bindings only (Pitfall 8); throw NotFoundError("block", id) on zero rowsWritten.
export function insertBlock(sql: SqlStorage, block: Memory): void { /* ... */ }
export function getBlock(sql: SqlStorage, id: string): Memory { /* throws NotFoundError on miss */ }
export function deleteBlock(sql: SqlStorage, args: { id: string; cascade?: boolean }): { blocks_deleted: number; relations_deleted: number } { /* ... */ }
export function createInboxEntry(sql: SqlStorage, args: { ... }): InboxEntry { /* existing — moveToInbox delegates to this */ }
```

From packages/workspace-do/src/index.ts (every method follows this 3-line skeleton — first executable line is the STO-07 guard):
```typescript
insertBlock(args: { workspace_id: string; block: Memory }): void {
  this.assertOwnsWorkspace(args.workspace_id);          // STO-07 — MUST be first executable line
  insertBlockQuery(this.ctx.storage.sql, args.block);
}
```

From packages/workspace-do/src/migrations.ts (MIGRATIONS array shape — append, never modify):
```typescript
export interface Migration { readonly version: number; readonly name: string; readonly sql: string; }
export const MIGRATIONS: readonly Migration[] = [
  { version: 1, name: "v1_initial_schema", sql: V1_SQL },
  // Phase 5 appends { version: 2, name: "v2_cold_storage", sql: V2_SQL }
];
// Runner walks the array in order; reads _schema_migrations table; skips already-applied versions; ALTER TABLE ADD COLUMN is NOT idempotent at SQL layer, the version check IS the idempotency guarantee.
```

From packages/workspace-do/src/schema.ts (V1_SQL closing backtick at ~line 143 — append AFTER it):
```typescript
export const V1_SQL = `...` as const;  // 7 tables, 10 indexes, STO-04 embedding columns
// APPEND V2_SQL HERE — additive, do NOT modify V1_SQL
```

From packages/mcp-server/src/schemas.ts (line 65–67 — single-line default flip + comment update):
```typescript
// BEFORE (current):
// D-03 + spike-findings-engram §1: default flipped to "both" (BORDERLINE gate)
verbosity: z.enum(["synthesis", "chunks", "both"]).optional().default("both"),
// AFTER (Phase 5 D-01): default = "chunks"; enum unchanged.
```

From packages/mcp-server/wrangler.jsonc (current — additive only; DO NOT touch existing keys):
```jsonc
{
  // existing: name, main, compatibility_date, compatibility_flags, observability, dev, durable_objects, kv_namespaces, migrations ...
}
```
Phase 5 adds top-level `"ai"` object + `"vectorize"` array. Same diff to wrangler.test.jsonc.

From packages/mcp-server/wrangler.test.jsonc lines 6–10,15–22 (FND-08 lint-glob opt-out via `.test.jsonc` suffix — DO NOT rename to `.jsonc`):
```jsonc
// This file is intentionally `.test.jsonc` so scripts/lint-wrangler.mjs (FND-08)
// skips it — production configs declare via `wrangler.jsonc` only.
```

From packages/triage-worker/wrangler.jsonc (current minimal frame; cross-Worker DO via durable_objects.bindings with script_name):
```jsonc
// Add: "ai": { "binding": "AI" }, "vectorize": [...], "durable_objects": { "bindings": [{ "name": "WORKSPACE", "class_name": "WorkspaceDO", "script_name": "engram-mcp-server" }] }
// Per RESEARCH Open Question 4, use durable_objects.bindings with script_name (NOT services with entrypoint) — battle-tested pattern.
// Queue consumer block lands in Phase 6 PIP-01.
// Also stub env.ANALYTICS binding for type-safety (write-call sites land in Plan 05-07) — RESEARCH Open Question 9.
```

From packages/mcp-server/src/__tests__/envelope.test.ts (existing verbosity-branch tests at lines 90–119, snapshot lock at lines 152–158, suggestions-absent describe block at lines 165–189):
```typescript
// Snapshot test 152–158 will fail when META_GAPS gains synthesis-omitted/truncation/cold-storage entries (Plan 05-05).
// "suggestions absent" describe block 165–189 needs the recall it() weakened (Plan 05-05 owns the actual flip; this plan only adds the RED scaffold).
```

From packages/mcp-server/src/__tests__/tools-integration.test.ts (TOL-04 round-trip at lines 237–265 — extend with AI-08 it() block in this Wave 0):
```typescript
// captureCallback("remember"|"forget"|"recall", workspace_id) + parseEnvelope helpers at lines 56–88 — REUSE verbatim.
// Phase 5 AI-08 it() adds sleep(5000) after forget; assertion is RED in Wave 0 (memories.length === 0 fails because remember doesn't embed yet) → GREEN in Plan 05-03.
```

From packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts (two-prong describe at lines 103–166 — extend describe title to "3 prongs" + add Prong C it()):
```typescript
// Reuse captureCallback + parseEnvelope from lines 56–82.
// Prong C is RED in Wave 0 (no vectorizeQuery helper exists yet) → GREEN in Plan 05-03.
```

From .planning/phases/04-core-tools-envelope/04-CF-CODE-ASSIST-USAGE.md (analog to copy structure verbatim — swap Phase 4 → Phase 5, projected mix 0% → 40–60%):
```markdown
# Phase 4 — cf-code-assist Routing Tracker
> Instructions for the executor (and Claude orchestrating)
> For **every task** ... that produces code, append one row...
> Each row records: Task | Artifact | Route | Reason | Approx tokens saved
## Routing Log | _seed_ row...
## End-of-Phase Summary
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [BLOCKING]: Create the cf-code-assist routing tracker</name>
  <files>.planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md</files>
  <read_first>
    - .planning/phases/04-core-tools-envelope/04-CF-CODE-ASSIST-USAGE.md (analog — copy header + table schema verbatim)
    - CLAUDE.md §"Auto-Load Routing" Phase 5 tracker mandate
    - ~/.claude/CLAUDE.md §"AI Model Routing" — the 3-question checklist (1. SYNTHESIS step cross-file? 2. Diff >50 lines mechanical? 3. Stable template/spec to anchor on?)
    - .planning/phases/05-ai-integration/05-CONTEXT.md §"Claude's Discretion" — CF-code-assist routing tracker bullet
  </read_first>
  <action>
    Create `.planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md` by COPYING the Phase 4 tracker structure verbatim, then making these surgical edits:
    1. Title: "Phase 5 — cf-code-assist Routing Tracker"
    2. Header note: replace "Phase 4 ... first heavy-generation phase" with "Phase 5 is the AI Integration phase, projected as a content-generation phase that should route 40–60% to cf-code-assist (per project CLAUDE.md and ~/.claude/CLAUDE.md phase-character heuristic). Specific Phase 5 task shapes that should default to cf-code-assist: zod schemas (generateTypes), vitest eval scripts (scaffoldTests), Triage Worker queue consumer scaffold (generateWorkerBoilerplate), recall() instr()→Vectorize swap (transformCode), 429-aware retry wrapper (generateCode), Workers Analytics Engine event-write helper (generateCode)."
    3. Add a "3-Question Checklist (mandatory per row)" section BEFORE the routing-log table, restating the three questions verbatim from ~/.claude/CLAUDE.md and noting they MUST be answered BEFORE committing the route, not after.
    4. Extend the routing-log table header to include a "Checklist (Q1/Q2/Q3)" column with each cell formatted as "N/N/Y" or "Y/N/Y" (3-character answer slot per question).
    5. Seed row 1: same `_seed_` placeholder as Phase 4.
    6. Seed row 2: a real example row demonstrating the checklist application. Use: Task `05-01-T1`, Artifact "tracker file creation", Route "claude", Checklist "N/N/N", Reason "Doc creation, not code generation — tracker rules don't apply to itself.", Tokens saved "n/a".
    7. End-of-Phase Summary section: copy Phase 4's blank shell ("Fill in after /gsd:verify-work 5 passes. Do not pre-populate.") — same metric labels.
    Scope is execute time: every code-producing task in Plans 05-02..05-07 appends one row HERE before committing its route decision. This is the BLOCKING infrastructure for the entire Phase 5 cf-code-assist mandate.
    DO NOT write code or change project source in this task.
    # Route candidate: none (pure doc creation).
  </action>
  <what-built>The Phase 5 cf-code-assist routing tracker file (`.planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md`) with the same row/header schema as Phase 4's tracker, augmented with a per-row 3-question checklist column.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md` in an editor.
    2. Confirm the title says "Phase 5", the projected mix sentence mentions "40–60%", and the 3-Question Checklist section appears BEFORE the routing-log table.
    3. Confirm the routing-log table header includes the new "Checklist (Q1/Q2/Q3)" column.
    4. Confirm two seed rows exist: the `_seed_` placeholder and the example row for Task 05-01-T1.
    5. Confirm the End-of-Phase Summary section is present and unfilled.
    6. Reply "approved — tracker is BLOCKING ready" or describe issues.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md && grep -c "Phase 5 — cf-code-assist Routing Tracker" .planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md | grep -q "^[1-9]" && grep -q "3-Question Checklist" .planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md && grep -q "40–60%" .planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md && grep -q "Checklist (Q1/Q2/Q3)" .planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md && grep -q "05-01-T1" .planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md && echo OK</automated>
  </verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>Tracker file exists with the Phase 5 header, the 3-Question Checklist section, the augmented table header, both seed rows, and the End-of-Phase Summary shell. No subsequent task may bypass this — every code-producing task in 05-02..05-07 references this file in its &lt;action&gt; block.</done>
</task>

<task type="auto">
  <name>Task 2: Add AI + VECTORIZE bindings to mcp-server and triage-worker wrangler configs</name>
  <files>packages/mcp-server/wrangler.jsonc, packages/mcp-server/wrangler.test.jsonc, packages/triage-worker/wrangler.jsonc, packages/triage-worker/wrangler.test.jsonc</files>
  <read_first>
    - packages/mcp-server/wrangler.jsonc (current state — add top-level "ai" + "vectorize" + stub "analytics_engine_datasets" entries; preserve existing durable_objects, kv_namespaces, migrations verbatim)
    - packages/mcp-server/wrangler.test.jsonc (lines 6–22 for the FND-08 lint-glob opt-out JSDoc — mirror the same JSDoc on the new triage-worker `.test.jsonc`)
    - packages/triage-worker/wrangler.jsonc (current minimal — extend with ai, vectorize, durable_objects.bindings[script_name=engram-mcp-server], analytics_engine_datasets stub)
    - .planning/phases/05-ai-integration/05-PATTERNS.md S8 (`.test.jsonc` suffix opts out of FND-08 lint) + S9 (cross-Worker service binding via script_name)
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Example 1" + §"Example 2" + Open Question 4 (durable_objects.bindings with script_name, NOT services with entrypoint) + Open Question 9 (declare env.ANALYTICS binding in Wave 0 for type-safety; write-call sites in Plan 05-07)
    - scripts/lint-wrangler.mjs (FND-08 lint — confirm the lint glob is literal `packages/*/wrangler.jsonc` so `.test.jsonc` opts out)
  </read_first>
  <action>
    Make four surgical, additive-only wrangler edits. Preserve every existing key in each file verbatim.

    **packages/mcp-server/wrangler.jsonc** — add at the top level (after existing scalar keys, before `durable_objects`):
    ```jsonc
    "ai": { "binding": "AI" },
    "vectorize": [
      { "binding": "VECTORIZE", "index_name": "engram-memories" }
    ],
    "analytics_engine_datasets": [
      { "binding": "ANALYTICS", "dataset": "engram_ai_analytics" }
    ],
    ```

    **packages/mcp-server/wrangler.test.jsonc** — same three keys, but in the vectorize entry comment that integration tests (AI-02 namespace isolation, AI-08 round-trip) will flip to `"remote": true` in Plan 05-03/05-05 when real Vectorize is required; for Wave 0 the entry stays `remote: false` (default) so unit-test mocking via `vi.spyOn(env.VECTORIZE, "query")` works.

    **packages/triage-worker/wrangler.jsonc** — extend the existing minimal frame. Add ai, vectorize, durable_objects.bindings (single entry: name=WORKSPACE, class_name=WorkspaceDO, script_name="engram-mcp-server"), and analytics_engine_datasets stub. Include a `// queue consumer block lands in Phase 6 PIP-01` comment block where the queues consumer would go (no actual queues key yet — wrangler tolerates absence). Confirm compatibility_date matches mcp-server's `"2026-05-22"` exactly (S9).

    **packages/triage-worker/wrangler.test.jsonc** — NEW file mirroring `packages/mcp-server/wrangler.test.jsonc` shape: `name: "engram-triage-worker-test"`, `main: "src/index.ts"`, JSDoc comment block explaining FND-08 lint-glob opt-out via `.test.jsonc` suffix (S8). Same three bindings (ai/vectorize/analytics_engine_datasets stub) PLUS the durable_objects.bindings entry. No `compatibility_date` (matches the mcp-server test pattern; pool infers latest).

    After editing, run `node scripts/lint-wrangler.mjs` and confirm it still passes for both production configs (FND-08 invariant). Run `npm run types:gen --workspace=packages/mcp-server` and same for triage-worker — confirm regenerated `worker-configuration.d.ts` includes the new `AI`, `VECTORIZE`, `ANALYTICS`, and (for triage) `WORKSPACE` bindings in the `Env` interface.
    # Route candidate: claude (cross-cutting wrangler edit with FND-08 lint dependency; <30 lines diff but Claude needs to verify lint passes + types regen across two packages simultaneously).
  </action>
  <verify>
    <automated>node scripts/lint-wrangler.mjs && grep -q "\"VECTORIZE\"" packages/mcp-server/wrangler.jsonc && grep -q "\"VECTORIZE\"" packages/mcp-server/wrangler.test.jsonc && grep -q "\"VECTORIZE\"" packages/triage-worker/wrangler.jsonc && grep -q "\"VECTORIZE\"" packages/triage-worker/wrangler.test.jsonc && grep -q "\"AI\"" packages/mcp-server/wrangler.jsonc && grep -q "\"AI\"" packages/triage-worker/wrangler.jsonc && grep -q "\"script_name\": \"engram-mcp-server\"" packages/triage-worker/wrangler.jsonc && grep -q "\"script_name\": \"engram-mcp-server\"" packages/triage-worker/wrangler.test.jsonc && grep -q "ANALYTICS" packages/mcp-server/wrangler.jsonc && echo OK</automated>
  </verify>
  <done>
    Four wrangler configs have AI + VECTORIZE + ANALYTICS bindings (and WORKSPACE cross-Worker DO binding for triage-worker). FND-08 lint still passes on both production configs (the lint scans only `packages/*/wrangler.jsonc`, not `.test.jsonc`). Regenerated `worker-configuration.d.ts` files type-check the new bindings in each package's `Env` interface.
  </done>
</task>

<task type="auto">
  <name>Task 3: Stand up triage-worker test infra (vitest.config.ts + package.json deps + tsconfig)</name>
  <files>packages/triage-worker/vitest.config.ts, packages/triage-worker/package.json, packages/triage-worker/tsconfig.json</files>
  <read_first>
    - packages/mcp-server/vitest.config.ts (analog — copy verbatim, change `@module` JSDoc package name)
    - packages/mcp-server/package.json (analog for scripts + devDependencies)
    - packages/triage-worker/package.json (current state — preserve `@engram/types` + `@engram/schema` workspace links)
    - packages/triage-worker/tsconfig.json (if exists — confirm strict TS + verbatimModuleSyntax compatible with new vitest harness)
    - .planning/phases/05-ai-integration/05-PATTERNS.md S7 (single workerd project vitest config — NOT multi-project)
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Test infrastructure gap (Wave 0 must close)" — explicit dep list (vitest@^4.1.7, @cloudflare/vitest-pool-workers@^0.16.9, zod@^4.0.0). zod-to-json-schema install is DEFERRED to Plan 05-04 behind a Package Legitimacy Audit checkpoint.
    - .planning/phases/05-ai-integration/05-VALIDATION.md §"Wave 0 Requirements" (triage-worker vitest + wrangler.test.jsonc + package.json deps)
  </read_first>
  <action>
    1. **packages/triage-worker/vitest.config.ts (NEW):** Copy `packages/mcp-server/vitest.config.ts` verbatim. Change the JSDoc `@module @engram/mcp-server/vitest.config` → `@module @engram/triage-worker/vitest.config`. Keep `wrangler: { configPath: "./wrangler.test.jsonc" }`. Keep `test.include: ["src/__tests__/**/*.test.ts"]`. Triage Worker has NO multi-project setup (mcp-server's pattern is single-pool already — copy is straightforward).

    2. **packages/triage-worker/package.json:** Use `npm install --workspace=packages/triage-worker --save-dev` to add `@cloudflare/vitest-pool-workers@^0.16.9` and `vitest@^4.1.7` (versions MUST match `packages/mcp-server/package.json` exactly — no drift per S7). Use `npm install --workspace=packages/triage-worker` (regular dep, not dev) to add `zod@^4.0.0` (matches mcp-server's pin). Add `"test": "vitest run"` and `"test:watch": "vitest"` scripts.

       Explicitly do NOT install `zod-to-json-schema` here — that gate lives in Plan 05-04 Task 1 behind a `checkpoint:human-verify` (Package Legitimacy Audit). The package.json devDependencies block lands without it; Plan 05-04 adds it after the human-verify gate.

       Run `npm install` (no args) from repo root afterward to refresh the workspace lockfile.

    3. **packages/triage-worker/tsconfig.json:** If it exists, confirm it extends the same base as mcp-server and inherits `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`, `verbatimModuleSyntax`. If it doesn't exist, create it by copying `packages/mcp-server/tsconfig.json` and adjusting only the `compilerOptions.types` and `include` paths to point at the triage-worker package. The vitest harness needs TS to compile the new test files.

    Verify with: `npm test --workspace=packages/triage-worker` exits 0 (no tests yet but the harness must bootstrap cleanly without "no test files" being a failure — vitest exits 0 on empty discovery by default; if it doesn't, add `"passWithNoTests": true` to the test command).
    # Route candidate: claude (multi-file config wiring with cross-package version-pin consistency check; <50 lines but synthesis cross-file).
  </action>
  <verify>
    <automated>test -f packages/triage-worker/vitest.config.ts && test -f packages/triage-worker/tsconfig.json && grep -q "@cloudflare/vitest-pool-workers" packages/triage-worker/package.json && grep -q "\"vitest\"" packages/triage-worker/package.json && grep -q "\"zod\"" packages/triage-worker/package.json && ! grep -q "zod-to-json-schema" packages/triage-worker/package.json && grep -q "\"test\"" packages/triage-worker/package.json && npm test --workspace=packages/triage-worker --silent --passWithNoTests=true 2>&1 | tail -5 && echo OK</automated>
  </verify>
  <done>
    The triage-worker package has a working vitest-pool-workers harness, declares zod + vitest + vitest-pool-workers (versions matching mcp-server exactly), and `npm test --workspace=packages/triage-worker` exits 0. `zod-to-json-schema` is intentionally NOT installed (Plan 05-04 owns the install-after-audit step). The triage-worker test surface is ready for Plan 05-04 to drop in `extract.test.ts` + Plan 05-06 to drop in `memorability-calibration.eval.test.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Schema migration v2 — append blocks.cold_storage column + extend Memory type</name>
  <files>packages/workspace-do/src/schema.ts, packages/workspace-do/src/migrations.ts, packages/workspace-do/src/types.ts</files>
  <read_first>
    - packages/workspace-do/src/schema.ts (V1_SQL ends ~line 143 — append V2_SQL AFTER the closing backtick; do NOT modify V1_SQL)
    - packages/workspace-do/src/migrations.ts lines 42–53 (Migration interface + MIGRATIONS array shape — append v2 entry to the array)
    - packages/workspace-do/src/types.ts (Memory type — add `cold_storage?: boolean` field as optional, since v0.1 callers don't need to read it on the hot path per RESEARCH §Example 9 note)
    - .planning/phases/05-ai-integration/05-PATTERNS.md S12 (forward-only migrations via _schema_migrations) + analog excerpt for V2_SQL
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Example 9" + Pitfall 4 + §"Runtime State Inventory" (cold_storage is a SQLite column, NOT a DO migration; wrangler.jsonc `[[migrations]]` is unchanged)
    - .planning/phases/05-ai-integration/05-CONTEXT.md D-07 (cold-storage routing, NOT discard)
    - .planning/phases/02-workspacedo-sqlite/02-CONTEXT.md D-01..D-04 (sync helpers, throw on miss, JSON parsed at boundary, explicit cascade — all carry forward to the new methods)
  </read_first>
  <action>
    1. **packages/workspace-do/src/schema.ts** — APPEND a new exported constant `V2_SQL` after the V1_SQL closing backtick. Do NOT modify V1_SQL. Use this shape verbatim per S12 + RESEARCH §Example 9:
       ```typescript
       export const V2_SQL = `
         ALTER TABLE blocks ADD COLUMN cold_storage INTEGER NOT NULL DEFAULT 0;
         CREATE INDEX IF NOT EXISTS idx_blocks_cold_storage ON blocks(cold_storage);
       ` as const;
       ```
       Add a JSDoc block above explaining: forward-only via _schema_migrations runner; ALTER TABLE ADD COLUMN is NOT idempotent at SQL layer — the version check in the runner IS the idempotency guarantee; INTEGER not BOOLEAN because SQLite has no native BOOLEAN type (Phase 2 used INTEGER for timestamps + REAL for confidence — stay consistent).

    2. **packages/workspace-do/src/migrations.ts** — extend the import line from `import { V1_SQL } from "./schema.js"` to `import { V1_SQL, V2_SQL } from "./schema.js"`. Append a second entry to the MIGRATIONS array: `{ version: 2, name: "v2_cold_storage", sql: V2_SQL }`. Do NOT modify the v1 entry or the runner body — the runner reads MIGRATIONS in version order, skips already-applied entries via the _schema_migrations table check.

    3. **packages/workspace-do/src/types.ts** — extend the `Memory` type with `cold_storage?: boolean` (optional, since v0.1 callers don't read it on the hot path; the `getBlocksByIds` helper in Task 5 EXCLUDES cold rows via SQL WHERE, so the field arriving at the call site is non-cold by construction). Add JSDoc: "Phase 5 D-07: blocks with memorability <0.4 are routed to cold-storage by the Triage Worker. Excluded from default recall results; v0.2's `include_cold` flag will surface them."

    4. Run `npm test --workspace=packages/workspace-do` — confirm the Phase 2 hibernation-replay test (`02-04`/`02-05` plan SUMMARYs reference this) still passes; the v2 migration must be applied exactly once across DO restarts. Note: Phase 2's existing schema/migration test will exercise the new migration on first init.
    # Route candidate: claude (multi-file additive edit with v1-preservation invariant + Memory type cross-package impact; <40 lines but synthesis cross-file across schema/migrations/types/tests).
  </action>
  <verify>
    <automated>grep -q "V2_SQL" packages/workspace-do/src/schema.ts && grep -q "cold_storage" packages/workspace-do/src/schema.ts && grep -q "V1_SQL, V2_SQL" packages/workspace-do/src/migrations.ts && grep -q "v2_cold_storage" packages/workspace-do/src/migrations.ts && grep -q "cold_storage" packages/workspace-do/src/types.ts && npm test --workspace=packages/workspace-do --silent 2>&1 | tail -10 && echo OK</automated>
  </verify>
  <done>
    `V2_SQL` exists in `schema.ts` (V1_SQL untouched); `MIGRATIONS` array has a v2 entry; `Memory` type has `cold_storage?: boolean`; Phase 2 workspace-do test suite (hibernation-replay included) still passes — verifying the migration runs once and is idempotent across restarts.
  </done>
</task>

<task type="auto">
  <name>Task 5: Add 5 WorkspaceDO RPC methods (stampEmbedding, getBlocksByIds, updateBlockEnrichment, moveToInbox, moveToColdStorage)</name>
  <files>packages/workspace-do/src/queries.ts, packages/workspace-do/src/index.ts</files>
  <read_first>
    - packages/workspace-do/src/queries.ts lines 314–332 (insertBlock — canonical sync-helper shape) + lines 345–367 (getBlock — throw-on-miss pattern) + lines 389–406 (lexicalSearchBlocks — IN-clause + positional bindings + narrowBlockRow) + lines 424–442 (deleteBlock — rowsWritten return shape) + the existing `createInboxEntry` helper (moveToInbox delegates to it)
    - packages/workspace-do/src/index.ts lines 73–82 (queries import block — extend with rename-on-import for 5 new helpers) + lines 139–146 (assertOwnsWorkspace guard implementation) + lines 163–206 (every existing method's 3-line skeleton — copy verbatim)
    - packages/workspace-do/src/types.ts (Memory type — extended in Task 4; new helpers import Memory + use narrowBlockRow if hydration is needed)
    - .planning/phases/05-ai-integration/05-PATTERNS.md §"packages/workspace-do/src/queries.ts (MODIFY — add 5 query helpers)" + §"packages/workspace-do/src/index.ts (MODIFY — expose 5 new RPC methods)" + S3 (mandatory workspace_id routing) + S12 (forward-only migrations)
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Example 8" (WorkspaceDO new methods)
    - .planning/phases/05-ai-integration/05-AI-SPEC.md §"State Management" (which column writes belong to which method)
    - .planning/phases/05-ai-integration/05-CONTEXT.md D-07 (moveToColdStorage NOT discardWithLog — cardinal-sin framing) + D-06 (AI-SPEC.md inherited verbatim except where amended)
  </read_first>
  <action>
    Add 5 typed sync helpers to `packages/workspace-do/src/queries.ts` and wrap each as a DO RPC method in `packages/workspace-do/src/index.ts`. Use the canonical insertBlock pattern (lines 314–332) as the template; every helper takes `sql: SqlStorage` as the first arg, uses positional `?` bindings only (Pitfall 8), single-statement `.exec()`, and throws `NotFoundError("block", id)` on zero rowsWritten when the helper expects a row.

    **5 new query helpers in queries.ts (appended after deleteBlock):**

    1. `stampEmbedding(sql, args: { block_id: string; embedding_model: string; embedding_version: number }): void` — `UPDATE blocks SET embedding_model = ?, embedding_version = ?, updated_at = ? WHERE id = ?`. Throw `NotFoundError("block", args.block_id)` on zero rowsWritten.

    2. `getBlocksByIds(sql, ids: string[]): Memory[]` — early-return `[]` if ids.length === 0. Build IN-clause via positional placeholders (`ids.map(() => "?").join(", ")`). SQL: `SELECT id, type, content, summary, properties, embedding_id, embedding_model, embedding_version, scope, project_id, source, confidence, created_at, updated_at FROM blocks WHERE id IN (...) AND cold_storage = 0` (the AND cold_storage = 0 clause excludes cold blocks per RESEARCH §Open Question 10 belt-and-suspenders — even though Vectorize never indexed cold vectors, the SQL filter is the second line of defense). Map rows through `narrowBlockRow` (the existing helper in queries.ts).

    3. `updateBlockEnrichment(sql, args: { block_id: string; properties: Record<string, unknown>; summary: string; confidence: number }): void` — `UPDATE blocks SET properties = ?, summary = ?, confidence = ?, updated_at = ? WHERE id = ?`. JSON.stringify the properties arg before binding. Throw `NotFoundError("block", args.block_id)` on zero rowsWritten.

    4. `moveToInbox(sql, args: { block_id: string; entry: { content: string; proposed_type: string; proposed_properties: Record<string, unknown>; memorability_score: number; source: string } }): void` — delegate to the existing `createInboxEntry` helper (lines 472–483 of queries.ts) so the inbox table insert pattern stays single-source. Pass `args.entry` directly; the existing helper handles JSON serialization.

    5. `moveToColdStorage(sql, args: { block_id: string; properties?: Record<string, unknown>; summary?: string; confidence?: number; memorability: number }): void` — `UPDATE blocks SET cold_storage = 1, properties = COALESCE(?, properties), summary = COALESCE(?, summary), confidence = COALESCE(?, confidence), updated_at = ? WHERE id = ?`. COALESCE allows the Triage Worker to ship enrichment data alongside the cold-storage marker without forcing the caller to pre-merge with existing values. Throw NotFoundError on zero rowsWritten.

    **5 new DO RPC methods in index.ts (appended after deleteBlock method):**

    Each method follows the canonical 3-line skeleton from line 163–166 verbatim:
    ```typescript
    methodName(args: { workspace_id: string; ... }): ReturnType {
      this.assertOwnsWorkspace(args.workspace_id);              // STO-07 — MUST be first executable line
      return methodNameQuery(this.ctx.storage.sql, { ... });    // delegate to typed helper
    }
    ```
    Rename-on-import each helper as `XxxQuery` to match the existing `insertBlockQuery / getBlockQuery / ...` convention (lines 73–82). Sync return types (`void` for writes, `Memory[]` for getBlocksByIds).

    After adding, run `npm test --workspace=packages/workspace-do` — the Phase 2 STO-07 defense-in-depth test (`02-06-PLAN.md`) verifies that calling a method with a mismatched workspace_id throws `McpError(InvalidRequest)`; the new methods MUST inherit this behavior (the assertOwnsWorkspace call is the proof).

    Add per-method JSDoc that cites the requirement (`AI-03` for stampEmbedding, `AI-04` for getBlocksByIds, `AI-05`+`AI-06` for the three Triage-routing methods) and notes the per-D-07 cold-storage routing for moveToColdStorage (cardinal-sin clause cite).
    # Route candidate: cf-code-assist:generateCode (5 mechanical typed helpers + 5 mechanical method wrappers, ~120 lines total, sentinel-anchored on existing insertBlock pattern, stable spec from PATTERNS.md + RESEARCH §Example 8). Q1: synthesis IS within a single package (queries.ts + index.ts in same workspace, no cross-package coordination) — answer: N. Q2: >50 lines mechanical: Y. Q3: stable template: Y. Answer N/Y/Y → ROUTE to cf-code-assist. Log row in 05-CF-CODE-ASSIST-USAGE.md BEFORE executing.
  </action>
  <verify>
    <automated>grep -q "export function stampEmbedding" packages/workspace-do/src/queries.ts && grep -q "export function getBlocksByIds" packages/workspace-do/src/queries.ts && grep -q "export function updateBlockEnrichment" packages/workspace-do/src/queries.ts && grep -q "export function moveToInbox" packages/workspace-do/src/queries.ts && grep -q "export function moveToColdStorage" packages/workspace-do/src/queries.ts && grep -q "cold_storage = 0" packages/workspace-do/src/queries.ts && grep -q "stampEmbedding(args:" packages/workspace-do/src/index.ts && grep -q "getBlocksByIds(args:" packages/workspace-do/src/index.ts && grep -q "moveToColdStorage(args:" packages/workspace-do/src/index.ts && grep -v '^[[:space:]]*//' packages/workspace-do/src/index.ts | grep -c "this.assertOwnsWorkspace(args.workspace_id)" | grep -qE "^(1[2-9]|[2-9][0-9])$" && npm test --workspace=packages/workspace-do --silent 2>&1 | tail -10 && echo OK</automated>
  </verify>
  <done>
    queries.ts has 5 new typed helpers; index.ts has 5 new DO RPC methods, each calling assertOwnsWorkspace as the first executable line (grep confirms ≥12 total assertOwnsWorkspace call sites after additions — 7 pre-existing + 5 new). `getBlocksByIds` filters out cold-storage rows (`AND cold_storage = 0`). Phase 2 workspace-do test suite still passes. The Triage Worker (Plan 05-04) and recall/remember handlers (Plans 05-03/05-05) can now call these methods.
  </done>
</task>

<task type="auto">
  <name>Task 6: Flip RecallInputSchema.verbosity default to "chunks" (D-01)</name>
  <files>packages/mcp-server/src/schemas.ts</files>
  <read_first>
    - packages/mcp-server/src/schemas.ts lines 56–67 (current RecallInputSchema; lines 65–66 are the single-line target)
    - .planning/phases/05-ai-integration/05-CONTEXT.md D-01 (verbosity default flips from "both" to "chunks"; enum unchanged) + D-05 (spike-findings supersession note)
    - .planning/phases/05-ai-integration/05-PATTERNS.md §"packages/mcp-server/src/schemas.ts (MODIFY — verbosity default)" — exact single-line diff + comment update
    - .planning/phases/04-core-tools-envelope/04-PHASE-5-HANDOFF.md §"What Phase 5 Should NOT Change" — verbosity enum is locked; only default flips
  </read_first>
  <action>
    Single-line diff at `packages/mcp-server/src/schemas.ts` line 66. Replace:
    ```typescript
    // D-03 + spike-findings-engram §1: default flipped to "both" (BORDERLINE gate)
    verbosity: z.enum(["synthesis", "chunks", "both"]).optional().default("both"),
    ```
    with:
    ```typescript
    // D-01 (Phase 5): default = "chunks" — synthesis is opt-in per recall-latency budget (2–5s LLM tax).
    // Phase 4 D-03 was "both"; Phase 5 supersedes per CONTEXT.md D-01. Discoverability triad (tool description + meta.gaps + suggestions.actions) lands in Plan 05-05.
    verbosity: z.enum(["synthesis", "chunks", "both"]).optional().default("chunks"),
    ```
    The enum shape MUST stay unchanged (3 values, same order) per Phase 4 hand-off lock. Only the `.default()` arg + the preceding comment lines change.

    Run `npm test --workspace=packages/mcp-server -- schemas` and confirm the existing `schemas.test.ts` constraint assertions still pass for the enum shape (they should — no enum change). NOTE: any existing test that asserted the default was `"both"` will fail here; if such an assertion exists, this task does NOT fix it (Plan 05-05 owns the envelope.test.ts D-03 verbosity parameterization). If schemas.test.ts has such an assertion, flag in the SUMMARY for Plan 05-05.
    # Route candidate: claude (single-line diff with cross-doc rationale citation; <10 lines but tightly load-bearing — needs to update comment text accurately).
  </action>
  <verify>
    <automated>grep -q '.default("chunks")' packages/mcp-server/src/schemas.ts && grep -q "D-01 (Phase 5)" packages/mcp-server/src/schemas.ts && grep -q "z.enum(\[\"synthesis\", \"chunks\", \"both\"\])" packages/mcp-server/src/schemas.ts && npm test --workspace=packages/mcp-server -- schemas --run 2>&1 | tail -10 && echo OK</automated>
  </verify>
  <done>
    RecallInputSchema.verbosity default = "chunks"; enum shape unchanged; comment cites D-01. schemas.test.ts enum-shape assertions still pass (any default-value test failures are flagged for Plan 05-05).
  </done>
</task>

<task type="auto">
  <name>Task 7: RED test stubs for Phase 5 helpers + existing-test scaffold extensions</name>
  <files>packages/mcp-server/src/__tests__/vectorize-helper.test.ts, packages/mcp-server/src/__tests__/ai-helper.test.ts, packages/mcp-server/src/__tests__/hybrid-rank.test.ts, packages/triage-worker/src/__tests__/extract.test.ts, packages/mcp-server/src/__tests__/envelope.test.ts, packages/mcp-server/src/__tests__/tools-integration.test.ts, packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts</files>
  <read_first>
    - packages/mcp-server/src/__tests__/envelope.test.ts (current describe/it shape — lines 47–58 import block, lines 64–146 pure-function tests, lines 90–119 verbosity branches, lines 152–158 snapshot, lines 165–189 suggestions-absent block)
    - packages/mcp-server/src/__tests__/tools-integration.test.ts lines 56–88 (`captureCallback` + `parseEnvelope` helpers — reuse verbatim) + lines 237–265 (TOL-04 round-trip pattern)
    - packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts lines 56–82 (helpers) + lines 103–166 (two-prong describe — extend title + add Prong C it())
    - packages/mcp-server/src/__tests__/error-mapping.test.ts (per-branch one-it pattern for pure unit tests)
    - .planning/phases/05-ai-integration/05-PATTERNS.md §"vectorize-helper.test.ts" + §"ai-helper.test.ts" + §"hybrid-rank.test.ts" + §"extract.test.ts" + §"envelope.test.ts (MODIFY)" + §"tools-integration.test.ts (MODIFY)" + §"cross-workspace-pentest.test.ts (MODIFY)" — full per-file analog excerpts
    - .planning/phases/05-ai-integration/05-VALIDATION.md §"Wave 0 Requirements" (4 RED test stubs + 3 existing-test extensions)
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Test Patterns" + §"Forcing a 429 in tests" (dual-path 429 patterns for ai-helper.test.ts + extract.test.ts)
    - .planning/phases/05-ai-integration/05-CONTEXT.md D-03 (envelope.test.ts verbosity parameterization)
  </read_first>
  <action>
    Create 4 NEW test files as RED stubs + extend 3 existing test files with RED it() blocks. Every test must compile but FAIL on assertion (because the production code under test does not exist yet — Plans 05-02/05-03/05-04 turn them GREEN). Use `it.fails()` or simply assertion-based RED — prefer assertion-based so the failing state surfaces a clear reason.

    **NEW test files (RED — fail because the helper modules don't exist yet):**

    1. **packages/mcp-server/src/__tests__/vectorize-helper.test.ts** — per PATTERNS.md analog. Import `{ vectorizeQuery, vectorizeUpsert, vectorizeDelete }` from `"../vectorize-helper.js"` (will fail to resolve until Plan 05-02 creates the file). Describe block titled `"vectorize-helper (AI-02 namespace mandatory)"`. Three it() stubs:
       - "vectorizeQuery sets namespace = workspace_id on every call" — mock env with vi.spyOn capture of opts, assert `opts.namespace === "ws-test-001"`.
       - "rejects namespace > 64 bytes" — assert throw with `/64-byte namespace/` regex.
       - "vectorizeUpsert stamps namespace on every vector regardless of caller intent" — pass vectors WITHOUT namespace, assert the helper adds it.

    2. **packages/mcp-server/src/__tests__/ai-helper.test.ts** — per PATTERNS.md analog. Import `{ EMBEDDING_MODEL, CLASSIFIER_MODEL, EMBEDDING_VERSION, detectRateLimit, isRateLimitError, safeRun, RateLimitError }` from `"../ai-helper.js"` (will fail until Plan 05-02). Three describe blocks:
       - `"AI helper constants (AI-03/04 drift guard)"` — assert `EMBEDDING_MODEL === "@cf/baai/bge-base-en-v1.5"`, `EMBEDDING_VERSION === 1`, `CLASSIFIER_MODEL === "@cf/meta/llama-3.1-8b-instruct"`; cross-file equality check stub (will be wired to triage-worker's sibling ai-helper in Plan 05-04 — comment a placeholder it.todo for now).
       - `"AI-07: 429 detection envelope path (detectRateLimit)"` — 4 it() blocks per RESEARCH §"Test Patterns" code 7501, code 3036, code 3040, message-regex match — all return true; success:true returns false.
       - `"AI-07: 429 detection thrown path (isRateLimitError)"` — 3 it() blocks: status 429 → true; name AiError + message /429/ → true; generic Error("invalid model id") → false.

    3. **packages/mcp-server/src/__tests__/hybrid-rank.test.ts** — per PATTERNS.md analog. Import `{ hybridRank, HYBRID_WEIGHTS }` (will fail until Plan 05-02). Describe `"hybridRank (AI-04 formula)"` with 5 it() blocks: locked-weights, recent-block-scores-higher, type_match-boost, scope_match-boost, monotonic-reorder. Stub assertions to AI-SPEC.md §4 starting values: `{ cosine: 1.0, recency: 0.15, type_match: 0.2, scope_match: 0.15 }`.

    4. **packages/triage-worker/src/__tests__/extract.test.ts** — per PATTERNS.md analog. Import `{ extractAndScore }` from `"../extract.js"` (will fail until Plan 05-04). Describe `"AI-05 + AI-07: extractAndScore"` with 4 it() blocks: dual-path 429 #1 (thrown), dual-path 429 #2 (envelope), Zod parse fail + attempts<2 → retry({delaySeconds:5}), Zod parse fail + attempts>=2 → ack. Use the `vi.fn()` message mock shape from RESEARCH §"Example 7".

    **EXISTING-test extensions (RED — fail because dependent production code doesn't exist yet):**

    5. **packages/mcp-server/src/__tests__/envelope.test.ts** — APPEND a new describe block titled `"buildRecallResponse — D-01 default flip + D-02 discoverability triad (RED until Plan 05-05)"` with 3 it() blocks per PATTERNS.md analog: default-verbosity-chunks asserts synthesis-null + suggestions.actions present + meta.gaps includes opt-in hint; verbosity-synthesis asserts synthesis-populated + suggestions absent; verbosity-both asserts both. These will fail until Plan 05-05 extends `buildRecallResponse` with the new params and META_GAPS.recallChunksOmittedSynthesis. Do NOT change the existing assertions in lines 90–119 or 165–189 in this task (Plan 05-05 owns those edits — flagging here would create cross-plan churn).

    6. **packages/mcp-server/src/__tests__/tools-integration.test.ts** — APPEND a new describe block `"AI-08: forget cascade with Vectorize delete + eventual consistency (RED until Plan 05-03)"` with one it() per PATTERNS.md analog: `remember → forget → sleep(5_000) → recall returns 0 memories`. Reuse captureCallback + parseEnvelope verbatim from lines 56–88. RED because remember() doesn't embed yet, so the recall after sleep returns whatever the lexical fallback finds (likely non-zero on a content-match query) — Plan 05-03 makes this GREEN by wiring embed + Vectorize delete.

    7. **packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts** — EXTEND the describe-block title from "two prongs" to "3 prongs required" and APPEND a new it() block `"AI-02 Prong C: vector upserted under workspace_A namespace NOT returned by query in workspace_B"` per PATTERNS.md analog. Reuse captureCallback. RED until Plan 05-03 wires vectorizeUpsert and remember()'s embed path.

    All 7 files must compile cleanly (vitest can discover them) and `npm test --workspaces` must report failures (not compile errors) on the new tests. Run `npm test --workspace=packages/mcp-server --silent 2>&1 | tail -30` and confirm the new failures are assertion-failures or module-not-found — these are expected RED state until Wave 1/2 ships the production code.
    # Route candidate: cf-code-assist:scaffoldTests (4 NEW test files following PATTERNS.md exact shape; 3 existing-file extensions adding describe blocks per analog excerpts; ~250 lines total mechanical scaffolding from stable spec). Q1: synthesis crosses test files within mcp-server and triage-worker BUT each file has its own analog excerpt that the spec packages independently — answer: N (within-file synthesis, helper-shared captureCallback is single import). Q2: >50 lines mechanical: Y. Q3: stable template: Y (PATTERNS.md has per-file analog code blocks). Answer N/Y/Y → ROUTE to cf-code-assist for the 4 NEW files; the 3 existing-file extensions stay with Claude because they need precise diff placement (append-to-existing-describe-block — small-diff cross-file). Log TWO routing rows in 05-CF-CODE-ASSIST-USAGE.md (one for the 4 NEW files routed to cf-code-assist:scaffoldTests, one for the 3 extensions kept with Claude).
  </action>
  <verify>
    <automated>test -f packages/mcp-server/src/__tests__/vectorize-helper.test.ts && test -f packages/mcp-server/src/__tests__/ai-helper.test.ts && test -f packages/mcp-server/src/__tests__/hybrid-rank.test.ts && test -f packages/triage-worker/src/__tests__/extract.test.ts && grep -q "AI-02 Prong C" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts && grep -q "AI-08: forget cascade" packages/mcp-server/src/__tests__/tools-integration.test.ts && grep -q "D-01 default flip" packages/mcp-server/src/__tests__/envelope.test.ts && grep -q "3 prongs required" packages/mcp-server/src/__tests__/cross-workspace-pentest.test.ts && (npm test --workspace=packages/mcp-server --silent 2>&1 | grep -E "(FAIL|failed)" | head -3) && echo "RED-state verified — assertion/resolve failures expected"</automated>
  </verify>
  <done>
    7 test files have new RED stubs. Production code under test does not yet exist (Wave 1/2 ships it). The test surface for AI-02 / AI-03 / AI-04 / AI-05 / AI-06 / AI-07 / AI-08 is in place; Plans 05-02/05-03/05-04/05-05 flip them GREEN by shipping the corresponding handlers/helpers. Plan 05-05 owns the further amendments to envelope.test.ts (snapshot update + suggestions-absent describe weakening).
  </done>
</task>

<task type="auto">
  <name>Task 8: Doc touch-ups — AI-SPEC.md §4, CLAUDE.md Ingest Pipeline, spike-findings SKILL.md, VALIDATION.md frontmatter</name>
  <files>.planning/phases/05-ai-integration/05-AI-SPEC.md, CLAUDE.md, .claude/skills/spike-findings-engram/SKILL.md, .planning/phases/05-ai-integration/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-ai-integration/05-AI-SPEC.md §4 "Core Pattern" recall contract diagram (line ~405 — the `[optional per verbosity arg]` line for synthesis)
    - CLAUDE.md §"## Ingest Pipeline" (the `< 0.4 → discard` branch that becomes `< 0.4 → cold-storage`)
    - .claude/skills/spike-findings-engram/SKILL.md `<requirements>` block — the line "verbosity parameter default is 'both' on recall(), NOT 'synthesis'"
    - .planning/phases/05-ai-integration/05-VALIDATION.md frontmatter (`wave_0_complete: false`, `nyquist_compliant: false`)
    - .planning/phases/05-ai-integration/05-CONTEXT.md D-04 (AI-SPEC.md diagram amendment) + D-05 (SKILL.md supersession note) + D-07 (cold-storage replaces discard) + §"Claude's Discretion" memorability routing
  </read_first>
  <action>
    Four surgical doc edits — additive annotations, not rewrites. Preserve every existing line that isn't being explicitly amended.

    1. **`.planning/phases/05-ai-integration/05-AI-SPEC.md` §4 Core Pattern (recall flow ~line 405–425):** the line `├─► env.AI.run("@cf/meta/llama-3.1-8b-instruct", { ... synthesis ... }) [optional per verbosity arg]` already says "optional" but doesn't make the default-skip explicit. Add a one-line note BELOW that arrow:
       > `  │   D-01 (Phase 5 CONTEXT.md): SKIPPED on default recall (verbosity="chunks"). Runs ONLY when verbosity ∈ {"synthesis", "both"}. Default recall returns synthesis=null + meta.gaps + suggestions.actions discoverability triad.`
       Preserve the arrow ASCII structure.

    2. **`CLAUDE.md` §"## Ingest Pipeline" (the Triage Worker steps block; look for "memorability" + "discard"):** locate the line/branch that says "<0.4 → discard". Replace with:
       > `     < 0.4  → cold-storage (per Phase 5 CONTEXT.md D-07 cardinal-sin clause — NEVER discard)`
       Also amend the surrounding prose paragraph (the one that says "If a task can be done by Cloudflare AI...") to reflect that cold-storage is the v0.1 routing per D-07. Cite `.planning/phases/05-ai-integration/05-CONTEXT.md` for the decision audit trail.

    3. **`.claude/skills/spike-findings-engram/SKILL.md` `<requirements>` block:** find the line `**verbosity parameter default is "both" on recall(), NOT "synthesis"**` and append a Phase 5 supersession annotation directly after that line (same bullet, new paragraph):
       > `  *Phase 5 supersession (2026-05-28, .planning/phases/05-ai-integration/05-CONTEXT.md D-01 / D-05): the default is "chunks", not "both". Synthesis is opt-in. The BORDERLINE quality finding still drives the F1 ≥ 75% gate on AI-04 closure — when callers opt in, synthesis must hit the bar. This line is historically accurate; D-01 is normatively binding.*`
       Do NOT delete the original line — append the annotation alongside.

    4. **`.planning/phases/05-ai-integration/05-VALIDATION.md` frontmatter:** flip `wave_0_complete: false` → `wave_0_complete: true` and `nyquist_compliant: false` → `nyquist_compliant: true`. These are the closing acknowledgments that all Wave 0 Requirements (listed in the same file) are addressed by this plan's tasks. Add a date stamp at the bottom of the file: `**Wave 0 closed:** 2026-05-XX (Plan 05-01)`. Update the `## Validation Sign-Off` checklist by ticking ALL boxes — the validation contract is met when this plan ships.
    # Route candidate: claude (doc edits with precise cross-document citation requirements; <60 lines diff but synthesis cross-document — each amendment cites a specific CONTEXT.md decision ID, AI-SPEC.md section anchor, or VALIDATION.md frontmatter field. cf-code-assist would risk doc-anchor drift.).
  </action>
  <verify>
    <automated>grep -q "D-01 (Phase 5 CONTEXT.md): SKIPPED on default recall" .planning/phases/05-ai-integration/05-AI-SPEC.md && grep -q "cold-storage (per Phase 5 CONTEXT.md D-07" CLAUDE.md && ! grep -q "< 0.4  → discard" CLAUDE.md && grep -q "Phase 5 supersession" .claude/skills/spike-findings-engram/SKILL.md && grep -q "wave_0_complete: true" .planning/phases/05-ai-integration/05-VALIDATION.md && grep -q "nyquist_compliant: true" .planning/phases/05-ai-integration/05-VALIDATION.md && grep -q "Wave 0 closed" .planning/phases/05-ai-integration/05-VALIDATION.md && echo OK</automated>
  </verify>
  <done>
    AI-SPEC.md §4 recall diagram explicitly notes the synthesis-skip on default (D-04). CLAUDE.md `## Ingest Pipeline` replaces the discard branch with cold-storage (D-07). spike-findings-engram SKILL.md verbosity-default line has the Phase 5 supersession annotation (D-05). VALIDATION.md frontmatter flips to `wave_0_complete: true` and `nyquist_compliant: true` — Wave 0 closes formally as this plan completes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → mcp-server Worker | JWT crosses; `props.workspace_id` is the only trusted source for workspace identity. Phase 5 surface that crosses this: any new test file that captures handler callbacks (test-only — production handlers don't change in this plan). |
| mcp-server Worker → WorkspaceDO | Cross-isolate boundary; `workspace_id` arg validated by `assertOwnsWorkspace` (STO-07) on every new RPC method. Phase 5 adds 5 new methods that all inherit this guard. |
| Triage Worker (Phase 6 consumer) → WorkspaceDO | Cross-Worker DO RPC via `script_name = "engram-mcp-server"`. No JWT on the consumer side; the producer (mcp-server `remember()` in Phase 6) puts `workspace_id` on the MemoryEvent; the DO trusts the field because the DO ID is `idFromName(workspace_id)` (matches the upstream STO-07 invariant). This boundary is declared in Wave 0 wrangler config; the actual consumer body lands in Plan 05-04 and the producer wiring in Phase 6. |
| Workers AI / Vectorize bindings ← code | One-way: env.AI.run and env.VECTORIZE.* are runtime bindings; no inbound trust required because they only accept structured args. Phase 5 wraps them in helpers (Plan 05-02) that enforce workspace_id positional args. Wave 0 only DECLARES these bindings; no production use. |
| Schema migration v2 | Migration runs once via the `_schema_migrations` runner inside `blockConcurrencyWhile()` (Phase 2 STO-02 invariant). Idempotent across DO restarts; ALTER TABLE ADD COLUMN is NOT idempotent at SQL layer but the version check is the idempotency guarantee. |
| BLOCKING tracker file | A pure doc artifact; not a runtime trust boundary. Logged as a procedural gate — every code-producing task in Plans 05-02..05-07 must append a row. The gate is enforced at execute time, not at validation time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-CWVL | Information Disclosure | Cross-workspace vector leakage via missing namespace param on a future `env.VECTORIZE.query()` call | mitigate | This plan does NOT add Vectorize calls — only declares the binding. The actual Vectorize calls live in Plans 05-02 (helper) + 05-03 (remember/forget) + 05-05 (recall). However, this plan establishes the boundary: the WORKSPACE binding on triage-worker uses `script_name = "engram-mcp-server"` so the DO is reachable only via the documented path. The mandatory-workspaceId enforcement lives in Plan 05-02's `vectorize-helper.ts`. (Maps to RESEARCH §"Security Domain" "Cross-workspace vector leakage" pattern.) |
| T-05-01-SCHEMA | Tampering | Migration v2 corrupts existing data (e.g., default value mismatch) | mitigate | `ALTER TABLE blocks ADD COLUMN cold_storage INTEGER NOT NULL DEFAULT 0` is additive — existing rows get `cold_storage = 0` (non-cold), preserving every query that doesn't filter on the new column. The CREATE INDEX is IF NOT EXISTS. Phase 2 hibernation-replay test exercises both the v1 and v2 migrations on first init and on every subsequent boot, verifying idempotency. (Maps to RESEARCH §"Vectorize index re-create destroys data" pattern, applied to SQLite layer.) |
| T-05-01-STO07 | Elevation of Privilege | A new DO RPC method skips `assertOwnsWorkspace`, allowing cross-workspace data access | mitigate | Every one of the 5 new methods (stampEmbedding, getBlocksByIds, updateBlockEnrichment, moveToInbox, moveToColdStorage) calls `this.assertOwnsWorkspace(args.workspace_id)` as the FIRST EXECUTABLE LINE — verified by the verify automation (grep counts ≥12 total assertOwnsWorkspace call sites after additions = 7 existing + 5 new). Phase 2 STO-07 defense-in-depth test exercises mismatched workspace_id and asserts `McpError(InvalidRequest)`. (Maps to RESEARCH §"Security Domain" V4 row.) |
| T-05-01-DOC | Repudiation | Doc canon drift — readers consume the unamended AI-SPEC.md / CLAUDE.md / SKILL.md and ship the wrong default (e.g., synthesis-always-on or hard-discard) | mitigate | Task 8 amends all three docs in a single commit; the VALIDATION.md frontmatter flip records the closure date. Cross-references between the docs (e.g., AI-SPEC.md cites CONTEXT.md D-01; SKILL.md cites D-05) make the audit trail explicit. |
| T-05-01-SC | Tampering | npm install of new packages introduces malicious code (supply chain) | accept | This plan does NOT install any new npm packages. zod, vitest, vitest-pool-workers are existing Phase 4 deps (mcp-server already has them) — added to triage-worker package.json with the same pinned versions (`^4.0.0`, `^4.1.7`, `^0.16.9`). zod-to-json-schema install is DEFERRED to Plan 05-04 behind the Package Legitimacy Audit checkpoint (RESEARCH §"Package Legitimacy Audit"). No `[ASSUMED]` or `[SUS]` package surfaces in this plan. |

All threats `mitigate` or `accept` — zero `high` severity blocks. Plan ships.
</threat_model>

<verification>
- `node scripts/lint-wrangler.mjs` passes (FND-08 invariant preserved on production configs).
- `npm test --workspaces` exits 0 for workspace-do (Phase 2 hibernation-replay still GREEN). For mcp-server + triage-worker, expect new RED failures from Task 7 — these are intentional and expected until Plans 05-02/05-03/05-04 ship.
- `grep -c "this.assertOwnsWorkspace(args.workspace_id)" packages/workspace-do/src/index.ts` returns ≥12 (7 existing + 5 new).
- `grep -q "wave_0_complete: true" .planning/phases/05-ai-integration/05-VALIDATION.md` returns 0 exit code.
- `.planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md` exists and is referenced by every subsequent plan in this phase's tasks (verifier confirms cross-plan citation in execute-phase).
</verification>

<success_criteria>
1. BLOCKING tracker exists and is the canonical place every Phase 5 code-producing task logs its 3-question-checklist + route decision BEFORE committing.
2. mcp-server and triage-worker both declare `AI` + `VECTORIZE` (+ `ANALYTICS` stub) bindings in production AND test wrangler configs; triage-worker also declares the cross-Worker `WORKSPACE` DO binding via `script_name = "engram-mcp-server"`.
3. triage-worker has a working vitest harness (`npm test --workspace=packages/triage-worker` exits 0) and declares vitest + vitest-pool-workers + zod with versions matching mcp-server exactly.
4. Schema migration v2 lands behind the `_schema_migrations` runner; `blocks.cold_storage INTEGER NOT NULL DEFAULT 0` column exists after first init; Phase 2 hibernation-replay test still passes.
5. 5 new WorkspaceDO RPC methods exist (stampEmbedding / getBlocksByIds / updateBlockEnrichment / moveToInbox / moveToColdStorage); each calls assertOwnsWorkspace as the first executable line; `getBlocksByIds` filters out cold-storage rows.
6. `RecallInputSchema.verbosity` default = `"chunks"` (D-01); enum shape unchanged.
7. 4 RED test stubs + 3 existing-test extensions are in place — they fail in expected ways (module-not-found for the helpers; assertion-fail for the integration tests).
8. AI-SPEC.md §4 diagram, CLAUDE.md Ingest Pipeline, spike-findings SKILL.md verbosity line, and VALIDATION.md frontmatter are all amended per D-04 / D-07 / D-05.
9. No new npm packages installed in this plan (zod-to-json-schema gated to Plan 05-04 by Package Legitimacy Audit).
</success_criteria>

<output>
Create `.planning/phases/05-ai-integration/05-01-SUMMARY.md` when done. Include:
- Routing tracker created + path
- Wrangler bindings added (4 files, list each)
- Triage-worker test infra (3 files)
- Schema v2 migration + 5 new DO methods (named)
- RecallInputSchema verbosity default flip
- RED test scaffolds (4 new + 3 extended, named)
- Doc touch-ups (3 files + VALIDATION.md frontmatter flip)
- All cf-code-assist routing rows appended to `05-CF-CODE-ASSIST-USAGE.md` with 3-question checklist answers
- Confirmation that Wave 0 Requirements in VALIDATION.md are CLOSED
- Any RED failure logs the executor saw (expected; flagged to next-wave planners)
</output>