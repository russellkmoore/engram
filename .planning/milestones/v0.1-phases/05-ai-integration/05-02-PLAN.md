---
phase: 05-ai-integration
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - scripts/setup-vectorize.sh
  - packages/mcp-server/src/vectorize-helper.ts
  - packages/mcp-server/src/ai-helper.ts
  - packages/mcp-server/src/hybrid-rank.ts
  - package.json
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04
  - AI-07

must_haves:
  truths:
    - "`scripts/setup-vectorize.sh` provisions the `engram-memories` Vectorize index idempotently — re-running it is a no-op via `wrangler vectorize get` precheck."
    - "The index is created with `--preset=@cf/baai/bge-base-en-v1.5` (locks dimensions=768 + metric=cosine permanently) and both metadata indexes (`type` + `scope`) are declared before the first query lands (Pitfall 9 mitigation)."
    - "`packages/mcp-server/src/vectorize-helper.ts` exports `vectorizeQuery / vectorizeUpsert / vectorizeDelete` — `workspaceId` is a non-optional positional argument; 64-byte namespace length guard fires before any binding call (Pitfall 1 + Pitfall 9 mitigation)."
    - "Direct `env.VECTORIZE.{query|upsert|deleteByIds}` access is BANNED outside `vectorize-helper.ts` — verified by a grep gate in the test suite (covers the AI-02 defense-in-depth invariant)."
    - "`packages/mcp-server/src/ai-helper.ts` exports `EMBEDDING_MODEL = \"@cf/baai/bge-base-en-v1.5\"`, `EMBEDDING_VERSION = 1`, `CLASSIFIER_MODEL = \"@cf/meta/llama-3.1-8b-instruct\"` as `as const` literals; `detectRateLimit(aiResp)` + `isRateLimitError(err)` cover the dual-path 429 detection (RESEARCH Open Question 1); `safeRun(env, model, body)` wraps env.AI.run with both paths and throws `RateLimitError` on either."
    - "`packages/mcp-server/src/hybrid-rank.ts` exports `hybridRank(matches, blocks, args, now)` implementing `score = 1.0·cosine + 0.15·recency + 0.2·type_match + 0.15·scope_match` with 30-day half-life recency decay (`exp(-ageHours / (24*30))`); `HYBRID_WEIGHTS` const is the starting-values source-of-truth (tuning lands in Plan 05-06)."
    - "All RED test stubs from Plan 05-01 Task 7 (vectorize-helper.test.ts, ai-helper.test.ts, hybrid-rank.test.ts) flip GREEN — the helpers exist and the dual-path 429 + namespace-mandatory + locked-weights assertions all pass."
    - "Setup script is wired into `npm run setup` chain at repo root (subsequent `npm run setup` re-runs are no-ops)."
  artifacts:
    - path: "scripts/setup-vectorize.sh"
      provides: "Idempotent Vectorize index + metadata index provisioning"
      contains: "wrangler vectorize get"
    - path: "packages/mcp-server/src/vectorize-helper.ts"
      provides: "Mandatory-workspaceId Vectorize binding wrapper (3 named exports: vectorizeQuery / vectorizeUpsert / vectorizeDelete)"
      contains: "assertNamespace"
    - path: "packages/mcp-server/src/ai-helper.ts"
      provides: "Shared model-ID constants + dual-path 429 detection + safeRun wrapper"
      contains: "EMBEDDING_MODEL = \"@cf/baai/bge-base-en-v1.5\""
    - path: "packages/mcp-server/src/hybrid-rank.ts"
      provides: "AI-04 hybrid ranking formula (cosine + recency + type + scope); pure transform, no IO"
      contains: "HYBRID_WEIGHTS"
    - path: "package.json"
      provides: "Root npm script setup chain extended with setup:vectorize"
      contains: "setup:vectorize"
  key_links:
    - from: "packages/mcp-server/src/vectorize-helper.ts"
      to: "env.VECTORIZE binding"
      via: "structural env arg `{ VECTORIZE: VectorizeIndex }` — mandatory workspaceId positional"
      pattern: "vectorize-helper.ts"
    - from: "packages/mcp-server/src/ai-helper.ts"
      to: "env.AI binding"
      via: "structural env arg `{ AI: Ai }` — safeRun throws RateLimitError on 429"
      pattern: "ai-helper.ts"
    - from: "packages/mcp-server/src/hybrid-rank.ts"
      to: "VectorizeMatches + LexicalSearchHit + RecallInput"
      via: "type-only imports; pure functional transform"
      pattern: "hybridRank"
---

<objective>
Wave 1: ship the four foundational helpers that every Wave 2+ handler depends on. After this plan ships, the `engram-memories` Vectorize index exists and is provisioned correctly, and three helper modules (vectorize-helper, ai-helper, hybrid-rank) export the locked-by-spec primitives that Plans 05-03 (remember/forget), 05-04 (Triage), and 05-05 (recall) consume.

Purpose:
1. Provision the Vectorize index idempotently — once vectors land, the preset (`@cf/baai/bge-base-en-v1.5`) and dimensions/metric (768 cosine) are PERMANENT.
2. Wrap `env.VECTORIZE.*` with `vectorize-helper.ts` so every later call is forced to pass `workspaceId` as a non-optional positional arg (defense in depth for AI-02 cross-workspace leakage).
3. Wrap `env.AI.run` model-IDs in `ai-helper.ts` with `as const` literals + dual-path 429 detection (envelope `success:false` AND thrown `AiError`) per RESEARCH Open Question 1. Reuse-by-import is the AI-SPEC.md §5 dimension #2 identity-check gate.
4. Implement the locked hybrid ranking formula in `hybrid-rank.ts` — pure transform, no IO, weights as exported const so Plan 05-06's tuning task can persist tuned values + audit trail.
5. Flip the Plan 05-01 RED helper test stubs to GREEN.

Output: setup script + 3 helper modules + root npm script wiring. Plans 05-03/05-04/05-05 import from these.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@.claude/skills/spike-findings-engram/SKILL.md
@.claude/skills/spike-findings-engram/references/phase-5-ranking-strategy.md
@.planning/phases/05-ai-integration/05-CONTEXT.md
@.planning/phases/05-ai-integration/05-AI-SPEC.md
@.planning/phases/05-ai-integration/05-RESEARCH.md
@.planning/phases/05-ai-integration/05-VALIDATION.md
@.planning/phases/05-ai-integration/05-PATTERNS.md
@.planning/phases/05-ai-integration/05-01-SUMMARY.md
@.planning/phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md

<interfaces>
<!-- Helpers built in this plan; consumed by Plans 05-03 / 05-04 / 05-05 -->

`packages/mcp-server/src/vectorize-helper.ts` exports:
```typescript
import type { VectorizeIndex, VectorizeVector, VectorizeMatches } from "@cloudflare/workers-types";

export async function vectorizeQuery(
  env: { VECTORIZE: VectorizeIndex },
  workspaceId: string,                                                  // mandatory positional — Pitfall 1 defense
  vector: number[],
  opts: { topK?: number; filter?: Record<string, unknown>; returnMetadata?: "none" | "indexed" | "all" },
): Promise<VectorizeMatches>;                                            // default topK=25 per D-10 + Pitfall 7

export async function vectorizeUpsert(
  env: { VECTORIZE: VectorizeIndex },
  workspaceId: string,
  vectors: Array<Omit<VectorizeVector, "namespace"> & { metadata?: Record<string, unknown> }>,
): Promise<{ mutationId: string }>;                                      // namespace forcibly stamped from workspaceId

export async function vectorizeDelete(
  env: { VECTORIZE: VectorizeIndex },
  workspaceId: string,
  ids: string[],
): Promise<{ mutationId: string }>;                                      // ids.length === 0 → early return { mutationId: "noop" }
```

`packages/mcp-server/src/ai-helper.ts` exports:
```typescript
import type { Ai } from "@cloudflare/workers-types";

export const EMBEDDING_MODEL = "@cf/baai/bge-base-en-v1.5" as const;
export const EMBEDDING_VERSION = 1 as const;
export const CLASSIFIER_MODEL = "@cf/meta/llama-3.1-8b-instruct" as const;

export interface AiBindingResponse {
  shape?: number[]; data?: number[][];                                    // embedding shape
  response?: string | object;                                             // chat shape
  success?: boolean; errors?: Array<{ code: number; message: string }>;   // error envelope shape
  result?: unknown;
}

export function detectRateLimit(resp: AiBindingResponse | undefined | null): boolean;  // envelope path
export function isRateLimitError(err: unknown): boolean;                                // thrown path
export class RateLimitError extends Error { readonly isRateLimit: true; readonly origin: "binding-envelope" | "thrown"; }
export async function safeRun(env: { AI: Ai }, model: string, body: Record<string, unknown>): Promise<AiBindingResponse>;
```

`packages/mcp-server/src/hybrid-rank.ts` exports:
```typescript
import type { VectorizeMatches } from "@cloudflare/workers-types";
import type { LexicalSearchHit } from "@engram/workspace-do";
import type { RecallInput } from "./schemas.js";

export const HYBRID_WEIGHTS = { cosine: 1.0, recency: 0.15, type_match: 0.2, scope_match: 0.15 } as const;

// Pure transform — no env, no IO, no mutation.
// Returns blocks reordered by combined score (descending). Drops Vectorize matches whose id has no SQLite hydration row (orphan-tolerant).
export function hybridRank(
  matches: VectorizeMatches["matches"],
  blocks: LexicalSearchHit[],
  args: RecallInput,
  now: number,
): LexicalSearchHit[];
```

<!-- Existing surface consumed/extended -->

From packages/mcp-server/src/error-mapping.ts (module shape analog — copy the JSDoc cross-phase-contract header pattern):
```typescript
/**
 * `<helper-name>` — <one-line>
 * Cross-phase contract:
 * - **Phase 5 AI-NN:** ...
 * Design notes (locked): ...
 * Threat model: ...
 * @module @engram/mcp-server/<name>
 */
```

From packages/workspace-do/src/types.ts `LexicalSearchHit` (hybrid-rank.ts consumes this type — read to confirm shape: { id, type, content, summary, score, created_at, scope, ... }).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create idempotent Vectorize setup script + wire into npm run setup</name>
  <files>scripts/setup-vectorize.sh, package.json</files>
  <read_first>
    - .planning/phases/05-ai-integration/05-AI-SPEC.md §3 "Installation" (the npx wrangler vectorize create + create-metadata-index commands)
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Example 3" (full setup-vectorize.sh body — copy verbatim, the precheck pattern uses `wrangler vectorize get`)
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Pitfall 9" (metadata index must be created BEFORE filtered queries land) + Assumption A3 (wrangler vectorize create is NOT natively idempotent — precheck via `wrangler vectorize get`)
    - package.json (repo root — scripts block; find or add the `setup` script that chains setup:* sub-scripts)
    - .planning/phases/05-ai-integration/05-PATTERNS.md §"scripts/setup-vectorize.sh" (no-analog row — use RESEARCH §Example 3 verbatim)
  </read_first>
  <action>
    1. Create `scripts/setup-vectorize.sh` per RESEARCH §"Example 3" verbatim, with these confirmed details:
       - `set -euo pipefail` at top.
       - `INDEX_NAME="engram-memories"`, `PRESET="@cf/baai/bge-base-en-v1.5"` as variables.
       - Precheck via `npx wrangler vectorize get "${INDEX_NAME}" >/dev/null 2>&1`; if 0-exit, echo "Index exists — skipping create"; else create via `npx wrangler vectorize create "${INDEX_NAME}" --preset="${PRESET}" --description="Engram v0.1 — single global index, namespace per workspace"`.
       - Metadata-index precheck via `wrangler vectorize list-metadata-index ... --json`; for each of `type` and `scope`, only create if not already present (use grep on the JSON output for `"propertyName":"<name>"`).
       - `chmod +x scripts/setup-vectorize.sh` after writing.

    2. Extend root `package.json` `scripts` block (preserve all existing entries):
       - Add `"setup:vectorize": "bash scripts/setup-vectorize.sh"`.
       - If a `"setup"` script already exists, extend its command chain to include `setup:vectorize`. If not, add `"setup": "npm run setup:vectorize"` as the initial scaffold (subsequent phases can chain more setup steps via `npm-run-all` or `&&`).

    3. Verify idempotency manually: run `bash scripts/setup-vectorize.sh` once (this assumes Russell is `wrangler login`'d — if not, the script will surface the auth error and exit non-zero, which is the correct behavior for a setup script). Then run a second time and confirm both the index-create and metadata-index steps echo "skipping" — no destructive operation occurs on the second run.

    Verification (CI-safe): the test gate runs the script with `--help`/`echo` substitutions — for CI we don't want to actually hit Cloudflare. The acceptance criterion is the precheck pattern is present in the file content and `bash -n scripts/setup-vectorize.sh` (syntax check) passes.
    # Route candidate: claude (CLI-orchestration script with precise wrangler subcommand sequences + idempotency precheck; <60 lines but cross-tool synthesis — bash + wrangler + npm scripts coordination).
  </action>
  <verify>
    <automated>test -f scripts/setup-vectorize.sh && test -x scripts/setup-vectorize.sh && grep -q "wrangler vectorize get" scripts/setup-vectorize.sh && grep -q "@cf/baai/bge-base-en-v1.5" scripts/setup-vectorize.sh && grep -q "create-metadata-index" scripts/setup-vectorize.sh && grep -q "property-name=type" scripts/setup-vectorize.sh && grep -q "property-name=scope" scripts/setup-vectorize.sh && bash -n scripts/setup-vectorize.sh && grep -q "setup:vectorize" package.json && echo OK</automated>
  </verify>
  <done>
    `scripts/setup-vectorize.sh` exists, is executable, uses the precheck pattern for idempotency, and declares both metadata indexes (`type` + `scope`) per Pitfall 9. Root `npm run setup:vectorize` triggers it. `bash -n` syntax-check passes. Manual smoke run (when Russell is logged in) succeeds with re-run as no-op.
  </done>
</task>

<task type="auto">
  <name>Task 2: vectorize-helper.ts — mandatory workspaceId wrapper + namespace length guard</name>
  <files>packages/mcp-server/src/vectorize-helper.ts</files>
  <read_first>
    - packages/mcp-server/src/error-mapping.ts lines 1–43 (JSDoc cross-phase-contract header pattern — copy this shape verbatim, swap @module name)
    - .planning/phases/05-ai-integration/05-PATTERNS.md §"packages/mcp-server/src/vectorize-helper.ts (NEW — helper, binding wrapper)" (full public-surface code block to use)
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Example 4" (the full implementation excerpt — 64-byte length guard via TextEncoder, default topK=25, namespace-stamp-on-upsert discipline)
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Pitfall 1" (cross-workspace leakage) + §"Pitfall 7" (topK default 5) + §"Pitfall 8" (returnMetadata "all" caps topK at 50)
    - packages/mcp-server/src/__tests__/vectorize-helper.test.ts (Plan 05-01 Task 7 RED stub — confirm the assertions the helper must satisfy)
  </read_first>
  <action>
    Implement `packages/mcp-server/src/vectorize-helper.ts` per PATTERNS.md + RESEARCH §Example 4. Apply these locked constraints:

    1. **JSDoc header** per S1 — cross-phase contract block citing Phase 5 AI-02 (cross-workspace defense), Phase 5 AI-03 (embedding-stamp precondition documented but NOT enforced — the helper doesn't do a SQL roundtrip; that responsibility is on the caller and verified by `embedding-consistency.test.ts` in Plan 05-06). Threat-model section cites T-05-CWVL pattern.

    2. **Three named exports** (no default — S2): `vectorizeQuery`, `vectorizeUpsert`, `vectorizeDelete`. Signatures match the `<interfaces>` block in this plan's context.

    3. **Mandatory `workspaceId` positional arg** — second argument of each function, NON-optional. Forgotten arg becomes a TS compile error (S3 defense-in-depth at the type layer).

    4. **`assertNamespace(workspaceId)` private helper** — fires before any binding call:
       - Throw `Error("Vectorize helper: workspaceId is required (failure mode #1 defense)")` if empty/missing.
       - Throw `Error("Vectorize helper: namespace '<id>' exceeds 64 bytes (got <N>)")` if `new TextEncoder().encode(workspaceId).byteLength > 64` (Pitfall 9).

    5. **`vectorizeQuery` defaults** — `topK: opts.topK ?? 25` (Phase 4 D-10 cap; Pitfall 7); `returnMetadata: opts.returnMetadata ?? "all"`. Forward `opts.filter` only when defined (preserves the structural `exactOptionalPropertyTypes` discipline from Phase 1 D-08).

    6. **`vectorizeUpsert`** — `assertNamespace`, then stamp every vector's `namespace` field to `workspaceId` REGARDLESS of caller intent. The vectors arg type explicitly OMITS the `namespace` field from `VectorizeVector` so the caller cannot supply a conflicting namespace. JSDoc notes the embedding-stamp precondition is the caller's responsibility (not enforced here to avoid a SQL roundtrip; verified by Plan 05-06's `embedding-consistency.test.ts`).

    7. **`vectorizeDelete`** — `assertNamespace`, then `if (ids.length === 0) return { mutationId: "noop" }`; otherwise `env.VECTORIZE.deleteByIds(ids)`. Note in JSDoc: Vectorize's `deleteByIds` is namespace-agnostic per the API (vectors carry their own namespace from upsert time) — the helper's role is the namespace-length precondition + the empty-ids short-circuit.

    8. **Constants:** `const NAMESPACE_MAX_BYTES = 64;` and `const VECTORIZE_TOPK_DEFAULT = 25;` as file-local (not exported) per S11 `as const` discipline only-on-public-surface.

    9. **Structural `env` param** — typed `{ VECTORIZE: VectorizeIndex }` (NOT the full Env interface) so tests can pass partial mocks via `as any` cast (matches the `env as Env` cast pattern in `tools-integration.test.ts:68`).

    After writing the file, run `npm test --workspace=packages/mcp-server -- vectorize-helper` and confirm the Plan 05-01 RED stubs flip GREEN:
    - "vectorizeQuery sets namespace = workspace_id on every call" PASSES
    - "rejects namespace > 64 bytes" PASSES
    - "vectorizeUpsert stamps namespace on every vector regardless of caller intent" PASSES
    # Route candidate: cf-code-assist:generateCode (helper module with locked signature + assertion shape from PATTERNS.md + RESEARCH §Example 4 verbatim; ~100 lines mechanical, sentinel-anchored on error-mapping.ts JSDoc shape, stable spec). Q1: synthesis IS single-file — answer N. Q2: ~100 lines mechanical — answer Y. Q3: stable template via PATTERNS analog — answer Y. N/Y/Y → ROUTE. Log row in 05-CF-CODE-ASSIST-USAGE.md BEFORE executing.
  </action>
  <verify>
    <automated>test -f packages/mcp-server/src/vectorize-helper.ts && grep -q "export async function vectorizeQuery" packages/mcp-server/src/vectorize-helper.ts && grep -q "export async function vectorizeUpsert" packages/mcp-server/src/vectorize-helper.ts && grep -q "export async function vectorizeDelete" packages/mcp-server/src/vectorize-helper.ts && grep -q "NAMESPACE_MAX_BYTES = 64" packages/mcp-server/src/vectorize-helper.ts && grep -q "TextEncoder" packages/mcp-server/src/vectorize-helper.ts && grep -q "topK.*25" packages/mcp-server/src/vectorize-helper.ts && ! grep -q "export default" packages/mcp-server/src/vectorize-helper.ts && npm test --workspace=packages/mcp-server -- vectorize-helper --run 2>&1 | tail -10 && echo OK</automated>
  </verify>
  <done>
    `vectorize-helper.ts` exports 3 functions with mandatory `workspaceId` positional arg + 64-byte length guard + default topK=25. Plan 05-01 RED stubs (vectorize-helper.test.ts) flip GREEN. No default export.
  </done>
</task>

<task type="auto">
  <name>Task 3: ai-helper.ts — shared model constants + dual-path 429 detection + safeRun</name>
  <files>packages/mcp-server/src/ai-helper.ts</files>
  <read_first>
    - packages/mcp-server/src/error-mapping.ts lines 1–43 (JSDoc cross-phase pattern — same shape as vectorize-helper.ts in Task 2)
    - .planning/phases/05-ai-integration/05-PATTERNS.md §"packages/mcp-server/src/ai-helper.ts (NEW — helper, binding wrapper)" (full public-surface code block)
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Example 5" (full implementation excerpt — model constants as const, dual-path detection, RateLimitError class with `origin` tag, safeRun wrapper)
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Open Question 1" (envelope-return vs throw — implement BOTH paths) + §"Pitfall 3" (Workers AI 429 → entire batch fail) + Assumption A1 (codes 7501 / 3036 / 3040 all possible; defense-in-depth fallback to message-regex)
    - .planning/phases/05-ai-integration/05-AI-SPEC.md §3 Pitfall #1 (binding returns vs throws on 429) + §"Common Pitfalls" + §"Model Configuration" table (locked model IDs + parameters)
    - packages/mcp-server/src/__tests__/ai-helper.test.ts (Plan 05-01 Task 7 RED stub — confirm dual-path 429 assertions the helper must satisfy)
  </read_first>
  <action>
    Implement `packages/mcp-server/src/ai-helper.ts` per PATTERNS.md + RESEARCH §Example 5. Apply these locked constraints:

    1. **JSDoc header** per S1 — cross-phase contract block citing Phase 5 AI-03 (embedding model), AI-04 (recall query embed), AI-07 (429 retry), AI-05 (classifier in Triage). Threat-model section cites T-05-01-RATE-LIMIT pattern (Workers AI 429 → batch fail).

    2. **Three locked-as-const exports** — model IDs:
       - `export const EMBEDDING_MODEL = "@cf/baai/bge-base-en-v1.5" as const;`
       - `export const EMBEDDING_VERSION = 1 as const;`
       - `export const CLASSIFIER_MODEL = "@cf/meta/llama-3.1-8b-instruct" as const;`
       Each comment cites AI-SPEC.md §3 Model Configuration table. The `as const` widening discipline (S11) is what makes the AI-SPEC.md §5 dimension #2 identity-check test possible — Plan 05-04's triage-worker ai-helper.ts imports the SAME string literals; cross-file equality is verified at Plan 05-04 wire-up.

    3. **`AiBindingResponse` interface** — typed envelope per RESEARCH §Example 5: includes `shape?`, `data?` (embedding), `response?` (chat), `success?`, `errors?`, `result?` (error envelope). Use this as the parameter type for `detectRateLimit`.

    4. **`detectRateLimit(resp)` function** — returns true when:
       - `resp.success === false` AND `resp.errors` has at least one entry, AND
       - some error has `code === 7501 || code === 3036 || code === 3040` OR `message` matches `/429|rate|too\s*many|capacity/i`.
       Returns false on null/undefined input, on success: true, on empty errors[].

    5. **`isRateLimitError(err)` function** — returns true when:
       - `err.status === 429`, OR
       - `err.name` matches `/AiError|InferenceUpstreamError/i` AND message matches `/429|rate|too\s*many|capacity/i`, OR
       - generic error message matches `/429|rate|too\s*many|capacity/i`.

    6. **`RateLimitError` class** — extends Error, `readonly isRateLimit = true`, constructor takes `origin: "binding-envelope" | "thrown"` and stores it. `name = "RateLimitError"`.

    7. **`safeRun(env, model, body)` wrapper** — try/catch around `env.AI.run`:
       - On throw: if `isRateLimitError(err)` → `throw new RateLimitError("thrown")`; otherwise re-throw.
       - On return: if `detectRateLimit(resp)` → `throw new RateLimitError("binding-envelope")`.
       - Return the response untouched on success (caller still does `embedResp.data[0]` per Pitfall 3).

    8. **Structural `env` param** — typed `{ AI: Ai }` for partial-mock support in tests.

    9. **NO default export** (S2).

    After writing the file, run `npm test --workspace=packages/mcp-server -- ai-helper` and confirm the Plan 05-01 RED stubs flip GREEN:
    - "AI helper constants" (EMBEDDING_MODEL / EMBEDDING_VERSION / CLASSIFIER_MODEL literal equality) PASSES
    - "AI-07: 429 detection envelope path" (4 codes + regex) PASSES
    - "AI-07: 429 detection thrown path" (status 429, AiError + 429 message, generic 429) PASSES
    # Route candidate: cf-code-assist:generateCode (helper module with locked constants + dual-path detection logic + custom error class, ~120 lines mechanical, stable spec from PATTERNS analog + RESEARCH §Example 5). Q1: single-file synthesis — N. Q2: >50 lines mechanical — Y. Q3: stable template — Y. N/Y/Y → ROUTE. Log row.
  </action>
  <verify>
    <automated>test -f packages/mcp-server/src/ai-helper.ts && grep -q 'EMBEDDING_MODEL = "@cf/baai/bge-base-en-v1.5" as const' packages/mcp-server/src/ai-helper.ts && grep -q 'EMBEDDING_VERSION = 1 as const' packages/mcp-server/src/ai-helper.ts && grep -q 'CLASSIFIER_MODEL = "@cf/meta/llama-3.1-8b-instruct" as const' packages/mcp-server/src/ai-helper.ts && grep -q "export function detectRateLimit" packages/mcp-server/src/ai-helper.ts && grep -q "export function isRateLimitError" packages/mcp-server/src/ai-helper.ts && grep -q "export class RateLimitError" packages/mcp-server/src/ai-helper.ts && grep -q "export async function safeRun" packages/mcp-server/src/ai-helper.ts && grep -qE "code === 7501|code === 3036|code === 3040" packages/mcp-server/src/ai-helper.ts && ! grep -q "export default" packages/mcp-server/src/ai-helper.ts && npm test --workspace=packages/mcp-server -- ai-helper --run 2>&1 | tail -10 && echo OK</automated>
  </verify>
  <done>
    `ai-helper.ts` exports model constants `as const`, dual-path 429 detection (envelope + thrown), `RateLimitError` class with origin tag, and `safeRun` wrapper. Plan 05-01 RED stubs (ai-helper.test.ts — three describe blocks) flip GREEN. The model-ID constants are the single source of truth that Plan 05-04 will import (and verify via cross-file identity test) for the triage-worker sibling helper.
  </done>
</task>

<task type="auto">
  <name>Task 4: hybrid-rank.ts — pure functional ranking transform with HYBRID_WEIGHTS const</name>
  <files>packages/mcp-server/src/hybrid-rank.ts</files>
  <read_first>
    - packages/mcp-server/src/envelope.ts lines 357–387 (`dropMemoryField`/`dropLastMemory` — immutability + spread + map discipline; hybrid-rank.ts mirrors this pure-functional shape)
    - .planning/phases/05-ai-integration/05-PATTERNS.md §"packages/mcp-server/src/hybrid-rank.ts (NEW — utility, pure transform)" (signature + HYBRID_WEIGHTS exact starting values)
    - .planning/phases/05-ai-integration/05-RESEARCH.md §"Phase 5 Ranking Strategy" (operative bullets 1–6) + §"Pattern 2 — recall" (handler usage)
    - .planning/phases/05-ai-integration/05-AI-SPEC.md §4 (hybrid ranking formula LOCKED: `cosine·1.0 + recency·0.15 + type_match·0.2 + scope_match·0.15`)
    - .claude/skills/spike-findings-engram/references/phase-5-ranking-strategy.md §3 (30-day half-life recency decay: `recency = exp(-ageDays / 30)` equivalent to `exp(-ageHours / (24*30))`)
    - packages/workspace-do/src/types.ts (`LexicalSearchHit` shape — id, type, content, summary, score, created_at, scope) + packages/mcp-server/src/schemas.ts (`RecallInput` type — args.types? args.scope?)
    - packages/mcp-server/src/__tests__/hybrid-rank.test.ts (Plan 05-01 Task 7 RED stub — 5 it() assertions to satisfy)
  </read_first>
  <action>
    Implement `packages/mcp-server/src/hybrid-rank.ts` per PATTERNS.md + spike-findings phase-5-ranking-strategy.md. Apply these locked constraints:

    1. **JSDoc header** per S1 — cross-phase contract block citing Phase 5 AI-04 (recall ranking). Cite the spike-findings provenance verbatim: "Spike 003 proved `bge-base-en-v1.5` encodes domain not memory type — hybrid ranking is MANDATORY, not optional. Starting weights from AI-SPEC.md §4; tuning lands in Plan 05-06 against the real-corpus samples." Mark the file as a pure transform — `@module @engram/mcp-server/hybrid-rank`.

    2. **`HYBRID_WEIGHTS` exported const** — locked starting values per AI-SPEC.md §4:
       ```typescript
       export const HYBRID_WEIGHTS = {
         cosine: 1.0,
         recency: 0.15,
         type_match: 0.2,
         scope_match: 0.15,
       } as const;
       ```
       Exported (not file-local) so Plan 05-06's weight-tuning task can re-export tuned values with a comment `// tuned YYYY-MM-DD against real-corpus N=20`. Exported also so `hybrid-rank.test.ts` can assert exact weights.

    3. **`hybridRank(matches, blocks, args, now)` function** — pure transform, no env, no IO, no mutation:
       - Build a `Map<string, LexicalSearchHit>` from `blocks` keyed by `id` for O(1) lookup.
       - For each `match` in `matches`:
         - Look up the block by `match.id`; if absent (orphan vector — failure mode #3 partial), skip + log via `console.warn("hybrid-rank:orphan-vector", { id: match.id })`.
         - Compute components:
           - `cosine = match.score` (already in [0..1] from Vectorize cosine distance — VectorizeMatches.matches[].score).
           - `recency`: `const ageHours = Math.max(0, (now - block.created_at) / 3_600_000); const recency = Math.exp(-ageHours / (24 * 30));` — 30-day half-life decay per spike findings.
           - `type_match = args.types && args.types.includes(block.type) ? 1 : 0`.
           - `scope_match = args.scope && args.scope === block.scope ? 1 : 0`.
         - Combined score: `score = HYBRID_WEIGHTS.cosine * cosine + HYBRID_WEIGHTS.recency * recency + HYBRID_WEIGHTS.type_match * type_match + HYBRID_WEIGHTS.scope_match * scope_match`.
         - Return a NEW object: `{ ...block, score }` (spread + map — never mutate input — matches envelope.ts:357–387 discipline).
       - Sort the resulting array descending by `score` (use stable sort: `[...arr].sort((a, b) => b.score - a.score)`).
       - Return the sorted array.

    4. **Type imports** — `import type { VectorizeMatches } from "@cloudflare/workers-types"; import type { LexicalSearchHit } from "@engram/workspace-do"; import type { RecallInput } from "./schemas.js";` per S2 + verbatimModuleSyntax.

    5. **NO default export** (S2).

    6. **Orphan tolerance** — the Vectorize-side may return matches whose SQLite row no longer exists (the failure mode #4 partial: SQLite deleted but Vectorize lingers, OR a race window where remember() upserted but stampEmbedding hadn't committed). Drop these matches with a warn log; do NOT throw. The downstream caller (Plan 05-05 recall handler) hydrates from `getBlocksByIds` which already excludes cold-storage rows.

    After writing the file, run `npm test --workspace=packages/mcp-server -- hybrid-rank` and confirm the Plan 05-01 RED stubs flip GREEN:
    - "weights are LOCKED at AI-SPEC.md §4 starting values" PASSES
    - "recent block scores higher than older block at same cosine" PASSES (write the test fixture: 2 blocks with same cosine = 0.7 but `created_at` differing by 30 days → newer ranks higher)
    - "type_match boosts when args.types contains block.type" PASSES
    - "scope_match boosts when args.scope === block.scope" PASSES
    - "reorders matches monotonically by combined score" PASSES (3+ blocks with distinct combined scores → sort order matches descending-score order)
    # Route candidate: cf-code-assist:generateCode (pure functional transform with locked formula + immutability discipline; ~80 lines mechanical, sentinel-anchored on envelope.ts:357–387 + RESEARCH §Pattern 2 formula). Q1: single-file synthesis — N. Q2: 50–100 lines — Y. Q3: stable template + locked formula — Y. N/Y/Y → ROUTE. Log row.
  </action>
  <verify>
    <automated>test -f packages/mcp-server/src/hybrid-rank.ts && grep -q "export const HYBRID_WEIGHTS" packages/mcp-server/src/hybrid-rank.ts && grep -q "cosine: 1.0" packages/mcp-server/src/hybrid-rank.ts && grep -q "recency: 0.15" packages/mcp-server/src/hybrid-rank.ts && grep -q "type_match: 0.2" packages/mcp-server/src/hybrid-rank.ts && grep -q "scope_match: 0.15" packages/mcp-server/src/hybrid-rank.ts && grep -q "export function hybridRank" packages/mcp-server/src/hybrid-rank.ts && grep -q "Math.exp" packages/mcp-server/src/hybrid-rank.ts && ! grep -q "export default" packages/mcp-server/src/hybrid-rank.ts && npm test --workspace=packages/mcp-server -- hybrid-rank --run 2>&1 | tail -10 && echo OK</automated>
  </verify>
  <done>
    `hybrid-rank.ts` exports `HYBRID_WEIGHTS` (locked starting values) and `hybridRank` (pure transform with 30-day half-life recency decay, type_match/scope_match boosts, orphan-tolerant). Plan 05-01 RED stubs (hybrid-rank.test.ts — 5 assertions) flip GREEN. Plan 05-05's recall handler imports this directly. Plan 05-06's weight-tuning task updates HYBRID_WEIGHTS with a dated audit comment.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External CLI (wrangler) ← setup script | One-way: setup-vectorize.sh invokes `npx wrangler vectorize ...` subcommands. wrangler authenticates against Russell's Cloudflare account via the existing login session (Phase 3 carry-forward). The setup script does not handle secrets directly. |
| Caller → vectorize-helper | All `env.VECTORIZE.*` access funnels through 3 helper functions; each requires `workspaceId` as non-optional positional arg. This is the AI-02 defense at the type layer (forgotten arg = TS compile error). |
| Caller → ai-helper | All Workers AI rate-limit detection funnels through `detectRateLimit` + `isRateLimitError` (dual-path) + `safeRun` (which throws a tagged `RateLimitError`). The class tag allows downstream consumers (Plan 05-04 Triage queue consumer) to distinguish AI 429 from generic AI failures and call `message.retry({delaySeconds:30})` instead of throwing. |
| Caller → hybrid-rank | Pure functional boundary — no env, no IO, no mutation. Inputs are typed; outputs are deterministic given the same inputs + `now`. The orphan-tolerant fallback (drop + warn-log) is the only non-deterministic path (depends on whether match ids hydrate). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-CWVL | Information Disclosure | Cross-workspace vector leakage via direct `env.VECTORIZE.query()` call site bypassing vectorize-helper.ts | mitigate | Vectorize-helper.ts exports the only sanctioned access paths. A grep gate enforces "no direct env.VECTORIZE.{query,upsert,deleteByIds} outside vectorize-helper.ts" — Plan 05-03 ships this grep gate as part of its remember/forget handler PR. (Maps to RESEARCH §"Security Domain" "Cross-workspace vector leakage" pattern; the helper itself is the structural defense; the lint is the enforcement.) |
| T-05-02-NSCAP | Information Disclosure / Denial of Service | Namespace > 64 bytes silently truncated by Vectorize → query lands in wrong bucket | mitigate | `assertNamespace` throws BEFORE the binding call (preempt rather than let Vectorize return a confusing error). 64-byte cap via `TextEncoder().encode().byteLength` (UTF-8 byte length, not char count) — important for future v0.3 UUID-shaped workspace IDs which fit but anything longer rejects. |
| T-05-02-DRIFT | Tampering | EMBEDDING_MODEL constant drifts between mcp-server and triage-worker (causing the canonical RAG bug) | mitigate | `EMBEDDING_MODEL` declared as `as const` literal type in `ai-helper.ts`. The cross-file identity test lands in Plan 05-04 when triage-worker's sibling ai-helper.ts ships — that test reads BOTH files and asserts `=== ` equality on the string literal. STO-04's `embedding_model TEXT` column on `blocks` is the migration escape hatch if Cloudflare ever silently rotates weights (AI-SPEC.md §3 Pitfall 8). |
| T-05-02-429 | Denial of Service | Workers AI 429 not detected, batch-fails | mitigate | Dual-path detection (`detectRateLimit` envelope + `isRateLimitError` thrown) per RESEARCH Open Question 1. `safeRun` throws `RateLimitError` (origin-tagged) in either case. Plan 05-04's Triage consumer translates `RateLimitError` to `message.retry({delaySeconds: 30})` per AI-07. |
| T-05-02-IDX | Repudiation | Vectorize index re-create destroys vectors | mitigate | `setup-vectorize.sh` uses `wrangler vectorize get` precheck before create — second run is a no-op. The script never runs `wrangler vectorize delete`. Documentation in the script comment block + setup README (Plan 05-07) explicitly warns: "the preset locks dimensions+metric at create time; re-creating the index is destructive and forces re-embed of all existing vectors — DO NOT run delete." |
| T-05-02-SC | Tampering | npm install of new packages introduces malicious code (supply chain) | accept | This plan does NOT install any new npm packages. wrangler is already installed (Phase 3 carry-forward). zod, vitest, vitest-pool-workers were added in Plan 05-01. No `[ASSUMED]` or `[SUS]` surfaces in Wave 1. |

All threats `mitigate` or `accept` — zero `high` severity blocks. Plan ships.
</threat_model>

<verification>
- `bash -n scripts/setup-vectorize.sh` exits 0 (syntax check).
- `npm test --workspace=packages/mcp-server -- vectorize-helper ai-helper hybrid-rank --run` exits 0 (3 helper test files all GREEN).
- `grep -c "export" packages/mcp-server/src/vectorize-helper.ts` returns ≥3 (vectorizeQuery + vectorizeUpsert + vectorizeDelete).
- `grep -c "export" packages/mcp-server/src/ai-helper.ts` returns ≥7 (EMBEDDING_MODEL + EMBEDDING_VERSION + CLASSIFIER_MODEL + AiBindingResponse + detectRateLimit + isRateLimitError + RateLimitError + safeRun).
- `grep -c "export" packages/mcp-server/src/hybrid-rank.ts` returns ≥2 (HYBRID_WEIGHTS + hybridRank).
- No `export default` in any of the 3 helpers (S2 invariant).
- 05-CF-CODE-ASSIST-USAGE.md has appended rows for every code-producing task (Task 2/3/4 routed to cf-code-assist with N/Y/Y checklists; Task 1 kept with Claude with claude reason).
</verification>

<success_criteria>
1. `engram-memories` Vectorize index can be provisioned idempotently via `npm run setup:vectorize` — first run creates, second run is a no-op.
2. `vectorize-helper.ts` exports 3 functions; every Vectorize call in Phase 5 hot-path code (Plans 05-03 / 05-05) goes through one of them.
3. `ai-helper.ts` exports the model-ID constants Plan 05-04 will import; dual-path 429 detection (`detectRateLimit` + `isRateLimitError` + `safeRun` + `RateLimitError`) is the single source of truth for AI-07 retry policy.
4. `hybrid-rank.ts` exports the locked AI-04 ranking formula as a pure transform; weights are an exported const for Plan 05-06's tuning task.
5. All RED helper test stubs from Plan 05-01 Task 7 flip GREEN.
6. Every code-producing task in this plan logged a row in `05-CF-CODE-ASSIST-USAGE.md` BEFORE committing the route decision.
</success_criteria>

<output>
Create `.planning/phases/05-ai-integration/05-02-SUMMARY.md` when done. Include:
- Vectorize setup script idempotency confirmation
- Helper file line counts + exported symbol list (3 files)
- Test-flip summary (3 RED → GREEN test files; specific it() blocks named)
- cf-code-assist routing decisions (4 tasks; routes + 3-question answers)
- Hand-off to Plan 05-03 / 05-04 / 05-05: which helpers each downstream plan imports
</output>