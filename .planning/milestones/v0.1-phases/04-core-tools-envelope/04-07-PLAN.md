---
phase: 04-core-tools-envelope
plan: "07"
type: execute
wave: 2
depends_on:
  - "04-06"
gap_closure: true
files_modified:
  - .planning/phases/04-core-tools-envelope/04-VERIFICATION.md
  - .planning/phases/04-core-tools-envelope/04-MCP-INSPECTOR-SMOKE.md
autonomous: true
requirements:
  - TOL-08

must_haves:
  truths:
    - "04-VERIFICATION.md frontmatter contains an overrides block with a TOL-08 entry citing accepted_by: rmoore and accepted_at: 2026-05-27"
    - "04-MCP-INSPECTOR-SMOKE.md has a new ### Verification Override subsection under ## Smoke Run cross-referencing the VERIFICATION.md override and noting that future smokes should capture raw JSON"
    - "04-VERIFICATION.md frontmatter overrides_applied counter is updated to 1"
    - "04-VERIFICATION.md overall status remains gaps_found (CR-01 gap is resolved by Plan 04-06 but this plan only records the TOL-08 override; status update happens at re-verification)"
  artifacts:
    - path: ".planning/phases/04-core-tools-envelope/04-VERIFICATION.md"
      provides: "overrides block with TOL-08 verbal-acceptance entry"
      contains: "accepted_by: rmoore"
    - path: ".planning/phases/04-core-tools-envelope/04-MCP-INSPECTOR-SMOKE.md"
      provides: "### Verification Override subsection"
      contains: "Verification Override"
  key_links:
    - from: ".planning/phases/04-core-tools-envelope/04-MCP-INSPECTOR-SMOKE.md"
      to: ".planning/phases/04-core-tools-envelope/04-VERIFICATION.md"
      via: "cross-reference in ### Verification Override section"
      pattern: "04-VERIFICATION\\.md"
---

<objective>
Record the verbal-acceptance decision for TOL-08 smoke evidence (Gap 2 — WARNING). The MCP Inspector smoke ran and AC-01..AC-12 were verbally confirmed by Russell on 2026-05-27, but raw per-call JSON was deliberately not captured. The orchestrator pre-selected the override path: add an explicit `overrides` block to `04-VERIFICATION.md` frontmatter acknowledging the self-reported evidence is accepted for Phase 4 closure, and cross-reference it in `04-MCP-INSPECTOR-SMOKE.md` with a note that future smokes (Phase 5, Phase 7 DEP-04) should capture JSON.

Purpose: Close TOL-08 as an accepted-with-override (not re-run), unblock Phase 4 re-verification, and set a forward-looking precedent for evidence capture discipline.

Output: Two documentation edits only. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-tools-envelope/04-VERIFICATION.md
@.planning/phases/04-core-tools-envelope/04-MCP-INSPECTOR-SMOKE.md
@.planning/phases/04-core-tools-envelope/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add overrides block to 04-VERIFICATION.md frontmatter and update counter</name>
  <files>
    .planning/phases/04-core-tools-envelope/04-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/04-core-tools-envelope/04-VERIFICATION.md — read the full frontmatter (lines 1-51); pay attention to the current `overrides_applied: 0` field and the `gaps:` block structure; the override must be added as a sibling `overrides:` key in the frontmatter YAML, not inside the `gaps:` block
  </read_first>
  <action>
    Edit the frontmatter of 04-VERIFICATION.md (the YAML block between the opening and closing `---` delimiters):

    1. Change `overrides_applied: 0` to `overrides_applied: 1`.

    2. Add an `overrides:` key (sibling to `gaps:`, `deferred:`, `human_verification:`) with the following entry:

    ```yaml
    overrides:
      - id: TOL-08
        truth: "TOL-08 smoke has verifiable evidence (raw JSON capture or independent replay capability)"
        original_status: partial
        override_status: accepted
        accepted_by: rmoore
        accepted_at: "2026-05-27"
        rationale: >
          AC-01..AC-12 were verbally confirmed by the runner (Russell Moore) during the live
          MCP Inspector smoke on 2026-05-27. The smoke ran cleanly after two code-deviation
          fixes (commits 6e20d2d + 01a225e). Raw per-call JSON capture was elected to be
          skipped for time reasons. The verbal confirmation is accepted as sufficient evidence
          for Phase 4 closure given: (a) the smoke was executed by the workspace owner, not a
          third party; (b) the two deviations found and fixed during the smoke validate that a
          genuine live run occurred (a fabricated report would not surface workerd SQLite
          LIKE-pattern-length bugs); (c) future smokes (Phase 5 recall semantic upgrade, Phase
          7 DEP-04 Russell-agent reconfig) are required to capture raw JSON and will establish
          the evidence standard going forward.
        forward_note: >
          Phase 5 and Phase 7 DEP-04 smokes MUST capture raw JSON inline in their smoke
          artifacts. This override should not be used as a precedent for skipping JSON capture
          in future phases.
    ```

    Do not alter any other part of the frontmatter or the body of the file.
  </action>
  <acceptance_criteria>
    - 04-VERIFICATION.md frontmatter YAML is valid (no parse errors)
    - `overrides_applied` equals 1
    - `overrides:` block is present with a single entry whose `id` is `TOL-08`
    - Entry has `accepted_by: rmoore`, `accepted_at: "2026-05-27"`, `override_status: accepted`
    - The existing `gaps:`, `deferred:`, and `human_verification:` blocks are unchanged
    - Body of the file below the frontmatter is unchanged
  </acceptance_criteria>
  <verify>
    <automated>grep -c "accepted_by: rmoore" /Users/rmoore/Workspaces/engram/.planning/phases/04-core-tools-envelope/04-VERIFICATION.md</automated>
  </verify>
  <done>04-VERIFICATION.md frontmatter has overrides_applied: 1 and an overrides block with the TOL-08 verbal-acceptance entry including accepted_by: rmoore and accepted_at: 2026-05-27.</done>
</task>

<task type="auto">
  <name>Task 2: Add ### Verification Override subsection to 04-MCP-INSPECTOR-SMOKE.md</name>
  <files>
    .planning/phases/04-core-tools-envelope/04-MCP-INSPECTOR-SMOKE.md
  </files>
  <read_first>
    - .planning/phases/04-core-tools-envelope/04-MCP-INSPECTOR-SMOKE.md — read the ## Smoke Run section (from line 232 to end of file); the ### Raw JSON capture subsection (lines 304-309) is where the override cross-reference should be added; read the ## Cross-references section to understand the existing cross-ref format
  </read_first>
  <action>
    In 04-MCP-INSPECTOR-SMOKE.md, append a new `### Verification Override` subsection immediately after the existing `### Raw JSON capture` subsection (after line 309, before `## Cross-references`).

    The new subsection text:

    ```markdown
    ### Verification Override

    The missing raw JSON capture for this smoke was accepted via a formal override recorded
    in `04-VERIFICATION.md` frontmatter (`overrides[TOL-08]`):

    - **Accepted by:** rmoore (Russell Moore)
    - **Accepted at:** 2026-05-27
    - **Override rationale:** The two deviations surfaced and fixed during the live run
      (workerd SQLite LIKE-pattern-length cap + kv-bootstrap.mjs cwd bug) validate that a
      genuine live run occurred. A fabricated AC checklist would not surface runtime-specific
      engine bugs. Verbal confirmation by the workspace owner is accepted as sufficient
      evidence for Phase 4 closure.
    - **Forward requirement:** Phase 5 (recall semantic upgrade smoke) and Phase 7
      (DEP-04 Russell-agent reconfig smoke) MUST capture raw JSON inline in their smoke
      artifacts. This override is a one-time exception, not a precedent.

    See `04-VERIFICATION.md` `overrides:` block for the canonical acceptance record.
    ```

    Do not alter any other part of the file.
  </action>
  <acceptance_criteria>
    - 04-MCP-INSPECTOR-SMOKE.md contains a `### Verification Override` heading
    - The subsection references `04-VERIFICATION.md` and `overrides[TOL-08]`
    - The subsection contains `accepted_by` / `accepted_at` context matching the VERIFICATION.md entry
    - The forward-requirement note about Phase 5 and Phase 7 JSON capture is present
    - The existing ## Cross-references section is unchanged (still immediately follows)
    - File is valid Markdown (no broken YAML frontmatter — frontmatter is not changed)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Verification Override" /Users/rmoore/Workspaces/engram/.planning/phases/04-core-tools-envelope/04-MCP-INSPECTOR-SMOKE.md</automated>
  </verify>
  <done>04-MCP-INSPECTOR-SMOKE.md has a ### Verification Override subsection after ### Raw JSON capture, cross-referencing the 04-VERIFICATION.md overrides block and noting the Phase 5 / Phase 7 JSON capture requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs → verifier | Docs are read by the GSD verifier; YAML frontmatter parse errors would break re-verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Tampering | 04-VERIFICATION.md YAML frontmatter | accept | Pure documentation edit; no runtime or security surface; YAML validity confirmed by grep acceptance criteria |
| T-04-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks:
1. `grep -c "accepted_by: rmoore" .planning/phases/04-core-tools-envelope/04-VERIFICATION.md` returns 1
2. `grep -c "Verification Override" .planning/phases/04-core-tools-envelope/04-MCP-INSPECTOR-SMOKE.md` returns 1
3. `grep "overrides_applied" .planning/phases/04-core-tools-envelope/04-VERIFICATION.md` shows `overrides_applied: 1`
4. The existing `gaps:` block in 04-VERIFICATION.md frontmatter is unchanged (still contains both CR-01 and TOL-08 entries — the override does not delete the gap record, it adds an acceptance annotation)
</verification>

<success_criteria>
- 04-VERIFICATION.md: overrides_applied = 1; overrides block present with TOL-08 entry (accepted_by: rmoore, accepted_at: 2026-05-27, rationale noting deviation evidence)
- 04-MCP-INSPECTOR-SMOKE.md: ### Verification Override subsection present, cross-references VERIFICATION.md, calls out Phase 5 / Phase 7 JSON capture requirement
- No code changes; no test suite impact
- Gap 2 (TOL-08 WARNING) is formally accepted and documented
</success_criteria>

<output>
Create `.planning/phases/04-core-tools-envelope/04-07-SUMMARY.md` when done.
Append one row to `.planning/phases/04-core-tools-envelope/04-CF-CODE-ASSIST-USAGE.md` per the CLAUDE.md Phase 4 tracking requirement.
</output>
