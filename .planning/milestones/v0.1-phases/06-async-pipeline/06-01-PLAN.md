---
phase: 06-async-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/todos/pending/2026-05-26-phase-6-validate-conflict-detection-precision.md
  - packages/workspace-do/src/schema.ts
  - packages/workspace-do/src/migrations.ts
  - packages/workspace-do/src/__tests__/schema.test.ts
autonomous: true
requirements: [PIP-04, PIP-06]
must_haves:
  truths:
    - "ROADMAP.md PIP-04 SC4 documents conflict-detection deferral to v0.2 (per D-01)"
    - "REQUIREMENTS.md PIP-04 line documents conflict-detection deferral to v0.2 (per D-01)"
    - "The pending conflict-detection-precision todo notes that validation moves to v0.2 (per D-01)"
    - "A fresh WorkspaceDO has a blocks.ingest_status column (TEXT NOT NULL DEFAULT 'pending') after v3 migration runs (per D-03)"
    - "An idx_blocks_ingest_status index exists on the blocks table after v3 migration runs"
    - "_schema_migrations table contains exactly 3 rows after first init: v1_initial_schema, v2_cold_storage, v3_ingest_status"
    - "Re-instantiating the WorkspaceDO does not re-run the v3 migration (idempotency via _schema_migrations runner)"
  artifacts:
    - path: "packages/workspace-do/src/schema.ts"
      provides: "V3_SQL exported constant containing ALTER TABLE + CREATE INDEX statements for ingest_status"
      contains: "export const V3_SQL"
    - path: "packages/workspace-do/src/migrations.ts"
      provides: "MIGRATIONS array v3 entry { version: 3, name: 'v3_ingest_status', sql: V3_SQL }"
      contains: "v3_ingest_status"
    - path: "packages/workspace-do/src/__tests__/schema.test.ts"
      provides: "GREEN test asserting v3 column shape + index presence + _schema_migrations row"
      contains: "ingest_status"
  key_links:
    - from: "packages/workspace-do/src/migrations.ts"
      to: "packages/workspace-do/src/schema.ts"
      via: "named import of V3_SQL"
      pattern: "import \\{[^}]*V3_SQL[^}]*\\} from \"./schema.js\""
    - from: "packages/workspace-do/src/__tests__/schema.test.ts"
      to: "blocks.ingest_status column"
      via: "PRAGMA table_info(blocks) introspection in runInDurableObject"
      pattern: "ingest_status"
---

<objective>
Wave 1: lock the scope-deferral doc touch-ups (D-01) and ship the v3 schema migration that adds `blocks.ingest_status` (D-03). After this plan, the SQLite surface for per-block enrichment-state tracking exists and the ROADMAP / REQUIREMENTS no longer overstate what Phase 6 delivers. All downstream Phase 6 plans (Wave 2+) depend on V3_SQL landing in `schema.ts` and the v3 entry landing in `MIGRATIONS`.

Purpose: this is the foundation wave. Without `blocks.ingest_status`, the query-helper amendments (06-03) and the `markIngestFailed` RPC (06-03) have nothing to write to, and the integration tests (06-05) have nothing to assert against. Doc touch-ups land here because they describe the scope this phase actually delivers — running them later would leave ROADMAP/REQUIREMENTS lying about Phase 6 during execution.

Output: V3_SQL constant in `schema.ts`; v3 entry in `MIGRATIONS`; new + amended GREEN tests in `schema.test.ts`; PIP-04 amendments in ROADMAP.md + REQUIREMENTS.md; annotation paragraph on the pending todo file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-async-pipeline/06-CONTEXT.md
@.planning/phases/06-async-pipeline/06-PATTERNS.md
@packages/workspace-do/src/schema.ts
@packages/workspace-do/src/migrations.ts
@packages/workspace-do/src/__tests__/schema.test.ts

<interfaces>
<!-- The migration runner is unchanged in Phase 6. The cross-plan contract is: -->
<!-- schema.ts exports V3_SQL; migrations.ts imports it; the runner picks it up via the MIGRATIONS array. -->

From packages/workspace-do/src/migrations.ts (existing):
```typescript
export interface Migration {
  version: number;
  name: string;
  sql: string;
}
export const MIGRATIONS: readonly Migration[];
export function runMigrations(sql: SqlStorage): void;
```

From packages/workspace-do/src/schema.ts (existing exports, this plan adds V3_SQL):
```typescript
export const V1_SQL: string;       // 7 tables + 10 indexes (Phase 2)
export const V2_SQL: string;       // ALTER blocks ADD cold_storage (Phase 5)
// NEW in this plan:
export const V3_SQL: string;       // ALTER blocks ADD ingest_status + idx
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Doc touch-ups — ROADMAP PIP-04, REQUIREMENTS PIP-04, pending todo annotation (D-01)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/todos/pending/2026-05-26-phase-6-validate-conflict-detection-precision.md</files>
  <read_first>
    - .planning/ROADMAP.md (the Phase 6 §Success Criteria block, currently lists "entity extraction, summarization, memorability scoring, and conflict detection against existing memories in the workspace" as SC4 — the line that needs amendment)
    - .planning/REQUIREMENTS.md (PIP-04 line: "Triage Worker performs: entity extraction, summarization, memorability scoring, conflict detection against existing memories in the workspace; writes results back to the WorkspaceDO via RPC" — the line that needs amendment)
    - .planning/todos/pending/2026-05-26-phase-6-validate-conflict-detection-precision.md (the validation harness description that gets the v0.2-deferral annotation)
    - .planning/phases/06-async-pipeline/06-CONTEXT.md (D-01 — the locked decision being implemented; §"Specifics" §"PIP-04 ROADMAP wording change is a Wave 0 doc touch-up" — the exact wording prescription)
    - .planning/phases/06-async-pipeline/06-PATTERNS.md §15–17 "Doc touch-ups (no code analogs)" — three target edits in one place
  </read_first>
  <action>
    Three documentation edits implementing D-01 (per D-01 + per CONTEXT.md §"Specifics" PIP-04 wording change + per CONTEXT.md §"Deferred Ideas" first bullet).

    **CRITICAL — per checker W1 fix.** The phrase "against existing memories in the workspace" was the modifier for "conflict detection" — when we remove "and conflict detection", that modifier MUST be removed along with it. Re-attaching it to "memorability scoring" is grammatically incorrect (you don't score memorability AGAINST existing memories — memorability scoring is a per-block AI judgment, not a comparative operation). Apply the corrected text below verbatim.

    1. **`.planning/ROADMAP.md`** — find the Phase 6 §"Success Criteria" block (under `### Phase 6: Async Pipeline`). SC item 4 currently reads:
       "The Triage Worker performs entity extraction, summarization, memorability scoring, and conflict detection against existing memories in the workspace; writes results back to the `WorkspaceDO` via RPC (not direct SQL)."

       Replace with (W1 corrected wording — the "against existing memories in the workspace" modifier is REMOVED entirely along with the conflict-detection clause it described):
       "The Triage Worker performs entity extraction, summarization, and memorability scoring; writes results back to the `WorkspaceDO` via RPC (not direct SQL). _Conflict detection deferred to v0.2 — see Phase 6 CONTEXT.md D-01._"

       Touch no other ROADMAP lines.

    2. **`.planning/REQUIREMENTS.md`** — find the PIP-04 bullet under §"Pipeline (PIP)". The line currently reads:
       "**PIP-04**: Triage Worker performs: entity extraction, summarization, memorability scoring, conflict detection against existing memories in the workspace; writes results back to the `WorkspaceDO` via RPC"

       Replace with (W1 corrected wording — drop the modifier along with the clause):
       "**PIP-04**: Triage Worker performs: entity extraction, summarization, and memorability scoring; writes results back to the `WorkspaceDO` via RPC (conflict detection deferred to v0.2 — see Phase 6 CONTEXT.md D-01)"

       Touch no other REQUIREMENTS lines (the Traceability table stays as-is — PIP-04 still maps to Phase 6).

    3. **`.planning/todos/pending/2026-05-26-phase-6-validate-conflict-detection-precision.md`** — append a new paragraph at the END of the file (preserve all existing content; do not modify the YAML frontmatter or any section header). The new paragraph (use a blockquote so it stands out as a planner annotation):
       "> **Validation gate moves to v0.2 per Phase 6 CONTEXT.md D-01.** v0.1 ships entity extraction + summarization + memorability scoring only. The 50-sample harness described above (25 genuine conflicts + 15 benign updates + 10 unrelated pairs) and the 3-way decision gate stay valid as written; v0.2 owns the execution."
       The file stays in `.planning/todos/pending/` (do not move it).

    No code-side changes in this task — Phase 6 honestly delivers what the amended ROADMAP/REQUIREMENTS now claim. No new `must_haves` for these edits beyond the literal string presence checks in `<verify>`.
  </action>
  <verify>
    <automated>grep -q "Conflict detection deferred to v0.2 — see Phase 6 CONTEXT.md D-01" .planning/ROADMAP.md \
      && grep -q "conflict detection deferred to v0.2 — see Phase 6 CONTEXT.md D-01" .planning/REQUIREMENTS.md \
      && grep -q "Validation gate moves to v0.2 per Phase 6 CONTEXT.md D-01" .planning/todos/pending/2026-05-26-phase-6-validate-conflict-detection-precision.md \
      && ! grep -q "memorability scoring, and conflict detection against existing memories" .planning/ROADMAP.md \
      && ! grep -q "memorability scoring, conflict detection against existing memories" .planning/REQUIREMENTS.md \
      && ! grep -q "memorability scoring against existing memories" .planning/ROADMAP.md \
      && ! grep -q "memorability scoring against existing memories" .planning/REQUIREMENTS.md \
      && grep -q "entity extraction, summarization, and memorability scoring" .planning/ROADMAP.md \
      && grep -q "entity extraction, summarization, and memorability scoring" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    ROADMAP.md PIP-04 SC4 + REQUIREMENTS.md PIP-04 line are amended (conflict-detection clause AND its "against existing memories in the workspace" modifier removed — W1 fix); the conflict-detection-precision todo carries the v0.2 annotation paragraph; no other lines in those files changed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: V3 migration — V3_SQL constant + MIGRATIONS append + schema.test.ts extension (D-03 / PIP-06)</name>
  <files>packages/workspace-do/src/schema.ts, packages/workspace-do/src/migrations.ts, packages/workspace-do/src/__tests__/schema.test.ts</files>
  <read_first>
    - packages/workspace-do/src/schema.ts (existing V1_SQL lines 66–143, V2_SQL lines 182–185, V2_SQL doc-block lines 145–181 — V3_SQL mirrors V2_SQL's exact tagged-template `as const` shape and JSDoc structure)
    - packages/workspace-do/src/migrations.ts (existing MIGRATIONS array lines 51–57; line 40 import statement; the runner body lines 59–90 needs zero changes — the for-loop applies any not-yet-applied entry automatically)
    - packages/workspace-do/src/__tests__/schema.test.ts (existing `_schema_migrations table (STO-02)` describe at lines 56–78 — currently asserts 2 rows; existing `blocks cold_storage column (D-07 / v2)` describe at lines 102–124 — analog for the new v3 describe block)
    - .planning/phases/06-async-pipeline/06-CONTEXT.md §"Decisions" D-03 — the cardinal D-03 contract: column shape, default value, index name, no DLQ queue
    - .planning/phases/06-async-pipeline/06-PATTERNS.md §1 (V3_SQL template exact form), §2 (MIGRATIONS append + import amendment), §12 (schema.test.ts amendment pattern — full v3 describe block including SQLite quoted-default expectation `"'pending'"`)
  </read_first>
  <behavior>
    - Test 1 (extension of existing `_schema_migrations table (STO-02)` describe): after first WorkspaceDO init, `_schema_migrations` table contains exactly 3 rows ordered by version: v1_initial_schema, v2_cold_storage, v3_ingest_status. Each row's `applied_at` is a number.
    - Test 2 (new `blocks ingest_status column (D-03 / v3)` describe): PRAGMA table_info(blocks) shows an `ingest_status` column with type `TEXT`, `notnull === 1`, and `dflt_value === "'pending'"` (SQLite quotes string-literal defaults; the assertion must match the quoted form).
    - Test 3 (same describe as test 2): `sqlite_master` lists an `idx_blocks_ingest_status` index on the `blocks` table.
    - Negative assertion: no V3-migration re-run on second DO instantiation — covered by the existing hibernation.test.ts (no edit needed) AND by the row-count `expect(rows.length).toBe(3)` (a re-run would either fail the column add or insert a 4th `_schema_migrations` row).
  </behavior>
  <action>
    Three coordinated edits — append-only on `schema.ts` and `migrations.ts`; extend (do not rewrite) `schema.test.ts`.

    1. **`packages/workspace-do/src/schema.ts`** — append a `V3_SQL` export after the existing `V2_SQL` block. Mirror V2_SQL's tagged-template `as const` form and its multi-paragraph doc-block. Required JSDoc anchor: the header sentence cites "Phase 6 D-03" (NOT Phase 5 D-07). Required design-notes bullets: forward-only via `_schema_migrations` runner; `ALTER TABLE ADD COLUMN` is NOT idempotent at SQL layer (version check is the guarantee); `DEFAULT 'pending'` rationale (existing rows treated as pre-enrichment so the Triage Worker can sweep them in v0.2); `CREATE INDEX IF NOT EXISTS` rationale (v0.2 inbox-UI "broken memories" query filters on this column). Cross-plan contract section names this plan (06-01) as the appender, identifies queries.ts as the updater (06-03), and identifies markIngestFailed as the failed-state writer. Match the `@module @engram/workspace-do/schema` tag.

       The V3_SQL value itself (literal SQL):
       - `ALTER TABLE blocks ADD COLUMN ingest_status TEXT NOT NULL DEFAULT 'pending';`
       - `CREATE INDEX IF NOT EXISTS idx_blocks_ingest_status ON blocks(ingest_status);`
       Two statements, semicolon-separated, no positional bindings (Pitfall 8 — bind-free DDL).

    2. **`packages/workspace-do/src/migrations.ts`** — two minimal edits:
       - Amend the existing import on line 40 to also pull `V3_SQL`: `import { V1_SQL, V2_SQL, V3_SQL } from "./schema.js";`
       - Append a third entry to the `MIGRATIONS` array (after the existing v2_cold_storage entry, before the closing `]`):
         `{ version: 3, name: "v3_ingest_status", sql: V3_SQL }`
       Add a brief inline comment matching the v2 entry style: "Phase 6 D-03: ingest_status column tracks per-block enrichment state (pending → enriched | failed). Forward-only via _schema_migrations runner."
       Do NOT modify the `runMigrations` function body. Do NOT change the `Migration` interface.

    3. **`packages/workspace-do/src/__tests__/schema.test.ts`** — two changes:
       - Amend the existing `_schema_migrations table (STO-02)` describe block (currently asserts `expect(rows.length).toBe(2)` plus v1 + v2 row assertions): change the row-count expectation to `3`, then add v3 assertions (`expect(v3?.version).toBe(3); expect(v3?.name).toBe("v3_ingest_status"); expect(typeof v3?.applied_at).toBe("number");`) after the existing v2 assertions. Use a unique idFromName for this test if needed for isolation (current value `ws-schema-migrations-test` is fine — keep it).
       - Append a new `describe("blocks ingest_status column (D-03 / v3)", ...)` block AFTER the existing `blocks cold_storage column (D-07 / v2)` describe (so the file reads chronologically by migration version). Mirror the cold_storage describe's exact shape: `runInDurableObject` + `PRAGMA table_info(blocks)` introspection + column-find + assertions. The three assertions: `expect(ingestStatus?.type.toUpperCase()).toBe("TEXT")`, `expect(ingestStatus?.notnull).toBe(1)` (because the column is NOT NULL), `expect(ingestStatus?.dflt_value).toBe("'pending'")` (SQLite quotes string defaults — the assertion MUST include the single quotes around `pending`). Add a second test inside the same describe asserting `idx_blocks_ingest_status` is in `sqlite_master` (`SELECT name FROM sqlite_master WHERE type = 'index' AND tbl_name = 'blocks'` then `expect(indexes).toContain("idx_blocks_ingest_status")`). Use a unique workspace name (e.g. `ws-schema-ingest-status-test`) for isolation.

       Do NOT modify the `table presence (STO-03)` describe (count of 7 user tables is unchanged — `blocks` already exists). Do NOT modify the `blocks embedding columns (STO-04)` describe.

       Note: this is a TDD task. RED → GREEN cycle: (a) update the test file FIRST so it expects 3 rows + the new column; (b) run `npm test -- schema.test.ts` and observe the failures (row count 2 ≠ 3, column not found); (c) implement V3_SQL + MIGRATIONS append; (d) re-run and observe GREEN. Commit RED separately from GREEN per the TDD plan-task convention.
  </action>
  <verify>
    <automated>cd packages/workspace-do && npx vitest run src/__tests__/schema.test.ts --reporter=verbose 2>&1 | tee /tmp/06-01-test.log; \
      grep -q "ingest_status column (D-03 / v3)" /tmp/06-01-test.log \
      && grep -q "v3_ingest_status" packages/workspace-do/src/migrations.ts \
      && grep -q "export const V3_SQL" packages/workspace-do/src/schema.ts \
      && grep -q "ALTER TABLE blocks ADD COLUMN ingest_status TEXT NOT NULL DEFAULT 'pending'" packages/workspace-do/src/schema.ts \
      && grep -q "idx_blocks_ingest_status" packages/workspace-do/src/schema.ts \
      && grep -q "V3_SQL" packages/workspace-do/src/migrations.ts</automated>
  </verify>
  <done>
    `schema.ts` exports `V3_SQL` with the ALTER + INDEX statements; `migrations.ts` imports it and appends the v3 entry to `MIGRATIONS`; `schema.test.ts` GREEN with the row-count-3 assertion + new v3 column + index describe block; vitest exits 0 for `schema.test.ts`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (doc edits) | ROADMAP.md / REQUIREMENTS.md / pending todo are project-internal docs; no external attacker surface. |
| WorkspaceDO SQLite migration | The V3_SQL DDL runs inside `blockConcurrencyWhile` on every cold start; no untrusted input reaches the migration body (it is a hard-coded string constant). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-DDL-INJECT | Tampering | `V3_SQL` constant in schema.ts | mitigate | DDL is a hard-coded `as const` string with no positional bindings, no template-substitution of user input, no env-var interpolation. Pitfall 8 (multi-statement bindings) is moot because the DDL takes no `?` placeholders. Reviewers verify the exported value is a literal string at PR time. |
| T-06-01-MIGRATION-REPLAY | Tampering | `migrations.ts` runner against a v2-only DO | accept (by design) | The `_schema_migrations` table is the single source of truth for applied versions; a v3 entry never replays once stamped. ALTER TABLE ADD COLUMN is non-idempotent at SQL but the version-check makes the runner idempotent overall. STO-09 hibernation-replay test already covers this (Phase 2). |
| T-06-01-DOC-OVERSTATEMENT | Information Disclosure | ROADMAP.md / REQUIREMENTS.md PIP-04 wording | mitigate | Task 1 explicitly strikes the over-promised "conflict detection" clause AND its "against existing memories in the workspace" modifier (W1 fix) so v0.1 ships what is documented. Without this edit, downstream agents (planners for v0.2) might assume Phase 6 ships conflict detection — a false starting point. |
| T-06-01-PENDING-TODO-LOSS | Repudiation | Conflict-detection-precision pending todo | mitigate | Task 1 annotates rather than deletes the todo; the v0.2 validation harness description survives intact + carries a planner-authored note that the gate moves milestones. |
| T-06-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure doc edits + sync TS additions against existing dependencies. Slopcheck N/A. |

</threat_model>

<verification>
After both tasks complete:

1. `cd packages/workspace-do && npx vitest run src/__tests__/schema.test.ts` exits 0 (all describe blocks GREEN: STO-02 / STO-03 / D-07 v2 / D-03 v3 / STO-04).
2. `grep -c "v3_ingest_status" packages/workspace-do/src/migrations.ts | grep -v '^#'` returns ≥ 1.
3. `grep -q "Conflict detection deferred to v0.2 — see Phase 6 CONTEXT.md D-01" .planning/ROADMAP.md` succeeds.
4. `! grep -q "memorability scoring against existing memories" .planning/ROADMAP.md` succeeds (W1 negative assertion — the modifier is gone, not re-attached to memorability).
5. `! grep -q "memorability scoring against existing memories" .planning/REQUIREMENTS.md` succeeds (W1 negative assertion).
6. `npx wrangler types --config packages/workspace-do/wrangler.test.jsonc 2>&1 | grep -v "warning" | grep -v "Generating"` does not introduce new errors (no Env-interface drift from this plan — V3_SQL is a string, not a binding).
7. (Optional, sanity) `cd packages/workspace-do && npx vitest run src/__tests__/hibernation.test.ts` exits 0 — re-instantiating the DO does not re-run v3.

If any check fails, the plan is incomplete. Common failure modes:
- `dflt_value` mismatch: SQLite quotes the default in `PRAGMA table_info`; expected `"'pending'"` not `"pending"`.
- Row count 2 not 3: forgot to amend the `_schema_migrations` row-count assertion.
- Import drift: forgot to add `V3_SQL` to the `migrations.ts` import statement.
- W1 regression: the executor accidentally re-attached "against existing memories in the workspace" to "memorability scoring" — re-read the W1 corrected text above and remove the modifier entirely.
</verification>

<success_criteria>
- All Phase 6 downstream plans can assume `blocks.ingest_status` exists at runtime.
- ROADMAP.md + REQUIREMENTS.md + pending todo no longer overstate what Phase 6 delivers (D-01 implemented; W1 wording correct).
- `_schema_migrations` carries 3 rows after a fresh DO init.
- schema.test.ts GREEN end-to-end.
</success_criteria>

<output>
Create `.planning/phases/06-async-pipeline/06-01-SUMMARY.md` when done. Summary should record:
- Confirmation that V3_SQL value matches D-03 exactly (TEXT NOT NULL DEFAULT 'pending' + supporting index)
- Confirmation that the doc edits are minimal-surface (no other ROADMAP/REQUIREMENTS lines changed) AND that the W1 wording is correct (no "against existing memories in the workspace" modifier attached to memorability scoring)
- Test run output (3 GREEN describes in schema.test.ts including the new v3 block)
- Any deviations from the plan (none expected — this is a tightly scoped foundation wave)
</output>
</content>
</invoke>