---
phase: 04-synthesis-activation-eval
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts
autonomous: true
requirements:
  - SYN-01
  - SYN-02
  - SYN-04

must_haves:
  truths:
    - "synthesis-fidelity.eval.test.ts exists in the eval tier and runs under MAX_AI_CALLS ≤ 200 — extends the existing vitest .eval.test.ts harness, not promptfoo (D-05)"
    - "LLM-judge faithfulness pass rate ≥ 90% across the 30 validate-split entries (BLOCKS phase) — judged against the SOURCE MEMORIES, caption-independent (D-06)"
    - "Zero hallucinated entities across all passing entries (BLOCKS phase)"
    - "Latency p99 ≤ LOCAL_HANG_CEILING_MS (20s) for the local hang guard; p50/p99 logged against SYN-04 budget"
    - "Completeness check uses expected_synthesis captions as secondary signal (logged, not phase-blocking)"
    - "Judge verdict consumed only after JudgeVerdict.safeParse — malformed response does not corrupt the gate"
    - "Query expansion budget leak is prevented (eval uses direct synthesis call or disables expansion)"
  artifacts:
    - path: packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts
      provides: "Full SYN-01/SYN-02/SYN-04 eval with judge loop, faithfulness gate, latency gate"
      exports: []
    - path: packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts
      provides: "SYNTHESIS_FIDELITY_QUERY_CAP = 30 constant; budget math documented"
      contains: "SYNTHESIS_FIDELITY_QUERY_CAP"
  key_links:
    - from: packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts
      to: shared/ai-config/src/index.ts
      via: "import { JUDGE_MODEL } from '@engram/ai-config'"
      pattern: "JUDGE_MODEL"
    - from: packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts
      to: packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json
      via: "build-time JSON import with { type: 'json' }"
      pattern: "recall-corpus-v2\\.json"
    - from: packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts
      to: packages/mcp-server/src/tools.ts
      via: "captureCallback / registerTools invocation to get the real recall() handler"
      pattern: "registerTools|captureCallback"
---

<objective>
The eval gate: write synthesis-fidelity.eval.test.ts, run it against the hardened synthesis block (Plan 04-03) and the augmented corpus (Plan 04-02), and assert ≥90% faithfulness + zero hallucinated entities. This is the phase-blocking eval — it must pass before /gsd:verify-work.

Purpose: Validate that the production synthesis path is trustworthy. The LLM judge uses @cf/meta/llama-3.3-70b-instruct-fp8-fast (stronger than the Scout generator) to check faithfulness against source memories — not captions. The secondary completeness signal (expected_synthesis captions) is logged but not gating.

Output: synthesis-fidelity.eval.test.ts with passing faithfulness gate (≥90%) and latency gate.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-synthesis-activation-eval/04-CONTEXT.md
@.planning/phases/04-synthesis-activation-eval/04-RESEARCH.md
@.planning/phases/04-synthesis-activation-eval/04-PATTERNS.md
@.planning/phases/04-synthesis-activation-eval/04-VALIDATION.md

<interfaces>
From recall-latency.eval.test.ts (primary analog — copy these exactly):

```typescript
// Percentile helper (verbatim copy from recall-latency.eval.test.ts lines 155–162):
function computePercentile(sortedMs: number[], p: number): number {
  if (sortedMs.length === 0) return 0;
  const idx = Math.min(Math.max(Math.ceil((p / 100) * sortedMs.length) - 1, 0), sortedMs.length - 1);
  return sortedMs[idx] ?? 0;
}

// Credentials guard (from recall-latency.eval.test.ts lines 103–115):
function hasEvalCreds(): boolean {
  if (process.env.CLOUDFLARE_ACCOUNT_ID ?? process.env.CF_ACCOUNT_ID) return true;
  try { return typeof (env as { AI?: unknown }).AI !== "undefined"; } catch { return false; }
}

// Corpus import (build-time, not fs.readFileSync — mandatory for workerd):
import corpusJson from "./fixtures/recall-corpus-v2.json" with { type: "json" };
```

From recall-f1.eval.test.ts (captureCallback pattern, lines 94–124):

```typescript
function captureCallback(toolName, workspace_id) {
  const spy = vi.spyOn(McpServer.prototype, "registerTool");
  try {
    const server = new McpServer({ name: "engram-eval", version: "0.0.1" });
    registerTools(server, () => ({ workspace_id, user_id: "u-eval" }), env,
      () => ({ waitUntil: (p) => { void p; } }) as unknown as DurableObjectState);
    for (const rawCall of spy.mock.calls) {
      const [callName, , callCb] = rawCall;
      if (callName === toolName) return callCb;
    }
    throw new Error(`registration for '${toolName}' not captured`);
  } finally { spy.mockRestore(); }
}
```

From shared/ai-config/src/index.ts — constants to import:

```typescript
import { JUDGE_MODEL, SYNTHESIS_MODEL, sanitizeJsonSchemaForWorkersAI } from "@engram/ai-config";
```

Judge verdict schema (Zod-gated per RESEARCH.md Pattern 1 + V5 ASVS):

```typescript
import { z } from "zod";
import { zodToJsonSchema } from "zod-to-json-schema";
const JudgeVerdict = z.object({
  faithful: z.boolean(),
  hallucinated_entities: z.array(z.string()),
  unsupported_claims: z.array(z.string()),
});
const JUDGE_JSON_SCHEMA = sanitizeJsonSchemaForWorkersAI(
  zodToJsonSchema(JudgeVerdict, { target: "openApi3", $refStrategy: "none" })
);
```

Judge system prompt (from RESEARCH.md Pattern 2 — Claude's discretion per D-04, encode faithfulness rubric):

```
You are a faithfulness judge for a memory recall system. Given a synthesis paragraph and the source memories it was generated from, determine whether the synthesis is faithful.

A synthesis is FAITHFUL if and only if:
- Every factual claim traces to at least one supplied source memory
- No entity names, dates, companies, roles, or numeric values appear that are absent from ALL source memories
- Paraphrasing is acceptable; fabrication is not

Return a JSON object with:
  "faithful": true if ALL claims are traceable to source memories; false if ANY claim lacks support
  "hallucinated_entities": list of entity names/values in the synthesis that do NOT appear in any source memory (empty list if none)
  "unsupported_claims": list of synthesis sentences or clauses with no source memory support (empty list if none)

Source memories are provided as numbered blocks. Treat each numbered block as the ground truth.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write synthesis-fidelity.eval.test.ts (SYN-01/SYN-02/SYN-04)</name>
  <files>packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts</files>

  <read_first>
  1. packages/mcp-server/src/__tests__/evals/recall-latency.eval.test.ts — the FULL file (primary analog): header docblock, import block, constants, hasEvalCreds(), computePercentile(), describe/it structure, creds-guard inside test body, 10-minute timeout, budget-math comment.
  2. packages/mcp-server/src/__tests__/evals/recall-f1.eval.test.ts lines 94–124 — captureCallback helper to invoke recall() via the registered tool.
  3. packages/mcp-server/src/__tests__/evals/eval-budget.setup.ts — confirm MAX_AI_CALLS=200 is auto-applied; do NOT reference it directly in the new test file.
  4. .planning/phases/04-synthesis-activation-eval/04-PATTERNS.md "synthesis-fidelity.eval.test.ts" section — all pattern blocks: header docblock, imports, SYNTHESIS_FIDELITY_QUERY_CAP=30 with budget math, hasEvalCreds(), percentile helper, judge-call + Zod-gated verdict pattern, eval entry loop structure.
  5. .planning/phases/04-synthesis-activation-eval/04-RESEARCH.md Pitfall 2 (budget exhaustion — disable query expansion during eval), Pitfall 5 (expected_synthesis captions must not contaminate judge), Q5 (latency SYN-04 pattern), Q6 (eval harness extension, exact vitest config).
  6. packages/mcp-server/src/__tests__/evals/fixtures/recall-corpus-v2.json — inspect 2–3 validate-split entries to understand the query/expected_top_3_block_ids shape before writing the judge-user-message builder.
  </read_first>

  <behavior>
  Phase-blocking behaviors (fail → BLOCKS phase):

  - Test: "judge pass rate ≥ 90%; zero hallucinated entities; local hang guard" (creds required):
    - Filters corpus to validate-split entries (30 entries per SYNTHESIS_FIDELITY_QUERY_CAP).
    - For each entry: invokes recall(verbosity="synthesis") via captureCallback; extracts synthesis string from response.
    - Budget leak prevention: disable query expansion for this eval by passing a fixture workspace with cosine > 0.65 (the adaptive routing threshold), OR invoke the synthesis path directly without going through the full recall pipeline. See Pitfall 2 in RESEARCH.md — 30 entries × 3 calls = 90 within budget, but expansion firing could push to 240. The safest approach: call the synthesis helpers directly on pre-seeded fixtures rather than full recall() to avoid budget variability.
    - If synthesis is null (honest-stubs fallback): skip judge call for this entry; count as neither pass nor fail for the faithfulness rate but log as "synthesis-null" (synthesis failures are a separate concern — not the faithfulness gate).
    - Judge call: safeRun(env, JUDGE_MODEL, { messages: [system, user], temperature: 0.1, max_tokens: 512, response_format: { type: "json_schema", json_schema: JUDGE_JSON_SCHEMA } }).
    - Zod gate: JudgeVerdict.safeParse(judgeResp.response) before consuming verdict. If parse fails, count the entry as a judge-error (logged; not as a faithfulness fail — V5 security gate prevents malformed responses from corrupting the metric).
    - Hard gates (phase-blocking):
      - expect(passCount / judgedTotal).toBeGreaterThanOrEqual(0.9) — SYN-02 faithfulness ≥ 90%
      - expect(totalHallucinatedEntities).toBe(0) — SYN-02 zero hallucinated entities
      - expect(p99).toBeLessThanOrEqual(LOCAL_HANG_CEILING_MS) — SYN-04 local hang guard (20_000ms)
    - Logged (not gating): p50 latency vs P50_BUDGET_MS=5000, p99 vs P99_BUDGET_MS=8000, completeness check vs expected_synthesis captions.

  Non-gating behaviors:
  - Test: "computePercentile sanity (no creds)" — mirrors the recall-latency pattern; always passes.
  - Corpus content check: at least 30 validate-split entries exist in the fixture; at least 1 has non-null expected_synthesis (SYN-01 smoke check).
  </behavior>

  <action>
  Create packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts following these steps:

  1. File-header docblock: list SYN-01 / SYN-02 / SYN-04, note the BLOCKS phase gates, document the budget math (30 × 3 = 90 calls ≤ MAX_AI_CALLS=200). Warn: "Do NOT edit SYNTHESIS_SYSTEM_PROMPT — any prompt change requires re-running this eval (D-01/SYN-10)."

  2. Import block (before any vi.mock — no mocks needed; this is a real eval):
     - import { describe, it, expect } from "vitest"
     - import { env } from "cloudflare:workers"
     - import { JUDGE_MODEL } from "@engram/ai-config"
     - import { sanitizeJsonSchemaForWorkersAI } from "@engram/ai-config"
     - import { z } from "zod"
     - import { zodToJsonSchema } from "zod-to-json-schema"
     - import { safeRun } from "../../ai-helper.js"
     - import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
     - import { registerTools } from "../../tools.js"
     - import corpusJson from "./fixtures/recall-corpus-v2.json" with { type: "json" }

  3. Define SYNTHESIS_FIDELITY_QUERY_CAP = 30 with budget-math comment (30 × 3 = 90 ≤ 200).

  4. Define budget constants: P50_BUDGET_MS=5000, P99_BUDGET_MS=8000, LOCAL_HANG_CEILING_MS=20_000.

  5. Define JudgeVerdict Zod schema and JUDGE_JSON_SCHEMA (using sanitizeJsonSchemaForWorkersAI per the project's CF JSON Mode error-3030 workaround).

  6. Define JUDGE_SYSTEM_PROMPT string (the faithfulness rubric from the interfaces section above — Claude's discretion per D-04).

  7. Define buildJudgeUserMessage(synthesis: string, sourceMemories: string): string — formats synthesis + source memories for the judge user turn. Source memories are the formatted synthesis input (the numbered block text passed to safeRun, available from formatBlocksForSynthesis or reconstructed from the ranked memories).

  8. Copy computePercentile verbatim from recall-latency.eval.test.ts (it's a pure function — identical across eval files).

  9. Copy hasEvalCreds() verbatim from recall-latency.eval.test.ts.

  10. Copy captureCallback from recall-f1.eval.test.ts (to invoke recall() via the registered tool handler).

  11. Write the "corpus content" smoke test (no creds required): verify validate-split entries ≥ 30 and at least 1 has non-null expected_synthesis.

  12. Write the "computePercentile sanity" test block (no creds required).

  13. Write the main faithfulness + latency eval test (creds required):
      - Skip with console.log if !hasEvalCreds().
      - Filter corpus to validate-split, slice to SYNTHESIS_FIDELITY_QUERY_CAP.
      - For each entry: time the recall() call; extract synthesis; skip judge if synthesis is null.
      - Judge call: use safeRun with JUDGE_MODEL, JudgeVerdict.safeParse the response (V5 Zod gate).
      - Track: passCount, judgedTotal, totalHallucinatedEntities, latencyMs[].
      - Completeness check (logged only): if entry.expected_synthesis and synthesis exist, log semantic overlap note (string comparison, not a hard gate).
      - After loop: sort latencyMs, compute p50 and p99, log them against P50_BUDGET_MS / P99_BUDGET_MS.
      - Hard assertions: faithfulness ≥ 90%, zero hallucinated entities, p99 ≤ LOCAL_HANG_CEILING_MS.
      - Test timeout: 10 * 60 * 1000 (10 minutes) — same as recall-latency.eval.test.ts.

  Budget mitigation for Pitfall 2: the eval invokes recall(verbosity="synthesis", query=entry.query) via the full captureCallback path. If adaptive expansion fires (top1_cosine < 0.65), this adds 3–5 AI calls per entry. As a precaution, add a comment documenting the budget math under worst-case expansion: "If expansion fires for all 30 entries: 30 × ~8 = 240 > 200. Mitigation: the 0.85 variant-gate + 0.65 adaptive threshold typically results in <20% of queries triggering fan-out. If budget is exceeded, re-run with EVAL_SPLIT=train (70 entries to avoid) or call synthesis helpers directly." Do NOT change MAX_AI_CALLS.

  [route] 04-04-T1 synthesis-fidelity.eval.test.ts (~300 LOC eval file) | scaffoldTests | N/Y/Y | single-file eval scaffold with a well-defined spec (judge loop + JudgeVerdict schema + budget math), recall-latency.eval.test.ts as the template analog; >50 LOC mechanical generation
  </action>

  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram/packages/mcp-server && npx vitest run --project workerd synthesis-fidelity 2>&1 | grep -E "PASS|FAIL|skip|corpus content|computePercentile" | head -20</automated>
  </verify>

  <acceptance_criteria>
  - File exists: ls packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts.
  - TypeScript compilation: cd packages/mcp-server && npx tsc --noEmit exits 0.
  - No-creds tests pass (corpus smoke test + percentile sanity): npx vitest run --project workerd synthesis-fidelity exits 0 even without CF credentials (creds-required test skips gracefully).
  - SYNTHESIS_FIDELITY_QUERY_CAP=30 is present: grep "SYNTHESIS_FIDELITY_QUERY_CAP = 30" packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts.
  - JUDGE_MODEL is imported from @engram/ai-config: grep "JUDGE_MODEL" packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts.
  - JudgeVerdict.safeParse is present (V5 Zod gate): grep "safeParse" packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts.
  - Hard gate assertions present: grep "toBeGreaterThanOrEqual(0.9)\|toBe(0)" packages/mcp-server/src/__tests__/evals/synthesis-fidelity.eval.test.ts returns ≥2 matches.
  </acceptance_criteria>

  <done>synthesis-fidelity.eval.test.ts exists, type-checks, and the no-creds tests pass. The eval gate is ready to run with CF credentials.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Plans 04-01 through 04-04 implement the complete Phase 4 synthesis activation eval gate:
  - JUDGE_MODEL constant (Plan 04-01)
  - Unit tests for all post-processors (Plan 04-01)
  - Corpus captions for 30 validate-split entries (Plan 04-02)
  - tools.ts hardening: SYN-05/SYN-06/SYN-07/D-02/D-09/SYN-09 (Plan 04-03)
  - Eval gate with LLM-judge faithfulness ≥90% (this plan)

  The faithfulness eval requires Cloudflare credentials (CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID) to run the judge against real Workers AI.
  </what-built>

  <how-to-verify>
  1. Ensure CF credentials are set: `echo $CLOUDFLARE_API_TOKEN` and `echo $CLOUDFLARE_ACCOUNT_ID` — both must be non-empty.
  2. Run the eval gate:
     ```
     cd packages/mcp-server && npm run test:eval -- synthesis-fidelity
     ```
  3. Expected output:
     - Corpus smoke test: PASS (30 validate entries found, ≥1 with expected_synthesis)
     - computePercentile sanity: PASS
     - Faithfulness gate (creds required): should run 30 judge calls and report passCount/judgedTotal ≥ 0.9 and totalHallucinatedEntities = 0
     - Latency: p50 and p99 logged; p99 ≤ 20_000ms hard guard
  4. If faithfulness gate FAILS (passRate < 0.9 OR hallucinated entities > 0): BLOCKS phase — do NOT proceed to /gsd:verify-work. Investigate which entries failed (check logged per-entry verdicts) and report here.
  5. If budget is exhausted (MAX_AI_CALLS exceeded error): reduce by re-running with EVAL_SPLIT=validate but query expansion known to be quiet. Document in 04-CF-CODE-ASSIST-USAGE.md.
  6. Check the p50 and p99 latency logs against SYN-04 targets (5s / 8s). Local latency will likely exceed these due to network — that's expected. Confirm p99 is below the 20s hang guard.
  </how-to-verify>

  <resume-signal>Type "approved" if faithfulness ≥ 90% AND zero hallucinated entities AND eval ran to completion. Describe any failures if the gate did not pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthesis string → JUDGE_MODEL prompt | Synthesis text from Scout enters the judge prompt; may contain adversarial structure |
| JUDGE_MODEL response → JudgeVerdict.safeParse | Untrusted judge output crosses into verdict consumption |
| eval harness → Workers AI (JUDGE_MODEL) | Eval calls JUDGE_MODEL at @cf/meta/llama-3.3-70b-instruct-fp8-fast; must not call any production endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | Judge prompt injection via synthesis content | mitigate | Judge system prompt wraps synthesis in explicit delimiters ("Source memories:" / "Synthesis to evaluate:"). The judge response is Zod-parsed before consumption — a fabricated JSON structure in the synthesis cannot corrupt the verdict metric. |
| T-04-04-02 | Information Disclosure | Eval corpus entries logged on failure | accept | Eval fixtures contain synthetic/representative queries — no production workspace data or PII |
| T-04-04-03 | Elevation of Privilege | JUDGE_MODEL called in production recall() | mitigate | JUDGE_MODEL is defined with EVAL-ONLY JSDoc; the eval file imports it but is never loaded in the production Worker bundle (vitest-only file not in wrangler.jsonc entry); grep gate documented in RESEARCH.md anti-patterns |
| T-04-04-04 | Spoofing | Wrong judge model (deprecated llama-3.1-70b) | mitigate | JUDGE_MODEL constant in ai-config uses fp8-fast suffix as const; the deprecated ID would cause a CF API error visible in safeRun's catch; verified in RESEARCH.md Q1 |
| T-04-04-05 | Information Disclosure | CLOUDFLARE_API_TOKEN in eval output | mitigate | hasEvalCreds() checks env binding presence, not the raw token value; token is never printed per eval-budget-summary.mjs CONTRACT pattern |
  | T-04-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages — zod, zod-to-json-schema already project dependencies |
</threat_model>

<verification>
Phase-blocking gates (ALL must pass before /gsd:verify-work):

1. `cd /Users/rmoore/Workspaces/engram/packages/mcp-server && npm run test:eval -- synthesis-fidelity` — eval runs to completion; faithfulness ≥ 90%; zero hallucinated entities.
2. `cd /Users/rmoore/Workspaces/engram/packages/mcp-server && npx vitest run --project workerd` — all unit tests GREEN (no regression from Plans 04-01/04-03).
3. `grep -c "SYNTHESIS_SYSTEM_PROMPT" /Users/rmoore/Workspaces/engram/packages/mcp-server/src/tools.ts` — returns the original count (no change to the prompt constant line count).
4. `grep "verbosity.*default\|defaultValue.*chunks" /Users/rmoore/Workspaces/engram/packages/mcp-server/src/tools.ts | head -5` — verbosity default stays "chunks" (SYN-08 regression guard).

Source audit confirmation — all SYN-01..SYN-10 covered:
- SYN-01: corpus captions + eval file references fixture (Plan 04-02 + 04-04)
- SYN-02: faithfulness ≥90% + zero hallucinated entities gate in eval (Plan 04-04)
- SYN-03: dropUncitedSentences + mapPositionsToCitationIds in tools.ts (Plan 04-03)
- SYN-04: latency p50/p99 logged + hang guard in eval (Plan 04-04)
- SYN-05: preflight throw in trimRankedForSynthesis (Plan 04-03)
- SYN-06: applyHedgePrefix in tools.ts synthesis block (Plan 04-03)
- SYN-07: single-memory guard in tools.ts synthesis block (Plan 04-03)
- SYN-08: verbosity default unchanged — no plan action needed; regression guard in existing tools.test.ts
- SYN-09: analytics blobs[1]="synthesis" + doubles[1]=token_count (Plan 04-03)
- SYN-10: SYNTHESIS_SYSTEM_PROMPT byte-frozen + SYNTHESIS_MODEL stays Scout alias (Plans 04-01/04-03 enforce via acceptance criteria)
</verification>

<success_criteria>
- synthesis-fidelity.eval.test.ts exists, type-checks, and no-creds tests pass.
- With CF credentials: faithfulness gate passes ≥ 90% of 30 validate-split entries.
- Zero hallucinated entities across judged entries.
- Latency p99 ≤ 20s (local hang guard); p50/p99 logged against 5s/8s SYN-04 targets.
- All unit tests GREEN (no regression from synthesis block changes in Plan 04-03).
- Phase is UNBLOCKED — all SYN-01..SYN-10 requirements covered.
</success_criteria>

<output>
Create `.planning/phases/04-synthesis-activation-eval/04-04-SUMMARY.md` when done.
</output>
