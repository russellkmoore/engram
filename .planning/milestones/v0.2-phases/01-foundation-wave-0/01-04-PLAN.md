---
phase: 01-foundation-wave-0
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md
autonomous: true
requirements:
  - PRE-05
tags:
  - cf-code-assist
  - routing-tracker
  - documentation
  - foundation-wave-0

must_haves:
  truths:
    - "The scaffolded tracker file lives at .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md (the ACTUAL phase dir, not the speculative path in REQUIREMENTS.md PRE-05)"
    - "Sections 2-4 (Instructions, 3-Question Checklist, Routing Log table header + seed row) are byte-identical to the canonical Phase 5 v0.1 instance at .planning/milestones/v0.1-phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md EXCEPT for the three documented swaps (phase number, scope statement, stop trigger)"
    - "The stop-trigger line reads exactly: Stop logging when /gsd:verify-work 1 passes"
    - "The scope statement explicitly states Phase 1 is content/infrastructure-heavy and expected cf-code-assist mix is <10% — Phase 1 is NOT a content-generation phase"
    - "The End-of-Phase Summary section exists as a stub (_TBD — populated at /gsd:verify-work 1 close._) so future plans can extend it without inventing the heading"
    - "Plan notes surface the REQUIREMENTS PRE-05 path discrepancy (`.planning/phases/01-foundation/` vs actual `01-foundation-wave-0/`) — one-line acknowledgement, not a PR blocker"
  artifacts:
    - path: ".planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md"
      provides: "v0.2 Phase 1 routing tracker scaffold; downstream Phase 2..5 plans inherit the same shape, swapping phase numbers"
      contains: "3-Question Checklist"
  key_links:
    - from: ".planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md"
      to: ".planning/milestones/v0.1-phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md"
      via: "verbatim copy of sections 2-4 with documented swaps"
      pattern: "3-Question Checklist"
    - from: ".planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md (stop trigger)"
      to: "/gsd:verify-work 1"
      via: "literal trigger line"
      pattern: "verify-work 1"
---

<objective>
PRE-05 mirrors the Phase 5 v0.1 routing-tracker pattern documented in CLAUDE.md's `## Auto-Load Routing` block. The Phase 4 v0.1 closeout post-mortem found 5 clear misses + 4 partial misses worth ~10-15K tokens of unrealized savings — the tracker exists so future phases catch those misses at the moment of routing, not retroactively.

Phase 1 is a content/infrastructure-heavy phase (mostly markdown + config edits + one short script). The honest expected cf-code-assist mix is < 10%. The scope statement says so explicitly so a future audit doesn't read a near-empty routing log as "the tracker is broken" — the tracker is correct; the phase character is the cause.

The file lives in the actual phase directory `.planning/phases/01-foundation-wave-0/` — not the path REQUIREMENTS.md PRE-05 speculatively named (`.planning/phases/01-foundation/`). The discrepancy is benign: PRE-05 was written before the phase slug was finalized. Plan notes surface it so a future operator searching for the file finds it at the real path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-wave-0/01-RESEARCH.md
@.planning/phases/01-foundation-wave-0/01-PATTERNS.md
@.planning/milestones/v0.1-phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md
@.planning/phases/04-core-tools-envelope/04-CF-CODE-ASSIST-USAGE.md
@CLAUDE.md

<interfaces>
<!-- The three swaps applied to the canonical Phase 5 v0.1 instance -->

Phase number swap:
- Header `# Phase 5 — cf-code-assist Routing Tracker` → `# Phase 1 — cf-code-assist Routing Tracker (v0.2 milestone)`
- All inline "Phase 5" mentions in the prose → "Phase 1" (search-and-replace)

Scope statement swap (canonical Phase 5 prose says "40-60%" — replace with Phase 1's honest range):
- Canonical: "Phase 5 is the AI Integration phase, projected as a content-generation phase that should route 40-60% to cf-code-assist..."
- Phase 1: "Phase 1 is the v0.2 Foundation phase. It produces markdown docs, vitest config, CI workflow YAML, and one short audit script — content-generation share is expected < 10%. The tracker exists to enforce the routing discipline even when most rows route to `claude`."

Stop-trigger swap:
- Canonical: "Stop tracking once `/gsd:verify-work 5` ... returns PASSED."
- Phase 1: "Stop logging when `/gsd:verify-work 1` passes."

</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tracker file → routing discipline | The tracker is a markdown doc; the threat surface is "logging stops happening" (operator skips a row). Mitigation belongs to plan-checker and to the CLAUDE.md inline routing tracker prompt that requires a [route] line at the moment of delegation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-STALE | Tampering | .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md routing log | mitigate | Stop-trigger line is byte-frozen so `/gsd:verify-work 1` step can grep for `Stop logging when \`/gsd:verify-work 1\` passes`. After that gate fires, the End-of-Phase Summary stub is populated and the tracker becomes a historical artifact. Phase 5 v0.1 instance is the empirical precedent: its End-of-Phase Summary post-mortem makes future operators take the discipline seriously. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold 01-CF-CODE-ASSIST-USAGE.md by copying canonical sections 2-4 with three swaps</name>
  <files>.planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md</files>
  <read_first>
    - .planning/milestones/v0.1-phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md (canonical source — read lines 1-45 for the section 2-4 content to copy)
    - .planning/phases/04-core-tools-envelope/04-CF-CODE-ASSIST-USAGE.md (Phase 4 v0.1 reference; CLOSED — illustrates the End-of-Phase Summary post-mortem shape)
    - CLAUDE.md (Auto-Load Routing block — the "Phase 1 cf-code-assist routing tracker" entry will be added in a follow-up; do not edit CLAUDE.md from this plan)
    - .planning/phases/01-foundation-wave-0/01-RESEARCH.md §CF code-assist tracker
    - .planning/phases/01-foundation-wave-0/01-PATTERNS.md §CF code-assist tracker analog
  </read_first>
  <behavior>
    - Test 1 (existence): file exists at `.planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md`
    - Test 2 (canonical structure): contains `## Instructions for the executor` (or equivalent — the canonical phrasing), `## 3-Question Checklist`, `## Routing Log` headings in order
    - Test 3 (scope swap): the scope statement at the top of the file contains the literal `<10%` token and the phrase "Phase 1" — NOT "Phase 5"
    - Test 4 (stop trigger): file contains the literal line `Stop logging when \`/gsd:verify-work 1\` passes` (back-tick around the slash-command)
    - Test 5 (3-question checklist byte-identical): the three numbered questions match the canonical Phase 5 v0.1 instance byte-for-byte (verified by `diff` against the canonical section after applying a phase-number sed swap)
    - Test 6 (table header byte-identical): the routing log table header row matches the canonical instance verbatim
    - Test 7 (End-of-Phase Summary stub): file contains a `## End-of-Phase Summary` heading immediately followed by `_TBD — populated at /gsd:verify-work 1 close._`
    - Test 8 (path discrepancy note): file contains a one-line acknowledgement: a HTML comment OR a small footer note saying the REQUIREMENTS PRE-05 path was speculative and the canonical location is this file
  </behavior>
  <action>
    Create `.planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md`. Open the canonical Phase 5 v0.1 instance (`.planning/milestones/v0.1-phases/05-ai-integration/05-CF-CODE-ASSIST-USAGE.md`). Copy the file's content with three swaps:
    (1) Header `# Phase 5 — cf-code-assist Routing Tracker` → `# Phase 1 — cf-code-assist Routing Tracker (v0.2 milestone)`. Replace any inline "Phase 5" references in the lead paragraph with "Phase 1".
    (2) Scope paragraph at the top: replace the canonical "Phase 5 is the AI Integration phase, projected as a content-generation phase that should route 40-60%..." sentence with: "Phase 1 is the v0.2 Foundation phase. It produces markdown docs, vitest config, CI workflow YAML, and one short audit script — content-generation share is expected `<10%`. The tracker exists to enforce the routing discipline even when most rows route to `claude`."
    (3) Stop trigger: replace the canonical "Stop tracking once `/gsd:verify-work 5` ..." line with: "Stop logging when `/gsd:verify-work 1` passes."
    Sections 2-4 (Instructions, 3-Question Checklist, Routing Log) are otherwise byte-identical — leave them alone. After the Routing Log table header + seed row, append a `## End-of-Phase Summary` heading with body `_TBD — populated at /gsd:verify-work 1 close._`.
    At the bottom of the file, add a small HTML comment: `<!-- Note: REQUIREMENTS.md PRE-05 specifies path .planning/phases/01-foundation/01-CF-CODE-ASSIST-USAGE.md. That path was written before the phase slug was finalized. The canonical location is this file, at .planning/phases/01-foundation-wave-0/. -->`
    Commit message convention: `docs(01): scaffold cf-code-assist routing tracker for Phase 1`.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/engram &amp;&amp; test -f .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md &amp;&amp; grep -q "# Phase 1 — cf-code-assist Routing Tracker (v0.2 milestone)" .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md &amp;&amp; grep -q "## 3-Question Checklist" .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md &amp;&amp; grep -q "## Routing Log" .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md &amp;&amp; grep -F "Stop logging when \`/gsd:verify-work 1\` passes" .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md &amp;&amp; grep -F "&lt;10%" .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md &amp;&amp; grep -q "## End-of-Phase Summary" .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md &amp;&amp; grep -F "_TBD" .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md &amp;&amp; grep -F "REQUIREMENTS.md PRE-05 specifies path" .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md &amp;&amp; ! grep -q "Phase 5 is the AI Integration phase" .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md &amp;&amp; ! grep -F "verify-work 5" .planning/phases/01-foundation-wave-0/01-CF-CODE-ASSIST-USAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the actual phase-dir path
    - Header line is exactly `# Phase 1 — cf-code-assist Routing Tracker (v0.2 milestone)`
    - Headings `## 3-Question Checklist`, `## Routing Log`, `## End-of-Phase Summary` all present
    - Stop-trigger line literal present: `Stop logging when \`/gsd:verify-work 1\` passes`
    - Scope statement contains `<10%` literal
    - End-of-Phase Summary body contains `_TBD`
    - Path-discrepancy HTML comment contains `REQUIREMENTS.md PRE-05 specifies path`
    - File does NOT contain any "Phase 5 is the AI Integration phase" line (negative — confirms scope swap happened)
    - File does NOT contain `verify-work 5` (negative — confirms stop-trigger swap happened)
    - 3-Question Checklist body matches the canonical instance byte-for-byte (verified by `diff <(awk '/^## 3-Question Checklist/,/^## Routing Log/' canonical) <(awk '/^## 3-Question Checklist/,/^## Routing Log/' new)` — no diff)
  </acceptance_criteria>
  <done>
    The tracker scaffold is on disk in the actual phase dir. Future Phase 2..5 planners (and the CLAUDE.md inline routing tracker rule) have a working anchor. The path-discrepancy note prevents a future operator from looking for the file at the speculative path and concluding the tracker is missing.
  </done>
</task>

</tasks>

<verification_criteria>
- Tracker file scaffolded at actual phase-dir path with three documented swaps applied (grep)
- 3-question checklist + routing-log table header byte-identical to canonical Phase 5 v0.1 instance (diff)
- End-of-Phase Summary stub present (grep)
- Stop trigger references /gsd:verify-work 1 (grep)
- Path discrepancy surfaced in HTML comment (grep)
</verification_criteria>
