---
phase: "05-integration-kitchen-sink"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md
  - .planning/research/v0.2-INTEGRATION-MATRIX.md
autonomous: true
requirements:
  - INT-04

must_haves:
  truths:
    - "05-CF-CODE-ASSIST-USAGE.md exists with 3-question-checklist columns and a seeded header row"
    - "v0.2-INTEGRATION-MATRIX.md has defensible Notes entries for all 6 rows documenting audit findings (even if rows stay `pending`)"
    - "No row is set to `out-of-scope` without a written reason; all 6 rows remain `pending` (GENUINE-GAP confirmed by RESEARCH audit)"
  artifacts:
    - path: ".planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md"
      provides: "Phase 5 routing tracker scaffold with 3-question-checklist columns"
    - path: ".planning/research/v0.2-INTEGRATION-MATRIX.md"
      provides: "6-row matrix with audit Notes filled in; all rows `pending` (covered by later plans)"
  key_links:
    - from: "05-CF-CODE-ASSIST-USAGE.md"
      to: "~/.claude/CLAUDE.md Phase 5 routing tracker rule"
      via: "file creation per CLAUDE.md 'file to be created by Plan 05-01' directive"
    - from: "v0.2-INTEGRATION-MATRIX.md Notes column"
      to: "05-RESEARCH.md D-01 matrix audit findings"
      via: "written reason per D-01 / D-02 / D-03"
---

<objective>
Wave 0 infrastructure for Phase 5: create the Phase 5 cf-code-assist routing tracker (required by CLAUDE.md
"Phase 5 cf-code-assist routing tracker" rule before any code-generating task may run), and document the
matrix audit findings from RESEARCH.md in the v0.2-INTEGRATION-MATRIX.md Notes column so the 6-row
gap classification has a written, defensible record.

Purpose: The routing tracker file is a CLAUDE.md requirement that every code-producing task in Phase 5
must append a routing row to. Creating it in Wave 1 unblocks all Wave 2 plan executions. The matrix Notes
update creates the auditor's paper trail (D-01 discipline): the executor verifies the 6 GENUINE-GAP
classifications on disk and writes the reasons, so that when rows flip to `tested` in Plan 05-05, the
audit trail is present.

Output: 05-CF-CODE-ASSIST-USAGE.md (new), v0.2-INTEGRATION-MATRIX.md (Notes column updated).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-kitchen-sink/05-CONTEXT.md
@.planning/phases/05-integration-kitchen-sink/05-RESEARCH.md
@.planning/research/v0.2-INTEGRATION-MATRIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 05-CF-CODE-ASSIST-USAGE.md routing tracker</name>
  <files>.planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md</files>

  <read_first>
    - `.planning/phases/04-synthesis-activation-eval/04-CF-CODE-ASSIST-USAGE.md` — Phase 4 tracker (exact column format to mirror)
    - `~/.claude/CLAUDE.md` "Phase 5 cf-code-assist routing tracker" section — the 3-question checklist column names and candidate task shapes
  </read_first>

  <action>
Create `.planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md` mirroring the Phase 4 tracker template.

The file header must state:
- Phase: 5 (Integration Kitchen Sink)
- Status: ACTIVE
- Target route ratio: 40–60% cf-code-assist (content-generation phase per CLAUDE.md phase character heuristic)
- Stop condition: when `/gsd:verify-work 5` passes

Table columns (mirror Phase 4 format): Task ID | Context (what is being generated) | Tool | Q1 (SYNTHESIS cross-file?) | Q2 (diff >50 lines?) | Q3 (stable template/spec?) | Route Decision | Reason

Seed the table with Phase 5 candidate shapes identified in CLAUDE.md:
- Row 1 (05-02-T1): v02-kitchen-sink.test.ts body generation (~200+ LOC test scaffold with known assertion spec from PATTERNS.md) — Q1:N Q2:Y Q3:Y → `scaffoldTests`
- Row 2 (05-03-T1): cross-workspace-pentest.test.ts additions (3 Prong-A blocks, ~60 LOC, Prong C skip pattern well-defined) — Q1:N Q2:Y Q3:Y → `scaffoldTests`
- Row 3 (05-04-T1): conflict-pipeline-isolation.test.ts (new file, ~80 LOC, makeWorkspaceStub pattern + D-10 assertion shape) — Q1:N Q2:Y Q3:Y → `scaffoldTests`

These are pre-classified estimates. Actual routing decisions are made at execution time by the executor per the 3-question checklist — rows will be updated with actual decisions. Annotate the seeded rows with "(pre-classified estimate — update at execution time)".

Include a footer note: "Per CLAUDE.md Phase 5 tracking rule: every code-producing task during Phase 5 execution must append one row (or update the estimate row) with the actual 3-question answers before committing. Stop logging when `/gsd:verify-work 5` passes."
  </action>

  <verify>
    <automated>test -f .planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md && grep -c "Q1\|cross-file" .planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md | grep -v '^0$'</automated>
  </verify>

  <done>05-CF-CODE-ASSIST-USAGE.md exists with the routing table header, 3 pre-classified estimate rows, and the stop-condition footer note.</done>
</task>

<task type="auto">
  <name>Task 2: Document matrix audit findings in v0.2-INTEGRATION-MATRIX.md Notes column</name>
  <files>.planning/research/v0.2-INTEGRATION-MATRIX.md</files>

  <read_first>
    - `.planning/research/v0.2-INTEGRATION-MATRIX.md` — current 6 rows (all `pending`, Notes column has placeholder text)
    - `.planning/phases/05-integration-kitchen-sink/05-RESEARCH.md` § "D-01 Matrix Audit — Ground Truth" — the per-row gap classification with evidence (the authoritative source for Notes content)
    - `.planning/phases/05-integration-kitchen-sink/05-CONTEXT.md` § D-01, D-02, D-03 — vocabulary rules and bias-toward-testing rule
  </read_first>

  <action>
Update the Notes column for each of the 6 matrix rows to record the D-01 audit findings from RESEARCH.md.
Status column remains `pending` for all 6 rows (rows flip to `tested` in Plan 05-05 when covering tests land).
Test File column remains `—` for all 6 rows until tests exist.

Per D-03, the status vocabulary is FIXED: only `tested`, `pending`, `out-of-scope` are valid. Do NOT introduce any new status tokens. Do NOT mark any row `out-of-scope` — RESEARCH audit found all 6 are GENUINE-GAP (0 already-tested, 0 out-of-scope).

Update the Notes text for each row to reflect the audit finding and the covering test plan:

Row 1 (RNK × CON): Notes = "GENUINE-GAP (RESEARCH audit 2026-06-10): `recall-conflicts.test.ts` uses Vectorize mock score=0.9 for all hits — no test asserts `context.conflicts[]` populated when conflict-linked memories rank lower than non-conflicting ones after hybridRank ordering. Covered by Plan 05-02 (v02-kitchen-sink.test.ts)."

Row 2 (RNK × EXP): Notes = "GENUINE-GAP (RESEARCH audit 2026-06-10): `recall.test.ts` Test 2 proves fan-out fires and RRF merges, but mocks `hybridRank` — weight behavior on expanded candidate set never exercised with real hybridRank. Covered by Plan 05-02 (v02-kitchen-sink.test.ts)."

Row 3 (EXP × SYN): Notes = "GENUINE-GAP (RESEARCH audit 2026-06-10): No test drives top1_cosine < 0.65 (fan-out) → RRF merge → ranked ≥ 2 → generateSynthesis() called → synthesis string in envelope. `tools-integration.test.ts` synthesis tests run on single-query path only. Covered by Plan 05-02 (v02-kitchen-sink.test.ts)."

Row 4 (CON × SYN): Notes = "GENUINE-GAP (RESEARCH audit 2026-06-10): All 4 `recall-conflicts.test.ts` tests use `verbosity: 'chunks'` — synthesis path never exercised with conflict rows present. Zero coverage of the path where both `context.conflicts[]` populated AND `result.synthesis` non-null simultaneously. Covered by Plan 05-02 (v02-kitchen-sink.test.ts)."

Row 5 (kitchen-sink): Notes = "GENUINE-GAP (RESEARCH audit 2026-06-10): No file drives all 4 features simultaneously with 10-conflict + 50-entity + verbosity=synthesis fixture. `token-budget.test.ts` uses `buildRecallResponse` directly (no recall() invocation through tools.ts). This is the INT-01 primary deliverable. Covered by Plan 05-02 (v02-kitchen-sink.test.ts)."

Row 6 (adaptive-routing × cosine-edge): Notes = "GENUINE-GAP (RESEARCH audit 2026-06-10): `recall.test.ts` Test 3 (EXP-10 fallback) does NOT exercise a simultaneous cosine-edge scenario — scores used (0.8, 0.75) are well above MIN_COSINE_THRESHOLD. The 'both go wrong at once' case (top1 near 0.64 + expansion 429) is untested. Per D-02, this maps to real v0.2 code (ADAPTIVE_TOP1_THRESHOLD=0.65, EXP-10 fallback catch). Covered by Plan 05-02 (v02-kitchen-sink.test.ts)."

The Covering Plan column for rows 1–4 was set from Phase planning (02-04, 03-03, 04-03, 04-04). Update those cells to reflect the actual Phase 5 plan that will close them: "05-02 (kitchen-sink)". Row 5 already maps to 04-05 / INT-01 — update to "05-02". Row 6 was "05-04" — update to "05-02" since all are folded into the kitchen-sink file per RESEARCH §Open Questions 1 resolution.
  </action>

  <verify>
    <automated>grep -c "GENUINE-GAP" /Users/rmoore/Workspaces/engram/.planning/research/v0.2-INTEGRATION-MATRIX.md | grep -E "^6$" && grep -c "pending" /Users/rmoore/Workspaces/engram/.planning/research/v0.2-INTEGRATION-MATRIX.md | grep -E "^6$"</automated>
  </verify>

  <done>All 6 matrix rows have Notes updated with GENUINE-GAP findings; all 6 rows still show `pending` status; Covering Plan column updated to "05-02 (kitchen-sink)" for all rows; Test File column remains `—`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts → executor | PLAN.md and matrix updates are read by gsd-executor; no runtime trust boundary in this setup plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — file creation only |
| T-05-01-01 | Tampering | v0.2-INTEGRATION-MATRIX.md status cells | mitigate | D-03 vocabulary enforcement: only `tested`/`pending`/`out-of-scope` are valid; verify grep gate stays at 6 `pending` after this plan |
</threat_model>

<verification>
After plan completes:

```bash
# CF-CODE-ASSIST tracker exists
test -f .planning/phases/05-integration-kitchen-sink/05-CF-CODE-ASSIST-USAGE.md && echo "PASS"

# Matrix still has 6 pending rows (no premature tested flip)
grep -c "pending" .planning/research/v0.2-INTEGRATION-MATRIX.md

# Matrix has 6 GENUINE-GAP annotations
grep -c "GENUINE-GAP" .planning/research/v0.2-INTEGRATION-MATRIX.md
```

Expected: tracker file present, 6 pending, 6 GENUINE-GAP entries.
</verification>

<success_criteria>
- 05-CF-CODE-ASSIST-USAGE.md created with routing table, 3 pre-classified estimate rows, and stop-condition footer
- v0.2-INTEGRATION-MATRIX.md Notes column updated for all 6 rows with GENUINE-GAP audit findings; all rows remain `pending`; Covering Plan column updated to "05-02 (kitchen-sink)"
- `grep -c "pending" .planning/research/v0.2-INTEGRATION-MATRIX.md` returns 6
</success_criteria>

<output>
Create `.planning/phases/05-integration-kitchen-sink/05-01-SUMMARY.md` when done.
</output>
