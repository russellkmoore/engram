---
phase: 03-query-expansion-reranker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp-server/src/rrf.ts
  - packages/mcp-server/src/__tests__/rrf.test.ts
  - shared/ai-config/src/index.ts
  - shared/ai-config/src/__tests__/ai-config.test.ts
autonomous: true
requirements: [EXP-04, EXP-05]
must_haves:
  truths:
    - "reciprocalRankFusion(lists, k=60) is a pure transform that fuses ranked lists by 1/(k+rank) and reproduces the Elasticsearch k=1 and the k=60 reference vectors exactly"
    - "RERANKER_MODEL = \"@cf/baai/bge-reranker-base\" is exported from @engram/ai-config"
    - "HYBRID_WEIGHTS.rerank already exists (Phase 2 D-05); EXP-05 only adds the constant, does not re-rename"
  artifacts:
    - path: "packages/mcp-server/src/rrf.ts"
      provides: "reciprocalRankFusion pure transform"
      exports: ["reciprocalRankFusion"]
    - path: "packages/mcp-server/src/__tests__/rrf.test.ts"
      provides: "reference-vector unit tests (Elasticsearch k=1 + k=60 default)"
    - path: "shared/ai-config/src/index.ts"
      provides: "RERANKER_MODEL constant"
      contains: "RERANKER_MODEL"
  key_links:
    - from: "shared/ai-config/src/index.ts"
      to: "RERANKER_MODEL = \"@cf/baai/bge-reranker-base\""
      via: "as const export mirroring EMBEDDING_MODEL form"
      pattern: "RERANKER_MODEL.*bge-reranker-base"
---

<objective>
Build the two dependency-free foundations of Phase 3: the pure `reciprocalRankFusion` transform (EXP-04) and the `RERANKER_MODEL` model-ID constant (EXP-05). Both are unit-testable without Cloudflare credentials and unblock the Wave 2 recall() integration.

Purpose: RRF is the scale-invariant merge that fuses the per-variant Vectorize result lists; the reranker constant is the single net-new identifier EXP-05 needs (the `HYBRID_WEIGHTS.cosine → rerank` rename already shipped in Phase 2 D-05). Landing them first means Wave 2 imports stable, tested contracts.
Output: `rrf.ts` + `rrf.test.ts` (pure, reference-vector-validated), `RERANKER_MODEL` constant in `@engram/ai-config` + a constant-presence test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**Project instructions:** Read ./CLAUDE.md — Phase 3 is NOT the active cf-code-assist tracker phase (that is Phase 5), so no per-task routing tracker row is required here.

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-query-expansion-reranker/03-RESEARCH.md
@.planning/phases/03-query-expansion-reranker/03-PATTERNS.md

<interfaces>
<!-- The RRF reference vectors are computed by hand in RESEARCH §Pattern 2 (lines 214-230). -->
<!-- Elasticsearch k=1 worked example (5 docs, BM25 + kNN retrievers): -->
<!--   doc1: 1/(1+4) + 1/(1+3) = 0.4500 -->
<!--   doc2: 1/(1+3) + 1/(1+2) = 0.5833 -->
<!--   doc3: 1/(1+2) + 1/(1+1) = 0.8333   ← winner -->
<!--   doc4: 1/(1+1)           = 0.5000 -->
<!--   doc5:           1/(1+4) = 0.2000 -->
<!-- k=60 default (two lists, D1/D2/D3): -->
<!--   D1 ranks (1,2): 1/61 + 1/62 ≈ 0.03226   ← winner (ties D3 to ~5dp; tiebreak insertion order) -->
<!--   D2 ranks (2,4): 1/62 + 1/64 ≈ 0.03200 -->
<!--   D3 ranks (3,1): 1/63 + 1/61 ≈ 0.03226 -->

<!-- Existing ai-config const form to mirror (index.ts:62): -->
<!--   export const EMBEDDING_MODEL = "@cf/qwen/qwen3-embedding-0.6b" as const; -->
<!-- HYBRID_WEIGHTS.rerank already exists at index.ts:151 (Phase 2 D-05 rename complete). -->
<!--   The Phase-3-anticipating comment block lives at index.ts:134-135. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reciprocalRankFusion pure transform + reference-vector unit tests (EXP-04)</name>
  <files>packages/mcp-server/src/rrf.ts, packages/mcp-server/src/__tests__/rrf.test.ts</files>
  <read_first>
    - packages/mcp-server/src/hybrid-rank.ts (the repo's reference pure-transform module — copy its purity discipline: no env/IO/mutation header comment at line 13, O(1) Map accumulation at line 77, `[...arr].sort(...)` returns a NEW array at lines 124-125, tuning constant as a defaulted parameter at lines 73-74)
    - packages/mcp-server/src/__tests__/hybrid-rank.test.ts (test structure to copy: `import { describe, it, expect } from "vitest"`; no `cloudflare:workers` env, no binding mocks; minimal local input interface; first `it()` asserts a fixed truth then per-behavior blocks)
    - .planning/phases/03-query-expansion-reranker/03-RESEARCH.md §Pattern 2 lines 187-230 (signature, body, and the two reference-vector fixtures)
  </read_first>
  <behavior>
    - Test 1 (Elasticsearch k=1 fixture): fuse two ranked lists (BM25 ordering, kNN ordering) with k=1; assert doc3 ranks first with rrfScore 0.8333, then doc2=0.5833, doc4=0.5000, doc1=0.4500, doc5=0.2000 (each within 1e-4).
    - Test 2 (k=60 default): with no explicit k argument, fuse two lists where D1 ranks (1,2), D2 ranks (2,4), D3 ranks (3,1); assert D1 and D3 both ≈0.03226 and D2≈0.03200, and that the D1/D3 tie breaks by insertion order (D1 before D3).
    - Test 3 (single-list passthrough): one list of 3 items returns them in the same order with rrfScore 1/(60+rank).
    - Test 4 (purity): the input arrays are not mutated (assert input list reference order unchanged after the call).
  </behavior>
  <action>
    Create `reciprocalRankFusion<T extends { id: string }>(lists: T[][], k = 60): { id: string; rrfScore: number; item: T }[]` exactly per RESEARCH §Pattern 2 lines 194-211. Accumulate `1/(k + rank)` (rank is 1-indexed: `idx + 1`) into an O(1) `Map<string, { rrfScore, item }>` keyed by `item.id`, summing contributions only over lists where the id appears. Return a NEW array via `[...scores.entries()].map(...).sort((a,b) => b.rrfScore - a.rrfScore)` — never sort in place. Default `k = 60` as a parameter (mirrors `hybridRank`'s defaulted `weights`/`now` so tests pass explicit values deterministically). Header doc-comment must state "Pure transform — no env, no IO, no mutation" mirroring hybrid-rank.ts:13. The Map insertion-order property is what makes the k=60 tie break deterministically by first-seen id — rely on JS Map iteration order, do not add a secondary sort key.
  </action>
  <verify>
    <automated>cd packages/mcp-server && npx vitest run rrf</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run rrf` passes with all four `it()` blocks green.
    - `rrf.ts` exports `reciprocalRankFusion`; the function references no `env`, no `await`, no `env.AI`/`env.VECTORIZE` (source-grep: `grep -v '^[[:space:]]*\*' packages/mcp-server/src/rrf.ts | grep -c "env\.\|await" ` returns 0).
    - The Elasticsearch k=1 fixture asserts doc3 first with rrfScore 0.8333 ±1e-4; the k=60 fixture asserts the D1/D3 ≈0.03226 tie breaks to D1 by insertion order.
    - Return value is a new array (input lists not mutated — asserted by Test 4).
  </acceptance_criteria>
  <done>reciprocalRankFusion is a pure, exported transform that reproduces both reference-vector fixtures exactly and does not mutate inputs.</done>
</task>

<task type="auto">
  <name>Task 2: RERANKER_MODEL constant in @engram/ai-config + constant-presence test (EXP-05)</name>
  <files>shared/ai-config/src/index.ts, shared/ai-config/src/__tests__/ai-config.test.ts</files>
  <read_first>
    - shared/ai-config/src/index.ts (lines 51-94 model-ID const block to mirror; line 62 `EMBEDDING_MODEL` `as const` form; lines 134-135 the Phase-3-anticipating comment; line 151 `HYBRID_WEIGHTS.rerank` — confirm the rename is already done, do NOT re-rename)
    - .planning/phases/03-query-expansion-reranker/03-PATTERNS.md §`shared/ai-config/src/index.ts` lines 252-262 (the exact `as const` literal style + the note that the rename is complete and only the constant is net-new)
  </read_first>
  <action>
    Add `export const RERANKER_MODEL = "@cf/baai/bge-reranker-base" as const;` to the model-ID const block in `shared/ai-config/src/index.ts`, matching the `EMBEDDING_MODEL` (index.ts:62) `as const` literal form and placement convention. Do NOT touch `HYBRID_WEIGHTS` — the `cosine → rerank` rename already shipped in Phase 2 D-05 (index.ts:151); re-touching it re-opens the Phase 2 sweep. Add a one-line doc-comment noting this is the bge-reranker model consumed via `safeRun` in Phase 3 EXP-06. If a `shared/ai-config/src/__tests__/ai-config.test.ts` exists, append a test asserting `RERANKER_MODEL === "@cf/baai/bge-reranker-base"` and that `HYBRID_WEIGHTS.rerank` is a number; if no such test file exists, create one with `import { describe, it, expect } from "vitest"` and those two assertions plus an assertion that `EMBEDDING_MODEL` is unchanged. Do NOT add the optional `EXPANSION_CHALLENGER_MODEL` here — that lands in the EXP-08 eval plan (03-05) as an eval-only constant.
  </action>
  <verify>
    <automated>cd shared/ai-config && npx vitest run ai-config 2>/dev/null || cd /Users/rmoore/Workspaces/engram && npx vitest run --project=unit ai-config</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RERANKER_MODEL.*@cf/baai/bge-reranker-base" shared/ai-config/src/index.ts` returns ≥1.
    - The ai-config unit test asserts `RERANKER_MODEL === "@cf/baai/bge-reranker-base"` and passes.
    - `git diff shared/ai-config/src/index.ts` shows the `HYBRID_WEIGHTS` block UNCHANGED (no rename, no weight-value edit) — only an added `RERANKER_MODEL` line and its comment.
    - TypeScript compiles: `cd shared/ai-config && npx tsc --noEmit` (or the workspace build) exits 0.
  </acceptance_criteria>
  <done>RERANKER_MODEL is exported from @engram/ai-config, asserted by a passing unit test, with HYBRID_WEIGHTS untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a pure transform and a string constant. No new input crosses a trust boundary; no AI call, no Vectorize call, no user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `rrf.ts` purity | accept | Pure transform with no IO; unit tests pin behavior to reference vectors. No injection surface. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs in this plan (RESEARCH §Package Legitimacy Audit confirms Phase 3 installs no external packages). No slopcheck surface. |
</threat_model>

<verification>
- `cd packages/mcp-server && npx vitest run rrf` — all RRF reference-vector tests green.
- ai-config unit test green; `RERANKER_MODEL` present; `HYBRID_WEIGHTS` diff-clean.
- `cd packages/mcp-server && npm test` (workerd + lint-node) passes — no regression in existing suites.
</verification>

<success_criteria>
- EXP-04: `reciprocalRankFusion(lists, k=60)` exported, pure, reproduces Elasticsearch k=1 + k=60 reference vectors exactly.
- EXP-05: `RERANKER_MODEL = "@cf/baai/bge-reranker-base"` exported from @engram/ai-config; rename confirmed already-done and untouched.
- No new npm dependencies. No Vectorize/SQLite/AI-binding changes.
</success_criteria>

<output>
Create `.planning/phases/03-query-expansion-reranker/03-01-SUMMARY.md` when done.
</output>
