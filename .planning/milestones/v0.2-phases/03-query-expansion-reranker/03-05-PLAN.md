---
phase: 03-query-expansion-reranker
plan: 05
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - packages/mcp-server/src/__tests__/evals/query-expansion-recall.eval.test.ts
  - packages/mcp-server/src/__tests__/evals/recall-latency.eval.test.ts
  - shared/ai-config/src/index.ts
autonomous: false
requirements: [EXP-08, EXP-09, EXP-11, EXP-12]
user_setup:
  - service: cloudflare-workers-ai
    why: "The A/B, anti-HyDE, entity-preservation, and latency evals run against the live Workers AI + Vectorize bindings."
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: "Cloudflare dashboard → My Profile → API Tokens (or `wrangler login` for local)"
      - name: CLOUDFLARE_ACCOUNT_ID
        source: "Cloudflare dashboard → account ID"
must_haves:
  truths:
    - "query-expansion-recall.eval.test.ts A/B tests Scout (QUERY_EXPANSION_MODEL) vs @cf/meta/llama-3.2-3b-instruct on recall@5; promotion to 3.2-3b is a follow-on PR if 3.2-3b recall@5 ≥ Scout recall@5 − 5pp (Scout stays default in v0.2)"
    - "The eval includes an anti-HyDE assertion (EXP-09): expansion variants read as search phrasings, not fabricated answers/documents"
    - "Entity preservation (EXP-12): >80% of named entities in the original query are present in ≥1 variant, via the capitalized-token heuristic"
    - "Recall latency eval (EXP-11): p50 with expansion ON ≤ 1.8s, p99 ≤ 3s"
    - "EXP-07 and EXP-08 evals run in SEPARATE vitest sessions (MAX_AI_CALLS=200 budget cannot hold two ~100-query pre-resolves)"
    - "EXPANSION_CHALLENGER_MODEL = @cf/meta/llama-3.2-3b-instruct is an eval-only constant; Scout stays the QUERY_EXPANSION_MODEL default"
  artifacts:
    - path: "packages/mcp-server/src/__tests__/evals/query-expansion-recall.eval.test.ts"
      provides: "EXP-08 A/B + EXP-09 anti-HyDE + EXP-12 entity-preservation assertions"
    - path: "packages/mcp-server/src/__tests__/evals/recall-latency.eval.test.ts"
      provides: "EXP-11 p50/p99 latency assertion for recall with expansion ON"
    - path: "shared/ai-config/src/index.ts"
      provides: "EXPANSION_CHALLENGER_MODEL eval-only constant"
      contains: "EXPANSION_CHALLENGER_MODEL"
  key_links:
    - from: "query-expansion-recall.eval.test.ts"
      to: "expandQuery (query-expansion.ts) + Scout vs llama-3.2-3b"
      via: "A/B recall@5 + anti-HyDE + entity-preservation across the corpus"
      pattern: "expandQuery|llama-3.2-3b"
    - from: "recall-latency.eval.test.ts"
      to: "recall() with expansion ON"
      via: "p50/p99 timing harness (eval-budget-summary --*-p99 pattern from CON-07)"
      pattern: "p50|p99"
---

<objective>
Build the query-expansion eval suite: the EXP-08 Scout-vs-llama-3.2-3b A/B, the EXP-09 anti-HyDE behavioral assertion, the EXP-12 entity-preservation metric, and the EXP-11 recall latency budget (p50 ≤ 1.8s, p99 ≤ 3s). Depends on 03-03 (the integrated recall path must exist to measure end-to-end latency and corpus recall@5).

Purpose: These evals prove the expansion feature meets its quality + latency contract. EXP-08 decides (in a follow-on PR, not this phase) whether the smaller/faster 3.2-3b model can replace Scout. EXP-09/EXP-12 catch drift and HyDE regressions behaviorally (the unit test in 03-02 only checks the prompt string). EXP-11 is the user-experience latency gate. This is a creds-gated eval plan — `autonomous: false`; the eval files are authored + type-checked autonomously, the live runs are the manual verification step, and crucially EXP-08 must run in a SEPARATE session from the EXP-07 ablation (plan 03-04) because two ~100-query pre-resolves exceed MAX_AI_CALLS=200.
Output: `query-expansion-recall.eval.test.ts` (A/B + anti-HyDE + entity-preservation), `recall-latency.eval.test.ts` (p50/p99), and the eval-only `EXPANSION_CHALLENGER_MODEL` constant.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**Project instructions:** Read ./CLAUDE.md — Phase 3 is NOT the active cf-code-assist tracker phase. No routing tracker row required.

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-query-expansion-reranker/03-RESEARCH.md
@.planning/phases/03-query-expansion-reranker/03-PATTERNS.md
@.planning/phases/03-query-expansion-reranker/03-VALIDATION.md

<interfaces>
<!-- recall-ranking.eval.test.ts harness to clone (same as plan 03-04): -->
<!--   pre-resolve-once Map<string,QueryResolution> + MAX_AI_CALLS=200 budget + computeF1/computeMRR/computeTop1. -->
<!--   EVAL_WORKSPACE_ID = "eval-fixtures"; corpus via build-time import { type: "json" }. -->

<!-- EXP-08 challenger: @cf/meta/llama-3.2-3b-instruct (verified on CF catalog). -->
<!--   QUERY_EXPANSION_MODEL stays aliased to Scout (index.ts:194). Promotion gate: 3.2-3b recall@5 ≥ Scout − 5pp. -->

<!-- EXP-12 entity-preservation heuristic (RESEARCH §Code Examples lines 384-395): -->
<!--   namedEntities(q) = unique matches of /\b[A-Z][a-zA-Z0-9.&-]+\b/g -->
<!--   entityPreservationRate(original, variants) = |entities present in ≥1 variant| / |entities|; gate > 0.80 averaged. -->

<!-- EXP-11 latency: reuse the scripts/eval-budget-summary.mjs --*-p99 timing pattern established for CON-07 -->
<!--   (Phase 2 plan 02-09). p50 ≤ 1.8s, p99 ≤ 3s for recall with expansion ON. -->

<!-- Latency estimates are planning-only (RESEARCH A2); the EXP-11 assertion measures REAL latency. -->
<!--   If over budget, the prescribed lever is tightening ADAPTIVE_TOP1_THRESHOLD 0.65→0.70 (NOT removing the feature). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EXPANSION_CHALLENGER_MODEL constant + query-expansion-recall.eval.test.ts — A/B + anti-HyDE + entity-preservation (EXP-08, EXP-09, EXP-12)</name>
  <files>shared/ai-config/src/index.ts, packages/mcp-server/src/__tests__/evals/query-expansion-recall.eval.test.ts</files>
  <read_first>
    - packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts (the harness to clone — imports, metric functions, pre-resolve-once, budget discipline)
    - packages/mcp-server/src/query-expansion.ts (expandQuery + EXPANSION_SYSTEM_PROMPT from plan 03-02 — the function under A/B)
    - packages/mcp-server/src/__tests__/evals/eval-budget.setup.ts (MAX_AI_CALLS=200)
    - shared/ai-config/src/index.ts (lines 51-94 model-const block; QUERY_EXPANSION_MODEL alias at 194)
    - .planning/phases/03-query-expansion-reranker/03-RESEARCH.md §Code Examples lines 384-395 (entity-preservation heuristic) + §Pitfall 5 lines 335-339 (anti-HyDE)
    - .planning/phases/03-query-expansion-reranker/03-PATTERNS.md §query-expansion-recall lines 174-180
  </read_first>
  <action>
    Add `export const EXPANSION_CHALLENGER_MODEL = "@cf/meta/llama-3.2-3b-instruct" as const;` to `shared/ai-config/src/index.ts` (eval-only; Scout stays QUERY_EXPANSION_MODEL default per EXP-08 — do NOT change the alias). Clone the `recall-ranking.eval.test.ts` harness into `query-expansion-recall.eval.test.ts`. For each corpus query, run `expandQuery` TWICE — once with Scout (QUERY_EXPANSION_MODEL) and once with the challenger (parameterize the model into expandQuery OR call the AI directly with EXPANSION_CHALLENGER_MODEL using the same prompt + zod gate) — then for each model's variant set, embed + Vectorize-fan-out + RRF-merge and compute recall@5 against `expected_top_3_block_ids` (recall@5 = |expected ∩ top5| / |expected|). Report `recall@5(Scout)` and `recall@5(3.2-3b)` and the promotion verdict: 3.2-3b promotable iff `recall@5(3.2-3b) ≥ recall@5(Scout) − 0.05` (5pp) — but the test only LOGS this; promotion is an out-of-phase follow-on PR. EXP-09 anti-HyDE assertion: for the Scout variants, assert each variant is a search phrasing not a fabricated answer/document — heuristic: variant does not start with a declarative answer pattern and does not contain fabricated specifics absent from the original (use a conservative check, e.g. variant length comparable to a query, no sentence-ending fact claims; document the heuristic). EXP-12 entity-preservation: implement `namedEntities` (capitalized-token regex `/\b[A-Z][a-zA-Z0-9.&-]+\b/g`, deduped) and `entityPreservationRate(original, variants)` per RESEARCH lines 384-395; assert the corpus-averaged rate > 0.80. Use the build-time JSON corpus import `{ type: "json" }`. Budget: running BOTH models doubles the expansion calls — this eval is ~2 expansion + 1 embed + 1 Vectorize per query; cap the query subset so total stays ≤ MAX_AI_CALLS=200 and log the count. MUST run in its own vitest session, separate from the EXP-07 ablation (plan 03-04 — A4).
  </action>
  <verify>
    <automated>cd packages/mcp-server && npx tsc --noEmit; grep -c "EXPANSION_CHALLENGER_MODEL\|entityPreservation\|recall@5\|recall5" src/__tests__/evals/query-expansion-recall.eval.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `query-expansion-recall.eval.test.ts` exists and TypeScript-compiles: `cd packages/mcp-server && npx tsc --noEmit` exits 0.
    - `grep -c "EXPANSION_CHALLENGER_MODEL.*llama-3.2-3b-instruct" shared/ai-config/src/index.ts` ≥1; QUERY_EXPANSION_MODEL alias unchanged (`git diff` shows no edit to line 194).
    - A/B present: both Scout and the challenger are invoked and recall@5 is computed for each (`grep -c "EXPANSION_CHALLENGER_MODEL\|QUERY_EXPANSION_MODEL" src/__tests__/evals/query-expansion-recall.eval.test.ts` ≥2); the 5pp promotion gate is logged (`grep -c "0.05\|5pp" ...` ≥1).
    - EXP-12: `entityPreservationRate` + `namedEntities` implemented; the regex `[A-Z][a-zA-Z0-9.&-]+` is present; the corpus-averaged-rate > 0.80 assertion exists.
    - EXP-09: an anti-HyDE assertion exists checking variants are search phrasings not fabricated answers (the heuristic is documented in a comment).
    - The query subset count is logged and kept ≤200 calls; a comment notes "run in a separate session from reranker-ablation (A4 MAX_AI_CALLS=200)".
  </acceptance_criteria>
  <done>The A/B eval is authored and type-checks: Scout vs 3.2-3b recall@5 with the 5pp promotion verdict logged, EXP-09 anti-HyDE assertion, and EXP-12 >80% entity-preservation, all within the 200-call budget in a dedicated session.</done>
</task>

<task type="auto">
  <name>Task 2: recall-latency.eval.test.ts — EXP-11 p50 ≤ 1.8s / p99 ≤ 3s with expansion ON</name>
  <files>packages/mcp-server/src/__tests__/evals/recall-latency.eval.test.ts</files>
  <read_first>
    - packages/mcp-server/src/__tests__/evals/recall-ranking.eval.test.ts (harness imports + corpus loading + EVAL_WORKSPACE_ID)
    - packages/mcp-server/src/tools.ts (the integrated recall() path from plan 03-03 — the path being timed)
    - scripts/eval-budget-summary.mjs (the --*-p99 timing/percentile pattern established for CON-07 in Phase 2 plan 02-09 — reuse the percentile computation shape)
    - .planning/phases/03-query-expansion-reranker/03-RESEARCH.md §Pitfall 3 lines 323-327 (latency stacking + the 0.65→0.70 lever) + §A2 (estimates are planning-only; the assertion measures real latency)
  </read_first>
  <action>
    Create `recall-latency.eval.test.ts` that exercises the integrated recall() path (expansion ON) against a corpus subset chosen so the run stays within MAX_AI_CALLS=200, timing each end-to-end recall (embed → adaptive gate → fan-out/RRF → reranker → hybridRank → envelope). Collect per-query wall-clock durations, compute p50 and p99 with the same percentile method used in `scripts/eval-budget-summary.mjs --*-p99` (CON-07), and assert `p50 ≤ 1800` ms and `p99 ≤ 3000` ms (EXP-11). To make the budget meaningful, bias the subset toward queries that trigger fan-out (top1 < 0.65) so the measured latency reflects the expensive path, and log the fan-out fire rate. Add a comment that if the assertion fails the prescribed lever is tightening `ADAPTIVE_TOP1_THRESHOLD` 0.65 → 0.70 (raising the gate so fewer queries fan out), NOT removing the feature (ROADMAP QE-5 note). Note in a header comment that the production-latency confirmation is a manual step (03-VALIDATION Manual-Only section): inspect the Analytics Engine `recall` latency blob against the deployed Worker. Use the build-time JSON corpus import `{ type: "json" }`.
  </action>
  <verify>
    <automated>cd packages/mcp-server && npx tsc --noEmit; grep -c "1800\|p50\|p99\|3000" src/__tests__/evals/recall-latency.eval.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `recall-latency.eval.test.ts` exists and TypeScript-compiles: `cd packages/mcp-server && npx tsc --noEmit` exits 0.
    - The p50 ≤ 1800ms and p99 ≤ 3000ms assertions are present: `grep -c "1800" ...` ≥1 AND `grep -c "3000" ...` ≥1.
    - p50/p99 are computed with a percentile method (mirrors eval-budget-summary --*-p99); the fan-out fire rate is logged.
    - A comment documents the 0.65→0.70 adaptive-threshold lever as the over-budget remedy and the manual Analytics-Engine production confirmation.
    - The query subset is bounded to ≤200 AI/Vectorize calls.
  </acceptance_criteria>
  <done>The latency eval is authored and type-checks: it times the expansion-ON recall path, computes p50/p99 with the CON-07 percentile method, and asserts p50 ≤ 1.8s / p99 ≤ 3s with the QE-5 lever documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the query-expansion + latency evals under live creds (separate session from EXP-07) — EXP-08/09/11/12</name>
  <files>(no files modified — live eval runs + result capture only)</files>
  <action>Run `query-expansion-recall.eval.test.ts` (A/B + anti-HyDE + entity-preservation) and `recall-latency.eval.test.ts` (p50/p99) under Cloudflare creds, each in its OWN session and separate from the EXP-07 ablation (MAX_AI_CALLS=200), read the logged recall@5/entity/anti-HyDE/p50/p99 results, then pause for human approval per `<resume-signal>`. This is a checkpoint — Claude runs the steps, then waits.</action>
  <what-built>The `query-expansion-recall.eval.test.ts` (A/B recall@5 + anti-HyDE + entity-preservation) and `recall-latency.eval.test.ts` (p50/p99) evals from Tasks 1-2.</what-built>
  <how-to-verify>
    1. Ensure Cloudflare creds: `wrangler login` (local) or export `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID`.
    2. Run the A/B eval IN ITS OWN SESSION, separate from the EXP-07 reranker ablation (plan 03-04) — the two together exceed MAX_AI_CALLS=200: `cd packages/mcp-server && npm run test:eval -- query-expansion-recall`.
    3. Read the logged output: `recall@5(Scout)=… recall@5(3.2-3b)=… promotable=… entityPreservation=… (gate >0.80)` and the anti-HyDE assertion result.
    4. Run the latency eval (its own session is fine): `npm run test:eval -- recall-latency`; confirm `p50 ≤ 1800ms`, `p99 ≤ 3000ms`, and read the fan-out fire rate.
    5. If latency is over budget, do NOT remove the feature — raise `ADAPTIVE_TOP1_THRESHOLD` 0.65 → 0.70 and re-run (QE-5 lever).
    6. Confirm no MAX_AI_CALLS overrun throw in either session.
  </how-to-verify>
  <verify>
    <human-check>Both evals ran under creds in separate sessions; recall@5(Scout/3.2-3b), promotable verdict, entityPreservation rate, anti-HyDE pass/fail, and p50/p99 + fan-out rate are recorded; no MAX_AI_CALLS overrun.</human-check>
  </verify>
  <done>The A/B and latency evals have been run under live creds (or explicitly deferred to nightly CI), with recall@5/entity/anti-HyDE/p50/p99 results captured and the QE-5 lever applied if latency was over budget.</done>
  <resume-signal>Reply with recall@5(Scout/3.2-3b), the promotable verdict, entityPreservation rate, anti-HyDE pass/fail, and p50/p99 + fan-out rate; or "creds unavailable — defer to nightly CI" to record the evals as authored-but-unrun.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → live Workers AI + Vectorize | A/B + latency evals run against real bindings under eval creds; EVAL_WORKSPACE_ID="eval-fixtures" isolates from production. |
| user query (corpus) → expansion model | Corpus queries are sent to Scout and the challenger; output is gated and used only as search variants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Denial of Service | A/B eval doubling expansion calls → budget overrun | mitigate | MAX_AI_CALLS=200 counter throws on overrun; the A/B subset is capped and runs in its own session, separate from EXP-07 (A4). |
| T-03-12 | Tampering | HyDE/fabrication regression slipping past the prompt-string unit test | mitigate | EXP-09 behavioral anti-HyDE assertion here checks actual variant output, complementing the 03-02 prompt-string check. |
| T-03-13 | Information Disclosure | eval querying a production workspace | mitigate | EVAL_WORKSPACE_ID="eval-fixtures"; vectorizeQuery positional workspaceId isolation. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs (RESEARCH §Package Legitimacy Audit). No slopcheck surface. |
</threat_model>

<verification>
- `cd packages/mcp-server && npx tsc --noEmit` — both eval files type-check.
- Live runs (creds-gated, Task 3): A/B + latency in SEPARATE sessions from EXP-07; recall@5 A/B + entity-preservation >0.80 + anti-HyDE pass; p50 ≤ 1.8s / p99 ≤ 3s; no budget overrun.
- `git diff shared/ai-config/src/index.ts` — EXPANSION_CHALLENGER_MODEL added; QUERY_EXPANSION_MODEL alias unchanged.
</verification>

<success_criteria>
- EXP-08: Scout-vs-3.2-3b recall@5 A/B with the 5pp promotion verdict logged; Scout stays default; EXPANSION_CHALLENGER_MODEL is eval-only.
- EXP-09: behavioral anti-HyDE assertion on actual variants.
- EXP-11: recall p50 ≤ 1.8s, p99 ≤ 3s with expansion ON; 0.65→0.70 lever documented.
- EXP-12: >80% named-entity preservation across the corpus.
- EXP-07 and EXP-08 evals run in separate vitest sessions (MAX_AI_CALLS=200).
</success_criteria>

<output>
Create `.planning/phases/03-query-expansion-reranker/03-05-SUMMARY.md` when done.
</output>
