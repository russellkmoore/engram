/**
 * @engram/schema — system memory type definitions for the Engram monorepo.
 *
 * Exports the `SYSTEM_TYPES` array consumed by Phase 2's `WorkspaceDO` seed
 * migration and the supporting type definitions (`FieldType`, `FieldDefinition`,
 * `SystemMemoryType`).
 *
 * @module @engram/schema
 */

export * from "./system-types.js";
